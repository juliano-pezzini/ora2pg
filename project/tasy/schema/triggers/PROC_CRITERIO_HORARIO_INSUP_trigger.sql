-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS proc_criterio_horario_insup ON proc_criterio_horario CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_proc_criterio_horario_insup() RETURNS trigger AS $BODY$
Declare

ie_origem_proced_calculo_w	bigint;

BEGIN
if (NEW.cd_procedimento_calculo is not null) and (NEW.ie_origem_proced_calculo is null) then
	--r.aise_application_error(-20011,'O procedimento deve ser localizado');
	CALL wheb_mensagem_pck.exibir_mensagem_abort(263421);

end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_proc_criterio_horario_insup() FROM PUBLIC;

CREATE TRIGGER proc_criterio_horario_insup
	AFTER INSERT OR UPDATE ON proc_criterio_horario FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_proc_criterio_horario_insup();

