-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS pfcs_algorithm_bf_upsert ON pfcs_algorithm CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_pfcs_algorithm_bf_upsert() RETURNS trigger AS $BODY$
DECLARE
ie_exists_w varchar(1) := 'N';

PRAGMA AUTONOMOUS_TRANSACTION;
BEGIN
    if (coalesce(wheb_usuario_pck.get_ie_executar_trigger, 'S') = 'S')  then
        select coalesce(max('Y'),'N')
        into STRICT ie_exists_w
        from pfcs_algorithm
        where cd_algorithm = NEW.cd_algorithm;

        if (TG_OP = 'UPDATE' and NEW.cd_algorithm = OLD.cd_algorithm) then
            ie_exists_w := 'N';
        end if;

        if (ie_exists_w = 'Y') then
            CALL wheb_mensagem_pck.exibir_mensagem_abort(1183490);
        end if;
    end if;
RETURN NEW;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_pfcs_algorithm_bf_upsert() FROM PUBLIC;

CREATE TRIGGER pfcs_algorithm_bf_upsert
	BEFORE INSERT OR UPDATE ON pfcs_algorithm FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_pfcs_algorithm_bf_upsert();

