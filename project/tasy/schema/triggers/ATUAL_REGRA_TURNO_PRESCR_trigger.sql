-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS atual_regra_turno_prescr ON regra_turno_prescr CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_atual_regra_turno_prescr() RETURNS trigger AS $BODY$
declare

BEGIN

if (NEW.hr_prim_turno is not null) and
   ((NEW.hr_prim_turno <> OLD.hr_prim_turno) or (OLD.dt_prim_turno is null)) then
	NEW.dt_prim_turno := to_date(to_char(LOCALTIMESTAMP,'dd/mm/yyyy') || ' ' || NEW.hr_prim_turno || ':00', 'dd/mm/yyyy hh24:mi:ss');
end if;

if (NEW.hr_seg_turno is not null) and
   ((NEW.hr_seg_turno <> OLD.hr_seg_turno) or (OLD.dt_seg_turno is null)) then
	NEW.dt_seg_turno := to_date(to_char(LOCALTIMESTAMP,'dd/mm/yyyy') || ' ' || NEW.hr_seg_turno || ':00', 'dd/mm/yyyy hh24:mi:ss');
end if;

if (NEW.hr_ter_turno is not null) and
   ((NEW.hr_ter_turno <> OLD.hr_ter_turno) or (OLD.dt_ter_turno is null)) then
	NEW.dt_ter_turno := to_date(to_char(LOCALTIMESTAMP,'dd/mm/yyyy') || ' ' || NEW.hr_ter_turno || ':00', 'dd/mm/yyyy hh24:mi:ss');
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_atual_regra_turno_prescr() FROM PUBLIC;

CREATE TRIGGER atual_regra_turno_prescr
	BEFORE INSERT OR UPDATE ON regra_turno_prescr FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_atual_regra_turno_prescr();

