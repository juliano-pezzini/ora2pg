-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS dominio_tp ON dominio CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_dominio_tp() RETURNS trigger AS $BODY$
DECLARE nr_seq_w bigint;ds_s_w   varchar(1);ds_c_w   varchar(1);ds_w	   varchar(1);ie_log_w varchar(1);BEGIN BEGIN ds_s_w := to_char(OLD.CD_DOMINIO);  ds_c_w:=null;SELECT * FROM gravar_log_alteracao(substr(OLD.CD_DOMINIO,1,4000), substr(NEW.CD_DOMINIO,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_DOMINIO', ie_log_w, ds_w, 'DOMINIO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.NM_DOMINIO,1,4000), substr(NEW.NM_DOMINIO,1,4000), NEW.nm_usuario, nr_seq_w, 'NM_DOMINIO', ie_log_w, ds_w, 'DOMINIO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.DS_DOMINIO,1,4000), substr(NEW.DS_DOMINIO,1,4000), NEW.nm_usuario, nr_seq_w, 'DS_DOMINIO', ie_log_w, ds_w, 'DOMINIO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_EXP_DOMINIO,1,4000), substr(NEW.CD_EXP_DOMINIO,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_EXP_DOMINIO', ie_log_w, ds_w, 'DOMINIO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_IMPLANTAR,1,4000), substr(NEW.IE_IMPLANTAR,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_IMPLANTAR', ie_log_w, ds_w, 'DOMINIO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.NR_SEQ_MODULO,1,4000), substr(NEW.NR_SEQ_MODULO,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_SEQ_MODULO', ie_log_w, ds_w, 'DOMINIO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_EXP_DESC_DOMINIO,1,4000), substr(NEW.CD_EXP_DESC_DOMINIO,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_EXP_DESC_DOMINIO', ie_log_w, ds_w, 'DOMINIO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_ALTERAR,1,4000), substr(NEW.IE_ALTERAR,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_ALTERAR', ie_log_w, ds_w, 'DOMINIO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;   exception when others then ds_w:= '1'; end;   END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_dominio_tp() FROM PUBLIC;

CREATE TRIGGER dominio_tp
	AFTER UPDATE ON dominio FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_dominio_tp();

