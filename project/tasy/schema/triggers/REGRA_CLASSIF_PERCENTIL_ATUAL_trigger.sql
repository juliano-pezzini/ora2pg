-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS regra_classif_percentil_atual ON regra_classif_percentil CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_regra_classif_percentil_atual() RETURNS trigger AS $BODY$
DECLARE

BEGIN

If (NEW.ie_referencia in ('F','F3')) and (coalesce(NEW.QT_IDADE_MAX,75) > 74) Then
	
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(454747);
	
elsif (NEW.ie_referencia = 'P') and (coalesce(NEW.QT_IDADE_MIN,0) < 65) then

	CALL Wheb_mensagem_pck.exibir_mensagem_abort(454748);	

elsif (NEW.ie_referencia = 'F2') and (coalesce(NEW.QT_IDADE_MAX,91) > 90) then

	CALL Wheb_mensagem_pck.exibir_mensagem_abort(1053219);		
		
End if;

RETURN NEW;
END;
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_regra_classif_percentil_atual() FROM PUBLIC;

CREATE TRIGGER regra_classif_percentil_atual
	BEFORE INSERT OR UPDATE ON regra_classif_percentil FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_regra_classif_percentil_atual();

