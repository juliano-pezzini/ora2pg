-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS banco_estabelecimento_atual ON banco_estabelecimento CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_banco_estabelecimento_atual() RETURNS trigger AS $BODY$
declare

cd_estabelecimento_w	smallint;
cd_empresa_w			smallint;

ds_conta_contabil_w		varchar(40);

BEGIN

if (wheb_usuario_pck.get_ie_executar_trigger = 'S') then
ds_conta_contabil_w		:= substr(wheb_mensagem_pck.get_texto(354087,''),1,40);

CALL ctb_consistir_conta_titulo(NEW.cd_conta_contabil, ds_conta_contabil_w);

ctb_consistir_conta_estab(NEW.cd_estabelecimento, NEW.cd_conta_contabil, ds_conta_contabil_w);

end if;
RETURN NEW;
end;
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_banco_estabelecimento_atual() FROM PUBLIC;

CREATE TRIGGER banco_estabelecimento_atual
	BEFORE INSERT OR UPDATE ON banco_estabelecimento FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_banco_estabelecimento_atual();

