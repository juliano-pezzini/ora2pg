-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS proced_autor_cirurgia_insert ON proced_autor_cirurgia CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_proced_autor_cirurgia_insert() RETURNS trigger AS $BODY$
declare

nr_sequencia_autor_w	bigint;

BEGIN

select	max(nr_sequencia)
into STRICT	nr_sequencia_autor_w
from	autorizacao_convenio
where	nr_seq_autor_cirurgia	= NEW.nr_seq_autorizacao;

if (nr_sequencia_autor_w is not null) then

	insert	into	procedimento_autorizado(nr_sequencia,
		nm_usuario,
		dt_atualizacao,
		nr_sequencia_autor,
		cd_procedimento,
		ie_origem_proced,
		qt_solicitada,
		qt_autorizada)
	values (nextval('procedimento_autorizado_seq'),
		NEW.nm_usuario,
		NEW.dt_atualizacao,
		nr_sequencia_autor_w,
		NEW.cd_procedimento,
		NEW.ie_origem_proced,
		NEW.qt_procedimento,
		0);
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_proced_autor_cirurgia_insert() FROM PUBLIC;

CREATE TRIGGER proced_autor_cirurgia_insert
	AFTER INSERT ON proced_autor_cirurgia FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_proced_autor_cirurgia_insert();

