-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS cpoe_anatomia_patolog_insert ON cpoe_anatomia_patologica CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_cpoe_anatomia_patolog_insert() RETURNS trigger AS $BODY$
DECLARE
BEGIN
NEW.ds_stack	:= substr(dbms_utility.format_call_stack,1,2000);

if (NEW.cd_perfil_ativo is null) then
	NEW.cd_perfil_ativo := obter_perfil_ativo;
end if;

if (NEW.dt_fim is null) and (NEW.dt_inicio is not null) then
	NEW.dt_fim :=  NEW.dt_inicio +1-1/(24*60*60);
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_cpoe_anatomia_patolog_insert() FROM PUBLIC;

CREATE TRIGGER cpoe_anatomia_patolog_insert
	BEFORE INSERT ON cpoe_anatomia_patologica FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_cpoe_anatomia_patolog_insert();

