-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS pls_sib_reenvio_atrib_atual ON pls_sib_reenvio_atrib CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_pls_sib_reenvio_atrib_atual() RETURNS trigger AS $BODY$
declare

BEGIN

if (NEW.dt_cancelamento is not null or NEW.nr_seq_motivo_cancelamento is not null) and (NEW.cd_atributo not in (31,32)) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(1146441); --Data e motivo de cancelamento nao podem ser informadas para o atributo selecionado
elsif (NEW.dt_reativacao is not null) and (NEW.cd_atributo <> 20) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(1146442); --Data de reativacao nao pode ser informada para o atributo selecionado
elsif (NEW.cd_beneficiario is not null) and (NEW.cd_atributo <> 8) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(1146530); --Codigo do beneficiario nao pode ser informado para o atributo selecionado
elsif (NEW.nr_plano_ans is not null) and (NEW.cd_atributo <> 9) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(1146533); --Protocolo ANS nao pode ser informado para o atributo selecionado
elsif (NEW.nr_plano_operadora is not null) and (NEW.cd_atributo <> 10) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(1146534); --Codigo SCPA nao pode ser informado para o atributo selecionado
elsif (NEW.dt_cancelamento is not null and NEW.nr_seq_motivo_cancelamento is null) or (NEW.dt_cancelamento is null and NEW.nr_seq_motivo_cancelamento is not null) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(1146443); --E necessario informar data e motivo de cancelamento, ao preencher uma das informacoes
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_pls_sib_reenvio_atrib_atual() FROM PUBLIC;

CREATE TRIGGER pls_sib_reenvio_atrib_atual
	BEFORE INSERT OR UPDATE ON pls_sib_reenvio_atrib FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_pls_sib_reenvio_atrib_atual();

