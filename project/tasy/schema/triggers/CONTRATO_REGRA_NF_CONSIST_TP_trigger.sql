-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS contrato_regra_nf_consist_tp ON contrato_regra_nf_consist CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_contrato_regra_nf_consist_tp() RETURNS trigger AS $BODY$
DECLARE nr_seq_w bigint;ds_s_w   varchar(1);ds_c_w   varchar(1);ds_w	   varchar(1);ie_log_w varchar(1);BEGIN BEGIN ds_s_w := to_char(OLD.NR_SEQUENCIA);  ds_c_w:=null;SELECT * FROM gravar_log_alteracao(substr(OLD.IE_JUSTIFICATIVA,1,4000), substr(NEW.IE_JUSTIFICATIVA,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_JUSTIFICATIVA', ie_log_w, ds_w, 'CONTRATO_REGRA_NF_CONSIST', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.VL_MAXIMO,1,4000), substr(NEW.VL_MAXIMO,1,4000), NEW.nm_usuario, nr_seq_w, 'VL_MAXIMO', ie_log_w, ds_w, 'CONTRATO_REGRA_NF_CONSIST', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.VL_MINIMO,1,4000), substr(NEW.VL_MINIMO,1,4000), NEW.nm_usuario, nr_seq_w, 'VL_MINIMO', ie_log_w, ds_w, 'CONTRATO_REGRA_NF_CONSIST', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_TIPO_CONSISTENCIA,1,4000), substr(NEW.IE_TIPO_CONSISTENCIA,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_TIPO_CONSISTENCIA', ie_log_w, ds_w, 'CONTRATO_REGRA_NF_CONSIST', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;   exception when others then ds_w:= '1'; end;   END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_contrato_regra_nf_consist_tp() FROM PUBLIC;

CREATE TRIGGER contrato_regra_nf_consist_tp
	AFTER UPDATE ON contrato_regra_nf_consist FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_contrato_regra_nf_consist_tp();

