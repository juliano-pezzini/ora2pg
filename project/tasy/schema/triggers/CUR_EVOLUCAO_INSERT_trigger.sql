-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS cur_evolucao_insert ON cur_evolucao CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_cur_evolucao_insert() RETURNS trigger AS $BODY$
DECLARE
 
ie_lacto_w		varchar(1);
BEGIN
  BEGIN 
 
if (NEW.dt_evolucao > LOCALTIMESTAMP) and (NEW.nr_atendimento is not null) then 
	--A data da evolução não pode ser maior que a data atual.#@#@'); 
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(262188);
end if;
 
BEGIN 
CALL gerar_lancamento_automatico(NEW.nr_atendimento, null, 545, NEW.nm_usuario, NEW.nr_sequencia, NEW.dt_evolucao, NEW.ie_evolucao_clinica, null, null, null);
exception 
	when others then 
	ie_lacto_w:= 'N';
end;
 
 
  END;
RETURN NEW;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_cur_evolucao_insert() FROM PUBLIC;

CREATE TRIGGER cur_evolucao_insert
	AFTER INSERT ON cur_evolucao FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_cur_evolucao_insert();

