-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS cpoe_procedimento_atual ON cpoe_procedimento CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_cpoe_procedimento_atual() RETURNS trigger AS $BODY$
declare
ds_erro_w	varchar(2000);
BEGIN
  BEGIN
BEGIN
    if (NEW.hr_prim_horario is not null) and ((NEW.hr_prim_horario <> OLD.hr_prim_horario) or (OLD.dt_prim_horario is null)) then
		NEW.dt_prim_horario := to_date(to_char(LOCALTIMESTAMP,'dd/mm/yyyy') || ' ' || NEW.hr_prim_horario,'dd/mm/yyyy hh24:mi');
	end if;	
    if (NEW.hr_prim_hor_proc1 is not null) and ((NEW.hr_prim_hor_proc1 <> OLD.hr_prim_hor_proc1) or (OLD.dt_prim_hor_proc1 is null)) then	
		NEW.dt_prim_hor_proc1 := to_date(to_char(LOCALTIMESTAMP,'dd/mm/yyyy') || ' ' || NEW.hr_prim_hor_proc1,'dd/mm/yyyy hh24:mi');
	end if;	
    if (NEW.hr_prim_hor_proc2 is not null) and ((NEW.hr_prim_hor_proc2 <> OLD.hr_prim_hor_proc2) or (OLD.dt_prim_hor_proc2 is null)) then	
		NEW.dt_prim_hor_proc2 := to_date(to_char(LOCALTIMESTAMP,'dd/mm/yyyy') || ' ' || NEW.hr_prim_hor_proc2,'dd/mm/yyyy hh24:mi');
	end if;		
    if (NEW.hr_prim_hor_proc3 is not null) and ((NEW.hr_prim_hor_proc3 <> OLD.hr_prim_hor_proc3) or (OLD.dt_prim_hor_proc3 is null)) then	
		NEW.dt_prim_hor_proc3 := to_date(to_char(LOCALTIMESTAMP,'dd/mm/yyyy') || ' ' || NEW.hr_prim_hor_proc3,'dd/mm/yyyy hh24:mi');
	end if;		
    if (NEW.hr_prim_hor_proc4 is not null) and ((NEW.hr_prim_hor_proc4 <> OLD.hr_prim_hor_proc4) or (OLD.dt_prim_hor_proc4 is null)) then	
		NEW.dt_prim_hor_proc4 := to_date(to_char(LOCALTIMESTAMP,'dd/mm/yyyy') || ' ' || NEW.hr_prim_hor_proc4,'dd/mm/yyyy hh24:mi');
	end if;		
    if (NEW.hr_prim_hor_proc5 is not null) and ((NEW.hr_prim_hor_proc5 <> OLD.hr_prim_hor_proc5) or (OLD.dt_prim_hor_proc5 is null)) then	
		NEW.dt_prim_hor_proc5 := to_date(to_char(LOCALTIMESTAMP,'dd/mm/yyyy') || ' ' || NEW.hr_prim_hor_proc5,'dd/mm/yyyy hh24:mi');
	end if;		
exception
	when others then
	null;
end;

BEGIN
	if (NEW.NR_SEQ_PROC_INTERNO is null) then
		ds_erro_w	:= substr(dbms_utility.format_call_stack,1,2000);

		CALL gravar_log_tasy(10007,
				'ERROR CPOE_PROCEDIMENTO NR_SEQ_PROC_INTERNO NULO : ' || ds_erro_w ||
				' Seq: ' || NEW.nr_sequencia ||
				' NEW nr_seq_proc_interno:'|| NEW.NR_SEQ_PROC_INTERNO ||
				' OLD nr_seq_proc_interno:'|| OLD.NR_SEQ_PROC_INTERNO,
				NEW.nm_usuario);
	end if;		
exception
	when others then
	null;
end;

  END;
RETURN NEW;
end;
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_cpoe_procedimento_atual() FROM PUBLIC;

CREATE TRIGGER cpoe_procedimento_atual
	BEFORE INSERT OR UPDATE ON cpoe_procedimento FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_cpoe_procedimento_atual();

