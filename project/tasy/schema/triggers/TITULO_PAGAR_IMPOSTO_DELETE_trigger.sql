-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS titulo_pagar_imposto_delete ON titulo_pagar_imposto CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_titulo_pagar_imposto_delete() RETURNS trigger AS $BODY$
declare
qt_registro_w	integer;
BEGIN

if (wheb_usuario_pck.get_ie_executar_trigger = 'S') then
select	count(1)
into STRICT	qt_registro_w
from	pls_pp_lr_base_trib
where	nr_seq_vl_tit_trib = OLD.nr_sequencia;

if (qt_registro_w > 0) then
	-- Existem registros dependentes

	CALL wheb_mensagem_pck.exibir_mensagem_abort(457543);
end if;

select	count(1)
into STRICT	qt_registro_w
from	pls_pp_base_acum_trib
where	nr_seq_vl_tit_trib = OLD.nr_sequencia;

if (qt_registro_w > 0) then
	-- Existem registros dependentes

	CALL wheb_mensagem_pck.exibir_mensagem_abort(457543);
end if;
end if;
RETURN OLD;
END;
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_titulo_pagar_imposto_delete() FROM PUBLIC;

CREATE TRIGGER titulo_pagar_imposto_delete
	BEFORE DELETE ON titulo_pagar_imposto FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_titulo_pagar_imposto_delete();

