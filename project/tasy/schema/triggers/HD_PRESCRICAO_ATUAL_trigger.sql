-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS hd_prescricao_atual ON hd_prescricao CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_hd_prescricao_atual() RETURNS trigger AS $BODY$
declare


BEGIN
if (coalesce(OLD.DT_ATUALIZACAO_NREC,LOCALTIMESTAMP + interval '10 days') <> NEW.DT_ATUALIZACAO_NREC) and (NEW.DT_ATUALIZACAO_NREC is not null) then
	NEW.ds_utc				:= obter_data_utc(NEW.DT_ATUALIZACAO_NREC, 'HV');
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_hd_prescricao_atual() FROM PUBLIC;

CREATE TRIGGER hd_prescricao_atual
	BEFORE INSERT OR UPDATE ON hd_prescricao FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_hd_prescricao_atual();

