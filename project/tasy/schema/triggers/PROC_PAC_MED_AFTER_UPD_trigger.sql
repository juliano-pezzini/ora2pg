-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS proc_pac_med_after_upd ON procedimento_pac_medico CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_proc_pac_med_after_upd() RETURNS trigger AS $BODY$
BEGIN
    IF (coalesce(wheb_usuario_pck.get_ie_executar_trigger, 'S') = 'S') THEN
        UPDATE SCORING_DATA
        SET DT_ATUALIZACAO = LOCALTIMESTAMP,
            NM_USUARIO = WHEB_USUARIO_PCK.GET_NM_USUARIO,
            IE_SITUACAO = 'I'
        WHERE NR_CASE = OLD.NR_SEQ_EPISODIO;
    END IF;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_proc_pac_med_after_upd() FROM PUBLIC;

CREATE TRIGGER proc_pac_med_after_upd
	AFTER UPDATE ON procedimento_pac_medico FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_proc_pac_med_after_upd();

