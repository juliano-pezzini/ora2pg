-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS smart_material ON prescr_material CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_smart_material() RETURNS trigger AS $BODY$
declare
	reg_integracao_w		gerar_int_padrao.reg_integracao;
	qt_medicamento_w        bigint;
BEGIN
  BEGIN
BEGIN
	if (OLD.DT_SUSPENSAO is null)  	and (NEW.DT_SUSPENSAO is not null) and (OBTER_SE_MAT_ANTIBIOTICO(NEW.CD_MATERIAL) = 'S')
	then
		BEGIN
			reg_integracao_w := gerar_int_padrao.gravar_integracao('305', NEW.NR_PRESCRICAO, NEW.nm_usuario, reg_integracao_w, 'NR_PRESCRICAO=' || NEW.NR_PRESCRICAO || ';NR_SEQUENCIA=' || NEW.NR_SEQUENCIA || ';DS_OPERACAO=UPDATE');
		END;	
	end if;
exception when others then
	null;
end;

  END;
RETURN NEW;
END;
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_smart_material() FROM PUBLIC;

CREATE TRIGGER smart_material
	AFTER UPDATE ON prescr_material FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_smart_material();

