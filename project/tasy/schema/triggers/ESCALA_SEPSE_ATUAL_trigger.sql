-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS escala_sepse_atual ON escala_sepse CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_escala_sepse_atual() RETURNS trigger AS $BODY$
declare
    nr_seq_evento_w		bigint;

BEGIN

if (TG_OP = 'INSERT') then
  NEW.ie_padrao_variavel := obter_padrao_deflagador_sepse;
end if;

if (OLD.dt_liberacao_status is null) and (NEW.dt_liberacao_status is not null) and (NEW.ie_status_sepsis in ('SC','RC')) then
	nr_seq_evento_w := gerar_evento_sepse(NEW.nr_atendimento, nr_seq_evento_w, NEW.nr_sequencia, NEW.nm_usuario_status);

	NEW.nr_seq_evento := nr_seq_evento_w;
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_escala_sepse_atual() FROM PUBLIC;

CREATE TRIGGER escala_sepse_atual
	BEFORE UPDATE ON escala_sepse FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_escala_sepse_atual();

