-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS lista_problema_pac_delete ON lista_problema_pac CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_lista_problema_pac_delete() RETURNS trigger AS $BODY$
declare

qt_reg_w		bigint;
pragma autonomous_transaction;
BEGIN

if (wheb_usuario_pck.get_ie_executar_trigger	= 'N')  then
	goto Final;
end if;

if ( OLD.nr_sequencia is not null) then

	select	count(1)
	into STRICT	qt_reg_w
	from	LISTA_PROBLEMA_PAC_ITEM
	where	nr_seq_problema	= OLD.nr_sequencia;

	if (qt_reg_w > 0) then

		delete from LISTA_PROBLEMA_PAC_ITEM
		where  nr_seq_problema	= OLD.nr_sequencia;

		commit;

	end if;

	select	count(1)
	into STRICT	qt_reg_w
	from	lista_problema_status
	where	nr_seq_problema	= OLD.nr_sequencia;

	if (qt_reg_w > 0) then

		delete from lista_problema_status
		where  nr_seq_problema	= OLD.nr_sequencia;

		commit;

	end if;

	select	count(1)
	into STRICT	qt_reg_w
	from	lista_problema_pac_incid
	where	nr_seq_problema	= OLD.nr_sequencia;

	if (qt_reg_w > 0) then

		delete from lista_problema_pac_incid
		where  nr_seq_problema	= OLD.nr_sequencia;

		commit;

	end if;


	select	count(1)
	into STRICT	qt_reg_w
	from	prescr_item_sintoma
	where	nr_seq_problema	= OLD.nr_sequencia;

	if (qt_reg_w > 0) then

		delete from prescr_item_sintoma
		where  nr_seq_problema	= OLD.nr_sequencia;

		commit;

	end if;

	select	count(1)
	into STRICT	qt_reg_w
	from	soap_item_sintoma
	where	nr_seq_problema	= OLD.nr_sequencia;

	if (qt_reg_w > 0) then

		delete from SOAP_ITEM_SINTOMA
		where  nr_seq_problema	= OLD.nr_sequencia;

		commit;

	end if;

	select	count(1)
	into STRICT	qt_reg_w
	from	lista_problema_pac_prog
	where	nr_seq_problema	= OLD.nr_sequencia;

	if (qt_reg_w > 0) then

		delete from lista_problema_pac_prog
		where  nr_seq_problema	= OLD.nr_sequencia;

		commit;

	end if;


end if;

<<Final>>
qt_reg_w	:= 0;

RETURN OLD;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_lista_problema_pac_delete() FROM PUBLIC;

CREATE TRIGGER lista_problema_pac_delete
	BEFORE DELETE ON lista_problema_pac FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_lista_problema_pac_delete();

