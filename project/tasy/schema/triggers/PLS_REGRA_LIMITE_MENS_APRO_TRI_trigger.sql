-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS pls_regra_limite_mens_apro_tri ON pls_regra_limite_mens_apro CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_pls_regra_limite_mens_apro_tri() RETURNS trigger AS $BODY$
declare

qt_registros_w		bigint	:= 0;

pragma autonomous_transaction;

BEGIN

select	count(1)
into STRICT	qt_registros_w
from	pls_regra_limite_mens_apro
where	nr_seq_centro_apropriacao = NEW.nr_seq_centro_apropriacao
and	nr_seq_regra = NEW.nr_seq_regra
and	nr_sequencia <> NEW.nr_sequencia;

if (qt_registros_w > 0) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(1031431);
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_pls_regra_limite_mens_apro_tri() FROM PUBLIC;

CREATE TRIGGER pls_regra_limite_mens_apro_tri
	AFTER INSERT OR UPDATE ON pls_regra_limite_mens_apro FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_pls_regra_limite_mens_apro_tri();

