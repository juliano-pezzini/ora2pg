-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS ptu_regra_venc_fatura_atual ON ptu_regra_venc_fatura CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_ptu_regra_venc_fatura_atual() RETURNS trigger AS $BODY$
declare

BEGIN
if (NEW.nr_seq_congenere is null) and (NEW.nr_seq_camara is null) then
	-- Deve ser informada ao menos uma cooperativa ou uma câmara.
	CALL wheb_mensagem_pck.exibir_mensagem_abort(267059);
end if;

if (NEW.nr_seq_congenere is not null) and (NEW.nr_seq_camara is not null) then
	-- Deve ser uma cooperativa ou uma câmara.
	CALL wheb_mensagem_pck.exibir_mensagem_abort(267060);
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_ptu_regra_venc_fatura_atual() FROM PUBLIC;

CREATE TRIGGER ptu_regra_venc_fatura_atual
	BEFORE INSERT OR UPDATE ON ptu_regra_venc_fatura FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_ptu_regra_venc_fatura_atual();

