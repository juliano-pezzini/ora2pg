-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS regra_fornecedor_compras_tp ON regra_fornecedor_compras CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_regra_fornecedor_compras_tp() RETURNS trigger AS $BODY$
DECLARE nr_seq_w bigint;ds_s_w   varchar(1);ds_c_w   varchar(1);ds_w	   varchar(1);ie_log_w varchar(1);BEGIN BEGIN ds_s_w := to_char(OLD.NR_SEQUENCIA);  ds_c_w:=null;SELECT * FROM gravar_log_alteracao(substr(OLD.CD_ESTABELECIMENTO,1,4000), substr(NEW.CD_ESTABELECIMENTO,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_ESTABELECIMENTO', ie_log_w, ds_w, 'REGRA_FORNECEDOR_COMPRAS', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_PERMITE_INFORMAR,1,4000), substr(NEW.IE_PERMITE_INFORMAR,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_PERMITE_INFORMAR', ie_log_w, ds_w, 'REGRA_FORNECEDOR_COMPRAS', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_CGC_FORNECEDOR,1,4000), substr(NEW.CD_CGC_FORNECEDOR,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_CGC_FORNECEDOR', ie_log_w, ds_w, 'REGRA_FORNECEDOR_COMPRAS', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;   exception when others then ds_w:= '1'; end;   END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_regra_fornecedor_compras_tp() FROM PUBLIC;

CREATE TRIGGER regra_fornecedor_compras_tp
	AFTER UPDATE ON regra_fornecedor_compras FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_regra_fornecedor_compras_tp();

