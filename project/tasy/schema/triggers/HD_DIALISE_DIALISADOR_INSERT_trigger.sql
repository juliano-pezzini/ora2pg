-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS hd_dialise_dialisador_insert ON hd_dialise_dialisador CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_hd_dialise_dialisador_insert() RETURNS trigger AS $BODY$
declare

cd_pessoa_fisica_w 		varchar(10);
cd_pessoa_fisica_dialise_w 	varchar(10);

BEGIN

select 	cd_pessoa_fisica
into STRICT	cd_pessoa_fisica_w
from 	hd_dializador
where	nr_sequencia	= NEW.nr_seq_dialisador;

select	cd_pessoa_fisica
into STRICT	cd_pessoa_fisica_dialise_w
from	hd_dialise
where	nr_sequencia = NEW.nr_seq_dialise;


if (cd_pessoa_fisica_w <> cd_pessoa_fisica_dialise_w) then

	CALL Wheb_mensagem_pck.exibir_mensagem_abort(264205);
end if;


RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_hd_dialise_dialisador_insert() FROM PUBLIC;

CREATE TRIGGER hd_dialise_dialisador_insert
	BEFORE INSERT ON hd_dialise_dialisador FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_hd_dialise_dialisador_insert();

