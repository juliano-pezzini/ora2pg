-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS conta_contabil_insert ON conta_contabil CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_conta_contabil_insert() RETURNS trigger AS $BODY$
BEGIN

if (wheb_usuario_pck.GET_IE_EXECUTAR_TRIGGER = 'S') then
	BEGIN
	if (NEW.dt_inicio_vigencia is not null) and (NEW.dt_fim_vigencia is not null) and (NEW.dt_inicio_vigencia > NEW.dt_fim_vigencia) then
		CALL wheb_mensagem_pck.exibir_mensagem_abort(266389);
		/*A data de início, não pode ser maior que a data final da vigência.');*/

	end if;
	end;
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_conta_contabil_insert() FROM PUBLIC;

CREATE TRIGGER conta_contabil_insert
	BEFORE INSERT OR UPDATE ON conta_contabil FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_conta_contabil_insert();

