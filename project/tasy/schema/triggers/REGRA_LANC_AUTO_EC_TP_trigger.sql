-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS regra_lanc_auto_ec_tp ON regra_lanc_auto_ec CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_regra_lanc_auto_ec_tp() RETURNS trigger AS $BODY$
DECLARE nr_seq_w bigint;ds_s_w   varchar(1);ds_c_w   varchar(1);ds_w	   varchar(1);ie_log_w varchar(1);BEGIN BEGIN ds_s_w := to_char(OLD.NR_SEQUENCIA);  ds_c_w:=null;SELECT * FROM gravar_log_alteracao(substr(OLD.IE_SITUACAO,1,4000), substr(NEW.IE_SITUACAO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_SITUACAO', ie_log_w, ds_w, 'REGRA_LANC_AUTO_EC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.DT_CORTE_CR,1,4000), substr(NEW.DT_CORTE_CR,1,4000), NEW.nm_usuario, nr_seq_w, 'DT_CORTE_CR', ie_log_w, ds_w, 'REGRA_LANC_AUTO_EC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_CGC,1,4000), substr(NEW.CD_CGC,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_CGC', ie_log_w, ds_w, 'REGRA_LANC_AUTO_EC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_PESSOA_FISICA,1,4000), substr(NEW.CD_PESSOA_FISICA,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_PESSOA_FISICA', ie_log_w, ds_w, 'REGRA_LANC_AUTO_EC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_FORMA_ENCONTRO_CONTAS,1,4000), substr(NEW.IE_FORMA_ENCONTRO_CONTAS,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_FORMA_ENCONTRO_CONTAS', ie_log_w, ds_w, 'REGRA_LANC_AUTO_EC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_TIPO_PESSOA,1,4000), substr(NEW.IE_TIPO_PESSOA,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_TIPO_PESSOA', ie_log_w, ds_w, 'REGRA_LANC_AUTO_EC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_DT_INI_CORTE_CP,1,4000), substr(NEW.IE_DT_INI_CORTE_CP,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_DT_INI_CORTE_CP', ie_log_w, ds_w, 'REGRA_LANC_AUTO_EC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.DT_INI_CORTE_CP,1,4000), substr(NEW.DT_INI_CORTE_CP,1,4000), NEW.nm_usuario, nr_seq_w, 'DT_INI_CORTE_CP', ie_log_w, ds_w, 'REGRA_LANC_AUTO_EC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_DT_INI_CORTE_CR,1,4000), substr(NEW.IE_DT_INI_CORTE_CR,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_DT_INI_CORTE_CR', ie_log_w, ds_w, 'REGRA_LANC_AUTO_EC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.DT_INI_CORTE_CR,1,4000), substr(NEW.DT_INI_CORTE_CR,1,4000), NEW.nm_usuario, nr_seq_w, 'DT_INI_CORTE_CR', ie_log_w, ds_w, 'REGRA_LANC_AUTO_EC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_TIPO_DATA_CP,1,4000), substr(NEW.IE_TIPO_DATA_CP,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_TIPO_DATA_CP', ie_log_w, ds_w, 'REGRA_LANC_AUTO_EC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_TIPO_DATA_CR,1,4000), substr(NEW.IE_TIPO_DATA_CR,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_TIPO_DATA_CR', ie_log_w, ds_w, 'REGRA_LANC_AUTO_EC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_DT_BASE,1,4000), substr(NEW.IE_DT_BASE,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_DT_BASE', ie_log_w, ds_w, 'REGRA_LANC_AUTO_EC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_DT_CORTE_CP,1,4000), substr(NEW.IE_DT_CORTE_CP,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_DT_CORTE_CP', ie_log_w, ds_w, 'REGRA_LANC_AUTO_EC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.DT_CORTE_CP,1,4000), substr(NEW.DT_CORTE_CP,1,4000), NEW.nm_usuario, nr_seq_w, 'DT_CORTE_CP', ie_log_w, ds_w, 'REGRA_LANC_AUTO_EC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_DT_CORTE_CR,1,4000), substr(NEW.IE_DT_CORTE_CR,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_DT_CORTE_CR', ie_log_w, ds_w, 'REGRA_LANC_AUTO_EC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.DT_BASE,1,4000), substr(NEW.DT_BASE,1,4000), NEW.nm_usuario, nr_seq_w, 'DT_BASE', ie_log_w, ds_w, 'REGRA_LANC_AUTO_EC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;   exception when others then ds_w:= '1'; end;   END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_regra_lanc_auto_ec_tp() FROM PUBLIC;

CREATE TRIGGER regra_lanc_auto_ec_tp
	AFTER UPDATE ON regra_lanc_auto_ec FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_regra_lanc_auto_ec_tp();

