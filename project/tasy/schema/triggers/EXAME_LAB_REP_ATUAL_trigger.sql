-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS exame_lab_rep_atual ON exame_lab_rep CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_exame_lab_rep_atual() RETURNS trigger AS $BODY$
declare
BEGIN
  BEGIN
BEGIN
    if (NEW.hr_prev_execucao is not null) and ((NEW.hr_prev_execucao <> OLD.hr_prev_execucao) or (OLD.dt_prev_execucao is null)) then
		NEW.dt_prev_execucao := to_date(to_char(LOCALTIMESTAMP,'dd/mm/yyyy') || ' ' || to_char(NEW.hr_prev_execucao,'hh24:mi'),'dd/mm/yyyy hh24:mi');
	end if;
exception
	when others then
	null;
end;

  END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_exame_lab_rep_atual() FROM PUBLIC;

CREATE TRIGGER exame_lab_rep_atual
	BEFORE INSERT OR UPDATE ON exame_lab_rep FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_exame_lab_rep_atual();

