-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS pls_proced_vig_insert ON pls_procedimento_vigencia CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_pls_proced_vig_insert() RETURNS trigger AS $BODY$
DECLARE

qt_pendente_w	bigint;

BEGIN

select	count(*)
into STRICT 	qt_pendente_w
from 	pls_procedimento_vigencia
where 	cd_procedimento = NEW.cd_procedimento
and 	ie_origem_proced = NEW.ie_origem_proced
and 	dt_fim_vigencia is null;

if (qt_pendente_w > 0) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(267460);
end if;

RETURN NEW;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_pls_proced_vig_insert() FROM PUBLIC;

CREATE TRIGGER pls_proced_vig_insert
	BEFORE INSERT ON pls_procedimento_vigencia FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_pls_proced_vig_insert();

