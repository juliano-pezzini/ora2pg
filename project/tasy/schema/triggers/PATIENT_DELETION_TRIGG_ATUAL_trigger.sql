-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS patient_deletion_trigg_atual ON patient_deletion_trigger CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_patient_deletion_trigg_atual() RETURNS trigger AS $BODY$
declare
ie_banco_w	patient_deletion_trigger.ie_banco%type;

BEGIN

if (NEW.ie_banco is null) then

		select	max(ie_banco)
		into STRICT	ie_banco_w
		from	objeto_sistema
		where	upper(nm_objeto) = upper(NEW.nm_objeto);

		NEW.ie_banco := ie_banco_w;
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_patient_deletion_trigg_atual() FROM PUBLIC;

CREATE TRIGGER patient_deletion_trigg_atual
	BEFORE INSERT OR UPDATE ON patient_deletion_trigger FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_patient_deletion_trigg_atual();

