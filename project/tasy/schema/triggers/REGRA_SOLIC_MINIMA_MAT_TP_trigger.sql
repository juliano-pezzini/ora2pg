-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS regra_solic_minima_mat_tp ON regra_solic_minima_mat CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_regra_solic_minima_mat_tp() RETURNS trigger AS $BODY$
DECLARE nr_seq_w bigint;ds_s_w   varchar(1);ds_c_w   varchar(1);ds_w	   varchar(1);ie_log_w varchar(1);BEGIN BEGIN ds_s_w := to_char(OLD.NR_SEQUENCIA);  ds_c_w:=null;SELECT * FROM gravar_log_alteracao(substr(OLD.IE_ATEND_REQ_PDA,1,4000), substr(NEW.IE_ATEND_REQ_PDA,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_ATEND_REQ_PDA', ie_log_w, ds_w, 'REGRA_SOLIC_MINIMA_MAT', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_SOLIC_COMPRA_MULT,1,4000), substr(NEW.IE_SOLIC_COMPRA_MULT,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_SOLIC_COMPRA_MULT', ie_log_w, ds_w, 'REGRA_SOLIC_MINIMA_MAT', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_MATERIAL,1,4000), substr(NEW.CD_MATERIAL,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_MATERIAL', ie_log_w, ds_w, 'REGRA_SOLIC_MINIMA_MAT', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_PRESCRICAO_ATEND,1,4000), substr(NEW.IE_PRESCRICAO_ATEND,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_PRESCRICAO_ATEND', ie_log_w, ds_w, 'REGRA_SOLIC_MINIMA_MAT', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_REQUISICAO_TRANSF,1,4000), substr(NEW.IE_REQUISICAO_TRANSF,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_REQUISICAO_TRANSF', ie_log_w, ds_w, 'REGRA_SOLIC_MINIMA_MAT', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_TRANSF_ESTOQUE,1,4000), substr(NEW.IE_TRANSF_ESTOQUE,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_TRANSF_ESTOQUE', ie_log_w, ds_w, 'REGRA_SOLIC_MINIMA_MAT', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_REQUISICAO,1,4000), substr(NEW.IE_REQUISICAO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_REQUISICAO', ie_log_w, ds_w, 'REGRA_SOLIC_MINIMA_MAT', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_PRESCRICAO,1,4000), substr(NEW.IE_PRESCRICAO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_PRESCRICAO', ie_log_w, ds_w, 'REGRA_SOLIC_MINIMA_MAT', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_EXECUCAO,1,4000), substr(NEW.IE_EXECUCAO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_EXECUCAO', ie_log_w, ds_w, 'REGRA_SOLIC_MINIMA_MAT', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.QT_MINIMO_MULTIPLO_SOLIC,1,4000), substr(NEW.QT_MINIMO_MULTIPLO_SOLIC,1,4000), NEW.nm_usuario, nr_seq_w, 'QT_MINIMO_MULTIPLO_SOLIC', ie_log_w, ds_w, 'REGRA_SOLIC_MINIMA_MAT', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_ALTERAR_QUANTIDADE,1,4000), substr(NEW.IE_ALTERAR_QUANTIDADE,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_ALTERAR_QUANTIDADE', ie_log_w, ds_w, 'REGRA_SOLIC_MINIMA_MAT', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_ATEND_LOTE_PDA,1,4000), substr(NEW.IE_ATEND_LOTE_PDA,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_ATEND_LOTE_PDA', ie_log_w, ds_w, 'REGRA_SOLIC_MINIMA_MAT', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_LOCAL_ESTOQUE,1,4000), substr(NEW.CD_LOCAL_ESTOQUE,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_LOCAL_ESTOQUE', ie_log_w, ds_w, 'REGRA_SOLIC_MINIMA_MAT', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_SOLIC_COMPRA,1,4000), substr(NEW.IE_SOLIC_COMPRA,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_SOLIC_COMPRA', ie_log_w, ds_w, 'REGRA_SOLIC_MINIMA_MAT', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_GESTAO_CIRURGIA,1,4000), substr(NEW.IE_GESTAO_CIRURGIA,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_GESTAO_CIRURGIA', ie_log_w, ds_w, 'REGRA_SOLIC_MINIMA_MAT', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_ALT_QTD_GES_CIRURGIA,1,4000), substr(NEW.IE_ALT_QTD_GES_CIRURGIA,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_ALT_QTD_GES_CIRURGIA', ie_log_w, ds_w, 'REGRA_SOLIC_MINIMA_MAT', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_EXECUCAO_DEV,1,4000), substr(NEW.IE_EXECUCAO_DEV,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_EXECUCAO_DEV', ie_log_w, ds_w, 'REGRA_SOLIC_MINIMA_MAT', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_ATEND_LOTE,1,4000), substr(NEW.IE_ATEND_LOTE,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_ATEND_LOTE', ie_log_w, ds_w, 'REGRA_SOLIC_MINIMA_MAT', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_ESTABELECIMENTO,1,4000), substr(NEW.CD_ESTABELECIMENTO,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_ESTABELECIMENTO', ie_log_w, ds_w, 'REGRA_SOLIC_MINIMA_MAT', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;   exception when others then ds_w:= '1'; end;   END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_regra_solic_minima_mat_tp() FROM PUBLIC;

CREATE TRIGGER regra_solic_minima_mat_tp
	AFTER UPDATE ON regra_solic_minima_mat FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_regra_solic_minima_mat_tp();

