-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS before_update_conta_pos_estab ON pls_conta_pos_estabelecido CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_before_update_conta_pos_estab() RETURNS trigger AS $BODY$
declare

BEGIN
if (coalesce(wheb_usuario_pck.get_ie_lote_contabil,'N') = 'N') and (coalesce(wheb_usuario_pck.get_ie_atualizacao_contabil,'N') = 'N') then
	if (NEW.nr_seq_lote_fat is not null) and (NEW.nr_seq_lote_disc is null) and /*aaschlote 13/05/2014 - Quando o item está na discussão, pode fazer o refaturamento*/
		(coalesce(NEW.ie_status_faturamento,'X') <> coalesce(OLD.ie_status_faturamento,'X')) then

		CALL wheb_mensagem_pck.exibir_mensagem_abort(190151);
	end if;
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_before_update_conta_pos_estab() FROM PUBLIC;

CREATE TRIGGER before_update_conta_pos_estab
	BEFORE UPDATE ON pls_conta_pos_estabelecido FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_before_update_conta_pos_estab();

