-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS material_horario_padrao_atual ON material_horario_padrao CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_material_horario_padrao_atual() RETURNS trigger AS $BODY$
declare
BEGIN
  BEGIN 
BEGIN 
	if (NEW.hr_final is not null) and ((NEW.hr_final <> OLD.hr_final) or (OLD.dt_fim is null)) then 
		NEW.dt_fim := to_date(to_char(LOCALTIMESTAMP,'dd/mm/yyyy') || ' ' || NEW.hr_final,'dd/mm/yyyy hh24:mi');
	end if;	
	if (NEW.hr_inicio is not null) and ((NEW.hr_inicio <> OLD.hr_inicio) or (OLD.dt_inicio is null)) then 
		NEW.dt_inicio := to_date(to_char(LOCALTIMESTAMP,'dd/mm/yyyy') || ' ' || NEW.hr_inicio,'dd/mm/yyyy hh24:mi');
	end if;
exception 
	when others then 
	null;
end;
 
  END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_material_horario_padrao_atual() FROM PUBLIC;

CREATE TRIGGER material_horario_padrao_atual
	BEFORE INSERT OR UPDATE ON material_horario_padrao FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_material_horario_padrao_atual();

