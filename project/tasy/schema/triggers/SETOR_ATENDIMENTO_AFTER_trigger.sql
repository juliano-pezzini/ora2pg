-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS setor_atendimento_after ON setor_atendimento CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_setor_atendimento_after() RETURNS trigger AS $BODY$
declare
 
reg_integracao_p	gerar_int_padrao.reg_integracao;
 
BEGIN 
reg_integracao_p.cd_classif_setor	:=	NEW.cd_classif_setor;
 
if (TG_OP = 'INSERT') then 
	reg_integracao_p.ie_operacao	:=	'I';
elsif (TG_OP = 'UPDATE') then 
	reg_integracao_p.ie_operacao	:=	'A';
elsif (TG_OP = 'DELETE') then 
	reg_integracao_p.ie_operacao	:=	'E';
end if;
 
reg_integracao_p.cd_estab_documento	:=	coalesce(NEW.cd_estabelecimento, OLD.cd_estabelecimento);
 
reg_integracao_p := gerar_int_padrao.gravar_integracao('13', coalesce(NEW.cd_setor_atendimento, OLD.cd_setor_atendimento), coalesce(NEW.nm_usuario, OLD.nm_usuario), reg_integracao_p);
IF TG_OP = 'DELETE' THEN
	RETURN OLD;
ELSE
	RETURN NEW;
END IF;

end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_setor_atendimento_after() FROM PUBLIC;

CREATE TRIGGER setor_atendimento_after
	AFTER INSERT OR UPDATE OR DELETE ON setor_atendimento FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_setor_atendimento_after();

