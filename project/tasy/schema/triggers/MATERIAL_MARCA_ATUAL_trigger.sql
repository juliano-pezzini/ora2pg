-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS material_marca_atual ON material_marca CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_material_marca_atual() RETURNS trigger AS $BODY$
declare
ie_obriga_val_anvisa_w	varchar(1);
BEGIN

ie_obriga_val_anvisa_w := substr(coalesce(obter_valor_param_usuario(132,164, obter_perfil_ativo, NEW.nm_usuario, obter_estabelecimento_ativo),'N'),1,1);

if	((ie_obriga_val_anvisa_w = 'S') and (NEW.nr_registro_anvisa is not null) and (NEW.dt_validade_reg_anvisa is null) and (coalesce(NEW.IE_VIGENTE_ANVISA,'X') = 'X') and
	((TG_OP = 'INSERT') or
	((TG_OP = 'UPDATE') and
	((OLD.nr_registro_anvisa is null) or (OLD.dt_validade_reg_anvisa is not null) or (coalesce(OLD.IE_VIGENTE_ANVISA,'X') <> 'X'))))) then
	/*obrigatório informar a validade do registro anvisa!*/

	CALL wheb_mensagem_pck.exibir_mensagem_abort(270848);
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_material_marca_atual() FROM PUBLIC;

CREATE TRIGGER material_marca_atual
	BEFORE INSERT OR UPDATE ON material_marca FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_material_marca_atual();

