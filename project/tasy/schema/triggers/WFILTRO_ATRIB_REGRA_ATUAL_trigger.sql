-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS wfiltro_atrib_regra_atual ON wfiltro_atrib_regra CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_wfiltro_atrib_regra_atual() RETURNS trigger AS $BODY$
declare

qt_records_w	bigint;

--Please, be careful
pragma autonomous_transaction;

BEGIN

if (wheb_usuario_pck.get_ie_executar_trigger = 'S')  then

	select	count(*)
	into STRICT	qt_records_w
	from	wfiltro_atrib_regra
	where	nr_seq_wfiltro		= NEW.nr_seq_wfiltro
	and	((cd_estabelecimento	= NEW.cd_estabelecimento)	or (cd_estabelecimento is null 	and NEW.cd_estabelecimento is null))
	and	((cd_perfil			= NEW.cd_perfil)		or (cd_perfil is null 		and NEW.cd_perfil is null))
	and	((nm_usuario_regra		= NEW.nm_usuario_regra)	or (nm_usuario_regra is null 	and NEW.nm_usuario_regra is null))
	and	nr_sequencia 		<> NEW.nr_sequencia;

	if (qt_records_w > 0) then

		--Já existe uma regra cadastrada com esses atributos.
		CALL wheb_mensagem_pck.exibir_mensagem_abort(991975);

	end if;

end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_wfiltro_atrib_regra_atual() FROM PUBLIC;

CREATE TRIGGER wfiltro_atrib_regra_atual
	BEFORE INSERT OR UPDATE ON wfiltro_atrib_regra FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_wfiltro_atrib_regra_atual();

