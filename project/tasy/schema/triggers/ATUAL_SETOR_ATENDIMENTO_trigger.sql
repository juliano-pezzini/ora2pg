-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS atual_setor_atendimento ON setor_atendimento CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_atual_setor_atendimento() RETURNS trigger AS $BODY$
declare

BEGIN

if (NEW.hr_limite_atendimento is not null) and
   ((NEW.hr_limite_atendimento <> OLD.hr_limite_atendimento) or (OLD.dt_limite_atendimento is null)) then
	NEW.dt_limite_atendimento := ESTABLISHMENT_TIMEZONE_UTILS.todayAtTime(NEW.hr_limite_atendimento);
end if;

if (NEW.hr_limite_medico is not null) and
   ((NEW.hr_limite_medico <> OLD.hr_limite_medico) or (OLD.dt_limite_medico is null)) then
	NEW.dt_limite_medico := ESTABLISHMENT_TIMEZONE_UTILS.todayAtTime(NEW.hr_limite_medico);
end if;

RETURN NEW;
end;
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_atual_setor_atendimento() FROM PUBLIC;

CREATE TRIGGER atual_setor_atendimento
	BEFORE INSERT OR UPDATE ON setor_atendimento FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_atual_setor_atendimento();

