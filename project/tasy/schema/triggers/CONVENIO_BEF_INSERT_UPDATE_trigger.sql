-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS convenio_bef_insert_update ON convenio CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_convenio_bef_insert_update() RETURNS trigger AS $BODY$
BEGIN

	if 	(( coalesce(NEW.ds_arquivo_logo_tiss, 'X') <> coalesce(OLD.ds_arquivo_logo_tiss,'X'))
	and (  length(coalesce(NEW.ds_arquivo_logo_tiss, 'X')) > 1 )
	and 	(( position('.bmp' in lower( NEW.ds_arquivo_logo_tiss )) = 0 ) and ( position('.jpg' in lower( NEW.ds_arquivo_logo_tiss )) = 0 ))) then

		CALL wheb_mensagem_pck.exibir_mensagem_abort(1040653);

	end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_convenio_bef_insert_update() FROM PUBLIC;

CREATE TRIGGER convenio_bef_insert_update
	BEFORE INSERT OR UPDATE ON convenio FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_convenio_bef_insert_update();

