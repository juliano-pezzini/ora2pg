-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS atual_regra_horario_solucao ON regra_horario_solucao CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_atual_regra_horario_solucao() RETURNS trigger AS $BODY$
declare

BEGIN

if (NEW.hr_inicio is not null) and
   ((NEW.hr_inicio <> OLD.hr_inicio) or (OLD.dt_inicio is null)) then
	NEW.dt_inicio := to_date(to_char(LOCALTIMESTAMP,'dd/mm/yyyy') || ' ' || NEW.hr_inicio || ':00', 'dd/mm/yyyy hh24:mi:ss');
end if;

if (NEW.hr_fim is not null) and
   ((NEW.hr_fim <> OLD.hr_fim) or (OLD.dt_fim is null)) then
	NEW.dt_fim := to_date(to_char(LOCALTIMESTAMP,'dd/mm/yyyy') || ' ' || NEW.hr_fim || ':00', 'dd/mm/yyyy hh24:mi:ss');
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_atual_regra_horario_solucao() FROM PUBLIC;

CREATE TRIGGER atual_regra_horario_solucao
	BEFORE INSERT OR UPDATE ON regra_horario_solucao FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_atual_regra_horario_solucao();

