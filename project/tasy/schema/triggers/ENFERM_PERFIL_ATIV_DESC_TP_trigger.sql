-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS enferm_perfil_ativ_desc_tp ON enferm_perfil_ativ_desc CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_enferm_perfil_ativ_desc_tp() RETURNS trigger AS $BODY$
DECLARE nr_seq_w bigint;ds_s_w   varchar(1);ds_c_w   varchar(1);ds_w	   varchar(1);ie_log_w varchar(1);BEGIN BEGIN ds_s_w := null; SELECT * FROM gravar_log_alteracao(to_char(OLD.DT_ATIV_DESC,'dd/mm/yyyy hh24:mi:ss'), to_char(NEW.DT_ATIV_DESC,'dd/mm/yyyy hh24:mi:ss'), NEW.nm_usuario, nr_seq_w, 'DT_ATIV_DESC', ie_log_w, ds_w, 'ENFERM_PERFIL_ATIV_DESC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.DS_ATIV_DESC_PROBLEMAS,1,4000), substr(NEW.DS_ATIV_DESC_PROBLEMAS,1,4000), NEW.nm_usuario, nr_seq_w, 'DS_ATIV_DESC_PROBLEMAS', ie_log_w, ds_w, 'ENFERM_PERFIL_ATIV_DESC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_ATIV_DESC_PROBLEMAS,1,4000), substr(NEW.IE_ATIV_DESC_PROBLEMAS,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_ATIV_DESC_PROBLEMAS', ie_log_w, ds_w, 'ENFERM_PERFIL_ATIV_DESC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;   exception when others then ds_w:= '1'; end;   END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_enferm_perfil_ativ_desc_tp() FROM PUBLIC;

CREATE TRIGGER enferm_perfil_ativ_desc_tp
	AFTER UPDATE ON enferm_perfil_ativ_desc FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_enferm_perfil_ativ_desc_tp();

