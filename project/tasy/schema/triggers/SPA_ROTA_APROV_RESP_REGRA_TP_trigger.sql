-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS spa_rota_aprov_resp_regra_tp ON spa_rota_aprov_resp_regra CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_spa_rota_aprov_resp_regra_tp() RETURNS trigger AS $BODY$
DECLARE nr_seq_w bigint;ds_s_w   varchar(1);ds_c_w   varchar(1);ds_w	   varchar(1);ie_log_w varchar(1);BEGIN BEGIN ds_s_w := to_char(OLD.NR_SEQUENCIA);  ds_c_w:=null;SELECT * FROM gravar_log_alteracao(substr(OLD.VL_DESCONTO_FINAL,1,4000), substr(NEW.VL_DESCONTO_FINAL,1,4000), NEW.nm_usuario, nr_seq_w, 'VL_DESCONTO_FINAL', ie_log_w, ds_w, 'SPA_ROTA_APROV_RESP_REGRA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.PR_DESCONTO_INICIAL,1,4000), substr(NEW.PR_DESCONTO_INICIAL,1,4000), NEW.nm_usuario, nr_seq_w, 'PR_DESCONTO_INICIAL', ie_log_w, ds_w, 'SPA_ROTA_APROV_RESP_REGRA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.PR_DESCONTO_FINAL,1,4000), substr(NEW.PR_DESCONTO_FINAL,1,4000), NEW.nm_usuario, nr_seq_w, 'PR_DESCONTO_FINAL', ie_log_w, ds_w, 'SPA_ROTA_APROV_RESP_REGRA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.NR_PARCELAS_INICIAL,1,4000), substr(NEW.NR_PARCELAS_INICIAL,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_PARCELAS_INICIAL', ie_log_w, ds_w, 'SPA_ROTA_APROV_RESP_REGRA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.NR_PARCELAS_FINAL,1,4000), substr(NEW.NR_PARCELAS_FINAL,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_PARCELAS_FINAL', ie_log_w, ds_w, 'SPA_ROTA_APROV_RESP_REGRA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.NR_SEQ_ROTA,1,4000), substr(NEW.NR_SEQ_ROTA,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_SEQ_ROTA', ie_log_w, ds_w, 'SPA_ROTA_APROV_RESP_REGRA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_CARGO,1,4000), substr(NEW.CD_CARGO,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_CARGO', ie_log_w, ds_w, 'SPA_ROTA_APROV_RESP_REGRA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.NR_NIVEL,1,4000), substr(NEW.NR_NIVEL,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_NIVEL', ie_log_w, ds_w, 'SPA_ROTA_APROV_RESP_REGRA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.NR_MES_INICIAL,1,4000), substr(NEW.NR_MES_INICIAL,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_MES_INICIAL', ie_log_w, ds_w, 'SPA_ROTA_APROV_RESP_REGRA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.NR_MES_FINAL,1,4000), substr(NEW.NR_MES_FINAL,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_MES_FINAL', ie_log_w, ds_w, 'SPA_ROTA_APROV_RESP_REGRA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.VL_DESCONTO_INICIAL,1,4000), substr(NEW.VL_DESCONTO_INICIAL,1,4000), NEW.nm_usuario, nr_seq_w, 'VL_DESCONTO_INICIAL', ie_log_w, ds_w, 'SPA_ROTA_APROV_RESP_REGRA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_JUROS,1,4000), substr(NEW.IE_JUROS,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_JUROS', ie_log_w, ds_w, 'SPA_ROTA_APROV_RESP_REGRA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;   exception when others then ds_w:= '1'; end;   END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_spa_rota_aprov_resp_regra_tp() FROM PUBLIC;

CREATE TRIGGER spa_rota_aprov_resp_regra_tp
	AFTER UPDATE ON spa_rota_aprov_resp_regra FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_spa_rota_aprov_resp_regra_tp();

