-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS wfiltro_atrib_regra_item_atual ON wfiltro_atrib_regra_item CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_wfiltro_atrib_regra_item_atual() RETURNS trigger AS $BODY$
declare

qt_records_w	bigint;

--Please, be careful
pragma autonomous_transaction;

BEGIN

if (wheb_usuario_pck.get_ie_executar_trigger = 'S')  then

	select	count(*)
	into STRICT	qt_records_w
	from	wfiltro_atrib_regra_item
	where	nr_seq_atrib_regra	= NEW.nr_seq_atrib_regra
	and	((cd_item		= NEW.cd_item) or (cd_item is null and NEW.cd_item is null))
	and	nr_sequencia 	<> NEW.nr_sequencia;

	if (qt_records_w > 0) then

		--Já existe uma regra cadastrada com esses atributos.
		CALL wheb_mensagem_pck.exibir_mensagem_abort(991975);

	end if;

end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_wfiltro_atrib_regra_item_atual() FROM PUBLIC;

CREATE TRIGGER wfiltro_atrib_regra_item_atual
	BEFORE INSERT OR UPDATE ON wfiltro_atrib_regra_item FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_wfiltro_atrib_regra_item_atual();

