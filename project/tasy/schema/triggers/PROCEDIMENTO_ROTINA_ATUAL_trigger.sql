-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS procedimento_rotina_atual ON procedimento_rotina CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_procedimento_rotina_atual() RETURNS trigger AS $BODY$
BEGIN

if (NEW.cd_procedimento is null) and (NEW.nr_proc_interno is null) then
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(279961);
end if;

if (NEW.cd_procedimento is not null) and (NEW.ie_origem_proced is null) then
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(279962);
end if;


RETURN NEW;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_procedimento_rotina_atual() FROM PUBLIC;

CREATE TRIGGER procedimento_rotina_atual
	BEFORE INSERT OR UPDATE ON procedimento_rotina FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_procedimento_rotina_atual();

