-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS atend_pac_chamado_atual ON atend_pac_chamado CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_atend_pac_chamado_atual() RETURNS trigger AS $BODY$
declare


BEGIN
if (coalesce(OLD.DT_CHAMADO,LOCALTIMESTAMP + interval '10 days') <> NEW.DT_CHAMADO) and (NEW.DT_CHAMADO is not null) then
	NEW.ds_utc		:= obter_data_utc(NEW.DT_CHAMADO, 'HV');
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_atend_pac_chamado_atual() FROM PUBLIC;

CREATE TRIGGER atend_pac_chamado_atual
	BEFORE INSERT OR UPDATE ON atend_pac_chamado FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_atend_pac_chamado_atual();

