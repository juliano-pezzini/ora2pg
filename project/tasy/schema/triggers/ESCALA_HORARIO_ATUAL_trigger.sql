-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS escala_horario_atual ON escala_horario CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_escala_horario_atual() RETURNS trigger AS $BODY$
DECLARE
    ie_situacao_w varchar(1) := 'S';
    ds_bloq_alt_escala_inativa_w varchar(255);
BEGIN
    ds_bloq_alt_escala_inativa_w := obter_valor_param_usuario(3009, 40, wheb_usuario_pck.get_cd_perfil, wheb_usuario_pck.get_nm_usuario, wheb_usuario_pck.get_cd_estabelecimento);

    IF (coalesce(wheb_usuario_pck.get_ie_executar_trigger, 'S') != 'N') THEN
        ie_situacao_w := fis_obter_escala_ativa(NEW.nr_seq_escala, 'ESCALA');

        IF (ie_situacao_w = 'N' AND coalesce(ds_bloq_alt_escala_inativa_w, 'N') = 'S') THEN
            CALL wheb_mensagem_pck.exibir_mensagem_abort(1186376, 'SEQ=' || NEW.nr_seq_escala);
        END IF;
    END IF;
IF TG_OP = 'DELETE' THEN
	RETURN OLD;
ELSE
	RETURN NEW;
END IF;

END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_escala_horario_atual() FROM PUBLIC;

CREATE TRIGGER escala_horario_atual
	BEFORE INSERT OR UPDATE OR DELETE ON escala_horario FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_escala_horario_atual();

