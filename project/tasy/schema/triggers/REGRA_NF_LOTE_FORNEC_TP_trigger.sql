-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS regra_nf_lote_fornec_tp ON regra_nf_lote_fornec CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_regra_nf_lote_fornec_tp() RETURNS trigger AS $BODY$
DECLARE nr_seq_w bigint;ds_s_w   varchar(1);ds_c_w   varchar(1);ds_w	   varchar(1);ie_log_w varchar(1);BEGIN BEGIN ds_s_w := to_char(OLD.NR_SEQUENCIA);  ds_c_w:=null;SELECT * FROM gravar_log_alteracao(substr(OLD.IE_IMPRIMIR_LOTE,1,4000), substr(NEW.IE_IMPRIMIR_LOTE,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_IMPRIMIR_LOTE', ie_log_w, ds_w, 'REGRA_NF_LOTE_FORNEC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.NR_SEQ_MARCA,1,4000), substr(NEW.NR_SEQ_MARCA,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_SEQ_MARCA', ie_log_w, ds_w, 'REGRA_NF_LOTE_FORNEC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_UNIDADE_MEDIDA,1,4000), substr(NEW.CD_UNIDADE_MEDIDA,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_UNIDADE_MEDIDA', ie_log_w, ds_w, 'REGRA_NF_LOTE_FORNEC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_QUANTIDADE,1,4000), substr(NEW.IE_QUANTIDADE,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_QUANTIDADE', ie_log_w, ds_w, 'REGRA_NF_LOTE_FORNEC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_GERAR_LOTE,1,4000), substr(NEW.IE_GERAR_LOTE,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_GERAR_LOTE', ie_log_w, ds_w, 'REGRA_NF_LOTE_FORNEC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_EMPRESA,1,4000), substr(NEW.CD_EMPRESA,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_EMPRESA', ie_log_w, ds_w, 'REGRA_NF_LOTE_FORNEC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_MATERIAL,1,4000), substr(NEW.CD_MATERIAL,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_MATERIAL', ie_log_w, ds_w, 'REGRA_NF_LOTE_FORNEC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_GRUPO_MATERIAL,1,4000), substr(NEW.CD_GRUPO_MATERIAL,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_GRUPO_MATERIAL', ie_log_w, ds_w, 'REGRA_NF_LOTE_FORNEC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_SUBGRUPO_MATERIAL,1,4000), substr(NEW.CD_SUBGRUPO_MATERIAL,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_SUBGRUPO_MATERIAL', ie_log_w, ds_w, 'REGRA_NF_LOTE_FORNEC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_CLASSE_MATERIAL,1,4000), substr(NEW.CD_CLASSE_MATERIAL,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_CLASSE_MATERIAL', ie_log_w, ds_w, 'REGRA_NF_LOTE_FORNEC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_BARRAS_MATERIAL,1,4000), substr(NEW.IE_BARRAS_MATERIAL,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_BARRAS_MATERIAL', ie_log_w, ds_w, 'REGRA_NF_LOTE_FORNEC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_TIPO_MATERIAL,1,4000), substr(NEW.IE_TIPO_MATERIAL,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_TIPO_MATERIAL', ie_log_w, ds_w, 'REGRA_NF_LOTE_FORNEC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_GERAR_DESDOBRADO,1,4000), substr(NEW.IE_GERAR_DESDOBRADO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_GERAR_DESDOBRADO', ie_log_w, ds_w, 'REGRA_NF_LOTE_FORNEC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_GERAR_LOTE_INSPECAO,1,4000), substr(NEW.IE_GERAR_LOTE_INSPECAO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_GERAR_LOTE_INSPECAO', ie_log_w, ds_w, 'REGRA_NF_LOTE_FORNEC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_ESTAB_REGRA,1,4000), substr(NEW.CD_ESTAB_REGRA,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_ESTAB_REGRA', ie_log_w, ds_w, 'REGRA_NF_LOTE_FORNEC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.NR_SEQ_FAMILIA,1,4000), substr(NEW.NR_SEQ_FAMILIA,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_SEQ_FAMILIA', ie_log_w, ds_w, 'REGRA_NF_LOTE_FORNEC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_GERAR_LOTE_EMPRESTIMO,1,4000), substr(NEW.IE_GERAR_LOTE_EMPRESTIMO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_GERAR_LOTE_EMPRESTIMO', ie_log_w, ds_w, 'REGRA_NF_LOTE_FORNEC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;   exception when others then ds_w:= '1'; end;   END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_regra_nf_lote_fornec_tp() FROM PUBLIC;

CREATE TRIGGER regra_nf_lote_fornec_tp
	AFTER UPDATE ON regra_nf_lote_fornec FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_regra_nf_lote_fornec_tp();

