-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS cml_prospect_insert ON cml_prospect CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_cml_prospect_insert() RETURNS trigger AS $BODY$
DECLARE
qt_reg_w	smallint;
BEGIN 
 
if (wheb_usuario_pck.get_ie_executar_trigger	= 'N') then 
	goto Final;
end if;
 
 
if (NEW.nr_seq_classif is not null) and (NEW.nr_seq_classif <> OLD.nr_seq_classif) then 
	insert into cml_prospect_classif( 
		nr_sequencia, 
		nr_seq_classif, 
		nr_seq_prospect, 
		nm_usuario, 
		dt_atualizacao) 
	values ( nextval('cml_prospect_classif_seq'), 
		NEW.nr_seq_classif, 
		NEW.nr_sequencia, 
		NEW.nm_usuario, 
		LOCALTIMESTAMP);
end if;
 
if (NEW.nr_seq_origem is not null) and (NEW.nr_seq_origem <> OLD.nr_seq_origem) then 
	insert into cml_prospect_origem( 
		nr_sequencia, 
		nr_seq_origem, 
		nr_seq_prospect, 
		nm_usuario, 
		dt_atualizacao) 
	values ( nextval('cml_prospect_origem_seq'), 
		NEW.nr_seq_origem, 
		NEW.nr_sequencia, 
		NEW.nm_usuario, 
		LOCALTIMESTAMP);
end if;
 
 
if (NEW.nr_seq_estagio is not null) and (NEW.nr_seq_estagio <> OLD.nr_seq_estagio) then 
	insert into cml_prospect_estagio( 
		nr_sequencia, 
		nr_seq_estagio, 
		nr_seq_prospect, 
		nm_usuario, 
		dt_atualizacao) 
	values ( nextval('cml_prospect_estagio_seq'), 
		NEW.nr_seq_estagio, 
		NEW.nr_sequencia, 
		NEW.nm_usuario, 
		LOCALTIMESTAMP);
end if;
 
if (NEW.nr_seq_atividade is not null) and (NEW.nr_seq_atividade <> OLD.nr_seq_atividade) then 
	insert into cml_prospect_atividade( 
		nr_sequencia, 
		nr_seq_atividade, 
		nr_seq_prospect, 
		nm_usuario, 
		dt_atualizacao) 
	values ( nextval('cml_prospect_atividade_seq'), 
		NEW.nr_seq_atividade, 
		NEW.nr_sequencia, 
		NEW.nm_usuario, 
		LOCALTIMESTAMP);
end if;
<<Final>> 
qt_reg_w	:= 0;
 
RETURN NEW;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_cml_prospect_insert() FROM PUBLIC;

CREATE TRIGGER cml_prospect_insert
	AFTER INSERT OR UPDATE ON cml_prospect FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_cml_prospect_insert();

