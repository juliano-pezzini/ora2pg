-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS medico_perfil_delegado_delete ON medico_perfil_delegado CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_medico_perfil_delegado_delete() RETURNS trigger AS $BODY$
declare
perfil_check_w   varchar(1);

BEGIN

    if (coalesce(wheb_usuario_pck.get_ie_executar_trigger, 'N') = 'S') then
        select 	coalesce(max('S'), 'N')
        into STRICT    perfil_check_w
        from 	perfil_delegacao
        where 	cd_perfil = OLD.cd_perfil;

        if (perfil_check_w = 'S') then
            CALL wheb_mensagem_pck.exibir_mensagem_abort(1164205);
        end if;
    end if;

RETURN OLD;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_medico_perfil_delegado_delete() FROM PUBLIC;

CREATE TRIGGER medico_perfil_delegado_delete
	BEFORE DELETE ON medico_perfil_delegado FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_medico_perfil_delegado_delete();

