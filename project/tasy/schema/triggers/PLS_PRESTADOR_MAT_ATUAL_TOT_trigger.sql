-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS pls_prestador_mat_atual_tot ON pls_prestador_mat CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_pls_prestador_mat_atual_tot() RETURNS trigger AS $BODY$
declare
BEGIN
-- antigamente tínhamos a PLS_PRESTADOR_MAT_TM que foi uma tentativa de melhoria de performance inicialmente na validação 30 (pls_oc_cta_tratar_val_30)
-- como a ação não foi muito eficaz, descemos mais um nível e trabalhamos apenas com a leitura da estrututa de materiais (PLS_ESTRUTURA_MATERIAL_TM), o grande gargalo de tudo.
-- por isso essa trigger não atualiza mais a PLS_PRESTADOR_MAT_TM, que inclusive, hoje 29/08/2014 10:15, existe mais não é mais utilizada.
null;

IF TG_OP = 'DELETE' THEN
	RETURN OLD;
ELSE
	RETURN NEW;
END IF;

end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_pls_prestador_mat_atual_tot() FROM PUBLIC;

CREATE TRIGGER pls_prestador_mat_atual_tot
	BEFORE INSERT OR UPDATE OR DELETE ON pls_prestador_mat FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_pls_prestador_mat_atual_tot();

