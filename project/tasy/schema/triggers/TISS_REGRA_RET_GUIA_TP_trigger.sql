-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS tiss_regra_ret_guia_tp ON tiss_regra_ret_guia CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_tiss_regra_ret_guia_tp() RETURNS trigger AS $BODY$
DECLARE nr_seq_w bigint;ds_s_w   varchar(1);ds_c_w   varchar(1);ds_w	   varchar(1);ie_log_w varchar(1);BEGIN BEGIN ds_s_w := to_char(OLD.NR_SEQUENCIA);  ds_c_w:=null; ds_w:=substr(NEW.DS_MASCARA_GUIA,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.DS_MASCARA_GUIA,1,4000), substr(NEW.DS_MASCARA_GUIA,1,4000), NEW.nm_usuario, nr_seq_w, 'DS_MASCARA_GUIA', ie_log_w, ds_w, 'TISS_REGRA_RET_GUIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.IE_USUARIO_CONVENIO,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.IE_USUARIO_CONVENIO,1,4000), substr(NEW.IE_USUARIO_CONVENIO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_USUARIO_CONVENIO', ie_log_w, ds_w, 'TISS_REGRA_RET_GUIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.IE_GUIA_PRESTADOR,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.IE_GUIA_PRESTADOR,1,4000), substr(NEW.IE_GUIA_PRESTADOR,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_GUIA_PRESTADOR', ie_log_w, ds_w, 'TISS_REGRA_RET_GUIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.IE_DIG_GUIA_HOSP,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.IE_DIG_GUIA_HOSP,1,4000), substr(NEW.IE_DIG_GUIA_HOSP,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_DIG_GUIA_HOSP', ie_log_w, ds_w, 'TISS_REGRA_RET_GUIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.NR_ADICIONAL,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.NR_ADICIONAL,1,4000), substr(NEW.NR_ADICIONAL,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_ADICIONAL', ie_log_w, ds_w, 'TISS_REGRA_RET_GUIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.DS_MASCARA_HOSP,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.DS_MASCARA_HOSP,1,4000), substr(NEW.DS_MASCARA_HOSP,1,4000), NEW.nm_usuario, nr_seq_w, 'DS_MASCARA_HOSP', ie_log_w, ds_w, 'TISS_REGRA_RET_GUIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.DT_INICIO_VIGENCIA,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.DT_INICIO_VIGENCIA,1,4000), substr(NEW.DT_INICIO_VIGENCIA,1,4000), NEW.nm_usuario, nr_seq_w, 'DT_INICIO_VIGENCIA', ie_log_w, ds_w, 'TISS_REGRA_RET_GUIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.IE_TIPO_PROTOCOLO,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.IE_TIPO_PROTOCOLO,1,4000), substr(NEW.IE_TIPO_PROTOCOLO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_TIPO_PROTOCOLO', ie_log_w, ds_w, 'TISS_REGRA_RET_GUIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.DS_MASCARA_GUIA_PREST,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.DS_MASCARA_GUIA_PREST,1,4000), substr(NEW.DS_MASCARA_GUIA_PREST,1,4000), NEW.nm_usuario, nr_seq_w, 'DS_MASCARA_GUIA_PREST', ie_log_w, ds_w, 'TISS_REGRA_RET_GUIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.IE_GUIA,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.IE_GUIA,1,4000), substr(NEW.IE_GUIA,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_GUIA', ie_log_w, ds_w, 'TISS_REGRA_RET_GUIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.NR_ANTERIOR,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.NR_ANTERIOR,1,4000), substr(NEW.NR_ANTERIOR,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_ANTERIOR', ie_log_w, ds_w, 'TISS_REGRA_RET_GUIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;   exception when others then ds_w:= '1'; end;   END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_tiss_regra_ret_guia_tp() FROM PUBLIC;

CREATE TRIGGER tiss_regra_ret_guia_tp
	AFTER UPDATE ON tiss_regra_ret_guia FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_tiss_regra_ret_guia_tp();

