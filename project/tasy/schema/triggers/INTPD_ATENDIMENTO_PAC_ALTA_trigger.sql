-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS intpd_atendimento_pac_alta ON atendimento_paciente CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_intpd_atendimento_pac_alta() RETURNS trigger AS $BODY$
declare
reg_integracao_p		gerar_int_padrao.reg_integracao;

BEGIN
if (TG_OP = 'INSERT') and (NEW.dt_alta is not null) then
	reg_integracao_p.ie_operacao		:=	'I';
elsif (TG_OP = 'UPDATE') and (NEW.dt_alta is not null) and (OLD.dt_alta is null) then
	reg_integracao_p.ie_operacao		:=	'I';
elsif (TG_OP = 'UPDATE') and (NEW.dt_alta is null) and (OLD.dt_alta is not null) then
	reg_integracao_p.ie_operacao		:=	'E';
end if;

if (reg_integracao_p.ie_operacao is not null) and (NEW.dt_cancelamento is null) then

	reg_integracao_p.nr_atendimento			:=	NEW.nr_atendimento;
	reg_integracao_p.cd_pessoa_fisica		:=	NEW.cd_pessoa_fisica;
	reg_integracao_p.ie_tipo_atendimento		:= 	NEW.ie_tipo_atendimento;
	reg_integracao_p.nr_seq_tipo_admissao_fat	:= 	NEW.nr_seq_tipo_admissao_fat;
	reg_integracao_p.cd_motivo_alta			:= 	NEW.cd_motivo_alta;

	reg_integracao_p := gerar_int_padrao.gravar_integracao('114', NEW.nr_atendimento, coalesce(obter_usuario_ativo, NEW.nm_usuario), reg_integracao_p);
end if;

RETURN NEW;
end;
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_intpd_atendimento_pac_alta() FROM PUBLIC;

CREATE TRIGGER intpd_atendimento_pac_alta
	AFTER INSERT OR UPDATE ON atendimento_paciente FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_intpd_atendimento_pac_alta();

