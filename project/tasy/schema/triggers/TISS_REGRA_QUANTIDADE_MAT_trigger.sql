-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS tiss_regra_quantidade_mat ON tiss_regra_quantidade_mat CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_tiss_regra_quantidade_mat() RETURNS trigger AS $BODY$
declare


BEGIN
if (wheb_usuario_pck.GET_IE_EXECUTAR_TRIGGER = 'S') then

	if (NEW.ie_origem_proced is not null) and (NEW.cd_procedimento is null) then
		NEW.ie_origem_proced := null;
	end if;

	if	((NEW.cd_material is not null) or (NEW.cd_grupo_material is not null) or (NEW.cd_classe_material is not null) or (NEW.cd_subgrupo_material is not null)) and
		((NEW.CD_PROCEDIMENTO is not null) or (NEW.CD_AREA_PROCEDIMENTO is not null) or (NEW.CD_ESPECIALIDADE is not null) or (NEW.CD_GRUPO_PROC is not null)) then
		CALL Wheb_mensagem_pck.exibir_mensagem_abort(853996);
	end if;

end if;

RETURN NEW;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_tiss_regra_quantidade_mat() FROM PUBLIC;

CREATE TRIGGER tiss_regra_quantidade_mat
	BEFORE UPDATE ON tiss_regra_quantidade_mat FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_tiss_regra_quantidade_mat();

