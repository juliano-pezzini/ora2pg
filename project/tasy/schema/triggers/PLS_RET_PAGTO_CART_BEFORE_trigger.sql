-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS pls_ret_pagto_cart_before ON pls_ret_pagto_cartao_cr CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_pls_ret_pagto_cart_before() RETURNS trigger AS $BODY$
declare
nr_seq_bandeira_w   bandeira_cartao_cr.nr_sequencia%type;
BEGIN
	if (NEW.ds_bandeira is not null) then
		select 	max(nr_sequencia)
		into STRICT 	nr_seq_bandeira_w
		from 	bandeira_cartao_cr
		where 	trim(both upper(ds_bandeira)) = trim(both upper(NEW.ds_bandeira));
		
		NEW.nr_seq_bandeira := nr_seq_bandeira_w;
	end if;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_pls_ret_pagto_cart_before() FROM PUBLIC;

CREATE TRIGGER pls_ret_pagto_cart_before
	BEFORE INSERT OR UPDATE ON pls_ret_pagto_cartao_cr FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_pls_ret_pagto_cart_before();

