-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS regra_controle_qtde_sc_atual ON regra_controle_qtde_sc CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_regra_controle_qtde_sc_atual() RETURNS trigger AS $BODY$
declare

BEGIN

if (NEW.IE_PERIODICIDADE = 'D') and (NEW.IE_MES is not null) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(687604); /*Quando a periodicidade é Diária, não é permitido informar o mês.*/
end if;

if (NEW.IE_PERIODICIDADE = 'M') and (NEW.IE_DIA_SEMANA is not null) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(687613); /*Quando a periodicidade é Mensal, não é permitido informar o dia da semana.*/
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_regra_controle_qtde_sc_atual() FROM PUBLIC;

CREATE TRIGGER regra_controle_qtde_sc_atual
	BEFORE INSERT OR UPDATE ON regra_controle_qtde_sc FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_regra_controle_qtde_sc_atual();

