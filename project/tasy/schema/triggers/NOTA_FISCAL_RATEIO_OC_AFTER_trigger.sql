-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS nota_fiscal_rateio_oc_after ON nota_fiscal CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_nota_fiscal_rateio_oc_after() RETURNS trigger AS $BODY$
declare

BEGIN

if (coalesce(wheb_usuario_pck.get_ie_executar_trigger,'S') = 'S') then
	if (NEW.nr_ordem_compra is not null) and (OLD.nr_ordem_compra is null) then
		-- Gerar nota rateio
		CALL gerar_rateio_nf_ordem_compra(NEW.nr_sequencia,NEW.nr_ordem_compra,NEW.nm_usuario,NEW.cd_estabelecimento,'N');
	end if;	
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_nota_fiscal_rateio_oc_after() FROM PUBLIC;

CREATE TRIGGER nota_fiscal_rateio_oc_after
	AFTER INSERT OR UPDATE ON nota_fiscal FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_nota_fiscal_rateio_oc_after();

