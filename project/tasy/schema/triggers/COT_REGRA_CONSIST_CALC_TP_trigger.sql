-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS cot_regra_consist_calc_tp ON cot_regra_consist_calc CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_cot_regra_consist_calc_tp() RETURNS trigger AS $BODY$
DECLARE nr_seq_w bigint;ds_s_w   varchar(1);ds_c_w   varchar(1);ds_w	   varchar(1);ie_log_w varchar(1);BEGIN BEGIN ds_s_w := to_char(OLD.NR_SEQUENCIA);  ds_c_w:=null;SELECT * FROM gravar_log_alteracao(substr(OLD.CD_ESTABELECIMENTO,1,4000), substr(NEW.CD_ESTABELECIMENTO,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_ESTABELECIMENTO', ie_log_w, ds_w, 'COT_REGRA_CONSIST_CALC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_SITUACAO,1,4000), substr(NEW.IE_SITUACAO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_SITUACAO', ie_log_w, ds_w, 'COT_REGRA_CONSIST_CALC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_BLOQUEIA,1,4000), substr(NEW.IE_BLOQUEIA,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_BLOQUEIA', ie_log_w, ds_w, 'COT_REGRA_CONSIST_CALC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_PFB_PMC,1,4000), substr(NEW.IE_PFB_PMC,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_PFB_PMC', ie_log_w, ds_w, 'COT_REGRA_CONSIST_CALC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_TIPO_REGRA,1,4000), substr(NEW.IE_TIPO_REGRA,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_TIPO_REGRA', ie_log_w, ds_w, 'COT_REGRA_CONSIST_CALC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_TAB_PRECO_MAT,1,4000), substr(NEW.CD_TAB_PRECO_MAT,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_TAB_PRECO_MAT', ie_log_w, ds_w, 'COT_REGRA_CONSIST_CALC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.VL_PARAMETRO,1,4000), substr(NEW.VL_PARAMETRO,1,4000), NEW.nm_usuario, nr_seq_w, 'VL_PARAMETRO', ie_log_w, ds_w, 'COT_REGRA_CONSIST_CALC', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;   exception when others then ds_w:= '1'; end;   END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_cot_regra_consist_calc_tp() FROM PUBLIC;

CREATE TRIGGER cot_regra_consist_calc_tp
	AFTER UPDATE ON cot_regra_consist_calc FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_cot_regra_consist_calc_tp();

