-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS pf_tipo_deficiencia_atual ON pf_tipo_deficiencia CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_pf_tipo_deficiencia_atual() RETURNS trigger AS $BODY$
declare


BEGIN
if (NEW.dt_liberacao is not null) and (OLD.dt_liberacao is null) then
	NEW.NM_USUARIO_LIBERACAO := wheb_usuario_pck.get_nm_usuario;
end if;

if (coalesce(OLD.DT_ATUALIZACAO_NREC,LOCALTIMESTAMP + interval '10 days') <> NEW.DT_ATUALIZACAO_NREC) and (NEW.DT_ATUALIZACAO_NREC is not null) then
	NEW.ds_utc		:= obter_data_utc(NEW.DT_ATUALIZACAO_NREC, 'HV');
end if;

if (coalesce(OLD.DT_LIBERACAO,LOCALTIMESTAMP + interval '10 days') <> NEW.DT_LIBERACAO) and (NEW.DT_LIBERACAO is not null) then
	NEW.ds_utc_atualizacao	:= obter_data_utc(NEW.DT_LIBERACAO,'HV');
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_pf_tipo_deficiencia_atual() FROM PUBLIC;

CREATE TRIGGER pf_tipo_deficiencia_atual
	BEFORE INSERT OR UPDATE ON pf_tipo_deficiencia FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_pf_tipo_deficiencia_atual();

