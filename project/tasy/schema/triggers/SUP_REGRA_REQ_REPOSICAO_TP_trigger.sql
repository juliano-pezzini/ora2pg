-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS sup_regra_req_reposicao_tp ON sup_regra_req_reposicao CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_sup_regra_req_reposicao_tp() RETURNS trigger AS $BODY$
DECLARE nr_seq_w bigint;ds_s_w   varchar(1);ds_c_w   varchar(1);ds_w	   varchar(1);ie_log_w varchar(1);BEGIN BEGIN ds_s_w := to_char(OLD.NR_SEQUENCIA);  ds_c_w:=null;SELECT * FROM gravar_log_alteracao(substr(OLD.CD_ESTABELECIMENTO,1,4000), substr(NEW.CD_ESTABELECIMENTO,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_ESTABELECIMENTO', ie_log_w, ds_w, 'SUP_REGRA_REQ_REPOSICAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_MONTAGEM_KIT,1,4000), substr(NEW.IE_MONTAGEM_KIT,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_MONTAGEM_KIT', ie_log_w, ds_w, 'SUP_REGRA_REQ_REPOSICAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_PRESCRICAO_CIR,1,4000), substr(NEW.IE_PRESCRICAO_CIR,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_PRESCRICAO_CIR', ie_log_w, ds_w, 'SUP_REGRA_REQ_REPOSICAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_REG_KIT,1,4000), substr(NEW.IE_REG_KIT,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_REG_KIT', ie_log_w, ds_w, 'SUP_REGRA_REQ_REPOSICAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_ARRED_BAIXA,1,4000), substr(NEW.IE_ARRED_BAIXA,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_ARRED_BAIXA', ie_log_w, ds_w, 'SUP_REGRA_REQ_REPOSICAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_CONSIDERA_REQ,1,4000), substr(NEW.IE_CONSIDERA_REQ,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_CONSIDERA_REQ', ie_log_w, ds_w, 'SUP_REGRA_REQ_REPOSICAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_SOMENTE_ZERADO,1,4000), substr(NEW.IE_SOMENTE_ZERADO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_SOMENTE_ZERADO', ie_log_w, ds_w, 'SUP_REGRA_REQ_REPOSICAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_LANCADO_CIR,1,4000), substr(NEW.IE_LANCADO_CIR,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_LANCADO_CIR', ie_log_w, ds_w, 'SUP_REGRA_REQ_REPOSICAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_MATERIAL_COM,1,4000), substr(NEW.IE_MATERIAL_COM,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_MATERIAL_COM', ie_log_w, ds_w, 'SUP_REGRA_REQ_REPOSICAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_AGENDA_CIR,1,4000), substr(NEW.IE_AGENDA_CIR,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_AGENDA_CIR', ie_log_w, ds_w, 'SUP_REGRA_REQ_REPOSICAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_CONSIDERA_EMP,1,4000), substr(NEW.IE_CONSIDERA_EMP,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_CONSIDERA_EMP', ie_log_w, ds_w, 'SUP_REGRA_REQ_REPOSICAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_MULTIPLO_ES_CO,1,4000), substr(NEW.IE_MULTIPLO_ES_CO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_MULTIPLO_ES_CO', ie_log_w, ds_w, 'SUP_REGRA_REQ_REPOSICAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_SALDO_MA_MINIMO,1,4000), substr(NEW.IE_SALDO_MA_MINIMO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_SALDO_MA_MINIMO', ie_log_w, ds_w, 'SUP_REGRA_REQ_REPOSICAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_MULT_MINIMO,1,4000), substr(NEW.IE_MULT_MINIMO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_MULT_MINIMO', ie_log_w, ds_w, 'SUP_REGRA_REQ_REPOSICAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_LOCAL_ESTOQUE,1,4000), substr(NEW.CD_LOCAL_ESTOQUE,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_LOCAL_ESTOQUE', ie_log_w, ds_w, 'SUP_REGRA_REQ_REPOSICAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;   exception when others then ds_w:= '1'; end;   END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_sup_regra_req_reposicao_tp() FROM PUBLIC;

CREATE TRIGGER sup_regra_req_reposicao_tp
	AFTER UPDATE ON sup_regra_req_reposicao FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_sup_regra_req_reposicao_tp();

