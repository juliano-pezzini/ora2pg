-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS kit_estoque_comp_delete ON kit_estoque_comp CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_kit_estoque_comp_delete() RETURNS trigger AS $BODY$
declare

ds_titulo_w		varchar(1000);
ds_historico_w		varchar(2000);
BEGIN
  BEGIN

ds_titulo_w	:=	WHEB_MENSAGEM_PCK.get_texto(310135); --Exclusão de componente do kit
ds_historico_w	:=	substr(WHEB_MENSAGEM_PCK.get_texto(310136) || OLD.cd_material || ' - ' || obter_desc_material(OLD.cd_material),1,1000); --Excluido o componente
BEGIN
CALL gravar_kit_estoque_hist(OLD.nr_seq_kit_estoque,
			ds_titulo_w,
			ds_historico_w,
			'S',
			wheb_usuario_pck.get_nm_usuario);
exception
when others then
	null;
end;

  END;
RETURN OLD;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_kit_estoque_comp_delete() FROM PUBLIC;

CREATE TRIGGER kit_estoque_comp_delete
	BEFORE DELETE ON kit_estoque_comp FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_kit_estoque_comp_delete();

