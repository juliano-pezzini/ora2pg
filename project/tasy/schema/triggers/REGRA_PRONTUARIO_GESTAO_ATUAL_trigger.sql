-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS regra_prontuario_gestao_atual ON regra_prontuario_gestao CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_regra_prontuario_gestao_atual() RETURNS trigger AS $BODY$
BEGIN

if (NEW.ie_setor_usuario = 'N') and (NEW.cd_setor_atendimento is null) then
	--Quando o setor do prontuário não for baseado no setor do usuário, deve ser selecionado um setor de atendimento.
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(264233);

end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_regra_prontuario_gestao_atual() FROM PUBLIC;

CREATE TRIGGER regra_prontuario_gestao_atual
	BEFORE INSERT OR UPDATE ON regra_prontuario_gestao FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_regra_prontuario_gestao_atual();

