-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS lote_ent_rec_cont_update ON lote_ent_reconvocado_cont CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_lote_ent_rec_cont_update() RETURNS trigger AS $BODY$
declare

ds_opcao_utilizada_w	varchar(20);

BEGIN

if (OLD.nr_seq_status_busca > NEW.nr_seq_status_busca) then
	ds_opcao_utilizada_w := Wheb_mensagem_pck.get_texto(308662); --'Voltou status';
elsif (NEW.nr_seq_status_busca > OLD.nr_seq_status_busca) then
	ds_opcao_utilizada_w := Wheb_mensagem_pck.get_texto(308663); --'Avançou status';
else
	ds_opcao_utilizada_w := null;
end if;

if (NEW.nr_seq_status_busca <> OLD.nr_seq_status_busca) and (coalesce(NEW.nr_seq_status_busca,0) <> 0) then

	insert into LOTE_ENT_REC_CONT_HIST(
		DT_ATUALIZACAO,
		DT_ATUALIZACAO_NREC,
		NM_USUARIO,
		NM_USUARIO_NREC,
		NR_SEQ_RECON_CONT,
		NR_SEQ_RECONVOCADO,
		NR_SEQUENCIA,
		NR_SEQ_STATUS_ANT,
		NR_SEQ_STATUS_ATUAL,
		DS_OBSERVACAO
	) values (
		LOCALTIMESTAMP,
		LOCALTIMESTAMP,
		NEW.nm_usuario,
		NEW.nm_usuario,
		NEW.nr_sequencia,
		NEW.nr_seq_reconvocado,
		nextval('lote_ent_rec_cont_hist_seq'),
		OLD.nr_seq_status_busca,
		NEW.nr_seq_status_busca,
		ds_opcao_utilizada_w
	);

end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_lote_ent_rec_cont_update() FROM PUBLIC;

CREATE TRIGGER lote_ent_rec_cont_update
	BEFORE UPDATE ON lote_ent_reconvocado_cont FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_lote_ent_rec_cont_update();

