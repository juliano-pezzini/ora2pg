-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS pls_evento_regra_item_nf_atual ON pls_evento_regra_item_nf CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_pls_evento_regra_item_nf_atual() RETURNS trigger AS $BODY$
declare
BEGIN
if (NEW.cd_procedimento is not null) and (NEW.cd_material is not null) then
	-- A regra só pode possuir um material, ou um procedimento.
	CALL wheb_mensagem_pck.exibir_mensagem_abort(266793);
end if;

if (NEW.cd_procedimento is null) and (NEW.cd_material is null) then
	-- A regra deve possuir ao menos um material, ou um procedimento.
	CALL wheb_mensagem_pck.exibir_mensagem_abort(266794);
end if;


RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_pls_evento_regra_item_nf_atual() FROM PUBLIC;

CREATE TRIGGER pls_evento_regra_item_nf_atual
	BEFORE INSERT OR UPDATE ON pls_evento_regra_item_nf FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_pls_evento_regra_item_nf_atual();

