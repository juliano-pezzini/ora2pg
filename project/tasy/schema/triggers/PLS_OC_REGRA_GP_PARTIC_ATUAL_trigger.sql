-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS pls_oc_regra_gp_partic_atual ON pls_oc_regra_grupo_partic CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_pls_oc_regra_gp_partic_atual() RETURNS trigger AS $BODY$
declare
nr_seq_regra_w	pls_oc_regra_participante.nr_sequencia%type;
BEGIN

-- se for apagar o registro
if (TG_OP = 'DELETE') then
	nr_seq_regra_w := OLD.nr_seq_regra_partic;

elsif (TG_OP = 'UPDATE') then
	-- hoje, 16/02/2015 não existe nenhuma situação de atualização que cause alguma
	-- mudança nos itens da regra, por isso não é necessário atualizar nada
	nr_seq_regra_w := null;

else
	-- inserção ou atualização
	nr_seq_regra_w := NEW.nr_seq_regra_partic;
end if;

-- se tiver a regra sinaliza que é necessário atualizar a tabela
if (nr_seq_regra_w is not null) then
	CALL pls_gerencia_upd_obj_pck.marcar_para_atualizacao(	'PLS_GRUPO_PARTIC_TM', wheb_usuario_pck.get_nm_usuario,
								'PLS_OC_REGRA_GP_PARTIC_ATUAL',
								'nr_seq_regra_p=' || nr_seq_regra_w);
end if;

IF TG_OP = 'DELETE' THEN
	RETURN OLD;
ELSE
	RETURN NEW;
END IF;

end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_pls_oc_regra_gp_partic_atual() FROM PUBLIC;

CREATE TRIGGER pls_oc_regra_gp_partic_atual
	BEFORE INSERT OR UPDATE OR DELETE ON pls_oc_regra_grupo_partic FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_pls_oc_regra_gp_partic_atual();

