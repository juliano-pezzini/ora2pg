-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS caixa_receb_delete ON caixa_receb CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_caixa_receb_delete() RETURNS trigger AS $BODY$
declare

BEGIN

if (wheb_usuario_pck.get_ie_executar_trigger = 'S') then
if (OLD.dt_fechamento is not null) then
	/* Nao e possivel excluir esse recibo!
	O recibo #@NR_RECIBO#@ ja foi fechado! */

	CALL wheb_mensagem_pck.exibir_mensagem_abort(267124, 'NR_RECIBO=' || OLD.nr_recibo);
end if;
end if;

RETURN OLD;
end;
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_caixa_receb_delete() FROM PUBLIC;

CREATE TRIGGER caixa_receb_delete
	BEFORE DELETE ON caixa_receb FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_caixa_receb_delete();

