-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS cirurgia_descricao_insup ON cirurgia_descricao CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_cirurgia_descricao_insup() RETURNS trigger AS $BODY$
declare


BEGIN
IF (wheb_usuario_pck.get_ie_executar_trigger	= 'S')  THEN
	if (coalesce(OLD.DT_REGISTRO,LOCALTIMESTAMP + interval '10 days') <> NEW.DT_REGISTRO) and (NEW.DT_REGISTRO is not null) then
	 NEW.ds_utc  := obter_data_utc(NEW.DT_REGISTRO, 'HV');
	end if;

	if (coalesce(OLD.DT_LIBERACAO,LOCALTIMESTAMP + interval '10 days') <> NEW.DT_LIBERACAO) and (NEW.DT_LIBERACAO is not null) then
	 NEW.ds_utc_atualizacao := obter_data_utc(NEW.DT_LIBERACAO,'HV');
	end if;

	if (TG_OP = 'INSERT' and NEW.IE_SITUACAO <> 'A') then
	 NEW.IE_SITUACAO := 'A';
	end if;

	if (TG_OP = 'INSERT' and NEW.dt_atualizacao_nrec is null) then
	 NEW.dt_atualizacao_nrec := NEW.dt_atualizacao;
	end if;
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_cirurgia_descricao_insup() FROM PUBLIC;

CREATE TRIGGER cirurgia_descricao_insup
	BEFORE INSERT OR UPDATE ON cirurgia_descricao FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_cirurgia_descricao_insup();

