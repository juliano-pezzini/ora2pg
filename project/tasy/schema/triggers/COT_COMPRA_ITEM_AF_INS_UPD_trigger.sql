-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS cot_compra_item_af_ins_upd ON cot_compra_item CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_cot_compra_item_af_ins_upd() RETURNS trigger AS $BODY$
BEGIN

    if (obter_bloc_canc_proj_rec(NEW.nr_seq_proj_rec) > 0) then
        CALL wheb_mensagem_pck.exibir_mensagem_abort(1144309);  -- Registro associado a um projeto bloqueado ou cancelado. 
    end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_cot_compra_item_af_ins_upd() FROM PUBLIC;

CREATE TRIGGER cot_compra_item_af_ins_upd
	AFTER INSERT OR UPDATE ON cot_compra_item FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_cot_compra_item_af_ins_upd();

