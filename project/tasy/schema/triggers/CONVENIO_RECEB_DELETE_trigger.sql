-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS convenio_receb_delete ON convenio_receb CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_convenio_receb_delete() RETURNS trigger AS $BODY$
DECLARE

NR_SEQ_MOVTO_W		bigint;
IE_STATUS_W		varchar(01);
DT_FECHAMENTO_W		timestamp := NULL;

BEGIN


IF (OLD.IE_STATUS <> 'N') THEN
	--Não pode ser excluido um recebimendo já vinculado');
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(261630);
END IF;

/* Grava o agendamento da informação para atualização do fluxo de caixa. */

CALL gravar_agend_fluxo_caixa(OLD.nr_sequencia,null,'RC',OLD.dt_recebimento,'E',OLD.nm_usuario);

RETURN OLD;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_convenio_receb_delete() FROM PUBLIC;

CREATE TRIGGER convenio_receb_delete
	BEFORE DELETE ON convenio_receb FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_convenio_receb_delete();

