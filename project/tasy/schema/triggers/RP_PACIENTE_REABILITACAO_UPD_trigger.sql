-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS rp_paciente_reabilitacao_upd ON rp_paciente_reabilitacao CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_rp_paciente_reabilitacao_upd() RETURNS trigger AS $BODY$
declare

BEGIN
if (NEW.nr_seq_status is not null) and (coalesce(NEW.nr_seq_status,0) <> coalesce(OLD.nr_seq_status,0)) then
	CALL rp_gravar_status_reab(NEW.nr_sequencia,NEW.nr_seq_status,NEW.nm_usuario);
end if;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_rp_paciente_reabilitacao_upd() FROM PUBLIC;

CREATE TRIGGER rp_paciente_reabilitacao_upd
	AFTER UPDATE ON rp_paciente_reabilitacao FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_rp_paciente_reabilitacao_upd();

