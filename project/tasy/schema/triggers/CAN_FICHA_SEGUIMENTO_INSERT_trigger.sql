-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS can_ficha_seguimento_insert ON can_ficha_seguimento CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_can_ficha_seguimento_insert() RETURNS trigger AS $BODY$
DECLARE

BEGIN

if (NEW.CD_TOPOGRAFIA_RECIDIVA1 is null) and (NEW.DT_RECIDIVA_1 is not null) then
	--Não é permitido informar a data da recidiva 1 sem ter informado o local 1!');
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(261565);
elsif (NEW.CD_TOPOGRAFIA_RECIDIVA2 is null) and (NEW.DT_RECIDIVA_2 is not null) then
	--Não é permitido informar a data da recidiva 2 sem ter informado o local 2!');
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(261566);
elsif (NEW.CD_TOPOGRAFIA_RECIDIVA3 is null) and (NEW.DT_RECIDIVA_3 is not null) then
	--Não é permitido informar a data da recidiva 3 sem ter informado o local 3!');
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(261567);
elsif (NEW.CD_TOPOGRAFIA_RECIDIVA4 is null) and (NEW.DT_RECIDIVA_4 is not null) then
	--Não é permitido informar a data da recidiva 4 sem ter informado o local 4!');
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(261570);
end if;

if	(NEW.CD_TOPOGRAFIA_RECIDIVA1 is not null AND NEW.DT_RECIDIVA_1 is null) or
	(NEW.CD_TOPOGRAFIA_RECIDIVA2 is not null AND NEW.DT_RECIDIVA_2 is null) or
	(NEW.CD_TOPOGRAFIA_RECIDIVA3 is not null AND NEW.DT_RECIDIVA_3 is null) or
	(NEW.CD_TOPOGRAFIA_RECIDIVA4 is not null AND NEW.DT_RECIDIVA_4 is null) then
	--Para cada local informado é necessário informar a data recidiva correspondente!');
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(261571);
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_can_ficha_seguimento_insert() FROM PUBLIC;

CREATE TRIGGER can_ficha_seguimento_insert
	BEFORE INSERT ON can_ficha_seguimento FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_can_ficha_seguimento_insert();

