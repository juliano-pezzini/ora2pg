-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS cot_compra_delete ON cot_compra CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_cot_compra_delete() RETURNS trigger AS $BODY$
declare
nr_solic_compra_w		bigint;
nr_item_cot_compra_w		bigint;
 
c01 CURSOR FOR 
SELECT	nr_solic_compra, 
	nr_item_cot_compra 
from	cot_compra_item 
where	nr_cot_compra = OLD.nr_cot_compra 
and	nr_solic_compra > 0;
 
pragma autonomous_transaction;
 
BEGIN 
 
 
open C01;
loop 
fetch C01 into	 
	nr_solic_compra_w, 
	nr_item_cot_compra_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	BEGIN 
	 
	if (OLD.nr_cot_compra > 0) and (nr_item_cot_compra_w > 0) then 
		 
		update	solic_compra_item a 
		set	a.nr_cot_compra		 = NULL, 
			a.nr_item_cot_compra	 = NULL 
		where	a.nr_cot_compra		= OLD.nr_cot_compra 
		and	a.nr_item_cot_compra	= nr_item_cot_compra_w;
 
	end if;
	end;
end loop;
close C01;
 
commit;
 
RETURN OLD;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_cot_compra_delete() FROM PUBLIC;

CREATE TRIGGER cot_compra_delete
	BEFORE DELETE ON cot_compra FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_cot_compra_delete();

