-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS mprev_atend_domicilio_atual ON mprev_atend_domicilio CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_mprev_atend_domicilio_atual() RETURNS trigger AS $BODY$
DECLARE

BEGIN

if (NEW.DT_FIM_VIAGEM < NEW.DT_INICIO_VIAGEM) then
	CALL wheb_mensagem_pck.Exibir_Mensagem_Abort(876850);
end if;

if (trunc(NEW.qt_km_final) < trunc(NEW.qt_km_inicial)) then
	CALL wheb_mensagem_pck.Exibir_Mensagem_Abort(877801);
end if;

RETURN NEW;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_mprev_atend_domicilio_atual() FROM PUBLIC;

CREATE TRIGGER mprev_atend_domicilio_atual
	BEFORE INSERT OR UPDATE ON mprev_atend_domicilio FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_mprev_atend_domicilio_atual();

