-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS diops_fin_sinistro_atual ON diops_fin_sinistro CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_diops_fin_sinistro_atual() RETURNS trigger AS $BODY$
declare
ie_60_dias_w		varchar(1)	:= 'N';
ie_30_dias_w		varchar(1)	:= 'N';

BEGIN
if (NEW.vl_evento_ate_30 is not null) or (NEW.vl_evento_pos_30 is not null) then
	ie_30_dias_w	:= 'S';
end if;

if (NEW.vl_evento_ate_60 is not null) or (NEW.vl_evento_pos_60 is not null) then
	ie_60_dias_w	:= 'S';
end if;

if (ie_60_dias_w = 'S') and (ie_30_dias_w = 'S') then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(190681);
end if;

if (ie_60_dias_w = 'N') and (ie_30_dias_w = 'N') then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(190682);
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_diops_fin_sinistro_atual() FROM PUBLIC;

CREATE TRIGGER diops_fin_sinistro_atual
	BEFORE INSERT OR UPDATE ON diops_fin_sinistro FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_diops_fin_sinistro_atual();

