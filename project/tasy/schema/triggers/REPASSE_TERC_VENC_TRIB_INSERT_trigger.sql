-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS repasse_terc_venc_trib_insert ON repasse_terc_venc_trib CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_repasse_terc_venc_trib_insert() RETURNS trigger AS $BODY$
DECLARE
ds_tributo_w 	varchar(255);

BEGIN

if (NEW.vl_imposto < 0) then
	select	ds_tributo
	into STRICT	ds_tributo_w
	from	tributo
	where	cd_tributo	= NEW.cd_tributo;

	/* O valor do tributo #@DS_TRIBUTO não pode ser negativo!
	Vl_tributo = #@VL_TRIBUTO#@ */
	CALL wheb_mensagem_pck.exibir_mensagem_abort(266955, 'DS_TRIBUTO=' || ds_tributo_w || ';VL_TRIBUTO=' || NEW.vl_imposto);
end if;

RETURN NEW;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_repasse_terc_venc_trib_insert() FROM PUBLIC;

CREATE TRIGGER repasse_terc_venc_trib_insert
	BEFORE INSERT ON repasse_terc_venc_trib FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_repasse_terc_venc_trib_insert();

