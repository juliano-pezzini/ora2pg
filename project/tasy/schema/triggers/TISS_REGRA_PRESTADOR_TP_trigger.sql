-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS tiss_regra_prestador_tp ON tiss_regra_prestador CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_tiss_regra_prestador_tp() RETURNS trigger AS $BODY$
DECLARE nr_seq_w bigint;ds_s_w   varchar(1);ds_c_w   varchar(1);ds_w	   varchar(1);ie_log_w varchar(1);BEGIN BEGIN ds_s_w := to_char(OLD.NR_SEQUENCIA);  ds_c_w:=null; ds_w:=substr(NEW.IE_RESPONSAVEL_CREDITO,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.IE_RESPONSAVEL_CREDITO,1,4000), substr(NEW.IE_RESPONSAVEL_CREDITO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_RESPONSAVEL_CREDITO', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.IE_TIPO_ATEND_CONTA,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.IE_TIPO_ATEND_CONTA,1,4000), substr(NEW.IE_TIPO_ATEND_CONTA,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_TIPO_ATEND_CONTA', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.IE_TIPO_ATENDIMENTO,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.IE_TIPO_ATENDIMENTO,1,4000), substr(NEW.IE_TIPO_ATENDIMENTO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_TIPO_ATENDIMENTO', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.CD_INTERNO,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.CD_INTERNO,1,4000), substr(NEW.CD_INTERNO,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_INTERNO', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.IE_CLINICA,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.IE_CLINICA,1,4000), substr(NEW.IE_CLINICA,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_CLINICA', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.CD_MEDICO_EXEC_TISS,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.CD_MEDICO_EXEC_TISS,1,4000), substr(NEW.CD_MEDICO_EXEC_TISS,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_MEDICO_EXEC_TISS', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.CD_MEDICO_EXECUTOR,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.CD_MEDICO_EXECUTOR,1,4000), substr(NEW.CD_MEDICO_EXECUTOR,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_MEDICO_EXECUTOR', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.DS_PRESTADOR_TISS,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.DS_PRESTADOR_TISS,1,4000), substr(NEW.DS_PRESTADOR_TISS,1,4000), NEW.nm_usuario, nr_seq_w, 'DS_PRESTADOR_TISS', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.CD_GRUPO_PROC,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.CD_GRUPO_PROC,1,4000), substr(NEW.CD_GRUPO_PROC,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_GRUPO_PROC', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.CD_PROCEDIMENTO,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.CD_PROCEDIMENTO,1,4000), substr(NEW.CD_PROCEDIMENTO,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_PROCEDIMENTO', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.IE_ORIGEM_PROCED,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.IE_ORIGEM_PROCED,1,4000), substr(NEW.IE_ORIGEM_PROCED,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_ORIGEM_PROCED', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.CD_CGC_SOLIC_PRESCR,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.CD_CGC_SOLIC_PRESCR,1,4000), substr(NEW.CD_CGC_SOLIC_PRESCR,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_CGC_SOLIC_PRESCR', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.IE_PRESTADOR,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.IE_PRESTADOR,1,4000), substr(NEW.IE_PRESTADOR,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_PRESTADOR', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.IE_ESCALA,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.IE_ESCALA,1,4000), substr(NEW.IE_ESCALA,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_ESCALA', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.DS_OBSERVACAO,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.DS_OBSERVACAO,1,4000), substr(NEW.DS_OBSERVACAO,1,4000), NEW.nm_usuario, nr_seq_w, 'DS_OBSERVACAO', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.CD_CGC_PRESTADOR,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.CD_CGC_PRESTADOR,1,4000), substr(NEW.CD_CGC_PRESTADOR,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_CGC_PRESTADOR', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.CD_CONVENIO,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.CD_CONVENIO,1,4000), substr(NEW.CD_CONVENIO,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_CONVENIO', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.IE_GUIA_RI,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.IE_GUIA_RI,1,4000), substr(NEW.IE_GUIA_RI,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_GUIA_RI', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.CD_SETOR_ATENDIMENTO,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.CD_SETOR_ATENDIMENTO,1,4000), substr(NEW.CD_SETOR_ATENDIMENTO,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_SETOR_ATENDIMENTO', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.IE_TIPO_REGRA,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.IE_TIPO_REGRA,1,4000), substr(NEW.IE_TIPO_REGRA,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_TIPO_REGRA', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.IE_SOLICITANTE,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.IE_SOLICITANTE,1,4000), substr(NEW.IE_SOLICITANTE,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_SOLICITANTE', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.IE_GUIA_HONORARIO,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.IE_GUIA_HONORARIO,1,4000), substr(NEW.IE_GUIA_HONORARIO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_GUIA_HONORARIO', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.CD_CATEGORIA,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.CD_CATEGORIA,1,4000), substr(NEW.CD_CATEGORIA,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_CATEGORIA', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.CD_AREA_PROCEDIMENTO,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.CD_AREA_PROCEDIMENTO,1,4000), substr(NEW.CD_AREA_PROCEDIMENTO,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_AREA_PROCEDIMENTO', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.CD_ESPECIALIDADE,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.CD_ESPECIALIDADE,1,4000), substr(NEW.CD_ESPECIALIDADE,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_ESPECIALIDADE', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.CD_MEDICO_ATEND,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.CD_MEDICO_ATEND,1,4000), substr(NEW.CD_MEDICO_ATEND,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_MEDICO_ATEND', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.IE_MED_EXEC,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.IE_MED_EXEC,1,4000), substr(NEW.IE_MED_EXEC,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_MED_EXEC', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.IE_FATUR_MEDICO,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.IE_FATUR_MEDICO,1,4000), substr(NEW.IE_FATUR_MEDICO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_FATUR_MEDICO', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.CD_CGC_PRESTADOR_TISS,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.CD_CGC_PRESTADOR_TISS,1,4000), substr(NEW.CD_CGC_PRESTADOR_TISS,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_CGC_PRESTADOR_TISS', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.DT_FIM_VIGENCIA,1,4000), substr(NEW.DT_FIM_VIGENCIA,1,4000), NEW.nm_usuario, nr_seq_w, 'DT_FIM_VIGENCIA', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_PROCEDENCIA,1,4000), substr(NEW.CD_PROCEDENCIA,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_PROCEDENCIA', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.DT_INICIO_VIGENCIA,1,4000), substr(NEW.DT_INICIO_VIGENCIA,1,4000), NEW.nm_usuario, nr_seq_w, 'DT_INICIO_VIGENCIA', ie_log_w, ds_w, 'TISS_REGRA_PRESTADOR', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;   exception when others then ds_w:= '1'; end;   END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_tiss_regra_prestador_tp() FROM PUBLIC;

CREATE TRIGGER tiss_regra_prestador_tp
	AFTER UPDATE ON tiss_regra_prestador FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_tiss_regra_prestador_tp();

