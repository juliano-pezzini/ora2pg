-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS valor_dominio_tp ON valor_dominio CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_valor_dominio_tp() RETURNS trigger AS $BODY$
DECLARE nr_seq_w bigint;ds_s_w   varchar(1);ds_c_w   varchar(1);ds_w	   varchar(1);ie_log_w varchar(1);BEGIN BEGIN ds_s_w := null;  ds_c_w:= 'CD_DOMINIO='||to_char(OLD.CD_DOMINIO)||'#@#@VL_DOMINIO='||to_char(OLD.VL_DOMINIO);SELECT * FROM gravar_log_alteracao(substr(OLD.IE_SITUACAO,1,4000), substr(NEW.IE_SITUACAO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_SITUACAO', ie_log_w, ds_w, 'VALOR_DOMINIO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_DOMINIO,1,4000), substr(NEW.CD_DOMINIO,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_DOMINIO', ie_log_w, ds_w, 'VALOR_DOMINIO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.VL_DOMINIO,1,4000), substr(NEW.VL_DOMINIO,1,4000), NEW.nm_usuario, nr_seq_w, 'VL_DOMINIO', ie_log_w, ds_w, 'VALOR_DOMINIO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.DS_VALOR_DOMINIO,1,4000), substr(NEW.DS_VALOR_DOMINIO,1,4000), NEW.nm_usuario, nr_seq_w, 'DS_VALOR_DOMINIO', ie_log_w, ds_w, 'VALOR_DOMINIO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_HTML5,1,4000), substr(NEW.IE_HTML5,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_HTML5', ie_log_w, ds_w, 'VALOR_DOMINIO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.DS_VALOR_DOMINIO_CLIENTE,1,4000), substr(NEW.DS_VALOR_DOMINIO_CLIENTE,1,4000), NEW.nm_usuario, nr_seq_w, 'DS_VALOR_DOMINIO_CLIENTE', ie_log_w, ds_w, 'VALOR_DOMINIO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_DELPHI,1,4000), substr(NEW.IE_DELPHI,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_DELPHI', ie_log_w, ds_w, 'VALOR_DOMINIO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_JAVA,1,4000), substr(NEW.IE_JAVA,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_JAVA', ie_log_w, ds_w, 'VALOR_DOMINIO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_EXP_VALOR_DOMINIO,1,4000), substr(NEW.CD_EXP_VALOR_DOMINIO,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_EXP_VALOR_DOMINIO', ie_log_w, ds_w, 'VALOR_DOMINIO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;   exception when others then ds_w:= '1'; end;   END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_valor_dominio_tp() FROM PUBLIC;

CREATE TRIGGER valor_dominio_tp
	AFTER UPDATE ON valor_dominio FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_valor_dominio_tp();

