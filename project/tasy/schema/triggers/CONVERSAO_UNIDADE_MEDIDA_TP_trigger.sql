-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS conversao_unidade_medida_tp ON conversao_unidade_medida CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_conversao_unidade_medida_tp() RETURNS trigger AS $BODY$
DECLARE nr_seq_w bigint;ds_s_w   varchar(1);ds_c_w   varchar(1);ds_w	   varchar(1);ie_log_w varchar(1);BEGIN BEGIN ds_s_w := null;  ds_c_w:= 'CD_CONVENIO='||to_char(OLD.CD_CONVENIO)||'#@#@CD_UNIDADE_MEDIDA='||to_char(OLD.CD_UNIDADE_MEDIDA);SELECT * FROM gravar_log_alteracao(substr(OLD.CD_CONVENIO,1,4000), substr(NEW.CD_CONVENIO,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_CONVENIO', ie_log_w, ds_w, 'CONVERSAO_UNIDADE_MEDIDA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_UNIDADE_CONVENIO,1,4000), substr(NEW.CD_UNIDADE_CONVENIO,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_UNIDADE_CONVENIO', ie_log_w, ds_w, 'CONVERSAO_UNIDADE_MEDIDA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_UNIDADE_MEDIDA,1,4000), substr(NEW.CD_UNIDADE_MEDIDA,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_UNIDADE_MEDIDA', ie_log_w, ds_w, 'CONVERSAO_UNIDADE_MEDIDA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;   exception when others then ds_w:= '1'; end;   END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_conversao_unidade_medida_tp() FROM PUBLIC;

CREATE TRIGGER conversao_unidade_medida_tp
	AFTER UPDATE ON conversao_unidade_medida FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_conversao_unidade_medida_tp();

