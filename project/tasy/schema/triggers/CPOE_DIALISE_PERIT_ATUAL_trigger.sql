-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS cpoe_dialise_perit_atual ON cpoe_dialise_perit CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_cpoe_dialise_perit_atual() RETURNS trigger AS $BODY$
declare
BEGIN
  BEGIN 
BEGIN 
  if (NEW.hr_inicio_capd is not null) and ((NEW.hr_inicio_capd <> OLD.hr_inicio_capd) or (OLD.dt_inicio_capd is null)) then 
		NEW.dt_inicio_capd := to_date(to_char(coalesce(NEW.dt_inicio, LOCALTIMESTAMP),'dd/mm/yyyy') || ' ' || NEW.hr_inicio_capd,'dd/mm/yyyy hh24:mi');
	end if;
  if (NEW.hr_fim_capd is not null) and ((NEW.hr_fim_capd <> OLD.hr_fim_capd) or (OLD.dt_fim_capd is null)) then	 
		NEW.dt_fim_capd := to_date(to_char(coalesce(NEW.dt_fim, LOCALTIMESTAMP),'dd/mm/yyyy') || ' ' || NEW.hr_fim_capd,'dd/mm/yyyy hh24:mi');
	end if;	
exception 
	when others then 
	null;
end;
 
  END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_cpoe_dialise_perit_atual() FROM PUBLIC;

CREATE TRIGGER cpoe_dialise_perit_atual
	BEFORE INSERT OR UPDATE ON cpoe_dialise_perit FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_cpoe_dialise_perit_atual();

