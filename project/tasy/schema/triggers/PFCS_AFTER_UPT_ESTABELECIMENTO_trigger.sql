-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS pfcs_after_upt_estabelecimento ON estabelecimento CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_pfcs_after_upt_estabelecimento() RETURNS trigger AS $BODY$
DECLARE
ds_log_w    varchar(4000);
qt_reg_w    smallint;

BEGIN
    if (coalesce(wheb_usuario_pck.get_ie_executar_trigger,'S') = 'N')  then
        goto Final;
    end if;

    if (OLD.nm_usuario_nrec = 'PFCS') then
        ds_log_w := (
            concat('cd_estabelecimento: ',          OLD.cd_estabelecimento || CHR(10))
            || concat('old nm_fantasia_estab: ',    OLD.nm_fantasia_estab)
            || concat(' | new nm_fantasia_estab: ', NEW.nm_fantasia_estab || CHR(10))
            || concat('old ie_situacao: ',          coalesce(OLD.ie_situacao,'null'))
            || concat(' | new ie_situacao: ',       coalesce(NEW.ie_situacao,'null'))
        );
        CALL pfcs_log_pck.pfcs_insert_org_struc_log(
            ie_type_p => 'UPDATE',
            nm_table_p => 'ESTABELECIMENTO',
            ds_log_p => ds_log_w,
            nm_usuario_p => NEW.nm_usuario
        );
    end if;

<<Final>>
qt_reg_w := 0;
RETURN NEW;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_pfcs_after_upt_estabelecimento() FROM PUBLIC;

CREATE TRIGGER pfcs_after_upt_estabelecimento
	AFTER UPDATE ON estabelecimento FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_pfcs_after_upt_estabelecimento();

