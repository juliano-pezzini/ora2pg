-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS prot_int_pac_start_date ON protocolo_int_paciente CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_prot_int_pac_start_date() RETURNS trigger AS $BODY$
declare
BEGIN
  BEGIN

  if (wheb_usuario_pck.get_ie_executar_trigger    = 'S') then
      UPDATE protocolo_int_pac_etapa
      SET dt_inicial_previsto = NEW.DT_INICIAL_PREVISTO + nr_dia_inicial - 1,
      dt_final_previsto = NEW.DT_INICIAL_PREVISTO + nr_dia_final - 1
      where nr_seq_protocolo_int_pac = NEW.nr_sequencia
      and dt_inicial_previsto is not null
      and nr_dia_inicial is not null;
  end if;

  EXCEPTION
  WHEN no_data_found THEN
      CALL wheb_mensagem_pck.exibir_mensagem_abort(102259);
  WHEN OTHERS THEN
      CALL wheb_mensagem_pck.exibir_mensagem_abort(102259);

  END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_prot_int_pac_start_date() FROM PUBLIC;

CREATE TRIGGER prot_int_pac_start_date
	AFTER INSERT OR UPDATE ON protocolo_int_paciente FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_prot_int_pac_start_date();

