-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS relatorio_atual_java ON relatorio CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_relatorio_atual_java() RETURNS trigger AS $BODY$
BEGIN
	if (TG_OP = 'INSERT' or (NEW.DT_ATUALIZACAO <> OLD.DT_ATUALIZACAO)) then
		NEW.IE_GERAR_RELATORIO := 'S';
		NEW.DT_LAST_MODIFICATION := LOCALTIMESTAMP;
	end if;

	if (TG_OP = 'INSERT' or (NEW.ie_gerar_relatorio = 'S' and coalesce(OLD.ie_gerar_relatorio,'N') != 'S')) then

		select nextval('cd_version_relat_seq')
		into STRICT NEW.cd_version
		;

	end if;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_relatorio_atual_java() FROM PUBLIC;

CREATE TRIGGER relatorio_atual_java
	BEFORE INSERT OR UPDATE ON relatorio FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_relatorio_atual_java();

