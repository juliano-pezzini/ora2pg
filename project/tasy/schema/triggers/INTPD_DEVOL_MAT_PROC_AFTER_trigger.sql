-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS intpd_devol_mat_proc_after ON intpd_devol_mat_proc CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_intpd_devol_mat_proc_after() RETURNS trigger AS $BODY$
declare

reg_integracao_p	gerar_int_padrao.reg_integracao;

BEGIN

	if (NEW.nr_seq_propaci is not null) then

		reg_integracao_p.ie_operacao				:= 'I';
		reg_integracao_p.cd_estab_documento 		:= wheb_usuario_pck.get_cd_estabelecimento();
		reg_integracao_p.ie_status					:= 'P';
		reg_integracao_p.nr_seq_item_documento_p 	:= '4';
		reg_integracao_p.nr_seq_agrupador			:= null;

		reg_integracao_p := gerar_int_padrao.gravar_integracao('161', NEW.nr_sequencia, NEW.nm_usuario, reg_integracao_p);

	end if;

	if (NEW.nr_seq_matpaci is not null) then

		reg_integracao_p.ie_operacao				:= 'I';
		reg_integracao_p.cd_estab_documento 		:= wheb_usuario_pck.get_cd_estabelecimento();
		reg_integracao_p.ie_status					:= 'P';
		reg_integracao_p.nr_seq_item_documento_p 	:= '3';
		reg_integracao_p.nr_seq_agrupador			:= null;

		reg_integracao_p := gerar_int_padrao.gravar_integracao('161', NEW.nr_sequencia, NEW.nm_usuario, reg_integracao_p);

	end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_intpd_devol_mat_proc_after() FROM PUBLIC;

CREATE TRIGGER intpd_devol_mat_proc_after
	AFTER INSERT ON intpd_devol_mat_proc FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_intpd_devol_mat_proc_after();

