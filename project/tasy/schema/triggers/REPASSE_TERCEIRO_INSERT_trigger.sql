-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS repasse_terceiro_insert ON repasse_terceiro CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_repasse_terceiro_insert() RETURNS trigger AS $BODY$
declare
cd_condicao_pagamento_w	bigint;
nr_seq_tipo_repasse_w	bigint;
BEGIN

select	max(cd_condicao_pagamento),
	max(nr_seq_tipo_rep)
into STRICT	cd_condicao_pagamento_w,
	nr_seq_tipo_repasse_w
from	terceiro
where	nr_sequencia		= NEW.nr_seq_terceiro;

if (cd_condicao_pagamento_w is not null) and (NEW.cd_condicao_pagamento is null) then
	NEW.cd_condicao_pagamento := cd_condicao_pagamento_w;
end if;

if (nr_seq_tipo_repasse_w is not null) and (NEW.nr_seq_tipo is null) then
	NEW.nr_seq_tipo := nr_seq_tipo_repasse_w;
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_repasse_terceiro_insert() FROM PUBLIC;

CREATE TRIGGER repasse_terceiro_insert
	BEFORE INSERT ON repasse_terceiro FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_repasse_terceiro_insert();

