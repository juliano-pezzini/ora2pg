-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS reg_escopo_customer_before ON reg_escopo_customer_req CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_reg_escopo_customer_before() RETURNS trigger AS $BODY$
declare

qt_test_case_scope_w			bigint;

BEGIN

select	count(1)
into STRICT	qt_test_case_scope_w
from	reg_caso_teste ct,
	reg_escopo_caso_teste ect
where	ct.nr_sequencia = ect.nr_seq_caso_teste
and	ct.nr_seq_customer = OLD.nr_customer_req
and	ect.nr_seq_escopo = OLD.nr_seq_escopo;

if (qt_test_case_scope_w > 0) then
	wheb_mensagem_pck.exibir_mensagem_abort(1016417); -- Este escopo já está incluso nos casos de teste deste requisito de cliente. A exclusão deste deve ser verificada com o time de Validação.
end if;

RETURN OLD;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_reg_escopo_customer_before() FROM PUBLIC;

CREATE TRIGGER reg_escopo_customer_before
	BEFORE DELETE ON reg_escopo_customer_req FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_reg_escopo_customer_before();

