-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS paciente_home_care_tp ON paciente_home_care CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_paciente_home_care_tp() RETURNS trigger AS $BODY$
DECLARE nr_seq_w bigint;ds_s_w   varchar(1);ds_c_w   varchar(1);ds_w	   varchar(1);ie_log_w varchar(1);BEGIN BEGIN ds_s_w := to_char(OLD.NR_SEQUENCIA);  ds_c_w:=null; ds_w:=substr(NEW.NR_SEQ_CLASSIFICACAO,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.NR_SEQ_CLASSIFICACAO,1,4000), substr(NEW.NR_SEQ_CLASSIFICACAO,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_SEQ_CLASSIFICACAO', ie_log_w, ds_w, 'PACIENTE_HOME_CARE', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(to_char(OLD.DT_INICIO,'dd/mm/yyyy hh24:mi:ss'), to_char(NEW.DT_INICIO,'dd/mm/yyyy hh24:mi:ss'), NEW.nm_usuario, nr_seq_w, 'DT_INICIO', ie_log_w, ds_w, 'PACIENTE_HOME_CARE', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(to_char(OLD.DT_FINAL,'dd/mm/yyyy hh24:mi:ss'), to_char(NEW.DT_FINAL,'dd/mm/yyyy hh24:mi:ss'), NEW.nm_usuario, nr_seq_w, 'DT_FINAL', ie_log_w, ds_w, 'PACIENTE_HOME_CARE', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_PESSOA_FISICA,1,4000), substr(NEW.CD_PESSOA_FISICA,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_PESSOA_FISICA', ie_log_w, ds_w, 'PACIENTE_HOME_CARE', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_SITUACAO,1,4000), substr(NEW.IE_SITUACAO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_SITUACAO', ie_log_w, ds_w, 'PACIENTE_HOME_CARE', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.NR_SEQ_ORIGEM,1,4000), substr(NEW.NR_SEQ_ORIGEM,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_SEQ_ORIGEM', ie_log_w, ds_w, 'PACIENTE_HOME_CARE', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.DS_OBSERVACAO,1,4000), substr(NEW.DS_OBSERVACAO,1,4000), NEW.nm_usuario, nr_seq_w, 'DS_OBSERVACAO', ie_log_w, ds_w, 'PACIENTE_HOME_CARE', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.QT_KM,1,4000), substr(NEW.QT_KM,1,4000), NEW.nm_usuario, nr_seq_w, 'QT_KM', ie_log_w, ds_w, 'PACIENTE_HOME_CARE', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.QT_MIN_ATEND,1,4000), substr(NEW.QT_MIN_ATEND,1,4000), NEW.nm_usuario, nr_seq_w, 'QT_MIN_ATEND', ie_log_w, ds_w, 'PACIENTE_HOME_CARE', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_MEDICO_INDICOU,1,4000), substr(NEW.CD_MEDICO_INDICOU,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_MEDICO_INDICOU', ie_log_w, ds_w, 'PACIENTE_HOME_CARE', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_MEDICO_RESP,1,4000), substr(NEW.CD_MEDICO_RESP,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_MEDICO_RESP', ie_log_w, ds_w, 'PACIENTE_HOME_CARE', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.NR_SEQ_STATUS_PAC,1,4000), substr(NEW.NR_SEQ_STATUS_PAC,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_SEQ_STATUS_PAC', ie_log_w, ds_w, 'PACIENTE_HOME_CARE', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_CLASSIFICACAO,1,4000), substr(NEW.IE_CLASSIFICACAO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_CLASSIFICACAO', ie_log_w, ds_w, 'PACIENTE_HOME_CARE', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.NR_SEQ_ENTIDADE,1,4000), substr(NEW.NR_SEQ_ENTIDADE,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_SEQ_ENTIDADE', ie_log_w, ds_w, 'PACIENTE_HOME_CARE', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;   exception when others then ds_w:= '1'; end;   END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_paciente_home_care_tp() FROM PUBLIC;

CREATE TRIGGER paciente_home_care_tp
	AFTER UPDATE ON paciente_home_care FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_paciente_home_care_tp();

