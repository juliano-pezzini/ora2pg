-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS convenio_estrut_atrib_atual ON convenio_estrut_atrib CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_convenio_estrut_atrib_atual() RETURNS trigger AS $BODY$
BEGIN

CALL exec_sql_dinamico('',
  ' UPDATE CONVENIO_ESTRUTURA_CONTA '||
  ' SET   IE_GERAR_RELATORIO = ''S'' ,'||
  '    DT_ATUALIZACAO     = SYSDATE,'||
  '    NM_USUARIO         = '|| chr(39) || coalesce(NEW.NM_USUARIO,OLD.NM_USUARIO) || chr(39) ||
  ' WHERE 	NR_SEQUENCIA 	     = '|| coalesce(NEW.NR_SEQUENCIA,OLD.NR_SEQUENCIA));
IF TG_OP = 'DELETE' THEN
	RETURN OLD;
ELSE
	RETURN NEW;
END IF;

END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_convenio_estrut_atrib_atual() FROM PUBLIC;

CREATE TRIGGER convenio_estrut_atrib_atual
	BEFORE INSERT OR UPDATE OR DELETE ON convenio_estrut_atrib FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_convenio_estrut_atrib_atual();

