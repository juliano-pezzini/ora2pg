-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS cot_regra_consist_calc_atual ON cot_regra_consist_calc CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_cot_regra_consist_calc_atual() RETURNS trigger AS $BODY$
declare
BEGIN
if (wheb_usuario_pck.get_ie_executar_trigger = 'S') then
	if (NEW.IE_TIPO_REGRA in ('11', '16')) and (NEW.CD_TAB_PRECO_MAT is null) then
		CALL wheb_mensagem_pck.Exibir_Mensagem_Abort(251652);
	end if;

	if (NEW.IE_TIPO_REGRA in ('1','8','9','15')) and (NEW.vl_parametro is null) then
		CALL wheb_mensagem_pck.Exibir_Mensagem_Abort(251653);
	end if;

	if (NEW.IE_TIPO_REGRA not in ('1','8','9','15', '16')) and (NEW.vl_parametro is not null) then
		CALL wheb_mensagem_pck.Exibir_Mensagem_Abort(251654);
	end if;
end if;
RETURN NEW;
end;
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_cot_regra_consist_calc_atual() FROM PUBLIC;

CREATE TRIGGER cot_regra_consist_calc_atual
	BEFORE INSERT OR UPDATE ON cot_regra_consist_calc FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_cot_regra_consist_calc_atual();

