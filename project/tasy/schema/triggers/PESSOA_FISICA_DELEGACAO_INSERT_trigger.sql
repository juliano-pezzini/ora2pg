-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS pessoa_fisica_delegacao_insert ON pessoa_fisica_delegacao CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_pessoa_fisica_delegacao_insert() RETURNS trigger AS $BODY$
DECLARE
 
ds_log_w		varchar(4000);
 
BEGIN 
 
ds_log_w 	:= substr(wheb_mensagem_pck.get_texto(795921,'DS_OBJETIVO_W=' || obter_valor_dominio(2106,NEW.ie_objetivo) || ';' 
				|| 'DS_PESSOA_FISICA_W=' || NEW.cd_pessoa_fisica 	|| ' - ' || obter_nome_pf(NEW.cd_pessoa_fisica) || ';' 
				|| 'DS_SUBSTITUTO_W=' || NEW.cd_pessoa_substituta 	|| ' - ' || obter_nome_pf(NEW.cd_pessoa_substituta) || ';' 
				|| 'DT_LIMITE_W=' || NEW.dt_limite),1,4000);
		 
insert into pessoa_fisica_deleg_log( 
	nr_sequencia, 
	dt_atualizacao, 
	nm_usuario, 
	dt_atualizacao_nrec, 
	nm_usuario_nrec, 
	ds_titulo, 
	ds_log, 
	cd_pessoa_substituida, 
	cd_pessoa_delegada) 
values (	nextval('pessoa_fisica_deleg_log_seq'), 
	LOCALTIMESTAMP, 
	NEW.nm_usuario, 
	LOCALTIMESTAMP, 
	NEW.nm_usuario, 
	wheb_mensagem_pck.get_texto(795926), 
	ds_log_w, 
	NEW.cd_pessoa_fisica, 
	NEW.cd_pessoa_substituta);
			 
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_pessoa_fisica_delegacao_insert() FROM PUBLIC;

CREATE TRIGGER pessoa_fisica_delegacao_insert
	BEFORE INSERT ON pessoa_fisica_delegacao FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_pessoa_fisica_delegacao_insert();

