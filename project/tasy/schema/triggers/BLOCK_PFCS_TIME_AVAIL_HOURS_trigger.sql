-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS block_pfcs_time_avail_hours ON pfcs_time_available_hours CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_block_pfcs_time_avail_hours() RETURNS trigger AS $BODY$
DECLARE

records_quantity_w			integer;

BEGIN

    select	count(*)
	into STRICT	records_quantity_w
	from 	pfcs_time_available_hours;

  if (NEW.ds_rule is not null and records_quantity_w > 0)  then
      CALL wheb_mensagem_pck.Exibir_Mensagem_Abort(1139766);
  end if;

RETURN NEW;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_block_pfcs_time_avail_hours() FROM PUBLIC;

CREATE TRIGGER block_pfcs_time_avail_hours
	BEFORE INSERT ON pfcs_time_available_hours FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_block_pfcs_time_avail_hours();

