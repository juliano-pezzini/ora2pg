-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS reg_tc_so_pendencies_before ON reg_tc_so_pendencies CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_reg_tc_so_pendencies_before() RETURNS trigger AS $BODY$
declare

ie_classificacao_cliente_w	man_ordem_servico.ie_classificacao_cliente%type;

BEGIN

	if (phi_is_base_philips = 'S') then
		BEGIN
			select	max(mos.ie_classificacao_cliente)
			into STRICT	ie_classificacao_cliente_w
			from	man_ordem_servico mos
			where	mos.nr_sequencia = NEW.nr_seq_service_order;
			
			if (coalesce(ie_classificacao_cliente_w, 'X') <> 'A') then
				BEGIN
					CALL wheb_mensagem_pck.exibir_mensagem_abort(1131764);
				end;
			end if;
		end;
	end if;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_reg_tc_so_pendencies_before() FROM PUBLIC;

CREATE TRIGGER reg_tc_so_pendencies_before
	BEFORE INSERT OR UPDATE ON reg_tc_so_pendencies FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_reg_tc_so_pendencies_before();

