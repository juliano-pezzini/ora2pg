-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS xml_atributo_atual ON xml_atributo CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_xml_atributo_atual() RETURNS trigger AS $BODY$
declare
BEGIN
  BEGIN

BEGIN
if (NEW.nm_atributo_xml <> OLD.nm_atributo_xml) or
	(OLD.nm_atributo_xml_pesquisa is null AND NEW.nm_atributo_xml  is not null) then
	NEW.nm_atributo_xml_pesquisa	:= upper(NEW.nm_atributo_xml);
end if;
exception
when others then
	NEW.nm_atributo_xml_pesquisa	:= null;
end;

  END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_xml_atributo_atual() FROM PUBLIC;

CREATE TRIGGER xml_atributo_atual
	BEFORE INSERT OR UPDATE ON xml_atributo FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_xml_atributo_atual();

