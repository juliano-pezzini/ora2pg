-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS servidor_integracao_tp ON servidor_integracao CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_servidor_integracao_tp() RETURNS trigger AS $BODY$
DECLARE nr_seq_w bigint;ds_s_w   varchar(1);ds_c_w   varchar(1);ds_w	   varchar(1);ie_log_w varchar(1);BEGIN BEGIN ds_s_w := to_char(OLD.NR_SEQUENCIA);  ds_c_w:=null; ds_w:=substr(NEW.NR_PORTA,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.NR_PORTA,1,4000), substr(NEW.NR_PORTA,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_PORTA', ie_log_w, ds_w, 'SERVIDOR_INTEGRACAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.DS_INFORMACAO,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.DS_INFORMACAO,1,4000), substr(NEW.DS_INFORMACAO,1,4000), NEW.nm_usuario, nr_seq_w, 'DS_INFORMACAO', ie_log_w, ds_w, 'SERVIDOR_INTEGRACAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.IE_SERVIDOR,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.IE_SERVIDOR,1,4000), substr(NEW.IE_SERVIDOR,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_SERVIDOR', ie_log_w, ds_w, 'SERVIDOR_INTEGRACAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.IP_CONEXAO,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.IP_CONEXAO,1,4000), substr(NEW.IP_CONEXAO,1,4000), NEW.nm_usuario, nr_seq_w, 'IP_CONEXAO', ie_log_w, ds_w, 'SERVIDOR_INTEGRACAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;  ds_w:=substr(NEW.NR_PORTA_LISTENER,1,500);SELECT * FROM gravar_log_alteracao(substr(OLD.NR_PORTA_LISTENER,1,4000), substr(NEW.NR_PORTA_LISTENER,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_PORTA_LISTENER', ie_log_w, ds_w, 'SERVIDOR_INTEGRACAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;   exception when others then ds_w:= '1'; end;   END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_servidor_integracao_tp() FROM PUBLIC;

CREATE TRIGGER servidor_integracao_tp
	AFTER UPDATE ON servidor_integracao FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_servidor_integracao_tp();

