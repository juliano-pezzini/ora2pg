-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS regra_envio_sms_agenda_atual ON regra_envio_sms_agenda CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_regra_envio_sms_agenda_atual() RETURNS trigger AS $BODY$
declare

BEGIN

if (NEW.ie_classif_agenda is not null) then
	if (NEW.ie_tipo_agendamento is not null) then
		if (NEW.ie_tipo_agendamento <> 'S' AND NEW.ie_tipo_agendamento <> 'C') then
			-- "A classificação é permitida somente para agendas de consulta e serviço."
			CALL Wheb_mensagem_pck.exibir_mensagem_abort(1045925);
		end if;
	end if;
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_regra_envio_sms_agenda_atual() FROM PUBLIC;

CREATE TRIGGER regra_envio_sms_agenda_atual
	BEFORE INSERT OR UPDATE ON regra_envio_sms_agenda FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_regra_envio_sms_agenda_atual();

