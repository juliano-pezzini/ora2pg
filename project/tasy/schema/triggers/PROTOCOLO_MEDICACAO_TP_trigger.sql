-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS protocolo_medicacao_tp ON protocolo_medicacao CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_protocolo_medicacao_tp() RETURNS trigger AS $BODY$
DECLARE nr_seq_w bigint;ds_s_w   varchar(1);ds_c_w   varchar(1);ds_w	   varchar(1);ie_log_w varchar(1);BEGIN BEGIN ds_s_w := null;  ds_c_w:= 'CD_PROTOCOLO='||to_char(OLD.CD_PROTOCOLO)||'#@#@NR_SEQUENCIA='||to_char(OLD.NR_SEQUENCIA);SELECT * FROM gravar_log_alteracao(substr(OLD.NR_CICLOS,1,4000), substr(NEW.NR_CICLOS,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_CICLOS', ie_log_w, ds_w, 'PROTOCOLO_MEDICACAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_PROTOCOLO,1,4000), substr(NEW.CD_PROTOCOLO,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_PROTOCOLO', ie_log_w, ds_w, 'PROTOCOLO_MEDICACAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.NM_USUARIO,1,4000), substr(NEW.NM_USUARIO,1,4000), NEW.nm_usuario, nr_seq_w, 'NM_USUARIO', ie_log_w, ds_w, 'PROTOCOLO_MEDICACAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.DT_ATUALIZACAO,1,4000), substr(NEW.DT_ATUALIZACAO,1,4000), NEW.nm_usuario, nr_seq_w, 'DT_ATUALIZACAO', ie_log_w, ds_w, 'PROTOCOLO_MEDICACAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.NR_SEQUENCIA,1,4000), substr(NEW.NR_SEQUENCIA,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_SEQUENCIA', ie_log_w, ds_w, 'PROTOCOLO_MEDICACAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;   exception when others then ds_w:= '1'; end;   END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_protocolo_medicacao_tp() FROM PUBLIC;

CREATE TRIGGER protocolo_medicacao_tp
	AFTER UPDATE ON protocolo_medicacao FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_protocolo_medicacao_tp();

