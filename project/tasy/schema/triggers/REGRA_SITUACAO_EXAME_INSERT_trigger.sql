-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS regra_situacao_exame_insert ON regra_situacao_exame CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_regra_situacao_exame_insert() RETURNS trigger AS $BODY$
declare

nr_sequencia_w	bigint := 0;

BEGIN

select obter_situacao_exame_lm(NEW.CD_ESTABELECIMENTO,NEW.IE_STATUS_EXECUCAO,NEW.IE_LISTA_CENTRAL_MEDICA,NEW.NR_SEQ_SITUACAO_PROC,NEW.IE_EXAME_TRAZIDO,NEW.IE_EXAME_ANTERIOR,NEW.IE_EXAME_IMPORTANTE,NEW.IE_EXAME_IMPORTADO,NEW.IE_EXAME_COMPLEMENTAR,NEW.CD_MEDICO_PREFERENCIAL,NEW.CD_ESPECIALIDADE) into STRICT nr_sequencia_w;

if (nr_sequencia_w <> 0) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(1133489);
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_regra_situacao_exame_insert() FROM PUBLIC;

CREATE TRIGGER regra_situacao_exame_insert
	BEFORE INSERT ON regra_situacao_exame FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_regra_situacao_exame_insert();

