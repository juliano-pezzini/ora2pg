-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS regra_liberacao_contrato_tp ON regra_liberacao_contrato CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_regra_liberacao_contrato_tp() RETURNS trigger AS $BODY$
DECLARE nr_seq_w bigint;ds_s_w   varchar(1);ds_c_w   varchar(1);ds_w	   varchar(1);ie_log_w varchar(1);BEGIN BEGIN ds_s_w := to_char(OLD.NR_SEQUENCIA);  ds_c_w:=null;SELECT * FROM gravar_log_alteracao(substr(OLD.DS_REGRA_LIBERACAO,1,4000), substr(NEW.DS_REGRA_LIBERACAO,1,4000), NEW.nm_usuario, nr_seq_w, 'DS_REGRA_LIBERACAO', ie_log_w, ds_w, 'REGRA_LIBERACAO_CONTRATO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_SITUACAO,1,4000), substr(NEW.IE_SITUACAO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_SITUACAO', ie_log_w, ds_w, 'REGRA_LIBERACAO_CONTRATO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_PERMITE_ACESSO,1,4000), substr(NEW.IE_PERMITE_ACESSO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_PERMITE_ACESSO', ie_log_w, ds_w, 'REGRA_LIBERACAO_CONTRATO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_AVISO_VENCIMENTO,1,4000), substr(NEW.IE_AVISO_VENCIMENTO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_AVISO_VENCIMENTO', ie_log_w, ds_w, 'REGRA_LIBERACAO_CONTRATO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_AVISO_REVISAO,1,4000), substr(NEW.IE_AVISO_REVISAO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_AVISO_REVISAO', ie_log_w, ds_w, 'REGRA_LIBERACAO_CONTRATO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_GRUPO_PADRAO,1,4000), substr(NEW.IE_GRUPO_PADRAO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_GRUPO_PADRAO', ie_log_w, ds_w, 'REGRA_LIBERACAO_CONTRATO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;   exception when others then ds_w:= '1'; end;   END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_regra_liberacao_contrato_tp() FROM PUBLIC;

CREATE TRIGGER regra_liberacao_contrato_tp
	AFTER UPDATE ON regra_liberacao_contrato FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_regra_liberacao_contrato_tp();

