-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS regra_aloc_custo_setor_insert ON regra_aloc_custo_setor CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_regra_aloc_custo_setor_insert() RETURNS trigger AS $BODY$
declare

BEGIN

if (NEW.ie_calculo_custo = 'F') then
	BEGIN
	if (NEW.cd_grupo_material is not null) or (NEW.cd_subgrupo_material is not null) or (NEW.cd_classe_material is not null) or (NEW.cd_material is not null) then
		/*'Para Fórmula de cálculo: Custo baseado em Tabela de preço AMB,' || chr(13) || 'deve ser informado somente estrutura de  procedimento!#@#@');*/

		CALL wheb_mensagem_pck.exibir_mensagem_abort(266374);
	end if;
	end;
elsif (NEW.cd_edicao_amb is not null) then
	NEW.cd_edicao_amb	:= null;
end if;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_regra_aloc_custo_setor_insert() FROM PUBLIC;

CREATE TRIGGER regra_aloc_custo_setor_insert
	BEFORE INSERT OR UPDATE ON regra_aloc_custo_setor FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_regra_aloc_custo_setor_insert();

