-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS man_regra_versao_build_before ON man_regra_versao_build CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_man_regra_versao_build_before() RETURNS trigger AS $BODY$
declare

BEGIN

if (NEW.ie_regra  = 'F' and NEW.cd_versao is null) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(1017739, null);
end if;

if (NEW.ie_regra  = 'V' and NEW.qt_versoes is null) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(1017739, null);
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_man_regra_versao_build_before() FROM PUBLIC;

CREATE TRIGGER man_regra_versao_build_before
	BEFORE INSERT OR UPDATE ON man_regra_versao_build FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_man_regra_versao_build_before();

