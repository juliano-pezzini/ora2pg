-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS proc_criterio_repasse_insert ON proc_criterio_repasse CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_proc_criterio_repasse_insert() RETURNS trigger AS $BODY$
DECLARE

BEGIN

if (NEW.ie_forma_calculo <> 'I') and
	((coalesce(NEW.tx_procedimento,0) <> 0) and
	 ((coalesce(NEW.TX_MEDICO,0) <> 0) or (coalesce(NEW.TX_ANESTESISTA,0) <> 0) or (coalesce(NEW.TX_MATERIAIS,0) <> 0) or (coalesce(NEW.TX_AUXILIARES,0)  <> 0))) then
	--r.aise_application_error(-20011, 'Não é permitido cadastrar um critério com taxa de procedimento junto com as demais taxas!');
	CALL wheb_mensagem_pck.exibir_mensagem_abort(176341);
end if;

RETURN NEW;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_proc_criterio_repasse_insert() FROM PUBLIC;

CREATE TRIGGER proc_criterio_repasse_insert
	BEFORE INSERT OR UPDATE ON proc_criterio_repasse FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_proc_criterio_repasse_insert();

