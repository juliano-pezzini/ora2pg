-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS procedimento_paciente_afterupd ON procedimento_paciente CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_procedimento_paciente_afterupd() RETURNS trigger AS $BODY$
declare

qt_reg_w				smallint;

BEGIN

if (wheb_usuario_pck.get_ie_executar_trigger = 'N')  then
	goto final;
end if;

if (NEW.ie_origem_proced = '11') and --Somente 11-OPS interfere na DRG
	(OLD.nr_interno_conta is null) and (NEW.nr_interno_conta is not null) then --Se o item foi 
	CALL INATIVAR_DRG_EPISODIO_PACIENTE(NEW.nr_atendimento, NEW.cd_procedimento, 'PROCEDIMENTO_PACIENTE', NEW.nm_usuario, 'TPP');
end if;


<<final>>

qt_reg_w	:= 0;
RETURN NEW;
end;
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_procedimento_paciente_afterupd() FROM PUBLIC;

CREATE TRIGGER procedimento_paciente_afterupd
	AFTER UPDATE ON procedimento_paciente FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_procedimento_paciente_afterupd();

