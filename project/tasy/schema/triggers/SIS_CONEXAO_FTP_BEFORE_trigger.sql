-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS sis_conexao_ftp_before ON sis_conexao_ftp CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_sis_conexao_ftp_before() RETURNS trigger AS $BODY$
BEGIN

  if (wheb_usuario_pck.get_ie_executar_trigger = 'S') then
    if (coalesce(NEW.ds_senha, 'XX') <> coalesce(OLD.ds_senha, 'XX')) then
      NEW.ds_senha := wheb_seguranca.encrypt(NEW.ds_senha, 'XxSisConexaoFtpxX');
      NEW.ie_atualizado_seg := 'S';
    end if;
  end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_sis_conexao_ftp_before() FROM PUBLIC;

CREATE TRIGGER sis_conexao_ftp_before
	BEFORE INSERT OR UPDATE ON sis_conexao_ftp FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_sis_conexao_ftp_before();

