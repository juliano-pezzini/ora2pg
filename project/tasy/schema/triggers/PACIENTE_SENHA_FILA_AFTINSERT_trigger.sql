-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS paciente_senha_fila_aftinsert ON paciente_senha_fila CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_paciente_senha_fila_aftinsert() RETURNS trigger AS $BODY$
declare
nr_seq_mov_senha_w	bigint;

BEGIN

select	nextval('movimentacao_senha_fila_seq')
into STRICT	nr_seq_mov_senha_w
;

insert	into movimentacao_senha_fila(
	nr_sequencia,
	dt_atualizacao,
	nm_usuario,
	dt_atualizacao_nrec,
	nm_usuario_nrec,
	nr_seq_pac_senha_fila,
	nr_seq_fila_espera,
	dt_entrada)
values (nr_seq_mov_senha_w,
	LOCALTIMESTAMP,
	NEW.nm_usuario,
	LOCALTIMESTAMP,
	NEW.nm_usuario,
	NEW.nr_sequencia,
	NEW.nr_seq_fila_senha,
	LOCALTIMESTAMP);

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_paciente_senha_fila_aftinsert() FROM PUBLIC;

CREATE TRIGGER paciente_senha_fila_aftinsert
	AFTER INSERT ON paciente_senha_fila FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_paciente_senha_fila_aftinsert();

