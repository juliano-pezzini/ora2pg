-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS pcs_atributo_beforeinsert ON pcs_atributo CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_pcs_atributo_beforeinsert() RETURNS trigger AS $BODY$
declare
qt_existe_macro_w bigint := 0;

pragma autonomous_transaction;

BEGIN

select	count(*) qt_registro
into STRICT	qt_existe_macro_w
from	pcs_atributo
where	upper(ds_macro) = upper(NEW.ds_macro)
and		nr_sequencia <> NEW.nr_sequencia;

if (qt_existe_macro_w > 0) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(277466);
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_pcs_atributo_beforeinsert() FROM PUBLIC;

CREATE TRIGGER pcs_atributo_beforeinsert
	BEFORE INSERT ON pcs_atributo FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_pcs_atributo_beforeinsert();

