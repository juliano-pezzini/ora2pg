-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS dis_regra_local_setor_atual ON dis_regra_local_setor CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_dis_regra_local_setor_atual() RETURNS trigger AS $BODY$
declare
BEGIN

if (TG_OP = 'INSERT')then
	CALL gerar_int_dankia_pck.dankia_regra_local_setor(NEW.nr_seq_dis_regra_setor,NEW.cd_local_estoque,'I',NEW.nm_usuario);
elsif (TG_OP = 'DELETE') then
	CALL gerar_int_dankia_pck.dankia_regra_local_setor(OLD.nr_seq_dis_regra_setor,OLD.cd_local_estoque,'E',OLD.nm_usuario);
elsif (TG_OP = 'UPDATE') then
	if (OLD.cd_local_estoque is not null) and (NEW.cd_local_estoque is not null) and (OLD.cd_local_estoque <> NEW.cd_local_estoque) then
		CALL gerar_int_dankia_pck.dankia_regra_local_setor(OLD.nr_seq_dis_regra_setor,OLD.cd_local_estoque,'E',NEW.nm_usuario);
		CALL gerar_int_dankia_pck.dankia_regra_local_setor(NEW.nr_seq_dis_regra_setor,NEW.cd_local_estoque,'I',NEW.nm_usuario);
	end if;
end if;

IF TG_OP = 'DELETE' THEN
	RETURN OLD;
ELSE
	RETURN NEW;
END IF;

end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_dis_regra_local_setor_atual() FROM PUBLIC;

CREATE TRIGGER dis_regra_local_setor_atual
	BEFORE INSERT OR UPDATE OR DELETE ON dis_regra_local_setor FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_dis_regra_local_setor_atual();

