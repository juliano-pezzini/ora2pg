-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS desenv_story_atual ON desenv_story CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_desenv_story_atual() RETURNS trigger AS $BODY$
declare

cd_tipo_w	varchar(3);

BEGIN

if (TG_OP = 'INSERT') then
	cd_tipo_w := 'SC';
else
	cd_tipo_w := 'SU';
end if;

insert into desenv_atividade(
	nr_sequencia,
	nr_epic,
	dt_atualizacao,
	nm_usuario,
	dt_atualizacao_nrec,
	nm_usuario_nrec,
	nr_release,
	nr_release_destino,
	nr_art,
	nr_art_destino,
	cd_tipo,
	nr_feature,
	nr_story,
	cd_status,
	dt_prevista)
values (	nextval('desenv_atividade_seq'),
	null,
	LOCALTIMESTAMP,
	NEW.nm_usuario,
	LOCALTIMESTAMP,
	NEW.nm_usuario,
	null,
	null,
	null,
	null,
	cd_tipo_w,
	null,
	NEW.nr_sequencia,
	null,
	NEW.dt_prevista);

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_desenv_story_atual() FROM PUBLIC;

CREATE TRIGGER desenv_story_atual
	AFTER INSERT OR UPDATE ON desenv_story FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_desenv_story_atual();

