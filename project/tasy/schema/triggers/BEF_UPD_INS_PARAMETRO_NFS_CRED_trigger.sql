-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS bef_upd_ins_parametro_nfs_cred ON parametro_nfs_credito CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_bef_upd_ins_parametro_nfs_cred() RETURNS trigger AS $BODY$
declare
qt_registros_tipo_fatura_w	smallint := 0;

pragma autonomous_transaction;

BEGIN

if (wheb_usuario_pck.get_ie_executar_trigger = 'S') then
	if (TG_OP = 'INSERT') then

		select count(*) qt
		into STRICT qt_registros_tipo_fatura_w
		from parametro_nfs_credito
		where cd_convenio = NEW.cd_convenio
		and cd_estabelecimento = NEW.cd_estabelecimento
		and ie_tipo_fatura = NEW.ie_tipo_fatura;
		
	elsif (TG_OP = 'UPDATE') then
	
		select count(*) qt
		into STRICT qt_registros_tipo_fatura_w
		from parametro_nfs_credito
		where cd_convenio = NEW.cd_convenio
		and cd_estabelecimento = NEW.cd_estabelecimento
		and ie_tipo_fatura = NEW.ie_tipo_fatura
		and nr_sequencia <> NEW.nr_sequencia;
	
	end if;
	
	if (qt_registros_tipo_fatura_w > 0) then
		CALL wheb_mensagem_pck.exibir_mensagem_abort(1169838);
	end if;

end if;


RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_bef_upd_ins_parametro_nfs_cred() FROM PUBLIC;

CREATE TRIGGER bef_upd_ins_parametro_nfs_cred
	BEFORE INSERT OR UPDATE ON parametro_nfs_credito FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_bef_upd_ins_parametro_nfs_cred();

