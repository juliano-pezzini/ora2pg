-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS pix_movimento_tp ON pix_movimento CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_pix_movimento_tp() RETURNS trigger AS $BODY$
DECLARE nr_seq_w bigint;ds_s_w   varchar(1);ds_c_w   varchar(1);ds_w	   varchar(1);ie_log_w varchar(1);BEGIN BEGIN ds_s_w := to_char(OLD.NR_SEQUENCIA);  ds_c_w:=null;SELECT * FROM gravar_log_alteracao(substr(OLD.DS_ENDTOEND_ID,1,4000), substr(NEW.DS_ENDTOEND_ID,1,4000), NEW.nm_usuario, nr_seq_w, 'DS_ENDTOEND_ID', ie_log_w, ds_w, 'PIX_MOVIMENTO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.VL_MOVIMENTO,1,4000), substr(NEW.VL_MOVIMENTO,1,4000), NEW.nm_usuario, nr_seq_w, 'VL_MOVIMENTO', ie_log_w, ds_w, 'PIX_MOVIMENTO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.DS_TXID,1,4000), substr(NEW.DS_TXID,1,4000), NEW.nm_usuario, nr_seq_w, 'DS_TXID', ie_log_w, ds_w, 'PIX_MOVIMENTO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;   exception when others then ds_w:= '1'; end;   END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_pix_movimento_tp() FROM PUBLIC;

CREATE TRIGGER pix_movimento_tp
	AFTER UPDATE ON pix_movimento FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_pix_movimento_tp();

