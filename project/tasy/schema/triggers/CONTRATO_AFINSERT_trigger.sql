-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS contrato_afinsert ON contrato CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_contrato_afinsert() RETURNS trigger AS $BODY$
declare
ds_retorno_integracao_w	text := null;

BEGIN
if (wheb_usuario_pck.get_ie_executar_trigger = 'S') then
if (OLD.dt_aprovacao is null) and (OLD.dt_reprovacao is null) and (NEW.dt_aprovacao is not null) then
	select	bifrost.send_integration(
		'contract.control.send.request',
		'com.philips.tasy.integration.contractcontrol.outbound.ContractControlCallback',
		''||NEW.nr_sequencia||'',
		NEW.nm_usuario)
	into STRICT	ds_retorno_integracao_w
	;
end if;
end if;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_contrato_afinsert() FROM PUBLIC;

CREATE TRIGGER contrato_afinsert
	AFTER INSERT OR UPDATE ON contrato FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_contrato_afinsert();

