-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS titulo_pagar_resp_pagto_atual ON titulo_pagar_resp_pagto CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_titulo_pagar_resp_pagto_atual() RETURNS trigger AS $BODY$
DECLARE

BEGIN

if (wheb_usuario_pck.get_ie_executar_trigger = 'S') then
if (NEW.cd_pessoa_fisica is null) and (NEW.cd_cgc is null) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(223504);
elsif (NEW.cd_pessoa_fisica is not null) and (NEW.cd_cgc is not null) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(223505);
end if;
end if;
RETURN NEW;
END;
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_titulo_pagar_resp_pagto_atual() FROM PUBLIC;

CREATE TRIGGER titulo_pagar_resp_pagto_atual
	BEFORE INSERT OR UPDATE ON titulo_pagar_resp_pagto FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_titulo_pagar_resp_pagto_atual();

