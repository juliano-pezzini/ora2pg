-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS pls_tipo_item_mens_atual ON pls_tipo_item_mens CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_pls_tipo_item_mens_atual() RETURNS trigger AS $BODY$
declare

BEGIN
if (NEW.cd_procedimento is not null) and (NEW.cd_material is not null) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(262464);
	/* Mensagem: Não pode ser criada uma regra com procedimento E material! */

end if;

if (NEW.nr_seq_grupo_item is not null) and (NEW.ie_tipo_item is not null) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(791557);
	/* Mensagem: Não pode ser criada uma regra com grupo de itens mensalidade E itens mensalidade! */

end if;

if (NEW.cd_procedimento is null) and (NEW.cd_material is null) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(262465);
	/* Mensagem: Não pode ser criada uma regra sem procedimento ou material! */

end if;

/*aaschlote 26/08/2011*/

if (NEW.cd_procedimento is not null) and (NEW.ie_origem_proced is null) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(262468);
	/* Mensagem: Favor informar a origem do procedimento. Utilize o localizador de procedimento para buscar a origem. */

end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_pls_tipo_item_mens_atual() FROM PUBLIC;

CREATE TRIGGER pls_tipo_item_mens_atual
	BEFORE INSERT OR UPDATE ON pls_tipo_item_mens FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_pls_tipo_item_mens_atual();

