-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS indicador_gestao_tp ON indicador_gestao CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_indicador_gestao_tp() RETURNS trigger AS $BODY$
DECLARE nr_seq_w bigint;ds_s_w   varchar(1);ds_c_w   varchar(1);ds_w	   varchar(1);ie_log_w varchar(1);BEGIN BEGIN ds_s_w := to_char(OLD.NR_SEQUENCIA);  ds_c_w:=null;SELECT * FROM gravar_log_alteracao(substr(OLD.IE_TIPO_DATA,1,4000), substr(NEW.IE_TIPO_DATA,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_TIPO_DATA', ie_log_w, ds_w, 'INDICADOR_GESTAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.DS_TABELA_VISAO,1,4000), substr(NEW.DS_TABELA_VISAO,1,4000), NEW.nm_usuario, nr_seq_w, 'DS_TABELA_VISAO', ie_log_w, ds_w, 'INDICADOR_GESTAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.DS_SQL_WHERE,1,4000), substr(NEW.DS_SQL_WHERE,1,4000), NEW.nm_usuario, nr_seq_w, 'DS_SQL_WHERE', ie_log_w, ds_w, 'INDICADOR_GESTAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_MOSTRAR_MEDIA,1,4000), substr(NEW.IE_MOSTRAR_MEDIA,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_MOSTRAR_MEDIA', ie_log_w, ds_w, 'INDICADOR_GESTAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_DECIMAL,1,4000), substr(NEW.IE_DECIMAL,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_DECIMAL', ie_log_w, ds_w, 'INDICADOR_GESTAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_MOSTRAR_TOTAL,1,4000), substr(NEW.IE_MOSTRAR_TOTAL,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_MOSTRAR_TOTAL', ie_log_w, ds_w, 'INDICADOR_GESTAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_SITUACAO,1,4000), substr(NEW.IE_SITUACAO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_SITUACAO', ie_log_w, ds_w, 'INDICADOR_GESTAO', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;   exception when others then ds_w:= '1'; end;   END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_indicador_gestao_tp() FROM PUBLIC;

CREATE TRIGGER indicador_gestao_tp
	AFTER UPDATE ON indicador_gestao FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_indicador_gestao_tp();

