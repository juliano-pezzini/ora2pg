-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS regra_fluxo_caixa_tp ON regra_fluxo_caixa CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_regra_fluxo_caixa_tp() RETURNS trigger AS $BODY$
DECLARE nr_seq_w bigint;ds_s_w   varchar(1);ds_c_w   varchar(1);ds_w	   varchar(1);ie_log_w varchar(1);BEGIN BEGIN ds_s_w := to_char(OLD.NR_SEQUENCIA);  ds_c_w:=null;SELECT * FROM gravar_log_alteracao(substr(OLD.VL_FIXO,1,4000), substr(NEW.VL_FIXO,1,4000), NEW.nm_usuario, nr_seq_w, 'VL_FIXO', ie_log_w, ds_w, 'REGRA_FLUXO_CAIXA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_ANTECIPAR_DIA_UTIL,1,4000), substr(NEW.IE_ANTECIPAR_DIA_UTIL,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_ANTECIPAR_DIA_UTIL', ie_log_w, ds_w, 'REGRA_FLUXO_CAIXA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_ACUMULAR_FLUXO,1,4000), substr(NEW.IE_ACUMULAR_FLUXO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_ACUMULAR_FLUXO', ie_log_w, ds_w, 'REGRA_FLUXO_CAIXA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.DT_VIGENCIA_INICIAL,1,4000), substr(NEW.DT_VIGENCIA_INICIAL,1,4000), NEW.nm_usuario, nr_seq_w, 'DT_VIGENCIA_INICIAL', ie_log_w, ds_w, 'REGRA_FLUXO_CAIXA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.DT_VIGENCIA_FINAL,1,4000), substr(NEW.DT_VIGENCIA_FINAL,1,4000), NEW.nm_usuario, nr_seq_w, 'DT_VIGENCIA_FINAL', ie_log_w, ds_w, 'REGRA_FLUXO_CAIXA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_CLASSIF_FLUXO,1,4000), substr(NEW.IE_CLASSIF_FLUXO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_CLASSIF_FLUXO', ie_log_w, ds_w, 'REGRA_FLUXO_CAIXA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_REGRA_DATA,1,4000), substr(NEW.IE_REGRA_DATA,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_REGRA_DATA', ie_log_w, ds_w, 'REGRA_FLUXO_CAIXA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.NR_DIA_FIXO,1,4000), substr(NEW.NR_DIA_FIXO,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_DIA_FIXO', ie_log_w, ds_w, 'REGRA_FLUXO_CAIXA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.QT_MES_ANTERIOR,1,4000), substr(NEW.QT_MES_ANTERIOR,1,4000), NEW.nm_usuario, nr_seq_w, 'QT_MES_ANTERIOR', ie_log_w, ds_w, 'REGRA_FLUXO_CAIXA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_DIA_FIXO,1,4000), substr(NEW.IE_DIA_FIXO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_DIA_FIXO', ie_log_w, ds_w, 'REGRA_FLUXO_CAIXA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.PR_FLUXO,1,4000), substr(NEW.PR_FLUXO,1,4000), NEW.nm_usuario, nr_seq_w, 'PR_FLUXO', ie_log_w, ds_w, 'REGRA_FLUXO_CAIXA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_REGRA_ORIGEM,1,4000), substr(NEW.IE_REGRA_ORIGEM,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_REGRA_ORIGEM', ie_log_w, ds_w, 'REGRA_FLUXO_CAIXA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;   exception when others then ds_w:= '1'; end;   END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_regra_fluxo_caixa_tp() FROM PUBLIC;

CREATE TRIGGER regra_fluxo_caixa_tp
	AFTER UPDATE ON regra_fluxo_caixa FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_regra_fluxo_caixa_tp();

