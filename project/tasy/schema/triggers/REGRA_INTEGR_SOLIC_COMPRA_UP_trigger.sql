-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS regra_integr_solic_compra_up ON regra_integr_solic_compra CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_regra_integr_solic_compra_up() RETURNS trigger AS $BODY$
declare

qt_registro_w		bigint;
PRAGMA AUTONOMOUS_TRANSACTION;
BEGIN

select	count(*)
into STRICT	qt_registro_w
from	regra_integr_solic_compra
where	ie_tipo_servico		= NEW.ie_tipo_servico
and	ie_integracao		= NEW.ie_integracao
and	coalesce(cd_perfil,0)	= coalesce(NEW.cd_perfil,0)
and	coalesce(cd_centro_custo,0)	= coalesce(NEW.cd_centro_custo,0);

if (qt_registro_w > 0) then
	/*(-20011,'Já existe uma regra cadastrada com esse Tipo de solicitação e Tipo de integração. Não é permitido regras duplicadas.');*/

	CALL wheb_mensagem_pck.Exibir_mensagem_abort(182216);
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_regra_integr_solic_compra_up() FROM PUBLIC;

CREATE TRIGGER regra_integr_solic_compra_up
	BEFORE INSERT OR UPDATE ON regra_integr_solic_compra FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_regra_integr_solic_compra_up();

