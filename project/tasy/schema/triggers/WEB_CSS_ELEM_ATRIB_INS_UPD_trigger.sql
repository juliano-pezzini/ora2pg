-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS web_css_elem_atrib_ins_upd ON web_css_elemento_atrib CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_web_css_elem_atrib_ins_upd() RETURNS trigger AS $BODY$
declare
ds_stack_w	varchar(2000);
old_val_w varchar(120);
BEGIN
if (TG_OP = 'INSERT' or (coalesce(OLD.DS_VALOR_CLIENTE, 'LOREM' ) <> coalesce(NEW.DS_VALOR_CLIENTE, 'IPSUM'))) then

	ds_stack_w := substr(dbms_utility.format_call_stack,1,1800);

	if (TG_OP = 'INSERT') then
		old_val_w := 'INSERT';
	else
		old_val_w := coalesce(OLD.DS_VALOR_CLIENTE, 'LOREM' );
	end if;

	/*insert 	into log_tasy 	(dt_atualizacao, nm_usuario, cd_log, ds_log)
		values 		(sysdate, 	:new.nm_usuario, 11414, 'Alteração no CSS Cliente!'||
									'; old_val '|| old_val_w ||
									'; new_val: '|| nvl(:new.DS_VALOR_CLIENTE, 'IPSUM') ||
									'; Stack: '|| ds_stack_w);*/
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_web_css_elem_atrib_ins_upd() FROM PUBLIC;

CREATE TRIGGER web_css_elem_atrib_ins_upd
	BEFORE INSERT OR UPDATE ON web_css_elemento_atrib FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_web_css_elem_atrib_ins_upd();

