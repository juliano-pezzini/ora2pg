-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS cur_curativo_item_atual ON cur_curativo_item CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_cur_curativo_item_atual() RETURNS trigger AS $BODY$
BEGIN

if (NEW.nr_seq_result is not null) then
	select	qt_valor
	into STRICT	NEW.qt_valor
	from	cur_item_result
	where	nr_sequencia	= NEW.nr_seq_result;
else
	NEW.qt_valor	:= 0;
end if;

RETURN NEW;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_cur_curativo_item_atual() FROM PUBLIC;

CREATE TRIGGER cur_curativo_item_atual
	BEFORE INSERT OR UPDATE ON cur_curativo_item FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_cur_curativo_item_atual();

