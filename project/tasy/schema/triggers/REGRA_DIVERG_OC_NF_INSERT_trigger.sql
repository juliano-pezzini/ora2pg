-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS regra_diverg_oc_nf_insert ON regra_diverg_oc_nf CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_regra_diverg_oc_nf_insert() RETURNS trigger AS $BODY$
BEGIN

if (NEW.qt_diferenca is null) and (NEW.pr_diferenca is null) then
	/*r.aise_application_error(-20011,'É preciso informar a quantidade, ou o valor, ou o percentual de diferença para a regra.');*/

	CALL wheb_mensagem_pck.Exibir_Mensagem_Abort(233800);
end if;

if (NEW.qt_diferenca is not null) and (NEW.pr_diferenca is not null) then
	/*r.aise_application_error(-20011,'Informar somente a quantidade, ou o valor, ou o percentual.');*/

	CALL wheb_mensagem_pck.Exibir_Mensagem_Abort(233801);
end if;

RETURN NEW;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_regra_diverg_oc_nf_insert() FROM PUBLIC;

CREATE TRIGGER regra_diverg_oc_nf_insert
	BEFORE INSERT OR UPDATE ON regra_diverg_oc_nf FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_regra_diverg_oc_nf_insert();

