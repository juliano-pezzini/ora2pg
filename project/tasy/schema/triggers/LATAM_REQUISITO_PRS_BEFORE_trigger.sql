-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS latam_requisito_prs_before ON latam_requisito_prs CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_latam_requisito_prs_before() RETURNS trigger AS $BODY$
DECLARE
qt_count_w latam_requisito_prs.nr_sequencia%type;

BEGIN
	qt_count_w := 0;
	if (wheb_usuario_pck.get_ie_executar_trigger <> 'N') then
		if (NEW.nr_seq_prs is not null and OLD.nr_seq_prs is null or (NEW.nr_seq_prs <> OLD.nr_seq_prs)) then
			select count(*)
			into STRICT qt_count_w
			from latam_requisito_prs
			where nr_seq_prs = NEW.nr_seq_prs
			and nr_seq_requisito_crs = NEW.nr_seq_requisito_crs;
			if (qt_count_w > 0) then
				CALL wheb_mensagem_pck.exibir_mensagem_abort(1204116);
			end if;
		end if;
	end if;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_latam_requisito_prs_before() FROM PUBLIC;

CREATE TRIGGER latam_requisito_prs_before
	BEFORE INSERT OR UPDATE ON latam_requisito_prs FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_latam_requisito_prs_before();

