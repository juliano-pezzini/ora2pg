-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS ehr_tipo_reg_item_atual ON ehr_tipo_reg_item CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_ehr_tipo_reg_item_atual() RETURNS trigger AS $BODY$
BEGIN

if (NEW.nr_seq_template is null) and (NEW.nr_seq_item_pep is null) then
	--Deve ser selecionado um template ou item de prontuário. Não permitido os dois em branco. #@#@');
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(261612);
end if;

if (NEW.nr_seq_template is not null) and (NEW.nr_seq_item_pep is not null) then
	--Deve ser selecionado um template ou item de prontuário. Não permitido os dois em um mesmo registro. #@#@');
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(261613);
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_ehr_tipo_reg_item_atual() FROM PUBLIC;

CREATE TRIGGER ehr_tipo_reg_item_atual
	BEFORE INSERT ON ehr_tipo_reg_item FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_ehr_tipo_reg_item_atual();

