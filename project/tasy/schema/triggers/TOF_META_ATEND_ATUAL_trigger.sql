-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS tof_meta_atend_atual ON tof_meta_atend CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_tof_meta_atend_atual() RETURNS trigger AS $BODY$
declare


BEGIN
if (coalesce(OLD.DT_INICIO,LOCALTIMESTAMP + interval '10 days') <> NEW.DT_INICIO) and (NEW.DT_INICIO is not null) then
	NEW.ds_utc				:= obter_data_utc(NEW.DT_INICIO, 'HV');
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_tof_meta_atend_atual() FROM PUBLIC;

CREATE TRIGGER tof_meta_atend_atual
	BEFORE INSERT OR UPDATE ON tof_meta_atend FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_tof_meta_atend_atual();

