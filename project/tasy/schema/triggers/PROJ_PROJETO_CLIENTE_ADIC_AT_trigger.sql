-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS proj_projeto_cliente_adic_at ON proj_projeto_cliente_adic CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_proj_projeto_cliente_adic_at() RETURNS trigger AS $BODY$
DECLARE
nr_seq_cliente_w	bigint;
nr_seq_gerencia_w	bigint;

BEGIN
/*
select	max(nr_seq_cliente),
	max(nr_seq_gerencia)
into	nr_seq_cliente_w,
	nr_seq_gerencia_w
from	proj_projeto
where	nr_sequencia	= :new.nr_seq_projeto;

 A pedido do Fabio retiramos esta consistência porque existem clientes que abrem mais de uma OS para o mesmo projeto
if	(nr_seq_cliente_w = :new.nr_seq_cliente_adic) and
	(nr_seq_gerencia_w <> 9) then /* Incluí o tratamento por causa dos projetos do departamento Comercial, onde temos várias OS's diferentes para a mesma situação
	begin
	Você não deve vincular uma OS do próprio cliente como cliente adicional ao projeto.
	end;
end if;*/
nr_seq_gerencia_w	:= 0;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_proj_projeto_cliente_adic_at() FROM PUBLIC;

CREATE TRIGGER proj_projeto_cliente_adic_at
	BEFORE INSERT OR UPDATE ON proj_projeto_cliente_adic FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_proj_projeto_cliente_adic_at();

