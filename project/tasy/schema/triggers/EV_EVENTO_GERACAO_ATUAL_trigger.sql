-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS ev_evento_geracao_atual ON ev_evento_geracao CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_ev_evento_geracao_atual() RETURNS trigger AS $BODY$
DECLARE
a_w varchar(1);
BEGIN

a_w := 'A';

/*
if	(:new.ie_regra_geracao = 'SD') then
	if	((nvl(:new.qt_hora,0) + nvl(:new.qt_minuto,0)) <= 0)) then
		(-20011,'Para este evento você deve preencher ou as horas ou minutos!');
	end if;
end if;
*/
RETURN NEW;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_ev_evento_geracao_atual() FROM PUBLIC;

CREATE TRIGGER ev_evento_geracao_atual
	BEFORE INSERT OR UPDATE ON ev_evento_geracao FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_ev_evento_geracao_atual();

