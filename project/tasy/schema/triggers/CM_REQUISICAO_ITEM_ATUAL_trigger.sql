-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS cm_requisicao_item_atual ON cm_requisicao_item CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_cm_requisicao_item_atual() RETURNS trigger AS $BODY$
declare

BEGIN

if (NEW.ie_devolucao = 'S') and (NEW.ie_emprestimo = 'S') then
	--Somente é possível informar que o item foi emprestado ou devolvido.;
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(267171);
end if;

if (TG_OP = 'INSERT') then
	if (NEW.NR_SEQ_CONJUNTO is null and NEW.NR_SEQ_CLASSIF is null and NEW.NR_SEQ_CLASSIF_CONJ is null) then
		CALL wheb_mensagem_pck.exibir_mensagem_abort(1041576);
	end if;
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_cm_requisicao_item_atual() FROM PUBLIC;

CREATE TRIGGER cm_requisicao_item_atual
	BEFORE INSERT OR UPDATE ON cm_requisicao_item FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_cm_requisicao_item_atual();

