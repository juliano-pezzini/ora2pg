-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS fis_reinf_r4040_atual ON fis_reinf_r4040 CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_fis_reinf_r4040_atual() RETURNS trigger AS $BODY$
declare
qtd_w		bigint;

pragma autonomous_transaction;

BEGIN

    select count(*)
    into STRICT   qtd_w
    from   fis_reinf_r4040_evento
    where  nr_seq_lote = OLD.nr_sequencia
    and    dt_transmissao is not null;

     if (qtd_w > 0) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(1080708);
    end if;

IF TG_OP = 'DELETE' THEN
	RETURN OLD;
ELSE
	RETURN NEW;
END IF;

END;
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_fis_reinf_r4040_atual() FROM PUBLIC;

CREATE TRIGGER fis_reinf_r4040_atual
	BEFORE UPDATE OR DELETE ON fis_reinf_r4040 FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_fis_reinf_r4040_atual();

