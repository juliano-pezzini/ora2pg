-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS pessoa_titular_convenio_bft ON pessoa_titular_convenio CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_pessoa_titular_convenio_bft() RETURNS trigger AS $BODY$
declare
cd_pessoa_dc_w    varchar(10);
ds_param_integ_hl7_w    varchar(255);
BEGIN

if (wheb_usuario_pck.get_ie_executar_trigger = 'S') then
CALL call_bifrost_content('patient.insurance.update','person_json_pck.get_patient_message_clob('||coalesce(NEW.cd_pessoa_fisica,OLD.cd_pessoa_fisica)||')', coalesce(NEW.nm_usuario,OLD.nm_usuario));
end if;

IF TG_OP = 'DELETE' THEN
	RETURN OLD;
ELSE
	RETURN NEW;
END IF;

end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_pessoa_titular_convenio_bft() FROM PUBLIC;

CREATE TRIGGER pessoa_titular_convenio_bft
	AFTER INSERT OR DELETE ON pessoa_titular_convenio FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_pessoa_titular_convenio_bft();

