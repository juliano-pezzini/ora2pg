-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS paciente_atendimento_hist ON paciente_atendimento CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_paciente_atendimento_hist() RETURNS trigger AS $BODY$
declare

nr_sequencia_w		bigint;

BEGIN

if (NEW.dt_chegada <> OLD.dt_chegada) then
	select	nextval('paciente_atendimento_hist_seq')
	into STRICT	nr_sequencia_w
	;

	insert into paciente_atendimento_hist(
		nr_sequencia,
		nr_seq_atendimento,
		dt_atualizacao,
		nm_usuario,
		dt_chegada)
	values (nr_sequencia_w,
		NEW.nr_seq_atendimento,
		LOCALTIMESTAMP,
		wheb_usuario_pck.get_nm_usuario,
		NEW.dt_chegada);
end if;

if (NEW.dt_fim_adm <> OLD.dt_fim_adm) then
	select	nextval('paciente_atendimento_hist_seq')
	into STRICT	nr_sequencia_w
	;

	insert into paciente_atendimento_hist(
		nr_sequencia,
		nr_seq_atendimento,
		dt_atualizacao,
		nm_usuario,
		dt_fim_adm)
	values (nr_sequencia_w,
		NEW.nr_seq_atendimento,
		LOCALTIMESTAMP,
		wheb_usuario_pck.get_nm_usuario,
		NEW.dt_fim_adm);
end if;

if (NEW.dt_inicio_adm <> OLD.dt_inicio_adm) then
	select	nextval('paciente_atendimento_hist_seq')
	into STRICT	nr_sequencia_w
	;

	insert into paciente_atendimento_hist(
		nr_sequencia,
		nr_seq_atendimento,
		dt_atualizacao,
		nm_usuario,
		dt_inicio_adm)
	values (nr_sequencia_w,
		NEW.nr_seq_atendimento,
		LOCALTIMESTAMP,
		wheb_usuario_pck.get_nm_usuario,
		NEW.dt_inicio_adm);
end if;

if (NEW.dt_acomodacao_paciente <> OLD.dt_acomodacao_paciente)   then
	select	nextval('paciente_atendimento_hist_seq')
	into STRICT	nr_sequencia_w
	;

	insert into paciente_atendimento_hist(
		nr_sequencia,
		nr_seq_atendimento,
		dt_atualizacao,
		nm_usuario,
		dt_acomodacao_paciente)
	values (nr_sequencia_w,
		NEW.nr_seq_atendimento,
		LOCALTIMESTAMP,
		wheb_usuario_pck.get_nm_usuario,
		NEW.dt_acomodacao_paciente);
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_paciente_atendimento_hist() FROM PUBLIC;

CREATE TRIGGER paciente_atendimento_hist
	BEFORE INSERT OR UPDATE ON paciente_atendimento FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_paciente_atendimento_hist();

