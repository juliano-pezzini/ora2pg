-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS usuario_lib_branch_before ON usuario_liberacao_branch CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_usuario_lib_branch_before() RETURNS trigger AS $BODY$
BEGIN

if (wheb_usuario_pck.get_ie_executar_trigger = 'S') then

    if	TG_OP = 'INSERT' or TG_OP = 'UPDATE' then
        if (NEW.nm_usuario_nrec not in ('jpweiss', 'cetrentin', 'Edilson', 'acgozdziejewski', 'Rafael')) then
            CALL wheb_mensagem_pck.exibir_mensagem_abort(1202100);
        end if;
    end if;
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_usuario_lib_branch_before() FROM PUBLIC;

CREATE TRIGGER usuario_lib_branch_before
	BEFORE INSERT OR UPDATE ON usuario_liberacao_branch FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_usuario_lib_branch_before();

