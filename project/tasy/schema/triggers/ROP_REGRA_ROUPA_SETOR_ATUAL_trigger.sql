-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS rop_regra_roupa_setor_atual ON rop_regra_roupa_setor CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_rop_regra_roupa_setor_atual() RETURNS trigger AS $BODY$
DECLARE

BEGIN

if (NEW.nr_seq_kit is null) and (NEW.nr_seq_roupa is null) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(265539);
	--'Deve ser informado uma Roupa ou um Kit.'
end if;

if (NEW.nr_seq_kit is not null) and (NEW.nr_seq_roupa is not null) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(265540);
	--'Deve ser informado somente uma Roupa ou somente um Kit.'
end if;

RETURN NEW;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_rop_regra_roupa_setor_atual() FROM PUBLIC;

CREATE TRIGGER rop_regra_roupa_setor_atual
	BEFORE INSERT OR UPDATE ON rop_regra_roupa_setor FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_rop_regra_roupa_setor_atual();

