-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS same_solic_pront_update ON same_solic_pront CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_same_solic_pront_update() RETURNS trigger AS $BODY$
declare

BEGIN

if (NEW.dt_liberacao is not null) and (OLD.DT_DEVOLUCAO_PREVISTA <> NEW.DT_DEVOLUCAO_PREVISTA) then

	insert into
	same_solic_pront_alteracao(nr_sequencia,
					dt_atualizacao,
					nm_usuario,
					dt_atualizacao_nrec,
					nm_usuario_nrec,
					nr_seq_solic,
					dt_devolucao_prevista)
	values (nextval('same_solic_pront_alteracao_seq'),
					LOCALTIMESTAMP,
					NEW.nm_usuario,
					LOCALTIMESTAMP,
					NEW.nm_usuario,
					NEW.nr_sequencia,
					OLD.DT_DEVOLUCAO_PREVISTA);

end if;


RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_same_solic_pront_update() FROM PUBLIC;

CREATE TRIGGER same_solic_pront_update
	BEFORE UPDATE ON same_solic_pront FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_same_solic_pront_update();

