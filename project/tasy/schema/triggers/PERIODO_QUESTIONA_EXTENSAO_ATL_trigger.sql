-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS periodo_questiona_extensao_atl ON periodo_questiona_extensao CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_periodo_questiona_extensao_atl() RETURNS trigger AS $BODY$
declare
BEGIN
  BEGIN 
BEGIN 
	if (NEW.hr_fim is not null) and ((NEW.hr_fim <> OLD.hr_fim) or (OLD.dt_fim is null)) then 
		NEW.dt_fim := to_date(to_char(LOCALTIMESTAMP,'dd/mm/yyyy') || ' ' || NEW.hr_fim,'dd/mm/yyyy hh24:mi');
	end if;	
	if (NEW.hr_inicio is not null) and ((NEW.hr_inicio <> OLD.hr_inicio) or (OLD.dt_inicio is null)) then 
		NEW.dt_inicio := to_date(to_char(LOCALTIMESTAMP,'dd/mm/yyyy') || ' ' || NEW.hr_inicio,'dd/mm/yyyy hh24:mi');
	end if;
exception 
	when others then 
	null;
end;
 
  END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_periodo_questiona_extensao_atl() FROM PUBLIC;

CREATE TRIGGER periodo_questiona_extensao_atl
	BEFORE INSERT OR UPDATE ON periodo_questiona_extensao FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_periodo_questiona_extensao_atl();

