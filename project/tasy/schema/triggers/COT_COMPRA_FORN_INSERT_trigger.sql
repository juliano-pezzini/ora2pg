-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS cot_compra_forn_insert ON cot_compra_forn CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_cot_compra_forn_insert() RETURNS trigger AS $BODY$
DECLARE
qt_reg_w	smallint;
BEGIN

if (wheb_usuario_pck.get_ie_executar_trigger	= 'N')  then
	goto Final;
end if;
if (NEW.cd_cgc_fornecedor is not null) and (NEW.cd_pessoa_fisica is not null) then
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(278762);
end if;

if (NEW.cd_cgc_fornecedor is null) and (NEW.cd_pessoa_fisica is null) then
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(278763);
end if;
<<Final>>
qt_reg_w	:= 0;


RETURN NEW;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_cot_compra_forn_insert() FROM PUBLIC;

CREATE TRIGGER cot_compra_forn_insert
	BEFORE INSERT OR UPDATE ON cot_compra_forn FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_cot_compra_forn_insert();

