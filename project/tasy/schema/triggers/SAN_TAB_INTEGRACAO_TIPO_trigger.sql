-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS san_tab_integracao_tipo ON san_tabela_integracao CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_san_tab_integracao_tipo() RETURNS trigger AS $BODY$
declare

BEGIN

if	(((NEW.nr_seq_integracao is not null) and
	((NEW.ie_tipo_integ_tasyger is not null) or (NEW.nr_seq_proj_xml is not null))) or
	((NEW.ie_tipo_integ_tasyger is not null) and
	((NEW.nr_seq_integracao is not null) or (NEW.nr_seq_proj_xml is not null))) or
	((NEW.nr_seq_proj_xml is not null) and
	((NEW.nr_seq_integracao is not null) or (NEW.ie_tipo_integ_tasyger is not null)))) or
	((NEW.nr_seq_proj_xml is null) and (NEW.nr_seq_integracao is null) and (NEW.ie_tipo_integ_tasyger is null)) then

	CALL wheb_mensagem_pck.exibir_mensagem_abort(729797);

end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_san_tab_integracao_tipo() FROM PUBLIC;

CREATE TRIGGER san_tab_integracao_tipo
	BEFORE INSERT OR UPDATE ON san_tabela_integracao FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_san_tab_integracao_tipo();

