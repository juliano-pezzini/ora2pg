-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS regra_voice_hcs_insert ON regra_voice_hcs CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_regra_voice_hcs_insert() RETURNS trigger AS $BODY$
declare
ie_exist_w	smallint;
pragma autonomous_transaction;
BEGIN

if (wheb_usuario_pck.get_ie_executar_trigger	= 'S')  then
	
	if (NEW.IE_SITUACAO = coalesce(OLD.IE_SITUACAO,NEW.IE_SITUACAO)) then
		select count(*)
		into STRICT ie_exist_w
		from regra_voice_hcs
		where cd_estabelecimento = NEW.cd_estabelecimento
		and ie_evento = NEW.ie_evento;
		
			if (ie_exist_w > 0)  then
				CALL exibir_erro_abortar(obter_desc_expressao(1038041)||' #@#@',null);
			end if;
	end if;
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_regra_voice_hcs_insert() FROM PUBLIC;

CREATE TRIGGER regra_voice_hcs_insert
	BEFORE INSERT OR UPDATE ON regra_voice_hcs FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_regra_voice_hcs_insert();

