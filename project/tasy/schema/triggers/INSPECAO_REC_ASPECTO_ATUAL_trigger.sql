-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS inspecao_rec_aspecto_atual ON inspecao_rec_aspecto CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_inspecao_rec_aspecto_atual() RETURNS trigger AS $BODY$
declare

BEGIN

if (NEW.cd_grupo_material is not null) then
	BEGIN
	if (NEW.cd_subgrupo_material is not null) or (NEW.cd_classe_material is not null) or (NEW.cd_material is not null) then
		CALL wheb_mensagem_pck.exibir_mensagem_abort(266075);
		--'Favor informar somente uma informação (Só o Grupo, ou só o Subgrupo, ou só a Classe, ou só o Material).');
	end if;
	end;
elsif (NEW.cd_subgrupo_material is not null) then
	BEGIN
	if (NEW.cd_grupo_material is not null) or (NEW.cd_classe_material is not null) or (NEW.cd_material is not null) then
		CALL wheb_mensagem_pck.exibir_mensagem_abort(266075);
		--'Favor informar somente uma informação (Só o Grupo, ou só o Subgrupo, ou só a Classe, ou só o Material).');
	end if;
	end;
elsif (NEW.cd_classe_material is not null) then
	BEGIN
	if (NEW.cd_grupo_material is not null) or (NEW.cd_subgrupo_material is not null) or (NEW.cd_material is not null) then
		CALL wheb_mensagem_pck.exibir_mensagem_abort(266075);
		--'Favor informar somente uma informação (Só o Grupo, ou só o Subgrupo, ou só a Classe, ou só o Material).');
	end if;
	end;
elsif (NEW.cd_material is not null) then
	BEGIN
	if (NEW.cd_grupo_material is not null) or (NEW.cd_subgrupo_material is not null) or (NEW.cd_classe_material is not null) then
		CALL wheb_mensagem_pck.exibir_mensagem_abort(266075);
		--'Favor informar somente uma informação (Só o Grupo, ou só o Subgrupo, ou só a Classe, ou só o Material).');
	end if;
	end;
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_inspecao_rec_aspecto_atual() FROM PUBLIC;

CREATE TRIGGER inspecao_rec_aspecto_atual
	BEFORE INSERT OR UPDATE ON inspecao_rec_aspecto FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_inspecao_rec_aspecto_atual();

