-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS convenio_retorno_update ON convenio_retorno CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_convenio_retorno_update() RETURNS trigger AS $BODY$
BEGIN

if (OLD.IE_STATUS_RETORNO = 'F') and (NEW.IE_STATUS_RETORNO <> 'F') then
	-- Você não pode fazer update do status do retorno via banco de dados!
	CALL wheb_mensagem_pck.exibir_mensagem_abort(266940);
end if;

RETURN NEW;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_convenio_retorno_update() FROM PUBLIC;

CREATE TRIGGER convenio_retorno_update
	BEFORE UPDATE ON convenio_retorno FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_convenio_retorno_update();

