-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS prescr_proced_lab_log_upd ON prescr_procedimento CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_prescr_proced_lab_log_upd() RETURNS trigger AS $BODY$
declare

usuario_banco_w	varchar(255);

BEGIN

if ((NEW.dt_integracao is not null and OLD.dt_integracao is null) or (NEW.dt_integracao != OLD.dt_integracao)) and (NEW.nr_seq_exame is not null) then
	select	user
	into STRICT	usuario_banco_w
	;
	CALL gravar_log_lab(48, obter_desc_expressao(287009) || ': ' || to_char(NEW.dt_integracao, 'dd/mm/yyyy hh24:mi:ss') ||
			   '; ' || obter_desc_expressao(314533) || ': ' || NEW.nm_usuario || '; ' || obter_desc_expressao(307024) || ': ' || usuario_banco_w ||
			   '; ' || obter_desc_expressao(298311) || ': ' || NEW.nr_sequencia || '; Call stack: ' || substr(dbms_utility.format_call_stack,1,1800), NEW.nm_usuario, NEW.nr_prescricao);
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_prescr_proced_lab_log_upd() FROM PUBLIC;

CREATE TRIGGER prescr_proced_lab_log_upd
	BEFORE UPDATE ON prescr_procedimento FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_prescr_proced_lab_log_upd();

