-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS regra_material_perfil_atual ON regra_material_perfil CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_regra_material_perfil_atual() RETURNS trigger AS $BODY$
DECLARE
qt_reg_w		bigint;
 
BEGIN 
if (wheb_usuario_pck.get_ie_executar_trigger	= 'N') then 
	goto Final;
end if;
 
if (NEW.cd_perfil is null) and (NEW.cd_estabelecimento is null) and (NEW.cd_setor_atendimento is null) and (NEW.nm_usuario_regra is null) then 
	CALL wheb_mensagem_pck.exibir_mensagem_abort(266267);
	--'É necessário informar ao menos um campo para criar a regra!'); 
end if;
 
<<Final>> 
qt_reg_w	:= 0;
 
RETURN NEW;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_regra_material_perfil_atual() FROM PUBLIC;

CREATE TRIGGER regra_material_perfil_atual
	BEFORE INSERT OR UPDATE ON regra_material_perfil FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_regra_material_perfil_atual();

