-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS trg_encripta_senha_usuario ON lab_parametro CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_trg_encripta_senha_usuario() RETURNS trigger AS $BODY$
BEGIN

	if (wheb_usuario_pck.get_ie_executar_trigger <> 'N') then
		select dbms_random.string('X', 15)
			into STRICT NEW.ds_tec
			;
		NEW.ds_hash_int := obter_sha2(upper(NEW.DS_HASH_INT) || NEW.ds_tec, 256);
	end if;
	
RETURN NEW;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_trg_encripta_senha_usuario() FROM PUBLIC;

CREATE TRIGGER trg_encripta_senha_usuario
    BEFORE INSERT OR UPDATE OF ds_hash_int ON lab_parametro FOR EACH ROW
    
	WHEN (new.ds_hash_int IS NOT NULL)
	EXECUTE PROCEDURE trigger_fct_trg_encripta_senha_usuario();

