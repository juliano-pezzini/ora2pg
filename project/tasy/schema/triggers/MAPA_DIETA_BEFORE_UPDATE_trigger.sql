-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS mapa_dieta_before_update ON mapa_dieta CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_mapa_dieta_before_update() RETURNS trigger AS $BODY$
declare
ie_verifica_dieta_inf_w		varchar(1);

BEGIN

ie_verifica_dieta_inf_w  := obter_param_usuario(1000, 123, obter_perfil_ativo, NEW.nm_usuario, wheb_usuario_pck.get_cd_estabelecimento, ie_verifica_dieta_inf_w );

--wheb_mensagem_pck.exibir_mensagem_abort(ie_verifica_dieta_inf_w||' - '||to_char(:new.dt_liberacao) || ' - '||to_char(:old.dt_liberacao) ||' - '||:new.cd_dieta);
if (ie_verifica_dieta_inf_w = 'S') and (NEW.dt_liberacao is not null) and (OLD.dt_liberacao is null)	and (NEW.cd_dieta is null )then

	CALL wheb_mensagem_pck.exibir_mensagem_abort(184626);

end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_mapa_dieta_before_update() FROM PUBLIC;

CREATE TRIGGER mapa_dieta_before_update
	BEFORE UPDATE ON mapa_dieta FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_mapa_dieta_before_update();

