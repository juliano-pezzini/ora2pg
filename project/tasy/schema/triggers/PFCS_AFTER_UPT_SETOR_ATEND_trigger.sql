-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS pfcs_after_upt_setor_atend ON setor_atendimento CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_pfcs_after_upt_setor_atend() RETURNS trigger AS $BODY$
DECLARE
ds_log_w    varchar(4000);
qt_reg_w    smallint;

BEGIN
    if (coalesce(wheb_usuario_pck.get_ie_executar_trigger,'S') = 'N')  then
        goto Final;
    end if;

    if (OLD.nm_usuario_nrec = 'PFCS') then
        ds_log_w := (
            concat('cd_setor_atendimento: ',                OLD.cd_setor_atendimento || CHR(10))
            || concat('ds_setor_atendimento: ',             OLD.ds_setor_atendimento || CHR(10))
            || concat('cd_estabelecimento: ',               OLD.cd_estabelecimento || CHR(10))
            || concat('old cd_classif_setor: ',             OLD.cd_classif_setor)
            || concat(' | new cd_classif_setor: ',          NEW.cd_classif_setor || CHR(10))
            || concat('old ie_semi_intensiva: ',            coalesce(OLD.ie_semi_intensiva, 'null'))
            || concat(' | new ie_semi_intensiva: ',         coalesce(NEW.ie_semi_intensiva, 'null') || CHR(10))
            || concat('old ie_situacao: ',                  OLD.ie_situacao)
            || concat(' | new ie_situacao: ',               NEW.ie_situacao || CHR(10))
            || concat('old ie_ocupacao_hospitalar: ',       coalesce(OLD.ie_ocup_hospitalar, 'null'))
            || concat(' | new ie_ocupacao_hospitalar: ',    coalesce(NEW.ie_ocup_hospitalar, 'null'))
        );

        CALL pfcs_log_pck.pfcs_insert_org_struc_log(
            ie_type_p => 'UPDATE',
            nm_table_p => 'SETOR_ATENDIMENTO',
            ds_log_p => ds_log_w,
            nm_usuario_p => NEW.nm_usuario
        );
    end if;

<<Final>>
qt_reg_w := 0;
RETURN NEW;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_pfcs_after_upt_setor_atend() FROM PUBLIC;

CREATE TRIGGER pfcs_after_upt_setor_atend
	AFTER UPDATE ON setor_atendimento FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_pfcs_after_upt_setor_atend();

