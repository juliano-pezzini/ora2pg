-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS pessoa_juridica_doc_atual ON pessoa_juridica_doc CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_pessoa_juridica_doc_atual() RETURNS trigger AS $BODY$
declare

BEGIN
if (NEW.dt_emissao is not null) and (NEW.dt_emissao > LOCALTIMESTAMP) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(887581);
	--A data de emissão deve ser igual ou menor a data atual!
end if;

if (NEW.dt_emissao is not null) and (NEW.dt_revisao is not null) and (NEW.dt_revisao < NEW.dt_emissao) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(900320);
	--A data de validade deve ser maior ou igual que data de emissão!'
end if;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_pessoa_juridica_doc_atual() FROM PUBLIC;

CREATE TRIGGER pessoa_juridica_doc_atual
	BEFORE INSERT OR UPDATE ON pessoa_juridica_doc FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_pessoa_juridica_doc_atual();

