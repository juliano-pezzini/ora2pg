-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS pls_ocor_estrut_item_atual ON pls_ocorrencia_estrut_item CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_pls_ocor_estrut_item_atual() RETURNS trigger AS $BODY$
declare
nr_sequencia_w	pls_ocorrencia_estrut_item.nr_sequencia%type;
BEGIN

-- se for apagar o registro
if (TG_OP = 'DELETE') then
	nr_sequencia_w := OLD.nr_seq_estrutura;
else
	nr_sequencia_w := NEW.nr_seq_estrutura;
end if;

CALL pls_gerencia_upd_obj_pck.marcar_para_atualizacao('PLS_ESTRUTURA_OCOR_TM', wheb_usuario_pck.get_nm_usuario,
						 'PLS_OCOR_ESTRUT_ITEM_ATUAL',
						 'nr_seq_ocor_estrut_p=' || nr_sequencia_w);

IF TG_OP = 'DELETE' THEN
	RETURN OLD;
ELSE
	RETURN NEW;
END IF;

end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_pls_ocor_estrut_item_atual() FROM PUBLIC;

CREATE TRIGGER pls_ocor_estrut_item_atual
	BEFORE INSERT OR UPDATE OR DELETE ON pls_ocorrencia_estrut_item FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_pls_ocor_estrut_item_atual();

