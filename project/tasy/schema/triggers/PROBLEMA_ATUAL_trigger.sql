-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS problema_atual ON problema CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_problema_atual() RETURNS trigger AS $BODY$
DECLARE
qt_reg_w	smallint;
BEGIN

if (wheb_usuario_pck.get_ie_executar_trigger	= 'N')  then
	goto Final;
end if;
if (coalesce(NEW.qt_urgente,1) > 10) or (coalesce(NEW.qt_importante,1) > 10) or (coalesce(NEW.qt_tendencia,1) > 10) or (coalesce(NEW.qt_urgente,1) < 1) or (coalesce(NEW.qt_importante,1) < 1) or (coalesce(NEW.qt_tendencia,1) < 1) then
	--Os valores de urgência, importância e tendência devem ser de 1 a 10.');
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(261595);
end if;


NEW.qt_escore	:= coalesce(NEW.qt_urgente,1) * coalesce(NEW.qt_importante,1) * coalesce(NEW.qt_tendencia,1);
<<Final>>
qt_reg_w	:= 0;
RETURN NEW;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_problema_atual() FROM PUBLIC;

CREATE TRIGGER problema_atual
	BEFORE INSERT OR UPDATE ON problema FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_problema_atual();

