-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS padrao_estoque_local_tp ON padrao_estoque_local CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_padrao_estoque_local_tp() RETURNS trigger AS $BODY$
DECLARE nr_seq_w bigint;ds_s_w   varchar(1);ds_c_w   varchar(1);ds_w	   varchar(1);ie_log_w varchar(1);BEGIN BEGIN ds_s_w := null;  ds_c_w:= 'CD_LOCAL_ESTOQUE='||to_char(OLD.CD_LOCAL_ESTOQUE)||'#@#@CD_MATERIAL='||to_char(OLD.CD_MATERIAL);SELECT * FROM gravar_log_alteracao(substr(OLD.QT_ESTOQUE_MINIMO,1,4000), substr(NEW.QT_ESTOQUE_MINIMO,1,4000), NEW.nm_usuario, nr_seq_w, 'QT_ESTOQUE_MINIMO', ie_log_w, ds_w, 'PADRAO_ESTOQUE_LOCAL', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.QT_MINIMO_AJUSTE,1,4000), substr(NEW.QT_MINIMO_AJUSTE,1,4000), NEW.nm_usuario, nr_seq_w, 'QT_MINIMO_AJUSTE', ie_log_w, ds_w, 'PADRAO_ESTOQUE_LOCAL', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.QT_CONSUMO,1,4000), substr(NEW.QT_CONSUMO,1,4000), NEW.nm_usuario, nr_seq_w, 'QT_CONSUMO', ie_log_w, ds_w, 'PADRAO_ESTOQUE_LOCAL', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.QT_DIA_AJUSTE_MINIMO,1,4000), substr(NEW.QT_DIA_AJUSTE_MINIMO,1,4000), NEW.nm_usuario, nr_seq_w, 'QT_DIA_AJUSTE_MINIMO', ie_log_w, ds_w, 'PADRAO_ESTOQUE_LOCAL', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.QT_DIA_CONSUMO,1,4000), substr(NEW.QT_DIA_CONSUMO,1,4000), NEW.nm_usuario, nr_seq_w, 'QT_DIA_CONSUMO', ie_log_w, ds_w, 'PADRAO_ESTOQUE_LOCAL', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_ATUALIZA_PADRAO_LOCAL,1,4000), substr(NEW.IE_ATUALIZA_PADRAO_LOCAL,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_ATUALIZA_PADRAO_LOCAL', ie_log_w, ds_w, 'PADRAO_ESTOQUE_LOCAL', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_SITUACAO,1,4000), substr(NEW.IE_SITUACAO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_SITUACAO', ie_log_w, ds_w, 'PADRAO_ESTOQUE_LOCAL', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_LOCAL_ESTOQUE,1,4000), substr(NEW.CD_LOCAL_ESTOQUE,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_LOCAL_ESTOQUE', ie_log_w, ds_w, 'PADRAO_ESTOQUE_LOCAL', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_MATERIAL,1,4000), substr(NEW.CD_MATERIAL,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_MATERIAL', ie_log_w, ds_w, 'PADRAO_ESTOQUE_LOCAL', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_LOCAL_ATENDE,1,4000), substr(NEW.CD_LOCAL_ATENDE,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_LOCAL_ATENDE', ie_log_w, ds_w, 'PADRAO_ESTOQUE_LOCAL', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.QT_ESTOQUE_MAXIMO,1,4000), substr(NEW.QT_ESTOQUE_MAXIMO,1,4000), NEW.nm_usuario, nr_seq_w, 'QT_ESTOQUE_MAXIMO', ie_log_w, ds_w, 'PADRAO_ESTOQUE_LOCAL', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_ESTABELECIMENTO,1,4000), substr(NEW.CD_ESTABELECIMENTO,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_ESTABELECIMENTO', ie_log_w, ds_w, 'PADRAO_ESTOQUE_LOCAL', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.DS_LOCALIZACAO,1,4000), substr(NEW.DS_LOCALIZACAO,1,4000), NEW.nm_usuario, nr_seq_w, 'DS_LOCALIZACAO', ie_log_w, ds_w, 'PADRAO_ESTOQUE_LOCAL', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.NR_SEQ_ESTRUT_MAT,1,4000), substr(NEW.NR_SEQ_ESTRUT_MAT,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_SEQ_ESTRUT_MAT', ie_log_w, ds_w, 'PADRAO_ESTOQUE_LOCAL', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.QT_DIA_AJUSTE_MAXIMO,1,4000), substr(NEW.QT_DIA_AJUSTE_MAXIMO,1,4000), NEW.nm_usuario, nr_seq_w, 'QT_DIA_AJUSTE_MAXIMO', ie_log_w, ds_w, 'PADRAO_ESTOQUE_LOCAL', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.NR_SEQ_CONTAGEM,1,4000), substr(NEW.NR_SEQ_CONTAGEM,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_SEQ_CONTAGEM', ie_log_w, ds_w, 'PADRAO_ESTOQUE_LOCAL', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;   exception when others then ds_w:= '1'; end;   END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_padrao_estoque_local_tp() FROM PUBLIC;

CREATE TRIGGER padrao_estoque_local_tp
	AFTER UPDATE ON padrao_estoque_local FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_padrao_estoque_local_tp();

