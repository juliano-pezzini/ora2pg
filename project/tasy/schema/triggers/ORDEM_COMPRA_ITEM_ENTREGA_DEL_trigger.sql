-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS ordem_compra_item_entrega_del ON ordem_compra_item_entrega CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_ordem_compra_item_entrega_del() RETURNS trigger AS $BODY$
DECLARE

qt_registros_w		bigint;

BEGIN

select	count(*)
into STRICT	qt_registros_w
from	inspecao_recebimento
where	nr_seq_entrega = NEW.nr_Sequencia;

if (qt_registros_w > 0) then
	CALL wheb_mensagem_pck.Exibir_Mensagem_Abort(237554);
end if;

RETURN OLD;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_ordem_compra_item_entrega_del() FROM PUBLIC;

CREATE TRIGGER ordem_compra_item_entrega_del
	BEFORE DELETE ON ordem_compra_item_entrega FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_ordem_compra_item_entrega_del();

