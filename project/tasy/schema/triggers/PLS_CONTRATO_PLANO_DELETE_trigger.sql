-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS pls_contrato_plano_delete ON pls_contrato_plano CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_pls_contrato_plano_delete() RETURNS trigger AS $BODY$
declare

qt_registros_w		bigint;

BEGIN

select	count(*)
into STRICT	qt_registros_w
from	pls_segurado
where	nr_seq_plano	= OLD.nr_seq_plano
and	nr_seq_contrato	= OLD.nr_seq_contrato;

if (qt_registros_w > 0) then
	--Produto vinculado a algum beneficiário. Não pode ser excluído!
	CALL wheb_mensagem_pck.exibir_mensagem_abort(267136);
end if;

RETURN OLD;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_pls_contrato_plano_delete() FROM PUBLIC;

CREATE TRIGGER pls_contrato_plano_delete
	BEFORE DELETE ON pls_contrato_plano FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_pls_contrato_plano_delete();

