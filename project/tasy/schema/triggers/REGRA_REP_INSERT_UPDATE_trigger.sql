-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS regra_rep_insert_update ON regra_consiste_inf_rep CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_regra_rep_insert_update() RETURNS trigger AS $BODY$
BEGIN
if	((NEW.ie_acao		= 'I') or (NEW.ie_momento	<> 'L')) and (coalesce(NEW.IE_CONSIDERAR_AUTOR_CONV,'N') = 'S') then
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(218363);
	/*Para utilizar a regra com o campo "Consistir somente se possuir autorização convênio", o momento de consistir deve ser
	na liberação da prescrição e a ação se não conforme deve ser somente alertar. Isto pois as autorizações são geradas somente na liberação*/
end if;
RETURN NEW;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_regra_rep_insert_update() FROM PUBLIC;

CREATE TRIGGER regra_rep_insert_update
	BEFORE INSERT OR UPDATE ON regra_consiste_inf_rep FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_regra_rep_insert_update();

