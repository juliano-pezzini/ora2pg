-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS atual_sup_planej_compras ON sup_planejamento_compras CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_atual_sup_planej_compras() RETURNS trigger AS $BODY$
declare

BEGIN

if (NEW.hr_entrega is not null) and
   ((NEW.hr_entrega <> OLD.hr_entrega) or (OLD.dt_entrega is null)) then
	NEW.dt_entrega := to_date(to_char(LOCALTIMESTAMP,'dd/mm/yyyy') || ' ' || NEW.hr_entrega || ':00', 'dd/mm/yyyy hh24:mi:ss');
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_atual_sup_planej_compras() FROM PUBLIC;

CREATE TRIGGER atual_sup_planej_compras
	BEFORE INSERT OR UPDATE ON sup_planejamento_compras FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_atual_sup_planej_compras();

