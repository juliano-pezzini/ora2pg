-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS gerint_solic_internacao_pf ON gerint_solic_internacao CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_gerint_solic_internacao_pf() RETURNS trigger AS $BODY$
DECLARE
BEGIN

if (wheb_usuario_pck.get_ie_executar_trigger = 'S') then
	if (coalesce(OLD.cd_pessoa_fisica, 'X') <> coalesce(NEW.cd_pessoa_fisica, 'X') and NEW.cd_pessoa_fisica is not null) then
		NEW.nm_pessoa_fisica := obter_nome_pf(NEW.cd_pessoa_fisica);
	elsif (NEW.cd_pessoa_fisica is null and coalesce(OLD.nm_pessoa_fisica, 'X') = coalesce(NEW.nm_pessoa_fisica, 'X')) then
		NEW.nm_pessoa_fisica := null;
	end if;
end if;

RETURN NEW;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_gerint_solic_internacao_pf() FROM PUBLIC;

CREATE TRIGGER gerint_solic_internacao_pf
	BEFORE INSERT OR UPDATE ON gerint_solic_internacao FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_gerint_solic_internacao_pf();

