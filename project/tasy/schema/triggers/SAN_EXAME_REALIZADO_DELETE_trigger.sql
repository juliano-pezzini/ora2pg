-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS san_exame_realizado_delete ON san_exame_realizado CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_san_exame_realizado_delete() RETURNS trigger AS $BODY$
declare
qt_procedimento_w	double precision;
excluir_todos_proc_w		varchar(1);

BEGIN

excluir_todos_proc_w := obter_param_usuario(450, 257, obter_perfil_ativo, Wheb_usuario_pck.get_nm_usuario, wheb_usuario_pck.get_cd_estabelecimento, excluir_todos_proc_w);

if (OLD.nr_seq_propaci is not null) then
	select coalesce(max(qt_procedimento),0)
	into STRICT	qt_procedimento_w
	from procedimento_paciente
	where nr_sequencia = OLD.nr_seq_propaci;

	if (qt_procedimento_w > 1) and (excluir_todos_proc_w = 'N') then
		update procedimento_paciente
		set qt_procedimento = qt_procedimento - 1
		where nr_sequencia = OLD.nr_seq_propaci;
	else
		delete	FROM procedimento_paciente
		where	NR_SEQ_PROC_PRINC= OLD.nr_seq_propaci;

		delete FROM procedimento_paciente
		where nr_sequencia = OLD.nr_seq_propaci;
	end if;
end if;
RETURN OLD;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_san_exame_realizado_delete() FROM PUBLIC;

CREATE TRIGGER san_exame_realizado_delete
	BEFORE DELETE ON san_exame_realizado FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_san_exame_realizado_delete();

