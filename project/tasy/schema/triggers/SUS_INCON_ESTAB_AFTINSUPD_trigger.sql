-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS sus_incon_estab_aftinsupd ON sus_inconsistencia_estab CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_sus_incon_estab_aftinsupd() RETURNS trigger AS $BODY$
declare

ie_executa		varchar(1) := 'S';
cd_inconsistencia_w             sus_inconsistencia.cd_inconsistencia%type;
BEGIN
  BEGIN
if (wheb_usuario_pck.get_ie_executar_trigger	= 'N')  then
	ie_executa := 'N';
end if;

if (ie_executa = 'S') then
        BEGIN
        select	cd_inconsistencia
        into STRICT	cd_inconsistencia_w
        from	sus_inconsistencia
        where	nr_sequencia	= NEW.nr_seq_inconsistencia;
        exception
        when no_data_found then
                cd_inconsistencia_w := 0;
        when too_many_rows then
                cd_inconsistencia_w := 0;
        when others then
                cd_inconsistencia_w := 0;
        end;

        CALL sus_obter_inco_ativa_pck.set_cd_estabelecimento(wheb_usuario_pck.get_cd_estabelecimento);
        CALL sus_obter_inco_ativa_pck.set_cd_inconsistencia(cd_inconsistencia_w);
        CALL sus_obter_inco_ativa_pck.set_ie_consiste(NEW.ie_consiste);
        CALL sus_obter_inco_ativa_pck.set_ie_aih(NEW.ie_aih);
        CALL sus_obter_inco_ativa_pck.set_ie_apac(NEW.ie_apac);
        CALL sus_obter_inco_ativa_pck.set_ie_bpa_con(NEW.ie_bpa_consolidado);
        CALL sus_obter_inco_ativa_pck.set_ie_bpa_ind(NEW.ie_bpa_individualizado);
        CALL sus_obter_inco_ativa_pck.set_sus_obter_inco_ativa_trg();
end if;
  END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_sus_incon_estab_aftinsupd() FROM PUBLIC;

CREATE TRIGGER sus_incon_estab_aftinsupd
	AFTER INSERT OR UPDATE ON sus_inconsistencia_estab FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_sus_incon_estab_aftinsupd();

