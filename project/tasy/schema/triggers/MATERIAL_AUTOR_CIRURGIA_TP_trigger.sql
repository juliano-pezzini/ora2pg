-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS material_autor_cirurgia_tp ON material_autor_cirurgia CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_material_autor_cirurgia_tp() RETURNS trigger AS $BODY$
DECLARE

nr_seq_w bigint;
ds_s_w   varchar(50);
ds_c_w   varchar(500);
ds_w	   varchar(500);
ie_log_w varchar(1);
BEGIN
  BEGIN 

BEGIN 

ds_s_w := to_char(OLD.NR_SEQUENCIA);
ds_c_w:=null;

ds_w:=substr(NEW.CD_CGC_FORNEC,1,500);
SELECT * FROM gravar_log_alteracao(substr(OLD.CD_CGC_FORNEC,1,4000), substr(NEW.CD_CGC_FORNEC,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_CGC_FORNEC', ie_log_w, ds_w, 'MATERIAL_AUTOR_CIRURGIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;

ds_w:=substr(NEW.CD_MATERIAL,1,500);
SELECT * FROM gravar_log_alteracao(substr(OLD.CD_MATERIAL,1,4000), substr(NEW.CD_MATERIAL,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_MATERIAL', ie_log_w, ds_w, 'MATERIAL_AUTOR_CIRURGIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;

ds_w:=substr(NEW.IE_ORIGEM_PRECO,1,500);
SELECT * FROM gravar_log_alteracao(substr(OLD.IE_ORIGEM_PRECO,1,4000), substr(NEW.IE_ORIGEM_PRECO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_ORIGEM_PRECO', ie_log_w, ds_w, 'MATERIAL_AUTOR_CIRURGIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;

ds_w:=substr(NEW.NR_SEQ_FABRICANTE,1,500);
SELECT * FROM gravar_log_alteracao(substr(OLD.NR_SEQ_FABRICANTE,1,4000), substr(NEW.NR_SEQ_FABRICANTE,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_SEQ_FABRICANTE', ie_log_w, ds_w, 'MATERIAL_AUTOR_CIRURGIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;

ds_w:=substr(NEW.PR_ADICIONAL,1,500);
SELECT * FROM gravar_log_alteracao(substr(OLD.PR_ADICIONAL,1,4000), substr(NEW.PR_ADICIONAL,1,4000), NEW.nm_usuario, nr_seq_w, 'PR_ADICIONAL', ie_log_w, ds_w, 'MATERIAL_AUTOR_CIRURGIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;

ds_w:=substr(NEW.NR_SEQ_MARCA,1,500);
SELECT * FROM gravar_log_alteracao(substr(OLD.NR_SEQ_MARCA,1,4000), substr(NEW.NR_SEQ_MARCA,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_SEQ_MARCA', ie_log_w, ds_w, 'MATERIAL_AUTOR_CIRURGIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;

ds_w:=substr(NEW.IE_VALOR_CONTA,1,500);
SELECT * FROM gravar_log_alteracao(substr(OLD.IE_VALOR_CONTA,1,4000), substr(NEW.IE_VALOR_CONTA,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_VALOR_CONTA', ie_log_w, ds_w, 'MATERIAL_AUTOR_CIRURGIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;

ds_w:=substr(NEW.IE_GERADO_AGENDA,1,500);
SELECT * FROM gravar_log_alteracao(substr(OLD.IE_GERADO_AGENDA,1,4000), substr(NEW.IE_GERADO_AGENDA,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_GERADO_AGENDA', ie_log_w, ds_w, 'MATERIAL_AUTOR_CIRURGIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;

ds_w:=substr(NEW.IE_VALOR_INFORMADO,1,500);
SELECT * FROM gravar_log_alteracao(substr(OLD.IE_VALOR_INFORMADO,1,4000), substr(NEW.IE_VALOR_INFORMADO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_VALOR_INFORMADO', ie_log_w, ds_w, 'MATERIAL_AUTOR_CIRURGIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;

ds_w:=substr(NEW.DS_OBSERVACAO,1,500);
SELECT * FROM gravar_log_alteracao(substr(OLD.DS_OBSERVACAO,1,4000), substr(NEW.DS_OBSERVACAO,1,4000), NEW.nm_usuario, nr_seq_w, 'DS_OBSERVACAO', ie_log_w, ds_w, 'MATERIAL_AUTOR_CIRURGIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;

ds_w:=substr(NEW.NR_SEQ_AUTORIZACAO,1,500);
SELECT * FROM gravar_log_alteracao(substr(OLD.NR_SEQ_AUTORIZACAO,1,4000), substr(NEW.NR_SEQ_AUTORIZACAO,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_SEQ_AUTORIZACAO', ie_log_w, ds_w, 'MATERIAL_AUTOR_CIRURGIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;

ds_w:=substr(NEW.QT_MATERIAL,1,500);
SELECT * FROM gravar_log_alteracao(substr(OLD.QT_MATERIAL,1,4000), substr(NEW.QT_MATERIAL,1,4000), NEW.nm_usuario, nr_seq_w, 'QT_MATERIAL', ie_log_w, ds_w, 'MATERIAL_AUTOR_CIRURGIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;

ds_w:=substr(NEW.VL_MATERIAL,1,500);
SELECT * FROM gravar_log_alteracao(substr(OLD.VL_MATERIAL,1,4000), substr(NEW.VL_MATERIAL,1,4000), NEW.nm_usuario, nr_seq_w, 'VL_MATERIAL', ie_log_w, ds_w, 'MATERIAL_AUTOR_CIRURGIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;

ds_w:=substr(NEW.VL_UNITARIO_MATERIAL,1,500);
SELECT * FROM gravar_log_alteracao(substr(OLD.VL_UNITARIO_MATERIAL,1,4000), substr(NEW.VL_UNITARIO_MATERIAL,1,4000), NEW.nm_usuario, nr_seq_w, 'VL_UNITARIO_MATERIAL', ie_log_w, ds_w, 'MATERIAL_AUTOR_CIRURGIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;

ds_w:=substr(NEW.IE_APROVACAO,1,500);
SELECT * FROM gravar_log_alteracao(substr(OLD.IE_APROVACAO,1,4000), substr(NEW.IE_APROVACAO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_APROVACAO', ie_log_w, ds_w, 'MATERIAL_AUTOR_CIRURGIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;

ds_w:=substr(NEW.QT_SOLICITADA,1,500);
SELECT * FROM gravar_log_alteracao(substr(OLD.QT_SOLICITADA,1,4000), substr(NEW.QT_SOLICITADA,1,4000), NEW.nm_usuario, nr_seq_w, 'QT_SOLICITADA', ie_log_w, ds_w, 'MATERIAL_AUTOR_CIRURGIA', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;

exception when others then 

ds_w:= '1';

end;

  END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_material_autor_cirurgia_tp() FROM PUBLIC;

CREATE TRIGGER material_autor_cirurgia_tp
	AFTER UPDATE ON material_autor_cirurgia FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_material_autor_cirurgia_tp();

