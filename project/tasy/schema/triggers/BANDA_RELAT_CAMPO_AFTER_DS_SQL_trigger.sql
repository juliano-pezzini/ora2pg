-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS banda_relat_campo_after_ds_sql ON banda_relat_campo CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_banda_relat_campo_after_ds_sql() RETURNS trigger AS $BODY$
BEGIN

  IF wheb_usuario_pck.get_ie_executar_trigger = 'S' THEN

    IF coalesce(DOC_EXPORT_AUTO_PKG.LINEARIZE_QUERY(OLD.DS_SQL), ' ') <>
       coalesce(DOC_EXPORT_AUTO_PKG.LINEARIZE_QUERY(NEW.DS_SQL), ' ') THEN

      CALL DOC_EXPORT_AUTO_PKG.BANDA_RELAT_CAMPO_CHANGE_ROW(NEW.NR_SEQUENCIA,
                                                       coalesce(NEW.NR_SEQ_BANDA, OLD.NR_SEQ_BANDA),
                                                       OLD.DS_SQL,
                                                       NEW.DS_SQL);

    END IF;

  END IF;

IF TG_OP = 'DELETE' THEN
	RETURN OLD;
ELSE
	RETURN NEW;
END IF;

END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_banda_relat_campo_after_ds_sql() FROM PUBLIC;

CREATE TRIGGER banda_relat_campo_after_ds_sql
AFTER UPDATE OR INSERT OR DELETE ON banda_relat_campo FOR EACH ROW
	WHEN (coalesce(OLD.DS_SQL, ' ') <> coalesce(NEW.DS_SQL, ' '))
	EXECUTE PROCEDURE trigger_fct_banda_relat_campo_after_ds_sql();

