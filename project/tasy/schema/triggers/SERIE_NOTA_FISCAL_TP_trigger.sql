-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS serie_nota_fiscal_tp ON serie_nota_fiscal CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_serie_nota_fiscal_tp() RETURNS trigger AS $BODY$
DECLARE nr_seq_w bigint;ds_s_w   varchar(1);ds_c_w   varchar(1);ds_w	   varchar(1);ie_log_w varchar(1);BEGIN BEGIN ds_s_w := null;  ds_c_w:= 'CD_SERIE_NF='||to_char(OLD.CD_SERIE_NF)||'#@#@CD_ESTABELECIMENTO='||to_char(OLD.CD_ESTABELECIMENTO);SELECT * FROM gravar_log_alteracao(substr(OLD.NR_ULTIMA_NF,1,4000), substr(NEW.NR_ULTIMA_NF,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_ULTIMA_NF', ie_log_w, ds_w, 'SERIE_NOTA_FISCAL', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_ESTABELECIMENTO,1,4000), substr(NEW.CD_ESTABELECIMENTO,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_ESTABELECIMENTO', ie_log_w, ds_w, 'SERIE_NOTA_FISCAL', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.NR_MIN_CARACTERES,1,4000), substr(NEW.NR_MIN_CARACTERES,1,4000), NEW.nm_usuario, nr_seq_w, 'NR_MIN_CARACTERES', ie_log_w, ds_w, 'SERIE_NOTA_FISCAL', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_SITUACAO,1,4000), substr(NEW.IE_SITUACAO,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_SITUACAO', ie_log_w, ds_w, 'SERIE_NOTA_FISCAL', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.IE_NUMERO_NOTA,1,4000), substr(NEW.IE_NUMERO_NOTA,1,4000), NEW.nm_usuario, nr_seq_w, 'IE_NUMERO_NOTA', ie_log_w, ds_w, 'SERIE_NOTA_FISCAL', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w; SELECT * FROM gravar_log_alteracao(substr(OLD.CD_SERIE_NF,1,4000), substr(NEW.CD_SERIE_NF,1,4000), NEW.nm_usuario, nr_seq_w, 'CD_SERIE_NF', ie_log_w, ds_w, 'SERIE_NOTA_FISCAL', ds_s_w, ds_c_w) INTO STRICT nr_seq_w, ie_log_w;   exception when others then ds_w:= '1'; end;   END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_serie_nota_fiscal_tp() FROM PUBLIC;

CREATE TRIGGER serie_nota_fiscal_tp
	AFTER UPDATE ON serie_nota_fiscal FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_serie_nota_fiscal_tp();

