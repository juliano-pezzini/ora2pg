-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS smart_recomendacao ON prescr_recomendacao CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_smart_recomendacao() RETURNS trigger AS $BODY$
declare
	reg_integracao_w		gerar_int_padrao.reg_integracao;
BEGIN
  BEGIN
BEGIN
	if (OLD.DT_SUSPENSAO is null) and (NEW.DT_SUSPENSAO is not null) then
		BEGIN
			reg_integracao_w := gerar_int_padrao.gravar_integracao('300', NEW.NR_PRESCRICAO, NEW.nm_usuario, reg_integracao_w, 'NR_PRESCRICAO=' || NEW.NR_PRESCRICAO || ';NR_SEQUENCIA=' || NEW.NR_SEQUENCIA|| ';DS_OPERACAO=UPDATE');
		END;
	end if;
exception when others then
	null;
end;

  END;
RETURN NEW;
END
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_smart_recomendacao() FROM PUBLIC;

CREATE TRIGGER smart_recomendacao
	AFTER UPDATE ON prescr_recomendacao FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_smart_recomendacao();

