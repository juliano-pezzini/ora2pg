-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS pls_contestacao_proc_update ON pls_contestacao_proc CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_pls_contestacao_proc_update() RETURNS trigger AS $BODY$
declare
nr_seq_proc_w		bigint;
nr_seq_analise_w	bigint;

BEGIN
select	max(b.nr_sequencia),
	max(a.nr_seq_analise)
into STRICT	nr_seq_proc_w,
	nr_seq_analise_w
from	pls_conta		a,
	pls_conta_proc		b
where	b.nr_seq_conta		= a.nr_sequencia
and	b.nr_sequencia		= OLD.nr_seq_conta_proc;

if	((NEW.vl_contestado > OLD.vl_procedimento) and ((OLD.vl_contestado <> NEW.vl_contestado) or (OLD.vl_procedimento <> NEW.vl_procedimento))) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(170908, 'NR_SEQ_ANALISE_W=' || nr_seq_analise_w ||';'||
							'NR_SEQ_PROC_W=' || nr_seq_proc_w);
end if;

if	((NEW.qt_contestada > OLD.qt_procedimento) and ((OLD.qt_contestada <> NEW.qt_contestada) or (OLD.qt_procedimento <> NEW.qt_procedimento))) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(170909, 'NR_SEQ_ANALISE_W=' || nr_seq_analise_w ||';'||
							'NR_SEQ_PROC_W=' || nr_seq_proc_w);
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_pls_contestacao_proc_update() FROM PUBLIC;

CREATE TRIGGER pls_contestacao_proc_update
	BEFORE INSERT OR UPDATE ON pls_contestacao_proc FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_pls_contestacao_proc_update();

