-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS atend_pac_mentor_insert ON atendimento_paciente CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_atend_pac_mentor_insert() RETURNS trigger AS $BODY$
declare

DS_ERRO_WW varchar(1);
jobno	bigint;
BEGIN
  BEGIN

	dbms_job.submit(jobno, 'GQA_GERAR_PROTOCOLO_ASSIST(' || NEW.nr_atendimento || ', 10, ''' || NEW.nm_usuario||''');', (LOCALTIMESTAMP + interval '7 seconds'));
EXCEPTION WHEN OTHERS THEN
	DS_ERRO_WW := NULL;
  END;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_atend_pac_mentor_insert() FROM PUBLIC;

CREATE TRIGGER atend_pac_mentor_insert
	AFTER INSERT ON atendimento_paciente FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_atend_pac_mentor_insert();

