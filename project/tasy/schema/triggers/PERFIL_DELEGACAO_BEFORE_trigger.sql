-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS perfil_delegacao_before ON perfil_delegacao CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_perfil_delegacao_before() RETURNS trigger AS $BODY$
BEGIN

    if (coalesce(wheb_usuario_pck.get_ie_executar_trigger, 'N') = 'S' and
        NEW.DT_INICIO_DELEGACAO is not null and NEW.DT_FIM_DELEGACAO is not null
        and PKG_DATE_UTILS.get_DiffDate(NEW.DT_FIM_DELEGACAO, NEW.DT_INICIO_DELEGACAO ,'DAY') > 0) then
            CALL wheb_mensagem_pck.exibir_mensagem_abort(212526);
    end if;
RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_perfil_delegacao_before() FROM PUBLIC;

CREATE TRIGGER perfil_delegacao_before
	BEFORE INSERT OR UPDATE ON perfil_delegacao FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_perfil_delegacao_before();

