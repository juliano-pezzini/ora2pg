-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS diagnostico_doenca_h_tie_trg ON diagnostico_doenca_hist CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_diagnostico_doenca_h_tie_trg() RETURNS trigger AS $BODY$
declare
   json_data_w text;
   ds_param_integration_w  varchar(500);
   nr_sequencia_p varchar(200);

BEGIN

if (wheb_usuario_pck.get_ie_executar_trigger    = 'S')  then
if (coalesce(pkg_i18n.get_user_locale, 'pt_BR') = 'ja_JP') then
    if (wheb_usuario_pck.get_nm_usuario is not null) then
            nr_sequencia_p := NEW.nr_sequencia;
       -- determine the transaction type
       if (TG_OP = 'INSERT' and NEW.NR_SEQ_INTERNO is not null and NEW.IE_STATUS is not null ) then
        ds_param_integration_w :=  '{"recordId" : "' || NEW.nr_sequencia|| '"' || '}';
        CALL execute_bifrost_integration(279,ds_param_integration_w);
	end if;
   end if;
  end if;
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_diagnostico_doenca_h_tie_trg() FROM PUBLIC;

CREATE TRIGGER diagnostico_doenca_h_tie_trg
	AFTER INSERT ON diagnostico_doenca_hist FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_diagnostico_doenca_h_tie_trg();

