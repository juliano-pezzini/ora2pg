-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

DROP TRIGGER IF EXISTS eme_reg_resposta_after_update ON eme_reg_resposta CASCADE;
CREATE OR REPLACE FUNCTION trigger_fct_eme_reg_resposta_after_update() RETURNS trigger AS $BODY$
declare
atualizar_chamado_w	varchar(1);
BEGIN
atualizar_chamado_w := obter_param_usuario(989, 51, obter_perfil_ativo, Wheb_usuario_pck.get_nm_usuario, wheb_usuario_pck.get_cd_estabelecimento, atualizar_chamado_w);

if	((atualizar_chamado_w = 'S') and
	((OLD.DT_PARTIDA <> NEW.DT_PARTIDA) or (OLD.DT_CHEGADA_LOCAL <> NEW.DT_CHEGADA_LOCAL) or (OLD.DT_SAIDA_LOCAL <> NEW.DT_SAIDA_LOCAL) or (OLD.DT_CHEGADA_BASE <> NEW.DT_CHEGADA_BASE))) then
	update	eme_chamado
	set	DT_SAIDA = NEW.DT_PARTIDA,
		DT_INICIO_ATEND = NEW.DT_CHEGADA_LOCAL,
		DT_FIM_ATEND = NEW.DT_SAIDA_LOCAL,
		DT_RETORNO = NEW.DT_CHEGADA_BASE
	where	nr_seq_regulacao = NEW.NR_SEQ_REG;
end if;

RETURN NEW;
end
$BODY$
 LANGUAGE 'plpgsql' SECURITY DEFINER;
-- REVOKE ALL ON FUNCTION trigger_fct_eme_reg_resposta_after_update() FROM PUBLIC;

CREATE TRIGGER eme_reg_resposta_after_update
	AFTER UPDATE ON eme_reg_resposta FOR EACH ROW
	EXECUTE PROCEDURE trigger_fct_eme_reg_resposta_after_update();

