-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE inserir_motivo_dif_convenio ( nr_sequencia_p bigint, nr_seq_motivo_dif_p bigint, nr_atendimento_p bigint) AS $body$
DECLARE


nr_sequencia_w 	atend_paciente_unidade.nr_sequencia%type;


BEGIN

if (coalesce(nr_atendimento_p,0) > 0) then
	
	select	coalesce(max(nr_sequencia),0)
	into STRICT	nr_sequencia_w
	from 	atend_paciente_unidade a
	where	a.nr_atendimento 		= nr_atendimento_p
	and		obter_classif_setor_atend(nr_atendimento_p) = 3;

	update	atend_paciente_unidade
	set     nr_seq_motivo_dif = nr_seq_motivo_dif_p
	where	nr_sequencia = nr_sequencia_w
	and		nr_atendimento = nr_atendimento_p;
	
end if;

if (coalesce(wheb_usuario_pck.get_ie_commit, 'S') = 'S') then commit; end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE inserir_motivo_dif_convenio ( nr_sequencia_p bigint, nr_seq_motivo_dif_p bigint, nr_atendimento_p bigint) FROM PUBLIC;

