-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE alterar_dt_entrega_item_cot ( nr_cot_compra_p bigint, nr_item_cot_compra_p bigint, nm_usuario_p text) AS $body$
DECLARE


nr_item_cot_compra_w		cot_compra_item.nr_item_cot_compra%type;
dt_cot_item_w			cot_compra_item.dt_limite_entrega%type;
dt_cot_item_ant_w		cot_compra_item.dt_limite_entrega%type;

c01 CURSOR FOR
	SELECT	dt_limite_entrega,
		nr_item_cot_compra
	from	cot_compra_item
	where	nr_cot_compra = nr_cot_compra_p
	and	nr_item_cot_compra <> nr_item_cot_compra_p;


BEGIN

select	trunc(dt_limite_entrega,'dd')
into STRICT	dt_cot_item_w
from	cot_compra_item
where	nr_cot_compra = nr_cot_compra_p
and	nr_item_cot_compra = nr_item_cot_compra_p;

open c01;
loop
fetch c01 into
	dt_cot_item_ant_w,
	nr_item_cot_compra_w;
EXIT WHEN NOT FOUND; /* apply on c01 */
	begin

	update	cot_compra_item
	set	dt_limite_entrega = dt_cot_item_w
	where	nr_cot_compra = nr_cot_compra_p
	and	nr_item_cot_compra = nr_item_cot_compra_w;

	if (dt_cot_item_w IS NOT NULL AND dt_cot_item_w::text <> '') then
		update	cot_compra_item_entrega
		set	dt_entrega = dt_cot_item_w
		where	nr_cot_compra = nr_cot_compra_p
		and	nr_item_cot_compra = nr_item_cot_compra_w
		and	trunc(dt_entrega,'dd') = dt_cot_item_ant_w;
	end if;
	end;
end loop;
close c01;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE alterar_dt_entrega_item_cot ( nr_cot_compra_p bigint, nr_item_cot_compra_p bigint, nm_usuario_p text) FROM PUBLIC;

