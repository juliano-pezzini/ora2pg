-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE ageint_excluir_item ( nr_seq_item_p bigint, nm_usuario_p text) AS $body$
DECLARE


nr_seq_agenda_cons_w	agenda_integrada_item.nr_seq_agenda_cons%type;
nr_seq_agenda_exame_w	agenda_integrada_item.nr_seq_agenda_exame%type;
			

BEGIN

select	max(nr_seq_agenda_cons),
	max(nr_seq_agenda_exame)
into STRICT	nr_seq_agenda_cons_w,
	nr_seq_agenda_exame_w
from	agenda_integrada_item
where	nr_sequencia	= nr_seq_item_p;

if (coalesce(nr_seq_agenda_cons_w,0) > 0) and (coalesce(nr_seq_agenda_exame_w,0) > 0) then
	delete	FROM ageint_equip_item
	where	nr_seq_agenda_item	= nr_seq_item_p;

	delete	FROM agenda_integrada_prof_item
	where	nr_seq_agenda_item	= nr_seq_item_p;

	delete	FROM agenda_integrada_item
	where	nr_sequencia	= nr_seq_item_p;
end if;
	
commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE ageint_excluir_item ( nr_seq_item_p bigint, nm_usuario_p text) FROM PUBLIC;

