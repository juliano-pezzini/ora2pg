-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE hd_paciente_faltou_js (cd_pessoa_fisica_p text, ds_observacao_p text, cd_unid_dialise_p text, nr_Seq_motivo_fim_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text, dt_registro_p timestamp, ds_erro_p INOUT text) AS $body$
DECLARE


ds_erro_w		    varchar(255);
nr_atendimento_w    bigint;
dt_registro_w       timestamp;


BEGIN

dt_registro_w := dt_registro_p;

if (coalesce(dt_registro_w::text, '') = '') then
dt_registro_w := clock_timestamp();
end if;

nr_atendimento_w := hd_obter_ultimo_atend_pac(cd_pessoa_fisica_p,cd_estabelecimento_p);

ds_erro_w := hd_paciente_faltou(cd_pessoa_fisica_p, ds_observacao_p, cd_unid_dialise_p, nr_atendimento_w, nr_Seq_motivo_fim_p, cd_estabelecimento_p, nm_usuario_p, dt_registro_w, ds_erro_w);

ds_erro_p := ds_erro_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE hd_paciente_faltou_js (cd_pessoa_fisica_p text, ds_observacao_p text, cd_unid_dialise_p text, nr_Seq_motivo_fim_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text, dt_registro_p timestamp, ds_erro_p INOUT text) FROM PUBLIC;

