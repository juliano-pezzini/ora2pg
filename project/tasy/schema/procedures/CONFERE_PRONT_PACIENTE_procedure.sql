-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE confere_pront_paciente ( nm_usuario_p text, cd_pessoa_fisica_p text, nr_Atendimento_p bigint, nr_seq_lote_p bigint, cd_estabelecimento_p bigint) AS $body$
DECLARE


qt_itens_w                 bigint;
nr_prontuario_w         bigint;
ds_parametro_w        varchar(10) := obter_valor_param_usuario(941,103,obter_perfil_ativo,nm_usuario_p,cd_estabelecimento_p);


BEGIN
nr_prontuario_w := obter_prontuario_pf(cd_estabelecimento_p,cd_pessoa_fisica_p);

select  count(*)
into STRICT qt_itens_w
from same_prontuario
where cd_pessoa_fisica  = cd_pessoa_fisica_p
and ((nr_atendimento  = coalesce(nr_Atendimento_p,0)) or (coalesce(nr_Atendimento_p,0) = 0))
and ((ds_parametro_w = 'N') or (cd_estabelecimento = cd_estabelecimento_p));

if (qt_itens_w = 0) then
  begin
  CALL wheb_mensagem_pck.exibir_mensagem_abort(200838,'NR_PRONTUARIO='||nr_prontuario_w);
  end;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE confere_pront_paciente ( nm_usuario_p text, cd_pessoa_fisica_p text, nr_Atendimento_p bigint, nr_seq_lote_p bigint, cd_estabelecimento_p bigint) FROM PUBLIC;

