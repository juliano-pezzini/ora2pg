-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_agenda_serv_chamada_ext (nr_seq_agenda_p bigint, cd_pessoa_fisica_p text, nm_usuario_p text, nr_seq_pac_reab_p bigint default null) AS $body$
BEGIN
if (nr_seq_agenda_p IS NOT NULL AND nr_seq_agenda_p::text <> '') and (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') then 
 
	update	agenda_consulta 
	set	nm_usuario_origem    = nm_usuario		 
	where	coalesce(nm_usuario_origem::text, '') = '' 
        and     nr_sequencia 	  = nr_seq_agenda_p;
 
	update	agenda_consulta 
	set	cd_pessoa_fisica	= cd_pessoa_fisica_p, 
		ie_status_agenda 	= 'N', 
		nm_paciente 		= substr(obter_nome_pf(cd_pessoa_fisica_p),1,60), 
		nr_seq_pac_reab		= nr_seq_pac_reab_p,	 
		nm_usuario		= nm_usuario_p 
	where	nr_sequencia 		= nr_seq_agenda_p;
end if;
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_agenda_serv_chamada_ext (nr_seq_agenda_p bigint, cd_pessoa_fisica_p text, nm_usuario_p text, nr_seq_pac_reab_p bigint default null) FROM PUBLIC;

