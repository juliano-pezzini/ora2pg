-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE ptu_inserir_inconsistencia_js ( ie_inconsistencias_ptu_p text, nr_seq_proc_origem_p text, nr_seq_mat_origem_p text, nr_seq_requisicao_p bigint, cd_estabelecimento_p bigint, ie_parametro_p text, nm_usuario_p text) AS $body$
DECLARE


qt_inconsisit_item_w	varchar(10);
ie_inconsis_inserida_w	varchar(2);

BEGIN

if (ie_inconsistencias_ptu_p = '') then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(267448);
end if;

select	substr(pls_obter_qt_inconsisit_item(nr_seq_proc_origem_p, nr_seq_mat_origem_p, '00304'),1,255)
into STRICT	qt_inconsisit_item_w
;

if (qt_inconsisit_item_w = '5') then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(267449);
end if;

select	substr(pls_obter_se_inconsis_inserida(nr_seq_proc_origem_p, nr_seq_mat_origem_p, ie_inconsistencias_ptu_p, '00304'),1,255)
into STRICT	ie_inconsis_inserida_w
;

if (ie_inconsis_inserida_w = 'S') then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(267451);
end if;

CALL ptu_inserir_inconsistencia(null, null, ie_inconsistencias_ptu_p, '', cd_estabelecimento_p, nr_seq_requisicao_p, 'R',
			'00304', nr_seq_proc_origem_p, nr_seq_mat_origem_p, ie_parametro_p, nm_usuario_p);

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE ptu_inserir_inconsistencia_js ( ie_inconsistencias_ptu_p text, nr_seq_proc_origem_p text, nr_seq_mat_origem_p text, nr_seq_requisicao_p bigint, cd_estabelecimento_p bigint, ie_parametro_p text, nm_usuario_p text) FROM PUBLIC;

