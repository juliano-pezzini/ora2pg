-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE checkup_update ( NR_SEQUENCIA_P NUMERIC, TOKEN_CHECKUP_P text ) AS $body$
DECLARE

  NR_SEQUENCIA_W CHECKUP.NR_SEQUENCIA%TYPE;
  TOKEN_CHECKUP_W CHECKUP.TOKEN_CHECKUP%TYPE;

BEGIN
  IF (NR_SEQUENCIA_P IS NOT NULL AND NR_SEQUENCIA_P::text <> '') THEN
    SELECT
      NR_SEQUENCIA,
      TOKEN_CHECKUP
    INTO STRICT
      NR_SEQUENCIA_W,
      TOKEN_CHECKUP_W
    FROM CHECKUP
    WHERE
      NR_SEQUENCIA = NR_SEQUENCIA_P;

    IF ((NR_SEQUENCIA_W IS NOT NULL AND NR_SEQUENCIA_W::text <> '') AND (coalesce(TOKEN_CHECKUP_W::text, '') = '' OR TOKEN_CHECKUP_W <> TOKEN_CHECKUP_P)) THEN
      UPDATE CHECKUP
      SET
        TOKEN_CHECKUP = TOKEN_CHECKUP_P
      WHERE
        NR_SEQUENCIA = NR_SEQUENCIA_W;
    END IF;
  END IF;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE checkup_update ( NR_SEQUENCIA_P NUMERIC, TOKEN_CHECKUP_P text ) FROM PUBLIC;

