-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE com_duplicar_estimativa_padrao ( nr_seq_estimativa_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint) AS $body$
DECLARE


nr_seq_estimativa_w	bigint;
cd_empresa_w		smallint;
nr_seq_estim_item_w	bigint;
nr_seq_estimativa_item_w	bigint;

c01 CURSOR FOR
SELECT	nr_sequencia
from	com_estimativa_item
where	nr_seq_estimativa	= nr_seq_estimativa_p;


BEGIN

select	cd_empresa
into STRICT	cd_empresa_w
from	estabelecimento
where	cd_estabelecimento	= cd_estabelecimento_p;

select	nextval('com_estimativa_seq')
into STRICT	nr_seq_estimativa_w
;

insert into com_estimativa(
	nr_sequencia,
	dt_atualizacao,
	nm_usuario,
	dt_atualizacao_nrec,
	nm_usuario_nrec,
	ds_estimativa,
	cd_empresa,
	dt_inicio_vigencia,
	ie_situacao)
SELECT	nr_seq_estimativa_w,
	clock_timestamp(),
	nm_usuario_p,
	clock_timestamp(),
	nm_usuario_p,
	substr('Cópia de ' || ds_estimativa,1,80),
	cd_empresa_w,
	dt_inicio_vigencia,
	ie_situacao
from	com_estimativa
where	nr_sequencia = nr_seq_estimativa_p;

open c01;
loop
fetch c01 into
	nr_seq_estim_item_w;
EXIT WHEN NOT FOUND; /* apply on c01 */

select	nextval('com_estimativa_item_seq')
into STRICT	nr_seq_estimativa_item_w
;

insert into com_estimativa_item(
	nr_sequencia,
	nr_seq_estimativa,
	dt_atualizacao,
	nm_usuario,
	dt_atualizacao_nrec,
	nm_usuario_nrec,
	nr_seq_apres,
	nr_seq_mod_implant,
	ds_complemento,
	qt_hora,
	ie_situacao,
	nr_seq_fase)
SELECT	nr_seq_estimativa_item_w,
	nr_seq_estimativa_w,
	clock_timestamp(),
	nm_usuario_p,
	clock_timestamp(),
	nm_usuario_p,
	nr_seq_apres,
	nr_seq_mod_implant,
	ds_complemento,
	qt_hora,
	ie_situacao,
	nr_seq_fase
from	com_estimativa_item
where	nr_sequencia	= nr_seq_estim_item_w;

end loop;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE com_duplicar_estimativa_padrao ( nr_seq_estimativa_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint) FROM PUBLIC;

