-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE diops_cad_gerar_adm ( nr_seq_operadora_p bigint, nr_seq_periodo_p bigint, nm_usuario_p text) AS $body$
DECLARE

			 
cd_pessoa_fisica_w	varchar(10);
nr_cpf_w		varchar(11);
nm_administrador_w	varchar(60);
nm_mae_w		varchar(60);
cd_cargo_w		integer;
dt_inicio_mandato_w	timestamp;
dt_fim_mandato_w	timestamp;

C01 CURSOR FOR 
	SELECT	cd_pessoa_fisica, 
		substr(obter_dados_pf(cd_pessoa_fisica, 'CPF'),1,11), 
		substr(obter_nome_pf(cd_pessoa_fisica),1,60), 
		substr(obter_compl_pf(cd_pessoa_fisica, 5, 'N'),1,60), 
		cd_cargo, 
		dt_inicio_mandato, 
		dt_fim_mandato 
	from	pls_administrador_ops 
	where	nr_seq_operadora	= nr_seq_operadora_p;


BEGIN 
open C01;
loop 
fetch C01 into	 
	cd_pessoa_fisica_w, 
	nr_cpf_w, 
	nm_administrador_w, 
	nm_mae_w, 
	cd_cargo_w, 
	dt_inicio_mandato_w, 
	dt_fim_mandato_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin 
	insert into w_diops_cad_administrador(nr_sequencia, 
		nr_seq_operadora, 
		nr_cpf, 
		nm_administrador, 
		nm_mae, 
		cd_cargo, 
		dt_inicio_mandato, 
		dt_fim_mandato, 
		dt_atualizacao, 
		nm_usuario, 
		nr_seq_periodo, 
		cd_pessoa_fisica) 
	values (nextval('w_diops_cad_administrador_seq'), 
		nr_seq_operadora_p, 
		nr_cpf_w, 
		nm_administrador_w, 
		nm_mae_w, 
		cd_cargo_w, 
		dt_inicio_mandato_w, 
		dt_fim_mandato_w, 
		clock_timestamp(), 
		nm_usuario_p, 
		nr_seq_periodo_p, 
		cd_pessoa_fisica_w);
	end;
end loop;
close C01;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE diops_cad_gerar_adm ( nr_seq_operadora_p bigint, nr_seq_periodo_p bigint, nm_usuario_p text) FROM PUBLIC;

