-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE obter_regra_atributo_wfiltro ( nr_seq_wfiltro_p bigint, cd_estabelecimento_p bigint, cd_perfil_p bigint, cd_setor_atendimento_p bigint, cd_funcao_p bigint, nm_usuario_regra_p text, ds_retorno_p INOUT text) AS $body$
DECLARE


nm_atributo_w	varchar(50);
ie_readonly_w	varchar(1);
vl_padrao_w	varchar(255);

C01 CURSOR FOR
	SELECT	b.nm_atributo,
		a.ie_readonly,
		a.vl_padrao
	from	atrib_regra_wfiltro a,
		dic_objeto_filtro b
	where	a.nr_seq_wfiltro = nr_seq_wfiltro_p
	and	a.nr_seq_atrib_wfiltro = b.nr_sequencia
	and	coalesce(a.cd_estabelecimento, cd_estabelecimento_p) = cd_estabelecimento_p
	and	coalesce(a.cd_perfil, cd_perfil_p) = cd_perfil_p
	and	coalesce(a.cd_setor_atendimento, cd_setor_atendimento_p) = cd_setor_atendimento_p
	and	coalesce(a.cd_funcao, cd_funcao_p) = cd_funcao_p
	and	coalesce(a.nm_usuario_regra, nm_usuario_regra_p) = nm_usuario_regra_p;


BEGIN

ds_retorno_p := '<?xml version="1.0" encoding="ISO-8859-1"?><regras>';

open C01;
loop
fetch C01 into
	nm_atributo_w,
	ie_readonly_w,
	vl_padrao_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin

	ds_retorno_p := ds_retorno_p || '<regra>';

	ds_retorno_p := ds_retorno_p || '<nmAtributo>' || nm_atributo_w || '</nmAtributo>';
	ds_retorno_p := ds_retorno_p || '<vlPadrao>' || vl_padrao_w || '</vlPadrao>';
	ds_retorno_p := ds_retorno_p || '<ieReadonly>' || ie_readonly_w || '</ieReadonly>';

	ds_retorno_p := ds_retorno_p || '</regra>';

	end;
end loop;
close C01;

ds_retorno_p := ds_retorno_p || '</regras>';

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE obter_regra_atributo_wfiltro ( nr_seq_wfiltro_p bigint, cd_estabelecimento_p bigint, cd_perfil_p bigint, cd_setor_atendimento_p bigint, cd_funcao_p bigint, nm_usuario_regra_p text, ds_retorno_p INOUT text) FROM PUBLIC;

