-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE sus_rateio_sh_conta ( nr_interno_conta_p bigint, nm_usuario_p text) AS $body$
DECLARE



nr_seq_propaci_w	bigint;
nr_seq_matpaci_w	bigint;
vl_receita_w		double precision;


/* Busca os itens rateados da conta*/

C01 CURSOR FOR
SELECT	coalesce(nr_seq_propaci,0),
	coalesce(nr_seq_matpaci,0),
	vl_receita
from 	w_conta_sus_sh;



BEGIN

open C01;
loop
	fetch C01 into
		nr_seq_propaci_w,
		nr_seq_matpaci_w,
		vl_receita_w;
	EXIT WHEN NOT FOUND; /* apply on C01 */
	/* Atualizar os procedimentos da conta */

	if (nr_Seq_propaci_w	> 0) then
		update	procedimento_paciente
		set	vl_procedimento	= vl_receita_w
		where	nr_sequencia	= nr_seq_propaci_w;
	/* Atualizar os materiais da conta */

	elsif (nr_seq_matpaci_w	> 0) then
		update	material_atend_paciente
		set	vl_material	= vl_receita_w
		where	nr_sequencia	= nr_seq_matpaci_w;
	end if;
end loop;
close C01;

commit;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE sus_rateio_sh_conta ( nr_interno_conta_p bigint, nm_usuario_p text) FROM PUBLIC;

