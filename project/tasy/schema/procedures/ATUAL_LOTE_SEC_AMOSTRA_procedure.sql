-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';



--
-- dblink wrapper to call function atual_lote_sec_amostra as an autonomous transaction
--
CREATE EXTENSION IF NOT EXISTS dblink;

CREATE OR REPLACE PROCEDURE atual_lote_sec_amostra ( nr_prescricao_p bigint, ie_amostra_p text) AS $body$
DECLARE
	-- Change this to reflect the dblink connection string
	v_conn_str  text := format('port=%s dbname=%s user=%s', current_setting('port'), current_database(), current_user);
	v_query     text;

BEGIN
	v_query := 'CALL atual_lote_sec_amostra_atx ( ' || quote_nullable(nr_prescricao_p) || ',' || quote_nullable(ie_amostra_p) || ' )';
	PERFORM * FROM dblink(v_conn_str, v_query) AS p (ret boolean);

END;
$body$ LANGUAGE plpgsql SECURITY DEFINER;




CREATE OR REPLACE PROCEDURE atual_lote_sec_amostra_atx ( nr_prescricao_p bigint, ie_amostra_p text) AS $body$
BEGIN

update	lote_ent_sec_ficha
set		ie_amostra = ie_amostra_p
where	nr_prescricao = nr_prescricao_p
and		coalesce(ie_amostra,'N') = 'N';

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE atual_lote_sec_amostra ( nr_prescricao_p bigint, ie_amostra_p text) FROM PUBLIC; -- REVOKE ALL ON PROCEDURE atual_lote_sec_amostra_atx ( nr_prescricao_p bigint, ie_amostra_p text) FROM PUBLIC;

