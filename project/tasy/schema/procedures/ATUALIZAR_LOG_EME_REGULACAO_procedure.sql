-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE atualizar_log_eme_regulacao ( nr_sequencia_p bigint, cd_log_p bigint, nm_usuario_p text) AS $body$
DECLARE


qt_existe_w	integer;
ds_log_w	varchar(255);

BEGIN
ds_log_w := '';
/*if	(nr_sequencia_p is not null) and
	(nm_usuario_p is not null) then
	begin
	select	count(*)
	into	qt_existe_w
	from	eme_regulacao
	where	nr_sequencia = nr_sequencia_p;

	if	(qt_existe_w > 0) then
		begin
		ds_log_w := 'Sequencia ' + nr_sequencia_p + '.' + 'qt_existe ' + qt_existe_w;
		insert into
		logxxxxxx_tasy(
			dt_atualizacao,
			nm_usuario,
			cd_log,
			ds_log)
		values (
			sysdate,
			nm_usuario_p,
			cd_log_p,
			ds_log_w);
		end;
	end if;
	end;
end if;
commit;*/
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE atualizar_log_eme_regulacao ( nr_sequencia_p bigint, cd_log_p bigint, nm_usuario_p text) FROM PUBLIC;

