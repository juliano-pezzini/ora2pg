-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE baca_centro_controle_unidade () AS $body$
DECLARE


c01 CURSOR FOR
SELECT	a.cd_estabelecimento,
	a.cd_centro_controle,
	a.nr_seq_unid_neg
from	centro_controle a
where	(a.nr_seq_unid_neg IS NOT NULL AND a.nr_seq_unid_neg::text <> '')
and	not exists (	select	1
			from	centro_controle_unid_neg y
			where	y.cd_estabelecimento	= a.cd_estabelecimento
			and	y.cd_centro_controle	= a.cd_centro_controle
			and	y.nr_seq_unid_neg	= a.nr_seq_unid_neg);

vet01	c01%RowType;


BEGIN

open C01;
loop
fetch C01 into
	vet01;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin

	insert into centro_controle_unid_neg(
		nr_sequencia,
		cd_estabelecimento,
		cd_centro_controle,
		nr_seq_unid_neg,
		nm_usuario,
		dt_atualizacao,
		nm_usuario_nrec,
		dt_atualizacao_nrec)
	values (	nextval('centro_controle_unid_neg_seq'),
		vet01.cd_estabelecimento,
		vet01.cd_centro_controle,
		vet01.nr_seq_unid_neg,
		'Tasy',
		clock_timestamp(),
		'Tasy',
		clock_timestamp());

	end;
end loop;
close C01;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE baca_centro_controle_unidade () FROM PUBLIC;

