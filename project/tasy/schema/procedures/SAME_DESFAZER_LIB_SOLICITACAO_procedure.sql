-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE same_desfazer_lib_solicitacao ( nr_seq_lote_p bigint, nr_seq_solic_p bigint, nm_usuario_p text) AS $body$
DECLARE


dt_liberacao_w		timestamp	:= clock_timestamp();
nr_seq_solic_w		bigint;

c01 CURSOR FOR
	SELECT	nr_sequencia
	from	same_solic_pront
	where	coalesce(nr_seq_lote, 0)	= coalesce(coalesce(nr_seq_lote_p, nr_seq_lote), 0)
	and	nr_sequencia		= coalesce(nr_seq_solic_p, nr_sequencia);


BEGIN

open c01;
loop
fetch c01 into
	nr_seq_solic_w;
EXIT WHEN NOT FOUND; /* apply on c01 */
	begin
	update	same_solic_pront
        set	dt_liberacao	 = NULL
        where	nr_sequencia	= nr_seq_solic_w;
	end;
end loop;
close c01;

if (nr_seq_lote_p IS NOT NULL AND nr_seq_lote_p::text <> '') then
	update	same_solic_pront_lote
	set	dt_liberacao	 = NULL
	where	nr_sequencia	= nr_seq_lote_p;
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE same_desfazer_lib_solicitacao ( nr_seq_lote_p bigint, nr_seq_solic_p bigint, nm_usuario_p text) FROM PUBLIC;

