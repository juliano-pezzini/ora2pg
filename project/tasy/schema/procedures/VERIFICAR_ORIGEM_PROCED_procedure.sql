-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE verificar_origem_proced ( cd_procedimento_p bigint, ie_origem_proced_p bigint, ie_classificacao_p INOUT bigint, ds_retorno_p INOUT text) AS $body$
DECLARE


qt_registro_w			bigint;
ie_origem_proced_w		bigint;
ie_classificacao_w		bigint;


BEGIN

select	count(*),
	max(ie_classificacao)
into STRICT	qt_registro_w,
	ie_classificacao_w
from	procedimento
where	cd_procedimento	= cd_procedimento_p
and	ie_origem_proced	= ie_origem_proced_p;

if (qt_registro_w = 1) then
	ds_retorno_p		:= '';
else
	select	count(*),
		max(ie_origem_proced),
		max(ie_classificacao)
	into STRICT	qt_registro_w,
		ie_origem_proced_w,
		ie_classificacao_w
	from	procedimento
	where	cd_procedimento	= cd_procedimento_p
	and	ie_ignora_origem	= 'S';
	if (qt_registro_w = 1) then
		ds_retorno_p		:= ie_origem_proced_w;
	else
		ds_retorno_p		:= WHEB_MENSAGEM_PCK.get_texto(311560);
	end if;
end if;

ie_classificacao_p			:= ie_classificacao_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE verificar_origem_proced ( cd_procedimento_p bigint, ie_origem_proced_p bigint, ie_classificacao_p INOUT bigint, ds_retorno_p INOUT text) FROM PUBLIC;

