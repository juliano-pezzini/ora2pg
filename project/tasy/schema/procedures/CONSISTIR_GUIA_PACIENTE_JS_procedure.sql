-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consistir_guia_paciente_js ( cd_convenio_p bigint, cd_estabelecimento_p bigint, nr_guia_p text, nr_atendimento_p bigint, ds_erro_p INOUT text) AS $body$
DECLARE

 
ie_permite_cod_w	varchar(1);
ds_consistencia_w	varchar(255);
ds_retorno_w		varchar(255);	
 

BEGIN 
 
if (cd_convenio_p IS NOT NULL AND cd_convenio_p::text <> '')then 
	begin 
	 
	select 	coalesce(max(ie_permite_cod_convenio_duplic),'N') 
	into STRICT	ie_permite_cod_w 
	from  	convenio_estabelecimento 
	where 	cd_convenio = cd_convenio_p 
	and  	cd_estabelecimento = cd_estabelecimento_p;
	 
	if (ie_permite_cod_w = 'S')then 
		begin 
		 
		ds_consistencia_w := consistir_guia_duplic_paciente(cd_convenio_p, nr_guia_p, nr_atendimento_p, ds_consistencia_w);
		 
		if (ds_consistencia_w = 'N')then 
			begin 
			 
			ds_erro_p	:= wheb_mensagem_pck.get_texto(279629);
			 
			end;
		end if;
		 
		end;
	end if;
	 
	end;
end if;
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consistir_guia_paciente_js ( cd_convenio_p bigint, cd_estabelecimento_p bigint, nr_guia_p text, nr_atendimento_p bigint, ds_erro_p INOUT text) FROM PUBLIC;

