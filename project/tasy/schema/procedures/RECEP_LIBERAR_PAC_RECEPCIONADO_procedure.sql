-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE recep_liberar_pac_recepcionado ( nm_usuario_p text) AS $body$
DECLARE


cd_pessoa_fisica_w	bigint;
nr_sequencia_w 		bigint;

BEGIN
	select 	max(nr_sequencia)
	into STRICT 	nr_sequencia_w
	from	recep_ficha_pre
	where 	nm_usuario_recep = nm_usuario_p
	and 	ie_pac_recepcao = 'S';

	select 	max(cd_pessoa_fisica)
	into STRICT 	cd_pessoa_fisica_w
	from	recep_ficha_pre
	where 	nm_usuario_recep = nm_usuario_p
	and 	ie_pac_recepcao = 'S'
	and	nr_sequencia = nr_sequencia_w;

	if (cd_pessoa_fisica_w IS NOT NULL AND cd_pessoa_fisica_w::text <> '')
		and (nr_sequencia_w IS NOT NULL AND nr_sequencia_w::text <> '') then
		begin
		CALL recep_pac_recepcionado(nr_sequencia_w, cd_pessoa_fisica_w, 'N', nm_usuario_p);
		end;
	end if;
commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE recep_liberar_pac_recepcionado ( nm_usuario_p text) FROM PUBLIC;

