-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE intpd_status_integracao_sinc ( nr_seq_fila_p bigint, ie_status_p INOUT text, ds_log_p INOUT text) AS $body$
BEGIN

if (coalesce(nr_seq_fila_p,0) = 0) then
	ds_log_p := 'Não há integração';
else

	select
		ie_status,
		ds_log
	into STRICT
		ie_status_p,
		ds_log_p
	from intpd_fila_transmissao
	where nr_sequencia = nr_seq_fila_p;

end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE intpd_status_integracao_sinc ( nr_seq_fila_p bigint, ie_status_p INOUT text, ds_log_p INOUT text) FROM PUBLIC;

