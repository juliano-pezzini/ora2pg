-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE proj_excluir_cron_etapa ( nr_seq_etapa_p bigint, nm_usuario_p text) AS $body$
DECLARE


nr_seq_interno_w	bigint;
nr_seq_superior_w	bigint;
nr_seq_cronograma_w	bigint;



BEGIN

select	max(nr_seq_interno),
	max(nr_seq_cronograma),
	max(nr_seq_superior)
into STRICT	nr_seq_interno_w,
	nr_seq_cronograma_w,
	nr_seq_superior_w
from	proj_cron_etapa
where	nr_sequencia = nr_seq_etapa_p;

update	proj_cron_etapa
set	nr_seq_interno = nr_seq_interno - 1,
	nr_predecessora = nr_predecessora - 1
where	nr_seq_interno >= nr_seq_interno_w
and	nr_Seq_cronograma = nr_seq_cronograma_w;

commit;

/*
update	proj_cron_etapa
set	nr_predecessora = nr_predecessora - 1
where	nr_seq_interno >= nr_seq_interno_w +1;
*/
commit;

delete	from proj_cron_etapa
where	nr_sequencia = nr_seq_etapa_p;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE proj_excluir_cron_etapa ( nr_seq_etapa_p bigint, nm_usuario_p text) FROM PUBLIC;

