-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gera_excl_proced_principal ( nr_prescricao_p bigint, nr_seq_proc_interno_p bigint) AS $body$
DECLARE


nr_sequencia_w	integer;
qt_w			bigint;
nr_seq_interno_w	bigint;


BEGIN

select	max(nr_sequencia),
		max(nr_seq_interno)
into STRICT	nr_sequencia_w,
		nr_seq_interno_w
from	prescr_procedimento
where	nr_seq_proc_interno	= nr_seq_proc_interno_p
and		nr_prescricao		= nr_prescricao_p;

select	count(*)
into STRICT	qt_w
from	prescr_Procedimento
where	nr_prescricao	= nr_prescricao_p
and		nr_seq_proc_excl = nr_seq_interno_w;

if (qt_w > 0) and (nr_sequencia_w IS NOT NULL AND nr_sequencia_w::text <> '') then
	delete
	from	prescr_procedimento
	where	nr_prescricao	= nr_prescricao_p
	and	nr_sequencia	= nr_sequencia_w;
end if;


commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gera_excl_proced_principal ( nr_prescricao_p bigint, nr_seq_proc_interno_p bigint) FROM PUBLIC;

