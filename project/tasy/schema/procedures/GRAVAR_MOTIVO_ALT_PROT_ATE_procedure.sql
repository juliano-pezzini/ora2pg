-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gravar_motivo_alt_prot_ate ( nr_seq_paciente_p bigint, nr_seq_atendimento_p bigint, nr_seq_motivo_p bigint, ds_justificativa_p text, cd_profissional_p text, ie_acao_p text, nm_usuario_p text) AS $body$
DECLARE



ie_tipo_alteracao_w	varchar(15);


BEGIN

if (coalesce(nr_seq_motivo_p::text, '') = '') and (coalesce(ds_justificativa_p::text, '') = '') then
	-- É necessário inserir uma justificativa, ou um motivo padrão. #@#@
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(264500);
end if;


ie_tipo_alteracao_w	:= ie_acao_p;
insert into paciente_prot_medic_hist(	nr_sequencia,
					dt_atualizacao,
					nm_usuario,
					dt_atualizacao_nrec,
					nm_usuario_nrec,
					dt_alteracao,
					cd_profissional,
					ie_tipo_alteracao,
					nr_seq_motivo,
					ds_justificativa,
					nr_seq_paciente,
					nr_seq_atendimento)
		values (	nextval('paciente_prot_medic_hist_seq'),
					clock_timestamp(),
					nm_usuario_p,
					clock_timestamp(),
					nm_usuario_p,
					clock_timestamp(),
					cd_profissional_p,
					ie_tipo_alteracao_w,
					nr_seq_motivo_p,
					ds_justificativa_p,
					nr_seq_paciente_p,
					nr_seq_atendimento_p);

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gravar_motivo_alt_prot_ate ( nr_seq_paciente_p bigint, nr_seq_atendimento_p bigint, nr_seq_motivo_p bigint, ds_justificativa_p text, cd_profissional_p text, ie_acao_p text, nm_usuario_p text) FROM PUBLIC;

