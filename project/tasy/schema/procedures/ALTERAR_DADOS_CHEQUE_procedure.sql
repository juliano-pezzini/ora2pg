-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE alterar_dados_cheque ( cd_banco_p bigint, cd_agencia_p text, nr_conta_p text, nr_cheque_p text, nr_seq_cheque_p bigint) AS $body$
BEGIN
if (cd_banco_p IS NOT NULL AND cd_banco_p::text <> '')
	and (cd_agencia_p IS NOT NULL AND cd_agencia_p::text <> '')
	and (nr_conta_p IS NOT NULL AND nr_conta_p::text <> '')
	and (nr_cheque_p IS NOT NULL AND nr_cheque_p::text <> '')
	and (nr_seq_cheque_p IS NOT NULL AND nr_seq_cheque_p::text <> '') then
	begin
	update  	cheque_cr a
	set     	a.cd_banco  = cd_banco_p,
		a.cd_agencia_bancaria  = cd_agencia_p,
		a.nr_conta             	= nr_conta_p,
		a.nr_cheque            = nr_cheque_p
	where   	a.nr_seq_cheque    = nr_seq_cheque_p;
	commit;
	end;
else
	CALL wheb_mensagem_pck.exibir_mensagem_abort(228171);
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE alterar_dados_cheque ( cd_banco_p bigint, cd_agencia_p text, nr_conta_p text, nr_cheque_p text, nr_seq_cheque_p bigint) FROM PUBLIC;

