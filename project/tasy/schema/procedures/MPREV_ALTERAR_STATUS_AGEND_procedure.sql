-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE mprev_alterar_status_agend ( nr_seq_status_p bigint, nr_seq_agendamento_p bigint, ie_opcao_p text, nm_usuario_p text) AS $body$
BEGIN

if (nr_seq_status_p IS NOT NULL AND nr_seq_status_p::text <> '' AND nr_seq_agendamento_p IS NOT NULL AND nr_seq_agendamento_p::text <> '') then

	if (upper(ie_opcao_p) = 'P') then
		update mprev_agendamento
		set nr_seq_status_pac = nr_seq_status_p,
		nm_usuario = nm_usuario_p,
		dt_atualizacao = clock_timestamp()
		where nr_sequencia = nr_seq_agendamento_p;
	end if;

end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE mprev_alterar_status_agend ( nr_seq_status_p bigint, nr_seq_agendamento_p bigint, ie_opcao_p text, nm_usuario_p text) FROM PUBLIC;

