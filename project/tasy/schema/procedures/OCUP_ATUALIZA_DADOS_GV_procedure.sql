-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE ocup_atualiza_dados_gv ( cd_setor_atendimento_p bigint, cd_unidade_basica_p text, cd_unidade_compl_p text, cd_pessoa_fisica_p text, nm_usuario_p text) AS $body$
BEGIN
 
if (coalesce(cd_setor_atendimento_p,0) > 0) and (cd_unidade_basica_p IS NOT NULL AND cd_unidade_basica_p::text <> '') and (cd_unidade_compl_p IS NOT NULL AND cd_unidade_compl_p::text <> '') and (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') then 
 
	update	gestao_vaga 
	set	cd_setor_desejado = cd_setor_atendimento_p, 
		cd_unidade_basica = cd_unidade_basica_p, 
		cd_unidade_compl = cd_unidade_compl_p, 
		ie_status	 = 'R', 
		nm_usuario	 = nm_usuario_p, 
		dt_atualizacao 	 = clock_timestamp() 
	where	cd_pessoa_fisica = cd_pessoa_fisica_p 
	and	nr_sequencia = (	SELECT 	max(nr_sequencia) 
					from 	gestao_vaga 
					where 	cd_pessoa_fisica = cd_pessoa_fisica_p 
					and	ie_status in ('A','P','H','L','T','E','R'));
end if;
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE ocup_atualiza_dados_gv ( cd_setor_atendimento_p bigint, cd_unidade_basica_p text, cd_unidade_compl_p text, cd_pessoa_fisica_p text, nm_usuario_p text) FROM PUBLIC;

