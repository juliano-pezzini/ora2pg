-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE incluir_proc_atend_prescr_js ( dt_procedimento_p timestamp, cd_procedimento_p bigint, nr_seq_exame_p bigint, ie_origem_proced_p bigint, nr_seq_proc_interno_p bigint, ie_via_acesso_p text, qt_procedimento_p bigint, nr_prescricao_p bigint, nr_sequencia_prescricao_p bigint, cd_setor_atendimento_p bigint, nr_seq_cor_exec_p bigint, cd_medico_executor_p text, cd_cgc_prestador_p text, nm_usuario_p text, nr_sequencia_p INOUT bigint) AS $body$
DECLARE


nr_seq_atepacu_w	procedimento_paciente.nr_seq_atepacu%type;
nr_atendimento_w	procedimento_paciente.nr_atendimento%type;
dt_entrada_unidade_w	procedimento_paciente.dt_entrada_unidade%type;



BEGIN

if (coalesce(nr_prescricao_p,0) > 0) then

	select	max(nr_atendimento)
	into STRICT	nr_atendimento_w
	from 	prescr_medica
	where 	nr_prescricao = nr_prescricao_p;

end if;


if (coalesce(nr_atendimento_w,0) > 0) then

	select 	max(nr_sequencia)
	into STRICT	nr_seq_atepacu_w
	from 	atend_paciente_unidade
	where 	nr_atendimento = nr_atendimento_w
	and 	cd_setor_atendimento = cd_setor_atendimento_p;

end if;

if (coalesce(nr_seq_atepacu_w,0) > 0) then

	select	max(dt_entrada_unidade)
	into STRICT	dt_entrada_unidade_w
	from 	atend_paciente_unidade
	where 	nr_sequencia = nr_seq_atepacu_w;

end if;

select	nextval('procedimento_paciente_seq')
into STRICT	nr_sequencia_p
;

insert into	procedimento_paciente(nr_sequencia,
	nm_usuario,
	dt_atualizacao,
	dt_procedimento,
	cd_procedimento,
	nr_seq_exame,
	ie_origem_proced,
	nr_seq_proc_interno,
	ie_via_acesso,
	qt_procedimento,
	nr_prescricao,
	nr_sequencia_prescricao,
	cd_setor_atendimento,
	nr_seq_cor_exec,
	cd_medico_executor,
	cd_cgc_prestador,
	nr_atendimento,
	dt_entrada_unidade,
	nr_seq_atepacu)
values (nr_sequencia_p,
	nm_usuario_p,
	clock_timestamp(),
	dt_procedimento_p,
	cd_procedimento_p,
	nr_seq_exame_p,
	ie_origem_proced_p,
	nr_seq_proc_interno_p,
	ie_via_acesso_p,
	qt_procedimento_p,
	nr_prescricao_p,
	nr_sequencia_prescricao_p,
	cd_setor_atendimento_p,
	nr_seq_cor_exec_p,
	cd_medico_executor_p,
	cd_cgc_prestador_p,
	nr_atendimento_w,
	dt_entrada_unidade_w,
	nr_seq_atepacu_w);

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE incluir_proc_atend_prescr_js ( dt_procedimento_p timestamp, cd_procedimento_p bigint, nr_seq_exame_p bigint, ie_origem_proced_p bigint, nr_seq_proc_interno_p bigint, ie_via_acesso_p text, qt_procedimento_p bigint, nr_prescricao_p bigint, nr_sequencia_prescricao_p bigint, cd_setor_atendimento_p bigint, nr_seq_cor_exec_p bigint, cd_medico_executor_p text, cd_cgc_prestador_p text, nm_usuario_p text, nr_sequencia_p INOUT bigint) FROM PUBLIC;

