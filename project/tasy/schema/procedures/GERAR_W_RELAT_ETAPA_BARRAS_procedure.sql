-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_w_relat_etapa_barras ( nr_atendimento_p bigint, nr_interno_conta_p bigint, nr_prontuario_p bigint, nm_paciente_p text, nr_seq_etapa_p bigint, cd_setor_atendimento_p bigint, ds_setor_atendimento_p text, ds_etapa_p text, cd_motivo_dev_etapa_p bigint, ds_motivo_dev_etapa_p text, cd_pessoa_etapa_p text, nm_pessoa_etapa_p text, ds_observacao_p text, nm_usuario_p text) AS $body$
DECLARE

 
nr_seq_protocolo_w	bigint;
vl_conta_w		double precision;


BEGIN 
 
if (nr_interno_conta_p > 0) then 
 
	select	max(nr_seq_protocolo), 
		max(obter_valor_conta(nr_interno_conta_p,0)) 
	into STRICT	nr_seq_protocolo_w, 
		vl_conta_w 
	from 	conta_paciente 
	where	nr_interno_conta = nr_interno_conta_p;
 
end if;
 
insert 	into w_relat_etapa_barras(	nr_atendimento,         
					nr_interno_conta,        
					nr_prontuario,          
					nm_paciente,           
					nr_seq_etapa,          
					ds_setor_atendimento,      
					ds_etapa,            
					dt_atualizacao,         
					nm_usuario,           
					dt_atualizacao_nrec,       
					nm_usuario_nrec,         
					cd_motivo_dev_etapa,       
					ds_motivo_dev_etapa,       
					cd_pessoa_etapa,         
					nm_pessoa_etapa,         
					ds_observacao, 
					nr_seq_protocolo, 
					vl_conta) 
	values (	nr_atendimento_p, 
		nr_interno_conta_p, 
		nr_prontuario_p, 
		nm_paciente_p, 
		nr_seq_etapa_p, 
		ds_setor_atendimento_p, 
		ds_etapa_p, 
		clock_timestamp(), 
		nm_usuario_p, 
		clock_timestamp(), 
		nm_usuario_p, 
		cd_motivo_dev_etapa_p, 
		ds_motivo_dev_etapa_p, 
		cd_pessoa_etapa_p, 
		nm_pessoa_etapa_p, 
		ds_observacao_p, 
		nr_seq_protocolo_w, 
		vl_conta_w);
 
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_w_relat_etapa_barras ( nr_atendimento_p bigint, nr_interno_conta_p bigint, nr_prontuario_p bigint, nm_paciente_p text, nr_seq_etapa_p bigint, cd_setor_atendimento_p bigint, ds_setor_atendimento_p text, ds_etapa_p text, cd_motivo_dev_etapa_p bigint, ds_motivo_dev_etapa_p text, cd_pessoa_etapa_p text, nm_pessoa_etapa_p text, ds_observacao_p text, nm_usuario_p text) FROM PUBLIC;

