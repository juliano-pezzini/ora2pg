-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consiste_prontuario_eup_js ( ie_forma_geracao_pront_anual_p text, ie_pront_cad_pf_p text, nm_usuario_p text, cd_pessoa_fisica_p text, ds_msg_erro_p INOUT text, ds_msg_impressao_p INOUT text) AS $body$
DECLARE


cd_pessoa_fisica_w	varchar(10);
ds_msg_erro_w		varchar(255);
nr_prontuario_w		bigint;


BEGIN

if (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') then

	Select	max(cd_pessoa_fisica)
	into STRICT	cd_pessoa_fisica_w
	from	pessoa_fisica
	where	nr_prontuario = Somente_Numero(cd_pessoa_fisica_p);

	if (ie_pront_cad_pf_p = 'S') then

		if (cd_pessoa_fisica_w IS NOT NULL AND cd_pessoa_fisica_w::text <> '') then

			ds_msg_erro_w := substr(obter_texto_dic_objeto(105383, wheb_usuario_pck.get_nr_seq_idioma, 'ITEM='||cd_pessoa_fisica_w),1,255);
			goto final;
		else
			nr_prontuario_w := Somente_Numero(cd_pessoa_fisica_p);
		end if;
	else
		if (ie_forma_geracao_pront_anual_p <> 'N') then
		nr_prontuario_w := gerar_prontuario_anual(ie_forma_geracao_pront_anual_p, nm_usuario_p, nr_prontuario_w);
		end if;
	end if;
	CALL Atualizar_Prontuario_PF_EUP(cd_pessoa_fisica_p, nr_prontuario_w, nm_usuario_p);

	ds_msg_impressao_p := substr(obter_texto_tasy(316069,null),1,255);
end if;

<<final>>

ds_msg_erro_p := ds_msg_erro_w;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consiste_prontuario_eup_js ( ie_forma_geracao_pront_anual_p text, ie_pront_cad_pf_p text, nm_usuario_p text, cd_pessoa_fisica_p text, ds_msg_erro_p INOUT text, ds_msg_impressao_p INOUT text) FROM PUBLIC;

