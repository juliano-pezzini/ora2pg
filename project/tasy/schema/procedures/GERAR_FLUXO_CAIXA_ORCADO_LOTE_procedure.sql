-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_fluxo_caixa_orcado_lote ( nr_seq_lote_fluxo_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) AS $body$
DECLARE

 
/*--------------------------------------------------------------- ATENÇÃO ----------------------------------------------------------------*/
 
/* Cuidado ao realizar alterações no fluxo de caixa em lote. Toda e qualquer alteração realizada em qualquer uma */
 
/* das procedures do fluxo de caixa em lote deve ser cuidadosamente verificada e realizada no fluxo de caixa   */
 
/* convencional. Devemos garantir que os dois fluxos de caixa tragam os mesmos valores no resultado, evitando   */
 
/* assim que existam diferenças entre os fluxos de caixa.                                                */
 
/*--------------- AO ALTERAR O FLUXO DE CAIXA EM LOTE ALTERAR TAMBÉM O FLUXO DE CAIXA ---------------*/
 
 
cd_empresa_w		empresa.cd_empresa%type;
ie_restringe_estab_w	fluxo_caixa_lote.ie_restringe_estab%type;
dt_inicio_w		timestamp;
dt_final_w		timestamp;


BEGIN 
 
select	max(a.cd_empresa) 
into STRICT	cd_empresa_w 
from	estabelecimento a 
where	a.cd_estabelecimento	= cd_estabelecimento_p;
 
select	max(a.ie_restringe_estab) 
into STRICT	ie_restringe_estab_w 
from	fluxo_caixa_lote a 
where	a.nr_sequencia	= nr_seq_lote_fluxo_p;
 
select	trunc(max(a.dt_mesano_referencia),'month') dt_inico, 
	fim_mes(max(a.dt_mesano_referencia)) dt_final, 
	max(a.ie_restringe_estab) 
into STRICT	dt_inicio_w, 
	dt_final_w, 
	ie_restringe_estab_w 
from	fluxo_caixa_lote a 
where	a.nr_sequencia	= nr_seq_lote_fluxo_p;
 
delete	from fluxo_caixa_item 
where	nr_seq_lote_fluxo	= nr_seq_lote_fluxo_p;
 
delete	from fluxo_caixa_data 
where	nr_seq_lote_fluxo	= nr_seq_lote_fluxo_p;
 
CALL GERAR_FLUXO_CAIXA_LOTE_ESP(	nr_seq_lote_fluxo_p, 
				cd_estabelecimento_p, 
				dt_inicio_w, 
				dt_final_w, 
				nm_usuario_p, 
				cd_empresa_w, 
				ie_restringe_estab_w);
 
CALL CALCULAR_FLUXO_CAIXA_LOTE(	nr_seq_lote_fluxo_p, 
				cd_estabelecimento_p, 
				0, /*tem que ver da onde vem o valor padrão */
 
				dt_inicio_w, 
				dt_final_w, 
				'O', 
				nm_usuario_p, 
				cd_empresa_w, 
				ie_restringe_estab_w, 
				'N');
 
CALL GERAR_FLUXO_CAIXA_LOTE_DATA(	nr_seq_lote_fluxo_p, 
				nm_usuario_p, 
				cd_estabelecimento_p);
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_fluxo_caixa_orcado_lote ( nr_seq_lote_fluxo_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) FROM PUBLIC;

