-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE agenda_pac_opme_before_post ( nm_usuario_p text, cd_material_p bigint, ds_mensagem_p INOUT text) AS $body$
DECLARE

ds_valor_aux_w		varchar(255);
ds_valor_parametro_w	varchar(255);
qt_retorno_w		bigint;
qt_estoque_w		double precision;
qt_conv_est_w		double precision;
ie_tipo_convenio_w	smallint;


BEGIN
if (cd_material_p IS NOT NULL AND cd_material_p::text <> '') and (nm_usuario_p IS NOT NULL AND nm_usuario_p::text <> '') then
	begin
	ds_valor_parametro_w := obter_parametro_funcao(871, 440, nm_usuario_p);
	if (ds_valor_parametro_w <> 'N') then
		begin
		ds_valor_aux_w := obter_se_validade_anvisa(cd_material_p, null, wheb_usuario_pck.get_cd_estabelecimento);
		if (ds_valor_aux_w = 'N') then
			begin
			if (ds_valor_parametro_w = 'S') then
				begin
				CALL Wheb_mensagem_pck.exibir_mensagem_abort(69966);
				end;
			else
				begin
				ds_mensagem_p := obter_texto_tasy(69967, wheb_usuario_pck.get_nr_seq_idioma);
				end;
			end if;
			end;
		end if;
		end;
	end if;
	end;
end if;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE agenda_pac_opme_before_post ( nm_usuario_p text, cd_material_p bigint, ds_mensagem_p INOUT text) FROM PUBLIC;

