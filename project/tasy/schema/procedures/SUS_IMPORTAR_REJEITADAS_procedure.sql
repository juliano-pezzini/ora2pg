-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE sus_importar_rejeitadas ( nr_aih_p bigint, cd_processo_p bigint, cd_proc_realizado_p bigint, dt_alta_p timestamp, ds_erro_p text, nr_seq_protocolo_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) AS $body$
DECLARE

 
cd_cgc_w	varchar(14);
nr_seq_aih_w	bigint;
dt_apres_w	timestamp;


BEGIN 
 
select	cd_cgc 
into STRICT	cd_cgc_w 
from	estabelecimento 
where	cd_estabelecimento	= cd_estabelecimento_p;
 
select	max(coalesce(nr_sequencia,0)) 
into STRICT	nr_seq_aih_w 
from	sus_aih 
where	nr_aih	= nr_aih_p;
 
select	dt_mesano_referencia 
into STRICT	dt_apres_w 
from	protocolo_convenio 
where	nr_seq_protocolo	= nr_seq_protocolo_p;
 
insert into sus_aih_rejeitada( 
			nr_sequencia, 
			dt_apresentacao, 
			nr_aih, 
			nr_processo, 
			cd_cgc, 
			cd_proc_realizado, 
			dt_alta, 
			ds_erro, 
			dt_atualizacao, 
			nm_usuario, 
			nr_seq_protocolo, 
			nr_seq_aih, 
			ie_origem_proced, 
			dt_atualizacao_nrec, 
			nm_usuario_nrec) 
		values ( 
			nextval('sus_aih_rejeitada_seq'), 
			dt_apres_w, 
			nr_aih_p, 
			cd_processo_p, 
			cd_cgc_w, 
			cd_proc_realizado_p, 
			dt_alta_p, 
			ds_erro_p, 
			clock_timestamp(), 
			nm_usuario_p, 
			nr_seq_protocolo_p, 
			nr_seq_aih_w, 
			2, 
			clock_timestamp(), 
			nm_usuario_p);
 
commit;	
 
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE sus_importar_rejeitadas ( nr_aih_p bigint, cd_processo_p bigint, cd_proc_realizado_p bigint, dt_alta_p timestamp, ds_erro_p text, nr_seq_protocolo_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) FROM PUBLIC;

