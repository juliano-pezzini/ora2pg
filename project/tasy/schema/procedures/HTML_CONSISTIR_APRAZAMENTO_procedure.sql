-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE html_consistir_aprazamento (( nr_prescricao_p bigint, nr_seq_cpoe_p bigint, dt_horario_p timestamp, nm_usuario_p text, cd_perfil_p bigint, cd_estabelecimento_p bigint, ds_consistencia_p out text) is nr_atendimento_w prescr_medica.nr_atendimento%type) AS $body$
BEGIN
		if (ds_macro_p IS NOT NULL AND ds_macro_p::text <> '') then 
			return;
		else 
			return;
		end if;
	end;
							 
begin 
 
if (nr_prescricao_p IS NOT NULL AND nr_prescricao_p::text <> '') and (nr_seq_cpoe_p IS NOT NULL AND nr_seq_cpoe_p::text <> '') and (dt_horario_p IS NOT NULL AND dt_horario_p::text <> '') then 
	 
ie_parametro_adep_51_w := obter_param_usuario(1113, 51, cd_perfil_p, nm_usuario_p, cd_estabelecimento_p, ie_parametro_adep_51_w);
 
select	max(nr_atendimento) 
into STRICT	nr_atendimento_w 
from	prescr_medica 
where	nr_prescricao = nr_prescricao_p;
 
open c01;
loop 
fetch c01 into	nr_seq_material_w, 
				cd_material_w, 
				cd_intervalo_w, 
				qt_dose_w, 
				ie_se_necessario_w, 
				ie_acm_w, 
				cd_unidade_medida_dose_w, 
				ie_via_aplicacao_w;
EXIT WHEN NOT FOUND; /* apply on c01 */
begin 
	ie_acm_sn_w := 'N';
	if (ie_se_necessario_w = 'S') or (ie_acm_w = 'S') then 
		ie_acm_sn_w := 'S';
	end if;
				 
	ds_consistencia_w := consistir_aprazamento(	cd_estabelecimento_p, nr_atendimento_w, nr_prescricao_p, nr_seq_material_w, cd_material_w, cd_intervalo_w, qt_dose_w, 'M', ie_acm_sn_w, null, dt_horario_p, 0, ds_consistencia_w, nm_usuario_p, cd_unidade_medida_dose_w, ie_via_aplicacao_w);		
							 
	if (ds_consistencia_w IS NOT NULL AND ds_consistencia_w::text <> '') then 
		exit;
	end if;
end;
end loop;
close c01;
 
if (ds_consistencia_w IS NOT NULL AND ds_consistencia_w::text <> '') then 
	if (ie_parametro_adep_51_w = 'C') then 
		ds_consistencia_p :=  obter_texto(29708,'MENSAGEM='||ds_consistencia_w);
	elsif (ie_parametro_adep_51_w = 'I') then 
		CALL Wheb_mensagem_pck.exibir_mensagem_abort(ds_consistencia_w);
	end if;
end if;
						 
end if;
						 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE html_consistir_aprazamento (( nr_prescricao_p bigint, nr_seq_cpoe_p bigint, dt_horario_p timestamp, nm_usuario_p text, cd_perfil_p bigint, cd_estabelecimento_p bigint, ds_consistencia_p out text) is nr_atendimento_w prescr_medica.nr_atendimento%type) FROM PUBLIC;

