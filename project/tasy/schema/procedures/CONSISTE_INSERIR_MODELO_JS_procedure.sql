-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consiste_inserir_modelo_js ( nr_sequencia_p bigint, cd_pessoa_fisica_p text, nr_seq_modelo_p text, cd_estabelecimento_p bigint, nm_usuario_p text) AS $body$
DECLARE


ds_existe_pac_reab_w	varchar(100);


BEGIN

if (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '')then
	begin

	ds_existe_pac_reab_w	:= rp_obter_seq_paciente_reab(cd_pessoa_fisica_p, cd_estabelecimento_p);

	if (ds_existe_pac_reab_w IS NOT NULL AND ds_existe_pac_reab_w::text <> '')then
		begin

		CALL inserir_modelo_agendamento(nr_seq_modelo_p, ds_existe_pac_reab_w, nm_usuario_p, clock_timestamp(),null,null,null,null,null);

		CALL rp_altera_status_lista(nr_sequencia_p, 'T');

		end;
	else
		begin

		CALL Wheb_mensagem_pck.exibir_mensagem_abort(203912);

		end;
	end if;


	end;
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consiste_inserir_modelo_js ( nr_sequencia_p bigint, cd_pessoa_fisica_p text, nr_seq_modelo_p text, cd_estabelecimento_p bigint, nm_usuario_p text) FROM PUBLIC;

