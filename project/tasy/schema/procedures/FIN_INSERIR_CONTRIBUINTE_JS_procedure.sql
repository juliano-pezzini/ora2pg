-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE fin_inserir_contribuinte_js ( nr_seq_contribuinte_p bigint, ds_contribuinte_p text, vl_pago_p bigint, dt_faturamento_p timestamp, dt_vencimento_p timestamp, dt_pagto_conta_p timestamp, nm_usuario_p text, ie_commit_p text) AS $body$
DECLARE


nr_seq_contribuinte_w		bigint	:= 0;
nr_sequencia_w			bigint	:= 0;


BEGIN

select	coalesce(max(nr_sequencia), 0) + 1
into STRICT	nr_sequencia_w
from	contribuinte_movto;

insert into contribuinte_movto(
	nr_seq_contribuinte,
	nr_sequencia,
	dt_vencimento,
	dt_movimento,
	ie_tipo_movimento,
	vl_movimento,
	dt_atualizacao,
	nm_usuario)
values (	nr_seq_contribuinte_P,
	nr_sequencia_w,
	dt_vencimento_p,
	dt_pagto_conta_p,
	'C',
	vl_pago_p,
	clock_timestamp(),
	nm_usuario_p);

if (coalesce(ie_commit_p, 'N') = 'S') then
	commit;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE fin_inserir_contribuinte_js ( nr_seq_contribuinte_p bigint, ds_contribuinte_p text, vl_pago_p bigint, dt_faturamento_p timestamp, dt_vencimento_p timestamp, dt_pagto_conta_p timestamp, nm_usuario_p text, ie_commit_p text) FROM PUBLIC;

