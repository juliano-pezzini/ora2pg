-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE validate_anzics_rep_generation (dt_inical_p timestamp, dt_final_p timestamp) AS $body$
DECLARE

  ds_atendimento_w  varchar(4000);

BEGIN
   select obter_select_concatenado_bv(' SELECT nr_atendimento FROM ATEND_ANZICS WHERE IE_SITUACAO = ' ||  '''A''' || ' AND DT_LIBERACAO IS NULL AND DT_GENERATE BETWEEN TRUNC(:dt_inical_p) AND fim_dia(:dt_final_p)','dt_inical_p=dt_inical_p;dt_final_p=dt_final_p;',',')
   into STRICT ds_atendimento_w;

  IF (ds_atendimento_w IS NOT NULL AND ds_atendimento_w::text <> '') THEN
    CALL Wheb_mensagem_pck.exibir_mensagem_abort(obter_desc_exp_idioma(941243,wheb_usuario_pck.get_nr_seq_idioma,'DS_ATENDIMENTO=' ||ds_atendimento_w));
  END IF;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE validate_anzics_rep_generation (dt_inical_p timestamp, dt_final_p timestamp) FROM PUBLIC;

