-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gc_prescr_proced_dblclick_js ( nr_seq_proc_interno_p bigint, nr_prescricao_p bigint, nr_atendimento_p bigint, cd_convenio_p bigint, nm_atributo_p text, nm_usuario_p text, cd_estabelecimento_p bigint, ie_origem_proced_p INOUT bigint, cd_procedimento_p INOUT bigint, ds_procedimento_p INOUT text, ie_classificacao_p INOUT text) AS $body$
DECLARE

 
cd_procedimento_w	bigint;
ie_origem_proced_w	bigint;
ds_procedimento_w	varchar(255);
nr_origem_proced_cat_w	bigint;
ie_classificacao_w	varchar(1);
						

BEGIN		 
if (nm_atributo_p = 'NR_SEQ_PROC_INTERNO') and (nr_seq_proc_interno_p IS NOT NULL AND nr_seq_proc_interno_p::text <> '') and (nr_atendimento_p IS NOT NULL AND nr_atendimento_p::text <> '') and (nr_prescricao_p IS NOT NULL AND nr_prescricao_p::text <> '') then 
	begin 
	SELECT * FROM obter_proc_tab_interno(nr_seq_proc_interno_p, nr_prescricao_p, nr_atendimento_p, 0, cd_procedimento_w, ie_origem_proced_w, null, null) INTO STRICT cd_procedimento_w, ie_origem_proced_w;
	 
	ds_procedimento_w := substr(obter_exame_agenda(0,0,nr_seq_proc_interno_p),1,240);
	end;
end if;
 
if (nm_atributo_p = 'CD_PROCEDIMENTO') and (nr_atendimento_p IS NOT NULL AND nr_atendimento_p::text <> '') and (cd_convenio_p IS NOT NULL AND cd_convenio_p::text <> '') then 
	begin 
	nr_origem_proced_cat_w := atualizar_origem_procedimento(cd_estabelecimento_p, cd_convenio_p, '0', 'C', nr_atendimento_p, nr_origem_proced_cat_w);	
	 
	ds_procedimento_w := obter_desc_prescr_proc(cd_procedimento_p,nr_origem_proced_cat_w,nr_seq_proc_interno_p);
	end;
end if;
 
 
if	((nm_atributo_p = 'CD_PROCEDIMENTO') or (nm_atributo_p = 'NR_SEQ_PROC_INTERNO'))and (nr_atendimento_p IS NOT NULL AND nr_atendimento_p::text <> '') and (cd_convenio_p IS NOT NULL AND cd_convenio_p::text <> '') then 
	begin 
	 
	select	coalesce(max(ie_classificacao),1) 
	into STRICT	ie_classificacao_w 
	from 	procedimento 
	where 	cd_procedimento = cd_procedimento_p 
	and	ie_origem_proced= ie_origem_proced_p;
	 
	end;
end if;
 
 
 
cd_procedimento_p	:= cd_procedimento_w;
ie_origem_proced_p	:= ie_origem_proced_w;
ds_procedimento_p	:= ds_procedimento_w;
ie_classificacao_p	:= ie_classificacao_w;
				 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gc_prescr_proced_dblclick_js ( nr_seq_proc_interno_p bigint, nr_prescricao_p bigint, nr_atendimento_p bigint, cd_convenio_p bigint, nm_atributo_p text, nm_usuario_p text, cd_estabelecimento_p bigint, ie_origem_proced_p INOUT bigint, cd_procedimento_p INOUT bigint, ds_procedimento_p INOUT text, ie_classificacao_p INOUT text) FROM PUBLIC;

