-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE lab_liberar_tent_comunic (nr_prescricao_p bigint, nr_seq_prescr_p bigint, nr_sequencia_p bigint, ie_comunicacao_critico_p text, nm_usuario_p text) AS $body$
DECLARE


cd_prescr_proc_compl_seq_w  bigint;


BEGIN

  select nr_seq_proc_compl
  into STRICT cd_prescr_proc_compl_seq_w
  from PRESCR_PROCEDIMENTO pp
  where pp.nr_prescricao = nr_prescricao_p
  and pp.nr_sequencia = nr_seq_prescr_p;

  if (cd_prescr_proc_compl_seq_w IS NOT NULL AND cd_prescr_proc_compl_seq_w::text <> '') then
    update PRESCR_PROCEDIMENTO_COMPL ppc
    set ppc.ie_comunicacao_critico = ie_comunicacao_critico_p
    where ppc.nr_sequencia = cd_prescr_proc_compl_seq_w;
  end if;

  update LAB_COMUNICACAO_CRITICO
  set dt_liberacao = clock_timestamp(),
      nm_usuario = nm_usuario_p,
      dt_atualizacao = clock_timestamp()
  where nr_sequencia = nr_sequencia_p;

  if (coalesce(wheb_usuario_pck.get_ie_commit, 'S') = 'S') then
    commit;
  end if;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE lab_liberar_tent_comunic (nr_prescricao_p bigint, nr_seq_prescr_p bigint, nr_sequencia_p bigint, ie_comunicacao_critico_p text, nm_usuario_p text) FROM PUBLIC;

