-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE registrar_entrega_exame_barra ( nr_atendimento_p bigint, cd_pessoa_fisica_p text, nm_pessoa_entrega_p text, ds_observacao_p text, dt_entrega_p timestamp, nm_usuario_p text) AS $body$
DECLARE


nr_seq_prescr_w		bigint;
nr_prescricao_w		bigint;
nr_seq_entrega_w	bigint;

c01 CURSOR FOR
	SELECT 	c.nr_prescricao,
		c.nr_sequencia
	from 	prescr_medica d,
		prescr_procedimento c
	where 	d.nr_atendimento = nr_atendimento_p
	and   	c.nr_prescricao = d.nr_prescricao
	and   	c.ie_status_atend > 30
	and   	c.ie_status_atend < 45;

BEGIN

open c01;
loop
fetch c01 into
	nr_prescricao_w,
	nr_seq_prescr_w;
EXIT WHEN NOT FOUND; /* apply on c01 */

	select 	nextval('lab_entrega_exame_seq')
	into STRICT	nr_seq_entrega_w
	;

	insert	into lab_entrega_exame(
		nr_sequencia,
		dt_atualizacao,
		nm_usuario,
		dt_atualizacao_nrec,
		nm_usuario_nrec,
		nr_prescricao,
		nr_seq_prescr,
		cd_pessoa_fisica,
		nm_pessoa_entrega,
		ds_observacao,
		dt_entrega,
		ie_situacao)
	values (
		nr_seq_entrega_w,
		clock_timestamp(),
		nm_usuario_p,
		clock_timestamp(),
		nm_usuario_p,
		nr_prescricao_w,
		nr_seq_prescr_w,
		cd_pessoa_fisica_p,
		nm_pessoa_entrega_p,
		ds_observacao_p,
		dt_entrega_p,
		'A');
	commit;

end loop;
close c01;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE registrar_entrega_exame_barra ( nr_atendimento_p bigint, cd_pessoa_fisica_p text, nm_pessoa_entrega_p text, ds_observacao_p text, dt_entrega_p timestamp, nm_usuario_p text) FROM PUBLIC;

