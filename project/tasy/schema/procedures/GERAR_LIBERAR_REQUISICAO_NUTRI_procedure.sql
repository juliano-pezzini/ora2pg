-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_liberar_requisicao_nutri ( ie_gera_requisicao_p text, ie_libera_ordem_p text, nr_seq_ordem_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint) AS $body$
DECLARE


parameter124_w varchar(1);


BEGIN

if (ie_gera_requisicao_p = 'S') then
	parameter124_w := obter_parametro_funcao_padrao(1003, 124, nm_usuario_p);
	if (parameter124_w = 'N') then
		CALL nut_gerar_requisicao(nr_seq_ordem_p, cd_estabelecimento_p, nm_usuario_p);
	else
		CALL NUT_GERAR_REQUISICAO_CARCAPIO(nr_seq_ordem_p, cd_estabelecimento_p, nm_usuario_p);
	end if;
end if;

if (ie_libera_ordem_p = 'S') then
	CALL nut_liberar_ordem(nr_seq_ordem_p, nm_usuario_p);
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_liberar_requisicao_nutri ( ie_gera_requisicao_p text, ie_libera_ordem_p text, nr_seq_ordem_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint) FROM PUBLIC;

