-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE man_excluir_ativ_prev_execut ( nr_seq_ordem_serv_p bigint) AS $body$
DECLARE


nm_usuario_exec_w	varchar(15);

C01 CURSOR FOR
	SELECT	nm_usuario_exec
	from	man_ordem_servico_exec
	where	nr_seq_ordem = nr_seq_ordem_serv_p
	and	(dt_fim_execucao IS NOT NULL AND dt_fim_execucao::text <> '');


BEGIN

if (coalesce(nr_seq_ordem_serv_p,0) > 0) then

	open C01;
	loop
	fetch C01 into
		nm_usuario_exec_w;
	EXIT WHEN NOT FOUND; /* apply on C01 */
		begin

		if (nm_usuario_exec_w IS NOT NULL AND nm_usuario_exec_w::text <> '') then

			delete	FROM man_ordem_ativ_prev
			where	nr_seq_ordem_serv = nr_seq_ordem_serv_p
			and	coalesce(dt_prevista::text, '') = ''
			and 	nm_usuario_prev = nm_usuario_exec_w;

		end if;

		end;
	end loop;
	close C01;
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE man_excluir_ativ_prev_execut ( nr_seq_ordem_serv_p bigint) FROM PUBLIC;

