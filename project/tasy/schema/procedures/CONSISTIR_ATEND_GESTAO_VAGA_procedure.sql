-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consistir_atend_gestao_vaga ( nr_seq_agenda_p bigint, nr_atendimento_p bigint, nm_usuario_p text) AS $body$
DECLARE


nr_atendimento_w	bigint;
qt_existe_w	bigint;


BEGIN

select	count(*)
into STRICT	qt_existe_w
from	agenda_paciente
where	nr_sequencia = nr_seq_agenda_p;

if (qt_existe_w > 0) then
	begin
	select	coalesce(max(nr_atendimento),0)
	into STRICT	nr_atendimento_w
	from	agenda_paciente
	where	nr_sequencia = nr_seq_agenda_p;

	if (nr_atendimento_w = 0) then
		update	agenda_paciente
		set	nr_atendimento	= nr_atendimento_p,
			nm_usuario	= nm_usuario_p,
			dt_atualizacao	= clock_timestamp()
		where	nr_sequencia	= nr_seq_agenda_p;

		commit;
	end if;
	end;
end if;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consistir_atend_gestao_vaga ( nr_seq_agenda_p bigint, nr_atendimento_p bigint, nm_usuario_p text) FROM PUBLIC;

