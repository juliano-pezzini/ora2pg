-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE compl_pf_eup_novo_reg_js ( cd_pessoa_fisica_p text, ie_tipo_complemento_p bigint, ds_msg_aborta_p INOUT text) AS $body$
DECLARE

ds_msg_aborta_w	varchar(255);
qt_compl_w	bigint;


BEGIN

if (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') then

	select	count(*)
	into STRICT	qt_compl_w
        from    compl_pessoa_fisica a
        where   a.cd_pessoa_fisica   = cd_pessoa_fisica_p
        and     a.ie_tipo_complemento = ie_tipo_complemento_p;

	if (qt_compl_w > 0) and (ie_tipo_complemento_p <> 9) then

		ds_msg_aborta_w :=  substr(obter_texto_tasy(288219, wheb_usuario_pck.get_nr_seq_idioma),1,255);
	end if;
end if;

ds_msg_aborta_p := ds_msg_aborta_w;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE compl_pf_eup_novo_reg_js ( cd_pessoa_fisica_p text, ie_tipo_complemento_p bigint, ds_msg_aborta_p INOUT text) FROM PUBLIC;

