-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gravar_log_impressao_cobr ( nr_seq_lote_p bigint, nm_usuario_p text, nr_seq_imp_p INOUT bigint) AS $body$
DECLARE


qt_reg		bigint;


BEGIN

/*if	(nvl(nr_seq_lote_p,0) > 0) then
	select	max(nr_sequencia)
	into	nr_seq_imp_p
	from	cobranca_paciente_imp
	where	nr_seq_lote = nr_seq_lote_p;
end if;
*/
--if	(nvl(nr_seq_imp_p,0) = 0) then
	select nextval('cobranca_paciente_imp_seq')
	into STRICT	nr_seq_imp_p
	;

	insert into cobranca_paciente_imp(
		nr_sequencia,
		dt_atualizacao,
		nm_usuario,
		nr_seq_lote)
	values (	nr_seq_imp_p,
		clock_timestamp(),
		nm_usuario_p,
		nr_seq_lote_p);

	update	cobranca_paciente_lote
	set	ie_status_lote = 'B'
	where	nr_sequencia = nr_seq_lote_p;
--end if;
commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gravar_log_impressao_cobr ( nr_seq_lote_p bigint, nm_usuario_p text, nr_seq_imp_p INOUT bigint) FROM PUBLIC;

