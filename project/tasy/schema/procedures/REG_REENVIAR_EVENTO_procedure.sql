-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE reg_reenviar_evento ( nr_seq_evento_p bigint, nr_seq_int_evento_gm_p bigint, nr_seq_log_ultima_p bigint, nm_usuario_p text) AS $body$
DECLARE


ds_parametros_w	varchar(255);


BEGIN
if	((coalesce(nr_seq_evento_p,0) > 0) and (coalesce(nr_seq_int_evento_gm_p,0) > 0) and (coalesce(nr_seq_log_ultima_p,0) > 0)) then
	begin

	CALL reg_resolver_contingencia(nr_seq_log_ultima_p);

	select	SUBSTR(obter_texto_entre_caracter(ds_observacao||'|','Parâmetros utilizados:','|'),22,255)
	into STRICT	ds_parametros_w
	from	log_integracao_evento
	where	nr_sequencia = nr_seq_int_evento_gm_p;

	CALL gravar_integracao_regulacao(nr_seq_evento_p, ds_parametros_w);
	end;
end if;
commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE reg_reenviar_evento ( nr_seq_evento_p bigint, nr_seq_int_evento_gm_p bigint, nr_seq_log_ultima_p bigint, nm_usuario_p text) FROM PUBLIC;

