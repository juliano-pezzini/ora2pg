-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE inserir_hist_envio_email_per ( cd_agenda_p bigint, nr_sequencia_p bigint, nm_usuario_p text, cd_pessoa_p text, nm_pessoa_p text, dt_agenda_p timestamp) AS $body$
DECLARE

	
cd_agenda_w	bigint;
cd_paciente_w	varchar(10);
nm_paciente_w	varchar(60);
dt_agenda_w	timestamp;
	

BEGIN					
	
cd_agenda_w	:= cd_agenda_p;
cd_paciente_w	:= cd_pessoa_p;
nm_paciente_w	:= nm_pessoa_p;
dt_agenda_w	:= dt_agenda_p;
	
if (coalesce(cd_pessoa_p::text, '') = '') then
	select	max(cd_agenda),
		max(cd_pessoa_fisica),
		max(nm_paciente),
		max(dt_agenda)
	into STRICT	cd_agenda_w,
		cd_paciente_w,
		nm_paciente_w,
		dt_agenda_w
	from	agenda_consulta
	where	nr_sequencia = nr_sequencia_p;
		
end if;
		
CALL gerar_agenda_consulta_hist(cd_agenda_w,
						nr_sequencia_p,
						'EP',
						nm_usuario_p,
						wheb_mensagem_pck.get_texto(795958),
						cd_paciente_w,
						nm_paciente_w,
						dt_agenda_w);

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE inserir_hist_envio_email_per ( cd_agenda_p bigint, nr_sequencia_p bigint, nm_usuario_p text, cd_pessoa_p text, nm_pessoa_p text, dt_agenda_p timestamp) FROM PUBLIC;

