-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE tiss_obter_valores (cd_convenio_p bigint, cd_estabelecimento_p bigint, ie_tipo_tiss_p text, dt_referencia_p timestamp default null, nr_seq_projeto_p INOUT bigint DEFAULT NULL, ds_versao_p INOUT text DEFAULT NULL, nr_seq_tiss_convenio_p INOUT bigint DEFAULT NULL) AS $body$
DECLARE


nr_seq_xml_projeto_w	bigint;
ie_tipo_convenio_w	bigint;
dt_inicio_vigencia_w	timestamp;
IE_VERSAO_TISS_w	tiss_convenio.IE_VERSAO_TISS%type;
nr_sequencia_w		tiss_convenio.nr_sequencia%type;
c01 CURSOR FOR
SELECT	nr_seq_xml_projeto,
	IE_VERSAO_TISS,
	nr_sequencia
from	tiss_convenio a
where	coalesce(a.cd_convenio, cd_convenio_p)		= cd_convenio_p
and	a.cd_estabelecimento				= cd_estabelecimento_p
and	a.ie_tipo_tiss					= ie_tipo_tiss_p
and	coalesce(a.ie_tipo_convenio,ie_tipo_convenio_w)	= ie_tipo_convenio_w
and	coalesce(a.nr_seq_outorgante::text, '') = ''
and 	coalesce(a.dt_inicio_vigencia,clock_timestamp()) = dt_inicio_vigencia_w					
order 	by coalesce(cd_convenio, 0),
	dt_inicio_vigencia,
	coalesce(a.ie_tipo_convenio,0);


BEGIN

select	max(ie_tipo_convenio)
into STRICT	ie_tipo_convenio_w
from	convenio
where	cd_convenio	= cd_convenio_p;

begin
select	coalesce(max(x.dt_inicio_vigencia),clock_timestamp())
into STRICT	dt_inicio_vigencia_w
from	tiss_convenio x
where	coalesce(x.cd_convenio,cd_convenio_p)		= cd_convenio_p
and	x.cd_estabelecimento				= cd_estabelecimento_p
and	x.ie_tipo_tiss					= ie_tipo_tiss_p
and	coalesce(x.ie_tipo_convenio,ie_tipo_convenio_w)	= ie_tipo_convenio_w
and	coalesce(x.nr_seq_outorgante::text, '') = ''
and	coalesce(x.dt_inicio_vigencia,coalesce(dt_referencia_p,clock_timestamp())) <= coalesce(dt_referencia_p,clock_timestamp());
exception
when others then
	dt_inicio_vigencia_w := clock_timestamp();
end;

open c01;
loop
fetch c01 into
	nr_seq_xml_projeto_w,
	IE_VERSAO_TISS_w,
	nr_sequencia_w;
EXIT WHEN NOT FOUND; /* apply on c01 */
end loop;
close c01;

nr_seq_projeto_p 	:= nr_seq_xml_projeto_w;
ds_versao_p	 	:= IE_VERSAO_TISS_w;
nr_seq_tiss_convenio_p	:= nr_sequencia_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE tiss_obter_valores (cd_convenio_p bigint, cd_estabelecimento_p bigint, ie_tipo_tiss_p text, dt_referencia_p timestamp default null, nr_seq_projeto_p INOUT bigint DEFAULT NULL, ds_versao_p INOUT text DEFAULT NULL, nr_seq_tiss_convenio_p INOUT bigint DEFAULT NULL) FROM PUBLIC;

