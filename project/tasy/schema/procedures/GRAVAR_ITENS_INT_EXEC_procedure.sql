-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gravar_itens_int_exec (cd_cnpj_p text, cd_material_p bigint, qt_material_p bigint, nr_atendimento_p bigint, nr_seq_proced_p bigint, nr_seq_agenda_p text, cd_estabelecimento_p bigint) AS $body$
DECLARE


ie_consignado_w		varchar(1);
cd_cgc_w			varchar(14);


BEGIN

select	obter_se_mat_consignado(cd_material_p)
into STRICT	ie_consignado_w
;

if (cd_material_p > 0) and (ie_consignado_w = '1') then

	select	max(cd_cgc)
	into STRICT	cd_cgc_w
	from	material_fornec
	where	cd_material = cd_material_p
	and	cd_estabelecimento = cd_estabelecimento_p;

	insert into w_int_opme_itens_util(
			cd_cnpj,
			cd_material,
			qt_material,
			nr_atendimento,
			nr_seq_agenda,
			nr_seq_proc_pac)
		values (CASE WHEN cd_cgc_w='' THEN cd_cnpj_p  ELSE cd_cgc_w END ,
			cd_material_p,
			qt_material_p,
			nr_atendimento_p,
			nr_seq_agenda_p,
			nr_seq_proced_p);
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gravar_itens_int_exec (cd_cnpj_p text, cd_material_p bigint, qt_material_p bigint, nr_atendimento_p bigint, nr_seq_proced_p bigint, nr_seq_agenda_p text, cd_estabelecimento_p bigint) FROM PUBLIC;

