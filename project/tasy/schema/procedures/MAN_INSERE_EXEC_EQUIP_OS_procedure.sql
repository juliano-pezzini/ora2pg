-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE man_insere_exec_equip_os ( nr_seq_ordem_p bigint, nr_seq_equipamento_p bigint, nr_seq_grupo_trab_p bigint, nr_seq_grupo_planej_p bigint, nm_usuario_p text) AS $body$
DECLARE


nm_usuario_exec_w		varchar(15);
qt_existe_w			bigint;

c01 CURSOR FOR
SELECT	nm_usuario_exec
from	man_equip_usuario_exec
where	nr_seq_equipamento = nr_seq_equipamento_p
and	coalesce(nr_seq_grupo_trabalho,coalesce(nr_seq_grupo_trab_p,0)) = coalesce(nr_seq_grupo_trab_p,0)
and	coalesce(nr_seq_grupo_planej,coalesce(nr_seq_grupo_planej_p,0)) = coalesce(nr_seq_grupo_planej_p,0);


BEGIN

if (nr_seq_ordem_p IS NOT NULL AND nr_seq_ordem_p::text <> '') then
	begin

	open c01;
	loop
	fetch c01 into
		nm_usuario_exec_w;
	EXIT WHEN NOT FOUND; /* apply on c01 */
		begin

		select	count(*)
		into STRICT	qt_existe_w
		from	man_ordem_servico_exec
		where	nr_seq_ordem = nr_seq_ordem_p
		and	nm_usuario_exec = nm_usuario_exec_w;

		if (qt_existe_w = 0) then
			begin

			insert into man_ordem_servico_exec(
				nr_sequencia,
				nr_seq_ordem,
				dt_atualizacao,
				nm_usuario,
				nm_usuario_exec) values (
					nextval('man_ordem_servico_exec_seq'),
					nr_seq_ordem_p,
					clock_timestamp(),
					nm_usuario_p,
					nm_usuario_exec_w);

			end;
		end if;

		end;
	end loop;
	close c01;

	end;
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE man_insere_exec_equip_os ( nr_seq_ordem_p bigint, nr_seq_equipamento_p bigint, nr_seq_grupo_trab_p bigint, nr_seq_grupo_planej_p bigint, nm_usuario_p text) FROM PUBLIC;

