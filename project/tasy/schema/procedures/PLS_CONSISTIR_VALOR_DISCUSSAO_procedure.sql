-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_consistir_valor_discussao (nr_seq_disc_proc_p bigint, nr_seq_disc_mat_p bigint, vl_aceito_p bigint, vl_negado_p bigint, vl_discussao_p bigint) AS $body$
DECLARE


vl_total_w		double precision;


BEGIN
if (nr_seq_disc_proc_p IS NOT NULL AND nr_seq_disc_proc_p::text <> '') then
	vl_total_w := (coalesce(vl_aceito_p,0) + coalesce(vl_negado_p,0));

	if (coalesce(vl_discussao_p,0) <> vl_total_w) then
		CALL wheb_mensagem_pck.exibir_mensagem_abort(148398);
	end if;

elsif (nr_seq_disc_mat_p IS NOT NULL AND nr_seq_disc_mat_p::text <> '') then
	vl_total_w := (coalesce(vl_aceito_p,0) + coalesce(vl_negado_p,0));

	if (coalesce(vl_discussao_p,0) <> vl_total_w) then
		CALL wheb_mensagem_pck.exibir_mensagem_abort(148398);
	end if;

end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_consistir_valor_discussao (nr_seq_disc_proc_p bigint, nr_seq_disc_mat_p bigint, vl_aceito_p bigint, vl_negado_p bigint, vl_discussao_p bigint) FROM PUBLIC;

