-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';



--
-- dblink wrapper to call function gravar_log_lab_pragma as an autonomous transaction
--
CREATE EXTENSION IF NOT EXISTS dblink;

CREATE OR REPLACE PROCEDURE gravar_log_lab_pragma ( cd_log_p bigint, ds_log_p text, nm_usuario_p text, nr_prescricao_p bigint default null, ds_integracao_p text default null) AS $body$
DECLARE
	-- Change this to reflect the dblink connection string
	v_conn_str  text := format('port=%s dbname=%s user=%s', current_setting('port'), current_database(), current_user);
	v_query     text;

BEGIN
	v_query := 'CALL gravar_log_lab_pragma_atx ( ' || quote_nullable(cd_log_p) || ',' || quote_nullable(ds_log_p) || ',' || quote_nullable(nm_usuario_p) || ',' || quote_nullable(nr_prescricao_p) || ',' || quote_nullable(ds_integracao_p) || ' )';
	PERFORM * FROM dblink(v_conn_str, v_query) AS p (ret boolean);

END;
$body$ LANGUAGE plpgsql SECURITY DEFINER;




CREATE OR REPLACE PROCEDURE gravar_log_lab_pragma_atx ( cd_log_p bigint, ds_log_p text, nm_usuario_p text, nr_prescricao_p bigint default null, ds_integracao_p text default null) AS $body$
DECLARE
/*
Realiza a chamada da procedure gravar_log_lab em uma sessão autônoma e realiza o commit.
Dessa forma, é possível gravar logs dentro de processos que executem um rollback ou em funções.
*/
BEGIN

    CALL gravar_log_lab(
        cd_log_p,
        ds_log_p,
        nm_usuario_p,
        nr_prescricao_p,
        ds_integracao_p
    );

    commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gravar_log_lab_pragma ( cd_log_p bigint, ds_log_p text, nm_usuario_p text, nr_prescricao_p bigint default null, ds_integracao_p text default null) FROM PUBLIC; -- REVOKE ALL ON PROCEDURE gravar_log_lab_pragma_atx ( cd_log_p bigint, ds_log_p text, nm_usuario_p text, nr_prescricao_p bigint default null, ds_integracao_p text default null) FROM PUBLIC;

