-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE ageserv_consistir_setor_unid (dt_agenda_p timestamp, cd_setor_atendimento_p bigint, nr_seq_unidade_p bigint, nr_seq_agenda_p bigint, nm_usuario_p text, ds_erro_p INOUT text) AS $body$
DECLARE


qt_agendado_w	bigint;


BEGIN

select	count(*)
into STRICT	qt_agendado_w
from	agenda_consulta
where	cd_setor_atendimento = cd_setor_atendimento_p
and	nr_seq_unidade	= nr_seq_unidade_p
and	dt_agenda = dt_agenda_p
and	ie_status_agenda not in ('C','B','F','I')
and	nr_sequencia <> nr_seq_agenda_p;

if (qt_agendado_w > 0) then
	ds_erro_p := WHEB_MENSAGEM_PCK.get_texto(278958,null);
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE ageserv_consistir_setor_unid (dt_agenda_p timestamp, cd_setor_atendimento_p bigint, nr_seq_unidade_p bigint, nr_seq_agenda_p bigint, nm_usuario_p text, ds_erro_p INOUT text) FROM PUBLIC;

