-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE fin_gerar_log_controle_banco ( cd_log_p bigint, ds_log_p text, nm_usuario_p text, ie_commit_p text default 'S', nr_seq_historico_p bigint default 0) AS $body$
BEGIN

if (cd_log_p IS NOT NULL AND cd_log_p::text <> '') and (ds_log_p IS NOT NULL AND ds_log_p::text <> '') then
	begin

	insert into FIN_LOG_CONTROLE_BANCO(
		cd_log,
		ds_log,
		dt_atualizacao,
		nm_usuario,
		nr_seq_historico)
	values (	cd_log_p,
		ds_log_p,
		clock_timestamp(),
		nm_usuario_p,
		nr_seq_historico_p);

	end;
end if;

if (ie_commit_p = 'S') then
	commit;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE fin_gerar_log_controle_banco ( cd_log_p bigint, ds_log_p text, nm_usuario_p text, ie_commit_p text default 'S', nr_seq_historico_p bigint default 0) FROM PUBLIC;

