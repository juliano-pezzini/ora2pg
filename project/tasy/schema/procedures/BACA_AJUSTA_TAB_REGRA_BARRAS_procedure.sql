-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE baca_ajusta_tab_regra_barras () AS $body$
DECLARE


qt_existe_w	bigint;


BEGIN

select	count(*)
into STRICT	qt_existe_w
from	MAT_REGRA_CODIGO_BARRAS;

if (qt_existe_w > 0) then
	RAISE EXCEPTION '%', 'Já existe registro nesta tabela! Verifique.' USING ERRCODE = '45011';
end if;

insert into MAT_REGRA_CODIGO_BARRAS(
	nr_sequencia,
	nm_usuario_nrec,
	nm_usuario,
	nm_regra,
	dt_atualizacao_nrec,
	dt_atualizacao,
	ds_regra) values (
		2,
		'TASY',
		'TASY',
		'Regra leitura cadastro material (Padrão Wheb)',
		clock_timestamp(),
		clock_timestamp(),
		'1) Código barras do cadastro do material.' || chr(13) || chr(10) ||
		'2) Código barras (Barras do material) do lote fornecedor.'|| chr(13) || chr(10) ||
		'3) Sequencia do lote fornecedor (Etiqueta impresssa no Tasy).');

insert into MAT_REGRA_CODIGO_BARRAS(
	nr_sequencia,
	nm_usuario_nrec,
	nm_usuario,
	nm_regra,
	dt_atualizacao_nrec,
	dt_atualizacao,
	ds_regra) values (
		3,
		'TASY',
		'TASY',
		'Regra leitura lote fornecedor',
		clock_timestamp(),
		clock_timestamp(),
		'1) Código barras (Descrição do barras) do lote fornecedor.' || chr(13) || chr(10) ||
		'2) Código barras (Barras do material) do lote fornecedor.' || chr(13) || chr(10) ||
		'3) Sequencia do lote fornecedor (Etiqueta impresssa no Tasy).' || chr(13) || chr(10) ||
		'4) Código barras do cadastro do material.');

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE baca_ajusta_tab_regra_barras () FROM PUBLIC;

