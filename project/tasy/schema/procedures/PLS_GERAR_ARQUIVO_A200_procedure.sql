-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_gerar_arquivo_a200 ( nr_seq_retorno_p bigint, cd_unimed_destino_p text, nm_usuario_p text, cd_estabelecimento_p bigint, ds_erro_p INOUT text, ds_nome_arq_p INOUT text, ds_caminho_rede_p INOUT text) AS $body$
DECLARE


cd_versao_ptu_w			ptu_regra_interface.cd_versao_ptu%type;
ds_erro_w			varchar(255) := null;
nm_arquivo_w			varchar(255);
ds_caminho_rede_w		varchar(255) := null;
nr_versao_transacao_w		varchar(4);


BEGIN

cd_versao_ptu_w		:= pls_obter_versao_ptu(cd_estabelecimento_p, cd_unimed_destino_p, clock_timestamp(), 'A200');
nr_versao_transacao_w	:= ptu_obter_versao_transacao('A200',cd_versao_ptu_w);

if (nr_versao_transacao_w = '08') then --Versão transação 08
	SELECT * FROM ptu_retorno_benef_a200_v8_pck.gerar_arquivo_a200(nr_seq_retorno_p, nm_usuario_p, cd_estabelecimento_p, ds_erro_w, nm_arquivo_w, ds_caminho_rede_w) INTO STRICT ds_erro_w, nm_arquivo_w, ds_caminho_rede_w;
elsif (cd_versao_ptu_w in ('9.0','9.1')) then --Versão transação 07
	SELECT * FROM ptu_retorno_benef_a200_90_pck.gerar_arquivo_a200(nr_seq_retorno_p, nm_usuario_p, cd_estabelecimento_p, ds_erro_w, nm_arquivo_w, ds_caminho_rede_w) INTO STRICT ds_erro_w, nm_arquivo_w, ds_caminho_rede_w;
else
	SELECT * FROM ptu_retorno_mov_benef_a200_pck.gerar_arquivo_a200(nr_seq_retorno_p, nm_usuario_p, cd_estabelecimento_p, ds_erro_w, nm_arquivo_w, ds_caminho_rede_w) INTO STRICT ds_erro_w, nm_arquivo_w, ds_caminho_rede_w;
end if;

ds_erro_p	:= ds_erro_w;
ds_nome_arq_p	:= nm_arquivo_w;
ds_caminho_rede_p := ds_caminho_rede_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_gerar_arquivo_a200 ( nr_seq_retorno_p bigint, cd_unimed_destino_p text, nm_usuario_p text, cd_estabelecimento_p bigint, ds_erro_p INOUT text, ds_nome_arq_p INOUT text, ds_caminho_rede_p INOUT text) FROM PUBLIC;

