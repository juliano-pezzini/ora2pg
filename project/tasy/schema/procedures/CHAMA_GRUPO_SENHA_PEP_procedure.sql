-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE chama_grupo_senha_pep (nr_atendimento_p bigint, ds_maquina_p text, nm_usuario_p text, nr_seq_triagem_p bigint default 0, ie_html_p text default null) AS $body$
DECLARE


nr_atendimento_w	bigint;

c01 CURSOR FOR
	SELECT	a.nr_atendimento
	from	agenda_consulta a
	where	a.nr_seq_agend_coletiva in (SELECT	b.nr_sequencia
										from	agendamento_coletivo b
										where	b.nr_seq_agenda_int = obter_dados_agend_coletivo(nr_atendimento_p,'AI'));


BEGIN

open c01;
loop
fetch c01 into	
	nr_atendimento_w;
EXIT WHEN NOT FOUND; /* apply on c01 */
	begin
	CALL chama_senha_paciente_pep(nr_atendimento_w, ds_maquina_p, nm_usuario_p, nr_seq_triagem_p, ie_html_p);
	end;
end loop;
close c01;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE chama_grupo_senha_pep (nr_atendimento_p bigint, ds_maquina_p text, nm_usuario_p text, nr_seq_triagem_p bigint default 0, ie_html_p text default null) FROM PUBLIC;

