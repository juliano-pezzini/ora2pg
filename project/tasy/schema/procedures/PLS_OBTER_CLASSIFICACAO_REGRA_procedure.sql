-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_obter_classificacao_regra ( qt_funcionarios_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint, nr_seq_classificacao_p INOUT text) AS $body$
DECLARE


nr_seq_classificacao_w	bigint;


BEGIN

begin
select	max(nr_seq_classificacao)
into STRICT	nr_seq_classificacao_w
from	pls_solic_regra_classif
where	cd_estabelecimento	= cd_estabelecimento_p
and	qt_funcionarios_p between coalesce(qt_vidas_inicial,qt_funcionarios_p) and coalesce(qt_vidas_final,qt_funcionarios_p);
exception
when others then
	nr_seq_classificacao_w	:= null;
end;

nr_seq_classificacao_p	:= nr_seq_classificacao_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_obter_classificacao_regra ( qt_funcionarios_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint, nr_seq_classificacao_p INOUT text) FROM PUBLIC;

