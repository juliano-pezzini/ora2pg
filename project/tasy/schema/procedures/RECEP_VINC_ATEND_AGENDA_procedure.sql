-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE recep_vinc_atend_agenda ( nr_atendimento_p bigint, nr_seq_agenda_p bigint, nm_usuario_p text) AS $body$
DECLARE

 
			 
qt_agenda_w bigint;			
			 

BEGIN 
 
if (coalesce(nr_atendimento_p,0) > 0) and (coalesce(nr_seq_agenda_p,0) > 0) then	 
	 
	 
	select	count(*) 
	into STRICT	qt_agenda_w 
	from	recep_ficha_pre_agenda 
	where	nr_seq_agenda = nr_seq_agenda_p;
	 
	if (qt_agenda_w > 0) then 
	 
		CALL vincular_atend_agenda(2, nr_seq_agenda_p, nr_atendimento_p, nm_usuario_p);
		 
		if ('S' = obter_se_existe_evento_agenda(wheb_usuario_pck.get_cd_estabelecimento,'GA','E')) then 
			 
			CALL exec_evento_agenda('GA','E', nr_seq_agenda_p, wheb_usuario_pck.get_cd_estabelecimento, nm_usuario_p);
			 
		end if;
	end if;
	 
end if;
 
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE recep_vinc_atend_agenda ( nr_atendimento_p bigint, nr_seq_agenda_p bigint, nm_usuario_p text) FROM PUBLIC;

