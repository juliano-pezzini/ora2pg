-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_resumo_oftalmo ( cd_pessoa_fisica_p text, nm_procedure_p text, ds_fundoscopia_p text, nm_usuario_p text) AS $body$
BEGIN
if (nm_procedure_p IS NOT NULL AND nm_procedure_p::text <> '') and (nm_usuario_p IS NOT NULL AND nm_usuario_p::text <> '') then
	begin
	if ('GERAR_RESUMO_OFTALMO' = upper(nm_procedure_p)) then
		begin
		CALL gerar_resumo_oftalmo_pkg.gerar_resumo_oftalmo(cd_pessoa_fisica_p, nm_usuario_p, ds_fundoscopia_p);
		end;
	elsif ('BV_GERAR_RESUMO_OFTALMO' = upper(nm_procedure_p)) then
		begin
		CALL gerar_resumo_oftalmo_pkg.bv_gerar_resumo_oftalmo(cd_pessoa_fisica_p, nm_usuario_p, ds_fundoscopia_p);
		end;
	elsif ('HDH_GERAR_RESUMO_OFTALMO' = upper(nm_procedure_p)) then
		begin
		CALL gerar_resumo_oftalmo_pkg.hdh_gerar_resumo_oftalmo(cd_pessoa_fisica_p, nm_usuario_p, ds_fundoscopia_p);
		end;
	end if;
	end;
end if;
commit;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_resumo_oftalmo ( cd_pessoa_fisica_p text, nm_procedure_p text, ds_fundoscopia_p text, nm_usuario_p text) FROM PUBLIC;

