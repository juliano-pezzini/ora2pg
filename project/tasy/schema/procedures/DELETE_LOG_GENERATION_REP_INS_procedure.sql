-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE delete_log_generation_rep_ins ( nr_atendimento_p bigint, nr_interno_conta_p bigint, cd_estabelecimento_p bigint, cd_convenio_p bigint) AS $body$
DECLARE


log_geracao_relat_conv_w        bigint;


BEGIN
        begin
        select  count(*)
        into STRICT    log_geracao_relat_conv_w
        from    log_geracao_relat_conv
        where   nr_atendimento = nr_atendimento_p
        and     nr_interno_conta = nr_interno_conta_p
        and     cd_estabelecimento = cd_estabelecimento_p
        and     cd_convenio = cd_convenio_p;
        exception
        when others then
                log_geracao_relat_conv_w := 0;
        end;

        if (log_geracao_relat_conv_w > 0) then
                begin
                delete from log_geracao_relat_conv
                where   nr_atendimento = nr_atendimento_p
                and     nr_interno_conta = nr_interno_conta_p
                and     cd_estabelecimento = cd_estabelecimento_p
                and     cd_convenio = cd_convenio_p;
                commit;
                end;
        end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE delete_log_generation_rep_ins ( nr_atendimento_p bigint, nr_interno_conta_p bigint, cd_estabelecimento_p bigint, cd_convenio_p bigint) FROM PUBLIC;

