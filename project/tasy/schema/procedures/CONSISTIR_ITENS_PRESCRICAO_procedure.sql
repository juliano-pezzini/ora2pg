-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consistir_itens_prescricao ( nr_prescricao_p bigint, nm_usuario_p text, ie_opcao_p text) AS $body$
DECLARE


/*
ie_opcao_p
A - Associados (PRESCR_MATERIAL)
*/
nr_sequencia_w			bigint;
ds_erro_w			varchar(2000);

c01 CURSOR FOR
SELECT	nr_sequencia
from	prescr_material a
where	a.ie_agrupador	in (5)
and	a.nr_prescricao 	= nr_prescricao_p;


BEGIN

if (ie_opcao_p = 'A') then
	open c01;
	loop
	fetch c01 into
		nr_sequencia_w;
	EXIT WHEN NOT FOUND; /* apply on c01 */
		ds_erro_w := consistir_prescr_material(nr_prescricao_p, nr_sequencia_w, nm_usuario_p, Obter_perfil_ativo, ds_erro_w);
	end loop;
	close c01;
end if;

--if (nvl(wheb_usuario_pck.get_ie_commit, 'S') = 'S') then commit; end if;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consistir_itens_prescricao ( nr_prescricao_p bigint, nm_usuario_p text, ie_opcao_p text) FROM PUBLIC;

