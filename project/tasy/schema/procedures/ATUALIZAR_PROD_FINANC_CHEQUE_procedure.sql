-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE atualizar_prod_financ_cheque (nr_seq_cheque_p bigint, nm_usuario_p text) AS $body$
DECLARE


nr_seq_produto_w	bigint;
nr_seq_grupo_prod_w	bigint;
cd_estabelecimento_w	bigint;


BEGIN

select	max(cd_estabelecimento)
into STRICT	cd_estabelecimento_w
from	cheque_cr
where	nr_seq_cheque		= nr_seq_cheque_p;

SELECT * FROM obter_produto_financeiro(cd_estabelecimento_w, null, null, nr_seq_produto_w, null, null, nr_seq_grupo_prod_w) INTO STRICT nr_seq_produto_w, nr_seq_grupo_prod_w;

if (nr_seq_produto_w IS NOT NULL AND nr_seq_produto_w::text <> '') then
	update	cheque_cr
	set	nr_seq_grupo_prod	= nr_seq_grupo_prod_w,
		nr_seq_produto		= nr_seq_produto_w
	where	nr_seq_cheque		= nr_seq_cheque_p;
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE atualizar_prod_financ_cheque (nr_seq_cheque_p bigint, nm_usuario_p text) FROM PUBLIC;

