-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_evolucao_anamnese_onco (nr_sequencia_p bigint, nm_usuario_p text) AS $body$
DECLARE


										
nr_atendimento_w		anamnese_oncologia.nr_atendimento%type;
ds_evolucao_w			anamnese_oncologia.ds_conduta%type;
cd_pessoa_fisica_w		anamnese_oncologia.cd_pessoa_fisica%type;
						

BEGIN

if (nr_sequencia_p IS NOT NULL AND nr_sequencia_p::text <> '') then

	select	max(nr_atendimento),
			max(ds_conduta),
			max(cd_pessoa_fisica)
	into STRICT	nr_atendimento_w,
			ds_evolucao_w,
			cd_pessoa_fisica_w
	from	anamnese_oncologia
	where	nr_sequencia = nr_sequencia_p;

	CALL gerar_evolucao_anamnese(nr_atendimento_w,		
							 nm_usuario_p,
							 ds_evolucao_w,
							 cd_pessoa_fisica_w,
							 null,
							 null);

	commit;

end if;


end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_evolucao_anamnese_onco (nr_sequencia_p bigint, nm_usuario_p text) FROM PUBLIC;

