-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_guias_nom ( dt_inicio_p timestamp, dt_final_p timestamp, ie_tipo_norma_p bigint, nm_usuario_p text) AS $body$
BEGIN

	-- lesiones
	if (ie_tipo_norma_p = 7) then
	    CALL gerar_guia_lesoes_nom(dt_inicio_p, dt_final_p, nm_usuario_p);
	end if;

	-- urgencias
	if (ie_tipo_norma_p = 8) then
	    CALL gerar_guia_urgencias_nom(dt_inicio_p, dt_final_p, nm_usuario_p);
	end if;

	-- consulta externa
	if (ie_tipo_norma_p = 9) then
	    CALL gerar_guia_consulta_nom(dt_inicio_p, dt_final_p, nm_usuario_p);
	end if;

	-- detecciones
	if (ie_tipo_norma_p = 10) then
	    CALL gerar_guia_deteccoes_nom(dt_inicio_p, dt_final_p, nm_usuario_p);
	end if;

	-- plan familiar
	if (ie_tipo_norma_p = 11) then
	    CALL gerar_guia_plan_familiar_nom(dt_inicio_p, dt_final_p, nm_usuario_p);
	end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_guias_nom ( dt_inicio_p timestamp, dt_final_p timestamp, ie_tipo_norma_p bigint, nm_usuario_p text) FROM PUBLIC;

