-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE atualizar_autorizacao_proced (nr_atendimento_p bigint, cd_procedimento_p bigint, ie_origem_proced_p bigint, nm_usuario_p text, nr_seq_autorizacao_p bigint) AS $body$
DECLARE


nr_prescricao_w		bigint;
nr_sequencia_w		integer;

c01 CURSOR FOR
	SELECT	nr_sequencia
	from	prescr_procedimento
	where	nr_prescricao		= nr_prescricao_w
	and	cd_procedimento 	= cd_procedimento_p
	and	ie_origem_proced 	= ie_origem_proced_p;


BEGIN

/* Edgar 27/06/2007, esta rotina já é feita na ATUALIZAR_AUTORIZACAO_CONVENIO

Select	nvl(max(a.nr_prescricao),0)
into	nr_prescricao_w
from	prescr_procedimento b,
	prescr_medica a
where	a.nr_atendimento = nr_atendimento_p
and	((a.dt_liberacao is not null) or (a.dt_liberacao_medico is not null))
and	a.nr_prescricao = b.nr_prescricao
and	b.cd_procedimento = cd_procedimento_p
and	b.ie_origem_proced = ie_origem_proced_p;

open C01;
loop
	fetch C01 into
		nr_sequencia_w;
	exit when C01%notfound;

	Update	prescr_procedimento
	set	ie_autorizacao	= 'A',
		dt_atualizacao	= sysdate,
		nm_usuario	= nm_usuario_p
	where	nr_prescricao	= nr_prescricao_w
	and	nr_sequencia	= nr_sequencia_w;

end loop;
close C01;

if	(nr_prescricao_w <> 0) then
	update	procedimento_autorizado
	set	nr_prescricao		= nr_prescricao_w,
		nr_seq_prescricao	= nr_sequencia_w
	where	nr_atendimento		= nr_atendimento_p
	and	cd_procedimento		= cd_procedimento_p
	and	nr_seq_autorizacao	= nr_seq_autorizacao_p;

end if;


commit;

*/
NULL;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE atualizar_autorizacao_proced (nr_atendimento_p bigint, cd_procedimento_p bigint, ie_origem_proced_p bigint, nm_usuario_p text, nr_seq_autorizacao_p bigint) FROM PUBLIC;

