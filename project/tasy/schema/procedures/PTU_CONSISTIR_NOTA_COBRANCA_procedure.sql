-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE ptu_consistir_nota_cobranca ( nr_seq_fatura_p bigint, nm_usuario_p text) AS $body$
DECLARE


nr_lote_w			integer;
nr_nota_w			ptu_nota_cobranca.nr_nota%type;
cd_usuario_plano_w		varchar(13);
cd_unimed_w			varchar(20);
dt_atendimento_w		timestamp;
cd_excecao_w			varchar(1);
ie_carater_atendimento_w	smallint;
ie_paciente_w			varchar(1);
ie_tipo_atendimento_w		varchar(2);
ie_tipo_saida_spdat_w		varchar(1);
nr_seq_cobranca_w		bigint;

C01 CURSOR FOR
	SELECT	nr_sequencia,
		nr_lote,
		nr_nota,
		cd_usuario_plano,
		cd_unimed,
		dt_atendimento,
		cd_excecao,
		ie_carater_atendimento,
		ie_paciente,
		ie_tipo_atendimento,
		ie_tipo_saida_spdat
	from	ptu_nota_cobranca
	where	nr_seq_fatura	= nr_seq_fatura_p;

BEGIN

/*
open C01;
loop
fetch C01 into
	nr_seq_cobranca_w,
	nr_lote_w,
	nr_nota_w,
	cd_usuario_plano_w,
	cd_unimed_w,
	dt_atendimento_w,
	cd_excecao_w,
	ie_carater_atendimento_w,
	ie_paciente_w,
	ie_tipo_atendimento_w,
	ie_tipo_saida_spdat_w;
exit when C01%notfound;
	begin
	if	(nr_lote_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '502', 'NR_LOTE', null, null, nr_seq_cobranca_w,nm_usuario_p);
	end if;

	if	(nr_nota_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '502', 'NR_NOTA', null, null, nr_seq_cobranca_w,nm_usuario_p);
	end if;

	if	(cd_usuario_plano_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '502', 'ID_BENEF', null, null, nr_seq_cobranca_w,nm_usuario_p);
	end if;

	if	(cd_unimed_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '502', 'CD_UNI', null, null, nr_seq_cobranca_w,nm_usuario_p);
	end if;

	if	(dt_atendimento_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '502', 'DT_ATEND', null, null, nr_seq_cobranca_w,nm_usuario_p);
	end if;

	if	(cd_excecao_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '502', 'CD_EXCECAO', null, null, nr_seq_cobranca_w,nm_usuario_p);
	end if;

	if	(ie_carater_atendimento_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '502', 'TP_CARATER_ATEND', null, null, nr_seq_cobranca_w,nm_usuario_p);
	end if;

	if	(ie_paciente_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '502', 'TP_PACIENTE', null, null, nr_seq_cobranca_w,nm_usuario_p);
	end if;
	if	(ie_tipo_atendimento_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '502', 'TP_ATENDIMENTO', null, null, nr_seq_cobranca_w,nm_usuario_p);
	end if;

	if	(ie_tipo_saida_spdat_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '502', 'TP_SAIDA_CONS_SADT', null, null, nr_seq_cobranca_w,nm_usuario_p);
	end if;
	end;
end loop;
close C01;

ptu_consistir_hospitalar(nr_seq_cobranca_w, nr_seq_fatura_p, nm_usuario_p);
ptu_consistir_servico(nr_seq_cobranca_w, nr_seq_fatura_p, nm_usuario_p);
ptu_consistir_complemento(nr_seq_cobranca_w, nr_seq_fatura_p, nm_usuario_p);
ptu_consistir_nota_fiscal(nr_seq_cobranca_w, nr_seq_fatura_p, nm_usuario_p);*/
cd_excecao_w	:= 'A';
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE ptu_consistir_nota_cobranca ( nr_seq_fatura_p bigint, nm_usuario_p text) FROM PUBLIC;

