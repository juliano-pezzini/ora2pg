-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE excluir_bloqueio_agendas ( dt_inicial_p timestamp, dt_final_p timestamp, hr_inicial_p timestamp, hr_final_p timestamp, ie_motivo_bloqueio_p text) AS $body$
BEGIN

delete	from agenda_bloqueio
where	TRUNC(dt_inicial)			=	TRUNC(dt_inicial_p)
and	TRUNC(dt_final)			=	TRUNC(dt_final_p)
and	((hr_inicio_bloqueio			=	hr_inicial_p) or (coalesce(hr_inicio_bloqueio::text, '') = '' and coalesce(hr_inicial_p::text, '') = ''))
and	((hr_final_bloqueio			=	hr_final_p) or (coalesce(hr_final_bloqueio::text, '') = '' and coalesce(hr_final_p::text, '') = ''))
and	ie_motivo_bloqueio			=	ie_motivo_bloqueio_p
and	obter_tipo_agenda(cd_agenda) 	= 1;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE excluir_bloqueio_agendas ( dt_inicial_p timestamp, dt_final_p timestamp, hr_inicial_p timestamp, hr_final_p timestamp, ie_motivo_bloqueio_p text) FROM PUBLIC;

