-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE definir_ret_atend_eup_js ( nr_atendimento_p bigint, nr_atend_retorno_p INOUT bigint, ds_mensagem_p INOUT text, ds_mensagem_sec_p INOUT text) AS $body$
DECLARE

 
nr_atend_retorno_w	bigint;
ds_convenio_w		varchar(255);
dt_entrada_w		varchar(25);
nm_medico_w		varchar(200);
ds_mensagem_w		varchar(255);
ds_mensagem_sec_w	varchar(255);
nr_seq_msg_ret_w	bigint;


BEGIN 
 
if (nr_atendimento_p IS NOT NULL AND nr_atendimento_p::text <> '')then 
	begin 
	 
	SELECT * FROM define_atendimento_retorno(nr_atendimento_p, nr_atend_retorno_w, nr_seq_msg_ret_w) INTO STRICT nr_atend_retorno_w, nr_seq_msg_ret_w;
	 
	if (nr_atend_retorno_w > 0)then 
		begin 
		 
		ds_convenio_w	:= substr(obter_dados_atendimento(nr_atend_retorno_w, 'NC'),1,255);
		dt_entrada_w	:= substr(obter_dados_atendimento(nr_atend_retorno_w, 'DE'),1,25);
		nm_medico_w	:= substr(obter_dados_atendimento(nr_atend_retorno_w, 'NMR'),1,200);
		 
		ds_mensagem_w 	:= wheb_mensagem_pck.get_texto(306704,	'NR_ATEND_RETORNO_W=' || nr_atend_retorno_w || ';' || 
																'DS_CONVENIO_W=' || ds_convenio_w || ';' || 
																'NM_MEDICO_W=' || nm_medico_w || ';' || 
																'DT_ENTRADA_W=' || dt_entrada_w);
				  
		ds_mensagem_sec_w	:= wheb_mensagem_pck.get_texto(306708,	'NR_ATEND_RETORNO_W=' || nr_atend_retorno_w || ';' || 
																	'DS_CONVENIO_W=' || ds_convenio_w || ';' || 
																	'NM_MEDICO_W=' || nm_medico_w || ';' || 
																	'DT_ENTRADA_W=' || dt_entrada_w);
		 
		end;
	end if;
	 
	end;
end if;
 
nr_atend_retorno_p	:= nr_atend_retorno_w;
ds_mensagem_p		:= ds_mensagem_w;
ds_mensagem_sec_p	:= ds_mensagem_sec_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE definir_ret_atend_eup_js ( nr_atendimento_p bigint, nr_atend_retorno_p INOUT bigint, ds_mensagem_p INOUT text, ds_mensagem_sec_p INOUT text) FROM PUBLIC;

