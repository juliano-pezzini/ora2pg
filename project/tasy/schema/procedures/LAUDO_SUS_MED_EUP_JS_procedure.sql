-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE laudo_sus_med_eup_js ( cd_estabelecimento_p bigint, nr_atendimento_p bigint, cd_procedimento_p bigint, cd_convenio_conta_p INOUT bigint, cd_convenio_parametro_p INOUT bigint, ie_cid_secundario_p INOUT text) AS $body$
DECLARE

 
cd_convenio_conta_w	integer;
cd_convenio_param_w	integer;
ie_cid_sec_w		varchar(1);
			

BEGIN 
 
if (coalesce(nr_atendimento_p,0) > 0) then 
	select	max(cd_convenio_parametro) 
	into STRICT	cd_convenio_conta_w 
	from	conta_paciente 
	where	nr_atendimento = nr_atendimento_p;
 
end if;
 
if (coalesce(cd_estabelecimento_p,1) > 0) then 
	select	max(cd_convenio_sus) 
	into STRICT	cd_convenio_param_w 
	from	parametro_faturamento 
	where	cd_estabelecimento = cd_estabelecimento_p;
end if;
 
ie_cid_sec_w :=	sus_obter_se_proc_cid_sec(cd_procedimento_p,7);
 
cd_convenio_conta_p	:= cd_convenio_conta_w;
cd_convenio_parametro_p	:= cd_convenio_param_w;
ie_cid_secundario_p	:= ie_cid_sec_w;
 
	 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE laudo_sus_med_eup_js ( cd_estabelecimento_p bigint, nr_atendimento_p bigint, cd_procedimento_p bigint, cd_convenio_conta_p INOUT bigint, cd_convenio_parametro_p INOUT bigint, ie_cid_secundario_p INOUT text) FROM PUBLIC;

