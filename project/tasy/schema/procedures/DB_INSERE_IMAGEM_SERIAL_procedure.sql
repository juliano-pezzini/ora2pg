-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE db_insere_imagem_serial ( nr_seq_prescr_p bigint, nr_seq_resultado_p bigint, ds_imagem_p text, ds_imagem_serial_p text, nm_usuario_p text, nr_seq_result_imagem_p INOUT bigint) AS $body$
DECLARE


nr_sequencia_w	exame_lab_result_imagem.nr_sequencia%type;


BEGIN

if (nr_seq_resultado_p IS NOT NULL AND nr_seq_resultado_p::text <> '') and (ds_imagem_serial_p IS NOT NULL AND ds_imagem_serial_p::text <> '') then

	select	nextval('exame_lab_result_imagem_seq')
	into STRICT	nr_sequencia_w
	;

	nr_seq_result_imagem_p := nr_sequencia_w;

	insert into exame_lab_result_imagem(
		nr_sequencia,
		nr_seq_resultado,
		nr_seq_prescr,
		ds_imagem_serial,
		ds_imagem,
		dt_atualizacao,
		dt_atualizacao_nrec,
		nm_usuario_nrec,
		nm_usuario)
	values (
		nr_sequencia_w,
		nr_seq_resultado_p,
		nr_seq_prescr_p,
		ds_imagem_serial_p,
		ds_imagem_p,
		clock_timestamp(),
		clock_timestamp(),
		nm_usuario_p,
		nm_usuario_p);

	commit;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE db_insere_imagem_serial ( nr_seq_prescr_p bigint, nr_seq_resultado_p bigint, ds_imagem_p text, ds_imagem_serial_p text, nm_usuario_p text, nr_seq_result_imagem_p INOUT bigint) FROM PUBLIC;

