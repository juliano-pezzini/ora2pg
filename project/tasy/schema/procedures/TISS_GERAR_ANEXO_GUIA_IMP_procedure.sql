-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE tiss_gerar_anexo_guia_imp (nm_usuario_p text, nr_seq_lote_imp_p bigint, nr_guia_prestador_p text, nr_guia_operadora_p text, cd_senha_p text, dt_autorizacao_p text, dt_validade_senha_p text, cd_usuario_convenio_p text, nm_paciente_p text, ie_status_solic_p text, nr_seq_guia_p INOUT bigint, dt_autorizacao_date_p timestamp default null, dt_validade_senha_date_p timestamp default null) AS $body$
DECLARE


dt_autorizacao_w	timestamp;
dt_validade_senha_w	timestamp;
nr_sequencia_w		bigint;


BEGIN

if (dt_autorizacao_p = 'null') then
	dt_autorizacao_w	:= null;
else
	dt_autorizacao_w	:= to_date(dt_autorizacao_p,'dd/mm/yyyy');
end if;

if (dt_validade_senha_p = 'null') then
	dt_validade_senha_w	:= null;
else
	dt_validade_senha_w	:= to_date(dt_validade_senha_p,'dd/mm/yyyy');
end if;

if (dt_autorizacao_date_p IS NOT NULL AND dt_autorizacao_date_p::text <> '') then
	dt_autorizacao_w	:= dt_autorizacao_date_p;
end if;
if (dt_validade_senha_date_p IS NOT NULL AND dt_validade_senha_date_p::text <> '') then
	dt_validade_senha_w	:= dt_validade_senha_date_p;
end if;

select	nextval('tiss_anexo_guia_imp_seq')
into STRICT	nr_sequencia_w
;

insert into TISS_ANEXO_GUIA_IMP(nr_sequencia,
	dt_atualizacao,
	nm_usuario,
	dt_atualizacao_nrec,
	nm_usuario_nrec,
	nr_seq_lote_imp,
	nr_guia_prestador,
	nr_guia_operadora,
	cd_senha,
	dt_autorizacao,
	dt_validade_senha,
	cd_usuario_convenio,
	nm_paciente,
	ie_status_solic)
values (nr_sequencia_w,
	clock_timestamp(),
	nm_usuario_p,
	clock_timestamp(),
	nm_usuario_p,
	nr_seq_lote_imp_p,
	nr_guia_prestador_p,
	nr_guia_operadora_p,
	cd_senha_p,
	dt_autorizacao_w,
	dt_validade_senha_w,
	cd_usuario_convenio_p,
	nm_paciente_p,
	ie_status_solic_p);

nr_seq_guia_p	:= nr_sequencia_w;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE tiss_gerar_anexo_guia_imp (nm_usuario_p text, nr_seq_lote_imp_p bigint, nr_guia_prestador_p text, nr_guia_operadora_p text, cd_senha_p text, dt_autorizacao_p text, dt_validade_senha_p text, cd_usuario_convenio_p text, nm_paciente_p text, ie_status_solic_p text, nr_seq_guia_p INOUT bigint, dt_autorizacao_date_p timestamp default null, dt_validade_senha_date_p timestamp default null) FROM PUBLIC;

