-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE verifica_visita_fora_horario ( nr_atendimento_p bigint, dt_entrada_p timestamp, ie_valor_param_p text, ie_visita_fora_hor_p INOUT text, ds_mensagem_abort_p INOUT text, ds_mensagem_p INOUT text, nm_usuario_p text) AS $body$
DECLARE

 
cd_setor_atend_w		integer;
ie_visita_fora_hor_w	varchar(10);
ds_mensagem_abort_w	varchar(255);
ds_mensagem_w		varchar(255);
ie_Consiste_Dt_Entrada_Real_w varchar(1) := 'N';


BEGIN 
ie_Consiste_Dt_Entrada_Real_w := Obter_param_Usuario(8014, 86, obter_perfil_ativo, nm_usuario_p, coalesce(wheb_usuario_pck.get_cd_estabelecimento,1), ie_Consiste_Dt_Entrada_Real_w);
 
if (ie_valor_param_p <> 'S') and (ie_Consiste_Dt_Entrada_Real_w = 'N') then 
 
	select	obter_setor_atendimento(nr_atendimento_p) 
	into STRICT	cd_setor_atend_w 
	;
	 
	if (cd_setor_atend_w > 0) then 
		 
		verifica_hor_visita_setor(dt_entrada_p, cd_setor_atend_w, nr_atendimento_p, 'P', ie_visita_fora_hor_w);
		 
		if (ie_valor_param_p = 'N') then 
			if (ie_visita_fora_hor_w = 'S') then 
				ds_mensagem_abort_w := obter_texto_tasy(72386, wheb_usuario_pck.get_nr_seq_idioma);
			elsif (ie_visita_fora_hor_w = 'LH') then 
				ds_mensagem_abort_w := obter_texto_tasy(104125, wheb_usuario_pck.get_nr_seq_idioma);
			elsif (ie_visita_fora_hor_w = 'LS') then 
				ds_mensagem_abort_w := obter_texto_tasy(104123, wheb_usuario_pck.get_nr_seq_idioma);
			elsif (ie_visita_fora_hor_w = 'LD') then 
				ds_mensagem_abort_w := obter_texto_tasy(104122, wheb_usuario_pck.get_nr_seq_idioma);
			elsif (ie_visita_fora_hor_w = 'LM') then 
				ds_mensagem_abort_w := obter_texto_tasy(104119, wheb_usuario_pck.get_nr_seq_idioma);
			end if;
		elsif (ie_valor_param_p = 'Q') then 
			if (ie_visita_fora_hor_w = 'S') then 
				ds_mensagem_w := obter_texto_tasy(60156, wheb_usuario_pck.get_nr_seq_idioma);
			elsif (ie_visita_fora_hor_w = 'LH') then 
				ds_mensagem_w := obter_texto_tasy(60157, wheb_usuario_pck.get_nr_seq_idioma);
			elsif (ie_visita_fora_hor_w = 'LS') then 
				ds_mensagem_w := obter_texto_tasy(60158, wheb_usuario_pck.get_nr_seq_idioma);
			elsif (ie_visita_fora_hor_w = 'LD') then 
				ds_mensagem_w := obter_texto_tasy(60159, wheb_usuario_pck.get_nr_seq_idioma);
			elsif (ie_visita_fora_hor_w = 'LM') then 
				ds_mensagem_w := obter_texto_tasy(104129, wheb_usuario_pck.get_nr_seq_idioma);
			end if;
		end if;
	end if;
end if;
 
ie_visita_fora_hor_p := ie_visita_fora_hor_w;
ds_mensagem_p := ds_mensagem_w;
ds_mensagem_abort_p := ds_mensagem_abort_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE verifica_visita_fora_horario ( nr_atendimento_p bigint, dt_entrada_p timestamp, ie_valor_param_p text, ie_visita_fora_hor_p INOUT text, ds_mensagem_abort_p INOUT text, ds_mensagem_p INOUT text, nm_usuario_p text) FROM PUBLIC;

