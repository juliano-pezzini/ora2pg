-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_inserir_dma_guia_imp ( nr_seq_dma_protocolo_p text, nr_guia_prestador_p text, nr_guia_operadora_p text, cd_senha_p text, nm_beneficiario_p text, nr_carteirinha_benef_p text, dt_inicio_faturamento_p timestamp, dt_fim_faturamento_p timestamp, hr_inicio_faturamento_p timestamp, hr_fim_faturamento_p timestamp, ie_situacao_p text, vl_informado_p text, vl_processado_p text, vl_liberado_p text, vl_glosa_p text, nm_usuario_p usuario.nm_usuario%type, nr_seq_dma_guia_imp_p INOUT pls_dma_guia_imp.nr_sequencia%type) AS $body$
DECLARE


hr_inicio_fat_w		varchar(20);
hr_fim_fat_w		varchar(20);

BEGIN


-- A principio no XML deve vir somente a hora, então é tratado o valor para deixar compativel com a coluna DATE
if	((coalesce(dt_inicio_faturamento_p::text, '') = '') or (coalesce(hr_inicio_faturamento_p::text, '') = '')) then
	hr_inicio_fat_w := null;
else
	if (coalesce(hr_inicio_faturamento_p::text, '') = '') then
		hr_inicio_fat_w	:= dt_inicio_faturamento_p;

	else
		hr_inicio_fat_w	:= dt_inicio_faturamento_p||' '|| to_char(hr_inicio_faturamento_p,'hh24:mi:ss');
	end if;
end if;

if	((coalesce(dt_fim_faturamento_p::text, '') = '') or (coalesce(hr_fim_faturamento_p::text, '') = '')) then
	hr_fim_fat_w := null;
else
	if (coalesce(hr_fim_faturamento_p::text, '') = '') then
		hr_fim_fat_w	:= dt_fim_faturamento_p;
	else
		hr_fim_fat_w := dt_fim_faturamento_p||' '|| to_char(hr_fim_faturamento_p,'hh24:mi:ss');
	end if;
end if;

insert	into pls_dma_guia_imp(nr_sequencia, dt_atualizacao, nm_usuario,
	 dt_atualizacao_nrec, nm_usuario_nrec, nr_seq_dma_protocolo,
	 nr_guia_prestador, nr_guia_operadora, cd_senha,
	 nm_beneficiario, nr_carteirinha_benef, dt_inicio_faturamento,
	 dt_fim_faturamento, hr_inicio_faturamento, hr_fim_faturamento,
	 ie_situacao, vl_informado, vl_processado,
	 vl_liberado, vl_glosa )
values (nextval('pls_dma_guia_imp_seq'), clock_timestamp(), nm_usuario_p,
	 clock_timestamp(), nm_usuario_p, nr_seq_dma_protocolo_p,
	 nr_guia_prestador_p, nr_guia_operadora_p, cd_senha_p,
	 nm_beneficiario_p, nr_carteirinha_benef_p, dt_inicio_faturamento_p,
	 dt_fim_faturamento_p, to_date(hr_inicio_fat_w, 'dd/mm/yyyy hh24:mi:ss'),  to_date(hr_fim_fat_w, 'dd/mm/yyyy hh24:mi:ss'),
	 ie_situacao_p, vl_informado_p, vl_processado_p,
	 vl_liberado_p, vl_glosa_p
	) returning nr_sequencia into nr_seq_dma_guia_imp_p;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_inserir_dma_guia_imp ( nr_seq_dma_protocolo_p text, nr_guia_prestador_p text, nr_guia_operadora_p text, cd_senha_p text, nm_beneficiario_p text, nr_carteirinha_benef_p text, dt_inicio_faturamento_p timestamp, dt_fim_faturamento_p timestamp, hr_inicio_faturamento_p timestamp, hr_fim_faturamento_p timestamp, ie_situacao_p text, vl_informado_p text, vl_processado_p text, vl_liberado_p text, vl_glosa_p text, nm_usuario_p usuario.nm_usuario%type, nr_seq_dma_guia_imp_p INOUT pls_dma_guia_imp.nr_sequencia%type) FROM PUBLIC;

