-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE inserir_item_pend_adep ( nr_seq_item_pend_p bigint, ie_tipo_item_p text, nr_seq_evento_p bigint, nm_usuario_p text) AS $body$
DECLARE


nr_seq_mat_alter_w 	bigint := null;
nr_seq_prescr_alter_w 	bigint := null;
nr_seq_prescr_gas_w 	bigint := null;
nr_seq_prescr_sol_w 	bigint := null;


BEGIN

if (coalesce(nr_seq_item_pend_p, 0) > 0) then
	if (ie_tipo_item_p = 'O') then
		nr_seq_prescr_gas_w := nr_seq_evento_p;
	elsif (ie_tipo_item_p = 'DI') then
		nr_seq_prescr_alter_w := nr_seq_evento_p;
	elsif (ie_tipo_item_p in ('SOL', 'SNE', 'HM', 'NAN', 'NPN', 'NPA', 'NPP', 'S', 'B')) then
		nr_seq_prescr_sol_w := nr_seq_evento_p;
	else
		nr_seq_mat_alter_w := nr_seq_evento_p;
	end if;

	insert into pep_item_pendente_comp(
		nr_sequencia,
		dt_atualizacao,
		nm_usuario,
		dt_atualizacao_nrec,
		nm_usuario_nrec,
		nr_seq_mat_alter,
		nr_seq_presc_alter,
		nr_seq_prescr_gas,
		nr_seq_prescr_sol,
		nr_seq_item_pend)
	values (nextval('pep_item_pendente_comp_seq'),
		clock_timestamp(),
		nm_usuario_p,
		clock_timestamp(),
		nm_usuario_p,
		nr_seq_mat_alter_w,
		nr_seq_prescr_alter_w,
		nr_seq_prescr_gas_w,
		nr_seq_prescr_sol_w,
		nr_seq_item_pend_p);

	commit;

end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE inserir_item_pend_adep ( nr_seq_item_pend_p bigint, ie_tipo_item_p text, nr_seq_evento_p bigint, nm_usuario_p text) FROM PUBLIC;

