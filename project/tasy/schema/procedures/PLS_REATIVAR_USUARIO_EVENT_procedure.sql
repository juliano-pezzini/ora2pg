-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_reativar_usuario_event ( nr_seq_segurado_p bigint, dt_reativacao_p timestamp, ds_observacao_p text, nm_usuario_p text) AS $body$
DECLARE



dt_suspensao_seg_w 	timestamp;

BEGIN

dt_suspensao_seg_w := pls_obter_dt_suspensao_seg(nr_seq_segurado_p);

update	pls_segurado
set	dt_rescisao			 = NULL,
	dt_limite_utilizacao		 = NULL,
	nm_usuario			= nm_usuario_p,
	dt_atualizacao			= clock_timestamp(),
	dt_reativacao			= dt_reativacao_p,
	nr_seq_motivo_cancelamento	 = NULL,
	dt_cancelamento			 = NULL,
	ie_tipo_rescisao		= '',
	ie_situacao_atend		= CASE WHEN dt_suspensao_seg_w = NULL THEN CASE WHEN dt_liberacao = NULL THEN 'I'  ELSE 'A' END   ELSE 'S' END
where	nr_sequencia			= nr_seq_segurado_p;

/* Gerar histórico */

CALL pls_gerar_segurado_historico(
	nr_seq_segurado_p, '2', clock_timestamp(),
	'Beneficiário reativado em ' || to_char(dt_reativacao_p, 'dd/mm/yyyy'), ds_observacao_p, null,
	dt_reativacao_p, null, null,
	dt_reativacao_p, null, null,
	null, null, null,
	null, nm_usuario_p, 'S');

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_reativar_usuario_event ( nr_seq_segurado_p bigint, dt_reativacao_p timestamp, ds_observacao_p text, nm_usuario_p text) FROM PUBLIC;

