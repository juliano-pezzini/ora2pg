-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE exec_consulta_linkeddata ( NM_TABELA_P text, NR_ATENDIMENTO_P bigint, CD_PESSOA_FISICA_P bigint, IE_SQL_CONSULTA_P text) AS $body$
DECLARE

  ds_query_w   varchar(8000);
  ds_module_w  varchar(48);
  nr_seq_log_w bigint;
  c001         integer;
  retorno_w    integer;
  ds_erro_w    varchar(512);

BEGIN
  ds_query_w := 'SELECT * FROM (' || 
    ' SELECT * FROM ' || NM_TABELA_P || ' WHERE NR_ATENDIMENTO = ' || 
      NR_ATENDIMENTO_P || ' AND CD_PESSOA_FISICA = '  || 
      CD_PESSOA_FISICA_P || ' '   ||
      IE_SQL_CONSULTA_P ||
    ' ) WHERE rownum = 1';

  EXECUTE(ds_query_w);
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE exec_consulta_linkeddata ( NM_TABELA_P text, NR_ATENDIMENTO_P bigint, CD_PESSOA_FISICA_P bigint, IE_SQL_CONSULTA_P text) FROM PUBLIC;

