-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE inserir_man_tasy_version ( cd_version_p text, nr_service_pack_p bigint ) AS $body$
DECLARE

	is_registro_existente_w varchar(1);

BEGIN

	select 	coalesce(max('S'), 'N')
	into STRICT 	is_registro_existente_w
	from 	man_tasy_version
	where 	cd_version 	= cd_version_p
	and 	nr_service_pack 	= nr_service_pack_p
	;
	
	if (is_registro_existente_w = 'N') then
		insert into man_tasy_version(
			cd_version,
			nr_service_pack
		) values (
			cd_version_p,
			nr_service_pack_p
		);
		commit;
	end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE inserir_man_tasy_version ( cd_version_p text, nr_service_pack_p bigint ) FROM PUBLIC;

