-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE atualizar_nivel_urgencia_js ( nr_atendimento_p bigint, ie_nivel_urgencia_p bigint, ie_nivel_prioridade_p bigint) AS $body$
BEGIN
 
if (coalesce(nr_atendimento_p,0) > 0) then 
 
	if (coalesce(ie_nivel_urgencia_p,0) > 0) then 
		begin 
		 
		update 	atendimento_paciente 
		set 	nr_seq_triagem = ie_nivel_urgencia_p 
		where 	nr_atendimento = nr_atendimento_p;
		 
		end;
	end if;
 
	if (coalesce(ie_nivel_prioridade_p,0) > 0) then 
		begin 
		 
		update 	atendimento_paciente 
		set 	nr_seq_triagem_prioridade 	= ie_nivel_prioridade_p 
		where 	nr_atendimento 			= nr_atendimento_p;
		 
		end;
	end if;
	 
end if;
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE atualizar_nivel_urgencia_js ( nr_atendimento_p bigint, ie_nivel_urgencia_p bigint, ie_nivel_prioridade_p bigint) FROM PUBLIC;

