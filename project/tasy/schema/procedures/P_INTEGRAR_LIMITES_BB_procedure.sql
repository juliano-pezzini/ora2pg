-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE p_integrar_limites_bb ( nr_atendimento_p atend_paciente_unidade.nr_atendimento%type, cd_setor_atendimento_p atend_paciente_unidade.cd_setor_atendimento%TYPE, cd_pessoa_fisica_p pessoa_fisica.cd_pessoa_fisica%TYPE ) AS $body$
DECLARE


  json_limits               philips_json;
  clob_limits               text;
  envio_integracao_bb       text;
  retorno_integracao_bb     text;


BEGIN
  IF (OBTER_SE_INTEGRACAO_ATIVA(973, 245) = 'S') THEN
    clob_limits := p_buscar_limites_bb(nr_atendimento_p, cd_setor_atendimento_p, cd_pessoa_fisica_p, null, clob_limits);
    json_limits := philips_json(clob_limits);
    dbms_lob.createtemporary(envio_integracao_bb, TRUE);
    json_limits.(envio_integracao_bb);

    SELECT BIFROST.SEND_INTEGRATION_CONTENT('Blackboard_Patient_Limits',envio_integracao_bb,wheb_usuario_pck.get_nm_usuario) into STRICT retorno_integracao_bb;
  END IF;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE p_integrar_limites_bb ( nr_atendimento_p atend_paciente_unidade.nr_atendimento%type, cd_setor_atendimento_p atend_paciente_unidade.cd_setor_atendimento%TYPE, cd_pessoa_fisica_p pessoa_fisica.cd_pessoa_fisica%TYPE ) FROM PUBLIC;

