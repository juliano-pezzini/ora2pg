-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_consistir_pag_acesso_web ( nr_seq_estip_pag_web_p bigint, nr_seq_estipulante_web_p bigint, nr_seq_pagador_p bigint, ds_erro_p INOUT text) AS $body$
DECLARE


qt_registros_w		bigint;
ds_erro_w		varchar(255);


BEGIN

ds_erro_w := '';

select 	count(1)
into STRICT	qt_registros_w
from	pls_estipulante_web			a,
		pls_estipulante_pag_web		b
where 	b.nr_seq_estipulante_web = a.nr_sequencia
and	b.nr_seq_estipulante_web = nr_seq_estipulante_web_p
and	b.nr_seq_pagador	 = nr_seq_pagador_p
and	b.nr_sequencia		<> nr_seq_estip_pag_web_p;

if (qt_registros_w > 0) then
	ds_erro_w := wheb_mensagem_pck.get_texto(280208);
end if;

ds_erro_p := ds_erro_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_consistir_pag_acesso_web ( nr_seq_estip_pag_web_p bigint, nr_seq_estipulante_web_p bigint, nr_seq_pagador_p bigint, ds_erro_p INOUT text) FROM PUBLIC;

