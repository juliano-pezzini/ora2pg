-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE rp_consistir_duplicidade_pac (cd_pessoa_fisica_p text, cd_estabelecimento_p bigint, ds_erro_p INOUT text) AS $body$
DECLARE


qt_existe_w	bigint;


BEGIN

ds_erro_p	:= '';

select	count(*)
into STRICT	qt_existe_w
from	rp_paciente_reabilitacao
where	cd_pessoa_fisica = cd_pessoa_fisica_p
and	cd_estabelecimento = cd_estabelecimento_p;

if (qt_existe_w > 0) then
	ds_erro_p	:= WHEB_MENSAGEM_PCK.get_texto(279126,null);
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE rp_consistir_duplicidade_pac (cd_pessoa_fisica_p text, cd_estabelecimento_p bigint, ds_erro_p INOUT text) FROM PUBLIC;

