-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE diops_cadastral_gerar_endereco ( nr_seq_operadora_p bigint, nr_seq_periodo_p bigint, nm_usuario_p text) AS $body$
DECLARE

 
/* +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ 
Finalidade: Gerar as informações cadastrais de endereço do DIOPS 
------------------------------------------------------------------------------------------------------------------- 
Locais de chamada direta: 
[ X ] Objetos do dicionário [ ] Tasy (Delphi/Java) [ ] Portal [ ] Relatórios [ ] Outros: 
 ------------------------------------------------------------------------------------------------------------------ 
Pontos de atenção: 
------------------------------------------------------------------------------------------------------------------- 
Referências: 
	PLS_GERAR_DIOPS_CADASTRAL 
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ */
 
 
cd_cgc_w			varchar(14);
cd_representante_w		varchar(10);
cd_repres_rn117_w		varchar(10);
cd_estabelecimento_w		smallint;


BEGIN 
/* Obter dados da operadora */
 
begin 
select	cd_cgc_outorgante, 
	cd_estabelecimento, 
	cd_representante, 
	cd_representante_rn117 
into STRICT	cd_cgc_w, 
	cd_estabelecimento_w, 
	cd_representante_w, 
	cd_repres_rn117_w 
from	pls_outorgante 
where	nr_sequencia	= nr_seq_operadora_p;
exception 
when others then 
	CALL wheb_mensagem_pck.exibir_mensagem_abort(233796, 'NR_SEQ_OPERADORA=' + nr_seq_operadora_p);
end;
 
/* Gravar o endereço da Matriz */
 
CALL diops_cadastral_gravar_end(nr_seq_operadora_p, nr_seq_periodo_p, '', cd_cgc_w, 'M', cd_estabelecimento_w, nm_usuario_p);
 
/* Gravar o endereço da Matriz */
 
CALL diops_cadastral_gravar_end(nr_seq_operadora_p, nr_seq_periodo_p, '', cd_cgc_w, 'C', cd_estabelecimento_w, nm_usuario_p);
 
/* Gravar o endereço do Representante */
 
CALL diops_cadastral_gravar_end(nr_seq_operadora_p, nr_seq_periodo_p, cd_representante_w, '', 'R', cd_estabelecimento_w, nm_usuario_p);
 
/* Gravar o endereço do Representante RN117 */
 
CALL diops_cadastral_gravar_end(nr_seq_operadora_p, nr_seq_periodo_p, cd_repres_rn117_w, '', 'RN', cd_estabelecimento_w, nm_usuario_p);
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE diops_cadastral_gerar_endereco ( nr_seq_operadora_p bigint, nr_seq_periodo_p bigint, nm_usuario_p text) FROM PUBLIC;

