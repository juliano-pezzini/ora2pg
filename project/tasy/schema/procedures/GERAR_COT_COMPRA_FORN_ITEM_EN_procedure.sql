-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_cot_compra_forn_item_en ( nr_cot_compra_p bigint, cd_material_p bigint, qt_item_p bigint, dt_entrega_p timestamp, cd_cgc_fornecedor_p text, nm_usuario_p text) AS $body$
DECLARE


nr_seq_cot_forn_item_w	bigint;
nr_seq_cot_forn_w		bigint;
nr_item_cot_compra_w	integer;
nr_sequencia_w		bigint;


BEGIN

select	min(coalesce(b.nr_item_cot_compra,0))
into STRICT	nr_item_cot_compra_w
from	cot_compra_forn a,
	cot_compra_forn_item b
where	a.nr_sequencia		= b.nr_seq_cot_forn
and	b.cd_material		= cd_material_p
and	b.nr_cot_compra		= nr_cot_compra_p
and	a.cd_cgc_fornecedor	= cd_cgc_fornecedor_p;


if (nr_item_cot_compra_w > 0) then
	begin
	select	max(nr_sequencia),
		max(nr_seq_cot_forn)
	into STRICT	nr_seq_cot_forn_item_w,
		nr_seq_cot_forn_w
	from	cot_compra_forn_item
	where	nr_cot_compra		= nr_cot_compra_p
	and	nr_item_cot_compra	= nr_item_cot_compra_w
	and	cd_cgc_fornecedor		= cd_cgc_fornecedor_p;

	select	nextval('cot_compra_forn_item_en_seq')
	into STRICT	nr_sequencia_w
	;

	insert into cot_compra_forn_item_en(
				nr_sequencia,
				dt_atualizacao,
				nm_usuario,
				nr_seq_cot_item_forn,
				dt_entrega,
				qt_entrega,
				nr_cot_compra,
				nr_item_cot_compra)
			values (nr_sequencia_w,
				clock_timestamp(),
				nm_usuario_p,
				nr_seq_cot_forn_item_w,
				dt_entrega_p,
				qt_item_p,
				nr_cot_compra_p,
				nr_item_cot_compra_w);

	end;
end if;

commit;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_cot_compra_forn_item_en ( nr_cot_compra_p bigint, cd_material_p bigint, qt_item_p bigint, dt_entrega_p timestamp, cd_cgc_fornecedor_p text, nm_usuario_p text) FROM PUBLIC;

