-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE inativar_sala_consulta ( nr_seq_sala_p bigint, nm_usuario_p text) AS $body$
DECLARE


ie_situacao_w	varchar(1);

BEGIN

if (nr_seq_sala_p IS NOT NULL AND nr_seq_sala_p::text <> '')then
	select	ie_situacao
	into STRICT	ie_situacao_w
	from	agenda_sala_consulta
	where	nr_sequencia = nr_seq_sala_p;

	if (ie_situacao_w = 'A')then
		update	agenda_sala_consulta
		set		ie_situacao 	= 'I'
		where	nr_sequencia 	= nr_seq_sala_p;
	end if;

end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE inativar_sala_consulta ( nr_seq_sala_p bigint, nm_usuario_p text) FROM PUBLIC;

