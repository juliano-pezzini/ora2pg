-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE libera_adm_ccg ( nr_seq_atend_glic_p bigint, nm_usuario_p text) AS $body$
DECLARE

 
nr_seq_glicemia_w	bigint;
ds_observacao_w		varchar(255);
				

BEGIN 
if (nr_seq_atend_glic_p IS NOT NULL AND nr_seq_atend_glic_p::text <> '') then 
	update	atendimento_glicemia 
	set	dt_lib_enfermagem	= clock_timestamp() 
	where 	nr_sequencia		= nr_seq_atend_glic_p;
	 
	commit;
	 
	select	max(nr_sequencia), 
		max(ds_observacao) 
	into STRICT 	nr_seq_glicemia_w, 
		ds_observacao_w 
	from 	atendimento_glicemia 
	where	nr_sequencia = nr_seq_atend_glic_p;
	 
	CALL gerar_alteracao_CCG( nr_seq_glicemia_w, 12, ds_observacao_w, nm_usuario_p);
 
end if;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE libera_adm_ccg ( nr_seq_atend_glic_p bigint, nm_usuario_p text) FROM PUBLIC;

