-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE transferir_atendimento_estab ( nr_atendimento_p bigint, cd_estabelecimento_p bigint, ie_executar_commit_p text default null) AS $body$
DECLARE


ie_atualizar_estab_atend_w	varchar(5);
cd_perfil_w			perfil.cd_perfil%type;
nm_usuario_w			usuario.nm_usuario%type;
cd_estabelecimento_w		estabelecimento.cd_estabelecimento%type;


BEGIN
cd_perfil_w	:= Obter_perfil_ativo;
nm_usuario_w	:= wheb_usuario_pck.get_nm_usuario;

ie_atualizar_estab_atend_w := obter_param_usuario(3111, 311, cd_perfil_w, nm_usuario_w, cd_estabelecimento_w, ie_atualizar_estab_atend_w);


if (coalesce(ie_atualizar_estab_atend_w,'N') = 'S') then
	update	atendimento_paciente
	set	cd_estabelecimento = cd_estabelecimento_p
	where	nr_atendimento = nr_atendimento_p;
end if;

if (coalesce(ie_executar_commit_p,'N') = 'S') then
	if (coalesce(wheb_usuario_pck.get_ie_commit, 'S') = 'S') then commit; end if;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE transferir_atendimento_estab ( nr_atendimento_p bigint, cd_estabelecimento_p bigint, ie_executar_commit_p text default null) FROM PUBLIC;

