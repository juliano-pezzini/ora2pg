-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE baca_pls_solic_dados_prest_web () AS $body$
DECLARE


qt_registro_w		bigint;


BEGIN

select	count(1)
into STRICT	qt_registro_w
from	funcao_parametro
where	cd_funcao	= 1246
and	nr_sequencia	= 33;

if (qt_registro_w > 0) then
	select	count(1)
	into STRICT	qt_registro_w
	from	funcao_parametro
	where	cd_funcao	= 1246
	and	nr_sequencia	= 45;

	if (qt_registro_w > 0) then
		-- Verifica se o parâmetro possui valores
		select count(1)
		into STRICT   qt_registro_w
		from   pls_web_param_usuario
		where  cd_funcao = 1246
		and    nr_seq_funcao_param = 33;

		if (qt_registro_w > 0) then
			insert into pls_web_param_usuario(nr_sequencia, cd_estabelecimento, nr_seq_param,
							  dt_atualizacao,  nm_usuario, dt_atualizacao_nrec,
							  nm_usuario_nrec, nr_seq_segurado, nr_seq_usu_estipulante,
							  nr_seq_usu_prestador, vl_parametro, nm_usuario_param,
							  nr_seq_funcao_param, cd_funcao, ie_tipo_acesso,
							  ds_observacao,nr_seq_perfil_web)
							 (SELECT	nextval('pls_web_param_usuario_seq'), cd_estabelecimento, nr_seq_param,
									clock_timestamp(),  nm_usuario, clock_timestamp(),
									nm_usuario_nrec, nr_seq_segurado, nr_seq_usu_estipulante,
									nr_seq_usu_prestador, vl_parametro, nm_usuario_param,
									45, cd_funcao, ie_tipo_acesso,
									ds_observacao,nr_seq_perfil_web
							    from	pls_web_param_usuario
							   where  	cd_funcao = 1246
							     and    	nr_seq_funcao_param = 33);
		end if;
	end if;
end if;


commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE baca_pls_solic_dados_prest_web () FROM PUBLIC;

