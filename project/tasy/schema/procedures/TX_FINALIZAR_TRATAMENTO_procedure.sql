-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE tx_finalizar_tratamento ( nr_seq_tratamento_p bigint, dt_fim_tratamento_p timestamp, nr_seq_motivo_p bigint, nr_seq_causa_p bigint, ds_observacao_p text) AS $body$
BEGIN
if (nr_seq_tratamento_p IS NOT NULL AND nr_seq_tratamento_p::text <> '') then

	update	paciente_tratamento
	set	dt_final_tratamento	= dt_fim_tratamento_p,
		nr_seq_motivo_fim	= nr_seq_motivo_p,
		nr_seq_causa_morte	= nr_seq_causa_p,
		ds_observacao		= coalesce(ds_observacao_p,ds_observacao),
		nm_usuario			= wheb_usuario_pck.get_nm_usuario,
		dt_atualizacao		= clock_timestamp()
	where	nr_sequencia	= nr_seq_tratamento_p;

end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE tx_finalizar_tratamento ( nr_seq_tratamento_p bigint, dt_fim_tratamento_p timestamp, nr_seq_motivo_p bigint, nr_seq_causa_p bigint, ds_observacao_p text) FROM PUBLIC;

