-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE atualizar_tipo_fatur_tiss ( nr_interno_conta_p bigint) AS $body$
DECLARE


dt_alta_w	timestamp;


BEGIN

select	max(b.dt_alta)
into STRICT	dt_alta_w
from	conta_paciente a,
	atendimento_paciente b
where	a.nr_atendimento = b.nr_atendimento
and	a.nr_interno_conta = nr_interno_conta_p;

if (coalesce(dt_alta_w::text, '') = '') then
	update	conta_paciente
	set	ie_tipo_fatur_tiss = 'P'
	where	nr_interno_conta = nr_interno_conta_p;
else
	update	conta_paciente
	set	ie_tipo_fatur_tiss = 'T'
	where	nr_interno_conta = nr_interno_conta_p;
end if;

if (coalesce(wheb_usuario_pck.get_ie_commit, 'S') = 'S') then commit; end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE atualizar_tipo_fatur_tiss ( nr_interno_conta_p bigint) FROM PUBLIC;

