-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE eme_consiste_usuario_conv ( cd_usuario_convenio_p text, cd_estabelecimento_p bigint, ds_msg_p INOUT text) AS $body$
DECLARE


cd_cgc_outorgante_w		varchar(14);
ds_digito_cart_usuario_w	varchar(4);
cd_cooperativa_w		varchar(10);



BEGIN

ds_msg_p := '';

select	max(cd_cgc_outorgante)
into STRICT	cd_cgc_outorgante_w
from	pls_outorgante;

if (cd_cgc_outorgante_w IS NOT NULL AND cd_cgc_outorgante_w::text <> '') then

	select  max(cd_cooperativa)
	into STRICT	cd_cooperativa_w
	from	pls_congenere
	where	cd_cgc = cd_cgc_outorgante_w;

	if (substr(cd_cooperativa_w,1,4) <> substr(cd_usuario_convenio_p,1,4)) then
		ds_msg_p := wheb_mensagem_pck.get_texto(801993);
	end if;
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE eme_consiste_usuario_conv ( cd_usuario_convenio_p text, cd_estabelecimento_p bigint, ds_msg_p INOUT text) FROM PUBLIC;

