-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE alterar_prestador_item ( nr_sequencia_p bigint, cd_cgc_solic_p text, nm_usuario_p text) AS $body$
DECLARE


cd_estabelecimento_w		estabelecimento.cd_estabelecimento%type := wheb_usuario_pck.get_cd_estabelecimento;
ie_altera_item_repasse_w	varchar(1) := 'S';


BEGIN
ie_altera_item_repasse_w := obter_param_usuario(1116, 189, obter_perfil_ativo, nm_usuario_p, cd_estabelecimento_w, ie_altera_item_repasse_w);

if (nr_sequencia_p IS NOT NULL AND nr_sequencia_p::text <> '') then

	update	material_atend_paciente a
	set	a.cd_cgc_prestador = cd_cgc_solic_p,
		a.nm_usuario = nm_usuario_p,
		a.dt_atualizacao = clock_timestamp()
	where	a.nr_sequencia = nr_sequencia_p
	and	not exists (	SELECT	1
				from	material_repasse x
				where	x.nr_seq_material = a.nr_sequencia
				and	coalesce(ie_altera_item_repasse_w,'S') = 'N');

end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE alterar_prestador_item ( nr_sequencia_p bigint, cd_cgc_solic_p text, nm_usuario_p text) FROM PUBLIC;

