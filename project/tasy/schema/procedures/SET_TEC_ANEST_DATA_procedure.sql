-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE set_tec_anest_data (NR_SEQUENCIA_P bigint, CD_PROFISSIONAL_P text, DT_INICIO_DH_P timestamp, DS_OBSERVACAO_P text, NM_USUARIO_P text) AS $body$
BEGIN

 UPDATE	CIRURGIA_TEC_ANESTESICA

	SET	DT_INICIO = DT_INICIO_DH_P,
		DT_FINAL = DT_INICIO_DH_P,
        CD_PROFISSIONAL = CD_PROFISSIONAL_P,
        DS_OBSERVACAO = DS_OBSERVACAO_P
        
	WHERE NR_SEQUENCIA = NR_SEQUENCIA_P;
    COMMIT;
    CALL liberar_informacao('CIRURGIA_TEC_ANESTESICA', 'NR_SEQUENCIA', NR_SEQUENCIA_P, NM_USUARIO_P);

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE set_tec_anest_data (NR_SEQUENCIA_P bigint, CD_PROFISSIONAL_P text, DT_INICIO_DH_P timestamp, DS_OBSERVACAO_P text, NM_USUARIO_P text) FROM PUBLIC;

