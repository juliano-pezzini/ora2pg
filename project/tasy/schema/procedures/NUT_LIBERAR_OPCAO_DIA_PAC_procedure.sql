-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE nut_liberar_opcao_dia_pac (nr_atendimento_p bigint, cd_setor_atendimento_p bigint, dt_referencia_p timestamp, nr_seq_servico_p bigint, cd_acompanhante_p text, nr_seq_acompanhante_p bigint, nm_usuario_p text, ie_opcao_p text default 'L') AS $body$
DECLARE


ie_status_w		varchar(5);
nr_seq_opcao_w		bigint;
dt_liberacao_w		timestamp;
nr_sequencia_w		bigint;

C01 CURSOR FOR
	SELECT	a.nr_sequencia
	FROM	nut_pac_opcao_rec a,
		nut_atend_serv_dia b
	WHERE	a.nr_seq_servico_dia		= b.nr_sequencia
	AND	b.cd_setor_atendimento	    	= cd_setor_atendimento_p
	AND	b.nr_atendimento		= nr_atendimento_p
	AND	TRUNC(b.DT_SERVICO)		= TRUNC(dt_referencia_p)
	AND	b.nr_seq_servico		= nr_seq_servico_p
	AND	(((cd_acompanhante_p = 0) 	AND (coalesce(cd_acompanhante,0) = 0))     OR ((cd_acompanhante_p <> 0)     AND (coalesce(cd_acompanhante,0)     = cd_acompanhante_p)))
	AND	(((nr_seq_acompanhante_p = 0)   AND (coalesce(nr_seq_acompanhante,0) = 0)) OR ((nr_seq_acompanhante_p <> 0) AND (coalesce(nr_seq_acompanhante,0) = nr_seq_acompanhante_p)));


BEGIN

open C01;
loop
fetch C01 into
	nr_sequencia_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin
	if (ie_opcao_p = 'D') then
		update 	nut_pac_opcao_rec
		set	dt_liberacao	 = NULL,
			dt_atualizacao	= clock_timestamp(),
			nm_usuario	= nm_usuario_p
		where	nr_sequencia	= nr_sequencia_w;
	else
		update 	nut_pac_opcao_rec
		set	dt_liberacao	= clock_timestamp(),
			dt_atualizacao	= clock_timestamp(),
			nm_usuario	= nm_usuario_p
		where	nr_sequencia	= nr_sequencia_w;
	end if;
	end;
end loop;
close C01;


commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE nut_liberar_opcao_dia_pac (nr_atendimento_p bigint, cd_setor_atendimento_p bigint, dt_referencia_p timestamp, nr_seq_servico_p bigint, cd_acompanhante_p text, nr_seq_acompanhante_p bigint, nm_usuario_p text, ie_opcao_p text default 'L') FROM PUBLIC;

