-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consiste_sus_compat_proc_agen (nr_seq_agenda_p bigint, cd_procedimento_p bigint, ie_origem_proced_p bigint, ds_retorno_p INOUT text) AS $body$
DECLARE


cd_procedimento_adic_w	bigint;
ie_permite_w		varchar(15);

c01 CURSOR FOR
	SELECT	cd_procedimento
	from	agenda_paciente_proc
	where	nr_sequencia = nr_seq_agenda_p
	and	((coalesce(cd_convenio::text, '') = '') or (obter_tipo_convenio(cd_convenio) = '3'));


BEGIN
ds_retorno_p := null;

-- Rotina foi descontinuada por tempo indeterminado, pois gerou problemas em diversos clientes.
-- Problemas de conceito de cadastro no SUS - UNIFICADO.
/*open C01;
loop
fetch C01 into
	cd_procedimento_adic_w;
exit when C01%notfound;
	begin
	select	substr(nvl(max(sus_obter_se_proc_compativel(cd_procedimento_p,ie_origem_proced_p,cd_procedimento_adic_w)),'S'),1,15)
	into	ie_permite_w
	from	dual;
	if	(ie_permite_w = 'N') then

		Não é possível alterar o convênio para SUS. Os procedimentos adicionais são incompatíveis com este procedimento.
		É necessário realizar a exclusão dos procedimentos adicionais para realizar a troca do convênio/procedimento¿.

		ds_retorno_p := wheb_mensagem_pck.get_texto(338965, null);
	end if;
	end;
end loop;
close C01;*/
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consiste_sus_compat_proc_agen (nr_seq_agenda_p bigint, cd_procedimento_p bigint, ie_origem_proced_p bigint, ds_retorno_p INOUT text) FROM PUBLIC;

