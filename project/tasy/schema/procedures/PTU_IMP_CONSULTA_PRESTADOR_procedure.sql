-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE ptu_imp_consulta_prestador ( ds_arquivo_p text, cd_estabelecimento_p bigint, nm_usuario_p text, nr_seq_retorno_p INOUT bigint) AS $body$
DECLARE

 
nr_seq_importacao_w		bigint;
ds_conteudo_w			varchar(4000);
cd_transacao_w			varchar(5);
ie_tipo_cliente_w		varchar(15);
cd_unimed_exec_w		smallint;
cd_unimed_benef_w		smallint;
nr_seq_execucao_w		bigint;
nm_prestador_w			varchar(40);
cd_cgc_cpf_w			bigint;
sg_cons_prof_w			varchar(12);
nr_cons_prof_w			bigint;
uf_cons_prof_w			varchar(2);
nr_seq_consulta_w		bigint;
				
c01 CURSOR FOR 
	SELECT	nr_seq_importacao, 
		ds_valores 
	from	w_scs_importacao 
	where	nm_usuario	= nm_usuario_p 
	order by nr_seq_importacao;


BEGIN 
 
open c01;
loop 
fetch c01 into 
	nr_seq_importacao_w, 
	ds_conteudo_w;
EXIT WHEN NOT FOUND; /* apply on c01 */
	begin 
	 
	if (substr(ds_conteudo_w,1,4)	<> 'FIM$') then 
		if (nr_seq_importacao_w	= 1) then 
			select 	substr(ds_conteudo_w,1,5), 
				trim(both substr(ds_conteudo_w,6,15)), 
				CASE WHEN (substr(ds_conteudo_w,21,4))::numeric =0 THEN ''  ELSE (substr(ds_conteudo_w,21,4))::numeric  END , 
				CASE WHEN (substr(ds_conteudo_w,25,4))::numeric =0 THEN ''  ELSE (substr(ds_conteudo_w,25,4))::numeric  END , 
				CASE WHEN (substr(ds_conteudo_w,29,10))::numeric =0 THEN ''  ELSE (substr(ds_conteudo_w,29,10))::numeric  END , 
				trim(both substr(ds_conteudo_w,39,40)), 
				CASE WHEN (substr(ds_conteudo_w,79,15))::numeric =0 THEN ''  ELSE (substr(ds_conteudo_w,79,15))::numeric  END , 
				trim(both substr(ds_conteudo_w,94,12)), 
				CASE WHEN (substr(ds_conteudo_w,106,15))::numeric =0 THEN ''  ELSE (substr(ds_conteudo_w,106,15))::numeric  END , 
				trim(both substr(ds_conteudo_w,121,2)) 
			into STRICT  cd_transacao_w, 
				ie_tipo_cliente_w, 
				cd_unimed_exec_w, 
				cd_unimed_benef_w, 
				nr_seq_execucao_w, 
				nm_prestador_w, 
				cd_cgc_cpf_w, 
				sg_cons_prof_w, 
				nr_cons_prof_w, 
				uf_cons_prof_w 
			;
 
			if (ie_tipo_cliente_w	= 'UNIMED') then 
				ie_tipo_cliente_w	:= 'U';
			elsif (ie_tipo_cliente_w	= 'PORTAL') then 
				ie_tipo_cliente_w	:= 'P';
			elsif (ie_tipo_cliente_w	= 'PRESTADOR') then 
				ie_tipo_cliente_w	:= 'R';
			end if;
			 
			select	nextval('ptu_consulta_prestador_seq') 
			into STRICT	nr_seq_consulta_w 
			;
			 
			insert	into ptu_consulta_prestador(nr_sequencia, cd_transacao, ie_tipo_cliente, 
				 cd_unimed_executora, cd_unimed_beneficiario, nr_seq_execucao, 
				 nm_prestador, dt_atualizacao, nm_usuario, 
				 cd_cgc_cpf, sg_cons_profissional, nr_cons_profissional, 
				 uf_cons_profissional, nr_seq_guia, nr_seq_requisicao, 
				 ds_arquivo_pedido) 
			values (nr_seq_consulta_w, cd_transacao_w, ie_tipo_cliente_w, 
				 cd_unimed_exec_w, cd_unimed_benef_w, nr_seq_execucao_w, 
				 nm_prestador_w, clock_timestamp(), nm_usuario_p, 
				 cd_cgc_cpf_w, sg_cons_prof_w, nr_cons_prof_w, 
				 uf_cons_prof_w, nr_seq_execucao_w, null, 
				 ds_arquivo_p);
		end if;
	end if;
	 
	end;
end loop;
close c01;
 
nr_seq_retorno_p := ptu_gerar_resp_consulta_prest(nr_seq_consulta_w, cd_estabelecimento_p, nm_usuario_p, nr_seq_retorno_p);
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE ptu_imp_consulta_prestador ( ds_arquivo_p text, cd_estabelecimento_p bigint, nm_usuario_p text, nr_seq_retorno_p INOUT bigint) FROM PUBLIC;

