-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE lab_insere_impressao_mapa ( nr_prescricao_p bigint, nr_seq_prescr_p bigint, nr_seq_exame_p bigint, nm_usuario_p text, cd_setor_atendimento_p text) AS $body$
DECLARE


nr_seq_grup_imp_w		bigint;
nr_sequencia_w			bigint;
cd_estabelecimento_w 	lab_impressao_mapa.cd_estabelecimento%type;

BEGIN

select coalesce(max(cd_estabelecimento), obter_estabelecimento_ativo)
into STRICT cd_estabelecimento_w
from setor_atendimento
where 	cd_setor_atendimento = cd_setor_atendimento_p;

select	max(nr_seq_grupo_imp)
into STRICT	nr_seq_grup_imp_w
from	exame_laboratorio
where	nr_seq_exame = nr_seq_exame_p;

if (nr_seq_grup_imp_w > 0) then

	select	nextval('lab_impressao_mapa_seq')
	into STRICT	nr_sequencia_w
	;

	insert into lab_impressao_mapa(
		nr_sequencia,
		nr_prescricao,
		nr_seq_prescr,
		nr_seq_grup_imp,
		cd_mapa,
		ie_status,
		dt_atualizacao,
		nm_usuario,
		cd_estabelecimento
	) values (
		nr_sequencia_w,
		nr_prescricao_p,
		nr_seq_prescr_p,
		nr_seq_grup_imp_w,
		null,
		10,
		clock_timestamp(),
		nm_usuario_p,
		cd_estabelecimento_w
	);

	CALL lab_insere_impressao_mapa_log(nr_prescricao_p, nr_seq_prescr_p, 10, null, nm_usuario_p);
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE lab_insere_impressao_mapa ( nr_prescricao_p bigint, nr_seq_prescr_p bigint, nr_seq_exame_p bigint, nm_usuario_p text, cd_setor_atendimento_p text) FROM PUBLIC;

