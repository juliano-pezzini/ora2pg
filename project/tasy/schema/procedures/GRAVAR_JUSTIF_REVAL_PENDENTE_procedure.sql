-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gravar_justif_reval_pendente ( nr_seq_processo_p adep_processo.nr_sequencia%type, ds_justificativa_p justificativa_revalidacao.ds_justificativa%type, nm_usuario_p usuario.nm_usuario%type) AS $body$
BEGIN

if (coalesce(nr_seq_processo_p, 0) > 0) and (ds_justificativa_p IS NOT NULL AND ds_justificativa_p::text <> '') and (nm_usuario_p IS NOT NULL AND nm_usuario_p::text <> '') then

	insert into justificativa_revalidacao(
		nr_sequencia,
		dt_atualizacao,
		dt_atualizacao_nrec,
		nm_usuario,
		nm_usuario_nrec,
		ds_justificativa,
		nr_seq_processo)
		values (
		nextval('justificativa_revalidacao_seq'),
		clock_timestamp(),
		clock_timestamp(),
		nm_usuario_p,
		nm_usuario_p,
		ds_justificativa_p,
		nr_seq_processo_p);

end if;

if (coalesce(wheb_usuario_pck.get_ie_commit, 'S') = 'S') then commit; end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gravar_justif_reval_pendente ( nr_seq_processo_p adep_processo.nr_sequencia%type, ds_justificativa_p justificativa_revalidacao.ds_justificativa%type, nm_usuario_p usuario.nm_usuario%type) FROM PUBLIC;

