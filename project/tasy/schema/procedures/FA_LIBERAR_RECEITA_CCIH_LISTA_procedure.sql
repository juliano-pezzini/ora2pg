-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE fa_liberar_receita_ccih_lista ( ds_sequencias_medic_p text, nm_usuario_p text) AS $body$
DECLARE


nr_seq_medic_w		varchar(255);
ds_linha_w		varchar(1000);


BEGIN
ds_linha_w	:= ds_sequencias_medic_p;

while(ds_linha_w IS NOT NULL AND ds_linha_w::text <> '') loop
	nr_seq_medic_w	:= substr(ds_linha_w,1,position(';' in ds_linha_w) - 1);
	ds_linha_w	:= substr(ds_linha_w,position(';' in ds_linha_w) + 1,length(ds_linha_w));
	CALL fa_liberar_receita_ccih(nr_seq_medic_w,nm_usuario_p);
end loop;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE fa_liberar_receita_ccih_lista ( ds_sequencias_medic_p text, nm_usuario_p text) FROM PUBLIC;

