-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE baixar_processo_agenda (nr_seq_agenda_p bigint, cd_processo_p bigint, nr_seq_etapa_p bigint, nm_usuario_p text) AS $body$
DECLARE



dt_fim_real_w				timestamp;


BEGIN

select	max(dt_fim_real)
into STRICT	dt_fim_real_w
from	agenda_pac_processo
where	nr_seq_agenda	= nr_seq_agenda_p
and	cd_processo	= cd_processo_p
and	nr_seq_etapa	= nr_seq_etapa_p;

if (coalesce(dt_fim_real_w::text, '') = '') then
	update	agenda_pac_processo
	set	dt_fim_real	= clock_timestamp(),
		ie_status	= 'F',
		nm_usuario	= nm_usuario_p,
		dt_atualizacao	= clock_timestamp()
	where	nr_seq_agenda	= nr_seq_agenda_p
	and	cd_processo	= cd_processo_p
	and	nr_seq_etapa	= nr_seq_etapa_p;
end if;

commit;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE baixar_processo_agenda (nr_seq_agenda_p bigint, cd_processo_p bigint, nr_seq_etapa_p bigint, nm_usuario_p text) FROM PUBLIC;

