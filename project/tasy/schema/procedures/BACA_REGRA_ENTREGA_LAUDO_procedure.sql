-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE baca_regra_entrega_laudo () AS $body$
DECLARE


cd_estabelecimento_w		smallint;
cd_estab_w			smallint;

c01 CURSOR FOR
	SELECT	cd_estabelecimento
	from	estabelecimento
	where	cd_estabelecimento	<> cd_estabelecimento_w;


BEGIN

select	min(cd_estabelecimento)
into STRICT	cd_estabelecimento_w
from	regra_entrega_laudo;

OPEN C01;
LOOP
FETCH c01 INTO
	cd_estab_w;
EXIT WHEN NOT FOUND; /* apply on c01 */
	insert into regra_entrega_laudo(
		nr_sequencia,
		cd_setor_atendimento,
		dt_atualizacao,
		nm_usuario,
		ie_feriado,
		ie_sabado,
		ie_domingo,
		ds_hora_inicial,
		ds_hora_final,
		qt_dia,
		qt_hora,
		ds_hora_fixa,
		ie_tipo_atendimento,
		cd_area_procedimento,
		cd_especialidade,
		cd_grupo_proc,
		cd_procedimento,
		ie_origem_proced,
		dt_atualizacao_nrec,
		nm_usuario_nrec,
		ie_tipo_convenio,
		cd_estabelecimento,
		ie_tipo_regra)
	SELECT	nextval('regra_entrega_laudo_seq'),
		cd_setor_atendimento,
		dt_atualizacao,
		nm_usuario,
		ie_feriado,
		ie_sabado,
		ie_domingo,
		ds_hora_inicial,
		ds_hora_final,
		qt_dia,
		qt_hora,
		ds_hora_fixa,
		ie_tipo_atendimento,
		cd_area_procedimento,
		cd_especialidade,
		cd_grupo_proc,
		cd_procedimento,
		ie_origem_proced,
		dt_atualizacao_nrec,
		nm_usuario_nrec,
		ie_tipo_convenio,
		cd_estab_w,
		ie_tipo_regra
	from	regra_entrega_laudo
	where	cd_estabelecimento	= cd_estabelecimento_w
	and not exists (	SELECT	1
			from	regra_entrega_laudo
			where	cd_estabelecimento	= cd_estab_w);

END LOOP;
CLOSE c01;

commit;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE baca_regra_entrega_laudo () FROM PUBLIC;

