-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE obter_regra_presc_prot_cir ( nr_seq_proc_interno_p bigint, cd_protocolo_p INOUT bigint, nr_seq_protocolo_p INOUT bigint, cd_prescritor_p INOUT text, nr_horas_validade_p INOUT bigint, nr_seq_protocolo_sae_p INOUT bigint, nr_seq_subtipo_p INOUT bigint ) AS $body$
DECLARE


cd_protocolo_w		bigint 	:= 0;
nr_seq_protocolo_w	bigint 	:= 0;
nr_seq_protocolo_sae_w	bigint 	:= 0;
nr_seq_subtipo_w	bigint 	:= 0;
nr_seq_prot_proc_w	bigint 	:= 0;
cd_pessoa_fisica_w	varchar(10)	:= null;
nr_horas_validade_w	integer	:= 0;
qt_registro_w		bigint 	:= 0;


BEGIN

select	count(*)
into STRICT	qt_registro_w
from	regra_prescr_prot_cirurgia
where	coalesce(ie_situacao,'A') = 'A';

if (qt_registro_w > 0) then
	select	max(cd_protocolo),
		max(nr_seq_protocolo),
		max(cd_pessoa_fisica),
		max(nr_horas_validade),
		max(nr_seq_protocolo_sae),
		max(nr_seq_subtipo)
	into STRICT	cd_protocolo_w,
		nr_seq_protocolo_w,
		cd_pessoa_fisica_w,
		nr_horas_validade_w,
		nr_seq_protocolo_sae_w,
		nr_seq_subtipo_w
	from	regra_prescr_prot_cirurgia
	where	coalesce(nr_seq_proc_interno,nr_seq_proc_interno_p) = nr_seq_proc_interno_p
	and	coalesce(ie_situacao,'A') = 'A';
end if;
cd_protocolo_p		:= cd_protocolo_w;
nr_seq_protocolo_p	:= nr_seq_protocolo_w;
cd_prescritor_p		:= cd_pessoa_fisica_w;
nr_horas_validade_p	:= nr_horas_validade_w;
nr_seq_protocolo_sae_p	:= nr_seq_protocolo_sae_w;
nr_seq_subtipo_p	:= nr_seq_subtipo_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE obter_regra_presc_prot_cir ( nr_seq_proc_interno_p bigint, cd_protocolo_p INOUT bigint, nr_seq_protocolo_p INOUT bigint, cd_prescritor_p INOUT text, nr_horas_validade_p INOUT bigint, nr_seq_protocolo_sae_p INOUT bigint, nr_seq_subtipo_p INOUT bigint ) FROM PUBLIC;

