-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consistir_altura_pac_exame (cd_agenda_p bigint, qt_altura_paciente_p bigint, ie_altura_p INOUT text) AS $body$
DECLARE

 
ie_altura_w		varchar(1) := 'S';
qt_altura_aparelho_w	real;


BEGIN 
if (cd_agenda_p IS NOT NULL AND cd_agenda_p::text <> '') and (qt_altura_paciente_p IS NOT NULL AND qt_altura_paciente_p::text <> '') and (qt_altura_paciente_p > 0) then 
 
	/* obter altura aparelho */
 
	select	coalesce(max(qt_altura_cm),0) 
	into STRICT	qt_altura_aparelho_w 
	from	agenda 
	where	cd_agenda = cd_agenda_p;
 
	/* consistir altura aparelho x paciente */
 
	if (qt_altura_aparelho_w > 0) and (qt_altura_paciente_p > qt_altura_aparelho_w) then 
 
		ie_altura_w := 'N';
 
	end if;
 
end if;
 
ie_altura_p := ie_altura_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consistir_altura_pac_exame (cd_agenda_p bigint, qt_altura_paciente_p bigint, ie_altura_p INOUT text) FROM PUBLIC;

