-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE inserir_log_responsavel_cred ( nr_cirurgia_p bigint, ds_resp_atual_p text, ds_resp_anterior_p text, ds_procedimento_p text, nm_usuario_p text) AS $body$
DECLARE


ds_log_w	varchar(2000);


BEGIN
null;
	/*ds_log_w := obter_texto_dic_objeto(182395, wheb_usuario_pck.get_nr_seq_idioma, 'DS_RESP_ATUAL='||ds_resp_atual_p||';DS_RESP_ANT='||ds_resp_anterior_p||';NR_CIRURGIA='||nr_cirurgia_p||';DS_PROCEDIMENTO='||ds_procedimento_p);

	if	(ds_log_w is not null) then

		insert	into log_tasy (
			dt_atualizacao,
			nm_usuario,
			cd_log,
			ds_log)
		values (
			sysdate,
			nm_usuario_p,
			88941,
			ds_log_w);

		commit;

	end if;*/
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE inserir_log_responsavel_cred ( nr_cirurgia_p bigint, ds_resp_atual_p text, ds_resp_anterior_p text, ds_procedimento_p text, nm_usuario_p text) FROM PUBLIC;

