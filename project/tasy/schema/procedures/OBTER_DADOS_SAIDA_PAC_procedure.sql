-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE obter_dados_saida_pac ( cd_Setor_Atendimento_p bigint, cd_unidade_basica_p text, cd_unidade_compl_p text, nm_usuario_p text, nr_Atendimento_p INOUT text, nm_paciente_p INOUT text) AS $body$
DECLARE

 
/* 
A	-	Número de atendimento 
P	-	Nome do paciente 
*/
 
					 
ds_retorno_w		varchar(100) := null;
cd_setor_atendimento_w	integer;
cd_unidade_basica_w	varchar(10);
cd_unidade_compl_w   varchar(10);
nr_atendimento_w	bigint;
dt_referencia_w		timestamp	:= trunc(clock_timestamp());
dt_refer_final_w	timestamp;
nm_paciente_W		varchar(60);
				

BEGIN 
dt_refer_final_w	:= fim_dia(dt_referencia_w);
 
select	max(a.nr_atendimento) 
into STRICT	nr_atendimento_w 
from	atend_paciente_unidade b, 
	atendimento_paciente a 
where 	a.nr_atendimento	= b.nr_atendimento 
and  	a.dt_alta_interno between dt_referencia_w and dt_refer_final_w 
and	(a.dt_alta IS NOT NULL AND a.dt_alta::text <> '') 
and	coalesce(a.dt_saida_real::text, '') = '' 
and	b.cd_setor_atendimento	=	cd_setor_atendimento_p 
and	b.cd_unidade_basica	=	cd_unidade_basica_p 
and	b.cd_unidade_compl	=	cd_unidade_compl_p;
 
if (coalesce(nr_atendimento_w,0)	> 0) then 
	select	substr(obter_nome_pf(cd_pessoa_fisica),1,60) 
	into STRICT	nm_paciente_w 
	from	atendimento_paciente 
	where	nr_atendimento	= nr_atendimento_w;
end if;
 
nr_Atendimento_p	:= nr_Atendimento_w;
nm_paciente_p		:= nm_paciente_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE obter_dados_saida_pac ( cd_Setor_Atendimento_p bigint, cd_unidade_basica_p text, cd_unidade_compl_p text, nm_usuario_p text, nr_Atendimento_p INOUT text, nm_paciente_p INOUT text) FROM PUBLIC;

