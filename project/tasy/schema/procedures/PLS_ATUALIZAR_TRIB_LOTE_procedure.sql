-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_atualizar_trib_lote ( nr_seq_lote_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) AS $body$
DECLARE

				 
cd_cgc_w			varchar(14);
cd_pessoa_fisica_w		varchar(10);
nr_seq_trib_prest_w		bigint;

C01 CURSOR FOR 
	SELECT	cd_pessoa_fisica, 
		cd_cgc, 
		nr_sequencia 
	from	pls_lote_ret_trib_prest 
	where	nr_seq_lote	= nr_seq_lote_p 
	and	coalesce(dt_conferencia::text, '') = '';


BEGIN 
 
open C01;
loop 
fetch C01 into	 
	cd_pessoa_fisica_w, 
	cd_cgc_w, 
	nr_seq_trib_prest_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin 
	CALL pls_atualizar_trib_prest_mes(	cd_pessoa_fisica_w, 
					cd_cgc_w, 
					nr_seq_trib_prest_w, 
					nm_usuario_p);
	end;
end loop;
close C01;
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_atualizar_trib_lote ( nr_seq_lote_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) FROM PUBLIC;

