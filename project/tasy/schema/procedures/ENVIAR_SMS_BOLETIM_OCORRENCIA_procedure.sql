-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE enviar_sms_boletim_ocorrencia ( ds_remetente_p text, ds_destinatario_p text, ds_mensagem_p text, nr_seq_bol_ocorr_p bigint, nm_usuario_p text) AS $body$
DECLARE


nr_sequencia_w	bigint;
id_sms_w		bigint;

BEGIN
if (ds_remetente_p IS NOT NULL AND ds_remetente_p::text <> '') and (ds_destinatario_p IS NOT NULL AND ds_destinatario_p::text <> '') and (ds_mensagem_p IS NOT NULL AND ds_mensagem_p::text <> '') and (nm_usuario_p IS NOT NULL AND nm_usuario_p::text <> '') and (nr_seq_bol_ocorr_p IS NOT NULL AND nr_seq_bol_ocorr_p::text <> '') then

	/* enviar sms */

	id_sms_w := wheb_sms.enviar_sms(ds_remetente_p, ds_destinatario_p, ds_mensagem_p, nm_usuario_p, id_sms_w);

	insert into sac_bol_ocor_envio(
		nr_sequencia,
		dt_atualizacao,
		nm_usuario,
		dt_atualizacao_nrec,
		nm_usuario_nrec,
		nr_seq_bo,
		dt_envio,
		ds_destino,
		ds_observacao,
		ie_reenvio,
		id_sms)
	values (	nextval('sac_bol_ocor_envio_seq'),
		clock_timestamp(),
		nm_usuario_p,
		clock_timestamp(),
		nm_usuario_p,
		nr_seq_bol_ocorr_p,
		clock_timestamp(),
		ds_destinatario_p,
		'ID SMS=' || id_sms_w,
		'N',
		id_sms_w);
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE enviar_sms_boletim_ocorrencia ( ds_remetente_p text, ds_destinatario_p text, ds_mensagem_p text, nr_seq_bol_ocorr_p bigint, nm_usuario_p text) FROM PUBLIC;

