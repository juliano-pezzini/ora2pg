-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE agenda_exame_consent_email ( ds_texto_p text, ds_texto_conv_p INOUT text, cd_estabelecimento_p bigint, cd_perfil_p bigint, nm_usuario_p text) AS $body$
DECLARE


nr_seq_html_w		bigint;
qt_texto_conv_w		bigint;
ds_texto_conv_w		text;


BEGIN

if (ds_texto_p IS NOT NULL AND ds_texto_p::text <> '') then
	begin
	nr_seq_html_w := obter_html_rtf(ds_texto_p, nm_usuario_p, nr_seq_html_w);

	select	count(*)
	into STRICT	qt_texto_conv_w
	from	tasy_conversao_rtf
	where	nr_sequencia = nr_seq_html_w;

	if (qt_texto_conv_w > 0) then
		begin

		select	ds_texto
		into STRICT	ds_texto_conv_w
		from	tasy_conversao_rtf
		where	nr_sequencia = nr_seq_html_w;

		end;
	end if;

	end;
end if;

ds_texto_conv_p	:= ds_texto_conv_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE agenda_exame_consent_email ( ds_texto_p text, ds_texto_conv_p INOUT text, cd_estabelecimento_p bigint, cd_perfil_p bigint, nm_usuario_p text) FROM PUBLIC;

