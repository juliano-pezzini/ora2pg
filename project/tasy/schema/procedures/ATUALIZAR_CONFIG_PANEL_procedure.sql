-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE atualizar_config_panel ( cd_perfil_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text, ds_fonte_p INOUT text, ds_cor_p INOUT text, qt_tamanho_p INOUT bigint) AS $body$
DECLARE


ds_fonte_w	varchar(255) := '';
ds_cor_w		varchar(255) := '';
qt_tamanho_w	varchar(255) := '';


BEGIN

if (cd_perfil_p IS NOT NULL AND cd_perfil_p::text <> '') and (cd_estabelecimento_p IS NOT NULL AND cd_estabelecimento_p::text <> '') and (nm_usuario_p IS NOT NULL AND nm_usuario_p::text <> '') then
	begin

	ds_fonte_w	:= substr(obter_valor_param_usuario(281, 5, cd_perfil_p, nm_usuario_p, cd_estabelecimento_p),1,255);
	ds_cor_w		:= substr(obter_valor_param_usuario(281, 7, cd_perfil_p, nm_usuario_p, cd_estabelecimento_p),1,255);
	qt_tamanho_w	:= substr(obter_valor_param_usuario(281, 6, cd_perfil_p, nm_usuario_p, cd_estabelecimento_p),1,255);

	end;
end if;

ds_fonte_p	:= ds_fonte_w;
ds_cor_p		:= ds_cor_w;
qt_tamanho_p	:= qt_tamanho_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE atualizar_config_panel ( cd_perfil_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text, ds_fonte_p INOUT text, ds_cor_p INOUT text, qt_tamanho_p INOUT bigint) FROM PUBLIC;

