-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';



--
-- dblink wrapper to call function gravar_log_mov as an autonomous transaction
--
CREATE EXTENSION IF NOT EXISTS dblink;

CREATE OR REPLACE PROCEDURE gravar_log_mov (CD_LOG_P bigint, DS_LOG_P text, NM_USUARIO_P text) AS $body$
DECLARE
	-- Change this to reflect the dblink connection string
	v_conn_str  text := format('port=%s dbname=%s user=%s', current_setting('port'), current_database(), current_user);
	v_query     text;

BEGIN
	v_query := 'CALL gravar_log_mov_atx ( ' || quote_nullable(CD_LOG_P) || ',' || quote_nullable(DS_LOG_P) || ',' || quote_nullable(NM_USUARIO_P) || ' )';
	PERFORM * FROM dblink(v_conn_str, v_query) AS p (ret boolean);

END;
$body$ LANGUAGE plpgsql SECURITY DEFINER;




CREATE OR REPLACE PROCEDURE gravar_log_mov_atx (CD_LOG_P bigint, DS_LOG_P text, NM_USUARIO_P text) AS $body$
BEGIN
  INSERT INTO LOG_MOV(DT_ATUALIZACAO, NM_USUARIO, DS_LOG, CD_LOG)
  VALUES (clock_timestamp(), NM_USUARIO_P, DS_LOG_P, CD_LOG_P);


END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gravar_log_mov (CD_LOG_P bigint, DS_LOG_P text, NM_USUARIO_P text) FROM PUBLIC; -- REVOKE ALL ON PROCEDURE gravar_log_mov_atx (CD_LOG_P bigint, DS_LOG_P text, NM_USUARIO_P text) FROM PUBLIC;

