-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE lab_gravar_retorno_vtrp (nr_lote_p bigint, dt_integracao_p timestamp, cd_mensagem_p bigint, ds_mensagem_p text, ds_carteira_p text, ds_guia_p text, ds_senha_p text, nm_usuario_p text) AS $body$
DECLARE


nr_sequencia_w	lab_retorno_integ_vtrp.nr_sequencia%type;


BEGIN
	if (nr_lote_p IS NOT NULL AND nr_lote_p::text <> '' AND cd_mensagem_p IS NOT NULL AND cd_mensagem_p::text <> '') then
		select	nextval('lab_retorno_integ_vtrp_seq')
		into STRICT	nr_sequencia_w
		;
		insert	into	lab_retorno_integ_vtrp(
			nr_sequencia,
			nr_lote,
			nm_usuario_nrec,
			nm_usuario,
			dt_atualizacao_nrec,
			dt_atualizacao,
			dt_integracao,
			cd_mensagem,
			ds_mensagem,
			ds_carteira,
			ds_guia,
			ds_senha)
		values (
			nr_sequencia_w,
			nr_lote_p,
			nm_usuario_p,
			nm_usuario_p,
			clock_timestamp(),
			clock_timestamp(),
			dt_integracao_p,
			cd_mensagem_p,
			ds_mensagem_p,
			ds_carteira_p,
			ds_guia_p,
			ds_senha_p);
		commit;
	end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE lab_gravar_retorno_vtrp (nr_lote_p bigint, dt_integracao_p timestamp, cd_mensagem_p bigint, ds_mensagem_p text, ds_carteira_p text, ds_guia_p text, ds_senha_p text, nm_usuario_p text) FROM PUBLIC;

