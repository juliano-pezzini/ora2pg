-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE agenda_paciente_griddblclick ( nr_seq_agenda_p bigint, cd_pessoa_fisica_p text, cd_estabelecimento_p bigint, nm_usuario_p text, ds_reservado_p INOUT text, ie_se_pac_agenda_p INOUT text, ds_telefone_p INOUT text) AS $body$
DECLARE

 
ie_reservado_w		varchar(1);
ie_se_pac_agenda_w	varchar(1);
ds_telefone_w		varchar(255);
						

BEGIN 
 
if (nr_seq_agenda_p > 0) then 
	begin 
	if (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') then 
		begin 
		ie_se_pac_agenda_w	:= substr(Obter_Se_Pac_Agenda(cd_pessoa_fisica_p, nm_usuario_p, cd_estabelecimento_p),1,1);
		ds_telefone_w		:= substr(obter_fone_pac_agenda(cd_pessoa_fisica_p),1,255);
		end;
	end if;	
	end;
end if;
 
ie_se_pac_agenda_p	:= ie_se_pac_agenda_w;
ds_telefone_p		:= ds_telefone_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE agenda_paciente_griddblclick ( nr_seq_agenda_p bigint, cd_pessoa_fisica_p text, cd_estabelecimento_p bigint, nm_usuario_p text, ds_reservado_p INOUT text, ie_se_pac_agenda_p INOUT text, ds_telefone_p INOUT text) FROM PUBLIC;

