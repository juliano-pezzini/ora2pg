-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE atual_senha_gestao_vaga_autor ( nr_seq_gestao_p bigint, nr_seq_agenda_p bigint, nr_seq_agenda_consulta_p bigint, cd_senha_p text, nm_usuario_p text) AS $body$
BEGIN
 
if (cd_senha_p IS NOT NULL AND cd_senha_p::text <> '') then 
 
	if (nr_seq_gestao_p > 0) then 
		update	gestao_vaga 
		set	cd_senha	= cd_senha_p 
		where	nr_sequencia	= nr_seq_gestao_p;
	elsif (nr_seq_agenda_p > 0) then 
		update	gestao_vaga 
		set	cd_senha	= cd_senha_p 
		where	nr_seq_agenda	= nr_seq_agenda_p;
	elsif (nr_seq_agenda_consulta_p > 0) then 
		update	gestao_vaga 
		set	cd_senha	= cd_senha_p 
		where	nr_seq_agenda	= nr_seq_agenda_consulta_p;
	end if;
end if;
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE atual_senha_gestao_vaga_autor ( nr_seq_gestao_p bigint, nr_seq_agenda_p bigint, nr_seq_agenda_consulta_p bigint, cd_senha_p text, nm_usuario_p text) FROM PUBLIC;

