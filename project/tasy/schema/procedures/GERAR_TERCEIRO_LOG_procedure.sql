-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_terceiro_log (cd_pessoa_fisica_p text, nm_usuario_p text, cd_estabelecimento_p bigint) AS $body$
DECLARE


nr_seq_terceiro_w	bigint;


BEGIN

select	max(nr_sequencia)
into STRICT	nr_seq_terceiro_w
from	terceiro
where	cd_pessoa_fisica = cd_pessoa_fisica_p
and	cd_estabelecimento = cd_estabelecimento_p;

if (coalesce(nr_seq_terceiro_w,0) > 0) then
	begin
	insert into usuario_setor_cargo(nr_sequencia,
			dt_atualizacao,
			nm_usuario,
			dt_referencia,
			cd_pessoa_fisica,
			nr_seq_terceiro)
		values (nextval('usuario_setor_cargo_seq'),
			clock_timestamp(),
			nm_usuario_p,
			clock_timestamp(),
			cd_pessoa_fisica_p,
			nr_seq_terceiro_w);

	commit;

	end;
	end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_terceiro_log (cd_pessoa_fisica_p text, nm_usuario_p text, cd_estabelecimento_p bigint) FROM PUBLIC;

