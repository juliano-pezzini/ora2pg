-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_horarios_ms (cd_estabelecimento_p estabelecimento.cd_estabelecimento%type, cd_agendas_p text, dt_agenda_inicial_p timestamp, dt_agenda_final_p timestamp, nm_usuario_p usuario.nm_usuario%type, ie_sem_atendimento_p text, ie_tipo_agendamento_p text) AS $body$
DECLARE


dt_agenda_inicial_w		timestamp;									
dt_agenda_final_w		timestamp;	
								

BEGIN
	if (ie_tipo_agendamento_p = 'D') then
		dt_agenda_inicial_w := inicio_dia(dt_agenda_inicial_p);
		dt_agenda_final_w 	:= fim_dia(dt_agenda_inicial_p);
		CALL mult_schedule_pck.generate_schedules(cd_estabelecimento_p, cd_agendas_p, dt_agenda_inicial_w, dt_agenda_final_w, nm_usuario_p, ie_sem_atendimento_p);
	elsif (ie_tipo_agendamento_p = 'S') then
		CALL mult_schedule_pck.generate_schedules(cd_estabelecimento_p, cd_agendas_p, dt_agenda_inicial_p, dt_agenda_final_p, nm_usuario_p, ie_sem_atendimento_p);
	end if;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_horarios_ms (cd_estabelecimento_p estabelecimento.cd_estabelecimento%type, cd_agendas_p text, dt_agenda_inicial_p timestamp, dt_agenda_final_p timestamp, nm_usuario_p usuario.nm_usuario%type, ie_sem_atendimento_p text, ie_tipo_agendamento_p text) FROM PUBLIC;

