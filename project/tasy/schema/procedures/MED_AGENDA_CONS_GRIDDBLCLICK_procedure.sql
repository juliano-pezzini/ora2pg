-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE med_agenda_cons_griddblclick ( cd_pessoa_fisica_p text, cd_medico_p text, nm_usuario_p text, nr_seq_cliente_p INOUT bigint, qt_primeira_consulta_p INOUT bigint) AS $body$
DECLARE


nr_seq_cliente_w	bigint := 0;
qt_primeira_consulta_w	bigint := 0;


BEGIN

if (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') and (cd_medico_p IS NOT NULL AND cd_medico_p::text <> '') then
	begin
	/* Utilizado para vincular o paciente ao médico */

	CALL incluir_med_cliente(cd_pessoa_fisica_p, cd_medico_p, nm_usuario_p);

	/* Busca a sequencia do cliente e se o paciente possui primeira consulta */

	SELECT * FROM med_obter_cliente_prim_cons(cd_pessoa_fisica_p, cd_medico_p, nr_seq_cliente_w, qt_primeira_consulta_w) INTO STRICT nr_seq_cliente_w, qt_primeira_consulta_w;
	end;
end if;

nr_seq_cliente_p		:= nr_seq_cliente_w;
qt_primeira_consulta_p	:= qt_primeira_consulta_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE med_agenda_cons_griddblclick ( cd_pessoa_fisica_p text, cd_medico_p text, nm_usuario_p text, nr_seq_cliente_p INOUT bigint, qt_primeira_consulta_p INOUT bigint) FROM PUBLIC;

