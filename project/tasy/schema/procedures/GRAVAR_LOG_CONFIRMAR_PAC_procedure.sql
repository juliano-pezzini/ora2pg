-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gravar_log_confirmar_pac (ie_modo_leitura_p text, ie_tipo_etiqueta_p text, nm_usuario_p text, nr_seq_motivo_p bigint default null, ds_motivo_p text default null, nr_seq_processo_p bigint default null, nr_seq_horario_p bigint default null, nr_atendimento_p bigint default null, cd_material_p bigint default null, ie_mismatched_p text default null) AS $body$
BEGIN
if((ie_mismatched_p IS NOT NULL AND ie_mismatched_p::text <> '') and ie_mismatched_p = 'S') then

	insert into log_confirmar_paciente(nr_sequencia,
		nr_atendimento,
		dt_atualizacao,
		nm_usuario,
		ie_modo_leitura,
    ie_tipo_etiqueta,
    nr_seq_motivo,
    ds_motivo,
	nr_seq_processo,
	cd_material,
	ie_mismatched_read)
	values (nextval('log_confirmar_paciente_seq'),
		nr_atendimento_p,
		clock_timestamp(),
		nm_usuario_p,
		ie_modo_leitura_p,
    ie_tipo_etiqueta_p,
    nr_seq_motivo_p,
    ds_motivo_p,
	CASE WHEN coalesce(nr_seq_processo_p,0)=0 THEN  null  ELSE nr_seq_processo_p END ,
	cd_material_p,
	ie_mismatched_p);
else
	if	((nr_seq_processo_p IS NOT NULL AND nr_seq_processo_p::text <> '') and (coalesce(nr_atendimento_p,0) = 0)) then

		insert into log_confirmar_paciente(nr_sequencia,
			nr_seq_processo,
			dt_atualizacao,
			nm_usuario,
			ie_modo_leitura,
			 ie_tipo_etiqueta,
			 nr_seq_motivo,
			 ds_motivo
			 )
		values (nextval('log_confirmar_paciente_seq'),
			nr_seq_processo_p,
			clock_timestamp(),
			nm_usuario_p,
			ie_modo_leitura_p,
			ie_tipo_etiqueta_p,
			nr_seq_motivo_p,
			ds_motivo_p
			);

	elsif (nr_seq_horario_p IS NOT NULL AND nr_seq_horario_p::text <> '') then

		insert into log_confirmar_paciente(nr_sequencia,
			nr_seq_horario,
			dt_atualizacao,
			nm_usuario,
			ie_modo_leitura,
		ie_tipo_etiqueta,
		nr_seq_motivo,
		ds_motivo)
		values (nextval('log_confirmar_paciente_seq'),
			nr_seq_horario_p,
			clock_timestamp(),
			nm_usuario_p,
			ie_modo_leitura_p,
		ie_tipo_etiqueta_p,
		nr_seq_motivo_p,
		ds_motivo_p);

	elsif (nr_atendimento_p IS NOT NULL AND nr_atendimento_p::text <> '') then

		insert into log_confirmar_paciente(nr_sequencia,
			nr_atendimento,
			dt_atualizacao,
			nm_usuario,
			ie_modo_leitura,
		ie_tipo_etiqueta,
		nr_seq_motivo,
		ds_motivo,
		nr_seq_processo,
		cd_material)
		values (nextval('log_confirmar_paciente_seq'),
			nr_atendimento_p,
			clock_timestamp(),
			nm_usuario_p,
			ie_modo_leitura_p,
		ie_tipo_etiqueta_p,
		nr_seq_motivo_p,
		ds_motivo_p,
		CASE WHEN coalesce(nr_seq_processo_p,0)=0 THEN  null  ELSE nr_seq_processo_p END ,
		cd_material_p);

	end if;

end if;

if (coalesce(wheb_usuario_pck.get_ie_commit, 'S') = 'S') then commit; end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gravar_log_confirmar_pac (ie_modo_leitura_p text, ie_tipo_etiqueta_p text, nm_usuario_p text, nr_seq_motivo_p bigint default null, ds_motivo_p text default null, nr_seq_processo_p bigint default null, nr_seq_horario_p bigint default null, nr_atendimento_p bigint default null, cd_material_p bigint default null, ie_mismatched_p text default null) FROM PUBLIC;

