-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE sus_bpa_unif_eup_js ( cd_estabelecimento_p bigint, nr_bpa_p text, nr_seq_bpa_p bigint, nm_usuario_p text, ie_tipo_consiste_p bigint, ds_erro_p INOUT text) AS $body$
DECLARE


ds_msg_w	varchar(255);

BEGIN

if (cd_estabelecimento_p > 0) then

	ds_msg_w := sus_consiste_numero_bpa_estab(cd_estabelecimento_p, nr_bpa_p, nr_seq_bpa_p, nm_usuario_p, ie_tipo_consiste_p, ds_msg_w);

	if (ds_msg_w IS NOT NULL AND ds_msg_w::text <> '') then

		ds_erro_p := ds_msg_w || chr(10) || obter_texto_tasy(286969, wheb_usuario_pck.get_nr_seq_idioma);

	end if;
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE sus_bpa_unif_eup_js ( cd_estabelecimento_p bigint, nr_bpa_p text, nr_seq_bpa_p bigint, nm_usuario_p text, ie_tipo_consiste_p bigint, ds_erro_p INOUT text) FROM PUBLIC;

