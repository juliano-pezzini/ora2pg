-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE sup_excluir_item_solic ( nr_solic_compra_p bigint, nr_item_solic_compra_p bigint) AS $body$
BEGIN

if (coalesce(nr_solic_compra_p,0) > 0) and (coalesce(nr_item_solic_compra_p,0) > 0) then
	begin
	delete
	from	solic_compra_item
	where	nr_solic_compra = nr_solic_compra_p
	and	nr_item_solic_compra = nr_item_solic_compra_p;
	exception
	when others then
		CALL wheb_mensagem_pck.exibir_mensagem_abort(266282);
		--'Não foi possível excluir o item da solicitação');
	end;
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE sup_excluir_item_solic ( nr_solic_compra_p bigint, nr_item_solic_compra_p bigint) FROM PUBLIC;

