-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE sus_gerar_qt_nascimento ( nr_sequencia_p bigint) AS $body$
DECLARE


qt_regra_w		bigint := 0;
nr_aih_w		bigint;
cd_procedimento_w	bigint;
nr_sequencia_w		bigint;


BEGIN

begin
select	nr_aih,
	cd_procedimento,
	nr_seq_aih
into STRICT	nr_aih_w,
	cd_procedimento_w,
	nr_sequencia_w
from	procedimento_paciente
where	nr_sequencia	= nr_sequencia_p;
exception
	when others then
		CALL WHEB_MENSAGEM_PCK.Exibir_Mensagem_Abort(277782);
end;



select	aih_validar_regra(11, cd_procedimento_W)
into STRICT	qt_regra_w
;

if (qt_regra_w > 0) and (nr_aih_w IS NOT NULL AND nr_aih_w::text <> '') then
	update	sus_aih
	set	qt_nascidos_vivos	= 1,
		qt_nasc_sai_alta	= 1
	where	nr_aih 		= nr_aih_w
	and	nr_sequencia	= nr_Sequencia_w;
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE sus_gerar_qt_nascimento ( nr_sequencia_p bigint) FROM PUBLIC;

