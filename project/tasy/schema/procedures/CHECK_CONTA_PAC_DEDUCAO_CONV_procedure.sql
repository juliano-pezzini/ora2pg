-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE check_conta_pac_deducao_conv ( nr_seq_conta_pac_dc_p conta_pac_deducao_conv.nr_sequencia%type) AS $body$
DECLARE


dt_processamento_w	conta_pac_deducao_conv.dt_processamento%type;


BEGIN

begin
select	a.dt_processamento
into STRICT	dt_processamento_w
from	conta_pac_deducao_conv a
where	a.nr_sequencia = nr_seq_conta_pac_dc_p;
exception when no_data_found or too_many_rows then
	dt_processamento_w := null;
end;

if (dt_processamento_w IS NOT NULL AND dt_processamento_w::text <> '') then
	/* Este registro ja esta processado. */

	CALL wheb_mensagem_pck.exibir_mensagem_abort(1205602);
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE check_conta_pac_deducao_conv ( nr_seq_conta_pac_dc_p conta_pac_deducao_conv.nr_sequencia%type) FROM PUBLIC;

