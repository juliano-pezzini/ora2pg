-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_obter_nr_seq_complemento ( nr_seq_guia_p bigint, nm_usuario_p text, nr_seq_proc_compl_p INOUT bigint, nr_seq_mat_compl_p INOUT bigint) AS $body$
DECLARE


nr_seq_proc_compl_w	bigint;
nr_seq_mat_compl_w	bigint;


BEGIN

if (nr_seq_guia_p IS NOT NULL AND nr_seq_guia_p::text <> '') and (nm_usuario_p IS NOT NULL AND nm_usuario_p::text <> '') then
	begin

	select  count(*)
	into STRICT	nr_seq_proc_compl_w
	from    pls_guia_plano_proc
	where   (nr_seq_compl_ptu IS NOT NULL AND nr_seq_compl_ptu::text <> '')
	and     nr_seq_guia       = nr_seq_guia_p;

	select  count(*)
	into STRICT	nr_seq_mat_compl_w
	from    pls_guia_plano_mat
	where   (nr_seq_compl_ptu IS NOT NULL AND nr_seq_compl_ptu::text <> '')
	and     nr_seq_guia       = nr_seq_guia_p;

	end;
end if;

nr_seq_proc_compl_p	:= nr_seq_proc_compl_w;
nr_seq_mat_compl_p	:= nr_seq_mat_compl_w;

commit;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_obter_nr_seq_complemento ( nr_seq_guia_p bigint, nm_usuario_p text, nr_seq_proc_compl_p INOUT bigint, nr_seq_mat_compl_p INOUT bigint) FROM PUBLIC;

