-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_assinat_consent_res ( cd_pessoa_fisica_p text) AS $body$
DECLARE


nr_sequencia_w	bigint;


BEGIN

if (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') then

	select max(nr_sequencia)
	into STRICT nr_sequencia_w
	from pessoa_documentacao
	where cd_pessoa_fisica = cd_pessoa_fisica_p
	and ie_entregue = 'N'
	and (ds_arquivo IS NOT NULL AND ds_arquivo::text <> '');
	
	if (nr_sequencia_w IS NOT NULL AND nr_sequencia_w::text <> '') then
	
		update pessoa_documentacao
		set ie_entregue = 'S'
		where nr_sequencia = nr_sequencia_w;
	
		commit;
		
	end if;
	
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_assinat_consent_res ( cd_pessoa_fisica_p text) FROM PUBLIC;

