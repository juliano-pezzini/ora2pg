-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consiste_cad_pac_atendimento (cd_pessoa_fisica_p text, ds_consistencia_p INOUT text) AS $body$
DECLARE


ie_sexo_w		varchar(01);
dt_nascimento_w		timestamp;


BEGIN

ds_consistencia_p	:= '';

select	max(b.ie_sexo),
	max(b.dt_nascimento)
into STRICT	ie_sexo_w,
	dt_nascimento_w
from	pessoa_fisica b
where	b.cd_pessoa_fisica	= cd_pessoa_fisica_p;


if (coalesce(dt_nascimento_w::text, '') = '') then
	ds_consistencia_p	:= ds_consistencia_p || obter_texto_tasy(290778, wheb_usuario_pck.get_nr_seq_idioma) || chr(13) || chr(10);
end if;

if (coalesce(ie_sexo_w::text, '') = '') then
	ds_consistencia_p	:= ds_consistencia_p || obter_texto_tasy(290779, wheb_usuario_pck.get_nr_seq_idioma) || chr(13) || chr(10);
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consiste_cad_pac_atendimento (cd_pessoa_fisica_p text, ds_consistencia_p INOUT text) FROM PUBLIC;

