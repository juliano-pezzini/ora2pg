-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE baca_acerto_pls_emissao () AS $body$
DECLARE


cont_w	bigint;


BEGIN

select	count(*)
into STRICT	cont_w
from	tabela_sistema
where	nm_tabela = 'PLS_EMISSAO_CARTEIRA';

if (cont_w = 0) then
	CALL exec_sql_Dinamico('Tasy', 'drop table PLS_EMISSAO_CARTEIRA');
end if;

select	count(*)
into STRICT	cont_w
from	tabela_sistema
where	nm_tabela = 'PLS_LOTE_EMISSAO_CARTEIRA';

if (cont_w = 0) then
	CALL exec_sql_Dinamico('Tasy', 'drop table PLS_LOTE_EMISSAO_CARTEIRA');
end if;

CALL exec_sql_Dinamico('Tasy', 'drop procedure PLS_GERAR_LOTE_ETIQUETA'
);

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE baca_acerto_pls_emissao () FROM PUBLIC;

