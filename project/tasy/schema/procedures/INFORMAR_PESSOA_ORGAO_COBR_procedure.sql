-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE informar_pessoa_orgao_cobr ( nr_sequencia_p bigint, cd_pessoa_fisica_p text, cd_cgc_p text, nm_usuario_p text) AS $body$
DECLARE



nr_seq_lote_w				outros_orgao_cobr.nr_seq_lote%type;
dt_liberacao_exclusao_w		timestamp;


BEGIN

if ((coalesce(cd_pessoa_fisica_p::text, '') = '') and (coalesce(cd_cgc_p::text, '') = '')) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(684412);
end if;

select 	nr_seq_lote,
		dt_liberacao_exclusao
into STRICT	nr_seq_lote_w,
		dt_liberacao_exclusao_w
from	outros_orgao_cobr
where 	nr_sequencia = nr_sequencia_p;

if (nr_seq_lote_w IS NOT NULL AND nr_seq_lote_w::text <> '') then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(684138);
end if;

if (dt_liberacao_exclusao_w IS NOT NULL AND dt_liberacao_exclusao_w::text <> '') then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(684141);
end if;

update 	outros_orgao_cobr
set		cd_pessoa_fisica = cd_pessoa_fisica_p,
		cd_cgc = cd_cgc_p,
		dt_atualizacao = clock_timestamp(),
		nm_usuario = nm_usuario_p
where 	nr_sequencia = nr_sequencia_p;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE informar_pessoa_orgao_cobr ( nr_sequencia_p bigint, cd_pessoa_fisica_p text, cd_cgc_p text, nm_usuario_p text) FROM PUBLIC;

