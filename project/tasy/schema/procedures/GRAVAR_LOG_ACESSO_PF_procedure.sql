-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gravar_log_acesso_pf ( cd_pessoa_fisica_p text, nm_usuario_p text, ds_maquina_p text default null, cd_estabelecimento_p bigint default null, cd_funcao_p bigint default null, cd_perfil_p bigint default null) AS $body$
BEGIN

if (coalesce(cd_pessoa_fisica_p,'0') <> '0') then
	begin
	insert into pessoa_fisica_log_acesso(
				nr_sequencia,
				cd_pessoa_fisica,
				dt_atualizacao,
				nm_usuario,
				dt_atualizacao_nrec,
				nm_usuario_nrec,
				ds_maquina,
				cd_estabelecimento,
				cd_funcao,
				cd_perfil)
			values (	nextval('pessoa_fisica_log_acesso_seq'),
				cd_pessoa_fisica_p,
				clock_timestamp(),
				nm_usuario_p,
				clock_timestamp(),
				nm_usuario_p,
				coalesce(ds_maquina_p,coalesce(CASE WHEN upper(wheb_usuario_pck.get_nm_estacao)='CONSOLE' THEN wheb_usuario_pck.get_nm_maquina  ELSE wheb_usuario_pck.get_nm_estacao END ,wheb_usuario_pck.get_machine)),
				coalesce(cd_estabelecimento_p,wheb_usuario_pck.get_cd_estabelecimento),
				coalesce(cd_funcao_p,wheb_usuario_pck.get_cd_funcao),
				coalesce(cd_perfil_p,wheb_usuario_pck.get_cd_perfil));

	commit;
	end;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gravar_log_acesso_pf ( cd_pessoa_fisica_p text, nm_usuario_p text, ds_maquina_p text default null, cd_estabelecimento_p bigint default null, cd_funcao_p bigint default null, cd_perfil_p bigint default null) FROM PUBLIC;

