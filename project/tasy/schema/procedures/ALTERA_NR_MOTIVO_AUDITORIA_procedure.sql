-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE altera_nr_motivo_auditoria ( ie_tipo_dbp_p bigint, nr_seq_motivo_p bigint, nr_sequencia_p bigint, nm_usuario_p text) AS $body$
BEGIN

if (ie_tipo_dbp_p = 1) then

update	auditoria_matpaci
set	nr_seq_motivo	= nr_seq_motivo_p,
	nm_usuario	= nm_usuario_p,
	dt_atualizacao	= clock_timestamp()
where	nr_sequencia	= nr_sequencia_p;

end if;

if (ie_tipo_dbp_p = 2) then

update	auditoria_propaci
set	nr_seq_motivo	= nr_seq_motivo_p,
	nm_usuario	= nm_usuario_p,
	dt_atualizacao	= clock_timestamp()
where	nr_sequencia	= nr_sequencia_p;

end if;

if (ie_tipo_dbp_p = 3) then

update	auditoria_externa
set	nr_seq_motivo	= nr_seq_motivo_p,
	nm_usuario	= nm_usuario_p,
	dt_atualizacao	= clock_timestamp()
where	nr_sequencia	= nr_sequencia_p;

end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE altera_nr_motivo_auditoria ( ie_tipo_dbp_p bigint, nr_seq_motivo_p bigint, nr_sequencia_p bigint, nm_usuario_p text) FROM PUBLIC;

