-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE sup_iniciar_mes_estoque () AS $body$
DECLARE


dt_mesano_vigente_ajuste_w	parametro_estoque.dt_mesano_vigente%type;
dt_mesano_vigente_w			parametro_estoque.dt_mesano_vigente%type;
cd_estabelecimento_w		estabelecimento.cd_estabelecimento%type;


BEGIN
cd_estabelecimento_w := wheb_usuario_pck.get_cd_estabelecimento;

begin
	select  min(dt_mesano_referencia)
	into STRICT	dt_mesano_vigente_ajuste_w
	from    saldo_estoque
	where	cd_estabelecimento = cd_estabelecimento_w;
exception
	when others	 then
		dt_mesano_vigente_ajuste_w := clock_timestamp();
end;

if (coalesce(dt_mesano_vigente_ajuste_w::text, '') = '') then
	dt_mesano_vigente_ajuste_w := clock_timestamp();
end if;

dt_mesano_vigente_w	:= pkg_date_utils.start_of(dt_mesano_vigente_ajuste_w,'MONTH',0);

update	parametro_estoque a
set		a.dt_mesano_vigente = dt_mesano_vigente_w
where	a.cd_estabelecimento = cd_estabelecimento_w
and		not exists ( SELECT	1
					from	parametro_estoque b
					where	b.cd_estabelecimento = cd_estabelecimento_w
					and		(b.dt_mesano_vigente IS NOT NULL AND b.dt_mesano_vigente::text <> ''));
					
					
commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE sup_iniciar_mes_estoque () FROM PUBLIC;

