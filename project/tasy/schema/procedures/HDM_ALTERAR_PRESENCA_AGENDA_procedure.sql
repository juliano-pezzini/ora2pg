-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE hdm_alterar_presenca_agenda (nr_sequencia_p text, ie_selecionado_p mprev_sel_partic_agenda.ie_selecionado%type) AS $body$
DECLARE


nr_sequencia_w  MPREV_SEL_PARTIC_AGENDA.NR_SEQUENCIA%type;WITH RECURSIVE cte AS (

C01 CURSOR FOR
SELECT  trim(both regexp_substr(nr_sequencia_p, '[^,]+', 1, level))

trim(both regexp_substr(nr_sequencia_p, '[^,]+', 1, level)) is not null  UNION ALL

C01 CURSOR FOR
SELECT  trim(both regexp_substr(nr_sequencia_p, '[^,]+', 1, level)) 

trim(both regexp_substr(nr_sequencia_p, '[^,]+', 1, level)) is not null JOIN cte c ON ()

) SELECT * FROM cte;
;

BEGIN
  open C01;
	loop
	fetch C01 into	
	  nr_sequencia_w;
	EXIT WHEN NOT FOUND; /* apply on C01 */
	  begin
  update mprev_sel_partic_agenda
  set    ie_selecionado = ie_selecionado_p
  where  nr_sequencia = nr_sequencia_w;

  end;
	end loop;
	close C01;
  commit;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE hdm_alterar_presenca_agenda (nr_sequencia_p text, ie_selecionado_p mprev_sel_partic_agenda.ie_selecionado%type) FROM PUBLIC;

