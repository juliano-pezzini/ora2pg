-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gravar_motivo_alt_prot_onc ( nr_seq_paciente_p bigint, nr_seq_atendimento_p bigint, cd_material_p bigint, nr_seq_motivo_p bigint, ds_justificativa_p text, cd_profissional_p text, ie_acao_p text, nm_usuario_p text, nm_tabela_p text, nm_atributo_p text, ds_valor_ant_p text, ds_valor_novo_p text, ie_identificador_p text) AS $body$
DECLARE



ie_tipo_alteracao_w	varchar(15);
nr_seq_paciente_w	bigint := nr_seq_paciente_p;


BEGIN


if (nr_seq_atendimento_p IS NOT NULL AND nr_seq_atendimento_p::text <> '') then
	nr_seq_paciente_w := null;
end if;


if (coalesce(nr_seq_motivo_p::text, '') = '') and (coalesce(ds_justificativa_p::text, '') = '') then
	-- É necessário inserir uma justificativa, ou um motivo padrão. #@#@'
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(264501);
end if;


ie_tipo_alteracao_w	:= ie_acao_p;
insert into paciente_prot_medic_hist(		nr_sequencia,
					dt_atualizacao,
					nm_usuario,
					dt_atualizacao_nrec,
					nm_usuario_nrec,
					dt_alteracao,
					cd_profissional,
					ie_tipo_alteracao,
					nr_seq_motivo,
					ds_justificativa,
					nr_seq_paciente,
					nr_seq_atendimento,
					cd_material,
					nm_tabela,
					nm_atributo,
					ds_valor_ant,
					ds_valor_novo,
					ie_identificador)
		values (	nextval('paciente_prot_medic_hist_seq'),
					clock_timestamp(),
					nm_usuario_p,
					clock_timestamp(),
					nm_usuario_p,
					clock_timestamp(),
					cd_profissional_p,
					ie_tipo_alteracao_w,
					nr_seq_motivo_p,
					ds_justificativa_p,
					nr_seq_paciente_p,
					nr_seq_atendimento_p,
					cd_material_p,
					nm_tabela_p,
					nm_atributo_p,
					ds_valor_ant_p,
					ds_valor_novo_p,
					ie_identificador_p);

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gravar_motivo_alt_prot_onc ( nr_seq_paciente_p bigint, nr_seq_atendimento_p bigint, cd_material_p bigint, nr_seq_motivo_p bigint, ds_justificativa_p text, cd_profissional_p text, ie_acao_p text, nm_usuario_p text, nm_tabela_p text, nm_atributo_p text, ds_valor_ant_p text, ds_valor_novo_p text, ie_identificador_p text) FROM PUBLIC;

