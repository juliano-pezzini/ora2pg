-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_proposta_libera_inco_lista ( nr_seq_validacao_lista_p text, nm_usuario_p text) AS $body$
DECLARE


nr_seq_validacao_lista_w	varchar(2000);
tam_lista_w		smallint;
ie_pos_virgula_w		smallint;
nr_seq_validacao_w		bigint;


BEGIN

nr_seq_validacao_lista_w	:= nr_seq_validacao_lista_p;

while(nr_seq_validacao_lista_w IS NOT NULL AND nr_seq_validacao_lista_w::text <> '') loop
	begin
	tam_lista_w	:= length(nr_seq_validacao_lista_w);
	ie_pos_virgula_w	:= position(',' in nr_seq_validacao_lista_w);

	if (ie_pos_virgula_w <> 0) then
		begin
		nr_seq_validacao_w		:= substr(nr_seq_validacao_lista_w, 1, (ie_pos_virgula_w - 1));
		nr_seq_validacao_lista_w	:= substr(nr_seq_validacao_lista_w, (ie_pos_virgula_w + 1), tam_lista_w);

		CALL pls_proposta_liberar_inco(
			nr_seq_validacao_w,
			nm_usuario_p);
		end;
	end if;
	end;
end loop;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_proposta_libera_inco_lista ( nr_seq_validacao_lista_p text, nm_usuario_p text) FROM PUBLIC;

