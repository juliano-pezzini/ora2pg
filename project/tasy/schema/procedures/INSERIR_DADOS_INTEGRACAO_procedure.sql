-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE inserir_dados_integracao (nr_prescricao_p bigint, cd_material_p bigint, cd_api_p text, ds_resultado_curto_p text, nr_gravidade_p bigint, nr_seq_cpoe_p bigint default null) AS $body$
DECLARE


nr_sequencia_w		bigint;


BEGIN

if	((nr_prescricao_p IS NOT NULL AND nr_prescricao_p::text <> '' AND nr_prescricao_p > 0) or (coalesce(nr_seq_cpoe_p,0) > 0)) then
	
	select	nextval('alerta_api_seq')
	into STRICT	nr_sequencia_w
	;
		
	insert into alerta_api(
		nr_sequencia,		
		nr_prescricao,
		cd_material,
		cd_api,
		ds_resultado_curto,		
		nr_gravidade,
		nr_seq_cpoe)
	values (	
		nr_sequencia_w,		
		nr_prescricao_p,
		CASE WHEN cd_material_p=0 THEN null  ELSE cd_material_p END ,
		cd_api_p,
		ds_resultado_curto_p,		
		nr_gravidade_p,
		nr_seq_cpoe_p);
		
	commit;

end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE inserir_dados_integracao (nr_prescricao_p bigint, cd_material_p bigint, cd_api_p text, ds_resultado_curto_p text, nr_gravidade_p bigint, nr_seq_cpoe_p bigint default null) FROM PUBLIC;

