-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE cm_gerar_devolucao_conjuntos ( nr_seq_conjunto_p bigint, nm_usuario_p text) AS $body$
DECLARE


nr_seq_requisicao_w		bigint;
nr_seq_item_req_w		bigint;
nr_seq_item_atend_w		bigint;


BEGIN

select	max(nr_sequencia),
	max(nr_seq_item_req)
into STRICT	nr_seq_item_atend_w,
	nr_seq_item_req_w
from	cm_requisicao_conj
where	nr_seq_conj_real = nr_seq_conjunto_p;

select	max(nr_seq_requisicao)
into STRICT	nr_seq_requisicao_w
from	cm_requisicao_item
where	nr_sequencia = nr_seq_item_req_w;

if (coalesce(nr_seq_item_atend_w,0) > 0) and (coalesce(nr_seq_item_req_w,0) > 0) and (coalesce(nr_seq_requisicao_w,0) > 0) then
	CALL cme_devolver_item_requisicao(nr_seq_requisicao_w,nr_seq_item_req_w,nr_seq_item_atend_w,'N',nm_usuario_p);
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE cm_gerar_devolucao_conjuntos ( nr_seq_conjunto_p bigint, nm_usuario_p text) FROM PUBLIC;

