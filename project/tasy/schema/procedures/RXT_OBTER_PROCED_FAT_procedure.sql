-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE rxt_obter_proced_fat ( cd_convenio_p bigint, ie_tipo_lancamento_p text, nr_seq_proc_interno_p INOUT bigint, cd_procedimento_p INOUT bigint, ie_origem_proced_p INOUT bigint, nm_usuario_p text) AS $body$
DECLARE


nr_seq_proc_interno_w	bigint;
cd_procedimento_w	bigint;
ie_origem_proced_w	bigint;

C01 CURSOR FOR
	SELECT	nr_Seq_proc_interno,
		cd_procedimento,
		ie_origem_proced
	from	rxt_proced_faturamento
	where	coalesce(cd_convenio, cd_convenio_p)	= cd_convenio_p
	and	ie_tipo_lancamento		= ie_tipo_lancamento_p
	and	ie_situacao			= 'A'
	order by coalesce(cd_convenio,0);


BEGIN

open C01;
loop
fetch C01 into
	nr_seq_proc_interno_w,
	cd_procedimento_w,
	ie_origem_proced_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin
	nr_seq_proc_interno_w	:= nr_seq_proc_interno_w;
	cd_procedimento_w	:= cd_procedimento_w;
	ie_origem_proced_w	:= ie_origem_proced_w;
	end;
end loop;
close C01;

nr_seq_proc_interno_p	:= nr_seq_proc_interno_w;
cd_procedimento_p	:= cd_procedimento_w;
ie_origem_proced_p	:= ie_origem_proced_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE rxt_obter_proced_fat ( cd_convenio_p bigint, ie_tipo_lancamento_p text, nr_seq_proc_interno_p INOUT bigint, cd_procedimento_p INOUT bigint, ie_origem_proced_p INOUT bigint, nm_usuario_p text) FROM PUBLIC;

