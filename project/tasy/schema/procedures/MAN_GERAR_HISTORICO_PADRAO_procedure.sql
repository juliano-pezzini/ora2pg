-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE man_gerar_historico_padrao ( cd_estabelecimento_p bigint, nr_seq_ordem_serv_p bigint, nr_seq_tipo_hist_p bigint, nm_usuario_p text, ie_origem_p text default 'I') AS $body$
DECLARE


ds_historico_w          man_tipo_hist_hist_padrao.ds_historico%type;


BEGIN

begin
        select 	ds_historico
        into STRICT	ds_historico_w
        from   	man_tipo_hist_hist_padrao
        where  	nr_seq_tipo_hist = nr_seq_tipo_hist_p
        and (cd_estabelecimento = cd_estabelecimento_p or coalesce(cd_estabelecimento::text, '') = '')  LIMIT 1;

        if (ds_historico_w IS NOT NULL AND ds_historico_w::text <> '') then
                CALL man_gravar_historico_ordem(nr_seq_ordem_serv_p, clock_timestamp(), ds_historico_w, ie_origem_p, nr_seq_tipo_hist_p, nm_usuario_p);
        end if;
exception
        when no_data_found then ds_historico_w := null;
        when too_many_rows then ds_historico_w := null;
end;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE man_gerar_historico_padrao ( cd_estabelecimento_p bigint, nr_seq_ordem_serv_p bigint, nr_seq_tipo_hist_p bigint, nm_usuario_p text, ie_origem_p text default 'I') FROM PUBLIC;

