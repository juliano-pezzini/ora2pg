-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_checkup_apos_cancel ( nr_seq_checkup_p bigint, cd_estabelecimento_p bigint, cd_setor_atendimento_p bigint, nm_usuario_p text) AS $body$
DECLARE


dt_cancelamento_w	timestamp;
dt_previsto_w		timestamp;
nr_sequencia_w		bigint;
ie_encaixe_w		varchar(1);

BEGIN

if (nr_seq_checkup_p IS NOT NULL AND nr_seq_checkup_p::text <> '') then
/*	(cd_estabelecimento_p is not null) and
	(cd_setor_atendimento_p is not null) then*/
	begin

	select 	max(a.dt_cancelamento),
		max(a.dt_previsto),
		coalesce(max(a.ie_encaixe),'N')
	into STRICT	dt_cancelamento_w,
		dt_previsto_w,
		ie_encaixe_w
	from 	checkup a
	where	a.nr_sequencia = nr_seq_checkup_p;
/*
	if	(ie_encaixe_w = 'N') then
		begin
		select checkup_seq.nextval
		into   nr_sequencia_w
		from   dual;

		insert into checkup
				(nr_sequencia,
				cd_setor_atendimento,
				cd_estabelecimento,
				dt_atualizacao,
				dt_atualizacao_nrec,
				nm_usuario,
				nm_usuario_nrec,
				dt_previsto)
			values
				(nr_sequencia_w,
				cd_setor_atendimento_p,
				cd_estabelecimento_p,
				sysdate,
				sysdate,
				nm_usuario_p,
				nm_usuario_p,
				dt_previsto_w);
		commit;
		end;
	end if;*/
	CALL gerar_hr_agenda_ck(dt_previsto_w,cd_setor_atendimento_p,cd_estabelecimento_p,nm_usuario_P);

	end;
end if;
commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_checkup_apos_cancel ( nr_seq_checkup_p bigint, cd_estabelecimento_p bigint, cd_setor_atendimento_p bigint, nm_usuario_p text) FROM PUBLIC;

