-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE desfazer_same_agrupamento_js ( nr_sequencia_p bigint, nm_usuario_p text) AS $body$
DECLARE


cd_pessoa_fisica_w	same_prontuario.cd_pessoa_fisica%type;

BEGIN

update 	same_prontuario
set 	nr_seq_agrup  = NULL,
	ie_status = 1,
	dt_atualizacao = clock_timestamp(),
	nm_usuario = nm_usuario_p
where 	nr_sequencia = nr_sequencia_p;

select	max(cd_pessoa_fisica)
into STRICT	cd_pessoa_fisica_w
from	same_prontuario
where	nr_sequencia = nr_sequencia_p;

if (cd_pessoa_fisica_w IS NOT NULL AND cd_pessoa_fisica_w::text <> '') then
	CALL same_ordernar_volume_pront(cd_pessoa_fisica_w,0,nm_usuario_p,wheb_usuario_pck.get_cd_estabelecimento);
end if;

CALL Same_Gerar_Historico(nr_sequencia_p,40,null,null,nm_usuario_p);

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE desfazer_same_agrupamento_js ( nr_sequencia_p bigint, nm_usuario_p text) FROM PUBLIC;

