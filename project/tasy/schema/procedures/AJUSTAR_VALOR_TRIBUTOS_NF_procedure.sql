-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE ajustar_valor_tributos_nf ( nr_seq_nf_p bigint, nr_item_nf_p bigint, cd_tributo_p bigint, vl_ajuste_p bigint, ie_tipo_alt_p bigint, dt_venc_tributo timestamp default null) AS $body$
DECLARE

/* ie_tipo_alt_p =
1 = nota_fiscal_item_trib
2 = nota_fiscal_trib
3 = nota_fiscal_venc_trib
*/
vl_tributo_w			double precision;
vl_base_calculo_w		double precision;
tx_tributo_w			double precision;


BEGIN
if (nr_seq_nf_p > 0) then
	if (ie_tipo_alt_p = 1) and (nr_item_nf_p > 0) then

		select	vl_base_calculo,
			vl_tributo
		into STRICT	vl_base_calculo_w,
			vl_tributo_w
		from	nota_fiscal_item_trib
		where	nr_sequencia = nr_seq_nf_p
		and	nr_item_nf = nr_item_nf_p
		and	cd_tributo = cd_tributo_p;

		vl_tributo_w	:= (vl_tributo_w + vl_ajuste_p);
		tx_tributo_w 	:= (vl_tributo_w * 100) / vl_base_calculo_w;

		update	nota_fiscal_item_trib
		set	vl_tributo = vl_tributo_w,
			tx_tributo = tx_tributo_w
		where	nr_sequencia = nr_seq_nf_p
		and	nr_item_nf = nr_item_nf_p
		and	cd_tributo = cd_tributo_p;

	elsif (ie_tipo_alt_p = 2) then
		
		select	vl_base_calculo,
			vl_tributo
		into STRICT	vl_base_calculo_w,
			vl_tributo_w
		from	nota_fiscal_trib
		where	nr_sequencia = nr_seq_nf_p
		and	cd_tributo = cd_tributo_p;

		vl_tributo_w	:= (vl_tributo_w + vl_ajuste_p);
		tx_tributo_w 	:= (vl_tributo_w * 100) / vl_base_calculo_w;

		update	nota_fiscal_trib
		set	vl_tributo = vl_tributo_w,
			tx_tributo = tx_tributo_w
		where	nr_sequencia = nr_seq_nf_p
		and	cd_tributo = cd_tributo_p;
		
	elsif (ie_tipo_alt_p = 3) then

		select	vl_base_calculo,
			vl_tributo
		into STRICT	vl_base_calculo_w,
			vl_tributo_w
		from	nota_fiscal_venc_trib
		where	nr_sequencia = nr_seq_nf_p
		and	cd_tributo = cd_tributo_p
	               and           dt_vencimento = dt_venc_tributo;

		vl_tributo_w	:= (vl_tributo_w + vl_ajuste_p);
		tx_tributo_w 	:= (vl_tributo_w * 100) / vl_base_calculo_w;

		update	nota_fiscal_venc_trib
		set	vl_tributo = vl_tributo_w,
			tx_tributo = tx_tributo_w
		where	nr_sequencia = nr_seq_nf_p
		and	cd_tributo = cd_tributo_p
     		and 	dt_vencimento = dt_venc_tributo;

	end if;
end if;
commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE ajustar_valor_tributos_nf ( nr_seq_nf_p bigint, nr_item_nf_p bigint, cd_tributo_p bigint, vl_ajuste_p bigint, ie_tipo_alt_p bigint, dt_venc_tributo timestamp default null) FROM PUBLIC;

