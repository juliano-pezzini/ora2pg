-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE obter_dados_triagem_senha ( nr_seq_pac_senha_fila_p bigint, ie_clinica_p INOUT bigint, nr_seq_queixa_p INOUT bigint, ds_sintoma_paciente_p INOUT text) AS $body$
DECLARE


qtd_registros_w		numeric(20);


BEGIN

select	count(*)
into STRICT	qtd_registros_w
from 	triagem_pronto_atend
where 	nr_seq_fila_senha = nr_seq_pac_senha_fila_p;

if (qtd_registros_w > 0) then
	select	max(ie_clinica),
		max(nr_seq_queixa),
		max(ds_queixa_princ)
	into STRICT	ie_clinica_p,
		nr_seq_queixa_p,
		ds_sintoma_paciente_p
	from 	triagem_pronto_atend
	where 	nr_seq_fila_senha = nr_seq_pac_senha_fila_p;
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE obter_dados_triagem_senha ( nr_seq_pac_senha_fila_p bigint, ie_clinica_p INOUT bigint, nr_seq_queixa_p INOUT bigint, ds_sintoma_paciente_p INOUT text) FROM PUBLIC;

