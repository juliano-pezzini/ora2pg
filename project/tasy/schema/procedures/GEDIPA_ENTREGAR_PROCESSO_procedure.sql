-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gedipa_entregar_processo ( nr_atendimento_p bigint, nr_processo_p bigint, nr_processo_gedipa_p bigint, nm_usuario_p text) AS $body$
DECLARE

		 
ie_valido_w	varchar(1);


BEGIN 
if (nr_atendimento_p IS NOT NULL AND nr_atendimento_p::text <> '') and (nr_processo_p IS NOT NULL AND nr_processo_p::text <> '') and (nr_processo_gedipa_p IS NOT NULL AND nr_processo_gedipa_p::text <> '') then 
	begin 
	if (nr_processo_gedipa_p = nr_processo_p) then 
		begin 
		ie_valido_w := validar_processo_atendimento(nr_atendimento_p, nr_processo_p);
		if (ie_valido_w = 'S') then 
			begin			 
			CALL atualiza_status_processo_adep(nr_processo_p, null, 'G', 'E', clock_timestamp(), nm_usuario_p);
			end;
		else 
			begin 
			--O processo e o atendimento são incompatíveis, favor verificar!#@#@'); 
			CALL Wheb_mensagem_pck.exibir_mensagem_abort(261616);
			end;
		end if;
		end;
	else 
		begin 
		--O processo selecionado inicialmente e o processo informado para entrega são diferentes, favor verificar!#@#@'); 
		CALL Wheb_mensagem_pck.exibir_mensagem_abort(261617);
		end;
	end if;
	end;	
end if;
commit;	
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gedipa_entregar_processo ( nr_atendimento_p bigint, nr_processo_p bigint, nr_processo_gedipa_p bigint, nm_usuario_p text) FROM PUBLIC;

