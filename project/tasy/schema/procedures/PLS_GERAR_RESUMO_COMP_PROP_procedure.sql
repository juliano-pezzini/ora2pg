-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_gerar_resumo_comp_prop (nr_seq_proposta_p bigint, nm_usuario_p text) AS $body$
DECLARE

 
				 nr_seq_plano_w			bigint;
nr_seq_sca_w			bigint;				
nr_seq_beneficiario_w		bigint;
nr_seq_bonificacao_prop_w	bigint;
nr_seq_bonif_benef_w		bigint;
nr_seq_tx_insc_w		bigint;
nr_seq_tx_insc_plano_w		bigint;

c01 CURSOR FOR 
	SELECT	nr_sequencia 
	from	pls_proposta_beneficiario	 
	where	nr_seq_proposta = nr_seq_proposta_p;
	
C02 CURSOR FOR 
	SELECT	nr_sequencia 
	from	pls_sca_vinculo 
	where	nr_seq_benef_proposta = nr_seq_beneficiario_w;
	
c03 CURSOR FOR 
	SELECT	nr_sequencia 
	from	pls_bonificacao_vinculo 
	where	nr_seq_proposta = nr_seq_proposta_p;
	
c04 CURSOR FOR 
	SELECT	nr_sequencia 
	from	pls_bonificacao_vinculo 
	where	nr_seq_segurado_prop = nr_seq_beneficiario_w;
	
C05 CURSOR FOR 
	SELECT	nr_sequencia 
	from	pls_regra_inscricao 
	where	nr_seq_proposta = nr_seq_proposta_p;

C06 CURSOR FOR 
	SELECT	nr_sequencia 
	from	pls_regra_inscricao 
	where	nr_seq_plano = nr_seq_plano_w;
	

BEGIN 
open C01;
loop 
fetch C01 into	 
	nr_seq_beneficiario_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin		 
	 
	select	nr_seq_plano 
	into STRICT	nr_seq_plano_w 
	from	pls_proposta_beneficiario	 
	where	nr_sequencia = nr_seq_beneficiario_w;
	 
	CALL pls_gerar_resumo_proposta(nr_seq_proposta_p, nr_seq_beneficiario_w, 'B', 
				 nr_seq_plano_w, nm_usuario_p);
	 
	 
	open C02;
	loop 
	fetch C02 into	 
		nr_seq_sca_w;
	EXIT WHEN NOT FOUND; /* apply on C02 */
		begin 
		CALL pls_gerar_resumo_proposta(nr_seq_proposta_p, nr_seq_beneficiario_w, 'S', 
					 nr_seq_sca_w, nm_usuario_p);	
		end;
	end loop;
	close C02;
	 
	open C03;
	loop 
	fetch C03 into	 
		nr_seq_bonificacao_prop_w;
	EXIT WHEN NOT FOUND; /* apply on C03 */
		begin 
		CALL pls_gerar_resumo_proposta(nr_seq_proposta_p, nr_seq_beneficiario_w, 'BP', 
					 nr_seq_bonificacao_prop_w, nm_usuario_p);	
		end;
	end loop;
	close C03;
	 
	open C04;
	loop 
	fetch C04 into	 
		nr_seq_bonif_benef_w;
	EXIT WHEN NOT FOUND; /* apply on C04 */
		begin 
		CALL pls_gerar_resumo_proposta(nr_seq_proposta_p, nr_seq_beneficiario_w, 'BO', 
					 nr_seq_bonif_benef_w, nm_usuario_p);		
		end;
	end loop;
	close C04;		
	 
	open C05;
	loop 
	fetch C05 into	 
		nr_seq_tx_insc_w;
	EXIT WHEN NOT FOUND; /* apply on C05 */
		begin 
		CALL pls_gerar_resumo_proposta(nr_seq_proposta_p, nr_seq_beneficiario_w, 'I', 
					 nr_seq_tx_insc_w, nm_usuario_p);											
		end;
	end loop;
	close C05;		
	 
	 
	open C06;
	loop 
	fetch C06 into	 
		nr_seq_tx_insc_plano_w;
	EXIT WHEN NOT FOUND; /* apply on C06 */
		begin 
			CALL pls_gerar_resumo_proposta(nr_seq_proposta_p, nr_seq_beneficiario_w, 'IP', 
					 nr_seq_tx_insc_plano_w, nm_usuario_p);	
		end;
	end loop;
	close C06;
 
 
	end;
end loop;
close C01;
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_gerar_resumo_comp_prop (nr_seq_proposta_p bigint, nm_usuario_p text) FROM PUBLIC;

