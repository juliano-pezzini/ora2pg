-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_fluxo_caixa_lote_filtro ( nr_seq_lote_fluxo_p bigint, cd_banco_p bigint, nr_seq_proj_rec_p bigint, nr_seq_caixa_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) AS $body$
DECLARE


/*--------------------------------------------------------------- ATENÇÃO ----------------------------------------------------------------*/

/* Cuidado ao realizar alterações no fluxo de caixa em lote. Toda e qualquer alteração realizada em qualquer uma */

/* das procedures do fluxo de caixa em lote deve ser cuidadosamente verificada e realizada no fluxo de caixa      */

/* convencional. Devemos garantir que os dois fluxos de caixa tragam os mesmos valores no resultado, evitando     */

/* assim que existam diferenças entre os fluxos de caixa.                                                                                                */

/*--------------- AO ALTERAR O FLUXO DE CAIXA EM LOTE ALTERAR TAMBÉM O FLUXO DE CAIXA ---------------*/

BEGIN

if (coalesce(cd_banco_p,0) <> 0) then

	insert into fluxo_caixa_lote_filtro(nr_sequencia,
		dt_atualizacao,
		dt_atualizacao_nrec,
		nm_usuario,
		nm_usuario_nrec,
		cd_banco,
		nr_seq_lote_fluxo)
	VALUES (nextval('fluxo_caixa_lote_filtro_seq'),
		clock_timestamp(),
		clock_timestamp(),
		nm_usuario_p,
		nm_usuario_p,
		cd_banco_p,
		nr_seq_lote_fluxo_p);

end if;

if (coalesce(nr_seq_proj_rec_p,0) <> 0) then

	insert into fluxo_caixa_lote_filtro(nr_sequencia,
		dt_atualizacao,
		dt_atualizacao_nrec,
		nm_usuario,
		nm_usuario_nrec,
		nr_seq_proj_rec,
		nr_seq_lote_fluxo)
	VALUES (nextval('fluxo_caixa_lote_filtro_seq'),
		clock_timestamp(),
		clock_timestamp(),
		nm_usuario_p,
		nm_usuario_p,
		nr_seq_proj_rec_p,
		nr_seq_lote_fluxo_p);

end if;

if (coalesce(nr_seq_caixa_p,0) <> 0) then

	insert into fluxo_caixa_lote_filtro(nr_sequencia,
		dt_atualizacao,
		dt_atualizacao_nrec,
		nm_usuario,
		nm_usuario_nrec,
		nr_seq_caixa,
		nr_seq_lote_fluxo)
	VALUES (nextval('fluxo_caixa_lote_filtro_seq'),
		clock_timestamp(),
		clock_timestamp(),
		nm_usuario_p,
		nm_usuario_p,
		nr_seq_caixa_p,
		nr_seq_lote_fluxo_p);

end if;

if (coalesce(cd_estabelecimento_p,0) <> 0) then

	insert into fluxo_caixa_lote_filtro(nr_sequencia,
		dt_atualizacao,
		dt_atualizacao_nrec,
		nm_usuario,
		nm_usuario_nrec,
		cd_estabelecimento,
		nr_seq_lote_fluxo)
	VALUES (nextval('fluxo_caixa_lote_filtro_seq'),
		clock_timestamp(),
		clock_timestamp(),
		nm_usuario_p,
		nm_usuario_p,
		cd_estabelecimento_p,
		nr_seq_lote_fluxo_p);

end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_fluxo_caixa_lote_filtro ( nr_seq_lote_fluxo_p bigint, cd_banco_p bigint, nr_seq_proj_rec_p bigint, nr_seq_caixa_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) FROM PUBLIC;

