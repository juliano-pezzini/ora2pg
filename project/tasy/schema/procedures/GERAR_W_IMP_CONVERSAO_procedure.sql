-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_w_imp_conversao (nr_seq_nota_p bigint, nr_seq_item_p bigint, nm_atributo_p text, cd_interno_p text, nm_usuario_p text, ie_commit_p text) AS $body$
BEGIN

if (nr_seq_nota_p IS NOT NULL AND nr_seq_nota_p::text <> '') or (nr_seq_item_p IS NOT NULL AND nr_seq_item_p::text <> '') then

	insert	into w_imp_conversao(cd_interno,
		nm_atributo,
		dt_atualizacao,
		nm_usuario,
		nr_seq_item,
		nr_seq_nota)
	values (cd_interno_p,
		upper(nm_atributo_p),
		clock_timestamp(),
		nm_usuario_p,
		nr_seq_item_p,
		nr_seq_nota_p);

end if;

if (coalesce(ie_commit_p,'S')	= 'S') then

	commit;

end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_w_imp_conversao (nr_seq_nota_p bigint, nr_seq_item_p bigint, nm_atributo_p text, cd_interno_p text, nm_usuario_p text, ie_commit_p text) FROM PUBLIC;

