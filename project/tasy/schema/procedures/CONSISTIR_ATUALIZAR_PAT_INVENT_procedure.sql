-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consistir_atualizar_pat_invent ( nr_seq_inventario_p bigint, qt_registros_p INOUT bigint, nm_usuario_p text) AS $body$
DECLARE


qt_registros_w	bigint;

BEGIN
if (nm_usuario_p IS NOT NULL AND nm_usuario_p::text <> '') then
	begin

	if (nr_seq_inventario_p IS NOT NULL AND nr_seq_inventario_p::text <> '') then
		begin
		consistir_pat_inventario(nr_seq_inventario_p, nm_usuario_p);
		end;
	end if;

	select	count(*)
	into STRICT	qt_registros_w
	from	pat_inventario_bem
	where	(ds_consistencia IS NOT NULL AND ds_consistencia::text <> '')
	and	nr_seq_inventario = nr_seq_inventario_p;

	end;
end if;
qt_registros_p	:= qt_registros_w;
commit;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consistir_atualizar_pat_invent ( nr_seq_inventario_p bigint, qt_registros_p INOUT bigint, nm_usuario_p text) FROM PUBLIC;

