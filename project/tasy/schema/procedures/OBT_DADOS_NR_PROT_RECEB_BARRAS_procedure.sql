-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE obt_dados_nr_prot_receb_barras ( nr_atendimento_p bigint, cd_setor_atendimento_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint) AS $body$
DECLARE


cd_recebimento_w		bigint;
cd_setor_destino_w		integer;
ie_cont_transf_solic_w		varchar(1);
ie_restringe_setor_receb_w	varchar(1);



BEGIN

ie_cont_transf_solic_w := obter_param_usuario(941, 53, obter_perfil_ativo, nm_usuario_p, cd_estabelecimento_p, ie_cont_transf_solic_w);
ie_restringe_setor_receb_w := obter_param_usuario(941, 93, obter_perfil_ativo, nm_usuario_p, cd_estabelecimento_p, ie_restringe_setor_receb_w);


select	max(a.nr_sequencia)
into STRICT	cd_recebimento_w
from	transf_prontuario a
where	a.nr_atendimento = nr_atendimento_p;

if (ie_cont_transf_solic_w = 'S') and (coalesce(cd_recebimento_w::text, '') = '') then
	begin
	--(-20011, substr(obter_texto_tasy (67885, wheb_usuario_pck.get_nr_seq_idioma),1,255) || '#@#@');
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(67885);
	end;
end if;

select 	cd_setor_destino
into STRICT	cd_setor_destino_w
from 	transf_prontuario
where  	nr_sequencia = cd_recebimento_w;

if (ie_restringe_setor_receb_w = 'S') and (cd_setor_destino_w = cd_setor_atendimento_p) and (coalesce(cd_setor_destino_w::text, '') = '') then
	begin

	--(-20011, substr(obter_texto_tasy (67893, wheb_usuario_pck.get_nr_seq_idioma),1,255) || '#@#@');
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(67893);
	end;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE obt_dados_nr_prot_receb_barras ( nr_atendimento_p bigint, cd_setor_atendimento_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint) FROM PUBLIC;

