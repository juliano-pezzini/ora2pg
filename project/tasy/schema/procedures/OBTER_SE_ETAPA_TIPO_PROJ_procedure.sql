-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE obter_se_etapa_tipo_proj (nr_seq_etapa_p bigint, nr_seq_tipo_proj_p bigint) AS $body$
DECLARE


ds_retorno_w	varchar(1);


BEGIN

select	CASE WHEN count(*)=0 THEN 'N'  ELSE 'S' END
into STRICT	ds_retorno_w
from	prp_necessidade_etapa
where	nr_seq_etapa_processo  = nr_seq_etapa_p
and		nr_seq_tipo_projeto = nr_seq_tipo_proj_p;

if (ds_retorno_w = 'N') then
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(308145);
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE obter_se_etapa_tipo_proj (nr_seq_etapa_p bigint, nr_seq_tipo_proj_p bigint) FROM PUBLIC;

