-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE ptu_inserir_inconsistencia_mat ( nr_seq_material_p bigint, nr_seq_inconsistencia_p bigint, nm_usuario_p text) AS $body$
DECLARE


/*Finalidade: Verificar se deve realizar a consistencia no A400/450
-------------------------------------------------------------------------------------------------------------------
Locais de chamada direta:
[ X]  Objetos do dicionário [  ] Tasy (Delphi/Java) [  ] Portal [  ]  Relatórios [ ] Outros:
 ------------------------------------------------------------------------------------------------------------------
Pontos de atenção:
*/
ds_inconsistencia_w	varchar(255);


BEGIN
select	substr(max(ds_inconsistencia),1,255)
into STRICT	ds_inconsistencia_w
from	ptu_inconsistencia_mat
where	nr_sequencia = nr_seq_inconsistencia_p;

if (nr_seq_inconsistencia_p IS NOT NULL AND nr_seq_inconsistencia_p::text <> '') and (nr_seq_material_p IS NOT NULL AND nr_seq_material_p::text <> '') then
	insert into pls_mat_unimed_inc(nr_sequencia,
		nm_usuario,
		dt_atualizacao,
		nm_usuario_nrec,
		dt_atualizacao_nrec,
		ds_inconsistencia,
		ie_tipo_inconsistencia,
		nr_seq_material)
	values (nextval('pls_mat_unimed_inc_seq'),
		nm_usuario_p,
		clock_timestamp(),
		nm_usuario_p,
		clock_timestamp(),
		ds_inconsistencia_w,
		'E',
		nr_seq_material_p);
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE ptu_inserir_inconsistencia_mat ( nr_seq_material_p bigint, nr_seq_inconsistencia_p bigint, nm_usuario_p text) FROM PUBLIC;

