-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE calcular_ir_titulo (nr_titulo_p bigint) AS $body$
DECLARE


cd_pessoa_fisica_w	varchar(10);
cd_cgc_w			varchar(14);
dt_emissao_w		timestamp;
vl_titulo_w			double precision;

vl_tit_mes_w		double precision;
vl_ir_mes_w			double precision;

vl_ir_w			double precision;


BEGIN

vl_ir_w	:= vl_ir_w;
/*	Edgar 11/02/2005 OS 15030, comentei esta procedure, deverá ser usada a nova rotina

select	cd_pessoa_fisica,
		cd_cgc,
		dt_emissao,
		vl_titulo
into	cd_pessoa_fisica_w,
	cd_cgc_w,
	dt_emissao_w,
	vl_titulo_w
from titulo_pagar
where nr_titulo = nr_titulo_p;

if (cd_pessoa_fisica_w is not null) then
	select nvl(sum(vl_titulo),0),
		 nvl(sum(vl_ir),0)
	into	vl_tit_mes_w,
		vl_ir_mes_w
	from titulo_pagar
	where cd_pessoa_fisica = cd_pessoa_fisica_w
	  and trunc(dt_emissao,'MONTH') = trunc(dt_emissao_w,'MONTH')
	  and nr_titulo <> nr_titulo_p;

	vl_ir_w := calcular_irpf(vl_tit_mes_w + vl_titulo_w);

	update titulo_pagar
	set vl_ir = (vl_ir_w - vl_ir_mes_w)
	where nr_titulo = nr_titulo_p;
end if;

commit;
*/
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE calcular_ir_titulo (nr_titulo_p bigint) FROM PUBLIC;

