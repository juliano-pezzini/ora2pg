-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';



--
-- dblink wrapper to call function gerar_evento_est_atend as an autonomous transaction
--
CREATE EXTENSION IF NOT EXISTS dblink;

CREATE OR REPLACE PROCEDURE gerar_evento_est_atend ( nm_usuario_p text, cd_estabelecimento_p bigint, ie_opcao_p text, nr_seq_unidade_p bigint, ie_status_p text default null) AS $body$
DECLARE
	-- Change this to reflect the dblink connection string
	v_conn_str  text := format('port=%s dbname=%s user=%s', current_setting('port'), current_database(), current_user);
	v_query     text;

BEGIN
	v_query := 'CALL gerar_evento_est_atend_atx ( ' || quote_nullable(nm_usuario_p) || ',' || quote_nullable(cd_estabelecimento_p) || ',' || quote_nullable(ie_opcao_p) || ',' || quote_nullable(nr_seq_unidade_p) || ',' || quote_nullable(ie_status_p) || ' )';
	PERFORM * FROM dblink(v_conn_str, v_query) AS p (ret boolean);

END;
$body$ LANGUAGE plpgsql SECURITY DEFINER;




CREATE OR REPLACE PROCEDURE gerar_evento_est_atend_atx ( nm_usuario_p text, cd_estabelecimento_p bigint, ie_opcao_p text, nr_seq_unidade_p bigint, ie_status_p text default null) AS $body$
DECLARE

 
nr_seq_evento_w		bigint;
nr_sequencia_w		bigint;c01 CURSOR FOR
	SELECT	nr_seq_evento 
	from	regra_envio_sms 
	where	cd_estabelecimento	= cd_estabelecimento_p 
	and	ie_evento_disp		= ie_opcao_p 
	and	coalesce(ie_situacao,'A') = 'A';


BEGIN 
 
	open c01;
	loop 
	fetch c01 into 
		nr_seq_evento_w;
	EXIT WHEN NOT FOUND; /* apply on c01 */
		CALL gerar_evento_paciente(nr_seq_evento_w,null,null,null,nm_usuario_p,null,null,null,null,null,null,null,null,null,null,null,nr_seq_unidade_p);
	end loop;
	close c01;
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_evento_est_atend ( nm_usuario_p text, cd_estabelecimento_p bigint, ie_opcao_p text, nr_seq_unidade_p bigint, ie_status_p text default null) FROM PUBLIC; -- REVOKE ALL ON PROCEDURE gerar_evento_est_atend_atx ( nm_usuario_p text, cd_estabelecimento_p bigint, ie_opcao_p text, nr_seq_unidade_p bigint, ie_status_p text default null) FROM PUBLIC;

