-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE atualiza_internacao_radio ( nr_atendimento_p bigint, nm_usuario_p text) AS $body$
DECLARE


nr_seq_interno_w		bigint;


BEGIN

select	max(nr_seq_interno)
into STRICT	nr_seq_interno_w
from	atend_paciente_unidade
where	nr_atendimento = nr_atendimento_p;

update  atend_paciente_unidade
set	ie_radiacao = 'S',
	nm_usuario = nm_usuario_p,
	dt_atualizacao = clock_timestamp()
where	nr_seq_interno = nr_seq_interno_w
and	nr_atendimento = nr_atendimento_p;

if (coalesce(wheb_usuario_pck.get_ie_commit, 'S') = 'S') then commit; end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE atualiza_internacao_radio ( nr_atendimento_p bigint, nm_usuario_p text) FROM PUBLIC;

