-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_log_conpaci_etapa ( nr_sequencia_p bigint, nr_interno_conta_p bigint, nm_usuario_p text) AS $body$
BEGIN

insert into log_conpaci_etapa(
	nr_sequencia,
	nr_interno_conta,
	dt_atualizacao,
	nm_usuario,
	dt_atualizacao_nrec,
	nm_usuario_nrec,
	dt_etapa,
	nr_seq_etapa,
	cd_setor_atendimento,
	cd_pessoa_fisica,
	dt_fim_etapa,
	nr_seq_motivo_dev,
	ds_observacao,
	nr_lote_barras,
	cd_pessoa_exec,
	dt_recebimento,
	vl_conta,
	nr_seq_protocolo,
	ie_etapa_protocolo,
	ie_etapa_critica,
	dt_periodo_inicial,
	dt_periodo_final,
	nr_seq_conta_origem)
SELECT	nextval('log_conpaci_etapa_seq'),
	nr_interno_conta,
	clock_timestamp(),
	nm_usuario_p,
	clock_timestamp(),
	nm_usuario_p,
	dt_etapa,
	nr_seq_etapa,
	cd_setor_atendimento,
	cd_pessoa_fisica,
	dt_fim_etapa,
	nr_seq_motivo_dev,
	ds_observacao,
	nr_lote_barras,
	cd_pessoa_exec,
	dt_recebimento,
	vl_conta,
	nr_seq_protocolo,
	ie_etapa_protocolo,
	ie_etapa_critica,
	dt_periodo_inicial,
	dt_periodo_final,
	nr_seq_conta_origem
from	conta_paciente_etapa
where	((nr_sequencia = nr_sequencia_p) or (nr_interno_conta = nr_interno_conta_p));

if (coalesce(wheb_usuario_pck.get_ie_commit, 'S') = 'S') then commit; end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_log_conpaci_etapa ( nr_sequencia_p bigint, nr_interno_conta_p bigint, nm_usuario_p text) FROM PUBLIC;

