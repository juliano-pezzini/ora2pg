-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE liberar_resultado_anticorpo (nr_seq_prescr_proc_p bigint, nr_seq_antic_result_p bigint, nr_seq_peca_p bigint, nm_usuario_p text) AS $body$
BEGIN

if (nr_seq_antic_result_p = 0) then
	update 	PRESCR_PROC_RESULT_ANTIC
	set	dt_liberacao = clock_timestamp(),
		nm_usuario   = nm_usuario_p,
		dt_atualizacao = clock_timestamp()
	where	nr_seq_prescr_proc_compl = nr_seq_prescr_proc_p
	and	nr_seq_prescr_proc_peca = nr_seq_peca_p
	and	coalesce(dt_liberacao::text, '') = '';
else
	update 	PRESCR_PROC_RESULT_ANTIC
	set	dt_liberacao = clock_timestamp(),
		nm_usuario   = nm_usuario_p,
		dt_atualizacao = clock_timestamp()
	where	nr_seq_prescr_proc_compl = nr_seq_prescr_proc_p
	and	nr_seq_prescr_proc_peca = nr_seq_peca_p
	and	nr_sequencia = nr_seq_antic_result_p;
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE liberar_resultado_anticorpo (nr_seq_prescr_proc_p bigint, nr_seq_antic_result_p bigint, nr_seq_peca_p bigint, nm_usuario_p text) FROM PUBLIC;

