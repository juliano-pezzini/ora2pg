-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_baca_ajuste_atribut_w_web () AS $body$
DECLARE


qt_registro_w			bigint;
nr_seq_restricao_funcao_w	bigint;
nr_seq_atributo_w		bigint;


BEGIN

select	count(1)
into STRICT	qt_registro_w
from	PLS_ATRIBUTOS_WEB
where	nr_sequencia in (20,21);

if (qt_registro_w = 2) then

	select	count(1)
	into STRICT	qt_registro_w
	from	pls_regra_atributo_web
	where	nr_seq_atributo_web in (20,21);

	if (qt_registro_w = 0) then

		insert into pls_regra_atributo_web(nr_sequencia, nr_seq_atributo_web, dt_atualizacao,
										   nm_usuario, dt_atualizacao_nrec, nm_usuario_nrec,
										   ie_obrigatorio)
									values (nextval('pls_regra_atributo_web_seq'), 21, clock_timestamp(),
										   'aasestari', clock_timestamp(), 'aasestari',
										   'S');

		insert into pls_regra_atributo_web(nr_sequencia, nr_seq_atributo_web, dt_atualizacao,
										   nm_usuario, dt_atualizacao_nrec, nm_usuario_nrec,
										   ie_obrigatorio)
									values (nextval('pls_regra_atributo_web_seq'), 20, clock_timestamp(),
										   'aasestari', clock_timestamp(), 'aasestari',
										   'S');

		commit;

	end if;

end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_baca_ajuste_atribut_w_web () FROM PUBLIC;

