-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE carregar_diluente_ds_diluir_md (ie_separar_p text, ds_diluir_p INOUT text, ds_conv_ml_p text, ds_aux_diluir_p text, ds_additional_item_p text, ds_dose_diferenciada_p bigint) AS $body$
BEGIN
  if (ie_separar_p = 'S') then
    ds_diluir_p := ds_diluir_p || wheb_mensagem_pck.get_texto(307089);
  else
    ds_diluir_p := ds_diluir_p || wheb_mensagem_pck.get_texto(307092);
  end if;

  ds_diluir_p := ds_diluir_p || ' ' || ds_conv_ml_p || ds_aux_diluir_p ||
                 ds_additional_item_p || ' ' ||
                 wheb_mensagem_pck.get_texto(307099) || '(' ||
                 ds_dose_diferenciada_p || ')';

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE carregar_diluente_ds_diluir_md (ie_separar_p text, ds_diluir_p INOUT text, ds_conv_ml_p text, ds_aux_diluir_p text, ds_additional_item_p text, ds_dose_diferenciada_p bigint) FROM PUBLIC;

