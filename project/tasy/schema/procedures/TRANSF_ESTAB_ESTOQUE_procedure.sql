-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE transf_estab_estoque (cd_estabelecimento_p movimento_estoque.cd_estabelecimento%type, cd_estab_origem_saldo_p movimento_estoque.cd_estabelecimento%type, nm_usuario_p movimento_estoque.nm_usuario%type, cd_local_estoque_p movimento_estoque.cd_local_estoque_destino%type, cd_local_origem_saldo_p movimento_estoque.cd_local_estoque%type, cd_material_p movimento_estoque.cd_material%type, qt_movimento_p movimento_estoque.qt_movimento%type, cd_unidade_medida_p movimento_estoque.cd_unidade_medida_estoque%type) AS $body$
DECLARE


dt_atualizacao_w timestamp := clock_timestamp();
cd_operacao_correspondente_w operacao_estoque.cd_operacao_correspondente%type := 0;
cd_acao_w constant varchar(1) := '1';
ie_origem_documento_w constant varchar(1) := '2';
cd_operacao_estoque_w operacao_estoque.cd_operacao_estoque%type := 0;


BEGIN
	cd_operacao_estoque_w := obter_param_usuario(3130, 506, obter_perfil_ativo, nm_usuario_p, cd_estabelecimento_p, cd_operacao_estoque_w);

	insert
	into movimento_estoque(nr_movimento_estoque,
		 cd_estabelecimento,
		 cd_local_estoque,
		 dt_movimento_estoque,
		 cd_operacao_estoque,
		 cd_acao,
		 cd_material,
		 dt_mesano_referencia,
		 qt_movimento,
		 dt_atualizacao,
		 nm_usuario,
		 ie_origem_documento,
		 cd_unidade_medida_estoque,
		 qt_estoque,
		 cd_local_estoque_destino,
		 cd_unidade_med_mov)
	values (nextval('movimento_estoque_seq'),
		 cd_estab_origem_saldo_p,
		 cd_local_origem_saldo_p,
		 dt_atualizacao_w,
		 cd_operacao_estoque_w,
		 cd_acao_w,
		 cd_material_p,
		 dt_atualizacao_w,
		 qt_movimento_p,
		 dt_atualizacao_w,
		 nm_usuario_p,
		 ie_origem_documento_w,
		 cd_unidade_medida_p,
		 qt_movimento_p,
		 cd_local_estoque_p,
		 cd_unidade_medida_p);

	begin
		select cd_operacao_correspondente
		into STRICT cd_operacao_correspondente_w
		from operacao_estoque
		where (cd_operacao_estoque  = cd_operacao_estoque_w);
		exception
	        when no_data_found then
			CALL wheb_mensagem_pck.exibir_mensagem_abort(261637);
	end;

	if (cd_operacao_correspondente_w IS NOT NULL AND cd_operacao_correspondente_w::text <> '') then
	    begin
			
			insert into movimento_estoque(nr_movimento_estoque,
				cd_estabelecimento,
				cd_local_estoque,
				dt_movimento_estoque,
				cd_operacao_estoque,
				cd_acao,
				cd_material,
				dt_mesano_referencia,
				qt_movimento,
				dt_atualizacao,
				nm_usuario,
				ie_origem_documento,
				cd_unidade_medida_estoque,
				qt_estoque,
				cd_unidade_med_mov,
			    nr_movimento_estoque_corresp)
			values (nextval('movimento_estoque_seq'),
				cd_estabelecimento_p,
				cd_local_estoque_p,
				dt_atualizacao_w,
				cd_operacao_correspondente_w,
				cd_acao_w,
				cd_material_p,
				dt_atualizacao_w,
				qt_movimento_p,
				dt_atualizacao_w,
				nm_usuario_p,
				ie_origem_documento_w,
				cd_unidade_medida_p,
				qt_movimento_p,
				cd_unidade_medida_p,
			    nextval('movimento_estoque_seq'));
	    end;
	end if;

commit;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE transf_estab_estoque (cd_estabelecimento_p movimento_estoque.cd_estabelecimento%type, cd_estab_origem_saldo_p movimento_estoque.cd_estabelecimento%type, nm_usuario_p movimento_estoque.nm_usuario%type, cd_local_estoque_p movimento_estoque.cd_local_estoque_destino%type, cd_local_origem_saldo_p movimento_estoque.cd_local_estoque%type, cd_material_p movimento_estoque.cd_material%type, qt_movimento_p movimento_estoque.qt_movimento%type, cd_unidade_medida_p movimento_estoque.cd_unidade_medida_estoque%type) FROM PUBLIC;

