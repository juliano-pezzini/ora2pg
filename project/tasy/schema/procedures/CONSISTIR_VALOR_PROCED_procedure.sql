-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consistir_valor_proced ( cd_convenio_p bigint, cd_categoria_p text, cd_estabelecimento_p bigint, cd_procedimento_p bigint, ie_origem_proced_p bigint, ie_exame_Lab_p text, nm_usuario_p text, nr_seq_exame_p bigint, nr_proc_interno_p bigint, ie_tipo_atendimento_p bigint) AS $body$
DECLARE

				 
ie_consiste_valor_zero_w	 	varchar(1);
ie_consiste_valor_proc_int_w 	varchar(1);
ie_consiste_valor_zero_lab_w		varchar(1);

ie_valor_proc_interno_w			varchar(1);
ie_preco_proc_w				varchar(1);
ie_exame_lib_preco_w			varchar(1);
ie_preco_exame_w			varchar(1);

/* 
ie_exame_Lab_p 
P procedimentos 
E exames de laboratório 
*/
 
 
 

BEGIN 
 
ie_consiste_valor_zero_w := Obter_param_Usuario(916, 923, obter_perfil_ativo, nm_usuario_p, cd_estabelecimento_p, ie_consiste_valor_zero_w);
ie_consiste_valor_proc_int_w := Obter_param_Usuario(916, 924, obter_perfil_ativo, nm_usuario_p, cd_estabelecimento_p, ie_consiste_valor_proc_int_w);
ie_consiste_valor_zero_lab_w := Obter_param_Usuario(916, 925, obter_perfil_ativo, nm_usuario_p, cd_estabelecimento_p, ie_consiste_valor_zero_lab_w);
 
if (ie_exame_Lab_p = 'P') then 
	 
	if (coalesce(ie_consiste_valor_proc_int_w,'N') = 'S') and (coalesce(nr_proc_interno_p,0) > 0) then 
		 
		select	obter_se_proc_interno_valor(nr_proc_interno_p,cd_convenio_p,cd_categoria_p,null,null,cd_estabelecimento_p, coalesce(ie_tipo_atendimento_p,0)) 
		into STRICT	ie_valor_proc_interno_w 
		;
		 
		if (coalesce(ie_valor_proc_interno_w,'S') <> 'S') then 
			CALL wheb_mensagem_pck.exibir_mensagem_abort(189394,'PARAMETRO='||'924');
			--'Não é possível inserir este procedimento. Verificar o parâmetro [924].'; 
		end if;	
	end if;
	 
	if (coalesce(ie_consiste_valor_zero_w,'N') = 'S') then 
		 
		select	obter_se_proc_convenio(cd_estabelecimento_p,cd_convenio_p,cd_categoria_p,cd_procedimento_p,ie_origem_proced_p,nr_proc_interno_p,nr_seq_exame_p, coalesce(ie_tipo_atendimento_p,0)) 
		into STRICT	ie_preco_proc_w 
		;
		 
		if (coalesce(ie_preco_proc_w,'S') <> 'S') then 
			CALL wheb_mensagem_pck.exibir_mensagem_abort(189394,'PARAMETRO='||'923');
			--'Não é possível inserir este procedimento. Verificar o parâmetro [923].'; 
		end if;
	end if;
	 
elsif (ie_exame_Lab_p = 'E') then 
 
	if (coalesce(ie_consiste_valor_zero_lab_w,'N') = 'S') then 
	 
		select	obter_se_proc_convenio(cd_estabelecimento_p,cd_convenio_p,cd_categoria_p,cd_procedimento_p,ie_origem_proced_p,nr_proc_interno_p,nr_seq_exame_p, coalesce(ie_tipo_atendimento_p,0)) 
		into STRICT	ie_preco_exame_w 
		;
		 
		if (coalesce(ie_preco_exame_w,'S') <> 'S') then 
			CALL wheb_mensagem_pck.exibir_mensagem_abort(189394,'PARAMETRO='||'925');
			--'Não é possível inserir este procedimento. Parâmetro[925].' 
		end if;		
	end if;
	if (coalesce(nr_seq_exame_p,0) > 0) then 
	 
		select	lab_obter_se_exame_lib(nr_seq_exame_p,cd_estabelecimento_p) 
		into STRICT	ie_exame_lib_preco_w 
		;
		 
		if (coalesce(ie_exame_lib_preco_w,'S') <> 'S') then 
		 
		CALL wheb_mensagem_pck.exibir_mensagem_abort(189398);
		--'Não é possível inserir este exame. Verificar a regra de estabelecimentos liberados para o exame.' 
		 
		end if;
	end if;
end if;
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consistir_valor_proced ( cd_convenio_p bigint, cd_categoria_p text, cd_estabelecimento_p bigint, cd_procedimento_p bigint, ie_origem_proced_p bigint, ie_exame_Lab_p text, nm_usuario_p text, nr_seq_exame_p bigint, nr_proc_interno_p bigint, ie_tipo_atendimento_p bigint) FROM PUBLIC;

