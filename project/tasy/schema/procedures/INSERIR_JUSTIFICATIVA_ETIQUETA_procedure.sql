-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE inserir_justificativa_etiqueta (nr_seq_ordem_p bigint, ds_justificativa_p text, nm_usuario_p text) AS $body$
DECLARE


nr_sequencia_w	bigint;


BEGIN

select 	nextval('quimio_justif_reimpressao_seq')
into STRICT	nr_sequencia_w
;

if (nr_seq_ordem_p IS NOT NULL AND nr_seq_ordem_p::text <> '') then
	insert into quimio_justif_reimpressao(
		nr_sequencia,
		ds_justificativa,
		nr_seq_ordem,
		dt_atualizacao,
		dt_atualizacao_nrec,
		nm_usuario,
		nm_usuario_nrec)
	values (nr_sequencia_w,
		ds_justificativa_p,
		nr_seq_ordem_p,
		clock_timestamp(),
		clock_timestamp(),
		nm_usuario_p,
		nm_usuario_p);
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE inserir_justificativa_etiqueta (nr_seq_ordem_p bigint, ds_justificativa_p text, nm_usuario_p text) FROM PUBLIC;

