-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consistir_estrut_regra_ccusto ( cd_grupo_material_p bigint, cd_subgrupo_material_p bigint, cd_classe_material_p bigint, cd_material_p bigint) AS $body$
DECLARE

qt_existe_w	bigint;


BEGIN

select	count(*)
into STRICT	qt_existe_w
from	estrutura_material_v
where	((cd_material_p = 0) or
	(cd_material_p > 0 AND cd_material = cd_material_p))
and	((cd_classe_material_p = 0) or
	(cd_classe_material_p > 0 AND cd_classe_material = cd_classe_material_p))
and	((cd_subgrupo_material_p = 0) or
	(cd_subgrupo_material_p > 0 AND cd_subgrupo_material = cd_subgrupo_material_p))
and	((cd_grupo_material_p = 0) or
	(cd_grupo_material_p > 0 AND cd_grupo_material = cd_grupo_material_p));

if (qt_existe_w = 0) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(265959);
	--'A estrutura informada nesta regra não existe. Por exemplo:' || chr(13) || chr(10) ||
	--'* O subgrupo deve pertencer ao grupo informado. ' || chr(13) || chr(10) ||
	--'* A  classe deve pertencer ao subgrupo ou grupo  informado.' || chr(13) || chr(10) ||
	--'* O material deve pertencer a classe, ou subgrupo ou grupo  informado.');
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consistir_estrut_regra_ccusto ( cd_grupo_material_p bigint, cd_subgrupo_material_p bigint, cd_classe_material_p bigint, cd_material_p bigint) FROM PUBLIC;

