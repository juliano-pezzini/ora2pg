-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pat_consistir_bem_palm ( cd_bem_p text, nr_seq_inventario_p bigint, nm_usuario_p text, ds_erro_p INOUT text) AS $body$
DECLARE


ie_consiste_w			varchar(255);
qt_bem_w			bigint;


BEGIN
if (nr_seq_inventario_p > 0) then
	begin
	ie_consiste_w := Obter_Param_Usuario(88, 292, obter_perfil_ativo, nm_usuario_p, wheb_usuario_pck.get_cd_estabelecimento, ie_consiste_w);

	select	count(1)
	into STRICT	qt_bem_w
	from	pat_inventario_bem
	where	cd_bem			= cd_bem_p
	and	nr_seq_inventario	= nr_seq_inventario_p;

	ds_erro_p	:= '';

	if (ie_consiste_w = 'S') and (qt_bem_w > 0) then
		ds_erro_p	:= substr(wheb_mensagem_pck.get_texto(320333),1,255);
	end if;
	end;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pat_consistir_bem_palm ( cd_bem_p text, nr_seq_inventario_p bigint, nm_usuario_p text, ds_erro_p INOUT text) FROM PUBLIC;

