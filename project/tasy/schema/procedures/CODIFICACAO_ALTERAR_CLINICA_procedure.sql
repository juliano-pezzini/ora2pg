-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE codificacao_alterar_clinica ( nr_seq_codificacao_p bigint, ie_tipo_clinica_p bigint, nm_usuario_p text) AS $body$
DECLARE


ie_tipo_clinica_w	codificacao_atendimento.ie_tipo_clinica%type;


BEGIN

select	max(coalesce(ie_tipo_clinica, ie_tipo_clinica_ori))
into STRICT	ie_tipo_clinica_w
from	codificacao_atendimento
where	nr_sequencia = nr_seq_codificacao_p;

update	codificacao_atendimento
set		ie_tipo_clinica = ie_tipo_clinica_p
where	nr_sequencia = nr_seq_codificacao_p;

CALL gerar_codificacao_atend_log(nr_seq_codificacao_p, obter_desc_expressao(330682) || ' ' ||
	obter_desc_expressao(310214) ||	' '	|| substr(obter_valor_dominio(17, ie_tipo_clinica_w), 1, 30) || ' ' ||
	obter_desc_expressao(618484) || ' ' || substr(obter_valor_dominio(17, ie_tipo_clinica_p), 1, 30), nm_usuario_p);

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE codificacao_alterar_clinica ( nr_seq_codificacao_p bigint, ie_tipo_clinica_p bigint, nm_usuario_p text) FROM PUBLIC;

