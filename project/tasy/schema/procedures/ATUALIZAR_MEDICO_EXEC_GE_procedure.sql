-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE atualizar_medico_exec_ge (nr_seq_propaci_p bigint, nm_usuario_p text) AS $body$
DECLARE



cd_medico_exec_w		varchar(10);
cd_equipamento_w		bigint;
CD_PESSOA_FISICA_w		varchar(10);
IE_FUNCAO_MEDICO_w		varchar(10);
CD_ESPECIALIDADE_w		integer;


BEGIN

select	max(cd_medico_executor),
	max(cd_equipamento),
	max(cd_pessoa_fisica),
	max(IE_FUNCAO_MEDICO),
	max(CD_ESPECIALIDADE)
into STRICT	cd_medico_exec_w,
	cd_equipamento_w,
	cd_pessoa_fisica_w,
	IE_FUNCAO_MEDICO_w,
	CD_ESPECIALIDADE_w
from	procedimento_paciente
where	nr_sequencia	= nr_seq_propaci_p;

update	procedimento_paciente
set	cd_medico_executor	= cd_medico_exec_w,
	cd_equipamento		= cd_equipamento_w,
	cd_pessoa_fisica	= cd_pessoa_fisica_w,
	IE_FUNCAO_MEDICO	= IE_FUNCAO_MEDICO_w,
	CD_ESPECIALIDADE	= CD_ESPECIALIDADE_w
where	nr_seq_proc_princ	= nr_seq_propaci_p;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE atualizar_medico_exec_ge (nr_seq_propaci_p bigint, nm_usuario_p text) FROM PUBLIC;

