-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE obter_orientacao_proced (cd_procedimento_p bigint, ie_origem_proced_p bigint, cd_convenio_p bigint, nr_seq_proc_interno_p bigint, ds_orientacao_p INOUT text) AS $body$
DECLARE


ds_orientacao_w		varchar(4000)	:= '';
ds_orient_convenio_w	varchar(2000)	:= '';
ie_orientacao_w		varchar(01);


BEGIN

select	max(ds_orientacao)
into STRICT	ds_orientacao_w
from	procedimento
where	cd_procedimento		= cd_procedimento_p
and	ie_origem_proced	= ie_origem_proced_p;


if (coalesce(nr_seq_proc_interno_p, 0) > 0) then
	begin

	select	max(ie_orientacao),
		max(ds_orientacao_usuario)
	into STRICT	ie_orientacao_w,
		ds_orientacao_w
	from	proc_interno
	where	nr_sequencia	= nr_seq_proc_interno_p;


	select	max(ds_orientacao)
	into STRICT	ds_orient_convenio_w
	from	proc_interno_conv_orient
	where	nr_seq_proc_interno	= nr_seq_proc_interno_p;

	if (ie_orientacao_w = 'C') then
		ds_orientacao_w	:= ds_orient_convenio_w;
	elsif (ie_orientacao_w = 'A') then
		ds_orientacao_w	:= ds_orientacao_w || chr(13) || chr(10) || ds_orient_convenio_w;
	end if;

	end;
end if;

ds_orientacao_p		:= ds_orientacao_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE obter_orientacao_proced (cd_procedimento_p bigint, ie_origem_proced_p bigint, cd_convenio_p bigint, nr_seq_proc_interno_p bigint, ds_orientacao_p INOUT text) FROM PUBLIC;

