-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE fa_consistir_medicamentos (nr_seq_entrega_p bigint, ds_erro_p INOUT text) AS $body$
DECLARE


qt_medicamentos_w	bigint;

BEGIN

select 	count(*)
into STRICT	qt_medicamentos_w
from	fa_entrega_medicacao_item
where	nr_seq_fa_entrega = nr_seq_entrega_p
and	ie_status_medicacao <> 'RR';


if (qt_medicamentos_w <> 0) then
	ds_erro_p := WHEB_MENSAGEM_PCK.get_texto(279183);
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE fa_consistir_medicamentos (nr_seq_entrega_p bigint, ds_erro_p INOUT text) FROM PUBLIC;

