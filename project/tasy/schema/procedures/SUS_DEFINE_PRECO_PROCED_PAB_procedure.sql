-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE sus_define_preco_proced_pab ( dt_procedimento_p timestamp, cd_procedimento_p bigint, ie_origem_proced_p bigint, cd_estabelecimento_p bigint, vl_sa_p INOUT bigint, vl_sh_p INOUT bigint, vl_sp_p INOUT bigint, vl_total_hospitalar_p INOUT bigint) AS $body$
DECLARE



vl_sa_w			double precision	:= 0;
vl_sh_w			double precision	:= 0;
vl_sp_w			double precision	:= 0;
vl_total_hospitalar_w	double precision	:= 0;
dt_competencia_w	sus_preco_municipio.dt_competencia%type;
cd_municipio_ibge_w	sus_preco_municipio.cd_municipio_ibge%type := 'X';



BEGIN

begin
select	coalesce(obter_dados_pf_pj(null,cd_cgc,'CDM'),'X')
into STRICT	cd_municipio_ibge_w
from	estabelecimento
where	cd_estabelecimento = cd_estabelecimento_p;
exception
when others then
	cd_municipio_ibge_w := 'X';
end;

select 	max(dt_competencia)
into STRICT	dt_competencia_w
from	sus_preco_municipio
where	cd_procedimento		= cd_procedimento_p
and	ie_origem_proced	= ie_origem_proced_p
and	coalesce(cd_municipio_ibge,cd_municipio_ibge_w) 	= cd_municipio_ibge_w
and	coalesce(cd_estabelecimento,cd_estabelecimento_p)	= cd_estabelecimento_p;

begin
select	vl_sa,
	vl_sh,
	vl_sp,
	vl_total_hospitalar
into STRICT	vl_sa_w,
	vl_sh_w,
	vl_sp_w,
	vl_total_hospitalar_w
from	sus_preco_municipio
where	cd_procedimento		= cd_procedimento_p
and	ie_origem_proced	= ie_origem_proced_p
and	coalesce(cd_municipio_ibge,cd_municipio_ibge_w) 	= cd_municipio_ibge_w
and	coalesce(cd_estabelecimento,cd_estabelecimento_p)	= cd_estabelecimento_p
and	dt_competencia		= dt_competencia_w;
exception
	when others then
        vl_sa_w			:= 0;
	vl_sh_w			:= 0;
	vl_sp_w			:= 0;
	vl_total_hospitalar_w	:= 0;
end;

vl_sa_p			:= vl_sa_w;
vl_sh_p			:= vl_sh_w;
vl_sp_p			:= vl_sp_w;
vl_total_hospitalar_p	:= vl_total_hospitalar_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE sus_define_preco_proced_pab ( dt_procedimento_p timestamp, cd_procedimento_p bigint, ie_origem_proced_p bigint, cd_estabelecimento_p bigint, vl_sa_p INOUT bigint, vl_sh_p INOUT bigint, vl_sp_p INOUT bigint, vl_total_hospitalar_p INOUT bigint) FROM PUBLIC;

