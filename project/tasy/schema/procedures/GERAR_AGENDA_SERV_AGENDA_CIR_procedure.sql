-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_agenda_serv_agenda_cir ( nr_sequencia_p bigint, nr_seq_agenda_p bigint, nm_usuario_p text) AS $body$
DECLARE

					 
cd_pessoa_fisica_w	varchar(10);
					

BEGIN 
if (nr_sequencia_p IS NOT NULL AND nr_sequencia_p::text <> '') and (nr_seq_agenda_p IS NOT NULL AND nr_seq_agenda_p::text <> '') then 
	 
	select	cd_pessoa_fisica 
	into STRICT	cd_pessoa_fisica_w 
	from 	agenda_paciente 
	where	nr_sequencia = nr_seq_agenda_p;
	 
	update	agenda_consulta 
	set	cd_pessoa_fisica	= cd_pessoa_fisica_w, 
		ie_status_agenda 	= 'N', 
		nm_paciente 		= substr(obter_nome_pf(cd_pessoa_fisica_w),1,60), 
		nr_seq_agepaci		= nr_seq_agenda_p, 
		nm_usuario		= nm_usuario_p 
	where	nr_sequencia 		= nr_sequencia_p;
end if;
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_agenda_serv_agenda_cir ( nr_sequencia_p bigint, nr_seq_agenda_p bigint, nm_usuario_p text) FROM PUBLIC;

