-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE inserir_anexo_auto_cirurgia ( nr_seq_autorizacao_p bigint, ds_arquivos_p text, qt_arquivos_p bigint, nm_usuario_p text) AS $body$
DECLARE


ds_caminho_w	varchar(4000);
ds_arquivos_w	varchar(4000);
BEGIN
ds_arquivos_w	:= ds_arquivos_p;

for i in 1..qt_arquivos_p loop
	ds_caminho_w := substr(ds_arquivos_w,1,position('?' in ds_arquivos_w) - 1);
	ds_arquivos_w := substr(ds_arquivos_w, position('?' in ds_arquivos_w) + 1, length(ds_arquivos_w));

	insert into	autorizacao_cirurgia_arq(nr_sequencia,
		nm_usuario,
		dt_atualizacao,
		nm_usuario_nrec,
		dt_atualizacao_nrec,
		nr_seq_autorizacao,
		ds_arquivo)
	values (nextval('autorizacao_cirurgia_arq_seq'),
		nm_usuario_p,
		clock_timestamp(),
		nm_usuario_p,
		clock_timestamp(),
		nr_seq_autorizacao_p,
		ds_caminho_w);
end loop;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE inserir_anexo_auto_cirurgia ( nr_seq_autorizacao_p bigint, ds_arquivos_p text, qt_arquivos_p bigint, nm_usuario_p text) FROM PUBLIC;

