-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE baca_ajustar_unid_tratamento () AS $body$
DECLARE


nr_seq_tratamento_w		bigint;
nr_seq_unidade_dialise_w	bigint;
dt_inicio_tratamento_w		timestamp;
cd_pessoa_fisica_w		varchar(10);

C01 CURSOR FOR
	SELECT	nr_sequencia,
		cd_pessoa_fisica,
		dt_inicio_tratamento
	from	paciente_tratamento
	where	coalesce(nr_seq_unid_dialise::text, '') = '';


BEGIN

open C01;
loop
fetch C01 into
	nr_seq_tratamento_w,
	cd_pessoa_fisica_w,
	dt_inicio_tratamento_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin

	nr_seq_unidade_dialise_w	:= hd_obter_unid_dialise_data(cd_pessoa_fisica_w, dt_inicio_tratamento_w);

	update	paciente_tratamento
	set	nr_seq_unid_dialise = nr_seq_unidade_dialise_w
	where	nr_sequencia = nr_seq_tratamento_w;

	end;
end loop;
close C01;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE baca_ajustar_unid_tratamento () FROM PUBLIC;

