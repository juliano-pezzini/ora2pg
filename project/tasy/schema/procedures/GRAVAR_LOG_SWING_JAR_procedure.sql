-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gravar_log_swing_jar ( nm_projeto_p text, nm_usuario_p text, dt_ult_commit_p timestamp, nm_usuario_commit_p text, ds_revisao_p text, ds_versao_java_p text) AS $body$
DECLARE


qt_registro_w	bigint;

BEGIN

select	count(*)
into STRICT	qt_registro_w
from	aplicacao_swing_log
where	nm_projeto = nm_projeto_p;

if (qt_registro_w = 0) then
	insert into aplicacao_swing_log(
		nr_sequencia,
		dt_atualizacao,
		nm_usuario,
		dt_atualizacao_nrec,
		nm_usuario_nrec,
		nm_projeto,
		dt_ult_commit,
		nm_usuario_commit,
		ds_revisao,
		ds_versao_java,
		dt_versao_java
	) values (
		nextval('aplicacao_swing_log_seq'),
		clock_timestamp(),
		nm_usuario_p,
		clock_timestamp(),
		nm_usuario_p,
		nm_projeto_p,
		dt_ult_commit_p,
		nm_usuario_commit_p,
		ds_revisao_p,
		ds_versao_java_p,
		clock_timestamp()
	);

	commit;

else

	update 	aplicacao_swing_log
	set	dt_atualizacao = clock_timestamp(),
		nm_usuario = nm_usuario_p,
		dt_ult_commit = dt_ult_commit_p,
		nm_usuario_commit = nm_usuario_commit_p,
		ds_revisao = ds_revisao_p,
		ds_versao_java = ds_versao_java_p,
		dt_versao_java = clock_timestamp()
	where	nm_projeto = nm_projeto_p;

	commit;

end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gravar_log_swing_jar ( nm_projeto_p text, nm_usuario_p text, dt_ult_commit_p timestamp, nm_usuario_commit_p text, ds_revisao_p text, ds_versao_java_p text) FROM PUBLIC;

