-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE duplicar_etapa_cronograma_os (nr_seq_etapa_os_p bigint) AS $body$
DECLARE


nr_seq_etapa_os_w	w_proj_cron_etapa_os.nr_sequencia%type := nr_seq_etapa_os_p;
nm_usuario_w		usuario.nm_usuario%type;


BEGIN

nm_usuario_w := wheb_usuario_pck.get_nm_usuario;

if (nr_seq_etapa_os_w IS NOT NULL AND nr_seq_etapa_os_w::text <> '') then

	insert into w_proj_cron_etapa_os(
		nr_sequencia,
		dt_atualizacao,
		nm_usuario,
		dt_atualizacao_nrec,
		nm_usuario_nrec,
		nr_seq_etapa,
		ds_atividade,
		ds_detalhamento,
		nr_seq_prs,
		nm_usuario_exec,
		nr_seq_ativ_exec,
		dt_inicio_prev,
		qt_hora_prev)
	SELECT	nextval('w_proj_cron_etapa_os_seq'),
		clock_timestamp(),
		coalesce(nm_usuario_w,nm_usuario),
		clock_timestamp(),
		coalesce(nm_usuario_w,nm_usuario_nrec),
		pceo.nr_seq_etapa,
		pceo.ds_atividade,
		pceo.ds_detalhamento,
		pceo.nr_seq_prs,
		pceo.nm_usuario_exec,
		pceo.nr_seq_ativ_exec,
		pceo.dt_inicio_prev,
		pceo.qt_hora_prev
	from	w_proj_cron_etapa_os pceo
	where   pceo.nr_sequencia = nr_seq_etapa_os_w;

	commit;

end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE duplicar_etapa_cronograma_os (nr_seq_etapa_os_p bigint) FROM PUBLIC;

