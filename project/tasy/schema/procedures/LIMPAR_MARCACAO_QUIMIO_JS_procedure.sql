-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE limpar_marcacao_quimio_js ( nr_seq_pend_agenda_p bigint, nm_usuario_p text) AS $body$
DECLARE


qt_registro_w	bigint;

BEGIN
if (nr_seq_pend_agenda_p IS NOT NULL AND nr_seq_pend_agenda_p::text <> '') and (nm_usuario_p IS NOT NULL AND nm_usuario_p::text <> '') then
	begin
	select	count(*)
	into STRICT	qt_registro_w
	from	agenda_quimio_marcacao
	where	nr_seq_pend_agenda = nr_seq_pend_agenda_p
	and	coalesce(ie_gerado, 'N') = 'N'
	and	nm_usuario = nm_usuario_p;

	if (qt_registro_w > 0) then
		begin
		CALL qt_limpar_dados_marcacao(nm_usuario_p);
		end;
	end if;
	end;
end if;
commit;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE limpar_marcacao_quimio_js ( nr_seq_pend_agenda_p bigint, nm_usuario_p text) FROM PUBLIC;

