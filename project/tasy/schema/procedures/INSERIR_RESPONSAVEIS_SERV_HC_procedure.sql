-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE inserir_responsaveis_serv_hc ( ds_lista_nr_seq_prof_p text, nr_seq_agenda_p bigint, nm_usuario_p text) AS $body$
DECLARE


ds_lista_nr_seq_prof_w	varchar(2000);
nr_seq_prof_w		bigint;
nr_pos_virgula_w 	bigint;

BEGIN
if (ds_lista_nr_seq_prof_p IS NOT NULL AND ds_lista_nr_seq_prof_p::text <> '') and (nr_seq_agenda_p IS NOT NULL AND nr_seq_agenda_p::text <> '') and (nm_usuario_p IS NOT NULL AND nm_usuario_p::text <> '') then
	begin
	ds_lista_nr_seq_prof_w 	:= ds_lista_nr_seq_prof_p;

	while(ds_lista_nr_seq_prof_w IS NOT NULL AND ds_lista_nr_seq_prof_w::text <> '') loop
		begin
		nr_pos_virgula_w 	:= position(',' in ds_lista_nr_seq_prof_w);
		if (nr_pos_virgula_w > 0) then
			begin
			nr_seq_prof_w 		:= substr(ds_lista_nr_seq_prof_w, 1, position(',' in ds_lista_nr_seq_prof_w) - 1);
			ds_lista_nr_seq_prof_w 	:= substr(ds_lista_nr_seq_prof_w, position(',' in ds_lista_nr_seq_prof_w) + 1, length(ds_lista_nr_seq_prof_w));
			end;
		else
			begin
			nr_seq_prof_w 		:= (ds_lista_nr_seq_prof_w)::numeric;
			ds_lista_nr_seq_prof_w	:= null;
			end;
		end if;

		if (coalesce(nr_seq_prof_w,0) > 0) then
			begin
			CALL inserir_resp_home_care(nr_seq_prof_w, nr_seq_agenda_p, nm_usuario_p);
			end;
		end if;
		end;
	end loop;
	end;
end if;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE inserir_responsaveis_serv_hc ( ds_lista_nr_seq_prof_p text, nr_seq_agenda_p bigint, nm_usuario_p text) FROM PUBLIC;

