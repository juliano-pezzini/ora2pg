-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE desvinc_tit_cancel_conta_prot ( nr_sequencia_p bigint, nm_usuario_p text) AS $body$
DECLARE

 
		 
nr_seq_lote_prot_w		titulo_receber.nr_seq_lote_prot%type;
nr_seq_protocolo_w	titulo_receber.nr_seq_protocolo%type;
nr_interno_conta_w	titulo_receber.nr_interno_conta%type;
ds_retorno_w		varchar(4000);
		
c01 CURSOR FOR 
SELECT	nr_seq_lote_prot, 
	nr_seq_protocolo, 
	nr_interno_conta 
from	titulo_receber 
where	nr_seq_nf_saida = nr_sequencia_p 
and	((nr_seq_lote_prot IS NOT NULL AND nr_seq_lote_prot::text <> '') or (nr_seq_protocolo IS NOT NULL AND nr_seq_protocolo::text <> '') or (nr_interno_conta IS NOT NULL AND nr_interno_conta::text <> ''));


BEGIN 
 
open C01;
loop 
fetch C01 into	 
	nr_seq_lote_prot_w, 
	nr_seq_protocolo_w, 
	nr_interno_conta_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin 
	if (nr_seq_lote_prot_w > 0) then 
		ds_retorno_w := DESVINCULAR_TITULO_LOTE_PROT(nr_seq_lote_prot_w, nm_usuario_p, ds_retorno_w);
	end if;
	 
	if (nr_seq_protocolo_w > 0) then 
		ds_retorno_w := DESVINCULAR_TITULO_PROTOCOLO(nr_seq_protocolo_w, nm_usuario_p, ds_retorno_w);
	end if;
	 
	if (nr_interno_conta_w > 0) then 
		ds_retorno_w := desvincular_titulo_conta(nr_interno_conta_w, nm_usuario_p, ds_retorno_w);
	end if;
	 
	end;
end loop;
close C01;
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE desvinc_tit_cancel_conta_prot ( nr_sequencia_p bigint, nm_usuario_p text) FROM PUBLIC;

