-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE sup_verifica_existe_nf ( cd_cgc_p text, nr_nota_fiscal_p text, dt_emissao_p text, cd_estabelecimento_p bigint, nr_seq_nota_p INOUT bigint, nm_usuario_p text) AS $body$
DECLARE


nr_seq_nota_w	bigint;


BEGIN

select	coalesce(max(nr_sequencia),0)
into STRICT	nr_seq_nota_w
from	nota_fiscal
where	cd_cgc			= cd_cgc_p
and	nr_nota_fiscal		= (nr_nota_fiscal_p)::numeric
and	dt_emissao		= to_date(substr(dt_emissao_p,9,2) || '/' || substr(dt_emissao_p,6,2) || '/' || substr(dt_emissao_p,1,4),'dd/mm/yyyy')
and	cd_estabelecimento		= cd_estabelecimento_p;

nr_seq_nota_p := nr_seq_nota_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE sup_verifica_existe_nf ( cd_cgc_p text, nr_nota_fiscal_p text, dt_emissao_p text, cd_estabelecimento_p bigint, nr_seq_nota_p INOUT bigint, nm_usuario_p text) FROM PUBLIC;

