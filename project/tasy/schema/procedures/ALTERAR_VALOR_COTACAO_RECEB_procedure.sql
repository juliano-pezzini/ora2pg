-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE alterar_valor_cotacao_receb ( cd_moeda_p bigint, vl_cotacao_p bigint, nr_sequencia_p bigint, cd_estabelecimento_p bigint, ie_opcao_p text) AS $body$
DECLARE


cd_moeda_padrao_w	parametro_contas_receber.cd_moeda_padrao%type;
vl_cotacao_w		cotacao_moeda.vl_cotacao%type;

BEGIN
/*
ie_opcao_p - TABELA
CH - CHEQUE_CR
CA - CAIXA_RECEB
*/
null;
/* DESATIVADO PELO MULTIMOEDA */

/*if	(obter_se_cotacao_moeda(cd_moeda_p, vl_cotacao_p) = 'S') then
	begin

	select	max(a.cd_moeda_padrao)
	into	cd_moeda_padrao_w
	from	parametro_contas_receber a
	where	a.cd_estabelecimento	= cd_estabelecimento_p;

	if	(ie_opcao_p = 'CH') then
		begin
		update	cheque_cr
		set	cd_moeda		= nvl(cd_moeda_padrao_w,cd_moeda_p),
			cd_moeda_original	= cd_moeda_p,
			vl_cotacao		= vl_cotacao_p,
			vl_cheque		= nvl(vl_cheque,0) * nvl(vl_cotacao_p,0)
		where	nr_seq_cheque		= nr_sequencia_p;
		end;
	elsif	(ie_opcao_p = 'CA') then
		begin
		update	caixa_receb
		set	cd_moeda		= nvl(cd_moeda_padrao_w,cd_moeda_p),
			cd_moeda_original	= cd_moeda_p,
			vl_cotacao		= vl_cotacao_p,
			vl_especie		= nvl(vl_especie,0) * nvl(vl_cotacao_p,0)
		where	nr_sequencia		= nr_sequencia_p;
		end;
	end if;
	end;

else
	begin
	select	max(b.vl_cotacao) vl_cotacao
	into	vl_cotacao_w
	from	cotacao_moeda b
	where	dt_cotacao = (	select	max(y.dt_cotacao)
				from	cotacao_moeda y,
					moeda x
				where	y.cd_moeda = x.cd_moeda
				and	x.cd_moeda = cd_moeda_p);
	wheb_mensagem_pck.exibir_mensagem_abort(304390, 'VL_COTACAO=' || vl_cotacao_w);
	end;

end if;

commit;*/
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE alterar_valor_cotacao_receb ( cd_moeda_p bigint, vl_cotacao_p bigint, nr_sequencia_p bigint, cd_estabelecimento_p bigint, ie_opcao_p text) FROM PUBLIC;

