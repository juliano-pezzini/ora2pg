-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';



--
-- dblink wrapper to call function intpd_gravar_log_fila as an autonomous transaction
--
CREATE EXTENSION IF NOT EXISTS dblink;

CREATE OR REPLACE PROCEDURE intpd_gravar_log_fila ( reg_integracao_p gerar_int_padrao.reg_integracao_conv, nm_usuario_p text default 'INTPDTASY') AS $body$
DECLARE
	-- Change this to reflect the dblink connection string
	v_conn_str  text := format('port=%s dbname=%s user=%s', current_setting('port'), current_database(), current_user);
	v_query     text;

BEGIN
	v_query := 'CALL intpd_gravar_log_fila_atx ( ' || quote_nullable(reg_integracao_p) || ',' || quote_nullable(nm_usuario_p) || ' )';
	PERFORM * FROM dblink(v_conn_str, v_query) AS p (ret boolean);

END;
$body$ LANGUAGE plpgsql SECURITY DEFINER;




CREATE OR REPLACE PROCEDURE intpd_gravar_log_fila_atx ( reg_integracao_p gerar_int_padrao.reg_integracao_conv, nm_usuario_p text default 'INTPDTASY') AS $body$
DECLARE


i		integer(10);
reg_integracao_w	gerar_int_padrao.reg_integracao_conv;
BEGIN
reg_integracao_w	:=	reg_integracao_p;

reg_integracao_w := gerar_int_padrao.gravar_log(reg_integracao_w, nm_usuario_p);

if (reg_integracao_p.qt_reg_log > 0) then	
	update	intpd_fila_transmissao
	set	ie_status	=	'E'
	where	nr_sequencia	=	reg_integracao_p.nr_seq_fila_transmissao;
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE intpd_gravar_log_fila ( reg_integracao_p gerar_int_padrao.reg_integracao_conv, nm_usuario_p text default 'INTPDTASY') FROM PUBLIC; -- REVOKE ALL ON PROCEDURE intpd_gravar_log_fila_atx ( reg_integracao_p gerar_int_padrao.reg_integracao_conv, nm_usuario_p text default 'INTPDTASY') FROM PUBLIC;

