-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE mp_atualiza_processo_ref ( nr_seq_proc_objeto_p bigint, nm_usuario_p text) AS $body$
DECLARE


nm_objeto_w		varchar(255);
nr_seq_processo_ref_w	bigint;
nr_seq_objeto_w		bigint;


BEGIN

select	max(nr_seq_processo_ref)
into STRICT	nr_seq_processo_ref_w
from	mp_processo_objeto
where	nr_sequencia	= nr_seq_proc_objeto_p;

if (nr_seq_processo_ref_w IS NOT NULL AND nr_seq_processo_ref_w::text <> '') then
	select	nm_objeto,
		nr_seq_objeto
	into STRICT	nm_objeto_w,
		nr_seq_objeto_w
	from	mp_processo_objeto
	where	nr_sequencia	= nr_seq_proc_objeto_p;

	update	mp_processo
	set	nm_processo	= nm_objeto_w,
		nm_usuario	= nm_usuario_p,
		nr_seq_objeto	= nr_seq_objeto_w,
		dt_atualizacao	= clock_timestamp()
	where	nr_sequencia	= nr_seq_processo_ref_w;
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE mp_atualiza_processo_ref ( nr_seq_proc_objeto_p bigint, nm_usuario_p text) FROM PUBLIC;

