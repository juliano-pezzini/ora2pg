-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE baca_seq_tipo_acomod_mat () AS $body$
DECLARE


qt_registros_w	bigint;
nr_seq_proxima	bigint;

BEGIN

select 	count(*)
into STRICT	qt_registros_w
from 	user_sequences
where 	sequence_name = 'PACOTE_TIPO_ACOMOD_MAT_SEQ';

if (qt_registros_w = 0) then

	select 	coalesce(max(nr_sequencia) ,0)
	into STRICT	nr_seq_proxima
	from	PACOTE_TIPO_ACOMOD_MAT;

	CALL Exec_Sql_Dinamico('Tasy', 	' create sequence PACOTE_TIPO_ACOMOD_MAT_SEQ ' ||
					' increment by 1 ' ||
					' start with  ' || (nr_seq_proxima + 1) ||
					' maxValue 9999999999 ' ||
					' cycle ' ||
					' nocache');

else
	CALL tasy_posicionar_sequence('PACOTE_TIPO_ACOMOD_MAT','NR_SEQUENCIA','N');
end if;


commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE baca_seq_tipo_acomod_mat () FROM PUBLIC;

