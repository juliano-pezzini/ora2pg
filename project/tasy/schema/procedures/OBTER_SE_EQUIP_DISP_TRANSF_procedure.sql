-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE obter_se_equip_disp_transf ( nr_seq_agenda_p bigint, nr_seq_agenda_ant_p bigint, ds_erro_p INOUT text, nm_usuario_p text, cd_estabelecimento_p bigint, ie_opcao_p text, ie_novo_registro_p text) AS $body$
DECLARE

 
 
nr_seq_classif_equip_w	bigint;
cd_equipamento_w	bigint;
ds_erro_w		varchar(255);

C01 CURSOR FOR 
	SELECT	nr_seq_classif_equip 
	from	agenda_pac_equip 
	where	nr_seq_agenda = nr_seq_agenda_ant_p 
	and	ie_opcao_p = 'C' 
	and	ie_origem_inf = 'I';	
 
C02 CURSOR FOR 
	SELECT	cd_equipamento 
	from	agenda_pac_equip 
	where	nr_seq_agenda = nr_seq_agenda_ant_p 
	and	ie_opcao_p = 'E' 
	and	ie_origem_inf = 'I';
	

BEGIN 
 
ds_erro_w	:= null;
 
open C01;
loop 
fetch C01 into	 
	nr_seq_classif_equip_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin 
	 
	if (coalesce(ds_erro_w::text, '') = '') then 
	 
		ds_erro_w := obter_se_classif_equip_disp(nr_seq_agenda_p, nr_seq_classif_equip_w, ds_erro_w, nm_usuario_p, cd_estabelecimento_p, nr_seq_agenda_ant_p, ie_novo_registro_p);
		 
	end if;
	 
	end;
end loop;
close C01;
 
 
open C02;
loop 
fetch C02 into	 
	cd_equipamento_w;
EXIT WHEN NOT FOUND; /* apply on C02 */
	begin 
	 
	if (coalesce(ds_erro_w::text, '') = '') then 
		ds_erro_w := obter_se_equip_disp_cirur(nr_seq_agenda_p, cd_equipamento_w, ds_erro_w, nm_usuario_p, cd_estabelecimento_p, nr_seq_agenda_ant_p, ie_novo_registro_p);
	end if;
	 
	end;
end loop;
close C02;
 
ds_erro_p	:= ds_erro_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE obter_se_equip_disp_transf ( nr_seq_agenda_p bigint, nr_seq_agenda_ant_p bigint, ds_erro_p INOUT text, nm_usuario_p text, cd_estabelecimento_p bigint, ie_opcao_p text, ie_novo_registro_p text) FROM PUBLIC;

