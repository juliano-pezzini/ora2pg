-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_gerar_usuario_pericia ( nr_seq_analise_p text, nm_usuario_p text) AS $body$
DECLARE


nr_seq_proposta_w		bigint;
cd_pessoa_fisica_w		varchar(10);
nr_seq_plano_w			bigint;
nr_seq_pagador_w		bigint;
nr_seq_segurado_w		bigint;
cd_estabelecimento_w		smallint;
nr_seq_pessoa_proposta_w	bigint;


BEGIN

select	max(nr_seq_pessoa_proposta)
into STRICT	nr_seq_pessoa_proposta_w
from	pls_analise_adesao
where	nr_sequencia	= nr_seq_analise_p;

if (coalesce(nr_seq_pessoa_proposta_w,0) <> 0) then
	/* Obter dados dados da pessoa da proposta */

	select	a.nr_seq_proposta,
		a.cd_beneficiario,
		a.nr_seq_plano,
		a.nr_seq_pagador,
		b.cd_estabelecimento
	into STRICT	nr_seq_proposta_w,
		cd_pessoa_fisica_w,
		nr_seq_plano_w,
		nr_seq_pagador_w,
		cd_estabelecimento_w
	from	pls_proposta_adesao		b,
		pls_proposta_beneficiario	a
	where	a.nr_seq_proposta		= b.nr_sequencia
	and	a.nr_sequencia			= nr_seq_pessoa_proposta_w;

	select	max(nr_sequencia)
	into STRICT	nr_seq_segurado_w
	from	pls_segurado
	where	ie_tipo_segurado	= 'P'
	and	nr_seq_pessoa_proposta	= nr_seq_pessoa_proposta_w;

	--if	(nvl(nr_seq_segurado_w,0)	= 0) then
	select	nextval('pls_segurado_seq')
	into STRICT	nr_seq_segurado_w
	;

	insert into pls_segurado(
		nr_sequencia, dt_atualizacao, nm_usuario,
		dt_atualizacao_nrec, nm_usuario_nrec, cd_pessoa_fisica,
		nr_seq_titular, nr_seq_contrato, nr_seq_pagador,
		nr_seq_plano, nr_seq_tabela, nr_proposta_adesao,
		ie_tipo_segurado, nr_seq_pessoa_proposta, ie_situacao_atend,
		cd_estabelecimento)
	values (	nr_seq_segurado_w, clock_timestamp(), nm_usuario_p,
		clock_timestamp(), nm_usuario_p, cd_pessoa_fisica_w,
		null, null, null,
		nr_seq_plano_w, null, nr_seq_proposta_w,
		'P', nr_seq_pessoa_proposta_w,'A',
		cd_estabelecimento_w);
	--else
	--	wheb_mensagem_pck.exibir_mensagem_abort(265818,'');
		--Usuário já está registrado na perícia médica!
	--end if;
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_gerar_usuario_pericia ( nr_seq_analise_p text, nm_usuario_p text) FROM PUBLIC;

