-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE obter_indicacao_atend_agenda (cd_pessoa_fisica_p text, nr_seq_indicacao_p INOUT bigint, cd_pessoa_indicacao_p INOUT text) AS $body$
DECLARE

 
nr_atendimento_w	bigint;
nr_seq_indicacao_w	bigint;
cd_pessoa_indicacao_w	varchar(10);


BEGIN 
if (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') then 
	select	coalesce(max(a.nr_atendimento),0) 
	into STRICT	nr_atendimento_w 
	from	atendimento_paciente a 
	where	a.cd_pessoa_fisica = cd_pessoa_fisica_p;
 
	if (nr_atendimento_w IS NOT NULL AND nr_atendimento_w::text <> '') then 
		select	max(nr_seq_indicacao), 
			max(cd_pessoa_indic) 
		into STRICT	nr_seq_indicacao_w, 
			cd_pessoa_indicacao_w 
		from	atendimento_paciente a 
		where	a.nr_atendimento = nr_atendimento_w;
	end if;
end if;
 
nr_seq_indicacao_p := nr_seq_indicacao_w;
cd_pessoa_indicacao_p := cd_pessoa_indicacao_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE obter_indicacao_atend_agenda (cd_pessoa_fisica_p text, nr_seq_indicacao_p INOUT bigint, cd_pessoa_indicacao_p INOUT text) FROM PUBLIC;

