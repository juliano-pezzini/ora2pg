-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE agenda_paciente_beforepost_js ( ie_tipo_classif_p text, cd_agenda_p bigint, dt_agenda_p timestamp, hr_inicio_p timestamp, nr_minuto_duracao_p bigint, nr_seq_agenda_p bigint, cd_medico_p text, ds_erro_p INOUT text, nm_usuario_p text) AS $body$
DECLARE

						
ds_erro_w	varchar(2000) := null;


BEGIN

if (ie_tipo_classif_p <> 'E') then
	ds_erro_w := Consistir_Duracao_Agenda_Pac(cd_agenda_p, dt_agenda_p, hr_inicio_p, nr_minuto_duracao_p, nr_seq_agenda_p, 0, ds_erro_w);
end if;

if (coalesce(ds_erro_w::text, '') = '') then
	ds_erro_w := consistir_horario_medico(nr_seq_agenda_p, cd_medico_p, nr_minuto_duracao_p, ds_erro_w);
else
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(261436, 'ERRO='|| substr(ds_erro_w,1,60));
end if;

ds_erro_p := ds_erro_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE agenda_paciente_beforepost_js ( ie_tipo_classif_p text, cd_agenda_p bigint, dt_agenda_p timestamp, hr_inicio_p timestamp, nr_minuto_duracao_p bigint, nr_seq_agenda_p bigint, cd_medico_p text, ds_erro_p INOUT text, nm_usuario_p text) FROM PUBLIC;

