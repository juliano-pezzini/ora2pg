-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE lab_insert_exame_resultado ( nr_atendimento_p bigint, nr_seq_resultado_p bigint, nr_seq_resultado_w INOUT bigint ) AS $body$
BEGIN

if (nr_seq_resultado_p IS NOT NULL AND nr_seq_resultado_p::text <> '') then
  update exame_lab_resultado set dt_liberacao = clock_timestamp() where nr_seq_resultado = nr_seq_resultado_p;
  commit;
else
  insert into exame_lab_resultado(nr_seq_resultado,
    dt_resultado,        
    dt_atualizacao,   
    nm_usuario,
    cd_medico,
    nr_atendimento,
    cd_pessoa_fisica)
  values
    ((SELECT MAX(NR_SEQ_RESULTADO) + 1 FROM exame_lab_resultado),
    clock_timestamp(),
    clock_timestamp(),
    wheb_usuario_pck.get_nm_usuario,
    (select cd_pessoa_fisica from usuario where nm_usuario = wheb_usuario_pck.get_nm_usuario),
    nr_atendimento_p,
    (SELECT cd_pessoa_fisica FROM ATENDIMENTO_PACIENTE WHERE nr_atendimento = nr_atendimento_p))
  returning nr_seq_resultado into STRICT nr_seq_resultado_w;

  commit;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE lab_insert_exame_resultado ( nr_atendimento_p bigint, nr_seq_resultado_p bigint, nr_seq_resultado_w INOUT bigint ) FROM PUBLIC;

