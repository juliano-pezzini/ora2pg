-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE man_att_os_duplicada ( nr_seq_ordem_p bigint, ds_relat_tecnico_p text, nm_usuario_p text) AS $body$
BEGIN


if	(ds_relat_tecnico_p IS NOT NULL AND ds_relat_tecnico_p::text <> '' AND nr_seq_ordem_p IS NOT NULL AND nr_seq_ordem_p::text <> '') then
	begin

insert into man_ordem_serv_tecnico(
	nr_sequencia,
	nr_seq_ordem_serv,
	dt_atualizacao,
	nm_usuario,
	ds_relat_tecnico,
	dt_historico,
	dt_liberacao,
	nm_usuario_lib,
	ie_origem)
values ( nextval('man_ordem_serv_tecnico_seq'),
	nr_seq_ordem_p,
	clock_timestamp(),
	nm_usuario_p,	
	obter_texto_dic_objeto(183438, wheb_usuario_pck.get_nr_seq_idioma, 'DS_RELAT_TECNICO_P='||ds_relat_tecnico_p),
	clock_timestamp(),
	clock_timestamp(),
	nm_usuario_p,
	'I');	
	
	end;	
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE man_att_os_duplicada ( nr_seq_ordem_p bigint, ds_relat_tecnico_p text, nm_usuario_p text) FROM PUBLIC;

