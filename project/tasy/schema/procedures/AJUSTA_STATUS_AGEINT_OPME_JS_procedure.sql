-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE ajusta_status_ageint_opme_js ( nr_seq_agenda_p text, ds_texto_p INOUT text, nm_usuario_p text) AS $body$
DECLARE


cd_evento_log_w		varchar(1);
		

BEGIN
    if (nr_seq_agenda_p IS NOT NULL AND nr_seq_agenda_p::text <> '') then
        select	ie_opme_integracao
        into STRICT	cd_evento_log_w
        from	agenda_paciente
        where	nr_sequencia = nr_seq_agenda_p;

        if (cd_evento_log_w = 'S') then
            CALL ajusta_status_agenda_int_opme(nr_seq_agenda_p,9,null,nm_usuario_p);
        else
            CALL ajusta_status_agenda_int_opme(nr_seq_agenda_p,0,null,nm_usuario_p);
        end if;

        ds_texto_p := obter_texto_tasy(84619, wheb_usuario_pck.get_nr_seq_idioma);

    end if;
commit;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE ajusta_status_ageint_opme_js ( nr_seq_agenda_p text, ds_texto_p INOUT text, nm_usuario_p text) FROM PUBLIC;

