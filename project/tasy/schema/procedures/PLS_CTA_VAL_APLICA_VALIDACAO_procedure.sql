-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_cta_val_aplica_validacao ( dados_regra_p pls_tipos_cta_val_pck.dados_regra, nr_id_transacao_p pls_rp_cta_selecao.nr_id_transacao%type, cd_estabelecimento_p estabelecimento.cd_estabelecimento%type, nm_usuario_p usuario.nm_usuario%type) AS $body$
DECLARE



/* +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Finalidade: 	Irá realizar a chamada para a rotina de validação conforme a validação selecionada na regra.
-------------------------------------------------------------------------------------------------------------------
Locais de chamada direta:
[ X]  Objetos do dicionário [ ] Tasy (Delphi/Java) [  ] Portal [  ]  Relatórios [ ] Outros:
 ------------------------------------------------------------------------------------------------------------------
Pontos de atenção:
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ */
BEGIN
-- Deve ter informação da regra para executar algo
if (dados_regra_p.nr_seq_regra IS NOT NULL AND dados_regra_p.nr_seq_regra::text <> '') then
	case(dados_regra_p.cd_validacao)
		-- Somente filtros
		-- se for somente filtros não é realizado manutenção em lugar algum e apenas gerado a regra de preço para todos os itens da tabela de seleção
		when 1 then
			pls_cta_val_tratar_val_1(dados_regra_p,nr_id_transacao_p, nm_usuario_p);
		when 2 then
			-- é somente filtro, não precisa fazer nada aqui
			null;
		else
			null;
	end case;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_cta_val_aplica_validacao ( dados_regra_p pls_tipos_cta_val_pck.dados_regra, nr_id_transacao_p pls_rp_cta_selecao.nr_id_transacao%type, cd_estabelecimento_p estabelecimento.cd_estabelecimento%type, nm_usuario_p usuario.nm_usuario%type) FROM PUBLIC;

