-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_desfazer_lote_a600 ( nr_seq_lote_camara_p bigint, nm_usuario_p text) AS $body$
DECLARE


nr_seq_lote_w		bigint;


BEGIN
select	max(nr_sequencia)
into STRICT	nr_seq_lote_w
from	ptu_camara_compensacao
where	nr_seq_lote_camara = nr_seq_lote_camara_p;

if (nr_seq_lote_w IS NOT NULL AND nr_seq_lote_w::text <> '') then
	delete	FROM ptu_camara_fatura
	where	nr_seq_camara = nr_seq_lote_w;

	delete	FROM ptu_camara_compensacao
	where	nr_sequencia = nr_seq_lote_w;
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_desfazer_lote_a600 ( nr_seq_lote_camara_p bigint, nm_usuario_p text) FROM PUBLIC;

