-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE schematic4test_checkschedule (QTD_SCHEDULE_P INOUT text, NR_SEQ_SCHEDULE_P bigint) AS $body$
DECLARE

QTD_SCHEDULE_W varchar(2);


BEGIN
  --procedure that return amount of schedules
  SELECT COUNT(NR_SEQUENCIA) QTD
      INTO STRICT QTD_SCHEDULE_W
  FROM SCHEM_TEST_SCHEDULE 
  WHERE NR_SEQUENCIA = QTD_SCHEDULE_P;

  QTD_SCHEDULE_P := TO_CHAR(QTD_SCHEDULE_W);
  EXCEPTION
  WHEN no_data_found THEN
    RAISE NOTICE 'Erro: Data not found';
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE schematic4test_checkschedule (QTD_SCHEDULE_P INOUT text, NR_SEQ_SCHEDULE_P bigint) FROM PUBLIC;

