-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE obter_codigo_usuario_atend ( nr_atendimento_p bigint, cd_convenio_p bigint, cd_usuario_convenio_p INOUT text, cd_plano_p INOUT text, nr_seq_interno_p INOUT bigint) AS $body$
DECLARE


cd_usuario_convenio_w		varchar(30);
cd_plano_w			varchar(10);
nr_seq_interno_w		bigint;
dt_inicio_vigencia_w		timestamp;

C01 CURSOR FOR
	SELECT	a.cd_usuario_convenio,
		a.cd_plano_convenio,
		a.nr_seq_interno
	from	atend_categoria_convenio a
	where	a.nr_atendimento	= nr_atendimento_p
	and	a.cd_convenio		= cd_convenio_p
	and	a.dt_inicio_vigencia	= dt_inicio_vigencia_w
	order by
		a.nr_seq_interno;


BEGIN
select	max(x.dt_inicio_vigencia)
into STRICT	dt_inicio_vigencia_w
from	atend_categoria_convenio x
where	x.nr_atendimento	= nr_atendimento_p
and	x.cd_convenio		= cd_convenio_p;

open C01;
loop
fetch C01 into
	cd_usuario_convenio_w,
	cd_plano_w,
	nr_seq_interno_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin
	cd_usuario_convenio_w	:= cd_usuario_convenio_w;
	cd_plano_w		:= cd_plano_w;
	nr_seq_interno_w	:= nr_seq_interno_w;
	end;
end loop;
close C01;

cd_usuario_convenio_p	:= cd_usuario_convenio_w;
cd_plano_p		:= cd_plano_w;
nr_seq_interno_p	:= nr_seq_interno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE obter_codigo_usuario_atend ( nr_atendimento_p bigint, cd_convenio_p bigint, cd_usuario_convenio_p INOUT text, cd_plano_p INOUT text, nr_seq_interno_p INOUT bigint) FROM PUBLIC;

