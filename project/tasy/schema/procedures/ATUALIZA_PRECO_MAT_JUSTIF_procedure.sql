-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE atualiza_preco_mat_justif ( cd_tab_preco_mat_p bigint, cd_material_p bigint, cd_estabelecimento_p bigint, dt_inicio_vigencia_p timestamp, nr_seq_motivo_p bigint, ds_justificativa_p text, nm_usuario_p text) AS $body$
DECLARE


nr_seq_motivo_w	bigint;


BEGIN

if (nr_seq_motivo_p <> 0) then
	nr_seq_motivo_w	:= nr_seq_motivo_p;
else
	nr_seq_motivo_w	:= null;
end if;

if (coalesce(nr_seq_motivo_w,0) <> 0) then
	insert into preco_mat_justificativa(
		nr_sequencia,
		cd_estabelecimento,
		dt_vigencia,
		dt_atualizacao,
		nm_usuario,
		dt_atualizacao_nrec,
		nm_usuario_nrec,
		cd_tab_preco_mat,
		cd_material,
		ds_justificativa,
		nr_seq_motivo
	) values (
		nextval('preco_mat_justificativa_seq'),
		cd_estabelecimento_p,
		dt_inicio_vigencia_p,
		clock_timestamp(),
		nm_usuario_p,
		clock_timestamp(),
		nm_usuario_p,
		cd_tab_preco_mat_p,
		cd_material_p,
		ds_justificativa_p,
		nr_seq_motivo_w
	);
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE atualiza_preco_mat_justif ( cd_tab_preco_mat_p bigint, cd_material_p bigint, cd_estabelecimento_p bigint, dt_inicio_vigencia_p timestamp, nr_seq_motivo_p bigint, ds_justificativa_p text, nm_usuario_p text) FROM PUBLIC;

