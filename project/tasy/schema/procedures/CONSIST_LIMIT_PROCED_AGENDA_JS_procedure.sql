-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consist_limit_proced_agenda_js ( cd_procedimento_p bigint, cd_perfil_p bigint,--tbyegmann em 30/03/2011 
 ie_origem_proced_p bigint, nr_seq_proc_interno_p bigint, cd_tipo_agenda_p bigint, dt_agenda_p timestamp, ds_procedimento_p text, ie_acao_regra_p INOUT text, ds_msg_p INOUT text) AS $body$
BEGIN
 
ie_acao_regra_p := consistir_limite_proced_agenda( 
	cd_procedimento_p, cd_perfil_p, ie_origem_proced_p, nr_seq_proc_interno_p, cd_tipo_agenda_p, dt_agenda_p, null, null, ie_acao_regra_p);
 
if (ie_acao_regra_p = 'A') or (ie_acao_regra_p = 'N') then 
	ds_msg_p	:= substr(obter_texto_tasy(47464, wheb_usuario_pck.get_nr_seq_idioma),1,255);
	ds_msg_p	:= replace(ds_msg_p, '#@DS_PROCEDIMENTO#@', ds_procedimento_p);
end if;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consist_limit_proced_agenda_js ( cd_procedimento_p bigint, cd_perfil_p bigint, ie_origem_proced_p bigint, nr_seq_proc_interno_p bigint, cd_tipo_agenda_p bigint, dt_agenda_p timestamp, ds_procedimento_p text, ie_acao_regra_p INOUT text, ds_msg_p INOUT text) FROM PUBLIC;

