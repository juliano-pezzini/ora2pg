-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_ajustar_seq_empresa_benef ( cd_estabelecimento_p bigint, nm_usuario_p text) AS $body$
DECLARE


nr_seq_segurado_w		bigint;
cd_operadora_empresa_w		bigint;
nr_sequencial_empresa_w		bigint;

C01 CURSOR FOR
	SELECT	nr_sequencia nr_seq_segurado,
		cd_operadora_empresa
	from	pls_segurado
	where	(cd_operadora_empresa IS NOT NULL AND cd_operadora_empresa::text <> '')
	and	(dt_liberacao IS NOT NULL AND dt_liberacao::text <> '')
	and	coalesce(nr_sequencial_empresa::text, '') = ''
	and	cd_estabelecimento	= cd_estabelecimento_p;

TYPE 		fetch_array IS TABLE OF c01%ROWTYPE;
s_array 	fetch_array;
i		integer := 1;
type Vetor is table of fetch_array index by integer;
Vetor_c01_w			Vetor;


BEGIN
/*
open c01;
loop
FETCH C01 BULK COLLECT INTO s_array LIMIT 1000;
	Vetor_c01_w(i) := s_array;
	i := i + 1;
EXIT WHEN C01%NOTFOUND;
END LOOP;
CLOSE C01;

for i in 1..Vetor_c01_w.COUNT loop
	s_array := Vetor_c01_w(i);
	for z in 1..s_array.COUNT loop
		begin

		nr_seq_segurado_w	:= s_array(z).nr_seq_segurado;
		cd_operadora_empresa_w	:= s_array(z).cd_operadora_empresa;

		select	max(nr_sequencial_empresa)
		into	nr_sequencial_empresa_w
		from	pls_segurado
		where	cd_operadora_empresa	= cd_operadora_empresa_w
		and	nr_sequencia		<> nr_seq_segurado_w;

		if	(nr_sequencial_empresa_w is null) then
			nr_sequencial_empresa_w	:= 1;
		else
			nr_sequencial_empresa_w	:= nr_sequencial_empresa_w + 1;
		end if;

		update	pls_segurado
		set	nr_sequencial_empresa	= nr_sequencial_empresa_w,
			nm_usuario		= nm_usuario_p,
			dt_atualizacao		= sysdate
		where	nr_sequencia		= nr_seq_segurado_w;

		end;
	end loop;
	commit;
end loop;


commit;

*/
null;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_ajustar_seq_empresa_benef ( cd_estabelecimento_p bigint, nm_usuario_p text) FROM PUBLIC;

