-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gc_gerar_kit_estoque_agrup_js ( nr_seq_kit_estoque_p bigint, nr_cirurgia_p bigint, nr_prescricao_p bigint, ie_origem_inf_p text, cd_local_estoque_p bigint, cd_perfil_p bigint, cd_medico_p text, nr_doc_interno_p bigint, nm_usuario_p text, nr_doc_interno_aux_p bigint, ie_tipo_doc_interno_p text, ie_tipo_doc_interno_aux_p text) AS $body$
DECLARE

 
qt_kit_estoque_w		bigint;

BEGIN
 
 
if (nr_seq_kit_estoque_p IS NOT NULL AND nr_seq_kit_estoque_p::text <> '') then 
	begin	 
 
	select	count(*) 
	into STRICT 	qt_kit_estoque_w 
	from	kit_estoque_reg 
	where	nr_sequencia = nr_seq_kit_estoque_p 
	and 	coalesce(dt_liberacao::text, '') = '';
 
	if (qt_kit_estoque_w > 0) then 
		begin 
		CALL WHEB_MENSAGEM_PCK.Exibir_Mensagem_Abort(51025);
		end;
	end if;
	end;
end if;
 
CALL gerar_kit_estoque_agrup(nr_seq_kit_estoque_p, 
						nr_cirurgia_p, 
						nr_prescricao_p, 
						ie_origem_inf_p, 
						cd_local_estoque_p, 
						cd_perfil_p, 
						cd_medico_p, 
						nr_doc_interno_p, 
						nm_usuario_p, 
						nr_doc_interno_aux_p, 
						ie_tipo_doc_interno_p, 
						ie_tipo_doc_interno_aux_p);
 
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gc_gerar_kit_estoque_agrup_js ( nr_seq_kit_estoque_p bigint, nr_cirurgia_p bigint, nr_prescricao_p bigint, ie_origem_inf_p text, cd_local_estoque_p bigint, cd_perfil_p bigint, cd_medico_p text, nr_doc_interno_p bigint, nm_usuario_p text, nr_doc_interno_aux_p bigint, ie_tipo_doc_interno_p text, ie_tipo_doc_interno_aux_p text) FROM PUBLIC;

