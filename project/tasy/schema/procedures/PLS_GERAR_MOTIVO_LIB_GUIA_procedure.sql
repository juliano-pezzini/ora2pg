-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_gerar_motivo_lib_guia ( nr_seq_motivo_p bigint, nr_opcao_guia_p bigint, ie_opcao_tabela_p text, nr_seq_guia_p bigint, ds_observacao_p text, nr_seq_guia_proc_p bigint, nr_seq_guia_mat_p bigint, ie_liberar_total_p text, qt_dia_autorizado_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) AS $body$
BEGIN
if (cd_estabelecimento_p IS NOT NULL AND cd_estabelecimento_p::text <> '')and (nr_opcao_guia_p IS NOT NULL AND nr_opcao_guia_p::text <> '')and (ie_opcao_tabela_p IS NOT NULL AND ie_opcao_tabela_p::text <> '')and (nm_usuario_p IS NOT NULL AND nm_usuario_p::text <> '')then 
	begin 
	if (coalesce(nr_seq_motivo_p::text, '') = '') then 
		begin 
		CALL wheb_mensagem_pck.exibir_mensagem_abort(52820,'');
		--Informe o motivo da liberação! 
		end;
	end if;
	 
	if (nr_opcao_guia_p <> 3) then 
		begin 
		CALL pls_liberar_guia(nr_seq_guia_p, nr_seq_guia_proc_p, nr_seq_guia_mat_p, ie_liberar_total_p, cd_estabelecimento_p, nm_usuario_p,null,null);	
		 
		if (nr_opcao_guia_p = 0) then 
			begin 
			insert into pls_guia_motivo_lib( 
					nr_sequencia, 
					dt_atualizacao, 
					nm_usuario, 
					nr_seq_guia, 
					nr_seq_motivo, 
					ds_observacao, 
					nm_usuario_liberacao, 
					dt_liberacao) 
				values (	 
					nextval('pls_guia_motivo_lib_seq'), 
					clock_timestamp(), 
					nm_usuario_p, 
					nr_seq_guia_p, 
					nr_seq_motivo_p, 
					ds_observacao_p, 
					nm_usuario_p, 
					clock_timestamp());
			end;
		elsif (nr_opcao_guia_p = 1) and (ie_opcao_tabela_p = 'P') then 
			begin 
			insert into pls_guia_motivo_lib( 
					nr_sequencia, 
					dt_atualizacao, 
					nm_usuario, 
					nr_seq_guia_proc, 
					nr_seq_motivo, 
					ds_observacao, 
					nm_usuario_liberacao, 
					dt_liberacao) 
				values ( 
					nextval('pls_guia_motivo_lib_seq'), 
					clock_timestamp(), 
					nm_usuario_p, 
					nr_seq_guia_proc_p, 
					nr_seq_motivo_p, 
					ds_observacao_p, 
					nm_usuario_p, 
					clock_timestamp());		
			end;
		elsif (nr_opcao_guia_p = 1) and (ie_opcao_tabela_p = 'M') then 
			begin 
			insert into pls_guia_motivo_lib( 
					nr_sequencia, 
					dt_atualizacao, 
					nm_usuario, 
					nr_seq_guia_mat, 
					nr_seq_motivo, 
					ds_observacao, 
					nm_usuario_liberacao, 
					dt_liberacao) 
				values ( 
					nextval('pls_guia_motivo_lib_seq'), 
					clock_timestamp(), 
					nm_usuario_p, 
					nr_seq_guia_mat_p, 
					nr_seq_motivo_p, 
					ds_observacao_p, 
					nm_usuario_p, 
					clock_timestamp());	
			end;
		end if;
		end;
	else		 
		begin 
		CALL pls_liberar_guia_manual(nr_seq_guia_p, nr_seq_motivo_p, ds_observacao_p, qt_dia_autorizado_p, nm_usuario_p);
		end;
	end if;	
	end;
end if;
commit;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_gerar_motivo_lib_guia ( nr_seq_motivo_p bigint, nr_opcao_guia_p bigint, ie_opcao_tabela_p text, nr_seq_guia_p bigint, ds_observacao_p text, nr_seq_guia_proc_p bigint, nr_seq_guia_mat_p bigint, ie_liberar_total_p text, qt_dia_autorizado_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) FROM PUBLIC;

