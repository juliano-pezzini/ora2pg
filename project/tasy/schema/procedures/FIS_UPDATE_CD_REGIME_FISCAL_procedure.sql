-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE fis_update_cd_regime_fiscal ( cd_pessoa_fisica_p pessoa_fisica.cd_pessoa_fisica%type, nm_usuario_p text, cd_regime_fiscal_p pessoa_fisica_aux.cd_regime_fiscal%type) AS $body$
DECLARE


nr_seq_pessoa_fisica_aux_w 	pessoa_fisica_aux.nr_sequencia%type;
nr_seq_pessoa_aux_w 		pessoa_fisica_aux.nr_sequencia%type;
ie_possui_aux_w			varchar(1);


BEGIN

select 	coalesce(max(nr_sequencia), -1)
into STRICT 	nr_seq_pessoa_fisica_aux_w
from 	pessoa_fisica_aux
where 	cd_pessoa_fisica = cd_pessoa_fisica_p;

if (nr_seq_pessoa_fisica_aux_w = -1) then

	insert into pessoa_fisica_aux(
		nr_sequencia,
		dt_atualizacao,
		nm_usuario,
		dt_atualizacao_nrec,
		nm_usuario_nrec,
		cd_pessoa_fisica,
		cd_regime_fiscal
	) values (
		nextval('pessoa_fisica_aux_seq'),
		clock_timestamp(),
		nm_usuario_p,
		clock_timestamp(),
		nm_usuario_p,
		cd_pessoa_fisica_p,
		cd_regime_fiscal_p
	);

else
	update 	pessoa_fisica_aux
	set 	cd_regime_fiscal = cd_regime_fiscal_p,
		dt_atualizacao_nrec = clock_timestamp(),
		nm_usuario_nrec = nm_usuario_p
	where 	cd_pessoa_fisica = cd_pessoa_fisica_p;

end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE fis_update_cd_regime_fiscal ( cd_pessoa_fisica_p pessoa_fisica.cd_pessoa_fisica%type, nm_usuario_p text, cd_regime_fiscal_p pessoa_fisica_aux.cd_regime_fiscal%type) FROM PUBLIC;

