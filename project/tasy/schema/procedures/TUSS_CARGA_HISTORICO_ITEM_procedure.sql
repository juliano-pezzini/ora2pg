-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE tuss_carga_historico_item ( nr_seq_historico_p text, cd_item_tuss_p text, ds_item_tuss_p text, ds_tipo_acao_hist_p text, ie_terminologia_p text, dt_inicio_vigencia_p timestamp, dt_fim_vigencia_p timestamp, dt_implantacao_p timestamp, nm_usuario_p text) AS $body$
BEGIN

begin
insert into tuss_historico_item(nr_sequencia,
	nr_seq_historico,
	dt_atualizacao,
	nm_usuario,
	dt_atualizacao_nrec,
	nm_usuario_nrec,
	cd_item_tuss,
	ds_item_tuss,
	ds_tipo_acao_hist,
	ie_terminologia,
	dt_inicio_vigencia,
	dt_final_vigencia,
	dt_importacao)
values (nextval('tuss_historico_item_seq'),
	nr_seq_historico_p,
	clock_timestamp(),
	substr(nm_usuario_p,1,15),
	clock_timestamp(),
	substr(nm_usuario_p,1,15),
	substr(cd_item_tuss_p,1,30),
	substr(rtrim(ltrim(ds_item_tuss_p)),1,255),
	substr(rtrim(ltrim(ds_tipo_acao_hist_p)),1,255),
	substr(rtrim(ltrim(ie_terminologia_p)),1,15),
	dt_inicio_vigencia_p,
	dt_fim_vigencia_p,
	dt_implantacao_p);
end;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE tuss_carga_historico_item ( nr_seq_historico_p text, cd_item_tuss_p text, ds_item_tuss_p text, ds_tipo_acao_hist_p text, ie_terminologia_p text, dt_inicio_vigencia_p timestamp, dt_fim_vigencia_p timestamp, dt_implantacao_p timestamp, nm_usuario_p text) FROM PUBLIC;

