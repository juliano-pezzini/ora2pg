-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE ctb_gerar_interf_k210_ecd ( nr_seq_controle_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint, dt_inicio_p timestamp, dt_fim_p timestamp, cd_empresa_p bigint, cd_conta_contabil_p text, qt_linha_p INOUT bigint, nr_sequencia_p INOUT bigint) AS $body$
DECLARE


ie_ind_movimento_w	varchar(2);
ds_arquivo_w		varchar(4000);
ds_compl_arquivo_w	varchar(4000);
ds_linha_w		varchar(8000);
nr_linha_w		bigint 	:= qt_linha_p;
nr_seq_registro_w		bigint 	:= nr_sequencia_p;
sep_w			varchar(1) 	:= '|';
tp_registro_w		varchar(15) 	:= 'K210';
cd_empresa_w		grupo_emp_estrutura.cd_empresa%type;
cd_empresa_ww		conta_contabil.cd_empresa%type;
cd_conta_contabil_w	varchar(40);
cd_classificacao_w		conta_contabil.cd_classificacao%type;
ie_apres_conta_ctb_w	ctb_regra_sped.ie_apres_conta_ctb%type;

c01 CURSOR FOR
	SELECT	a.cd_empresa
	from	grupo_emp_estrutura a
	where	a.nr_seq_grupo	= holding_pck.get_grupo_emp_estrut_vigente(cd_empresa_p)
	and     a.cd_empresa <> cd_empresa_p
	order by cd_empresa;

c02 CURSOR FOR
	SELECT	cc1.cd_empresa,
		cc1.cd_conta_contabil,
		substr(ctb_obter_classif_conta(cc1.cd_conta_contabil, cc1.cd_classificacao, dt_fim_p),1,40) cd_classificacao  --- apenas analíticas  'A,1.01.01' 
	from	conta_contabil cc1
	where	cc1.cd_empresa = cd_empresa_w  --(filha)
	and	cc1.cd_conta_referencia = cd_conta_contabil_p  -- Conta Contábil da Controladora
	and	cc1.ie_tipo = 'A'
	and cc1.ie_situacao = 'A';


BEGIN

select	coalesce(max(a.ie_apres_conta_ctb),'CD')
into STRICT	ie_apres_conta_ctb_w
from 	ctb_regra_sped a,
		ctb_sped_controle b
where	b.nr_seq_regra_sped 	= a.nr_sequencia
and	b.nr_sequencia		= nr_seq_controle_p;

open C01;
loop
fetch C01 into
	cd_empresa_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin

	open c02;
	loop
	fetch c02 into	
		cd_empresa_ww,
		cd_conta_contabil_w,
		cd_classificacao_w;
	EXIT WHEN NOT FOUND; /* apply on c02 */
		begin
		
		if (ie_apres_conta_ctb_w = 'CL') then
			begin
			cd_conta_contabil_w	:= cd_classificacao_w;
			end;
		elsif (ie_apres_conta_ctb_w = 'CP') then
			begin
			cd_conta_contabil_w	:= substr(replace(cd_classificacao_w,'.',''),1,40);
			end;
		end if;

		ds_linha_w	:= substr(sep_w || tp_registro_w 	|| sep_w ||
					cd_empresa_ww 		|| sep_w ||
					cd_conta_contabil_w 	|| sep_w,1,8000);

		ds_arquivo_w		:= substr(ds_linha_w,1,4000);
		ds_compl_arquivo_w	:= substr(ds_linha_w,4001,4000);
		nr_seq_registro_w		:= nr_seq_registro_w + 1;
		nr_linha_w		:= nr_linha_w + 1;

		insert into ctb_sped_registro(
				nr_sequencia,
				ds_arquivo,
				dt_atualizacao,
				nm_usuario,
				dt_atualizacao_nrec,
				nm_usuario_nrec,
				nr_seq_controle_sped,
				ds_arquivo_compl,
				cd_registro,
				nr_linha)
			values (
				nr_seq_registro_w,
				ds_arquivo_w,
				clock_timestamp(),
				nm_usuario_p,
				clock_timestamp(),
				nm_usuario_p,
				nr_seq_controle_p,
				ds_compl_arquivo_w,
				tp_registro_w,
				nr_linha_w);

		end;
	end loop;
	close c02;

	end;
end loop;
close C01;

commit;
qt_linha_p	:= nr_linha_w;
nr_sequencia_p	:= nr_seq_registro_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE ctb_gerar_interf_k210_ecd ( nr_seq_controle_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint, dt_inicio_p timestamp, dt_fim_p timestamp, cd_empresa_p bigint, cd_conta_contabil_p text, qt_linha_p INOUT bigint, nr_sequencia_p INOUT bigint) FROM PUBLIC;

