-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_w_nota_arquivo (cd_estabelecimento_p text, dt_inicio_p timestamp, dt_fim_p timestamp, nm_usuario_p text, ie_registro_p text) AS $body$
DECLARE

				 
cd_empresa_w	bigint;


BEGIN 
 
delete from w_inss_direp_arquivo;
commit;
 
select	cd_empresa 
into STRICT	cd_empresa_w 
from	estabelecimento 
where	cd_estabelecimento = cd_estabelecimento_p;
 
if (ie_registro_p = '0') then 
	CALL nota_saida_entrada(cd_estabelecimento_p, dt_inicio_p, dt_fim_p, nm_usuario_p);
end if;
 
if (ie_registro_p = '1') then 
	CALL nota_saida_entrada_item(cd_estabelecimento_p, dt_inicio_p, dt_fim_p, nm_usuario_p);
end if;	
 
if (ie_registro_p = '2') then 	 
	CALL nota_entrada_terceiro(cd_estabelecimento_p, dt_inicio_p, dt_fim_p, nm_usuario_p);
end if;
 
if (ie_registro_p = '3') then 
	CALL item_entrada_terceiro( cd_estabelecimento_p, dt_inicio_p, dt_fim_p, nm_usuario_p);
end if;	
 
if (ie_registro_p = '4') then	 
	CALL nota_fiscal_servico(cd_estabelecimento_p, dt_inicio_p, dt_fim_p, nm_usuario_p);
end if;
 
if (ie_registro_p = '5') then 
	CALL nota_fiscal_servico_item( cd_estabelecimento_p, dt_inicio_p, dt_fim_p, nm_usuario_p);
end if;	
 
if (ie_registro_p = '6') then 
	CALL nota_fiscal_servico_terceiro(cd_estabelecimento_p, dt_inicio_p, dt_fim_p, nm_usuario_p);
end if;	
 
if (ie_registro_p = '7') then 
	CALL arquivo_pj_pf(cd_estabelecimento_p, dt_inicio_p, dt_fim_p, nm_usuario_p);
end if;	
 
if (ie_registro_p = '8') then 
	CALL arquivo_plano_conta(cd_estabelecimento_p, dt_inicio_p, dt_fim_p, nm_usuario_p, cd_empresa_w);
end if;	
 
if (ie_registro_p = '9') then 
	CALL arquivo_natureza_operacao(cd_estabelecimento_p, dt_inicio_p, dt_fim_p, nm_usuario_p);
end if;	
 
if (ie_registro_p = '10') then 
	CALL arquivo_mercadoria_servico(cd_estabelecimento_p, dt_inicio_p, dt_fim_p, nm_usuario_p);
end if;	
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_w_nota_arquivo (cd_estabelecimento_p text, dt_inicio_p timestamp, dt_fim_p timestamp, nm_usuario_p text, ie_registro_p text) FROM PUBLIC;

