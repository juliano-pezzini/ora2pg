-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE reg_record_revision_pdf_hash ( nr_seq_revision_p bigint, ds_hash_pdf_p text, nm_usuario_p text ) AS $body$
BEGIN

if (nr_seq_revision_p IS NOT NULL AND nr_seq_revision_p::text <> '') then

	update	reg_version_revision
	set		nm_usuario = nm_usuario_p,
			dt_atualizacao = clock_timestamp(),
			ds_hash_pdf = ds_hash_pdf_p
	where	nr_sequencia = nr_seq_revision_p;

end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE reg_record_revision_pdf_hash ( nr_seq_revision_p bigint, ds_hash_pdf_p text, nm_usuario_p text ) FROM PUBLIC;

