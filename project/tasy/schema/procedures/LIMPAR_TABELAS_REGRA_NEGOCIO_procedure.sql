-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE limpar_tabelas_regra_negocio () AS $body$
DECLARE


ds_erro_w	varchar(512);


BEGIN
/*
begin
	gravar_processo_longo('LIMPAR_AGENDA_CONSULTA','LIMPAR_TABELAS_REGRA_NEGOCIO',-1);
	limpar_agenda_consulta;
exception
when others then
	ds_erro_w := substr(SQLERRM(sqlcode),1,512);
	tratar_erro_banco(ds_erro_w);
	GRAVAR_LOG_TASY(88936,'LIMPAR_AGENDA_CONSULTA','TASY');
end;


begin
	gravar_processo_longo('LIMPAR_AGENDA_PACIENTE','LIMPAR_TABELAS_REGRA_NEGOCIO',-1);
	limpar_agenda_paciente;
exception
when others then
	ds_erro_w := substr(SQLERRM(sqlcode),1,512);
	tratar_erro_banco(ds_erro_w);
	GRAVAR_LOG_TASY(88936,'LIMPAR_AGENDA_PACIENTE','TASY');
end;

begin
	gravar_processo_longo('BAIXAR_REQUISICAO_PENDENTE','LIMPAR_TABELAS_REGRA_NEGOCIO',-1);
	baixar_requisicao_pendente(30);
exception
when others then
	ds_erro_w := substr(SQLERRM(sqlcode),1,512);
	tratar_erro_banco(ds_erro_w);
	GRAVAR_LOG_TASY(88936,'BAIXAR_REQUISICAO_PENDENTE(30)','TASY');
end;*/
ds_erro_w := null;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE limpar_tabelas_regra_negocio () FROM PUBLIC;

