-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE inutilizar_hemocomp_prod_lista ( nr_seq_producao_lista_p text, nr_seq_inutilizacao_p bigint, nm_usuario_p text, nm_pasta_p text, ds_msg_p INOUT text) AS $body$
DECLARE


nr_seq_producao_lista_w		varchar(2000);
nr_seq_producao_w		bigint;


BEGIN

nr_seq_producao_lista_w	:= nr_seq_producao_lista_p;

while (nr_seq_producao_lista_w IS NOT NULL AND nr_seq_producao_lista_w::text <> '') loop
	begin
	nr_seq_producao_w		:= substr(nr_seq_producao_lista_w, 1, position(',' in nr_seq_producao_lista_w) - 1);
	nr_seq_producao_lista_w		:= substr(nr_seq_producao_lista_w, position(',' in nr_seq_producao_lista_w) + 1, length(nr_seq_producao_lista_w));

	CALL inutilizar_hemocomp_producao(
		nr_seq_producao_w,
		nr_seq_inutilizacao_p,
		nm_pasta_p,
		nm_usuario_p);
	end;
end loop;

ds_msg_p	:= obter_texto_tasy(84430, wheb_usuario_pck.get_nr_seq_idioma);

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE inutilizar_hemocomp_prod_lista ( nr_seq_producao_lista_p text, nr_seq_inutilizacao_p bigint, nm_usuario_p text, nm_pasta_p text, ds_msg_p INOUT text) FROM PUBLIC;

