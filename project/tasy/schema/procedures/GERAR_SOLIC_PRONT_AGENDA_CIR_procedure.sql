-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_solic_pront_agenda_cir (cd_pessoa_fisica_p text, nr_seq_agenda_cir_p bigint, cd_agenda_p bigint, dt_agenda_p timestamp, nm_usuario_p text) AS $body$
DECLARE


nr_prontuario_w			bigint;
nr_seq_prontuario_w		bigint;
nr_seq_solic_w			bigint;
cd_setor_atendimento_w		bigint;
cd_estabelecimento_w		bigint;
qt_existe_cpi_w			bigint;
nr_sequencia_w			bigint;


BEGIN

select	max(cd_setor_exclusivo),
	max(cd_estabelecimento)
into STRICT	cd_setor_atendimento_w,
	cd_estabelecimento_w
from	agenda
where	cd_agenda		= cd_agenda_p;

/*select	nvl(max(nr_prontuario), 0)
into	nr_prontuario_w
from	pessoa_fisica
where 	cd_pessoa_fisica	= cd_pessoa_fisica_p;
Matheus OS 182242 subs pela function abaixo */
nr_prontuario_w	:= coalesce(obter_prontuario_pf(cd_estabelecimento_w, cd_pessoa_fisica_p),0);

select	count(*)
into STRICT	qt_existe_cpi_w
from	same_cpi_prontuario
where	cd_pessoa_fisica	= cd_pessoa_fisica_p
and	nr_prontuario	= nr_prontuario_w
and	cd_estabelecimento	= cd_estabelecimento_w;

if (qt_existe_cpi_w = 0) and (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') and (nr_prontuario_w > 0) then

	select	nextval('same_cpi_prontuario_seq')
	into STRICT	nr_sequencia_w
	;

	insert into same_cpi_prontuario(
		nr_sequencia,
		cd_estabelecimento,
		dt_atualizacao,
		nm_usuario,
		nr_prontuario,
		cd_pessoa_fisica,
		qt_volume,
		ds_localizacao,
		dt_atualizacao_nrec,
		nm_usuario_nrec)
	values (	nr_sequencia_w,
		cd_estabelecimento_w,
		clock_timestamp(),
		nm_usuario_p,
		nr_prontuario_w,
		cd_pessoa_fisica_p,
		1,
		null,
		clock_timestamp(),
		nm_usuario_p);
end if;




select	coalesce(max(nr_sequencia),0)
into STRICT	nr_seq_prontuario_w
from	same_cpi_prontuario
where	nr_prontuario		= nr_prontuario_w;


if (nr_seq_prontuario_w > 0) and (cd_setor_atendimento_w > 0) and (cd_estabelecimento_w > 0) then
	begin

	select	coalesce(max(nr_sequencia),0)
	into STRICT	nr_seq_solic_w
	from	same_cpi_solic
	where	cd_estabelecimento		= cd_estabelecimento_w
	and		nr_prontuario			= nr_prontuario_w
	and		cd_setor_atendimento	= cd_setor_atendimento
	and		cd_agenda				= cd_agenda_p
	and		coalesce(dt_cancelamento::text, '') = ''
	and	  ESTABLISHMENT_TIMEZONE_UTILS.startOfDay(dt_desejada)	= ESTABLISHMENT_TIMEZONE_UTILS.startOfDay(dt_agenda_p);

	if (nr_seq_solic_w = 0) then
		begin

		select	nextval('same_cpi_solic_seq')
		into STRICT	nr_seq_solic_w
		;

		insert 	into same_cpi_solic(nr_sequencia,
			cd_estabelecimento,
			nr_prontuario,
			dt_atualizacao,
			nm_usuario,
			cd_setor_atendimento,
			dt_solicitacao,
			dt_desejada,
			ie_status_solic,
			nm_usuario_solic,
			cd_agenda,
			nr_seq_agenda_cir)
		values (nr_seq_solic_w,
			cd_estabelecimento_w,
			nr_prontuario_w,
			clock_timestamp(),
			nm_usuario_p,
			cd_setor_atendimento_w,
			clock_timestamp(),
			dt_agenda_p,
			'A',
			nm_usuario_p,
			cd_agenda_p,
			nr_seq_agenda_cir_p);
		end;
	end if;

	end;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_solic_pront_agenda_cir (cd_pessoa_fisica_p text, nr_seq_agenda_cir_p bigint, cd_agenda_p bigint, dt_agenda_p timestamp, nm_usuario_p text) FROM PUBLIC;

