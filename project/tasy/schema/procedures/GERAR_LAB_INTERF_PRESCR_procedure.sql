-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_lab_interf_prescr (nr_prescricao_p bigint, nm_usuario_p text, nr_seq_arquivo_p INOUT bigint) AS $body$
DECLARE


nr_seq_lab_interf_w		bigint;

BEGIN
select 	coalesce(max(nr_seq_arquivo),0)+1
into STRICT	nr_seq_arquivo_p
from   	lab_prescr_interf
where  	trunc(dt_atualizacao_nrec,'dd') = trunc(clock_timestamp(), 'dd')
AND	dt_atualizacao_nrec > clock_timestamp() - interval '1 days';

select	nextval('lab_prescr_interf_seq')
into STRICT	nr_seq_lab_interf_w
;

insert into lab_prescr_interf(
	nr_sequencia,
        dt_atualizacao,
	nm_usuario,
	dt_atualizacao_nrec,
	nm_usuario_nrec,
	nr_prescricao,
	nr_seq_arquivo)
values (
	nr_seq_lab_interf_w,
	clock_timestamp(),
	nm_usuario_p,
	clock_timestamp(),
	nm_usuario_p,
	nr_prescricao_p,
	nr_seq_arquivo_p);


commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_lab_interf_prescr (nr_prescricao_p bigint, nm_usuario_p text, nr_seq_arquivo_p INOUT bigint) FROM PUBLIC;

