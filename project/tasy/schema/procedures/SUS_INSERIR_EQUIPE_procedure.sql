-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE sus_inserir_equipe ( esf_cmp_p text, esf_ibge_p text, esf_cnes_p text, esf_seq_p text, esf_area_p text, esf_nome_p text, esf_tipo_p text, dt_inicial_p text, dt_final_p text, esf_aux_p text, nm_usuario_p text) AS $body$
DECLARE


dt_competencia_w	timestamp;
dt_inicial_w		timestamp;
dt_final_w		timestamp;


BEGIN

dt_competencia_w 	:= to_date('01/'||substr(esf_cmp_p,5,2)||'/'||substr(esf_cmp_p,1,4));
if (dt_final_p <> '') then
	begin
	if (substr(dt_final_p,1,4) > '2999') then
		dt_final_w 	:= to_date(substr(dt_final_p,7,2)||'/'||substr(dt_final_p,5,2)||'/2999');
	else
		dt_final_w 	:= to_date(substr(dt_final_p,7,2)||'/'||substr(dt_final_p,5,2)||'/'||substr(dt_final_p,1,4));
	end if;
	end;
else
	dt_final_w 	:= to_date('31/12/2999');
end if;

if (length((dt_inicial_p)::numeric ) < 8) then
	dt_inicial_w 	:= to_date('01/01/2009');
else
	begin
	dt_inicial_w 	:= to_date(substr(dt_inicial_p,7,2)||'/'||substr(dt_inicial_p,5,2)||'/'||substr(dt_inicial_p,1,4));
	exception
	when others then
		dt_inicial_w 	:= to_date('01/01/2009');
	end;
end if;

insert into sus_equipe(
		nr_sequencia,
		cd_area_equipe,
		cd_campo_aux,
		cd_municipio_ibge,
		cd_tipo_equipe,
		dt_atualizacao,
		dt_atualizacao_nrec,
		dt_competencia,
		dt_final_vigencia,
		dt_inicio_vigencia,
		nm_equipe,
		nm_usuario,
		nm_usuario_nrec,
		nr_cnes_equipe,
		nr_seq_equipe
	)
	values (	nextval('sus_equipe_seq'),
		esf_area_p,
		esf_aux_p,
		esf_ibge_p,
		esf_tipo_p,
		clock_timestamp(),
		clock_timestamp(),
		dt_competencia_w,
		dt_final_w,
		dt_inicial_w,
		esf_nome_p,
		nm_usuario_p,
		nm_usuario_p,
		esf_cnes_p,
		esf_seq_p
		);

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE sus_inserir_equipe ( esf_cmp_p text, esf_ibge_p text, esf_cnes_p text, esf_seq_p text, esf_area_p text, esf_nome_p text, esf_tipo_p text, dt_inicial_p text, dt_final_p text, esf_aux_p text, nm_usuario_p text) FROM PUBLIC;

