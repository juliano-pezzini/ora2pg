-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_todos_agentes_medic ( cd_estabelecimento_p bigint, nr_seq_agente_p bigint, nr_cirurgia_p bigint, cd_lista_material_p text, cd_profissional_p text, nm_usuario_p text, nr_sequencia_p INOUT bigint, ie_via_aplicacao_p text, cd_unidade_medida_p text, nr_seq_pepo_p bigint) AS $body$
DECLARE


nr_seq_material_w	varchar(255);
nr_pos_virgula_w	bigint;
cd_material_w		bigint;
nr_sequencia_w		bigint;
nr_controle_loop_w	smallint := 0;


BEGIN
if (cd_lista_material_p IS NOT NULL AND cd_lista_material_p::text <> '') then
	begin
	nr_seq_material_w := cd_lista_material_p;
	while((nr_seq_material_w IS NOT NULL AND nr_seq_material_w::text <> '') and nr_controle_loop_w < 1000) loop
		begin
		nr_controle_loop_w 	:= nr_controle_loop_w + 1;
		nr_pos_virgula_w 	:= position(',' in nr_seq_material_w);
		if (nr_pos_virgula_w > 0) then
			begin
			cd_material_w := (substr(nr_seq_material_w, 1, nr_pos_virgula_w - 1))::numeric;
			nr_seq_material_w := substr(nr_seq_material_w, nr_pos_virgula_w + 1, length(nr_seq_material_w));
			if (cd_material_w > 0) then
				begin
				nr_sequencia_w := gerar_agentes_medicamentos(	cd_estabelecimento_p, nr_seq_agente_p, nr_cirurgia_p, cd_material_w, cd_profissional_p, nm_usuario_p, nr_sequencia_w, ie_via_aplicacao_p, cd_unidade_medida_p, null, nr_seq_pepo_p);
				end;
			end if;
			end;
		else
			nr_seq_material_w := null;
		end if;
		end;
	end loop;
	end;
else
	begin
	nr_sequencia_w := gerar_agentes_medicamentos(	cd_estabelecimento_p, nr_seq_agente_p, nr_cirurgia_p, cd_material_w, cd_profissional_p, nm_usuario_p, nr_sequencia_w, ie_via_aplicacao_p, cd_unidade_medida_p, null, nr_seq_pepo_p);
	end;
end if;

nr_sequencia_p := nr_sequencia_w;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_todos_agentes_medic ( cd_estabelecimento_p bigint, nr_seq_agente_p bigint, nr_cirurgia_p bigint, cd_lista_material_p text, cd_profissional_p text, nm_usuario_p text, nr_sequencia_p INOUT bigint, ie_via_aplicacao_p text, cd_unidade_medida_p text, nr_seq_pepo_p bigint) FROM PUBLIC;

