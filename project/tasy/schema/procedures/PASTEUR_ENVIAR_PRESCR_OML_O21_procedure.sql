-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';



--
-- dblink wrapper to call function pasteur_enviar_prescr_oml_o21 as an autonomous transaction
--
CREATE EXTENSION IF NOT EXISTS dblink;

CREATE OR REPLACE PROCEDURE pasteur_enviar_prescr_oml_o21 ( nr_prescricao_p bigint, nr_atendimento_p bigint, ie_opcao_p text, nr_seq_prescr_p bigint) AS $body$
DECLARE
	-- Change this to reflect the dblink connection string
	v_conn_str  text := format('port=%s dbname=%s user=%s', current_setting('port'), current_database(), current_user);
	v_query     text;

BEGIN
	v_query := 'CALL pasteur_enviar_prescr_oml_o21_atx ( ' || quote_nullable(nr_prescricao_p) || ',' || quote_nullable(nr_atendimento_p) || ',' || quote_nullable(ie_opcao_p) || ',' || quote_nullable(nr_seq_prescr_p) || ' )';
	PERFORM * FROM dblink(v_conn_str, v_query) AS p (ret boolean);

END;
$body$ LANGUAGE plpgsql SECURITY DEFINER;




CREATE OR REPLACE PROCEDURE pasteur_enviar_prescr_oml_o21_atx ( nr_prescricao_p bigint, nr_atendimento_p bigint, ie_opcao_p text, nr_seq_prescr_p bigint) AS $body$
DECLARE

  ds_sep_bv_w          varchar(100);
  ds_param_integ_hl7_w varchar(4000);
  qt_exames_w          bigint;
BEGIN
  BEGIN
    qt_exames_w            := lab_obter_qt_exames_prescr(nr_prescricao_p);
    IF (qt_exames_w         > 0) THEN
      ds_sep_bv_w          := obter_separador_bv;
      ds_param_integ_hl7_w := 'nr_prescricao=' || nr_prescricao_p || ds_sep_bv_w || 'ie_opcao=' || ie_opcao_p || ds_sep_bv_w || 'nr_seq_prescr=' || nr_seq_prescr_p || ds_sep_bv_w;
      CALL gravar_agend_integracao(868, ds_param_integ_hl7_w);
    END IF;
  EXCEPTION
  WHEN OTHERS THEN
    NULL;
  END;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pasteur_enviar_prescr_oml_o21 ( nr_prescricao_p bigint, nr_atendimento_p bigint, ie_opcao_p text, nr_seq_prescr_p bigint) FROM PUBLIC; -- REVOKE ALL ON PROCEDURE pasteur_enviar_prescr_oml_o21_atx ( nr_prescricao_p bigint, nr_atendimento_p bigint, ie_opcao_p text, nr_seq_prescr_p bigint) FROM PUBLIC;

