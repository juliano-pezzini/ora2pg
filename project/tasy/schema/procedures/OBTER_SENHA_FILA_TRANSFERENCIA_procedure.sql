-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE obter_senha_fila_transferencia (nr_atendimento_p bigint, ds_retorno_senha INOUT text, ds_retorno_fila INOUT text, ds_retorno_pac_senha_fila INOUT text) AS $body$
DECLARE


nr_seq_pac_senha_fila_w     varchar(10);


BEGIN

select  nr_seq_pac_senha_fila
into STRICT    nr_seq_pac_senha_fila_w
from    atendimento_paciente
where   nr_atendimento = nr_atendimento_p;

if (nr_seq_pac_senha_fila_w > 0) then

	ds_retorno_pac_senha_fila := nr_seq_pac_senha_fila_w;
	ds_retorno_senha := obter_dados_senha(nr_seq_pac_senha_fila_w, 'CD');
    ds_retorno_fila := obter_dados_senha(nr_seq_pac_senha_fila_w, 'SF');
	
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE obter_senha_fila_transferencia (nr_atendimento_p bigint, ds_retorno_senha INOUT text, ds_retorno_fila INOUT text, ds_retorno_pac_senha_fila INOUT text) FROM PUBLIC;

