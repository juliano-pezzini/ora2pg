-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_exec_job_proc_adic_versao ( nr_seq_proc_adic_p bigint, nm_usuario_p text) AS $body$
DECLARE


jobno			bigint;
ds_comando_job_w	varchar(2000);

C01 CURSOR FOR
	-- falhas is not null, significa que já foi executado pelo menos uma vez, se já foi executado ao menos uma vez, apaga. durante a execução ele fica null
	SELECT	job
	from	job_v
	where	comando	like 'pls_exec_proc_adic_versao_html%'
	and	(falhas IS NOT NULL AND falhas::text <> '');


BEGIN

/*
-- Verificar se a job já existe, se existir remove
for r_C01_w in C01 loop
	dbms_job.remove(r_C01_w.job);
	commit;
end loop;
*/
-- Alteração de status para Aguardando processamento
update	pls_processo_adic_versao
set	ie_status = '2'
where	nr_sequencia = nr_seq_proc_adic_p;

CALL pls_exec_proc_adic_versao_html(nr_seq_proc_adic_p,nm_usuario_p);

/*
ds_comando_job_w	:= 'pls_exec_proc_adic_versao_html(' ||	nr_seq_proc_adic_p || ',' ||
								pls_util_pck.aspas_w || nm_usuario_p || pls_util_pck.aspas_w || ');'; 

-- Vai iniciar a procedure em 2.5 segundos após a conclusão desta e sempre joga a próxima execução para daqui a 99999 dias
dbms_job.submit(jobno, ds_comando_job_w, sysdate + 2.5 * (1/24/60/60), 'sysdate + 99999');
*/
commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_exec_job_proc_adic_versao ( nr_seq_proc_adic_p bigint, nm_usuario_p text) FROM PUBLIC;

