-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE adicionar_lote_exame_js ( nr_seq_lote_p bigint, nr_prescricao_p bigint, nr_seq_prescr_p bigint, nm_usuario_p text) AS $body$
DECLARE


qt_lote_exame_w		bigint;


BEGIN

select 	count(*)
into STRICT	qt_lote_exame_w
from 	lab_lote_exame_item
where 	nr_prescricao = nr_prescricao_p
and 	nr_seq_prescr = nr_seq_prescr_p;

if (nr_seq_lote_p > 0) and (qt_lote_exame_w = 0) then

	insert into lab_lote_exame_item(
		nr_seq_lote,
		nr_prescricao,
		nr_seq_prescr,
		dt_atualizacao,
		nm_usuario)
	values (nr_seq_lote_p,
		nr_prescricao_p,
		nr_seq_prescr_p,
		clock_timestamp(),
		nm_usuario_p);
	commit;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE adicionar_lote_exame_js ( nr_seq_lote_p bigint, nr_prescricao_p bigint, nr_seq_prescr_p bigint, nm_usuario_p text) FROM PUBLIC;

