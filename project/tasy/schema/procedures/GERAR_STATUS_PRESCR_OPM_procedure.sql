-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_status_prescr_opm ( ie_momento_p text, nr_prescricao_p bigint, ie_opcao_p text, cd_estabelecimento_p bigint, nm_usuario_p text) AS $body$
DECLARE


nr_seq_status_prescr_opm_w	regra_status_prescr_opm.nr_seq_status_prescr_opm%type;


BEGIN

if (ie_momento_p IS NOT NULL AND ie_momento_p::text <> '') then
	select  max(nr_seq_status_prescr_opm)
	into STRICT	nr_seq_status_prescr_opm_w
	from    regra_status_prescr_opm where	ie_momento = ie_momento_p LIMIT 1;

	if (nr_seq_status_prescr_opm_w IS NOT NULL AND nr_seq_status_prescr_opm_w::text <> '') then
		update	prescr_medica
		set		nr_seq_status_opm = nr_seq_status_prescr_opm_w
		where	nr_prescricao = nr_prescricao_p;
		if (coalesce(wheb_usuario_pck.get_ie_commit, 'S') = 'S') then commit; end if;
	end if;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_status_prescr_opm ( ie_momento_p text, nr_prescricao_p bigint, ie_opcao_p text, cd_estabelecimento_p bigint, nm_usuario_p text) FROM PUBLIC;

