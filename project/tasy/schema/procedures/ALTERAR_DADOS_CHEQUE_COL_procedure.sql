-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE alterar_dados_cheque_col (cd_banco_p bigint, cd_agencia_p text, nr_conta_p text, nr_cheque_p text, nr_seq_cheque_p bigint, cd_banco_loc_p text default null) AS $body$
DECLARE


cd_banco_w banco.cd_banco_loc%type;
		

BEGIN
    if (cd_banco_loc_p IS NOT NULL AND cd_banco_loc_p::text <> '') then

        select obter_cd_banco(cd_banco_loc_p)
        into STRICT cd_banco_w
;

    else
        cd_banco_w := cd_banco_p;
    end if;

    CALL alterar_dados_cheque(cd_banco_w, cd_agencia_p, nr_conta_p, nr_cheque_p, nr_seq_cheque_p);
            				
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE alterar_dados_cheque_col (cd_banco_p bigint, cd_agencia_p text, nr_conta_p text, nr_cheque_p text, nr_seq_cheque_p bigint, cd_banco_loc_p text default null) FROM PUBLIC;

