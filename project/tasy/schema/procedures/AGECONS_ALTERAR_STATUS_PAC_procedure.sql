-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE agecons_alterar_status_pac (ds_lista_p text, nr_seq_status_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) AS $body$
DECLARE


ds_lista_w			varchar(4000);
tam_lista_w			bigint;
ie_pos_virgula_w		smallint;
nr_seq_agenda_w			agenda_consulta.nr_sequencia%type;
				

BEGIN

ds_lista_w 	:= ds_lista_p;

while(ds_lista_w IS NOT NULL AND ds_lista_w::text <> '')  loop
	begin
	tam_lista_w		:= length(ds_lista_w);
	ie_pos_virgula_w	:= position(',' in ds_lista_w);

	if (ie_pos_virgula_w <> 0) then
		nr_seq_agenda_w		:= (substr(ds_lista_w,1,(ie_pos_virgula_w - 1)))::numeric;
		ds_lista_w		:= substr(ds_lista_w,(ie_pos_virgula_w + 1),tam_lista_w);
	end if;

	CALL alterar_status_pac_agecons(nr_seq_agenda_w,
				   nr_seq_status_p,
				   nm_usuario_p);
	
	end;
	end loop;
	
commit;


commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE agecons_alterar_status_pac (ds_lista_p text, nr_seq_status_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) FROM PUBLIC;

