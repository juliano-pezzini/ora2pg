-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE alterar_medico_executor ( cd_medico_p text, nr_sequencia_p bigint, vl_parametro_p text, cd_especialidade_p bigint, ie_funcao_medico_p bigint, nm_usuario_p text) AS $body$
BEGIN

if (vl_parametro_p = 'N') then

	update	procedimento_paciente
	set	cd_medico_executor   = cd_medico_p,
		dt_atualizacao       = clock_timestamp(),
		nm_usuario           = nm_usuario_p
	where	nr_sequencia         = nr_sequencia_p;

else
	update	procedimento_paciente
	set	cd_medico_executor   = cd_medico_p,
		dt_atualizacao       = clock_timestamp(),
		nm_usuario           = nm_usuario_p,
		ie_funcao_medico     = ie_funcao_medico_p,
		cd_especialidade     = cd_especialidade_p
	where	nr_sequencia         = nr_sequencia_p;

end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE alterar_medico_executor ( cd_medico_p text, nr_sequencia_p bigint, vl_parametro_p text, cd_especialidade_p bigint, ie_funcao_medico_p bigint, nm_usuario_p text) FROM PUBLIC;

