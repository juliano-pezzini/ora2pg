-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE rop_gravar_w_rop_lote_movto ( cd_barra_roupa_p text, qt_roupa_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) AS $body$
DECLARE


nr_seq_lote_movto_w		bigint;
ie_grava_quantidade_w		varchar(1);
qt_roupa_w			double precision;


BEGIN

ie_grava_quantidade_w	:= coalesce((obter_valor_param_usuario(1301, 95, obter_perfil_ativo, nm_usuario_p, cd_estabelecimento_p)), 'N');
qt_roupa_w		:= qt_roupa_p;

if (ie_grava_quantidade_w = 'N') then
	qt_roupa_w := null;
end if;

select	nextval('w_rop_lote_movto_seq')
into STRICT	nr_seq_lote_movto_w
;

insert into w_rop_lote_movto(
	nr_sequencia,
	dt_atualizacao,
	nm_usuario,
	cd_barra_roupa,
	qt_roupa)
values (nr_seq_lote_movto_w,
	clock_timestamp(),
	nm_usuario_p,
	cd_barra_roupa_p,
	qt_roupa_w);

commit;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE rop_gravar_w_rop_lote_movto ( cd_barra_roupa_p text, qt_roupa_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) FROM PUBLIC;

