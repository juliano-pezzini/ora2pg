-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_evento_item_adep_proc (nr_seq_item_p text, ie_evento_p text, dt_evento_p timestamp, nm_usuario_p text) AS $body$
DECLARE


nr_seq_evento_w	bigint;


BEGIN
if (nr_seq_item_p IS NOT NULL AND nr_seq_item_p::text <> '') and (ie_evento_p IS NOT NULL AND ie_evento_p::text <> '') and (dt_evento_p IS NOT NULL AND dt_evento_p::text <> '') and (nm_usuario_p IS NOT NULL AND nm_usuario_p::text <> '') then

	select	nextval('adep_processo_evento_seq')
	into STRICT	nr_seq_evento_w
	;

	insert into adep_processo_evento(
		nr_sequencia,
		dt_atualizacao_nrec,
		nm_usuario_nrec,
		dt_atualizacao,
		nm_usuario,
		nr_seq_item,
		ie_evento,
		dt_evento,
		cd_pessoa_evento)
	values (
		nr_seq_evento_w,
		clock_timestamp(),
		nm_usuario_p,
		clock_timestamp(),
		nm_usuario_p,
		nr_seq_item_p,
		ie_evento_p,
		dt_evento_p,
		substr(obter_dados_usuario_opcao(nm_usuario_p,'C'),1,10));

end if;

if (coalesce(wheb_usuario_pck.get_ie_commit, 'S') = 'S') then commit; end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_evento_item_adep_proc (nr_seq_item_p text, ie_evento_p text, dt_evento_p timestamp, nm_usuario_p text) FROM PUBLIC;

