-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE sus_insert_pro_incr_origem (nr_sequencia_p bigint, cd_habilitacao_p bigint, pr_incremento_sa_p bigint, pr_incremento_sh_p bigint, pr_incremento_sp_p bigint, cd_procedimento_p bigint, dt_competencia_p timestamp, ie_versao_p text, nm_usuario_p text, cd_estabelecimento_p bigint default null) AS $body$
DECLARE


ie_incrementa_comp_w	varchar(1);
dt_competencia_w	timestamp;
ie_versao_w		varchar(20);


BEGIN

ie_incrementa_comp_w := obter_param_usuario(1126, 11, obter_perfil_ativo, nm_usuario_p, COALESCE(cd_estabelecimento_p,wheb_usuario_pck.get_cd_estabelecimento()), ie_incrementa_comp_w);

if (ie_incrementa_comp_w = 'S') then
	begin	
	dt_competencia_w := dt_competencia_p;
	ie_versao_w	 := ie_versao_p;
	end;
end if;

insert 	into sus_proced_incremento_origem(nr_sequencia,
	cd_habilitacao, 
	pr_incremento_sa, 
	pr_incremento_sh, 
	pr_incremento_sp,
	dt_atualizacao, 
	nm_usuario, 
	dt_atualizacao_nrec, 
	nm_usuario_nrec,
	cd_procedimento, 
	ie_origem_proced,
	dt_competencia,
	ie_versao)
values (nr_sequencia_p, 
	cd_habilitacao_p, 
	pr_incremento_sa_p, 
	pr_incremento_sh_p, 
	pr_incremento_sp_p,
	clock_timestamp(), 
	nm_usuario_p, 
	clock_timestamp(), 
	nm_usuario_p,
	cd_procedimento_p, 
	7,
	dt_competencia_w,
	ie_versao_w);	

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE sus_insert_pro_incr_origem (nr_sequencia_p bigint, cd_habilitacao_p bigint, pr_incremento_sa_p bigint, pr_incremento_sh_p bigint, pr_incremento_sp_p bigint, cd_procedimento_p bigint, dt_competencia_p timestamp, ie_versao_p text, nm_usuario_p text, cd_estabelecimento_p bigint default null) FROM PUBLIC;

