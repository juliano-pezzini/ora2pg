-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gravar_lab_log_arquivo_interf (ds_arquivo_p text, ds_conteudo_p text, nm_usuario_p text) AS $body$
DECLARE


ds_maquina_w		varchar(255);


BEGIN

select	substr(MAX(machine),1,255)
into STRICT	ds_maquina_w
from	v$session
where	audsid = (SELECT userenv('sessionid') );

insert into lab_log_arquivo_interf(	nr_sequencia,
					dt_atualizacao,
					nm_usuario,
					dt_atualizacao_nrec,
					nm_usuario_nrec,
					ds_arquivo,
					ds_conteudo,
					ds_maquina)
values (	nextval('lab_log_arquivo_interf_seq'),
					clock_timestamp(),
					nm_usuario_p,
					clock_timestamp(),
					nm_usuario_p,
					ds_arquivo_p,
					ds_conteudo_p,
					ds_maquina_w);

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gravar_lab_log_arquivo_interf (ds_arquivo_p text, ds_conteudo_p text, nm_usuario_p text) FROM PUBLIC;

