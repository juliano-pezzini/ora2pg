-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE carregar_qte_user_concorrente (nr_sequencia_p bigint) AS $body$
DECLARE


vl_inf_w		varchar(100);
id_inf_w		varchar(50);
cd_cnpj_w	varchar(14);
dt_versao_w	timestamp;


BEGIN

SELECt	MAX(a.dt_versao)
into STRICT	dt_versao_w
FROM	APLICACAO_TASY_VERSAO a
WHERE	TRUNC(a.dt_versao) <> TRUNC(clock_timestamp());

CALL concurrent_login_report(dt_versao_w,clock_timestamp());

SELECT	distinct substr(Obter_Cgc_Estabelecimento(Obter_Estabelecimento_Ativo),1,50),
	nr_count,
	Obter_Global_Name
into STRICT	cd_cnpj_w,
	vl_inf_w,
	id_inf_w
FROM	CONCURRENT_USERS_T a
WHERE	a.nr_count = (SELECT MAX(b.nr_count) FROM CONCURRENT_USERS_T b);

CALL insert_inf_base_cliente(
	nr_sequencia_p,
	vl_inf_w,
	id_inf_w,
	cd_cnpj_w,
	null,
	'Tasy');

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE carregar_qte_user_concorrente (nr_sequencia_p bigint) FROM PUBLIC;

