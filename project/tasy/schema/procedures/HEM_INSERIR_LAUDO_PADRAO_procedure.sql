-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE hem_inserir_laudo_padrao ( nr_seq_proc_p bigint, nr_seq_texto_p bigint, nm_usuario_p text) AS $body$
DECLARE


ds_protocolo_w			text;
nr_atendimento_w		bigint;


BEGIN
if	((coalesce(nr_seq_proc_p,0) > 0) and (coalesce(nr_seq_texto_p,0) > 0)) then

	select	ds_protocolo
	into STRICT	ds_protocolo_w
	from	protocolo
	where	cd_protocolo = nr_seq_texto_p;

	select	max(nr_atendimento)
	into STRICT	nr_atendimento_w
	from	hem_proc
	where	nr_sequencia = nr_seq_proc_p;

	CALL substituir_macro_protocolo_pa(	nr_seq_proc_p, ds_protocolo_w, nr_atendimento_w, nm_usuario_p, 'HP');

end if;
commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE hem_inserir_laudo_padrao ( nr_seq_proc_p bigint, nr_seq_texto_p bigint, nm_usuario_p text) FROM PUBLIC;

