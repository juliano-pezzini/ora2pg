-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE obter_exame_lab_agecons ( nr_atendimento_p bigint, cd_convenio_p bigint, cd_categoria_p text, nr_seq_exame_p bigint, nr_seq_proc_interno_p bigint, cd_material_exame_p bigint, cd_estabelecimento_p bigint, ds_procedimento_p INOUT text, cd_procedimento_p INOUT bigint, ie_origem_proced_p INOUT bigint, ds_erro_p INOUT text, nm_usuario_p text) AS $body$
DECLARE


ie_tipo_atendimento_w	smallint;
ie_tipo_convenio_w	smallint;
cd_procedimento_w	bigint;
ds_procedimento_w	PROCEDIMENTO.DS_PROCEDIMENTO%type;
ie_origem_proced_w	bigint;
ds_erro_w		varchar(255);
cd_setor_w		integer;
nr_seq_proc_interno_aux_w	bigint;
cd_plano_convenio_w	varchar(10);


BEGIN
if (nm_usuario_p IS NOT NULL AND nm_usuario_p::text <> '') and (nr_seq_exame_p IS NOT NULL AND nr_seq_exame_p::text <> '') then
	begin

	ie_tipo_atendimento_w	:= obter_tipo_atendimento(nr_atendimento_p);
	ie_tipo_convenio_w	:= obter_tipo_convenio(cd_convenio_p);
	cd_plano_convenio_w	:=	obter_plano_conv_atend(nr_atendimento_p);

	SELECT * FROM obter_exame_lab_convenio(
		nr_seq_exame_p, cd_convenio_p, cd_categoria_p, ie_tipo_atendimento_w, cd_estabelecimento_p, ie_tipo_convenio_w, nr_seq_proc_interno_p, cd_material_exame_p, cd_plano_convenio_w, cd_setor_w, cd_procedimento_w, ie_origem_proced_w, ds_erro_w, nr_seq_proc_interno_aux_w) INTO STRICT cd_setor_w, cd_procedimento_w, ie_origem_proced_w, ds_erro_w, nr_seq_proc_interno_aux_w;

  if (cd_procedimento_w <> 0) and (ie_origem_proced_w <> 0) then
  select	ds_procedimento

	into STRICT	ds_procedimento_w
	from	procedimento
	where	cd_procedimento		= cd_procedimento_w
	and	ie_origem_proced	= ie_origem_proced_w;
  end if;

	end;
end if;
ds_procedimento_p	:= ds_procedimento_w;
cd_procedimento_p 	:= cd_procedimento_w;
ie_origem_proced_p 	:= ie_origem_proced_w;
ds_erro_p 		:= ds_erro_w;
commit;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE obter_exame_lab_agecons ( nr_atendimento_p bigint, cd_convenio_p bigint, cd_categoria_p text, nr_seq_exame_p bigint, nr_seq_proc_interno_p bigint, cd_material_exame_p bigint, cd_estabelecimento_p bigint, ds_procedimento_p INOUT text, cd_procedimento_p INOUT bigint, ie_origem_proced_p INOUT bigint, ds_erro_p INOUT text, nm_usuario_p text) FROM PUBLIC;

