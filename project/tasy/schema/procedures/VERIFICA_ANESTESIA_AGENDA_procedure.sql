-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE verifica_anestesia_agenda ( nr_seq_proc_interno_p bigint, qt_minuto_duracao_p INOUT bigint) AS $body$
DECLARE


nr_minuto_duracao_w bigint;


BEGIN

if (coalesce(nr_seq_proc_interno_p,0) > 0) then
		select	coalesce(max(nr_minuto_duracao),0)
		into STRICT	nr_minuto_duracao_w
		from	ageint_tempo_exame_anest
		where	nr_seq_proc_interno	= nr_seq_proc_interno_p
		and 	coalesce(cd_estabelecimento, wheb_usuario_pck.get_cd_estabelecimento) = wheb_usuario_pck.get_cd_estabelecimento;

	if ( nr_minuto_duracao_w	> 0 ) then
		qt_minuto_duracao_p	:= nr_minuto_duracao_w;
	end if;
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE verifica_anestesia_agenda ( nr_seq_proc_interno_p bigint, qt_minuto_duracao_p INOUT bigint) FROM PUBLIC;

