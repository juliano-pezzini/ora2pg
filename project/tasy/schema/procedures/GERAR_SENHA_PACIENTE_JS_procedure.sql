-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_senha_paciente_js ( ie_tipo_paciente_fila_p bigint, nr_seq_agenda_p bigint, nr_seq_fila_p bigint, ie_tipo_agenda_p text, nm_usuario_p text, cd_estabelecimento_p bigint, nr_seq_senha_p INOUT bigint, ds_retorno_p INOUT text, cd_pessoa_fisica_p text default null, ie_senha_prioritaria_p text DEFAULT NULL, nr_atendimento_p bigint default null) AS $body$
DECLARE

 
nr_senha_w	integer;


BEGIN 
SELECT * FROM gerar_senha_paciente( 
	nr_seq_fila_p, cd_estabelecimento_p, nm_usuario_p, ie_senha_prioritaria_p, nr_senha_w, nr_seq_senha_p, cd_pessoa_fisica_p, nr_atendimento_p) INTO STRICT nr_senha_w, nr_seq_senha_p;
 
if (ie_tipo_paciente_fila_p = 0) then 
	begin 
	if (nr_seq_senha_p <> 0) then 
		begin 
		CALL vincular_senha_agendamento( 
			nr_seq_senha_p, 
			nr_seq_agenda_p, 
			ie_tipo_agenda_p);
 
		ds_retorno_p	:= substr(obter_texto_tasy(54187, wheb_usuario_pck.get_nr_seq_idioma),1,255);
		ds_retorno_p	:= replace(ds_retorno_p, '#@NR_SENHA#@', nr_senha_w);
		end;
	else 
		ds_retorno_p	:= substr(obter_texto_tasy(54188, wheb_usuario_pck.get_nr_seq_idioma),1,255);
	end if;
	end;
elsif (ie_tipo_paciente_fila_p = 1) then 
	begin 
	if (nr_seq_senha_p <> 0) then 
		begin 
		ds_retorno_p	:= substr(obter_texto_tasy(54187, wheb_usuario_pck.get_nr_seq_idioma),1,255);
		ds_retorno_p	:= replace(ds_retorno_p, '#@NR_SENHA#@', nr_senha_w);
		end;
	else 
		ds_retorno_p	:= substr(obter_texto_tasy(54188, wheb_usuario_pck.get_nr_seq_idioma),1,255);
	end if;
	end;
end if;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_senha_paciente_js ( ie_tipo_paciente_fila_p bigint, nr_seq_agenda_p bigint, nr_seq_fila_p bigint, ie_tipo_agenda_p text, nm_usuario_p text, cd_estabelecimento_p bigint, nr_seq_senha_p INOUT bigint, ds_retorno_p INOUT text, cd_pessoa_fisica_p text default null, ie_senha_prioritaria_p text DEFAULT NULL, nr_atendimento_p bigint default null) FROM PUBLIC;

