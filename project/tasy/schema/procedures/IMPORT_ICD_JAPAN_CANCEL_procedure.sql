-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE import_icd_japan_cancel (nm_usuario_p text,return_value INOUT text) AS $body$
DECLARE

    modifier_w   bigint;
    index_w      bigint;
    icd_code_w   bigint;

BEGIN
IF ( obtain_user_locale(nm_usuario_p) = 'ja_JP' ) THEN
icd_code_w := obter_valor_dinamico('select count(*) from icd_main_jpn', icd_code_w);
modifier_w := obter_valor_dinamico('select count(*) from icd_modifier_jpn', modifier_w);
index_w := obter_valor_dinamico('select count(*) from icd_index_jpn', index_w);
END IF;

IF (modifier_w > 0) OR (index_w > 0) OR ( icd_code_w > 0) THEN
    EXECUTE 'truncate table TASY.icd_main_jpn';
    EXECUTE 'truncate table TASY.icd_modifier_jpn';
    EXECUTE 'truncate table TASY.icd_index_jpn';
     return_value := 'S';
ELSE
     return_value := 'N';
END IF;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE import_icd_japan_cancel (nm_usuario_p text,return_value INOUT text) FROM PUBLIC;

