-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE com_atualizar_status_sd ( nm_usuario_p text, nr_seq_ordem_p bigint, nr_seq_solic_p bigint, ie_status_p text) AS $body$
BEGIN

if	((coalesce(nr_seq_solic_p,0) > 0) and (coalesce(nr_seq_ordem_p,0) > 0) and (ie_status_p IS NOT NULL AND ie_status_p::text <> ''))then

	update	com_solic_sd
	set	nr_ordem_servico = nr_seq_ordem_p,
		ie_status = ie_status_p,
		nm_usuario = nm_usuario_p,
		dt_atualizacao = clock_timestamp()
	where	nr_sequencia = nr_seq_solic_p;

end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE com_atualizar_status_sd ( nm_usuario_p text, nr_seq_ordem_p bigint, nr_seq_solic_p bigint, ie_status_p text) FROM PUBLIC;

