-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_guia_plano_atribfocuslost ( cd_usuario_plano_p text, ds_mensagem_um_p INOUT text, ds_mensagem_dois_p INOUT text, ds_mensagem_tres_p INOUT text, ie_existe_benef_p INOUT bigint, dt_validade_cart_p INOUT text, nm_usuario_p text, cd_estabelecimento_p bigint) AS $body$
DECLARE


nr_seq_segurado_w	bigint := 0;
dt_validade_carteira_w	varchar(50);


BEGIN

select	coalesce(max(pls_obter_beneficiario_existe(cd_usuario_plano_p, nm_usuario_p, cd_estabelecimento_p)),0)
into STRICT	nr_seq_segurado_w
;

ie_existe_benef_p	:= nr_seq_segurado_w;

ds_mensagem_um_p := obter_texto_dic_objeto(48859,null,null);

select	to_char(coalesce(max(dt_validade_carteira),clock_timestamp()),'dd/mm/yyyy hh24:mi:ss') dt_validade
into STRICT	dt_validade_carteira_w
from	pls_segurado_carteira
where	nr_seq_segurado = nr_seq_segurado_w
and	cd_usuario_plano = cd_usuario_plano_p;

dt_validade_cart_p := dt_validade_carteira_w;

ds_mensagem_dois_p := obter_texto_dic_objeto(48860,null,null);

ds_mensagem_tres_p := obter_texto_dic_objeto(48860,null,null);

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_guia_plano_atribfocuslost ( cd_usuario_plano_p text, ds_mensagem_um_p INOUT text, ds_mensagem_dois_p INOUT text, ds_mensagem_tres_p INOUT text, ie_existe_benef_p INOUT bigint, dt_validade_cart_p INOUT text, nm_usuario_p text, cd_estabelecimento_p bigint) FROM PUBLIC;

