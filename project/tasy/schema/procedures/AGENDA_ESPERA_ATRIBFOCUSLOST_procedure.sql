-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE agenda_espera_atribfocuslost ( nr_seq_interno_p bigint, cd_convenio_p bigint, cd_estabelecimento_p bigint, cd_procedimento_p INOUT bigint, ie_origem_proced_p INOUT bigint, ds_procedimento_p INOUT text) AS $body$
DECLARE

 
cd_procedimento_w	bigint;
ie_origem_proced_w	bigint;
ds_procedimento_w		procedimento.ds_procedimento%TYPE;
					

BEGIN 
 
if (nr_seq_interno_p IS NOT NULL AND nr_seq_interno_p::text <> '') then 
	begin 
	SELECT * FROM Obter_Proc_Tab_Inter_Agenda(nr_seq_interno_p, 0, cd_convenio_p, '', null, cd_estabelecimento_p, clock_timestamp(), null, null, null, null, null, null, null, null, cd_procedimento_w, ie_origem_proced_w) INTO STRICT cd_procedimento_w, ie_origem_proced_w;
	 
	ds_procedimento_w	:= substr(obter_exame_agenda(cd_procedimento_w,ie_origem_proced_w,nr_seq_interno_p),1,255);	
	end;
end if;
 
cd_procedimento_p	:= cd_procedimento_w;
ie_origem_proced_p	:= ie_origem_proced_w;
ds_procedimento_p	:= ds_procedimento_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE agenda_espera_atribfocuslost ( nr_seq_interno_p bigint, cd_convenio_p bigint, cd_estabelecimento_p bigint, cd_procedimento_p INOUT bigint, ie_origem_proced_p INOUT bigint, ds_procedimento_p INOUT text) FROM PUBLIC;

