-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_atualizar_lista_mat_hosp ( ds_lista_materiais_p text, cd_estabelecimento_p bigint, nm_usuario_p text) AS $body$
DECLARE


/* +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Finalidade: Executar a procedure PLS_ATUALIZAR_MATERIAL_HOSP para todos os materiais selecionados.
-------------------------------------------------------------------------------------------------------------------
Locais de chamada direta:
[ ]  Objetos do dicionário [X] Tasy (Delphi/Java) [  ] Portal [  ]  Relatórios [ ] Outros:
 ------------------------------------------------------------------------------------------------------------------
Pontos de atenção:
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ */
ds_lista_materiais_w		varchar(4000);
nr_seq_material_w		varchar(4000);
cd_material_w			varchar(4000);
nr_pos_virgula_w		bigint;


BEGIN
if (ds_lista_materiais_p IS NOT NULL AND ds_lista_materiais_p::text <> '') then
	ds_lista_materiais_w := ds_lista_materiais_p;

	while(ds_lista_materiais_w IS NOT NULL AND ds_lista_materiais_w::text <> '') loop
		nr_pos_virgula_w	:= position(',' in ds_lista_materiais_w);

		if (nr_pos_virgula_w > 0) then
			nr_seq_material_w 	:= substr(ds_lista_materiais_w, 1, nr_pos_virgula_w - 1);
			ds_lista_materiais_w 	:= substr(ds_lista_materiais_w, nr_pos_virgula_w + 1, length(ds_lista_materiais_w));
		else
			nr_seq_material_w	:= ds_lista_materiais_w;
			ds_lista_materiais_w	:= null;
		end if;

		if (nr_seq_material_w IS NOT NULL AND nr_seq_material_w::text <> '') then
			select	a.cd_material
			into STRICT	cd_material_w
			from	pls_material a
			where	a.nr_sequencia = nr_seq_material_w;

			CALL pls_atualizar_material_hosp(cd_material_w, nr_seq_material_w, cd_estabelecimento_p, nm_usuario_p);
		end if;
	end loop;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_atualizar_lista_mat_hosp ( ds_lista_materiais_p text, cd_estabelecimento_p bigint, nm_usuario_p text) FROM PUBLIC;

