-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE alterar_datas_proc_mat_repasse ( ie_proc_mat_p bigint, dt_contabil_titulo_p timestamp, dt_contabil_p timestamp, nr_sequencia_p text, nm_usuario_p text) AS $body$
BEGIN
if (ie_proc_mat_p = 0) then
	update	procedimento_repasse
	set	nm_usuario = nm_usuario_p,
		dt_atualizacao = clock_timestamp()
        where nr_sequencia = nr_sequencia_p;

	if (dt_contabil_p IS NOT NULL AND dt_contabil_p::text <> '') then
		update	procedimento_repasse
		set	nm_usuario = nm_usuario_p,
			dt_contabil = dt_contabil_p,
			dt_atualizacao = clock_timestamp()
		where nr_sequencia = nr_sequencia_p;
	end if;

	if (dt_contabil_titulo_p IS NOT NULL AND dt_contabil_titulo_p::text <> '') then
		update	procedimento_repasse
		set	nm_usuario = nm_usuario_p,
			dt_contabil = dt_contabil_p,
			dt_contabil_titulo = dt_contabil_titulo_p,
			dt_atualizacao = clock_timestamp()
		where nr_sequencia = nr_sequencia_p;
	end if;
end if;

if (ie_proc_mat_p = 1) then
	update	material_repasse
	set	nm_usuario = nm_usuario_p,
		dt_atualizacao = clock_timestamp()
        where nr_sequencia = nr_sequencia_p;

	if (dt_contabil_p IS NOT NULL AND dt_contabil_p::text <> '') then
		update	material_repasse
		set	nm_usuario = nm_usuario_p,
			dt_contabil = dt_contabil_p,
			dt_atualizacao = clock_timestamp()
		where nr_sequencia = nr_sequencia_p;

	end if;

	if (dt_contabil_titulo_p IS NOT NULL AND dt_contabil_titulo_p::text <> '') then
		update	material_repasse
		set	nm_usuario = nm_usuario_p,
			dt_contabil = dt_contabil_p,
			dt_contabil_titulo = dt_contabil_titulo_p,
			dt_atualizacao = clock_timestamp()
		where nr_sequencia = nr_sequencia_p;
	end if;
end if;



commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE alterar_datas_proc_mat_repasse ( ie_proc_mat_p bigint, dt_contabil_titulo_p timestamp, dt_contabil_p timestamp, nr_sequencia_p text, nm_usuario_p text) FROM PUBLIC;

