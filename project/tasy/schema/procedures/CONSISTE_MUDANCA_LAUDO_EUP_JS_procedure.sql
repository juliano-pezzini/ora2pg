-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consiste_mudanca_laudo_eup_js ( CD_PROC_ORIGINAL_P text, ds_msg_erro_p INOUT text) AS $body$
DECLARE

cd_retorno_w	bigint;
ds_msg_erro_w	varchar(2000);


BEGIN

cd_retorno_w := sus_obter_se_detalhe_proc(cd_proc_original_p,7,'008');

if (coalesce(cd_retorno_w,0) > 0) then

	ds_msg_erro_w := obter_texto_dic_objeto(289794, 1, 'CD_PROC_ORIGINAL_P=' || cd_proc_original_p) || chr(10) ||
					  obter_texto_tasy(289795, 1);
end if;

ds_msg_erro_p := ds_msg_erro_w;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consiste_mudanca_laudo_eup_js ( CD_PROC_ORIGINAL_P text, ds_msg_erro_p INOUT text) FROM PUBLIC;

