-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consistir_dados_guia_eup_js ( cd_exame_p text, ie_pasta_laboratorio_p text, cd_guia_p text, cd_senha_p text, cd_convenio_p bigint, ie_tipo_atendimento_p bigint, ie_carater_inter_sus_p text, ie_clinica_p bigint, cd_procedimento_p bigint, ie_origem_proced_p bigint, nr_seq_classificacao_p bigint, nr_seq_proc_interno_p bigint, cd_categoria_p text, cd_plano_p text, nr_atendimento_p bigint, nr_prescricao_p bigint, nr_seq_proced_p bigint, cd_material_exame_p text, dt_prev_execucao_p timestamp, cd_estabelecimento_p bigint, cd_perfil_p bigint, nm_usuario_p text, ds_msg_erro_p INOUT text, ds_msg_aviso_p INOUT text) AS $body$
DECLARE


vl_parametro_w		varchar(1);
ie_mesma_prescr_w	varchar(10);
ds_msg_erro_w		varchar(255);
ds_msg_aviso_w		varchar(255);
ie_continua_exec_w	varchar(1)	:= 'S';
ie_exige_guia_w		varchar(1);
ie_exige_senha_w	varchar(1);
ie_proc_duplic_prescr_w	varchar(1);


BEGIN

vl_parametro_w := obter_param_usuario(916, 538, cd_perfil_p, nm_usuario_p, cd_estabelecimento_p, vl_parametro_w);
ie_mesma_prescr_w := obter_param_usuario(924, 653, cd_perfil_p, nm_usuario_p, cd_estabelecimento_p, ie_mesma_prescr_w);

if (vl_parametro_w = 'S')and (ie_pasta_laboratorio_p = 'S')and (coalesce(cd_exame_p::text, '') = '')then
	begin

	ds_msg_erro_w		:= wheb_mensagem_pck.get_texto(313580);
	ie_continua_exec_w	:= 'N';

	end;
end if;

if (ie_continua_exec_w = 'S')then
	begin

	vl_parametro_w := obter_param_usuario(916, 159, cd_perfil_p, nm_usuario_p, cd_estabelecimento_p, vl_parametro_w);

	if (vl_parametro_w = 'R')then
		begin

		ie_exige_guia_w		:= obter_se_exige_guia_senha(cd_convenio_p, ie_tipo_atendimento_p, 1, 'G', cd_estabelecimento_p, ie_carater_inter_sus_p, ie_clinica_p, cd_procedimento_p, ie_origem_proced_p, nr_seq_classificacao_p, nr_seq_proc_interno_p, cd_categoria_p, cd_plano_p, null);

		if (coalesce(cd_guia_p::text, '') = '') and (ie_exige_guia_w = 'S')then
			begin

			ds_msg_erro_w		:= wheb_mensagem_pck.get_texto(83725);
			ie_continua_exec_w	:= 'N';

			end;
		end if;

		ie_exige_senha_w	:= obter_se_exige_guia_senha(cd_convenio_p, ie_tipo_atendimento_p, 1, 'S', cd_estabelecimento_p, ie_carater_inter_sus_p, ie_clinica_p, cd_procedimento_p, ie_origem_proced_p, nr_seq_classificacao_p, nr_seq_proc_interno_p,cd_categoria_p, cd_plano_p, null);

		if (coalesce(cd_senha_p::text, '') = '') and (ie_exige_senha_w = 'S')and (ie_continua_exec_w = 'S')then
			begin

			ds_msg_erro_w		:= wheb_mensagem_pck.get_texto(83965);
			ie_continua_exec_w	:= 'N';

			end;
		end if;

		end;
	end if;

	end;
end if;

if (ie_continua_exec_w = 'S')then
	begin

	vl_parametro_w := obter_param_usuario(916, 489, cd_perfil_p, nm_usuario_p, cd_estabelecimento_p, vl_parametro_w);

	if (vl_parametro_w = 'S')then
		begin

		ie_proc_duplic_prescr_w	:= obter_se_proced_duplic_prescr(nr_atendimento_p, nr_prescricao_p, nr_seq_proced_p, cd_procedimento_p, ie_origem_proced_p, cd_material_exame_p, dt_prev_execucao_p, 0, nr_seq_proc_interno_p, ie_mesma_prescr_w, cd_exame_p);

		if (ie_proc_duplic_prescr_w = 'S')then
			begin

			ds_msg_aviso_w	:= wheb_mensagem_pck.get_texto(313581);

			end;
		end if;

		end;
	end if;

	end;
end if;

ds_msg_erro_p	:= ds_msg_erro_w;
ds_msg_aviso_p	:= ds_msg_aviso_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consistir_dados_guia_eup_js ( cd_exame_p text, ie_pasta_laboratorio_p text, cd_guia_p text, cd_senha_p text, cd_convenio_p bigint, ie_tipo_atendimento_p bigint, ie_carater_inter_sus_p text, ie_clinica_p bigint, cd_procedimento_p bigint, ie_origem_proced_p bigint, nr_seq_classificacao_p bigint, nr_seq_proc_interno_p bigint, cd_categoria_p text, cd_plano_p text, nr_atendimento_p bigint, nr_prescricao_p bigint, nr_seq_proced_p bigint, cd_material_exame_p text, dt_prev_execucao_p timestamp, cd_estabelecimento_p bigint, cd_perfil_p bigint, nm_usuario_p text, ds_msg_erro_p INOUT text, ds_msg_aviso_p INOUT text) FROM PUBLIC;

