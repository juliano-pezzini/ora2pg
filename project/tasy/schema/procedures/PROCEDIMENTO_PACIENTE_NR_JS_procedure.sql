-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE procedimento_paciente_nr_js ( nr_atendimento_p bigint, cd_setor_atendimento_p INOUT bigint, dt_entrada_unidade_p INOUT timestamp, cd_convenio_p INOUT bigint, cd_categoria_p INOUT bigint, nr_doc_convenio_p INOUT bigint, nr_seq_atepacu_p INOUT bigint, cd_cgc_p INOUT text) AS $body$
DECLARE

 
cd_estabelecimento_w	bigint;


BEGIN 
 
select 	max(cd_setor_atendimento), 
	max(dt_entrada_unidade), 
	max(cd_convenio), 
	max(cd_categoria), 
	max(nr_doc_convenio), 
	max(nr_seq_atepacu), 
	max(cd_estabelecimento) 
into STRICT	cd_setor_atendimento_p, 
	dt_entrada_unidade_p, 
	cd_convenio_p, 
	cd_categoria_p, 
	nr_doc_convenio_p, 
	nr_seq_atepacu_p, 
	cd_estabelecimento_w 
from 	atendimento_paciente_v 
where 	nr_atendimento = nr_atendimento_p;
 
select 	max(cd_cgc) 
into STRICT	cd_cgc_p 
from 	estabelecimento 
where 	cd_estabelecimento = cd_estabelecimento_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE procedimento_paciente_nr_js ( nr_atendimento_p bigint, cd_setor_atendimento_p INOUT bigint, dt_entrada_unidade_p INOUT timestamp, cd_convenio_p INOUT bigint, cd_categoria_p INOUT bigint, nr_doc_convenio_p INOUT bigint, nr_seq_atepacu_p INOUT bigint, cd_cgc_p INOUT text) FROM PUBLIC;

