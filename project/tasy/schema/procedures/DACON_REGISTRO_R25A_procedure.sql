-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE dacon_registro_r25a (nm_usuario_p text, nr_seq_dacon_p bigint ) AS $body$
DECLARE


ds_arquivo_w	varchar(2000);


BEGIN

select	rpad('R25A',4,' ')
	||lpad((to_char(dt_referencia, 'MM'))::numeric ,2,0)
	||lpad(1,2,0) --coluna,
	||lpad(replace(campo_mascara(coalesce(round(((obter_dados_dacon(nr_seq_dacon_p, 'AC') * obter_dados_dacon(nr_seq_dacon_p, 'TBCC'))/100),2),'0'),2),'.',''),14,0)
	||lpad(0,14,0)
	||lpad(0,14,0)
	||lpad(0,14,0)
	||lpad(0,14,0)
	||lpad(0,14,0)
	||lpad(0,14,0)
	||lpad(0,14,0)
	||lpad(replace(campo_mascara(coalesce(round(((obter_dados_dacon(nr_seq_dacon_p, 'AC') * obter_dados_dacon(nr_seq_dacon_p, 'TBCC'))/100),2),'0'),2),'.',''),14,0)
	||lpad(0,14,0)
	||lpad(0,14,0)
	||lpad(replace(campo_mascara(coalesce(obter_dados_dacon(nr_seq_dacon_p, 'TCR'),'0'),2),'.',''),14,0)
	||lpad(0,14,0)
	||lpad(0,14,0)
	||lpad(0,14,0)
	||lpad(0,14,0)
	||lpad(0,14,0)
	||lpad(0,14,0)
	||lpad(0,14,0)
	||lpad(0,14,0)
	||lpad(replace(campo_mascara(coalesce((((obter_dados_dacon(nr_seq_dacon_p, 'AC'))*((obter_dados_dacon(nr_seq_dacon_p, 'TBCC'))/100)) - (obter_dados_dacon(nr_seq_dacon_p, 'TCR'))),'0'),2),'.',''),14,0)
	||lpad(0,14,0)
	||lpad(0,14,0)
	||lpad(0,14,0)
	||lpad(0,164,0)
	||lpad(0,10,0)
into STRICT	ds_arquivo_w
from	dacon
where	nr_sequencia = nr_seq_dacon_p;

insert into w_dacon(nr_sequencia,
	    dt_atualizacao,
	    nm_usuario,
	    dt_atualizacao_nrec,
	    nm_usuario_nrec,
	    ds_arquivo,
            nr_linha,
	    ie_tipo_registro)
values (nextval('w_dacon_seq'),
	    clock_timestamp(),
	    nm_usuario_p,
	    clock_timestamp(),
	    nm_usuario_p,
	    ds_arquivo_w,
	    1,
 	    35);

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE dacon_registro_r25a (nm_usuario_p text, nr_seq_dacon_p bigint ) FROM PUBLIC;

