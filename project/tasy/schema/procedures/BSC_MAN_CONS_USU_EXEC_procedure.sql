-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE bsc_man_cons_usu_exec ( nr_seq_ordem_p bigint, nr_seq_indicador_p bigint, nm_usuario_p text) AS $body$
DECLARE


NM_USUARIO_EXEC_W	varchar(15);

C01 CURSOR FOR
SELECT	c.nm_usuario
from	bsc_indicador b,
	bsc_ind_resp a,
	usuario c
where	b.nr_sequencia	= a.nr_seq_indicador
and	c.cd_pessoa_fisica = a.cd_pessoa_fisica
and	b.nr_sequencia	= nr_seq_indicador_p
and	clock_timestamp() between coalesce(a.dt_inicio_vigencia,clock_timestamp()) and fim_dia(coalesce(a.dt_fim_vigencia,clock_timestamp()));


BEGIN
if (nr_seq_ordem_p IS NOT NULL AND nr_seq_ordem_p::text <> '') and (nr_seq_indicador_p IS NOT NULL AND nr_seq_indicador_p::text <> '') then
	begin
	open C01;
	loop
	fetch C01 into
		nm_usuario_exec_w;
	EXIT WHEN NOT FOUND; /* apply on C01 */
		begin

		insert into man_ordem_servico_exec(
			nr_sequencia,
			nr_seq_ordem,
			dt_atualizacao,
			nm_usuario,
			nm_usuario_exec,
			dt_atualizacao_nrec,
			nm_usuario_nrec)
		values (	nextval('man_ordem_servico_exec_seq'),
			nr_seq_ordem_p,
			clock_timestamp(),
			nm_usuario_p,
			nm_usuario_exec_w,
			clock_timestamp(),
			nm_usuario_p);
		end;
	end loop;
	close C01;
	end;
commit;

end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE bsc_man_cons_usu_exec ( nr_seq_ordem_p bigint, nr_seq_indicador_p bigint, nm_usuario_p text) FROM PUBLIC;

