-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_imp_xml_a1350_w ( ie_tipo_registro_p bigint, ds_linha_p text) AS $body$
DECLARE


/*
ie_tipo_registro_p:	1 - INSERT CABECALHO
		2 - INSERT DADOS
		3 - TRUNCATE
*/
BEGIN

if (coalesce(ie_tipo_registro_p,0) = 1) or (coalesce(ie_tipo_registro_p,0) = 2) then

	insert into w_pls_a1350(
		ie_tipo_registro,
		ds_linha)
	values (ie_tipo_registro_p,
		ds_linha_p);

elsif (coalesce(ie_tipo_registro_p,0) = 3) then

	EXECUTE 'truncate table w_pls_a1350';
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_imp_xml_a1350_w ( ie_tipo_registro_p bigint, ds_linha_p text) FROM PUBLIC;

