-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE update_dt_entrega_convenio ( NR_SEQ_PROTOCOLO_P PROTOCOLO_CONVENIO.NR_SEQ_PROTOCOLO%TYPE, DT_ENTREGA_CONVENIO_P PROTOCOLO_CONVENIO.DT_ENTREGA_CONVENIO%TYPE ) AS $body$
DECLARE


dt_atual_s	CONSTANT PROTOCOLO_CONVENIO.DT_ATUALIZACAO%TYPE := clock_timestamp();


BEGIN
IF (NR_SEQ_PROTOCOLO_P IS NOT NULL AND NR_SEQ_PROTOCOLO_P::text <> '' AND DT_ENTREGA_CONVENIO_P IS NOT NULL AND DT_ENTREGA_CONVENIO_P::text <> '') THEN
    UPDATE  PROTOCOLO_CONVENIO
    SET     DT_ENTREGA_CONVENIO = DT_ENTREGA_CONVENIO_P,
            DT_ATUALIZACAO      = dt_atual_s,
            NM_USUARIO          = wheb_usuario_pck.get_nm_usuario
    WHERE   NR_SEQ_PROTOCOLO    = NR_SEQ_PROTOCOLO_P;
END IF;

COMMIT;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE update_dt_entrega_convenio ( NR_SEQ_PROTOCOLO_P PROTOCOLO_CONVENIO.NR_SEQ_PROTOCOLO%TYPE, DT_ENTREGA_CONVENIO_P PROTOCOLO_CONVENIO.DT_ENTREGA_CONVENIO%TYPE ) FROM PUBLIC;

