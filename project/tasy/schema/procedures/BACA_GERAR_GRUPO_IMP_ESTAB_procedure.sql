-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE baca_gerar_grupo_imp_estab ( nr_seq_grupo_imp_atual_p bigint, nr_seq_grupo_imp_estab_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) AS $body$
DECLARE



nr_seq_exame_w	bigint;
qt_itens_imp_w	integer;

c01 CURSOR FOR
	SELECT 	distinct
		nr_seq_exame
	from	exame_laboratorio
	where 	nr_seq_grupo_imp = nr_seq_grupo_imp_atual_p
	and	(nr_seq_grupo_imp_estab_p IS NOT NULL AND nr_seq_grupo_imp_estab_p::text <> '')
	and	(cd_estabelecimento_p IS NOT NULL AND cd_estabelecimento_p::text <> '');



BEGIN

OPEN C01;
LOOP
FETCH C01 into
	nr_seq_exame_w;
EXIT WHEN NOT FOUND; /* apply on c01 */
	begin

	select	count(*)
	into STRICT	qt_itens_imp_w
	from 	exame_lab_grupo_imp
	where	nr_seq_exame = nr_seq_exame_w
	and	cd_estabelecimento = cd_estabelecimento_p;

	if (qt_itens_imp_w = 0) then

		insert into exame_lab_grupo_imp(nr_sequencia,
					cd_estabelecimento,
					dt_atualizacao,
					nm_usuario,
					dt_atualizacao_nrec,
					nm_usuario_nrec,
					nr_seq_exame,
					nr_seq_grupo_imp)
				values (nextval('exame_lab_grupo_imp_seq'),
					cd_estabelecimento_p,
					clock_timestamp(),
					nm_usuario_p,
					clock_timestamp(),
					nm_usuario_p,
					nr_seq_exame_w,
					nr_seq_grupo_imp_estab_p);
	end if;

	/*insert into logxxxxx_tasy (dt_atualizacao, nm_usuario,cd_log, ds_log)
	values (sysdate, nm_usuario_p, 2170,'Estab.: ' || cd_estabelecimento_p || ' Exame: ' || nr_seq_exame_w);*/
	end;
END LOOP;
CLOSE C01;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE baca_gerar_grupo_imp_estab ( nr_seq_grupo_imp_atual_p bigint, nr_seq_grupo_imp_estab_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) FROM PUBLIC;

