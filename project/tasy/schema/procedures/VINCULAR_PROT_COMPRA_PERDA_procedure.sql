-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE vincular_prot_compra_perda ( nr_seq_protocolo_p bigint, nr_seq_perda_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text, ds_erro_p INOUT text) AS $body$
DECLARE
	
ie_status_protocolo_w	smallint;	
ds_erro_w		varchar(255);

BEGIN
	select	coalesce(max(a.ie_status_protocolo),0)
	into STRICT	ie_status_protocolo_w
	from	protocolo_convenio a
	where	a.nr_seq_protocolo	= nr_seq_protocolo_p;
	if (ie_status_protocolo_w <> 2)then
		ds_erro_w := obter_texto_tasy(54520, philips_param_pck.get_nr_seq_idioma);
	else
		CALL vincular_protocolo_perda_fat(cd_estabelecimento_p,nr_seq_perda_p,nr_seq_protocolo_p,nm_usuario_p);	
	end if;
	ds_erro_p := ds_erro_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE vincular_prot_compra_perda ( nr_seq_protocolo_p bigint, nr_seq_perda_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text, ds_erro_p INOUT text) FROM PUBLIC;

