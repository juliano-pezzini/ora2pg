-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consiste_regra_presc_laudo ( nr_seq_proc_p bigint, cd_tipo_protocolo_p INOUT bigint, cd_protocolo_p INOUT bigint, nr_seq_protocolo_p INOUT bigint) AS $body$
DECLARE


cd_procedimento_w       	procedimento.cd_procedimento%type;
nr_seq_proc_interno_w   	proc_interno.nr_sequencia%type;
ie_origem_proced_w      	procedimento.ie_origem_proced%type;

C01 CURSOR FOR
	SELECT	coalesce(cd_tipo_protocolo,0),
			coalesce(cd_protocolo,0),
			coalesce(nr_seq_protocolo,0)
	from	prot_padrao_exa_laudo
	where 	(((coalesce(cd_procedimento, coalesce(cd_procedimento_w,0)) = coalesce(cd_procedimento_w,0)) and (coalesce(ie_origem_proced, coalesce(ie_origem_proced_w,0)) = coalesce(ie_origem_proced_w,0))) or (coalesce(nr_seq_proc_interno, coalesce(nr_seq_proc_interno_w,0)) = coalesce(nr_seq_proc_interno_w,0)))
	order by	coalesce(cd_procedimento,0) desc,
				coalesce(nr_seq_proc_interno,0) desc,
				coalesce(ie_origem_proced,0) desc;


BEGIN
cd_tipo_protocolo_p	:= 0;
cd_protocolo_p		:= 0;
nr_seq_protocolo_p	:= 0;

select	coalesce(max(ie_origem_proced),0),
		coalesce(max(cd_procedimento),0),
		coalesce(max(nr_seq_proc_interno),0)
into STRICT	ie_origem_proced_w,
		cd_procedimento_w,
		nr_seq_proc_interno_w
from	procedimento_paciente
where	nr_sequencia = nr_seq_proc_p;

if (cd_procedimento_w > 0 ) or (nr_seq_proc_interno_w > 0)  then
	open c01;
	loop
	fetch c01 into
		cd_tipo_protocolo_p,
		cd_protocolo_p,
		nr_seq_protocolo_p;
	EXIT WHEN NOT FOUND; /* apply on c01 */
		exit;
	end loop;
	close c01;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consiste_regra_presc_laudo ( nr_seq_proc_p bigint, cd_tipo_protocolo_p INOUT bigint, cd_protocolo_p INOUT bigint, nr_seq_protocolo_p INOUT bigint) FROM PUBLIC;

