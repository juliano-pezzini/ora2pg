-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE lote_ent_reconvocar_ficha (nr_seq_ficha_p bigint, ie_area_busca_p text, nm_usuario_p text) AS $body$
DECLARE

 
									 
ds_exames_w			varchar(255);
nr_seq_ficha_w		lote_ent_sec_ficha.nr_sequencia%type;

BEGIN
 
select	max(a.nr_sequencia), 
		max(obter_select_concatenado_bv('select 	x.nr_sequencia 
					from 		lote_ent_sec_ficha_exam x, 
							lote_ent_sec_ficha y 
					where 		x.nr_seq_ficha = :nr_seq_ficha 
					and			x.nr_seq_ficha = y.nr_sequencia 
					and			y.nr_seq_lote_sec = :nr_seq_lote_sec', 
					'nr_seq_ficha='||a.nr_sequencia||';'|| 
					'nr_seq_lote_sec='||a.nr_seq_lote_sec,',')) 
into STRICT	nr_seq_ficha_w, 
		ds_exames_w 
from	lote_ent_sec_ficha a 
where	1 = 1 
and		a.nr_sequencia = nr_seq_ficha_p 
order by 1;
 
CALL reconvocacao_ficha_lote_ent(nr_seq_ficha_w, ds_exames_w, nm_usuario_p, ie_area_busca_p);
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE lote_ent_reconvocar_ficha (nr_seq_ficha_p bigint, ie_area_busca_p text, nm_usuario_p text) FROM PUBLIC;

