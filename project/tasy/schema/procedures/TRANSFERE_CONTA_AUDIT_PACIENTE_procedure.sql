-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE transfere_conta_audit_paciente ( cd_categoria_p text, cd_convenio_p bigint, cd_convenio_dbp_p bigint, nr_sequencia_p bigint, nr_interno_conta_p bigint, ie_proc_mat_p bigint) AS $body$
BEGIN

if (cd_convenio_p = cd_convenio_dbp_p) then

	if (ie_proc_mat_p = 1) then
		update	procedimento_paciente
		set	nr_interno_conta = nr_interno_conta_p,
			CD_MOTIVO_EXC_CONTA  = NULL,
			DS_COMPL_MOTIVO_EXCON  = NULL,
			cd_convenio    = cd_convenio_p,
			cd_categoria   = cd_categoria_p
		where  nr_sequencia = nr_sequencia_p;
	else
		update	material_atend_paciente
		set	nr_interno_conta = nr_interno_conta_p,
			CD_MOTIVO_EXC_CONTA  = NULL,
			DS_COMPL_MOTIVO_EXCON  = NULL,
			cd_convenio    = cd_convenio_p,
			cd_categoria   = cd_categoria_p
		where  nr_sequencia = nr_sequencia_p;
	end if;

else

	if (ie_proc_mat_p = 1) then
		update	procedimento_paciente
		set	NR_INTERNO_CONTA = nr_interno_conta_p,
			CD_MOTIVO_EXC_CONTA  = NULL,
			DS_COMPL_MOTIVO_EXCON  = NULL
		where nr_sequencia = nr_sequencia_p;
	else
		update	material_atend_paciente
		set	NR_INTERNO_CONTA = nr_interno_conta_p,
			CD_MOTIVO_EXC_CONTA  = NULL,
			DS_COMPL_MOTIVO_EXCON  = NULL
		where nr_sequencia = nr_sequencia_p;
	end if;

end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE transfere_conta_audit_paciente ( cd_categoria_p text, cd_convenio_p bigint, cd_convenio_dbp_p bigint, nr_sequencia_p bigint, nr_interno_conta_p bigint, ie_proc_mat_p bigint) FROM PUBLIC;

