-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_obter_dados_atend_web ( nr_seq_guia_princ_p bigint, dt_internacao_p INOUT text, dt_alta_p INOUT text) AS $body$
DECLARE



nr_seq_atend_w		bigint;
dt_internacao_w		varchar(10) := '';
dt_alta_w		varchar(10) := '';


BEGIN


select  max(nr_sequencia)
into STRICT	nr_seq_atend_w
from	pls_guia_atendimento
where	nr_seq_guia = nr_seq_guia_princ_p;


if (nr_seq_atend_w IS NOT NULL AND nr_seq_atend_w::text <> '') then
	select	to_char(dt_internacao,'dd/mm/yyyy'),
		to_char(dt_alta,'dd/mm/yyyy')
	into STRICT	dt_internacao_w,
		dt_alta_w
	from	pls_guia_atendimento
	where	nr_sequencia = nr_seq_atend_w;
end if;

dt_internacao_p := dt_internacao_w;
dt_alta_p := dt_alta_w;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_obter_dados_atend_web ( nr_seq_guia_princ_p bigint, dt_internacao_p INOUT text, dt_alta_p INOUT text) FROM PUBLIC;

