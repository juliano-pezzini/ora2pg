-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consistir_cracha_paciente_js ( nr_atendimento_p bigint, cd_setor_atendimento_p bigint, cd_unidade_basica_p text, cd_unidade_compl_p text, nr_controle_p text, nm_usuario_p text, ds_erro_p INOUT text) AS $body$
DECLARE

 
cd_setor_atendimento_w	integer;
cd_unidade_basica_w		varchar(10);
cd_unidade_compl_w		varchar(10);


BEGIN 
 
if (cd_setor_atendimento_p IS NOT NULL AND cd_setor_atendimento_p::text <> '') then 
	begin 
	cd_setor_atendimento_w	:= cd_setor_atendimento_p;
	cd_unidade_basica_w		:= cd_unidade_basica_p;
	cd_unidade_compl_w		:= cd_unidade_compl_p;
	end;
 
elsif (nr_atendimento_p IS NOT NULL AND nr_atendimento_p::text <> '') then 
	begin 
	select	(obter_setor_atendimento(nr_atendimento_p))::numeric  
	into STRICT	cd_setor_atendimento_w 
	;
 
	select	substr(obter_unidade_atendimento(nr_atendimento_p, 'A', 'UB'), 1, 10) 
	into STRICT	cd_unidade_basica_w 
	;
 
	select	substr(obter_unidade_atendimento(nr_atendimento_p, 'A', 'UC'), 1, 10) 
	into STRICT	cd_unidade_compl_w 
	;
	end;
end if;
 
ds_erro_p := consistir_cracha_paciente( 
	nr_controle_p, cd_setor_atendimento_w, cd_unidade_basica_w, cd_unidade_compl_w, nm_usuario_p, ds_erro_p);
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consistir_cracha_paciente_js ( nr_atendimento_p bigint, cd_setor_atendimento_p bigint, cd_unidade_basica_p text, cd_unidade_compl_p text, nr_controle_p text, nm_usuario_p text, ds_erro_p INOUT text) FROM PUBLIC;

