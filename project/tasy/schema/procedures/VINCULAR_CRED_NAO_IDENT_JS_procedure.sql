-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE vincular_cred_nao_ident_js ( nr_seq_trans_fin_p bigint, nr_sequencia_lote_p bigint, ie_cred_nao_ident_p INOUT text, qt_registros_p INOUT bigint) AS $body$
DECLARE


ie_cred_nao_ident_w	varchar(1);
qt_registros_w		bigint;


BEGIN
if (nr_seq_trans_fin_p IS NOT NULL AND nr_seq_trans_fin_p::text <> '') then
	begin
	select 	max(ie_cred_nao_ident)
	into STRICT	ie_cred_nao_ident_w
	from 	transacao_financeira
	where 	nr_sequencia = nr_seq_trans_fin_p;
	end;
end if;

if (nr_sequencia_lote_p IS NOT NULL AND nr_sequencia_lote_p::text <> '') then
	begin
	select 	count(*)
	into STRICT	qt_registros_w
	from   	movto_banco_pend a,
		movto_banco_pend_baixa b
	where  	b.nr_seq_movto_pend  = a.nr_sequencia
	and    	b.nr_seq_lote_cartao = nr_sequencia_lote_p;
	end;
end if;
ie_cred_nao_ident_p	:=	ie_cred_nao_ident_w;
qt_registros_p		:=	qt_registros_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE vincular_cred_nao_ident_js ( nr_seq_trans_fin_p bigint, nr_sequencia_lote_p bigint, ie_cred_nao_ident_p INOUT text, qt_registros_p INOUT bigint) FROM PUBLIC;

