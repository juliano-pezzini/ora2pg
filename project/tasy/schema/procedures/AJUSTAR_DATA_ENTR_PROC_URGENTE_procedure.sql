-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE ajustar_data_entr_proc_urgente ( nr_prescricao_p bigint, nm_usuario_p text) AS $body$
DECLARE

dt_entrega_w		timestamp;


BEGIN

select	max(dt_resultado)
into STRICT	dt_entrega_w
from	prescr_procedimento
where	nr_prescricao = nr_prescricao_p;

if (dt_entrega_w IS NOT NULL AND dt_entrega_w::text <> '') then
	update 	prescr_medica
	set	dt_entrega = dt_entrega_w
	where	nr_prescricao = nr_prescricao_p;
end if;

if (coalesce(wheb_usuario_pck.get_ie_commit, 'S') = 'S') then commit; end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE ajustar_data_entr_proc_urgente ( nr_prescricao_p bigint, nm_usuario_p text) FROM PUBLIC;

