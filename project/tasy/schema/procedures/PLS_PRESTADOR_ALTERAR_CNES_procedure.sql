-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_prestador_alterar_cnes ( nr_sequencia_p bigint, cd_cnes_p text, nm_usuario_p text) AS $body$
DECLARE



cd_cgc_w			varchar(20);
cd_pessoa_fisica_w		varchar(15);


BEGIN

select	cd_cgc,
	cd_pessoa_fisica
into STRICT	cd_cgc_w,
	cd_pessoa_fisica_w
from	pls_prestador
where	nr_sequencia	= nr_sequencia_p;


/*if 	(cd_cgc_w is null) then
	update	pessoa_fisica
	set	cd_cnes			= cd_cnes_p,
		nm_usuario		= nm_usuario_p,
		dt_atualizacao		= sysdate
	where	cd_pessoa_fisica	= cd_pessoa_fisica_w;
	Diether - OS - 316066 - Alterado para só mudar CNES de prestador PJ*/
if (coalesce(cd_pessoa_fisica_w::text, '') = '') then
	update	pessoa_juridica
	set	cd_cnes	= cd_cnes_p,
		nm_usuario	= nm_usuario_p,
		dt_atualizacao	= clock_timestamp()
	where	cd_cgc		= cd_cgc_w;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_prestador_alterar_cnes ( nr_sequencia_p bigint, cd_cnes_p text, nm_usuario_p text) FROM PUBLIC;

