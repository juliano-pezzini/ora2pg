-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE automatic_unlock_procedures (cd_estabelecimento_p bigint, nm_usuario_p text) AS $body$
DECLARE


automatic_unlock_time_w varchar(10);
unlock_time_w bigint;

BEGIN
  automatic_unlock_time_w := obter_param_usuario(99010, 139, obter_perfil_ativo, nm_usuario_p, cd_estabelecimento_p, automatic_unlock_time_w);
  unlock_time_w := cast(automatic_unlock_time_w as bigint);

  if (unlock_time_w > 0) then
    delete from PROC_ATEND_BLOQ_MEDICO
     where (dt_atualizacao + (unlock_time_w/1440)) <= clock_timestamp();

    commit;
  end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE automatic_unlock_procedures (cd_estabelecimento_p bigint, nm_usuario_p text) FROM PUBLIC;

