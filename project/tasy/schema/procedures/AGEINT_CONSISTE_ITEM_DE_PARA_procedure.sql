-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE ageint_consiste_item_de_para ( nr_seq_ageint_p bigint, cd_estab_destino_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text, ds_retorno_p INOUT text) AS $body$
DECLARE


ds_Retorno_w		varchar(1)	:= 'S';
nr_seq_item_selec_w	bigint;
nr_seq_item_ret_w	bigint;
nr_seq_item_proc_w	bigint;
nr_seq_proc_interno_w	bigint;

C01 CURSOR FOR
	SELECT	nr_seq_item_selec,
		nr_seq_proc_interno
	from	agenda_integrada_item
	where	nr_seq_Agenda_int	= nr_seq_ageint_p
	and		(nr_seq_item_Selec IS NOT NULL AND nr_seq_item_Selec::text <> '')
	order by 1;


BEGIN

open C01;
loop
fetch C01 into
	nr_seq_item_selec_w,
	nr_seq_proc_interno_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin

	begin
	select	a.nr_sequencia
	into STRICT	nr_seq_item_ret_w
	from	agenda_int_grupo_item a,
		agenda_int_grupo b,
		agenda_int_area c
	where	a.nr_seq_grupo = b.nr_sequencia
	and	c.nr_sequencia = b.nr_seq_area
	and	a.nr_seq_proc_interno = nr_seq_proc_interno_w
	and	coalesce(c.cd_estabelecimento,cd_estabelecimento_p) = cd_estabelecimento_p;
	exception
	when no_data_found then
		nr_seq_item_proc_w := 0;
	when others then
		nr_seq_item_proc_w := 2;
	end;

	if (nr_seq_item_proc_w = 2) then
		nr_seq_item_ret_w := Ageint_Obter_Item_De_Para(
				nr_seq_item_selec_w, cd_estab_destino_p, cd_estabelecimento_p, nm_usuario_p, nr_seq_item_ret_w);
	end if;
	if (nr_seq_item_ret_w	= 0) then
		ds_retorno_w	:= 'N';
	end if;
	end;
end loop;
close C01;

ds_retorno_p	:= ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE ageint_consiste_item_de_para ( nr_seq_ageint_p bigint, cd_estab_destino_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text, ds_retorno_p INOUT text) FROM PUBLIC;

