-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE tre_gerar_candidato_agenda (nr_seq_agenda_p bigint, cd_pessoa_fisica_p text, nr_seq_modulo_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint) AS $body$
DECLARE


ds_msg_w 		varchar(255):= '';


BEGIN
ds_msg_w := tre_consiste_candidatos(nr_seq_agenda_p, 2, nr_seq_modulo_p, nm_usuario_p, cd_estabelecimento_p, ds_msg_w);
if (ds_msg_w <> ' ')	then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(262009,'DS_MSG_W=' || DS_MSG_W);
else
	if (nr_seq_modulo_p IS NOT NULL AND nr_seq_modulo_p::text <> '') then

		insert into tre_candidato(
			nr_sequencia,
			dt_atualizacao,
			nm_usuario,
			dt_atualizacao_nrec,
			nm_usuario_nrec,
			cd_pessoa_fisica,
			nr_seq_agenda,
			nr_seq_modulo,
			ie_cancelamento)
		values (nextval('tre_candidato_seq'),
			clock_timestamp(),
			nm_usuario_p,
			clock_timestamp(),
			nm_usuario_p,
			cd_pessoa_fisica_p,
			nr_seq_agenda_p,
			nr_seq_modulo_p,
			'N');

		commit;

	end if;

end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE tre_gerar_candidato_agenda (nr_seq_agenda_p bigint, cd_pessoa_fisica_p text, nr_seq_modulo_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint) FROM PUBLIC;

