-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE obter_ultimo_setor_agenda ( cd_pessoa_fisica_p text, cd_setor_atendimento_p INOUT text) AS $body$
DECLARE


nr_seq_agenda_w		agenda_consulta.cd_pessoa_fisica%type;
cd_setor_atendimento_w	varchar(10);


BEGIN

if (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') then
	begin	
	select	max(nr_sequencia)
	into STRICT	nr_seq_agenda_w
	from	agenda_consulta
	where	cd_pessoa_fisica = cd_pessoa_fisica_p
	and	obter_tipo_agenda(cd_agenda) in (3,4);
	
	if (nr_seq_agenda_w IS NOT NULL AND nr_seq_agenda_w::text <> '') then
		select	cd_setor_atendimento
		into STRICT	cd_setor_atendimento_w
		from	agenda_consulta
		where	nr_sequencia = nr_seq_agenda_w;
	end if;
	end;
end if;

cd_setor_atendimento_p	:= cd_setor_atendimento_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE obter_ultimo_setor_agenda ( cd_pessoa_fisica_p text, cd_setor_atendimento_p INOUT text) FROM PUBLIC;

