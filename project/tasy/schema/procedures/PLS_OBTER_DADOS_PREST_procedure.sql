-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_obter_dados_prest ( nr_seq_prestador_p bigint, cd_pessoa_fisica_p INOUT text, cd_medico_p INOUT text) AS $body$
DECLARE

 
cd_pessoa_fisica_w	varchar(10);
cd_medico_w		varchar(255);			
 

BEGIN 
if (nr_seq_prestador_p IS NOT NULL AND nr_seq_prestador_p::text <> '') then 
	begin 
	 
	select	cd_pessoa_fisica 
	into STRICT	cd_pessoa_fisica_w 
	from	pls_prestador 
	where	nr_sequencia = nr_seq_prestador_p;
	 
	if (cd_pessoa_fisica_w IS NOT NULL AND cd_pessoa_fisica_w::text <> '') then	 
		begin 
		 
		select coalesce(substr(obter_dados_medico(cd_pessoa_fisica_w,'CRM'),1,255),'X') 
		into STRICT	cd_medico_w 
		;
		 
		end;
	end if;
	 
	end;
end if;
 
cd_pessoa_fisica_p	:= cd_pessoa_fisica_w;
cd_medico_p		:= cd_medico_w;
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_obter_dados_prest ( nr_seq_prestador_p bigint, cd_pessoa_fisica_p INOUT text, cd_medico_p INOUT text) FROM PUBLIC;

