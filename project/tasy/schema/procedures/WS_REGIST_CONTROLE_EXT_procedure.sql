-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE ws_regist_controle_ext (	--nr_seq_grupo_mat_p	number,
 nr_prescricao_p bigint, nr_seq_prescr_p bigint, cd_barras_p text, nm_usuario_p text, cd_erro_p INOUT text, ds_erro_p INOUT text) AS $body$
DECLARE


/*
0 - SUCESSO
1 - Dados não informados
	1.1 - Sequência da prescrição não informado
2 - Dados inconsistentes
	2.1 - Procedimento não encontrado
3 - Erro ao registrar
*/
ie_existe_w			varchar(1);
ie_status_atend_w	prescr_procedimento.ie_status_atend%type;


BEGIN

cd_erro_p	:= 0;

if (coalesce(nr_seq_prescr_p::text, '') = '') then
	cd_erro_p	:= 1.1;
	ds_erro_p	:= WHEB_MENSAGEM_PCK.get_texto(278535,null);
end if;

if (cd_erro_p = 0) then

	select	CASE WHEN count(*)=0 THEN 'N'  ELSE 'S' END
	into STRICT	ie_existe_w
	from	prescr_procedimento a
	where	a.nr_prescricao = nr_prescricao_p
	and		a.nr_sequencia = nr_seq_prescr_p;

	if (ie_existe_w = 'N') then
		cd_erro_p	:= 2.1;
		ds_erro_p	:= WHEB_MENSAGEM_PCK.get_texto(278536,'NR_PRESCRICAO='||to_char(nr_prescricao_p)||';NR_SEQ_PRESCR='||to_char(nr_seq_prescr_p));
	end if;

	if (cd_erro_p = 0) then

		begin

		update	prescr_procedimento
		set		nr_controle_ext = cd_barras_p
		where	nr_prescricao = nr_prescricao_p
		and		nr_sequencia = nr_seq_prescr_p;

		/*select	max(a.ie_status_atend)
		into	ie_status_atend_w
		from	prescr_procedimento a
		where	a.nr_prescricao = nr_prescricao_p
		and		a.nr_sequencia = nr_seq_prescr_p;

		insert into prescr_proc_mat_item(nr_sequencia,
				dt_atualizacao,
				nm_usuario,
				dt_atualizacao_nrec,
				nm_usuario_nrec,
				nr_prescricao,
				nr_seq_prescr,
				nr_seq_prescr_proc_mat,
				ie_status,
				cd_barras
				--,nr_seq_frasco
				)
		values	(prescr_proc_mat_item_seq.nextval,
				sysdate,
				nm_usuario_p,
				sysdate,
				nm_usuario_p,
				nr_prescricao_p,
				nr_seq_prescr_p,
				nr_seq_grupo_mat_p,
				ie_status_atend_w,
				cd_barras_p
				--,nr_seq_frasco_w
				);*/
		commit;
		exception
		when others then
			cd_erro_p	:= 3;
			ds_erro_p	:= substr(sqlerrm,1,2000);
		end;

	end if;

end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE ws_regist_controle_ext ( nr_prescricao_p bigint, nr_seq_prescr_p bigint, cd_barras_p text, nm_usuario_p text, cd_erro_p INOUT text, ds_erro_p INOUT text) FROM PUBLIC;

