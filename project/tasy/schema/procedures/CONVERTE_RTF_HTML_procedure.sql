-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE converte_rtf_html ( ds_sql_consulta_p text, ds_parametros_sql_p text, nm_usuario_p text, nr_sequencia_p INOUT text) AS $body$
DECLARE





/*
	Foi efetuada a mudanca da implementacao do corpo deste objeto para que ele
	chame as respectivas funcoes, agora converte_rtf_html2 e converte_rtf_string
	devido a incidencias de situacoes das OSs 743949, 666255.
*/
cd_estabelecimento_w	bigint;
cd_perfil_w		bigint;
ie_formatacao_w		varchar(1);


BEGIN

cd_perfil_w		:= somente_numero(wheb_usuario_pck.get_cd_perfil);
cd_estabelecimento_w	:= wheb_usuario_pck.get_cd_estabelecimento;
ie_formatacao_w := obter_param_usuario(0, 207, cd_perfil_w, nm_usuario_p, cd_estabelecimento_w, ie_formatacao_w);

if (ie_formatacao_w = 'N') then
	nr_sequencia_p := converte_rtf_string(ds_sql_consulta_p, ds_parametros_sql_p, nm_usuario_p, nr_sequencia_p);
elsif (ie_formatacao_w = 'R') then
	nr_sequencia_p := converte_rtf_html_sem_CRLF(ds_sql_consulta_p, ds_parametros_sql_p, nm_usuario_p, nr_sequencia_p);
else
	nr_sequencia_p := converte_rtf_html2(ds_sql_consulta_p, ds_parametros_sql_p, nm_usuario_p, nr_sequencia_p);
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE converte_rtf_html ( ds_sql_consulta_p text, ds_parametros_sql_p text, nm_usuario_p text, nr_sequencia_p INOUT text) FROM PUBLIC;

