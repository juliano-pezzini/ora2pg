-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_gerar_dados_proc_particp ( nr_sequencia_p bigint, nr_seq_prestador_p bigint, ie_tipo_guia_p text, cd_procedimento_p bigint, ie_origem_proced_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint) AS $body$
DECLARE

		 
nr_seq_regra_w		bigint;
cd_medico_executor_w	varchar(10);
nr_seq_grau_partic_w	bigint;


BEGIN 
if (nr_sequencia_p IS NOT NULL AND nr_sequencia_p::text <> '') and (nr_seq_prestador_p IS NOT NULL AND nr_seq_prestador_p::text <> '') and (ie_tipo_guia_p IS NOT NULL AND ie_tipo_guia_p::text <> '') and (cd_procedimento_p IS NOT NULL AND cd_procedimento_p::text <> '') and (ie_origem_proced_p IS NOT NULL AND ie_origem_proced_p::text <> '') and (nm_usuario_p IS NOT NULL AND nm_usuario_p::text <> '') and (cd_estabelecimento_p IS NOT NULL AND cd_estabelecimento_p::text <> '') then 
	begin 
	 
	CALL pls_atualiza_valor_proc( 
		nr_sequencia_p, 
		'N', 
		nm_usuario_p,'S',null,null);
	 
	SELECT * FROM pls_gerar_dados_procedimento( 
		nr_seq_prestador_p, ie_tipo_guia_p, cd_procedimento_p, ie_origem_proced_p, nm_usuario_p, cd_estabelecimento_p, nr_seq_regra_w, cd_medico_executor_w, nr_seq_grau_partic_w) INTO STRICT nr_seq_regra_w, cd_medico_executor_w, nr_seq_grau_partic_w;
	 
	if (cd_medico_executor_w IS NOT NULL AND cd_medico_executor_w::text <> '') then 
		begin 
		CALL pls_gerar_proc_participante( 
			nr_sequencia_p, 
			cd_medico_executor_w, 
			nr_seq_grau_partic_w, 
			nr_seq_regra_w, 
			nm_usuario_p);
		end;
	end if;
	end;
end if;
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_gerar_dados_proc_particp ( nr_sequencia_p bigint, nr_seq_prestador_p bigint, ie_tipo_guia_p text, cd_procedimento_p bigint, ie_origem_proced_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint) FROM PUBLIC;

