-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_w_sintese_cotacao ( tp_movimento_p text, dt_gravacao_p text, cd_cotacao_p text, cd_requisicao_p text, cd_condicao_pagamento_p text, dt_inicio_cotacao_p text, dt_fim_cotacao_p text, dt_validade_preco_p text, dt_cadastro_p text, ds_cotacao_p text, nm_usuario_cot_p text, ds_observacao_p text, ie_lido_p text, nm_usuario_p text, nr_seq_cotacao_p INOUT text) AS $body$
DECLARE


nr_seq_cotacao_w	bigint;
dt_gravacao_w		timestamp;
dt_inicio_cotacao_w	timestamp;
dt_fim_cotacao_w	timestamp;
dt_validade_preco_w	timestamp;
dt_cadastro_w		timestamp;


BEGIN

select	nextval('w_sintese_cotacao_seq')
into STRICT	nr_seq_cotacao_w
;

dt_gravacao_w := to_date(dt_gravacao_p, 'dd/mm/yyyy hh24:mi:ss');
dt_inicio_cotacao_w := to_date(dt_inicio_cotacao_p, 'dd/mm/yyyy hh24:mi:ss');
dt_fim_cotacao_w := to_date(dt_fim_cotacao_p, 'dd/mm/yyyy hh24:mi:ss');
dt_validade_preco_w := to_date(dt_validade_preco_p, 'dd/mm/yyyy hh24:mi:ss');
dt_cadastro_w := to_date(dt_cadastro_p, 'dd/mm/yyyy hh24:mi:ss');

insert into w_sintese_cotacao(
	nr_sequencia,
	dt_atualizacao,
	nm_usuario,
	dt_atualizacao_nrec,
	nm_usuario_nrec,
	tp_movimento,
	dt_gravacao,
	cd_cotacao,
	cd_requisicao,
	cd_condicao_pagamento,
	dt_inicio_cotacao,
	dt_fim_cotacao,
	dt_validade_preco,
	dt_cadastro,
	ds_cotacao,
	nm_usuario_cot,
	ds_observacao,
	ie_lido)
values (	nr_seq_cotacao_w,
	clock_timestamp(),
	nm_usuario_p,
	clock_timestamp(),
	nm_usuario_p,
	tp_movimento_p,
	dt_gravacao_w,
	cd_cotacao_p,
	cd_requisicao_p,
	cd_condicao_pagamento_p,
	dt_inicio_cotacao_w,
	dt_fim_cotacao_w,
	dt_validade_preco_w,
	dt_cadastro_w,
	substr(ds_cotacao_p,1,50),
	nm_usuario_cot_p,
	ds_observacao_p,
	ie_lido_p);

nr_seq_cotacao_p := nr_seq_cotacao_w;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_w_sintese_cotacao ( tp_movimento_p text, dt_gravacao_p text, cd_cotacao_p text, cd_requisicao_p text, cd_condicao_pagamento_p text, dt_inicio_cotacao_p text, dt_fim_cotacao_p text, dt_validade_preco_p text, dt_cadastro_p text, ds_cotacao_p text, nm_usuario_cot_p text, ds_observacao_p text, ie_lido_p text, nm_usuario_p text, nr_seq_cotacao_p INOUT text) FROM PUBLIC;

