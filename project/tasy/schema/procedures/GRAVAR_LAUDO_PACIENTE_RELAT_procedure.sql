-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gravar_laudo_paciente_relat (nr_seq_laudo_p bigint, nr_seq_relatorio_p bigint, nm_usuario_p text) AS $body$
DECLARE

 
nr_sequencia_w		bigint;
cd_classif_relat_w	varchar(04);
cd_relatorio_w		bigint;


BEGIN 
 
if (nr_seq_laudo_p > 0) and (nr_seq_relatorio_p > 0) then 
	begin 
 
	select	max(cd_classif_relat), 
		max(cd_relatorio) 
	into STRICT	cd_classif_relat_w, 
		cd_relatorio_w 
	from	relatorio 
	where	nr_sequencia	= nr_seq_relatorio_p;
 
	select	nextval('laudo_paciente_relat_seq') 
	into STRICT	nr_sequencia_w 
	;
 
	insert	into laudo_paciente_relat(nr_sequencia, 
		nr_seq_laudo, 
		cd_classif_relat, 
		cd_relatorio, 
		nm_usuario, 
		dt_atualizacao) 
	values (nr_sequencia_w, 
		nr_seq_laudo_p, 
		cd_classif_relat_w, 
		cd_relatorio_w, 
		nm_usuario_p, 
		clock_timestamp());
 
	end;
end if;
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gravar_laudo_paciente_relat (nr_seq_laudo_p bigint, nr_seq_relatorio_p bigint, nm_usuario_p text) FROM PUBLIC;

