-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_consistir_reaj_coletivo ( nr_seq_reajuste_p bigint, dt_reajuste_p timestamp, dt_periodo_inicial_p timestamp, dt_periodo_final_p timestamp, nm_usuario_p text) AS $body$
BEGIN

if (trunc(dt_reajuste_p,'dd') < trunc(dt_periodo_inicial_p,'dd')) or (trunc(dt_reajuste_p,'dd') > trunc(dt_periodo_final_p,'dd')) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(265915,'');
	--Mensagem: A data de aplicação do reajuste deve estar entre a data período inicial e final. Favor verifique!
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_consistir_reaj_coletivo ( nr_seq_reajuste_p bigint, dt_reajuste_p timestamp, dt_periodo_inicial_p timestamp, dt_periodo_final_p timestamp, nm_usuario_p text) FROM PUBLIC;

