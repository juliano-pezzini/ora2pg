-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE vipe_obter_gas ( cd_estabelecimento_p bigint, cd_setor_usuario_p bigint, cd_perfil_p bigint, nm_usuario_p text, nr_atendimento_p bigint, dt_inicial_horarios_p timestamp, dt_final_horarios_p timestamp, dt_validade_limite_p timestamp, ie_exibir_hor_realizados_p text, ie_exibir_hor_suspensos_p text, ie_regra_inclusao_p text, ie_data_lib_prescr_p text, ie_lib_pend_rep_p text, ie_exibir_suspensos_p text, ie_agrupar_acm_sn_p text, ie_prescr_setor_p text, cd_setor_paciente_p bigint) AS $body$
DECLARE

 
nr_seq_wadep_w		bigint;
nr_prescricao_w		bigint;
nr_seq_gasoterapia_w	bigint;
nr_seq_gas_w		bigint;
ds_gas_w		varchar(255);
ie_acm_sn_w		varchar(1);
cd_intervalo_w		varchar(7);
qt_gasoterapia_w	double precision;
ds_prescricao_w		varchar(100);
ds_config_gas_w		varchar(255);
ie_lib_pend_rep_w	varchar(1);
ie_status_gas_w		varchar(10);
ds_interv_prescr_w	varchar(15);
					
c01 CURSOR FOR 
SELECT 	--decode(ie_lib_pend_rep, null, decode(ie_acm_sn, 'S', decode(obter_se_agrupar_acm_sn_adep(nr_prescricao,nr_seq_gasoterapia,ie_agrupar_acm_sn_p), 'N', nr_prescricao, null), null), nr_prescricao), 
	--decode(ie_lib_pend_rep, null, decode(ie_acm_sn, 'S', decode(obter_se_agrupar_acm_sn_adep(nr_prescricao,nr_seq_gasoterapia,ie_agrupar_acm_sn_p), 'N', nr_seq_gasoterapia, null), null), nr_seq_gasoterapia), 
	CASE WHEN coalesce(ie_lib_pend_rep::text, '') = '' THEN  CASE WHEN ie_acm_sn='S' THEN  CASE WHEN obter_se_agrupar_acm_sn_adep(nr_prescricao,nr_seq_gasoterapia,ie_agrupar_acm_sn_p)='N' THEN  nr_prescricao  ELSE null END   ELSE nr_prescricao END   ELSE nr_prescricao END , 
	CASE WHEN coalesce(ie_lib_pend_rep::text, '') = '' THEN  CASE WHEN ie_acm_sn='S' THEN  CASE WHEN obter_se_agrupar_acm_sn_adep(nr_prescricao,nr_seq_gasoterapia,ie_agrupar_acm_sn_p)='N' THEN  nr_seq_gasoterapia  ELSE null END   ELSE nr_seq_gasoterapia END   ELSE nr_seq_gasoterapia END , 
	nr_seq_gas, 
	ds_gas, 
	ie_acm_sn,	 
	cd_intervalo, 
	qt_gasoterapia, 
	ds_prescricao, 
	ds_config_gas, 
	ie_lib_pend_rep, 
	ie_status_item, 
	ds_intervalo 
from	( 
	SELECT	a.nr_prescricao, 
		x.nr_sequencia nr_seq_gasoterapia, 
		x.nr_seq_gas, 
		y.ds_gas, 
		'N' ie_acm_sn,		 
		x.cd_intervalo, 
		x.qt_gasoterapia, 
		x.qt_gasoterapia || ' ' || w.ds_valor_dominio || ' ' || z.ds_intervalo ds_prescricao, 
		substr(adep_obter_config_gas(x.cd_modalidade_vent,x.ie_respiracao,x.ie_modo_adm),1,255) ds_config_gas, 
		CASE WHEN coalesce(a.dt_liberacao::text, '') = '' THEN substr(adep_obter_lib_pend_rep_gestao(a.dt_liberacao_medico,a.dt_liberacao,a.dt_liberacao_farmacia),1,1)  ELSE null END  ie_lib_pend_rep, 
		substr(Obter_Status_Gasoterapia(x.nr_sequencia, 'C'),1,10) ie_status_item, 
		substr(obter_desc_intervalo_prescr(x.cd_intervalo),1,15) ds_intervalo 
	FROM gas y, prescr_medica a, prescr_gasoterapia x
LEFT OUTER JOIN valor_dominio w ON (x.ie_unidade_medida = w.vl_dominio AND 1580 = w.cd_dominio)
LEFT OUTER JOIN intervalo_prescricao z ON (x.cd_intervalo = z.cd_intervalo)
WHERE y.nr_sequencia = x.nr_seq_gas and x.nr_prescricao = a.nr_prescricao and obter_se_exibir_rep_adep_setor(cd_setor_paciente_p,a.cd_setor_atendimento,coalesce(a.ie_adep,'S')) = 'S' and a.nr_atendimento = nr_atendimento_p and a.dt_validade_prescr > dt_validade_limite_p and obter_se_prescr_lib_adep(a.dt_liberacao_medico, a.dt_liberacao, a.dt_liberacao_farmacia, ie_data_lib_prescr_p) = 'S' and ((coalesce(x.dt_prev_execucao::text, '') = '') or (x.dt_prev_execucao between dt_inicial_horarios_p and dt_final_horarios_p)) and ((ie_regra_inclusao_p = 'S') or 
		 ((ie_regra_inclusao_p = 'R') and (adep_obter_regra_inclusao(	'OX', 
																		cd_estabelecimento_p, 
																		cd_setor_usuario_p, 
																		cd_perfil_p, 
																		null, 
																		null, 
																		null, 
																		null, 
																		a.cd_setor_Atendimento, 
																		null, 
																		null, -- -- nr_prescricao_p. Passei nulo porque criaram o param na adep_obter_regra_inclusao como default null, e não haviam passado nada 
																		null) = 'S'))) 	 -- nr_seq_exame_p 
  and ((ie_exibir_hor_realizados_p = 'S') or ((ie_exibir_hor_realizados_p = 'N') and coalesce(substr(Obter_Status_Gasoterapia(x.nr_sequencia, 'C'),1,10),'N') <> 'T')) and ((ie_exibir_hor_suspensos_p = 'S') or ((ie_exibir_hor_suspensos_p = 'N') and coalesce(substr(Obter_Status_Gasoterapia(x.nr_sequencia, 'C'),1,10),'N') <> 'S')) and ((ie_prescr_setor_p = 'N') or (ie_prescr_setor_p = 'S' AND a.cd_setor_atendimento = cd_setor_paciente_p)) group by 
		a.nr_prescricao, 
		x.nr_sequencia, 
		x.nr_seq_gas, 
		y.ds_gas, 
		'N', 
		x.cd_intervalo, 
		x.qt_gasoterapia, 
		w.ds_valor_dominio, 
		z.ds_intervalo, 
		x.cd_modalidade_vent, 
		x.ie_respiracao, 
		x.ie_modo_adm, 
		a.dt_liberacao_medico, 
		a.dt_liberacao, 
		a.dt_liberacao_farmacia, 
		substr(Obter_Status_Gasoterapia(x.nr_sequencia, 'C'),1,10) 
	) alias45 
group by 
	--decode(ie_lib_pend_rep, null, decode(ie_acm_sn, 'S', decode(obter_se_agrupar_acm_sn_adep(nr_prescricao,nr_seq_gasoterapia,ie_agrupar_acm_sn_p), 'N', nr_prescricao, null), null), nr_prescricao), 
	--decode(ie_lib_pend_rep, null, decode(ie_acm_sn, 'S', decode(obter_se_agrupar_acm_sn_adep(nr_prescricao,nr_seq_gasoterapia,ie_agrupar_acm_sn_p), 'N', nr_seq_gasoterapia, null), null), nr_seq_gasoterapia), 
	CASE WHEN coalesce(ie_lib_pend_rep::text, '') = '' THEN  CASE WHEN ie_acm_sn='S' THEN  CASE WHEN obter_se_agrupar_acm_sn_adep(nr_prescricao,nr_seq_gasoterapia,ie_agrupar_acm_sn_p)='N' THEN  nr_prescricao  ELSE null END   ELSE nr_prescricao END   ELSE nr_prescricao END , 
	CASE WHEN coalesce(ie_lib_pend_rep::text, '') = '' THEN  CASE WHEN ie_acm_sn='S' THEN  CASE WHEN obter_se_agrupar_acm_sn_adep(nr_prescricao,nr_seq_gasoterapia,ie_agrupar_acm_sn_p)='N' THEN  nr_seq_gasoterapia  ELSE null END   ELSE nr_seq_gasoterapia END   ELSE nr_seq_gasoterapia END , 
	nr_seq_gas, 
	ds_gas, 
	ie_acm_sn,	 
	cd_intervalo, 
	qt_gasoterapia, 
	ds_prescricao, 
	ds_config_gas, 
	ie_lib_pend_rep, 
	ie_status_item, 
	ds_intervalo;
	

BEGIN 
open c01;
loop 
fetch c01 into	nr_prescricao_w, 
		nr_seq_gasoterapia_w, 
		nr_seq_gas_w, 
		ds_gas_w, 
		ie_acm_sn_w,		 
		cd_intervalo_w, 
		qt_gasoterapia_w, 
		ds_prescricao_w, 
		ds_config_gas_w, 
		ie_lib_pend_rep_w, 
		ie_status_gas_w, 
		ds_interv_prescr_w;
EXIT WHEN NOT FOUND; /* apply on c01 */
	begin 
	select	nextval('w_vipe_t_seq') 
	into STRICT	nr_seq_wadep_w 
	;
	 
	insert into w_vipe_t( 
		nr_sequencia, 
		nm_usuario, 
		ie_tipo_item, 
		nr_prescricao, 
		nr_seq_item,		 
		cd_item, 
		ds_item, 
		ie_acm_sn,		 
		cd_intervalo, 
		qt_item, 
		ds_prescricao, 
		ds_diluicao, 
		nr_agrupamento, 
		ie_diferenciado, 
		ie_pendente_liberacao, 
		ie_status_item, 
		ds_interv_prescr) 
	values ( 
		nr_seq_wadep_w, 
		nm_usuario_p, 
		'O', 
		nr_prescricao_w, 
		nr_seq_gasoterapia_w, 
		nr_seq_gas_w, 
		ds_gas_w, 
		ie_acm_sn_w, 
		cd_intervalo_w, 
		qt_gasoterapia_w, 
		ds_prescricao_w, 
		ds_config_gas_w, 
		0, 
		'N', 
		ie_lib_pend_rep_w, 
		ie_status_gas_w, 
		ds_interv_prescr_w);
	end;
end loop;
close c01;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE vipe_obter_gas ( cd_estabelecimento_p bigint, cd_setor_usuario_p bigint, cd_perfil_p bigint, nm_usuario_p text, nr_atendimento_p bigint, dt_inicial_horarios_p timestamp, dt_final_horarios_p timestamp, dt_validade_limite_p timestamp, ie_exibir_hor_realizados_p text, ie_exibir_hor_suspensos_p text, ie_regra_inclusao_p text, ie_data_lib_prescr_p text, ie_lib_pend_rep_p text, ie_exibir_suspensos_p text, ie_agrupar_acm_sn_p text, ie_prescr_setor_p text, cd_setor_paciente_p bigint) FROM PUBLIC;

