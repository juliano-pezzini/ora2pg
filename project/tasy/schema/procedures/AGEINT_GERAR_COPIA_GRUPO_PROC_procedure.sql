-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE ageint_gerar_copia_grupo_proc ( nr_seq_grupo_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint, nr_seq_regra_p INOUT bigint) AS $body$
DECLARE


nr_seq_proc_interno_w	bigint;
ds_grupo_w		varchar(80);
nr_seq_grupo_w		bigint;

C01 CURSOR FOR
	SELECT	nr_Seq_proc_interno
	from	agenda_int_grupo_item
	where	nr_Seq_grupo	= nr_Seq_grupo_p
	and	(nr_seq_proc_interno IS NOT NULL AND nr_seq_proc_interno::text <> '')
	order by nr_Seq_Apres;


BEGIN

select	max(ds_grupo)
into STRICT	ds_grupo_w
from	agenda_int_grupo
where	nr_sequencia	= nr_seq_grupo_p;

select	nextval('ageint_regra_grupo_proc_seq')
into STRICT	nr_seq_grupo_w
;

insert into ageint_regra_grupo_proc(nr_Sequencia,
	dt_atualizacao,
	nm_usuario,
	dt_Atualizacao_nrec,
	nm_usuario_nrec,
	ie_situacao,
	ds_grupo)
values (nr_Seq_grupo_w,
	clock_timestamp(),
	nm_usuario_p,
	clock_timestamp(),
	nm_usuario_p,
	'A',
	ds_grupo_w);

open C01;
loop
fetch C01 into
	nr_seq_proc_interno_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin
	insert into ageint_proced_regra(nr_sequencia,
		dt_atualizacao,
		nm_usuario,
		dt_atualizacao_nrec,
		nm_usuario_nrec,
		nr_Seq_proc_interno,
		ie_situacao,
		nr_seq_grupo)
	values (nextval('ageint_proced_regra_seq'),
		clock_timestamp(),
		nm_usuario_p,
		clock_timestamp(),
		nm_usuario_p,
		nr_seq_proc_interno_w,
		'A',
		nr_seq_grupo_w);
	end;
end loop;
close C01;

nr_seq_regra_p	:= nr_seq_grupo_w;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE ageint_gerar_copia_grupo_proc ( nr_seq_grupo_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint, nr_seq_regra_p INOUT bigint) FROM PUBLIC;

