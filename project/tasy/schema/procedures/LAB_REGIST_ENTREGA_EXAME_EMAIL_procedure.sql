-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE lab_regist_entrega_exame_email ( seq_prescr_p bigint, nr_prescricao_p bigint, ds_email_p text, dt_entrega_p timestamp, nm_usuario_p text) AS $body$
DECLARE


nr_seq_prescr_w		integer;
nr_seq_entrega_w	bigint;

ieParametro334		varchar(1);

ie_ds_observacao_obrig_w		varchar(1);

c01 CURSOR FOR
SELECT	nr_sequencia
from	prescr_procedimento
where	nr_prescricao = nr_prescricao_p
and	ie_status_atend > 30
and	((seq_prescr_p = nr_sequencia) or (coalesce(seq_prescr_p::text, '') = ''));


BEGIN

ieParametro334	:= obter_valor_param_usuario(722, 334, obter_perfil_ativo, nm_usuario_p, obter_estabelecimento_ativo);

open c01;
loop
fetch c01 into
	nr_seq_prescr_w;
EXIT WHEN NOT FOUND; /* apply on c01 */

	select 	nextval('lab_entrega_exame_seq')
	into STRICT	nr_seq_entrega_w
	;

	insert	into lab_entrega_exame(
		nr_sequencia,
		dt_atualizacao,
		nm_usuario,
		dt_atualizacao_nrec,
		nm_usuario_nrec,
		nr_prescricao,
		nr_seq_prescr,
		ds_email,
		dt_entrega,
		ie_situacao)
	values (
		nr_seq_entrega_w,
		clock_timestamp(),
		nm_usuario_p,
		clock_timestamp(),
		nm_usuario_p,
		nr_prescricao_p,
		nr_seq_prescr_w,
		substr(coalesce(ds_email_p, ''), 1, 255),
		dt_entrega_p,
		'A');

end loop;
close c01;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE lab_regist_entrega_exame_email ( seq_prescr_p bigint, nr_prescricao_p bigint, ds_email_p text, dt_entrega_p timestamp, nm_usuario_p text) FROM PUBLIC;

