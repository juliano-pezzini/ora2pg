-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE inserir_genero_pessoa ( cd_pessoa_fisica_p pessoa_fisica.cd_pessoa_fisica%type, nm_usuario_p text, nr_seq_genero_p pessoa_fisica_aux.nr_seq_genero%type ) AS $body$
DECLARE


nr_seq_pessoa_fisica_aux_w	pessoa_fisica_aux.nr_sequencia%type;
nr_seq_pessoa_aux_w			pessoa_fisica_aux.nr_sequencia%type;


BEGIN


if (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') then

	select max(nr_sequencia)
	into STRICT nr_seq_pessoa_aux_w
	from pessoa_fisica_aux 
	where cd_pessoa_fisica = cd_pessoa_fisica_p;


	if (coalesce(nr_seq_pessoa_aux_w::text, '') = '') then

		select	nextval('pessoa_fisica_aux_seq')
				into STRICT	nr_seq_pessoa_fisica_aux_w LIMIT 1;

		insert into pessoa_fisica_aux(
			nr_sequencia,
			dt_atualizacao,
			nm_usuario,
			dt_atualizacao_nrec,
			nm_usuario_nrec,
			cd_pessoa_fisica,
			nr_seq_genero
		) values (
			nr_seq_pessoa_fisica_aux_w,
			clock_timestamp(),
			nm_usuario_p,
			clock_timestamp(),
			nm_usuario_p,
			cd_pessoa_fisica_p,
			nr_seq_genero_p
		);

	elsif (nr_seq_pessoa_aux_w IS NOT NULL AND nr_seq_pessoa_aux_w::text <> '') then

		update pessoa_fisica_aux p 
		set p.nr_seq_genero = nr_seq_genero_p,
			p.dt_atualizacao_nrec = clock_timestamp(),
			p.nm_usuario_nrec = nm_usuario_p
		where p.cd_pessoa_fisica = cd_pessoa_fisica_p
		and p.nr_sequencia = nr_seq_pessoa_aux_w;

	end if;

end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE inserir_genero_pessoa ( cd_pessoa_fisica_p pessoa_fisica.cd_pessoa_fisica%type, nm_usuario_p text, nr_seq_genero_p pessoa_fisica_aux.nr_seq_genero%type ) FROM PUBLIC;

