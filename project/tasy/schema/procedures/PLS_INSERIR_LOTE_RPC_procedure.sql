-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_inserir_lote_rpc ( nr_seq_contrato_p bigint, nr_seq_reajuste_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint) AS $body$
DECLARE

 
nr_seq_lote_w	bigint;
nr_contrato_w	bigint;


BEGIN 
 
select	max(nr_contrato) 
into STRICT	nr_contrato_w 
from	pls_contrato 
where	nr_sequencia	= nr_seq_contrato_p;
 
 
select	nextval('pls_rpc_lote_seq') 
into STRICT	nr_seq_lote_w
;
 
insert	into	pls_rpc_lote(	nr_sequencia, cd_estabelecimento, dt_atualizacao, 
		nm_usuario, dt_atualizacao_nrec, nm_usuario_nrec, 
		nr_seq_lote_reajuste, nr_seq_contrato, 
		nr_contrato) 
	values (	nr_seq_lote_w, cd_estabelecimento_p, clock_timestamp(), 
		nm_usuario_p, clock_timestamp(), nm_usuario_p, 
		nr_seq_reajuste_p, nr_seq_contrato_p, 
		nr_contrato_w);
 
CALL pls_gerar_lote_rpc(nr_seq_lote_w, 'G', nm_usuario_p, cd_estabelecimento_p);
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_inserir_lote_rpc ( nr_seq_contrato_p bigint, nr_seq_reajuste_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint) FROM PUBLIC;

