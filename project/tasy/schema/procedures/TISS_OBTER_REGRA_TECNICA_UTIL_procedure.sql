-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE tiss_obter_regra_tecnica_util (cd_area_procedimento_p bigint, cd_especialidade_p bigint, cd_grupo_proc_p bigint, cd_procedimento_p bigint, ie_origem_proced_p bigint, cd_convenio_p bigint, cd_estabelecimento_p bigint, ie_utilizou_video_p text, nr_seq_proc_interno_p bigint, ie_tecnica_utilizada_p INOUT text) AS $body$
DECLARE


ds_retorno_w		varchar(2);
ie_tecnica_utilizada_w	varchar(2);

c01 CURSOR FOR
SELECT	ie_tecnica_utilizada
from	tiss_regra_tecnica_util
where	coalesce(cd_area_procedimento, coalesce(cd_area_procedimento_p,0)) = coalesce(cd_area_procedimento_p, 0)
and	coalesce(cd_especialidade, coalesce(cd_especialidade_p, 0))	= coalesce(cd_especialidade_p, 0)
and	coalesce(cd_grupo_proc, coalesce(cd_grupo_proc_p, 0))		= coalesce(cd_grupo_proc_p, 0)
and	coalesce(cd_procedimento, coalesce(cd_procedimento_p, 0))		= coalesce(cd_procedimento_p, 0)
and	((coalesce(cd_procedimento,0) = 0) or (coalesce(ie_origem_proced,coalesce(ie_origem_proced_p, 0)) = coalesce(ie_origem_proced_p, 0)))
and	coalesce(cd_convenio, coalesce(cd_convenio_p,0))			= coalesce(cd_convenio_p, 0)
and	coalesce(nr_seq_proc_interno, coalesce(nr_seq_proc_interno_p,0))	= coalesce(nr_seq_proc_interno_p,0)
and	((coalesce(ie_video, 'A') = coalesce(ie_utilizou_video_p, 'A')) or (coalesce(ie_video, 'A') = 'A'))
and	cd_estabelecimento					= cd_estabelecimento_p
order by coalesce(cd_procedimento, 0),
	 coalesce(nr_seq_proc_interno,0),
	 coalesce(cd_grupo_proc, 0),
	 coalesce(cd_especialidade, 0),
	 coalesce(cd_area_procedimento, 0),
	 coalesce(cd_convenio,0);


BEGIN

open c01;
loop
fetch c01 into
	ie_tecnica_utilizada_w;
EXIT WHEN NOT FOUND; /* apply on c01 */
	ie_tecnica_utilizada_w	:= ie_tecnica_utilizada_w;
end loop;
close c01;

ie_tecnica_utilizada_p	:= ie_tecnica_utilizada_w;
	
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE tiss_obter_regra_tecnica_util (cd_area_procedimento_p bigint, cd_especialidade_p bigint, cd_grupo_proc_p bigint, cd_procedimento_p bigint, ie_origem_proced_p bigint, cd_convenio_p bigint, cd_estabelecimento_p bigint, ie_utilizou_video_p text, nr_seq_proc_interno_p bigint, ie_tecnica_utilizada_p INOUT text) FROM PUBLIC;

