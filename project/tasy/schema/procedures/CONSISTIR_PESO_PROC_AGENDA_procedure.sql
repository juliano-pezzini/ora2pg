-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consistir_peso_proc_agenda (cd_procedimento_p bigint, ie_origem_proced_p bigint, qt_peso_P bigint, ds_erro_p INOUT text) AS $body$
DECLARE


ie_exige_peso_agenda_w	varchar(1);


BEGIN

select	coalesce(max(ie_exige_peso_agenda),'N')
into STRICT	ie_exige_peso_agenda_w
from	procedimento
where	cd_procedimento		= cd_procedimento_p
and	ie_origem_proced	= ie_origem_proced_p;

if (ie_exige_peso_agenda_w = 'S') and (coalesce(qt_peso_p::text, '') = '') then

	ds_erro_p := WHEB_MENSAGEM_PCK.get_texto(278637,null);

end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consistir_peso_proc_agenda (cd_procedimento_p bigint, ie_origem_proced_p bigint, qt_peso_P bigint, ds_erro_p INOUT text) FROM PUBLIC;

