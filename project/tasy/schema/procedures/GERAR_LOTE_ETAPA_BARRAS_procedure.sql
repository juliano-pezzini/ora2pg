-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_lote_etapa_barras ( cd_estabelecimento_p bigint, nr_seq_etapa_origem_p bigint, nr_seq_etapa_destino_p bigint, nm_usuario_p text, nr_seq_lote_p INOUT bigint) AS $body$
DECLARE


nr_seq_lote_w  bigint;


BEGIN

select 	nextval('lote_etapa_seq')
into STRICT 	nr_seq_lote_w
;

insert into lote_etapa(
       nr_sequencia,
	   cd_estabelecimento,
	   dt_atualizacao,
	   nm_usuario,
	   dt_geracao_lote,
	   nr_seq_etapa_origem,
	   nr_seq_etapa_destino)
values (nr_seq_lote_w,
	   cd_estabelecimento_p,
	   clock_timestamp(),
	   nm_usuario_p,
	   clock_timestamp(),
	   nr_seq_etapa_origem_p,
	   nr_seq_etapa_destino_p);


Commit;
nr_seq_lote_p := nr_seq_lote_w;



END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_lote_etapa_barras ( cd_estabelecimento_p bigint, nr_seq_etapa_origem_p bigint, nr_seq_etapa_destino_p bigint, nm_usuario_p text, nr_seq_lote_p INOUT bigint) FROM PUBLIC;

