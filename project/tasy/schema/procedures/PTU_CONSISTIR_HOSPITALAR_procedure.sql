-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE ptu_consistir_hospitalar ( nr_seq_nota_cobr_p bigint, nr_seq_fatura_p bigint, nm_usuario_p text) AS $body$
DECLARE


nr_lote_w			integer;
nr_nota_w			ptu_nota_hospitalar.nr_nota%type;
cd_unimed_hospital_w		smallint;
cd_hospital_w			integer;
nm_hospital_w			varchar(25);
ie_tipo_acomodacao_w		varchar(2);
dt_internacao_w			timestamp;
dt_alta_w			timestamp;
tx_mult_amb_w			real;
cd_motivo_saida_w		varchar(2);
cd_cgc_hospital_w		varchar(14);
ie_tipo_internacao_w		smallint;
ie_faturamento_w		smallint;
nr_sequencia_w			bigint;
C01 CURSOR FOR
	SELECT	nr_sequencia,
		nr_lote,
		nr_nota,
		cd_unimed_hospital,
		cd_hospital,
		nm_hospital,
		ie_tipo_acomodacao,
		dt_internacao,
		dt_alta,
		tx_mult_amb,
		cd_motivo_saida,
		cd_cgc_hospital,
		ie_tipo_internacao,
		ie_faturamento
	from	ptu_nota_hospitalar
	where	nr_seq_nota_cobr	= nr_seq_nota_cobr_p;

BEGIN

/*open C01;
loop
fetch C01 into
	nr_sequencia_w,
	nr_lote_w,
	nr_nota_w,
	cd_unimed_hospital_w,
	cd_hospital_w,
	nm_hospital_w,
	ie_tipo_acomodacao_w,
	dt_internacao_w,
	dt_alta_w,
	tx_mult_amb_w,
	cd_motivo_saida_w,
	cd_cgc_hospital_w,
	ie_tipo_internacao_w,
	ie_faturamento_w;
exit when C01%notfound;
	begin
	if	(nr_lote_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '503', 'NR_LOTE', null, null, nr_sequencia_w,nm_usuario_p);
	end if;

	if	(nr_nota_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '503', 'NR_NOTA', null, null, nr_sequencia_w,nm_usuario_p);
	end if;

	if	(cd_unimed_hospital_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '503', 'CD_UNI_HOSP', null, null, nr_sequencia_w,nm_usuario_p);
	end if;

	if	(cd_hospital_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '503', 'CD_HOSP', null, null, nr_sequencia_w,nm_usuario_p);
	end if;

	if	(nm_hospital_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '503', 'NM_HOSP', null, null, nr_sequencia_w,nm_usuario_p);
	end if;

	if	(ie_tipo_acomodacao_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '503', 'TP_ACOMODACAO', null, null, nr_sequencia_w,nm_usuario_p);
	end if;

	if	(dt_internacao_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '503', 'DT_INTERNACAO', null, null, nr_sequencia_w,nm_usuario_p);
	end if;

	if	(dt_alta_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '503', 'DT_ALTA', null, null, nr_sequencia_w,nm_usuario_p);
	end if;

	if	(tx_mult_amb_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '503', 'FT_MULT_AMB', null, null, nr_sequencia_w,nm_usuario_p);
	end if;

	if	(cd_motivo_saida_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '503', 'TP_SAIDA', null, null, nr_sequencia_w,nm_usuario_p);
	end if;

	if	(cd_cgc_hospital_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '503', 'CGC_HOSPITAL', null, null, nr_sequencia_w,nm_usuario_p);
	end if;

	if	(ie_tipo_internacao_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '503', 'TP_INTERNACAO', null, null, nr_sequencia_w,nm_usuario_p);
	end if;

	if	(ie_faturamento_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '503', 'TP_FATURAMENTO', null, null, nr_sequencia_w,nm_usuario_p);
	end if;
	end;
end loop;
close C01;*/
cd_cgc_hospital_w	:= 'A';
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE ptu_consistir_hospitalar ( nr_seq_nota_cobr_p bigint, nr_seq_fatura_p bigint, nm_usuario_p text) FROM PUBLIC;

