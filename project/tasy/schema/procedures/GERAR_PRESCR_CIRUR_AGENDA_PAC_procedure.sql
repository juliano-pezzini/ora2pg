-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_prescr_cirur_agenda_pac ( nr_cirurgia_p bigint, nr_seq_agenda_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text, ie_tipo_pessoa_p text) AS $body$
DECLARE

nr_prescricao_w bigint;
ds_valor_parametro_w varchar(255);

BEGIN
if (nm_usuario_p IS NOT NULL AND nm_usuario_p::text <> '') and (nr_seq_agenda_p IS NOT NULL AND nr_seq_agenda_p::text <> '') and (cd_estabelecimento_p IS NOT NULL AND cd_estabelecimento_p::text <> '') then 
	begin 
	begin 
	nr_prescricao_w := gerar_prescricao_agenda( 
		nr_seq_agenda_p, nm_usuario_p, nr_prescricao_w, cd_estabelecimento_p);
	exception 
	when others then 
		CALL Wheb_mensagem_pck.exibir_mensagem_abort(37768);	
	end;
	 
	CALL gerar_proc_agenda_cirurgia(nr_seq_agenda_p, nr_prescricao_w, nm_usuario_p);
	 
	CALL gerar_prescricao_cirurgia( 
		0, 
		0, 
		nr_cirurgia_p, 
		nr_seq_agenda_p, 
		ie_tipo_pessoa_p, 
		nm_usuario_p, 
		nr_prescricao_w, 
		0, 
		0, 
		871, 
		wheb_usuario_pck.get_cd_perfil, 
		null, 
		null, 
		null, 
		null);
	 
	ds_valor_parametro_w := obter_parametro_funcao(871, 405, nm_usuario_p);
	if ('S' = ds_valor_parametro_w) then 
		begin 
		CALL gerar_prescr_patologia_cirur( 
			nr_seq_agenda_p, 
			nm_usuario_p, 
			cd_estabelecimento_p);
		end;
	end if;
	 
	ds_valor_parametro_w := obter_parametro_funcao(871, 405, nm_usuario_p);
	if ('S' <> ds_valor_parametro_w) then 
		begin 
		CALL gerar_opme_prescr_cirurgia(		 
			nr_seq_agenda_p, 
			ie_tipo_pessoa_p, 
			nm_usuario_p, 
			nr_prescricao_w, 
			cd_estabelecimento_p);
		end;
	end if;
	 
	 
	 
	end;	
end if;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_prescr_cirur_agenda_pac ( nr_cirurgia_p bigint, nr_seq_agenda_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text, ie_tipo_pessoa_p text) FROM PUBLIC;

