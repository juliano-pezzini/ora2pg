-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gc_handlerbar_excluir_js ( nr_seq_proc_princ_p bigint, nr_prescricao_p bigint, qt_proced_pac_p INOUT bigint, qt_prescr_proced_p INOUT bigint, ds_pergunta_p INOUT text ) AS $body$
DECLARE


qt_proced_pac_w		bigint := 0;
qt_prescr_proced_w	bigint := 0;

BEGIN

if (nr_seq_proc_princ_p IS NOT NULL AND nr_seq_proc_princ_p::text <> '') then
	begin
	select	count(*)
	into STRICT	qt_proced_pac_w
	from	procedimento_paciente
	where	nr_seq_proc_princ = nr_seq_proc_princ_p;
	end;
end if;

if (nr_prescricao_p IS NOT NULL AND nr_prescricao_p::text <> '') then
	begin
	select	count(*)
	into STRICT	qt_prescr_proced_w
	from	prescr_procedimento
	where	nr_prescricao = nr_prescricao_p;
	end;
end if;

ds_pergunta_p := substr(obter_texto_tasy(54483, wheb_usuario_pck.get_nr_seq_idioma),1,255);

qt_proced_pac_p		:= qt_proced_pac_w;
qt_prescr_proced_p 	:= qt_prescr_proced_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gc_handlerbar_excluir_js ( nr_seq_proc_princ_p bigint, nr_prescricao_p bigint, qt_proced_pac_p INOUT bigint, qt_prescr_proced_p INOUT bigint, ds_pergunta_p INOUT text ) FROM PUBLIC;

