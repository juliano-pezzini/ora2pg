-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE tiss_gerar_anexo_item_imp (nm_usuario_p text, nr_seq_guia_p bigint, cd_procedimento_p text, cd_edicao_amb_p text, ds_procedimento_p text, qt_solicitada_p text, qt_autorizada_p text, vl_solicitado_p text, vl_autorizado_p text, nr_opcao_fabricante_p text, nr_registro_anvisa_p text, cd_ref_fabricante_p text, nr_autorizacao_func_p text, nr_seq_item_p INOUT bigint) AS $body$
DECLARE



nr_sequencia_w		bigint;
qt_solicitada_w		smallint;
qt_autorizada_w		smallint;
vl_solicitado_w		double precision;
vl_autorizado_w		double precision;


BEGIN

begin
	qt_solicitada_w	:= (replace(qt_solicitada_p,'.',','))::numeric;
exception
when others then
	qt_solicitada_w	:= 0;
end;
begin
	qt_autorizada_w	:= replace(qt_autorizada_p,'.',',');
exception
when others then
	qt_autorizada_w	:= 0;
end;
begin
	vl_solicitado_w	:= replace(vl_solicitado_p,'.',',');
exception
when others then
	vl_solicitado_w	:= 0;
end;
begin
	vl_autorizado_w := replace(vl_autorizado_p,'.',',');
exception
when others then
	vl_autorizado_w	:= 0;
end;

select	nextval('tiss_anexo_guia_item_imp_seq')
into STRICT	nr_sequencia_w
;

insert into tiss_anexo_guia_item_imp(nr_sequencia,
	dt_atualizacao,
	nm_usuario,
	dt_atualizacao_nrec,
	nm_usuario_nrec,
	nr_seq_guia_imp,
	cd_procedimento,
	cd_edicao_amb,
	ds_procedimento,
	qt_solicitada,
	qt_autorizada,
	vl_solicitado,
	vl_autorizado,
	nr_opcao_fabricante,
	nr_registro_anvisa,
	cd_ref_fabricante,
	nr_autorizacao_func)
values (nr_sequencia_w,
	clock_timestamp(),
	nm_usuario_p,
	clock_timestamp(),
	nm_usuario_p,
	nr_seq_guia_p,
	cd_procedimento_p,
	cd_edicao_amb_p,
	ds_procedimento_p,
	qt_solicitada_w,
	qt_autorizada_w,
	vl_solicitado_w,
	vl_autorizado_w,
	nr_opcao_fabricante_p,
	nr_registro_anvisa_p,
	cd_ref_fabricante_p,
	nr_autorizacao_func_p);

nr_seq_item_p	:= nr_sequencia_w;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE tiss_gerar_anexo_item_imp (nm_usuario_p text, nr_seq_guia_p bigint, cd_procedimento_p text, cd_edicao_amb_p text, ds_procedimento_p text, qt_solicitada_p text, qt_autorizada_p text, vl_solicitado_p text, vl_autorizado_p text, nr_opcao_fabricante_p text, nr_registro_anvisa_p text, cd_ref_fabricante_p text, nr_autorizacao_func_p text, nr_seq_item_p INOUT bigint) FROM PUBLIC;

