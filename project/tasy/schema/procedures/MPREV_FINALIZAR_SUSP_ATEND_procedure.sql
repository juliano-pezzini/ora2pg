-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE mprev_finalizar_susp_atend ( nr_seq_suspensao_p bigint, dt_final_p timestamp, nm_usuario_p text) AS $body$
DECLARE


/* +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Finalidade:  Preencher a data final do registro de suspensão de atendimento do participante
-------------------------------------------------------------------------------------------------------------------
Locais de chamada direta:
[  ]  Objetos do dicionário [ x ] Tasy (Delphi/Java) [ ] Portal [  ]  Relatórios [ ] Outros:
 ------------------------------------------------------------------------------------------------------------------
Pontos de atenção:
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ */
nr_seq_participante_w		mprev_suspensao_atend.nr_seq_participante%type;
dt_inicio_w			mprev_suspensao_atend.dt_inicio%type;
ie_tipo_w			mprev_motivo_susp_atend.ie_tipo%type;
nr_seq_motivo_w			mprev_suspensao_atend.nr_seq_motivo%type;


BEGIN

select	nr_seq_participante
into STRICT	nr_seq_participante_w
from	mprev_suspensao_atend
where 	nr_sequencia = nr_seq_suspensao_p;

select	dt_inicio,
	nr_seq_motivo
into STRICT	dt_inicio_w,
	nr_seq_motivo_w
from 	mprev_suspensao_atend
where 	nr_sequencia = nr_seq_suspensao_p;

if (dt_final_p >= dt_inicio_w) then
	update	mprev_suspensao_atend
	set	dt_termino = dt_final_p,
		dt_atualizacao = clock_timestamp(),
		nm_usuario = nm_usuario_p
	where 	nr_sequencia = nr_seq_suspensao_p;

	update	mprev_participante
	set	ie_situacao = 'A',
		dt_atualizacao = clock_timestamp(),
		nm_usuario = nm_usuario_p
	where 	nr_sequencia = nr_seq_participante_w;

	if (nr_seq_motivo_w IS NOT NULL AND nr_seq_motivo_w::text <> '') then
		select	ie_tipo
		into STRICT	ie_tipo_w
		from 	mprev_motivo_susp_atend
		where	nr_sequencia = nr_seq_motivo_w;

		if (ie_tipo_w = 'INT') then
			update	mprev_participante
			set	ie_internacao =	'R'
			where 	nr_sequencia = nr_seq_participante_w;
		end if;
	end if;

else
	CALL wheb_mensagem_pck.exibir_mensagem_abort(320493,null);
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE mprev_finalizar_susp_atend ( nr_seq_suspensao_p bigint, dt_final_p timestamp, nm_usuario_p text) FROM PUBLIC;

