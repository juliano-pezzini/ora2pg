-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE solicitar_resultado_pardiniws ( NR_SEQ_LOTE_EXTERNO_P bigint, CD_ESTABELECIMENTO_P bigint, DT_INICIAL timestamp default null, DT_FINAL timestamp default null) AS $body$
BEGIN

    CALL gravar_agend_integracao(678,
            'nr_seq_lote=' || NR_SEQ_LOTE_EXTERNO_P || ';' ||
            'cd_estabelecimento=' || CD_ESTABELECIMENTO_P || ';' ||
            'dt_inicial=' || to_char(DT_INICIAL, 'yyyy-mm-dd') || ';' ||
            'dt_final=' || to_char(DT_FINAL, 'yyyy-mm-dd') || ';' ||
            'hr_inicial=' || to_char(DT_INICIAL, 'hh24:mi:ss') || ';' ||
            'hr_final=' || to_char(DT_FINAL, 'hh24:mi:ss') || ';');

    CALL lab_set_lote_integrando(
        nr_sequencia_p => nr_seq_lote_externo_p,
        ie_lote_integrando_p => 'R'
    );

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE solicitar_resultado_pardiniws ( NR_SEQ_LOTE_EXTERNO_P bigint, CD_ESTABELECIMENTO_P bigint, DT_INICIAL timestamp default null, DT_FINAL timestamp default null) FROM PUBLIC;

