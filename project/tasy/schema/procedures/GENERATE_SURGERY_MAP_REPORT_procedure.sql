-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE generate_surgery_map_report (nr_seq_agenda_p bigint, ds_file_path_p text) AS $body$
DECLARE


nm_usuario_w		varchar(15);


BEGIN

nm_usuario_w			:= wheb_usuario_pck.get_nm_usuario;

delete FROM agenda_pac_email_anexo
where nr_seq_agenda = nr_seq_agenda_p;

if (ds_file_path_p IS NOT NULL AND ds_file_path_p::text <> '') then

	insert into agenda_pac_email_anexo(NR_SEQUENCIA,
										DT_ATUALIZACAO,
										NM_USUARIO,
										DS_ARQUIVO,
										NR_SEQ_AGENDA)
								values((SELECT coalesce(max(nr_sequencia), 0) + 1 from agenda_pac_email_anexo),
										clock_timestamp(),
										nm_usuario_w,
										ds_file_path_p,
										nr_seq_agenda_p);
end if;

commit;


end	;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE generate_surgery_map_report (nr_seq_agenda_p bigint, ds_file_path_p text) FROM PUBLIC;

