-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE baca_pls_glosa_custos_assist () AS $body$
DECLARE


qt_registro_w		bigint;
nr_seq_motivo_glosa_w	bigint;



BEGIN

/* Glosa 9922 - Protocolo de guias inexistente ou não informado no recurso de glosa. */

select	count(1)
into STRICT	qt_registro_w
from	tiss_motivo_glosa
where	cd_motivo_tiss	= 9922;

if ( qt_registro_w = 0 ) then
	insert into tiss_motivo_glosa(	nr_sequencia, dt_atualizacao, nm_usuario,
					dt_atualizacao_nrec, nm_usuario_nrec, ie_grupo_glosa,
					cd_motivo_tiss, ds_motivo_tiss, ie_acao_pls,
					cd_convenio, cd_motivo_glosa, ie_fechar_conta,
					ie_importacao_ops, ie_param_ops, ie_permite_alteracao_ops,
					ie_regra_integracao_ops)
				values (	nextval('tiss_motivo_glosa_seq'), clock_timestamp(), 'ejsilva',
					clock_timestamp(), 'ejsilva', 22,
					'9922', 'Protocolo de guias inexistente ou não informado no recurso de glosa', 'D',
					null,null, 'S',
					'N', 'N', 'N',
					'N');
end if;



/* Glosa 9923 - Guia não pertence ao protocolo de guias enviado no recurso de glosa */

select	count(1)
into STRICT	qt_registro_w
from	tiss_motivo_glosa
where	cd_motivo_tiss	= 9923;

if ( qt_registro_w = 0 ) then
	insert into tiss_motivo_glosa(	nr_sequencia, dt_atualizacao, nm_usuario,
					dt_atualizacao_nrec, nm_usuario_nrec, ie_grupo_glosa,
					cd_motivo_tiss, ds_motivo_tiss, ie_acao_pls,
					cd_convenio, cd_motivo_glosa, ie_fechar_conta,
					ie_importacao_ops, ie_param_ops, ie_permite_alteracao_ops,
					ie_regra_integracao_ops)
				values (	nextval('tiss_motivo_glosa_seq'), clock_timestamp(), 'ejsilva',
					clock_timestamp(), 'ejsilva', 22,
					'9923', 'Protocolo de guias inexistente ou não informado no recurso de glosa', 'D',
					null,null, 'S',
					'N', 'N', 'N',
					'N');
end if;

/* Glosa  9924 -Procedimento não confere com a guia do recurso de glosa */

select	count(1)
into STRICT	qt_registro_w
from	tiss_motivo_glosa
where	cd_motivo_tiss	= 9924;

if ( qt_registro_w = 0 ) then
	insert into tiss_motivo_glosa(	nr_sequencia, dt_atualizacao, nm_usuario,
					dt_atualizacao_nrec, nm_usuario_nrec, ie_grupo_glosa,
					cd_motivo_tiss, ds_motivo_tiss, ie_acao_pls,
					cd_convenio, cd_motivo_glosa, ie_fechar_conta,
					ie_importacao_ops, ie_param_ops, ie_permite_alteracao_ops,
					ie_regra_integracao_ops)
				values (	nextval('tiss_motivo_glosa_seq'), clock_timestamp(), 'ejsilva',
					clock_timestamp(), 'ejsilva', 22,
					'9924', 'Protocolo de guias inexistente ou não informado no recurso de glosa', 'D',
					null,null, 'S',
					'N', 'N', 'N',
					'N');
end if;



/*Glosa  9925 - Guia já apresentada em outro recurso de glosa */

select	count(1)
into STRICT	qt_registro_w
from	tiss_motivo_glosa
where	cd_motivo_tiss	= 9925;

if ( qt_registro_w = 0 ) then
	insert into tiss_motivo_glosa(	nr_sequencia, dt_atualizacao, nm_usuario,
					dt_atualizacao_nrec, nm_usuario_nrec, ie_grupo_glosa,
					cd_motivo_tiss, ds_motivo_tiss, ie_acao_pls,
					cd_convenio, cd_motivo_glosa, ie_fechar_conta,
					ie_importacao_ops, ie_param_ops, ie_permite_alteracao_ops,
					ie_regra_integracao_ops)
				values (	nextval('tiss_motivo_glosa_seq'), clock_timestamp(), 'ejsilva',
					clock_timestamp(), 'ejsilva', 22,
					'9925', 'Protocolo de guias inexistente ou não informado no recurso de glosa', 'D',
					null,null, 'S',
					'N', 'N', 'N',
					'N');
end if;


/* Glosa  9926 - Glosa apresentada não pertence a guia ou procedimento do recurso de glosa */

select	count(1)
into STRICT	qt_registro_w
from	tiss_motivo_glosa
where	cd_motivo_tiss	= 9926;

if ( qt_registro_w = 0 ) then
	insert into tiss_motivo_glosa(	nr_sequencia, dt_atualizacao, nm_usuario,
					dt_atualizacao_nrec, nm_usuario_nrec, ie_grupo_glosa,
					cd_motivo_tiss, ds_motivo_tiss, ie_acao_pls,
					cd_convenio, cd_motivo_glosa, ie_fechar_conta,
					ie_importacao_ops, ie_param_ops, ie_permite_alteracao_ops,
					ie_regra_integracao_ops)
				values (	nextval('tiss_motivo_glosa_seq'), clock_timestamp(), 'ejsilva',
					clock_timestamp(), 'ejsilva', 22,
					'9926', 'Protocolo de guias inexistente ou não informado no recurso de glosa', 'D',
					null,null, 'S',
					'N', 'N', 'N',
					'N');
end if;

/* Glosa  9927 -  Valor recursado não confere com o valor glosado da conta ou procedimento */

select	count(1)
into STRICT	qt_registro_w
from	tiss_motivo_glosa
where	cd_motivo_tiss	= 9927;

if ( qt_registro_w = 0 ) then
	insert into tiss_motivo_glosa(	nr_sequencia, dt_atualizacao, nm_usuario,
					dt_atualizacao_nrec, nm_usuario_nrec, ie_grupo_glosa,
					cd_motivo_tiss, ds_motivo_tiss, ie_acao_pls,
					cd_convenio, cd_motivo_glosa, ie_fechar_conta,
					ie_importacao_ops, ie_param_ops, ie_permite_alteracao_ops,
					ie_regra_integracao_ops)
				values (	nextval('tiss_motivo_glosa_seq'), clock_timestamp(), 'ejsilva',
					clock_timestamp(), 'ejsilva', 22,
					'9927', 'Protocolo de guias inexistente ou não informado no recurso de glosa', 'D',
					null,null, 'S',
					'N', 'N', 'N',
					'N');
end if;


commit;

end 	;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE baca_pls_glosa_custos_assist () FROM PUBLIC;

