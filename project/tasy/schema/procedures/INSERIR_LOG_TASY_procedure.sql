-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE inserir_log_tasy ( cd_log_p bigint, ds_log_p text, nm_usuario_p text) AS $body$
BEGIN
null;
/*if	(cd_log_p is not null) and
	(ds_log_p is not null) then
	begin
	insert into log_tasy(	dt_atualizacao,
			nm_usuario,
			cd_log,
			ds_log)
		values (	sysdate,
			nm_usuario_p,
			cd_log_p,
			ds_log_p);
	commit;
	end;
end if;*/
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE inserir_log_tasy ( cd_log_p bigint, ds_log_p text, nm_usuario_p text) FROM PUBLIC;

