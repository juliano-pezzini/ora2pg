-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE obter_conta_pj_nf ( cd_estabelecimento_p bigint, cd_cgc_p text, cd_material_p bigint, cd_operacao_nf_p bigint, cd_conta_contabil_p INOUT text, nr_seq_nota_fiscal_p bigint) AS $body$
DECLARE


cd_empresa_w		bigint;
cd_conta_contabil_w	varchar(20);
cd_grupo_material_w	smallint;
cd_subgrupo_material_w	smallint;
cd_classe_material_w	integer;
dt_emissao_w		timestamp;


c01 CURSOR FOR
SELECT	a.cd_conta_contabil
from	pessoa_jur_conta_nf a
where	cd_empresa					= cd_empresa_w
and	a.cd_cgc						= cd_cgc_p
and	coalesce(cd_material,cd_material_p) 			= cd_material_p
and	coalesce(cd_grupo_material,cd_grupo_material_w)		= cd_grupo_material_w
and	coalesce(cd_subgrupo_material,cd_subgrupo_material_w)	= cd_subgrupo_material_w
and	coalesce(cd_classe_material,cd_classe_material_w)		= cd_classe_material_w
and	coalesce(cd_operacao_nf, cd_operacao_nf_p)		= cd_operacao_nf_p
and	substr(obter_se_periodo_vigente(a.dt_inicio_vigencia, a.dt_fim_vigencia, dt_emissao_w),1,1) = 'S'
order by	 coalesce(cd_operacao_nf,0),
	 coalesce(cd_material,0),
	 coalesce(cd_classe_material,0),
	 coalesce(cd_subgrupo_material,0),
	 coalesce(cd_grupo_material,0);


BEGIN

cd_empresa_w	:= obter_empresa_estab(cd_estabelecimento_p);

select	max(cd_classe_material),
	max(cd_subgrupo_material),
	max(cd_grupo_material)
into STRICT	cd_classe_material_w,
	cd_subgrupo_material_w,
	cd_grupo_material_w
from	estrutura_material_v
where	cd_material	 	= cd_material_p;

select	max(dt_emissao)
into STRICT	dt_emissao_w
from	nota_fiscal
where	nr_sequencia = nr_seq_nota_fiscal_p;

open C01;
loop
fetch C01 into
	cd_conta_contabil_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin
	cd_conta_contabil_p	:= cd_conta_contabil_w;
	end;
end loop;
close C01;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE obter_conta_pj_nf ( cd_estabelecimento_p bigint, cd_cgc_p text, cd_material_p bigint, cd_operacao_nf_p bigint, cd_conta_contabil_p INOUT text, nr_seq_nota_fiscal_p bigint) FROM PUBLIC;

