-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE aval_paciente_cih_focuslost_js ( cd_procedimento_p bigint, ie_origem_proced_p INOUT bigint, ie_procedimento_p INOUT text, ds_procedimento_p INOUT text ) AS $body$
DECLARE


ie_procedimento_w	varchar(1) := 'N';
ie_origem_proced_w	bigint;
ds_procedimento_w	procedimento.ds_procedimento%TYPE := '';

BEGIN

if (cd_procedimento_p IS NOT NULL AND cd_procedimento_p::text <> '') then
	begin

	select	coalesce(max('S'), 'N')
	into STRICT	ie_procedimento_w
	from	procedimento
	where	cd_procedimento = cd_procedimento_p
	and	ie_origem_proced = ie_origem_proced_p;

	if (ie_procedimento_w = 'N') then
		begin

		select	max(ie_origem_proced)
		into STRICT	ie_origem_proced_w
		from	procedimento
		where	cd_procedimento = cd_procedimento_p;

		select	ds_procedimento
		into STRICT	ds_procedimento_w
		from	procedimento
		where	cd_procedimento = cd_procedimento_p
		and	ie_origem_proced = ie_origem_proced_w;

		end;
	end if;

	end;
end if;

ie_origem_proced_p	:= ie_origem_proced_w;
ds_procedimento_p	:= ds_procedimento_w;
ie_procedimento_p	:= ie_procedimento_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE aval_paciente_cih_focuslost_js ( cd_procedimento_p bigint, ie_origem_proced_p INOUT bigint, ie_procedimento_p INOUT text, ds_procedimento_p INOUT text ) FROM PUBLIC;

