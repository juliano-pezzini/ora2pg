-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE hc_inserir_proc_servico (nr_seq_servico_p bigint, nr_seq_pac_servico_p bigint, nm_usuario_p text) AS $body$
DECLARE


cd_procedimento_w	bigint;
ie_origem_proced_w	bigint;
nr_seq_proc_interno_w	bigint;
nr_registro_w		bigint;

C01 CURSOR FOR
	SELECT	cd_procedimento,
		ie_origem_proced,
		nr_seq_proc_interno
	from	hc_servico_proc
	where	nr_seq_servico = nr_seq_servico_p;





BEGIN

select	count(*)
into STRICT	nr_registro_w
from	hc_servico_proc
where	nr_seq_servico = nr_seq_servico_p;

if (nr_seq_pac_servico_p IS NOT NULL AND nr_seq_pac_servico_p::text <> '') and (nr_seq_servico_p IS NOT NULL AND nr_seq_servico_p::text <> '') and (nr_registro_w > 0)	then

	open C01;
		loop
		fetch C01 into
			cd_procedimento_w,
			ie_origem_proced_w,
			nr_seq_proc_interno_w;
		EXIT WHEN NOT FOUND; /* apply on C01 */
			begin

			insert into hc_paciente_servico_proc(
				nr_sequencia,
				dt_atualizacao,
				dt_atualizacao_nrec,
				nm_usuario,
				nm_usuario_nrec,
				nr_seq_servico,
				nr_seq_proc_interno,
				cd_procedimento,
				ie_origem_proced)
			values (	nextval('hc_paciente_servico_proc_seq'),
				clock_timestamp(),
				clock_timestamp(),
				nm_usuario_p,
				nm_usuario_p,
				nr_seq_pac_servico_p,
				nr_seq_proc_interno_w,
				cd_procedimento_w,
				ie_origem_proced_w);

			end;
		end loop;
	close C01;
	commit;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE hc_inserir_proc_servico (nr_seq_servico_p bigint, nr_seq_pac_servico_p bigint, nm_usuario_p text) FROM PUBLIC;

