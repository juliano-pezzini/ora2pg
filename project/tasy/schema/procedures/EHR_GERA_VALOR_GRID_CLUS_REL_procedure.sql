-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE ehr_gera_valor_grid_clus_rel ( nr_seq_reg_template_p bigint) AS $body$
DECLARE



nr_seq_template_w		bigint;
nr_seq_template_custer_w	bigint;
ds_aux_w			varchar(255);

C01 CURSOR FOR
	SELECT	NR_SEQ_TEMPLATE_CLUSTER
	from	ehr_template_conteudo
	where	nr_seq_template	= nr_seq_template_w
	and	(NR_SEQ_TEMPLATE_CLUSTER IS NOT NULL AND NR_SEQ_TEMPLATE_CLUSTER::text <> '');

BEGIN

select	max(nr_seq_template)
into STRICT	nr_seq_template_w
from	ehr_reg_template
where	nr_sequencia	= nr_seq_reg_template_p;
open C01;
loop
fetch C01 into
	nr_seq_template_custer_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin
	begin
	SELECT * FROM ehr_gerar_tabela_cluster(nr_seq_template_custer_w, null, ds_aux_w, ds_aux_w, ds_aux_w, ds_aux_w, ds_aux_w) INTO STRICT ds_aux_w, ds_aux_w, ds_aux_w, ds_aux_w, ds_aux_w;
	exception
	when others then
		null;
	end;
	CALL ehr_gerar_valores_grid_cluster(nr_seq_reg_template_p,nr_seq_template_custer_w);
	exception
		when others then
		null;
	end;
end loop;
close C01;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE ehr_gera_valor_grid_clus_rel ( nr_seq_reg_template_p bigint) FROM PUBLIC;

