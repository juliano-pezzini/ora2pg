-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE eup_focusgained_usu_conv_js ( cd_estabelecimento_p bigint, nr_atendimento_p bigint, cd_convenio_p bigint, cd_usuario_convenio_p text, nm_usuario_p text, qt_evento_conv_p INOUT bigint, nr_seq_atend_ident_p INOUT bigint, ie_regra_cart_usuario_p INOUT text) AS $body$
DECLARE

 
qt_evento_conv_w	bigint;
ie_usu_conv_w		varchar(1);					
					 

BEGIN 
 
 
if (coalesce(cd_convenio_p,0) > 0) then 
 
	if (cd_usuario_convenio_p IS NOT NULL AND cd_usuario_convenio_p::text <> '') then 
		 
		select	count(*) 
		into STRICT	qt_evento_conv_p 
		from	evento_convenio  
		where	nr_seq_evento = 2 
		and	cd_convenio = cd_convenio_p;
 
		select	coalesce(max(nr_sequencia),0)  
		into STRICT	nr_seq_atend_ident_p 
		from 	atend_identificacao  
		where	nr_atendimento = nr_atendimento_p;
	 
	else 
		select 	coalesce(max(ie_regra_cart_usuario), 'N') 
		into STRICT	ie_regra_cart_usuario_p 
        from  convenio_estabelecimento    
        where  cd_convenio = cd_convenio_p 
        and   cd_estabelecimento = cd_estabelecimento_p;
	end if;
	 
end if;
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE eup_focusgained_usu_conv_js ( cd_estabelecimento_p bigint, nr_atendimento_p bigint, cd_convenio_p bigint, cd_usuario_convenio_p text, nm_usuario_p text, qt_evento_conv_p INOUT bigint, nr_seq_atend_ident_p INOUT bigint, ie_regra_cart_usuario_p INOUT text) FROM PUBLIC;

