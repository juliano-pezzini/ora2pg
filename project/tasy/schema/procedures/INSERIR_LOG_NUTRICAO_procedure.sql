-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE inserir_log_nutricao ( cd_log_p bigint, ds_log_p text, nm_usuario_p text) AS $body$
BEGIN
if (cd_log_p IS NOT NULL AND cd_log_p::text <> '') and (ds_log_p IS NOT NULL AND ds_log_p::text <> '') then
	begin
	insert into log_nutricao(
			dt_atualizacao,
			nm_usuario,
			cd_log,
			ds_log)
		values (
			clock_timestamp(),
			nm_usuario_p,
			cd_log_p,
			ds_log_p);
	commit;
	end;
end if;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE inserir_log_nutricao ( cd_log_p bigint, ds_log_p text, nm_usuario_p text) FROM PUBLIC;

