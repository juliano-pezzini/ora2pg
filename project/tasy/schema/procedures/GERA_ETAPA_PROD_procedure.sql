-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gera_etapa_prod (dt_etapa_p timestamp, cd_pessoa_fisica_p text, cd_estabelecimento_p bigint, nm_usuario_p text) AS $body$
DECLARE


nr_sequencia_w	bigint;
nr_seq_cabine_w	bigint;


BEGIN

nr_seq_cabine_w := Obter_Param_Usuario(3130, 301, obter_perfil_ativo, nm_usuario_p, cd_estabelecimento_p, nr_seq_cabine_w);

if (coalesce(nr_seq_cabine_w,0) > 0) then
	select	nextval('far_etapa_producao_seq')
	into STRICT	nr_sequencia_w
	;

	insert into far_etapa_producao(
		nr_sequencia,
		cd_estabelecimento,
		dt_atualizacao,
		dt_atualizacao_nrec,
		dt_etapa,
		nm_usuario,
		nm_usuario_nrec,
		nr_seq_cabine)
	values (nr_sequencia_w,
		cd_estabelecimento_p,
		clock_timestamp(),
		clock_timestamp(),
		dt_etapa_p,
		nm_usuario_p,
		nm_usuario_p,
		nr_seq_cabine_w);

	update 	can_ordem_prod
	set 	nr_seq_etapa_prod = nr_sequencia_w
	where  	trunc(dt_prevista) 	= trunc(dt_etapa_p)
	and    	cd_pessoa_fisica 	= cd_pessoa_fisica_p
	and    	coalesce(ie_conferido,'N') = 'S'
	and 	coalesce(nr_seq_etapa_prod::text, '') = '';
else
	CALL wheb_mensagem_pck.exibir_mensagem_abort(173464);
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gera_etapa_prod (dt_etapa_p timestamp, cd_pessoa_fisica_p text, cd_estabelecimento_p bigint, nm_usuario_p text) FROM PUBLIC;

