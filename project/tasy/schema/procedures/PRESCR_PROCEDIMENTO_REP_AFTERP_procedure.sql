-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE prescr_procedimento_rep_afterp (nr_seq_derivado_p bigint, nr_prescricao_p bigint, nr_seq_solic_sangue_p bigint, nr_atendimento_p bigint, cd_estabelecimento_p bigint, dt_prev_execucao_p timestamp, cd_intervalo_p text, nr_sequencia_p bigint, ie_acao_executada_p bigint, nm_usuario_p text, ie_gerar_proc_assoc_hem_p text) AS $body$
DECLARE

				 
gerar_assoc_proc_w boolean;


BEGIN 
 
gerar_assoc_proc_w := ('S' = Obter_Valor_Param_Usuario(924,682, Obter_perfil_Ativo, nm_usuario_p, 0));
 
if (ie_acao_executada_p = 1) then 
	CALL Gerar_Solic_testes(	nr_seq_derivado_p,nr_prescricao_p,nr_seq_solic_sangue_p, nr_atendimento_p, cd_estabelecimento_p, nm_usuario_p, 
			dt_prev_execucao_p, cd_intervalo_p);
end if;
 
if (gerar_assoc_proc_w) then 
	CALL Gerar_Med_Mat_Assoc(nr_prescricao_p, nr_sequencia_p);
end if;
 
if (ie_gerar_proc_assoc_hem_p IS NOT NULL AND ie_gerar_proc_assoc_hem_p::text <> '') and (ie_gerar_proc_assoc_hem_p = 'S') then 
	CALL Gerar_prescr_proc_assoc(nr_prescricao_p, nr_sequencia_p, nm_usuario_p);
end if;
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE prescr_procedimento_rep_afterp (nr_seq_derivado_p bigint, nr_prescricao_p bigint, nr_seq_solic_sangue_p bigint, nr_atendimento_p bigint, cd_estabelecimento_p bigint, dt_prev_execucao_p timestamp, cd_intervalo_p text, nr_sequencia_p bigint, ie_acao_executada_p bigint, nm_usuario_p text, ie_gerar_proc_assoc_hem_p text) FROM PUBLIC;

