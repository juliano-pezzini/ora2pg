-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';



--
-- dblink wrapper to call function eis_fechamento_part_alert_sign as an autonomous transaction
--
CREATE EXTENSION IF NOT EXISTS dblink;

CREATE OR REPLACE PROCEDURE eis_fechamento_part_alert_sign (nm_usuario_p text) AS $body$
DECLARE
	-- Change this to reflect the dblink connection string
	v_conn_str  text := format('port=%s dbname=%s user=%s', current_setting('port'), current_database(), current_user);
	v_query     text;

BEGIN
	v_query := 'CALL eis_fechamento_part_alert_sign_atx ( ' || quote_nullable(nm_usuario_p) || ' )';
	PERFORM * FROM dblink(v_conn_str, v_query) AS p (ret boolean);

END;
$body$ LANGUAGE plpgsql SECURITY DEFINER;




CREATE OR REPLACE PROCEDURE eis_fechamento_part_alert_sign_atx (nm_usuario_p text) AS $body$
BEGIN
CALL exec_sql_dinamico(nm_usuario_p,'begin dbms_alert.signal' || '(' || chr(39) || 'EISFechPartic' || chr(39) || ',' || chr(39) || 'TESTE' || chr(39) || '); end;');

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE eis_fechamento_part_alert_sign (nm_usuario_p text) FROM PUBLIC; -- REVOKE ALL ON PROCEDURE eis_fechamento_part_alert_sign_atx (nm_usuario_p text) FROM PUBLIC;

