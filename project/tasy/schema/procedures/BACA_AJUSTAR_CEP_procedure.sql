-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE baca_ajustar_cep (cd_Cep_antigo_p text, cd_cep_atual_p text, nm_usuario_p text) AS $body$
DECLARE



cd_pessoa_fisica_w	varchar(10);

c01 CURSOR FOR
SELECT	cd_pessoa_fisica
from	pessoa_fisica
where	NR_CEP_CIDADE_NASC	= cd_Cep_antigo_p;


BEGIN

open c01;
loop
fetch c01 into
	cd_pessoa_fisica_w;
EXIT WHEN NOT FOUND; /* apply on c01 */

	update	pessoa_fisica
	set	NR_CEP_CIDADE_NASC	= cd_cep_atual_p
	where	cd_pessoa_fisica	= cd_pessoa_fisica_w
	and	(NR_CEP_CIDADE_NASC IS NOT NULL AND NR_CEP_CIDADE_NASC::text <> '');

	/*insert into Logxxx_tasy
		(dt_atualizacao,
		nm_usuario,
		cd_log,
		ds_log)
	values	(sysdate,
		nm_usuario_p,
		55715,
		'Feito update no NR_CEP_CIDADE_NASC da pessoa física. ' || chr(13) || chr(10) ||
		'Objeto: BACA_AJUSTAR_CEP ' || chr(13) || chr(10) ||
		'Pessoa: ' || cd_pessoa_fisica_w || chr(13) || chr(10) ||
		'CEP anterior: ' || cd_Cep_antigo_p);*/
	commit;

end loop;
close c01;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE baca_ajustar_cep (cd_Cep_antigo_p text, cd_cep_atual_p text, nm_usuario_p text) FROM PUBLIC;

