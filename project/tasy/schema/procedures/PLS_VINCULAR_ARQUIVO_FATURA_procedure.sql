-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_vincular_arquivo_fatura ( nr_seq_fatura_p bigint, nr_seq_fat_guia_arq_p bigint, ds_arquivo_p text, nm_usuario_p text) AS $body$
BEGIN

CALL pls_grava_log_execucao_temp('Início - OS2011730 - ' || nr_seq_fatura_p || ' - ' || nr_seq_fat_guia_arq_p || ' - ' || ds_arquivo_p || ' - ' || nm_usuario_p, 'pls_vincular_arquivo_fatura', nm_usuario_p, 'S');

if (ds_arquivo_p IS NOT NULL AND ds_arquivo_p::text <> '') then
	update	pls_fatura
	set	ds_arquivo	= ds_arquivo_p,
		nm_usuario_arq	= nm_usuario_p,
		dt_geracao_arq	= clock_timestamp(),
		nm_usuario	= nm_usuario_p,
		dt_atualizacao	= clock_timestamp()
	where	nr_sequencia	= nr_seq_fatura_p;
	
	if (nr_seq_fat_guia_arq_p IS NOT NULL AND nr_seq_fat_guia_arq_p::text <> '') then
		update	pls_fat_guia_arquivo
		set	nm_arquivo 	= ds_arquivo_p
		where	nr_sequencia	= nr_seq_fat_guia_arq_p;
		
	end if;
	
	commit;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_vincular_arquivo_fatura ( nr_seq_fatura_p bigint, nr_seq_fat_guia_arq_p bigint, ds_arquivo_p text, nm_usuario_p text) FROM PUBLIC;

