-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE obter_dados_atend_visit_pessoa ( nr_atendimento_p bigint, nr_idade_p bigint, cd_pessoa_fisica_p text, qt_atend_visita_p INOUT bigint, nr_idade_pessoa_p INOUT bigint) AS $body$
BEGIN

if (nr_atendimento_p IS NOT NULL AND nr_atendimento_p::text <> '' AND nr_idade_p IS NOT NULL AND nr_idade_p::text <> '') then
	select	count(*)
	into STRICT	qt_atend_visita_p
	from	atendimento_visita
	where	nr_atendimento	= nr_atendimento_p
	and	coalesce(dt_saida::text, '') = ''
	and	coalesce(ie_paciente,'N') = 'N'
	and (coalesce(cd_pessoa_fisica::text, '') = ''
		or coalesce(obter_idade_pf(cd_pessoa_fisica, clock_timestamp(), 'A'), 0) > nr_idade_p);
end if;

if (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') then
	select	(obter_idade_pf(cd_pessoa_fisica_p, clock_timestamp(), 'A'))::numeric
	into STRICT	nr_idade_pessoa_p
	;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE obter_dados_atend_visit_pessoa ( nr_atendimento_p bigint, nr_idade_p bigint, cd_pessoa_fisica_p text, qt_atend_visita_p INOUT bigint, nr_idade_pessoa_p INOUT bigint) FROM PUBLIC;

