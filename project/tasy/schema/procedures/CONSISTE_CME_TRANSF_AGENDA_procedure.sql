-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consiste_cme_transf_agenda (nr_seq_origem_p bigint, nr_seq_destino_p bigint, nm_usuario_p text, ds_erro_p INOUT text ) AS $body$
DECLARE

 
nr_seq_conjunto_w	bigint;	
ds_erro_w		varchar(2000);				
 
c01 CURSOR FOR 
	SELECT	nr_seq_conjunto 
	from	agenda_pac_cme 
	where	nr_seq_agenda	= nr_seq_origem_p;


BEGIN 
 
 
open C01;
loop 
fetch C01 into	 
	nr_seq_conjunto_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin 
	ds_erro_w := null;
	 
	ds_erro_w := cme_consistir_conj_agenda(nr_seq_destino_p, nr_seq_conjunto_w, 'N', nm_usuario_p, wheb_usuario_pck.get_cd_estabelecimento, ds_erro_w, nr_seq_origem_p);
	 
	if (ds_erro_w IS NOT NULL AND ds_erro_w::text <> '') then 
		ds_erro_p := ds_erro_w;
	end if;	
	end;
end loop;
close C01;
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consiste_cme_transf_agenda (nr_seq_origem_p bigint, nr_seq_destino_p bigint, nm_usuario_p text, ds_erro_p INOUT text ) FROM PUBLIC;

