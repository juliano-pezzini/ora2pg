-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE obter_convenio_sus_eup_js ( nr_atendimento_p bigint, cd_estabelecimento_p bigint, cd_convenio_parametro_p INOUT bigint) AS $body$
DECLARE

 
cd_convenio_parametro_w	integer;


BEGIN 
 
if (coalesce(nr_atendimento_p,0) > 0)then 
	begin 
	 
	select  coalesce(max(cd_convenio_parametro),0) 
	into STRICT	 cd_convenio_parametro_w 
	from   conta_paciente 
	where  nr_atendimento = nr_atendimento_p  LIMIT 1;
	 
	if (cd_convenio_parametro_w = 0)then 
		begin 
		 
		select 	coalesce(max(cd_convenio_sus),0) 
		into STRICT	cd_convenio_parametro_w 
		from 	parametro_faturamento 	 
		where 	cd_estabelecimento = cd_estabelecimento_p;
		 
		end;
	end if;
	 
	end;
end if;
 
cd_convenio_parametro_p	:= coalesce(cd_convenio_parametro_w,0);
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE obter_convenio_sus_eup_js ( nr_atendimento_p bigint, cd_estabelecimento_p bigint, cd_convenio_parametro_p INOUT bigint) FROM PUBLIC;

