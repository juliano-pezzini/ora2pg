-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE atualizar_ult_dose_medic_proc ( nr_prescricao_p text, nr_sequencia_p text, dt_ultima_dose_p timestamp, qt_ultima_dose_p bigint, cd_ultima_unid_med_p text ) AS $body$
BEGIN

if (nr_prescricao_p IS NOT NULL AND nr_prescricao_p::text <> '') and (nr_sequencia_p IS NOT NULL AND nr_sequencia_p::text <> '') and (dt_ultima_dose_p IS NOT NULL AND dt_ultima_dose_p::text <> '') then

	Update	prescr_procedimento
	set	dt_ult_dose_medic = coalesce(dt_ultima_dose_p, dt_ult_dose_medic),
		qt_ult_dose_medic = coalesce(qt_ultima_dose_p, qt_ult_dose_medic),
		cd_unidade_ult_dose_medic = coalesce(cd_ultima_unid_med_p, cd_unidade_ult_dose_medic)
	where	nr_prescricao	= nr_prescricao_p
	and	nr_sequencia	= nr_sequencia_p;

end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE atualizar_ult_dose_medic_proc ( nr_prescricao_p text, nr_sequencia_p text, dt_ultima_dose_p timestamp, qt_ultima_dose_p bigint, cd_ultima_unid_med_p text ) FROM PUBLIC;

