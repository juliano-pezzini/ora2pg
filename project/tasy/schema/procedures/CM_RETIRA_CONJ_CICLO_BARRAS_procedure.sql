-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE cm_retira_conj_ciclo_barras ( nr_seq_conjunto_p bigint, nr_seq_ciclo_p bigint, nm_usuario_p text, ds_erro_p INOUT text) AS $body$
DECLARE


qt_existe_w		bigint;


BEGIN

select	count(*)
into STRICT	qt_existe_w
from	cm_conjunto_cont
where	nr_sequencia = nr_seq_conjunto_p
and	nr_seq_ciclo = nr_seq_ciclo_p;

if (qt_existe_w = 0) then
	ds_erro_p	:= WHEB_MENSAGEM_PCK.get_texto(280294);
else
	begin

	update	cm_conjunto_cont
	set	nr_seq_ciclo  = NULL,
		nm_usuario = nm_usuario_p,
		dt_atualizacao = clock_timestamp()
	where	nr_sequencia = nr_seq_conjunto_p;

	commit;

	end;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE cm_retira_conj_ciclo_barras ( nr_seq_conjunto_p bigint, nr_seq_ciclo_p bigint, nm_usuario_p text, ds_erro_p INOUT text) FROM PUBLIC;

