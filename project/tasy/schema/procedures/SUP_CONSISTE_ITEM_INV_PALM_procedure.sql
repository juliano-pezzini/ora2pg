-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE sup_consiste_item_inv_palm ( nr_seq_inv_p bigint, nr_seq_item_inv_p bigint, cd_local_estoque_p bigint default 0, cd_estabelecimento_p bigint DEFAULT NULL, nm_usuario_p text DEFAULT NULL, ds_retorno_p INOUT text DEFAULT NULL) AS $body$
DECLARE


qt_existe_w		integer;
ie_realiza_contagem_w	varchar(1) := 'S';
ds_retorno_w		varchar(200) := 'X';

/*Local de estoque do usuário logado no aplicativo*/

cd_local_estoque_w 	integer := 0;


BEGIN
cd_local_estoque_w 	:= coalesce(cd_local_estoque_p,0);

select	substr(obter_se_usuario_realiza_cont(nr_seq_inv_p, nr_seq_item_inv_p, cd_estabelecimento_p, nm_usuario_p),1,1) ie_realiza_contagem
into STRICT	ie_realiza_contagem_w
;
if (coalesce(ie_realiza_contagem_w,'S') = 'N') then
	--ds_retorno_w := 'Usuário sem permissão para realizar uma nova contagem deste item.';
	ds_retorno_w := wheb_mensagem_pck.get_texto(314872);
end if;

ds_retorno_p := ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE sup_consiste_item_inv_palm ( nr_seq_inv_p bigint, nr_seq_item_inv_p bigint, cd_local_estoque_p bigint default 0, cd_estabelecimento_p bigint DEFAULT NULL, nm_usuario_p text DEFAULT NULL, ds_retorno_p INOUT text DEFAULT NULL) FROM PUBLIC;

