-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_evento_lib_prescricao ( nr_prescricao_p bigint, nm_usuario_p text) AS $body$
DECLARE


cd_protocolo_w		bigint;
nr_seq_protocolo_w	bigint;
nr_seq_evento_w		bigint;
cd_pessoa_fisica_w	varchar(10);
nr_atendimento_w	bigint;


C01 CURSOR FOR
	SELECT	nr_seq_evento
	from	protocolo_medic_evento
	where	cd_protocolo	= cd_protocolo_w
	and	nr_sequencia	= nr_seq_protocolo_w;

BEGIN

select	cd_protocolo,
	nr_seq_protocolo,
	nr_atendimento,
	cd_pessoa_fisica
into STRICT	cd_protocolo_w,
	nr_seq_protocolo_w,
	nr_atendimento_w,
	cd_pessoa_fisica_w
from	prescr_medica
where	nr_prescricao	= nr_prescricao_p;

open C01;
loop
fetch C01 into
	nr_seq_evento_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin
	CALL gerar_evento_paciente_prescr(nr_seq_evento_w,nr_atendimento_w,cd_pessoa_fisica_w,null,nr_prescricao_p,nm_usuario_p);
	end;
end loop;
close C01;


if (coalesce(wheb_usuario_pck.get_ie_commit, 'S') = 'S') then commit; end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_evento_lib_prescricao ( nr_prescricao_p bigint, nm_usuario_p text) FROM PUBLIC;

