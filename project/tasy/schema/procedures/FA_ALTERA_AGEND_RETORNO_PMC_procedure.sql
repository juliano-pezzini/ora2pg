-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE fa_altera_agend_retorno_pmc (nr_seq_paciente_pmc_p bigint, nr_seq_agenda_p text) AS $body$
DECLARE


dt_agendamento_w	timestamp;


BEGIN

if (nr_seq_agenda_p <> 0) and (nr_seq_agenda_p IS NOT NULL AND nr_seq_agenda_p::text <> '') then
	select	dt_agenda
	into STRICT	dt_agendamento_w
	from	agenda_consulta
	where	nr_sequencia = nr_seq_agenda_p;

	if (dt_agendamento_w IS NOT NULL AND dt_agendamento_w::text <> '') then
		update	fa_paciente_pmc
		set	dt_retorno = dt_agendamento_w
		where	nr_sequencia = nr_seq_paciente_pmc_p;
	end if;
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE fa_altera_agend_retorno_pmc (nr_seq_paciente_pmc_p bigint, nr_seq_agenda_p text) FROM PUBLIC;

