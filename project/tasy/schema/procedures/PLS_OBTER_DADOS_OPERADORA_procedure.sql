-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_obter_dados_operadora ( cd_estabelecimento_p bigint, nm_usuario_p text, cd_cgc_p INOUT text, cd_registro_ans_p INOUT text) AS $body$
DECLARE


cd_cgc_outorgante_w	varchar(14);
cd_ans_w		varchar(6);


BEGIN

begin
select	cd_cgc_outorgante,
	substr(cd_ans,1,6)
into STRICT	cd_cgc_outorgante_w,
	cd_ans_w
from	pls_outorgante
where	cd_estabelecimento	= cd_estabelecimento_p;
exception
	when others then
	cd_cgc_outorgante_w	:= '';
	cd_ans_w		:= '';
end;

cd_cgc_p		:= cd_cgc_outorgante_w;
cd_registro_ans_p	:= cd_ans_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_obter_dados_operadora ( cd_estabelecimento_p bigint, nm_usuario_p text, cd_cgc_p INOUT text, cd_registro_ans_p INOUT text) FROM PUBLIC;

