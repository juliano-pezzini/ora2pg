-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE baca_gestao_seq_unid_atual () AS $body$
DECLARE


cd_pessoa_fisica_w	varchar(10);
ie_situacao_w		varchar(1);
nr_seq_unidade_atual_w	bigint;

C01 CURSOR FOR
	SELECT	cd_pessoa_fisica
	from	hd_pac_renal_cronico
	where	coalesce(nr_seq_unidade_atual::text, '') = ''
	order by 1;

BEGIN

open C01;
loop
fetch C01 into
	cd_pessoa_fisica_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin

	nr_seq_unidade_atual_w:= hd_obter_unidade_prc(cd_pessoa_fisica_w,'C');

	if (nr_seq_unidade_atual_w > 0) then

		update	hd_pac_renal_cronico
		set	nr_seq_unidade_atual    = nr_seq_unidade_atual_w
		where	cd_pessoa_fisica	= cd_pessoa_fisica_w;
	end if;


	end;
end loop;
close C01;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE baca_gestao_seq_unid_atual () FROM PUBLIC;

