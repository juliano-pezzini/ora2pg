-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE atualizar_qt_bonus_apres ( nr_atendimento_p bigint, cd_procedimento_p bigint, ie_origem_proced_p bigint, nr_seq_proc_interno_p bigint) AS $body$
DECLARE


qt_bonus_conv_apresentado_w bigint;


BEGIN
	select	coalesce(max(qt_bonus_conv_apresentado), 0)
	into STRICT	qt_bonus_conv_apresentado_w
	from	atend_categoria_convenio
	where	nr_seq_interno = obter_atecaco_atendimento(nr_atendimento_p);

	update	procedimento_autorizado
	set		qt_bonus_apres = qt_bonus_conv_apresentado_w
	where	nr_atendimento = nr_atendimento_p
	and		cd_procedimento = cd_procedimento_p
	and (coalesce(ie_origem_proced::text, '') = '' or ie_origem_proced = coalesce(ie_origem_proced_p, 0))
	and (coalesce(nr_seq_proc_interno::text, '') = '' or nr_seq_proc_interno = coalesce(nr_seq_proc_interno_p, 0));

	commit;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE atualizar_qt_bonus_apres ( nr_atendimento_p bigint, cd_procedimento_p bigint, ie_origem_proced_p bigint, nr_seq_proc_interno_p bigint) FROM PUBLIC;

