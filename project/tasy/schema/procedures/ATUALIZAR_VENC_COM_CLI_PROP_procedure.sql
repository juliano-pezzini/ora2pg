-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE atualizar_venc_com_cli_prop ( nr_sequencia_p bigint, dt_vencimento_p timestamp, ds_mot_alt_venc_p text, nm_usuario_p text) AS $body$
BEGIN
if (nr_sequencia_p IS NOT NULL AND nr_sequencia_p::text <> '') and (dt_vencimento_p IS NOT NULL AND dt_vencimento_p::text <> '') and (ds_mot_alt_venc_p IS NOT NULL AND ds_mot_alt_venc_p::text <> '') and (nm_usuario_p IS NOT NULL AND nm_usuario_p::text <> '') then
	begin

	update	com_cliente_proposta
	set	dt_vencimento	= dt_vencimento_p,
		nm_usuario	= nm_usuario_p,
		dt_atualizacao	= clock_timestamp(),
		ds_mot_alt_venc = ds_mot_alt_venc_p
	where	nr_sequencia	= nr_sequencia_p;
	end;
else

 if (coalesce(nr_sequencia_p::text, '') = '') then
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(290526);
 elsif (coalesce(dt_vencimento_p::text, '') = '') then
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(290527);
 elsif (coalesce(ds_mot_alt_venc_p::text, '') = '') then
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(290528);
 elsif (coalesce(nm_usuario_P::text, '') = '') then
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(290529);
 end if;

end if;

commit;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE atualizar_venc_com_cli_prop ( nr_sequencia_p bigint, dt_vencimento_p timestamp, ds_mot_alt_venc_p text, nm_usuario_p text) FROM PUBLIC;

