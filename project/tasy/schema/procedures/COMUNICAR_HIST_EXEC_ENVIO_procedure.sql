-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE comunicar_hist_exec_envio ( nr_seq_ordem_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint) AS $body$
DECLARE

nr_sequencia_w	bigint;

BEGIN
if (nr_seq_ordem_p IS NOT NULL AND nr_seq_ordem_p::text <> '') then 
	select 	max(coalesce(nr_sequencia,0)) 
	into STRICT	nr_sequencia_w 
	from 	man_ordem_serv_tecnico 
	where 	nr_seq_ordem_serv = nr_seq_ordem_p;
	 
	CALL man_comunicar_historico(nr_sequencia_w, 'E', nm_usuario_p, 'N', cd_estabelecimento_p);
	 
	commit;	
end if;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE comunicar_hist_exec_envio ( nr_seq_ordem_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint) FROM PUBLIC;

