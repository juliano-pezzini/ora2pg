-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE ck_inicio_etapa_grid_painel ( nr_seq_checkup_etapa_p bigint, nm_usuario_p text, ds_pergunta_p INOUT text, ds_mensagem_etapa_p INOUT text) AS $body$
DECLARE

 
ds_pergunta_w		varchar(255) := '';
ds_mensagem_etapa_w	varchar(255) := '';
		

BEGIN 
 
CALL valida_etapa_requerida(nr_seq_checkup_etapa_p,nm_usuario_p);
 
select	a.ds_mensagem 
into STRICT	ds_mensagem_etapa_w 
from	etapa_checkup a, 
	checkup_etapa b 
where	a.nr_sequencia = b.nr_seq_etapa 
and	b.nr_sequencia = nr_seq_checkup_etapa_p;
 
ds_pergunta_w	:= obter_texto_tasy(61014, wheb_usuario_pck.get_nr_seq_idioma);
 
ds_pergunta_p		:= ds_pergunta_w;
ds_mensagem_etapa_p	:= ds_mensagem_etapa_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE ck_inicio_etapa_grid_painel ( nr_seq_checkup_etapa_p bigint, nm_usuario_p text, ds_pergunta_p INOUT text, ds_mensagem_etapa_p INOUT text) FROM PUBLIC;

