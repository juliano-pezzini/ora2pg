-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gravar_log_acesso_conta ( cd_estabelecimento_p bigint, nr_atendimento_p bigint, nr_interno_conta_p bigint, cd_pessoa_fisica_p text, nm_usuario_p text) AS $body$
DECLARE


nr_sequencia_w		bigint;
nr_atendimento_w		bigint;
cd_perfil_w		integer;


BEGIN

nr_atendimento_w	:= nr_atendimento_p;

select	obter_perfil_ativo
into STRICT	cd_perfil_w
;

if (cd_perfil_w = '') or (cd_perfil_w = 0) then
	cd_perfil_w := null;
end if;

if (nr_atendimento_p = 0) then
	nr_atendimento_w	:= null;
end if;

/*select	pep_acesso_seq.nextVal*/

select	nextval('conta_paciente_acesso_seq')
into STRICT	nr_sequencia_w
;

if (coalesce(nr_interno_conta_p,0) > 0) then
	insert into conta_paciente_acesso(
		nr_sequencia,
		cd_estabelecimento,
		nr_interno_conta,
		dt_atualizacao,
		nm_usuario,
		dt_atualizacao_nrec,
		nm_usuario_nrec,
		cd_pessoa_fisica,
		ds_maquina,
		ds_maquina_cliente,
		cd_perfil,
		nr_atendimento)
	values (	nr_sequencia_w,
		cd_estabelecimento_p,
		nr_interno_conta_p,
		clock_timestamp(),
		nm_usuario_p,
		clock_timestamp(),
		nm_usuario_p,
		cd_pessoa_fisica_p,
		null,
		null,
		cd_perfil_w,
		nr_atendimento_w);
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gravar_log_acesso_conta ( cd_estabelecimento_p bigint, nr_atendimento_p bigint, nr_interno_conta_p bigint, cd_pessoa_fisica_p text, nm_usuario_p text) FROM PUBLIC;

