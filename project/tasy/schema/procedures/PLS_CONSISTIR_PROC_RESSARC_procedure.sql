-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_consistir_proc_ressarc ( nr_seq_processo_p bigint, nr_processo_p text, dt_inicio_competencia_p timestamp, dt_fim_competencia_p timestamp, nm_usuario_p text, ds_erro_p INOUT text) AS $body$
DECLARE


qt_processos_periodo_w		bigint;
ds_erro_w			varchar(255);


BEGIN

select	count(*)
into STRICT	qt_processos_periodo_w
from	pls_processo
where	nr_processo = nr_processo_p
and	((dt_inicio_competencia_p between dt_inicio_competencia and dt_fim_competencia)
or (dt_fim_competencia_p between dt_inicio_competencia and dt_fim_competencia))
and	nr_sequencia <> nr_seq_processo_p;

if (qt_processos_periodo_w > 0) then
	ds_erro_w	:= ds_erro_w || wheb_mensagem_pck.get_texto(281117);
end if;

ds_erro_p := ds_erro_w;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_consistir_proc_ressarc ( nr_seq_processo_p bigint, nr_processo_p text, dt_inicio_competencia_p timestamp, dt_fim_competencia_p timestamp, nm_usuario_p text, ds_erro_p INOUT text) FROM PUBLIC;

