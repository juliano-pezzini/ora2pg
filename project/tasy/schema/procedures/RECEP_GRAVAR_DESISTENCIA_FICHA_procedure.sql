-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE recep_gravar_desistencia_ficha ( nr_seq_ficha_p text, nr_seq_motivo_p bigint, nm_usuario_p text, ds_justificativa_p text, cd_etapa_desistencia_p bigint, cd_pessoa_fisica_p text) AS $body$
BEGIN

if (nr_seq_motivo_p IS NOT NULL AND nr_seq_motivo_p::text <> '') and (nm_usuario_p IS NOT NULL AND nm_usuario_p::text <> '') and (cd_etapa_desistencia_p IS NOT NULL AND cd_etapa_desistencia_p::text <> '') and (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') and (nr_seq_ficha_p IS NOT NULL AND nr_seq_ficha_p::text <> '') then
begin

	update 	recep_ficha_pre
	set	nr_seq_motivo_desistencia = nr_seq_motivo_p,
		dt_atualizacao = clock_timestamp(),
		dt_desistencia = clock_timestamp(),
		nm_usuario_desistencia = nm_usuario_p,
		ds_justif_desistencia = ds_justificativa_p,
		cd_etapa_desistencia = cd_etapa_desistencia_p,
		nm_usuario = nm_usuario_p
	where	nr_sequencia = nr_seq_ficha_p
	and	cd_pessoa_fisica = cd_pessoa_fisica_p;

end;
end if;


commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE recep_gravar_desistencia_ficha ( nr_seq_ficha_p text, nr_seq_motivo_p bigint, nm_usuario_p text, ds_justificativa_p text, cd_etapa_desistencia_p bigint, cd_pessoa_fisica_p text) FROM PUBLIC;

