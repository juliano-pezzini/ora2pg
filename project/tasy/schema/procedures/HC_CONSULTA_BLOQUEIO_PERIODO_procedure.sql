-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE hc_consulta_bloqueio_periodo ( nr_seq_agenda_p bigint, dt_hr_agenda_p timestamp, dt_final_p timestamp, ie_informacao_p text, ds_erro_p INOUT text) AS $body$
DECLARE

/*
ie_informacao_p:
M - Motivo
O - Observações
*/
dt_atual_w		timestamp;
ds_retorno_w		varchar(255);


BEGIN
ds_retorno_w := null;
dt_atual_w := dt_hr_agenda_p;
while	((coalesce(ds_retorno_w,'x') = 'x') and (trunc(dt_atual_w) <= trunc(dt_final_p))) loop
	begin

	select	hc_consulta_bloqueio(nr_seq_agenda_p, dt_atual_w, 'M')
	into STRICT 	ds_retorno_w
	;

	dt_atual_w := dt_atual_w + 1;
	end;
end loop;

ds_erro_p := ds_Retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE hc_consulta_bloqueio_periodo ( nr_seq_agenda_p bigint, dt_hr_agenda_p timestamp, dt_final_p timestamp, ie_informacao_p text, ds_erro_p INOUT text) FROM PUBLIC;

