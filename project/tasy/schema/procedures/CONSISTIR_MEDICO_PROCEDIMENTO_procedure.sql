-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consistir_medico_procedimento ( cd_medico_p text, nr_seq_proc_interno_p bigint, ds_retorno_p INOUT text) AS $body$
DECLARE


qt_entrou_w		bigint;
nr_seq_proc_interno_w   bigint;
ds_retorno_w		varchar(255) := '';


BEGIN

select	coalesce(count(*),0)
into STRICT	qt_entrou_w
from	proc_int_medico_proc
where	nr_seq_proc_interno = nr_seq_proc_interno_p;

if (qt_entrou_w > 0) then

	select	coalesce(count(*),0)
	into STRICT    nr_seq_proc_interno_w
	from    proc_int_medico_proc
	where	cd_medico = cd_medico_p
	and	nr_seq_proc_interno = nr_seq_proc_interno_p;

	if (nr_seq_proc_interno_w = 0) then
		ds_retorno_w :=	substr(Wheb_mensagem_pck.get_texto(983350),1,255);
	end if;
end if;

ds_retorno_p	:=	ds_retorno_w;


end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consistir_medico_procedimento ( cd_medico_p text, nr_seq_proc_interno_p bigint, ds_retorno_p INOUT text) FROM PUBLIC;

