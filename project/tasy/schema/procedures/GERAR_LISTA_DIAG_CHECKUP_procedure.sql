-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_lista_diag_checkup ( nr_atendimento_p bigint, nr_seq_lista_diag_p text, cd_pessoa_fisica_p text, nm_usuario_p text, nr_seq_checkup_etapa_p bigint default null, ie_liberar_p text default null) AS $body$
DECLARE



nr_seq_lista_w		varchar(255);
nr_pos_virgula_w	bigint;
nr_sequencia_w		bigint;


BEGIN
if (nr_seq_lista_diag_p IS NOT NULL AND nr_seq_lista_diag_p::text <> '') then
	begin

	nr_seq_lista_w	:= nr_seq_lista_diag_p;

	while(nr_seq_lista_w IS NOT NULL AND nr_seq_lista_w::text <> '') loop
		begin

		nr_pos_virgula_w	:= position(',' in nr_seq_lista_w);

		if (nr_pos_virgula_w > 0) then
			begin

			nr_sequencia_w	:= (substr(nr_seq_lista_w,1,nr_pos_virgula_w-1))::numeric;
			nr_seq_lista_w	:= substr(nr_seq_lista_w,nr_pos_virgula_w+1,length(nr_seq_lista_w));

			if (nr_sequencia_w > 0) then
				begin

				CALL Gerar_diagnostico_checkup(nr_atendimento_p, nr_sequencia_w, cd_pessoa_fisica_p,	nm_usuario_p, nr_seq_checkup_etapa_p, ie_liberar_p);

				end;
			end if;
			end;
		end if;
		end;
	end loop;
	end;
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_lista_diag_checkup ( nr_atendimento_p bigint, nr_seq_lista_diag_p text, cd_pessoa_fisica_p text, nm_usuario_p text, nr_seq_checkup_etapa_p bigint default null, ie_liberar_p text default null) FROM PUBLIC;

