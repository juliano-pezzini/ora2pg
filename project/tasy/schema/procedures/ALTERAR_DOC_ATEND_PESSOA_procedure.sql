-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE alterar_doc_atend_pessoa (nr_seq_documento_old_p bigint, nr_seq_documento_p bigint, nr_sequencia_p bigint, dt_validade_p timestamp) AS $body$
DECLARE


ds_arquivo_w		      atendimento_paciente_anexo.ds_arquivo%type;
cd_pessoa_fisica_w	      pessoa_fisica.cd_pessoa_fisica%type;
nm_usuario_w		      usuario.nm_usuario%type;
nr_sequencia_pessoa_doc_w pessoa_documentacao.nr_sequencia%type;


BEGIN

if (nr_sequencia_p IS NOT NULL AND nr_sequencia_p::text <> '') then
	select	a.ds_arquivo,
            b.cd_pessoa_fisica
	into STRICT	ds_arquivo_w,
            cd_pessoa_fisica_w
	from	atendimento_paciente_anexo a,
            atendimento_paciente b
	where	a.nr_atendimento = b.nr_atendimento
	and	    a.nr_sequencia   = nr_sequencia_p;

    select	max(nr_sequencia)
    into STRICT    nr_sequencia_pessoa_doc_w
    from	pessoa_documentacao
    where	cd_pessoa_fisica = cd_pessoa_fisica_w
    and     ds_arquivo       = ds_arquivo_w
    and     nr_seq_documento = NR_SEQ_DOCUMENTO_OLD_P;

	select	obter_usuario_ativo
	into STRICT	nm_usuario_w
	;

	update pessoa_documentacao
    set    dt_validade 	    = dt_validade_p
          ,nr_seq_documento = NR_SEQ_DOCUMENTO_P
          ,dt_atualizacao 	= clock_timestamp()
          ,nm_usuario 		= nm_usuario_w
	where  nr_sequencia     = nr_sequencia_pessoa_doc_w;

	commit;

end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE alterar_doc_atend_pessoa (nr_seq_documento_old_p bigint, nr_seq_documento_p bigint, nr_sequencia_p bigint, dt_validade_p timestamp) FROM PUBLIC;

