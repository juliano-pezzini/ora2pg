-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE atualizar_servico_exec_unid ( nr_sequencia_p bigint, nr_seq_servico_exec_p bigint, nm_usuario_p text) AS $body$
DECLARE

cd_setor_atendimento_w	bigint;


BEGIN

if (coalesce(nr_sequencia_p,0) > 0) then

	update	sl_unid_atend
	set	nr_seq_servico_exec = nr_seq_servico_exec_p,
		nm_usuario = nm_usuario_p,
		dt_atualizacao = clock_timestamp()
	where	nr_sequencia = nr_sequencia_p;
	select max(cd_setor_atendimento)
    into STRICT cd_setor_atendimento_w
    from sl_historico_geracao
    where nr_seq_serv_unid = nr_sequencia_p;
    CALL gravar_historico_higienizacao(nm_usuario_p, 'EMO', null, nr_seq_servico_exec_p, clock_timestamp(), cd_setor_atendimento_w, nr_sequencia_p);
		
	CALL inserir_hist_gestao_servico('A', nr_sequencia_p, nm_usuario_p);
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE atualizar_servico_exec_unid ( nr_sequencia_p bigint, nr_seq_servico_exec_p bigint, nm_usuario_p text) FROM PUBLIC;

