-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consistir_prescricao_rep ( nr_prescricao_p bigint, cd_perfil_p bigint, cd_estabelecimento_p bigint, ie_duplica_proc_lado_p text, ds_erro_p INOUT text, ds_usuario_p INOUT text, ds_perfil_p INOUT text, ds_proced_glosa_p INOUT text, nm_usuario_p text) AS $body$
DECLARE

 
ds_erro_w		varchar(255);
ds_proced_glosa_w	varchar(255);
ds_usuario_w		varchar(255);
ds_perfil_w		varchar(255);


BEGIN 
if (nr_prescricao_p IS NOT NULL AND nr_prescricao_p::text <> '') and (nm_usuario_p IS NOT NULL AND nm_usuario_p::text <> '') then 
	begin 
	 
	consistir_prescricao( 
		nr_prescricao_p, 
		cd_perfil_p, 
		nm_usuario_p, 
		ds_erro_w, 
		ds_usuario_w, 
		ds_perfil_w, 
		ds_proced_glosa_w);
	 
	CALL consiste_aferese_terapeutica( 
		nr_prescricao_p, 
		cd_estabelecimento_p, 
		nm_usuario_p);
	 
	if ('S' = ie_duplica_proc_lado_p) then 
		begin 
		CALL duplicar_lado_procedimento( 
			nr_prescricao_P);
		end;
	end if;
	end;
end if;
ds_erro_p		:= ds_erro_w;
ds_usuario_p		:= ds_usuario_w;
ds_perfil_p		:= ds_perfil_w;
ds_proced_glosa_p	:= ds_proced_glosa_w;
commit;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consistir_prescricao_rep ( nr_prescricao_p bigint, cd_perfil_p bigint, cd_estabelecimento_p bigint, ie_duplica_proc_lado_p text, ds_erro_p INOUT text, ds_usuario_p INOUT text, ds_perfil_p INOUT text, ds_proced_glosa_p INOUT text, nm_usuario_p text) FROM PUBLIC;

