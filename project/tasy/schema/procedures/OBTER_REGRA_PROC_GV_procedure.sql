-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE obter_regra_proc_gv ( cd_convenio_p bigint, ie_tipo_vaga_p text, cd_procedimento_p INOUT bigint, ie_origem_proced_p INOUT bigint, nr_seq_proc_interno_p INOUT bigint) AS $body$
DECLARE


			
c01 CURSOR FOR
	SELECT	c.cd_procedimento,
			c.ie_origem_proced,
			c.nr_seq_proc_interno
	from	conv_regra_proc_gv c,
			procedimento p
	where	c.cd_procedimento = p.cd_procedimento
	and		c.cd_convenio		= cd_convenio_p
	and		c.ie_tipo_vaga	= ie_tipo_vaga_p
	and		coalesce(p.ie_situacao,'I') = 'A'
	order by coalesce(c.nr_seq_proc_interno,0),
			 coalesce(c.cd_procedimento,0);

BEGIN
open C01;
loop
fetch C01 into	
	cd_procedimento_p,
	ie_origem_proced_p,
	nr_seq_proc_interno_p;
EXIT WHEN NOT FOUND; /* apply on C01 */
end loop;
close C01;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE obter_regra_proc_gv ( cd_convenio_p bigint, ie_tipo_vaga_p text, cd_procedimento_p INOUT bigint, ie_origem_proced_p INOUT bigint, nr_seq_proc_interno_p INOUT bigint) FROM PUBLIC;

