-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE inserir_motivo_audit_adic ( nr_seq_audit_itempaci_p bigint, nr_seq_motivo_p bigint, ie_tipo_p bigint, nm_usuario_p text) AS $body$
BEGIN

if (ie_tipo_p = 1) then

	insert into auditoria_matpaci_mot_adic(
		nr_sequencia,
		nr_seq_audit_matpaci,
		dt_atualizacao,
		nm_usuario,
		dt_atualizacao_nrec,
		nm_usuario_nrec,
		nr_seq_motivo)
	values (nextval('auditoria_matpaci_mot_adic_seq'),
		nr_seq_audit_itempaci_p,
		clock_timestamp(),
		nm_usuario_p,
		clock_timestamp(),
		nm_usuario_p,
		nr_seq_motivo_p);

elsif (ie_tipo_p = 2) then

	insert into auditoria_propaci_mot_adic(
		nr_sequencia,
		nr_seq_audit_propaci,
		dt_atualizacao,
		nm_usuario,
		dt_atualizacao_nrec,
		nm_usuario_nrec,
		nr_seq_motivo)
	values (nextval('auditoria_propaci_mot_adic_seq'),
		nr_seq_audit_itempaci_p,
		clock_timestamp(),
		nm_usuario_p,
		clock_timestamp(),
		nm_usuario_p,
		nr_seq_motivo_p);

end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE inserir_motivo_audit_adic ( nr_seq_audit_itempaci_p bigint, nr_seq_motivo_p bigint, ie_tipo_p bigint, nm_usuario_p text) FROM PUBLIC;

