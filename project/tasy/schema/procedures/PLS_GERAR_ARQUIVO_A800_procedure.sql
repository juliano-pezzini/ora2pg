-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_gerar_arquivo_a800 ( nr_seq_fatura_p bigint, cd_unimed_destino_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint, ds_erro_p INOUT text, ds_nome_arq_p INOUT text, ds_caminho_rede_p INOUT text, ie_versao_ptu_valida_p INOUT text) AS $body$
DECLARE


cd_versao_ptu_w			ptu_regra_interface.cd_versao_ptu%type;
ds_erro_w			varchar(255) := null;
nm_arquivo_w			varchar(255);
ds_caminho_rede_w		varchar(255) := null;
ie_versao_ptu_valida_w		varchar(1);


BEGIN

ie_versao_ptu_valida_w	:= 'S';

cd_versao_ptu_w	:= pls_obter_versao_ptu(cd_estabelecimento_p, cd_unimed_destino_p, clock_timestamp(), 'A800');

if (cd_versao_ptu_w in ('8.0','9.0','9.1','10.0')) then
	SELECT * FROM ptu_fatura_pre_a800_pck.gerar_arquivo_a800(	nr_seq_fatura_p, cd_unimed_destino_p, nm_usuario_p, cd_estabelecimento_p, ds_erro_w, nm_arquivo_w, ds_caminho_rede_w) INTO STRICT ds_erro_w, nm_arquivo_w, ds_caminho_rede_w;
else
	ie_versao_ptu_valida_w	:= 'N';
end if;

ds_erro_p		:= ds_erro_w;
ds_nome_arq_p		:= nm_arquivo_w;
ds_caminho_rede_p 	:= ds_caminho_rede_w;
ie_versao_ptu_valida_p 	:= ie_versao_ptu_valida_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_gerar_arquivo_a800 ( nr_seq_fatura_p bigint, cd_unimed_destino_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint, ds_erro_p INOUT text, ds_nome_arq_p INOUT text, ds_caminho_rede_p INOUT text, ie_versao_ptu_valida_p INOUT text) FROM PUBLIC;

