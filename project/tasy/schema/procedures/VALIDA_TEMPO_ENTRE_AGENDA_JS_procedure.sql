-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE valida_tempo_entre_agenda_js (nr_seq_ageint_item_p bigint, cd_agenda_p bigint, hr_agenda_p timestamp, nr_minuto_duracao_p bigint, ds_mensagem_p INOUT text ) AS $body$
DECLARE

cd_estab_pri_w		estabelecimento.cd_estabelecimento%type;
nr_seq_proc_pri_w	agenda_integrada_item.nr_seq_proc_interno%type;
nr_seq_grupo_pri_w	agenda_integrada_item.nr_seq_grupo_selec%type;
cd_paciente_w		pessoa_fisica.cd_pessoa_fisica%type;
cd_setor_exclusivo_w	setor_atendimento.cd_setor_atendimento%type;

BEGIN
select 	max(b.nr_seq_proc_interno),
	max(b.nr_seq_grupo_selec),
	max(a.cd_pessoa_fisica)
into STRICT	nr_seq_proc_pri_w,
	nr_seq_grupo_pri_w,
	cd_paciente_w
from 	agenda_integrada a,
	agenda_integrada_item b
where 	a.nr_sequencia = b.nr_seq_agenda_int
and	b.nr_sequencia = nr_seq_ageint_item_p;

select 	max(cd_setor_exclusivo),
	max(cd_estabelecimento)
into STRICT	cd_setor_exclusivo_w,
	cd_estab_pri_w
from	agenda
where	cd_agenda = cd_agenda_p;

ds_mensagem_p := valida_tempo_entre_agenda(obter_estabelecimento_ativo, cd_agenda_p, cd_estab_pri_w, nr_seq_proc_pri_w, nr_seq_grupo_pri_w, cd_setor_exclusivo_w, nr_minuto_duracao_p, cd_paciente_w, hr_agenda_p, nr_seq_ageint_item_p);

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE valida_tempo_entre_agenda_js (nr_seq_ageint_item_p bigint, cd_agenda_p bigint, hr_agenda_p timestamp, nr_minuto_duracao_p bigint, ds_mensagem_p INOUT text ) FROM PUBLIC;

