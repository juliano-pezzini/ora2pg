-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gc_atualizar_hig_final_cir_js ( nr_cirurgia_p bigint, dt_termino_p timestamp, nr_min_duracao_real_p bigint, nr_atendimento_p bigint, dt_entrada_unidade_p timestamp, nm_usuario_p text, cd_estabelecimento_p bigint, ds_pergunta_p INOUT text ) AS $body$
DECLARE

		 
		 
ds_pergunta_w	varchar(255) := '';
		

BEGIN 
 
-- RETIRADO NA OS 697785, POIS JÁ FINALIZA A CIRURGIA NO CÓDIGO FONTE. 
--if	(nr_min_duracao_real_p is not null) and 
--	(dt_termino_p is not null) then 
--	begin 
--	finalizar_cirurgia(nr_cirurgia_p,dt_termino_p,nr_min_duracao_real_p,nm_usuario_p); 
	 
	if (nr_atendimento_p IS NOT NULL AND nr_atendimento_p::text <> '') and (dt_entrada_unidade_p IS NOT NULL AND dt_entrada_unidade_p::text <> '') then 
		begin 
		CALL gerar_higieni_final_cirurgia(nr_atendimento_p,dt_entrada_unidade_p,nm_usuario_p,cd_estabelecimento_p);
		end;
	end if;
--	end;	 
	 
--end if; 
 
ds_pergunta_w := substr(obter_texto_tasy(51612, wheb_usuario_pck.get_nr_seq_idioma),1,255);
 
ds_pergunta_p := ds_pergunta_w;
 
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gc_atualizar_hig_final_cir_js ( nr_cirurgia_p bigint, dt_termino_p timestamp, nr_min_duracao_real_p bigint, nr_atendimento_p bigint, dt_entrada_unidade_p timestamp, nm_usuario_p text, cd_estabelecimento_p bigint, ds_pergunta_p INOUT text ) FROM PUBLIC;

