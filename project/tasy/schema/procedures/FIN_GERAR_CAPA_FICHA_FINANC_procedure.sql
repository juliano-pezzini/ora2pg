-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE fin_gerar_capa_ficha_financ ( nr_seq_capa_p bigint, dt_inicial_p timestamp, dt_final_p timestamp, cd_pessoa_fisica_p text, cd_cgc_p text, nm_usuario_p text) AS $body$
BEGIN
CALL fin_capa_ficha_financ_pck.fin_gerar_vl_capa(
	nr_seq_capa_p,
	coalesce(dt_inicial_p, clock_timestamp() - interval '2000 days'),
	coalesce(dt_final_p, clock_timestamp() + interval '2000 days'),
	cd_pessoa_fisica_p,
	cd_cgc_p,
	nm_usuario_p);

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE fin_gerar_capa_ficha_financ ( nr_seq_capa_p bigint, dt_inicial_p timestamp, dt_final_p timestamp, cd_pessoa_fisica_p text, cd_cgc_p text, nm_usuario_p text) FROM PUBLIC;

