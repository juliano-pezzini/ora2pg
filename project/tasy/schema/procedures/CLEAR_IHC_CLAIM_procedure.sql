-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE clear_ihc_claim (NR_SEQ_CLAIM_P IHC_CLAIM.NR_SEQUENCIA%type) AS $body$
BEGIN
  delete FROM eclipse_inco_account a 
  where a.nr_interno_conta in (SELECT b.nr_account
         from ihc_claim b 
         where b.nr_sequencia = nr_seq_claim_p);

  delete FROM ihc_rem where nr_seq_claim = nr_seq_claim_p;
  delete FROM ihc_svb where nr_seq_claim = nr_seq_claim_p;
  delete FROM ihc_anb where nr_seq_claim = nr_seq_claim_p;
  delete FROM ihc_tfr where nr_seq_claim = nr_seq_claim_p;

  delete FROM ihc_acd a where a.nr_seq_acs in (SELECT a.nr_sequencia 
    from  ihc_claim b, 
          ihc_acs a 
    where a.nr_seq_claim = b.nr_sequencia
    and   a.nr_seq_claim = nr_seq_claim_p);

  delete FROM ihc_acs where nr_seq_claim = nr_seq_claim_p;

  delete FROM ihc_msg where nr_Seq_psg in (	SELECT	nr_sequencia
										from	ihc_psg
										where	nr_seq_claim = nr_seq_claim_p);
  
  delete FROM ihc_psg where nr_seq_claim = nr_seq_claim_p;
  delete FROM ihc_trg where nr_seq_claim = nr_seq_claim_p;
  delete FROM ihc_mig where nr_seq_claim = nr_seq_claim_p;
  delete FROM ihc_dmg_diagnosis where nr_seq_claim = nr_seq_claim_p;
  delete FROM ihc_dmg_procedure where nr_seq_claim = nr_seq_claim_p;

  delete FROM ihc_mor_score a where a.nr_seq_mor in (SELECT a.nr_sequencia 
     from   ihc_claim b, ihc_mor a 
     where  a.nr_seq_claim = b.nr_sequencia
     and    a.nr_seq_claim = nr_seq_claim_p);

  delete FROM ihc_mor where nr_seq_claim = nr_seq_claim_p;
  delete FROM ihc_cer where nr_seq_claim = nr_seq_claim_p;
  delete FROM ihc_med where nr_seq_claim = nr_seq_claim_p;

  commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE clear_ihc_claim (NR_SEQ_CLAIM_P IHC_CLAIM.NR_SEQUENCIA%type) FROM PUBLIC;

