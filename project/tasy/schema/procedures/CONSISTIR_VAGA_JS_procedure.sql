-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consistir_vaga_js ( ds_consiste_solic_p text, cd_pessoa_fisica_p text, cd_estabelecimento_p bigint, nm_usuario_p text, nr_seq_gv_p bigint, ds_erro_p INOUT text) AS $body$
DECLARE

 
ds_cons_vaga_w	varchar(10);
ds_erro_w	varchar(255);
				

BEGIN 
 
if (ds_consiste_solic_p <> 'N')then 
	begin 
	 
	ds_cons_vaga_w := consistir_solicitacao_vaga(cd_pessoa_fisica_p, cd_estabelecimento_p, nm_usuario_p, nr_seq_gv_p);
	 
	if	((ds_cons_vaga_w = 'S') or (ds_cons_vaga_w = 'SU')) then 
	 
		ds_erro_w := obter_texto_tasy(43416, 1);
		 
	elsif (ds_cons_vaga_w <> 'S')then 
	 
		ds_erro_w := obter_texto_tasy(43417, 1);
		 
	end if;
	 
	end;
end if;
 
ds_erro_p	:= ds_erro_w;
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consistir_vaga_js ( ds_consiste_solic_p text, cd_pessoa_fisica_p text, cd_estabelecimento_p bigint, nm_usuario_p text, nr_seq_gv_p bigint, ds_erro_p INOUT text) FROM PUBLIC;

