-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE mprev_gravar_pop_alvo_pessoas (nr_seq_populacao_alvo_p text, lista_pessoas_p text) AS $body$
DECLARE


nr_seq_regra_cubo_w	mprev_regra_cubo.nr_sequencia%type;
lista_w                 text;


BEGIN
       if (nr_seq_populacao_alvo_p IS NOT NULL AND nr_seq_populacao_alvo_p::text <> '') then
		lista_w := lista_pessoas_p;

		select	max(a.nr_seq_regra_cubo)
		into STRICT	nr_seq_regra_cubo_w
		from	mprev_populacao_alvo a
		where	a.nr_sequencia	= nr_seq_populacao_alvo_p;

		if (lista_pessoas_p IS NOT NULL AND lista_pessoas_p::text <> '' AND nr_seq_regra_cubo_w IS NOT NULL AND nr_seq_regra_cubo_w::text <> '') then
			CALL mprev_gravar_dados_pessoa_pck.mprev_gravar_pop_alvo_pessoas(nr_seq_regra_cubo_w, nr_seq_populacao_alvo_p, lista_w);
		end if;

		commit;
	end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE mprev_gravar_pop_alvo_pessoas (nr_seq_populacao_alvo_p text, lista_pessoas_p text) FROM PUBLIC;

