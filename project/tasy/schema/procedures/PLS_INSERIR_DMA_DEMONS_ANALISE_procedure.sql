-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_inserir_dma_demons_analise ( nm_usuario_p usuario.nm_usuario%type, cd_estabelecimento_p estabelecimento.cd_estabelecimento%type, ds_arquivo_p pls_dma_demons_analise.ds_arquivo%type, ds_hash_p pls_dma_demons_analise.ds_hash%type, nr_seq_dma_demons_analise_p INOUT pls_dma_demons_analise.nr_sequencia%type) AS $body$
DECLARE


qt_hash_w	integer;


BEGIN

select	count(1)
into STRICT	qt_hash_w
from	pls_dma_demons_analise
where	ds_hash	= upper(ds_hash_p);

if (coalesce(qt_hash_w, 0) > 0) then

	CALL wheb_mensagem_pck.exibir_mensagem_abort(857329);
end if;


insert	into pls_dma_demons_analise(nr_sequencia, dt_atualizacao, nm_usuario,
	 dt_atualizacao_nrec, nm_usuario_nrec, cd_estabelecimento,
	 dt_importacao, dt_geracao_rec, nm_usuario_imp, ds_arquivo, ds_hash )
values (nextval('pls_dma_demons_analise_seq'), clock_timestamp(), nm_usuario_p,
	 clock_timestamp(), nm_usuario_p, cd_estabelecimento_p,
	 clock_timestamp(), null, nm_usuario_p, ds_arquivo_p, upper(ds_hash_p)	) returning nr_sequencia into nr_seq_dma_demons_analise_p;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_inserir_dma_demons_analise ( nm_usuario_p usuario.nm_usuario%type, cd_estabelecimento_p estabelecimento.cd_estabelecimento%type, ds_arquivo_p pls_dma_demons_analise.ds_arquivo%type, ds_hash_p pls_dma_demons_analise.ds_hash%type, nr_seq_dma_demons_analise_p INOUT pls_dma_demons_analise.nr_sequencia%type) FROM PUBLIC;

