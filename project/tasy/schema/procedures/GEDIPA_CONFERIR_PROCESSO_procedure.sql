-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gedipa_conferir_processo ( cd_estabelecimento_p bigint, cd_perfil_p bigint, nr_processo_p bigint, nr_seq_area_p bigint, ie_conferir_p text, nm_usuario_p text) AS $body$
DECLARE

				 
nr_sequencia_w	bigint;
				

BEGIN 
if (nr_processo_p IS NOT NULL AND nr_processo_p::text <> '') and (ie_conferir_p IS NOT NULL AND ie_conferir_p::text <> '') and (nm_usuario_p IS NOT NULL AND nm_usuario_p::text <> '') then 
	 
	if (ie_conferir_p = 'S') then 
	 
		/* 
		update	adep_processo 
		set	dt_leitura		= sysdate, 
			nm_usuario_leitura	= nm_usuario_p 
		where	nr_sequencia		= nr_processo_p 
		and	dt_leitura		is null 
		and	nm_usuario_leitura	is null; 
		*/
 
		 
		CALL atualiza_status_processo_adep(nr_processo_p, nr_seq_area_p, 'G', 'C', clock_timestamp(), nm_usuario_p);
				 
	end if;
	 
end if;
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gedipa_conferir_processo ( cd_estabelecimento_p bigint, cd_perfil_p bigint, nr_processo_p bigint, nr_seq_area_p bigint, ie_conferir_p text, nm_usuario_p text) FROM PUBLIC;

