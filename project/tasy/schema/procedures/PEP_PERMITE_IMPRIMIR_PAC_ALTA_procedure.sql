-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pep_permite_imprimir_pac_alta (cd_pessoa_fisica_p text) AS $body$
DECLARE

 
qt_atendimento_w	bigint;


BEGIN 
 
if (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') then 
	begin 
 
	select	count(*) 
	into STRICT	qt_atendimento_w 
	from	atendimento_paciente 
	where	cd_pessoa_fisica = cd_pessoa_fisica_p 
	and	coalesce(dt_alta::text, '') = '';
 
	if (qt_atendimento_w = 0) then 
		--(-20011, substr(obter_texto_tasy(36833, wheb_usuario_pck.get_nr_seq_idioma),1,255)); 
		CALL Wheb_mensagem_pck.exibir_mensagem_abort(36833);
	end if;
	end;
end if;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pep_permite_imprimir_pac_alta (cd_pessoa_fisica_p text) FROM PUBLIC;

