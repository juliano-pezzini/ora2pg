-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE cih_copia_def_infecto (nr_prescricao_p bigint, nr_seq_prescr_p bigint, nr_prescricao_novo_p bigint, nr_seq_prescr_novo_p bigint, nm_usuario_p text) AS $body$
DECLARE



nr_seq_def_infect_w		bigint;
ie_copiar_def_infecto_w		varchar(1);

C01 CURSOR FOR
	SELECT	nr_sequencia
	from	cih_def_infect
	where	nr_prescricao = nr_prescricao_p
	and	nr_seq_prescr = nr_seq_prescr_p;

BEGIN
begin

open C01;
loop
fetch C01 into
	nr_seq_def_infect_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin
	insert into cih_def_infect(
		cd_material,
		cd_unidade_medida,
		nr_sequencia,
		dt_atualizacao,
		nm_usuario,
		dt_atualizacao_nrec,
		nm_usuario_nrec,
		nr_atendimento,
		cd_medicamento,
		qt_dia_lib,
		qt_dose,
		cd_intervalo,
		ie_via_aplicacao,
		ds_horarios,
		nr_seq_prescr,
		ds_observacao,
		ie_definicao,
		nr_prescricao,
		ie_gera_alerta,
		cd_topografia_cih,
		ie_origem_infeccao)
	SELECT	cd_material,
		cd_unidade_medida,
		nextval('cih_def_infect_seq'),
		clock_timestamp(),
		nm_usuario_p,
		clock_timestamp(),
		nm_usuario_p,
		nr_atendimento,
		cd_medicamento,
		qt_dia_lib,
		qt_dose,
		cd_intervalo,
		ie_via_aplicacao,
		ds_horarios,
		nr_seq_prescr_novo_p,
		ds_observacao,
		ie_definicao,
		nr_prescricao_novo_p,
		ie_gera_alerta,
		cd_topografia_cih,
		ie_origem_infeccao
	from	cih_def_infect
	where	nr_sequencia = nr_seq_def_infect_w;
	end;
end loop;
close C01;
exception
	when others then
	nr_seq_def_infect_w := 0;
end;

if (coalesce(wheb_usuario_pck.get_ie_commit, 'S') = 'S') then commit; end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE cih_copia_def_infecto (nr_prescricao_p bigint, nr_seq_prescr_p bigint, nr_prescricao_novo_p bigint, nr_seq_prescr_novo_p bigint, nm_usuario_p text) FROM PUBLIC;

