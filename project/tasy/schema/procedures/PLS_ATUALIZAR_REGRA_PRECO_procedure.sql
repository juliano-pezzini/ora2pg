-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_atualizar_regra_preco (nr_seq_cp_combinada_p bigint, nr_seq_regra_p bigint, ie_tipo_tabela_p bigint, ie_opcao_p text, nm_usuario_p text) AS $body$
DECLARE

					 
/* IE_TIPO_TABELA_P : Define qual tabela que será realizado os inserts e consultas! 
* IE_TIPO_TABELA_P 1 - PLS_REGRA_PRECO_PROC; 
* IE_TIPO_TABELA_P 2 - PLS_REGRA_PRECO_MAT; 
* IE_TIPO_TABELA_P 3 - PLS_REGRA_PRECO_SERVICO; 
*/
 
 
qt_reg_tabela_w		bigint;


BEGIN 
 
qt_reg_tabela_w := 0;
 
if (nr_seq_regra_p IS NOT NULL AND nr_seq_regra_p::text <> '') then 
 
	if (ie_opcao_p = 'A') then 
		if (ie_tipo_tabela_p = 1) then 
			select 	count(*) 
			into STRICT	qt_reg_tabela_w 
			from	pls_regra_preco_proc 
			where	nr_seq_cp_combinada = nr_seq_cp_combinada_p;
			 
			if (qt_reg_tabela_w >= 1) then 
				update 	pls_regra_preco_proc 
				set	nr_seq_cp_combinada  = NULL 
				where	nr_seq_cp_combinada = nr_seq_cp_combinada_p;
			end if;
			 
			update	pls_regra_preco_proc 
			set	nr_seq_cp_combinada = nr_seq_cp_combinada_p 
			where	nr_sequencia = nr_seq_regra_p;
 
		end if;
		 
		if (ie_tipo_tabela_p = 2) then 
			select 	count(*) 
			into STRICT	qt_reg_tabela_w 
			from	pls_regra_preco_mat 
			where	nr_seq_cp_combinada = nr_seq_cp_combinada_p;
			 
			if (qt_reg_tabela_w >= 1) then 
				update 	pls_regra_preco_mat 
				set	nr_seq_cp_combinada  = NULL 
				where	nr_seq_cp_combinada = nr_seq_cp_combinada_p;
			end if;
			 
			update	pls_regra_preco_mat 
			set	nr_seq_cp_combinada = nr_seq_cp_combinada_p 
			where	nr_sequencia = nr_seq_regra_p;
			 
			 
		end if;
		 
		if (ie_tipo_tabela_p = 3) then 
			select 	count(*) 
			into STRICT	qt_reg_tabela_w 
			from	pls_regra_preco_servico 
			where	nr_seq_cp_combinada = nr_seq_cp_combinada_p;
			 
			if (qt_reg_tabela_w >= 1) then 
				update 	pls_regra_preco_servico 
				set	nr_seq_cp_combinada  = NULL 
				where	nr_seq_cp_combinada = nr_seq_cp_combinada_p;
			end if;
			 
			update	pls_regra_preco_servico 
			set	nr_seq_cp_combinada = nr_seq_cp_combinada_p 
			where	nr_sequencia = nr_seq_regra_p;
 
		end if;
	end if;
	 
	if (ie_opcao_p = 'E') then 
		if (ie_tipo_tabela_p = 1) then 
			select 	count(*) 
			into STRICT	qt_reg_tabela_w 
			from	pls_regra_preco_proc 
			where	nr_seq_cp_combinada = nr_seq_cp_combinada_p;
			 
			if (qt_reg_tabela_w >= 1) then 
				update 	pls_regra_preco_proc 
				set	nr_seq_cp_combinada  = NULL 
				where	nr_seq_cp_combinada = nr_seq_cp_combinada_p;
			end if;
			 
			update	pls_regra_preco_proc 
			set	nr_seq_cp_combinada  = NULL 
			where	nr_sequencia = nr_seq_regra_p;
 
		end if;
		 
		if (ie_tipo_tabela_p = 2) then 
			select 	count(*) 
			into STRICT	qt_reg_tabela_w 
			from	pls_regra_preco_mat 
			where	nr_seq_cp_combinada = nr_seq_cp_combinada_p;
			 
			if (qt_reg_tabela_w >= 1) then 
				update 	pls_regra_preco_mat 
				set	nr_seq_cp_combinada  = NULL 
				where	nr_seq_cp_combinada = nr_seq_cp_combinada_p;
			end if;
			 
			update	pls_regra_preco_mat 
			set	nr_seq_cp_combinada  = NULL 
			where	nr_sequencia = nr_seq_regra_p;
			 
			 
		end if;
		 
		if (ie_tipo_tabela_p = 3) then 
			select 	count(*) 
			into STRICT	qt_reg_tabela_w 
			from	pls_regra_preco_servico 
			where	nr_seq_cp_combinada = nr_seq_cp_combinada_p;
			 
			if (qt_reg_tabela_w >= 1) then 
				update 	pls_regra_preco_servico 
				set	nr_seq_cp_combinada  = NULL 
				where	nr_seq_cp_combinada = nr_seq_cp_combinada_p;
			end if;
			 
			update	pls_regra_preco_servico 
			set	nr_seq_cp_combinada  = NULL 
			where	nr_sequencia = nr_seq_regra_p;
 
		end if;
	 
	 
	end if;
end if;
 
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_atualizar_regra_preco (nr_seq_cp_combinada_p bigint, nr_seq_regra_p bigint, ie_tipo_tabela_p bigint, ie_opcao_p text, nm_usuario_p text) FROM PUBLIC;

