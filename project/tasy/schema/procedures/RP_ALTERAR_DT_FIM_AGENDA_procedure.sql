-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE rp_alterar_dt_fim_agenda ( nr_sequencia_p bigint, dt_fim_agenda_p timestamp, ie_opcao_p text) AS $body$
BEGIN
if (dt_fim_agenda_p IS NOT NULL AND dt_fim_agenda_p::text <> '') then
	if (ie_opcao_p = 'I') and (nr_sequencia_p IS NOT NULL AND nr_sequencia_p::text <> '') then
		update	rp_pac_agend_individual
		set	dt_fim_agendamento = dt_fim_agenda_p
		where	nr_sequencia = nr_sequencia_p;
	elsif (ie_opcao_p = 'M') and (nr_sequencia_p IS NOT NULL AND nr_sequencia_p::text <> '') then
		update	rp_pac_modelo_agendamento
		set	dt_fim_agendamento = dt_fim_agenda_p
		where	nr_sequencia = nr_sequencia_p;
	end if;
end if;
commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE rp_alterar_dt_fim_agenda ( nr_sequencia_p bigint, dt_fim_agenda_p timestamp, ie_opcao_p text) FROM PUBLIC;

