-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE atualizar_dose_far_estoque (nr_sequencia_p bigint, qt_estoque_p bigint, cd_material_p bigint, cd_unidade_medida_p text, qt_dose_p bigint) AS $body$
DECLARE


qt_estoque_w double precision;

BEGIN
begin
select 	obter_conversao_unid_med_cons(cd_material_p, cd_unidade_medida_p, coalesce(qt_dose_p,0)) + coalesce(qt_estoque_p,0)
into STRICT	qt_estoque_w
;

if (nr_sequencia_p IS NOT NULL AND nr_sequencia_p::text <> '') then
	update far_estoque_cabine
	set qt_estoque = qt_estoque_w
	where nr_sequencia = nr_sequencia_p;
end if;

exception

	when others then
	rollback;
	CALL WHEB_MENSAGEM_PCK.Exibir_Mensagem_Abort(198122);
end;
commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE atualizar_dose_far_estoque (nr_sequencia_p bigint, qt_estoque_p bigint, cd_material_p bigint, cd_unidade_medida_p text, qt_dose_p bigint) FROM PUBLIC;

