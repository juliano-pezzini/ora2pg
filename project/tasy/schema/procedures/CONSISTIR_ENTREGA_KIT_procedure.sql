-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consistir_entrega_kit (cd_kit_material_p bigint, nr_seq_kit_estoque_p bigint, nr_doc_interno_p text, nr_doc_interno_aux_p text, ie_tipo_doc_interno_p text, ie_tipo_doc_interno_aux_p text, nm_usuario_p text, ds_retorno_p INOUT text) AS $body$
DECLARE


cd_material_w		integer;
cd_pessoa_fisica_F_w	varchar(10);
cd_pessoa_fisica_E_w	varchar(10);
ds_retorno_w		varchar(2000):= null;

c01 CURSOR FOR
	SELECT	cd_material
	from	prescr_material
	where	cd_kit_material = cd_kit_material_p;

c02 CURSOR FOR
	SELECT	cd_material
	from	prescr_material
	where	nr_seq_kit_estoque = nr_seq_kit_estoque_p;



BEGIN
if (ie_tipo_doc_interno_p = 'B') then
	select 	max(cd_pessoa_fisica)
	into STRICT	cd_pessoa_fisica_F_w
	from 	usuario
	
	where 	Campo_numerico_parametro(cd_barras, '999999999999999999999999999D9999') = to_char(nr_doc_interno_p);
elsif (ie_tipo_doc_interno_p = 'C') then
	select 	max(cd_pessoa_fisica)
	into STRICT	cd_pessoa_fisica_F_w
	from 	medico
	
	where 	Campo_numerico_parametro(nr_crm, '999999999999999999999999999D9999') = to_char(nr_doc_interno_p);
end if;

if (ie_tipo_doc_interno_aux_p = 'B') then
	select 	max(cd_pessoa_fisica)
	into STRICT	cd_pessoa_fisica_E_w
	from 	usuario
	
	where 	Campo_numerico_parametro(cd_barras, '999999999999999999999999999D9999') = to_char(nr_doc_interno_aux_p);
elsif (ie_tipo_doc_interno_aux_p = 'C') then
	select 	max(cd_pessoa_fisica)
	into STRICT	cd_pessoa_fisica_E_w
	from 	medico
	
	where 	Campo_numerico_parametro(nr_crm, '999999999999999999999999999D9999') = to_char(nr_doc_interno_aux_p);
end if;

open C01;
loop
fetch C01 into
	cd_material_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin
	if (coalesce(ds_retorno_w::text, '') = '') and (obter_se_mat_lib_profissional(cd_pessoa_fisica_F_w,cd_material_w,'E') = 'N') then
		ds_retorno_w := Wheb_mensagem_pck.get_texto(306515); -- 'Este usuário não possui permissão para entregar este item!';
	elsif (coalesce(ds_retorno_w::text, '') = '') and (obter_se_mat_lib_profissional(cd_pessoa_fisica_E_w,cd_material_w,'R') = 'N') then
		ds_retorno_w := Wheb_mensagem_pck.get_texto(306516); -- 'Este usuário não possui permissão para retirar este item!';
	end if;
	end;
end loop;
close C01;

open C02;
loop
fetch C02 into
	cd_material_w;
EXIT WHEN NOT FOUND; /* apply on C02 */
	begin
	if (coalesce(ds_retorno_w::text, '') = '') and (obter_se_mat_lib_profissional(cd_pessoa_fisica_F_w,cd_material_w,'E') = 'N') then
		ds_retorno_w := Wheb_mensagem_pck.get_texto(306515); -- 'Este usuário não possui permissão para entregar este item!';
	elsif (coalesce(ds_retorno_w::text, '') = '') and (obter_se_mat_lib_profissional(cd_pessoa_fisica_E_w,cd_material_w,'R') = 'N') then
		ds_retorno_w := Wheb_mensagem_pck.get_texto(306516); -- 'Este usuário não possui permissão para retirar este item!';
	end if;
	end;
end loop;
close C02;

ds_retorno_p := ds_retorno_w;

end	;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consistir_entrega_kit (cd_kit_material_p bigint, nr_seq_kit_estoque_p bigint, nr_doc_interno_p text, nr_doc_interno_aux_p text, ie_tipo_doc_interno_p text, ie_tipo_doc_interno_aux_p text, nm_usuario_p text, ds_retorno_p INOUT text) FROM PUBLIC;

