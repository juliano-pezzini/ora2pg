-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE inserir_kit_recomendacao ( nm_usuario_p text, nr_prescricao_p bigint, nr_seq_recomendacao_p bigint default null, cd_recomendacao_p bigint DEFAULT NULL, cd_kit_p bigint DEFAULT NULL) AS $body$
DECLARE


nr_seq_kit_w	bigint;

BEGIN

select	coalesce(max(nr_sequencia),0)+1
into STRICT	nr_seq_kit_w
from	kit_prescr_recomendacao;

insert into kit_prescr_recomendacao(
		nr_sequencia,
		dt_atualizacao,
		nm_usuario,
		nr_prescricao,
		nr_seq_recomendacao,
		cd_kit,
		cd_recomendacao)
	values (
		nr_seq_kit_w,
		clock_timestamp(),
		nm_usuario_p,
		nr_prescricao_p,
		nr_seq_recomendacao_p,
		cd_kit_p,
		cd_recomendacao_p);
if (coalesce(wheb_usuario_pck.get_ie_commit, 'S') = 'S') then commit; end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE inserir_kit_recomendacao ( nm_usuario_p text, nr_prescricao_p bigint, nr_seq_recomendacao_p bigint default null, cd_recomendacao_p bigint DEFAULT NULL, cd_kit_p bigint DEFAULT NULL) FROM PUBLIC;

