-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE inserir_criterio_rateio ( nr_seq_criterio_rateio_p bigint, nr_seq_bem_p text) AS $body$
BEGIN
if (nr_seq_bem_p IS NOT NULL AND nr_seq_bem_p::text <> '') then
	begin
	if (nr_seq_criterio_rateio_p IS NOT NULL AND nr_seq_criterio_rateio_p::text <> '') then
		begin
		update  pat_bem
		set    	nr_seq_crit_rateio = nr_seq_criterio_rateio_p
		where  	nr_sequencia = nr_seq_bem_p;
		end;
	end if;
	commit;
	end;
end if;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE inserir_criterio_rateio ( nr_seq_criterio_rateio_p bigint, nr_seq_bem_p text) FROM PUBLIC;

