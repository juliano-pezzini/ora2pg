-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE fa_consistir_item_separacao (nr_seq_entrega_p bigint, ds_retorno_p INOUT text) AS $body$
DECLARE


qt_itens_w	bigint;
ds_retorno_w	varchar(1) := 'N';

BEGIN

select	count(*)
into STRICT	qt_itens_w
from	fa_entrega_medicacao_item
where	nr_seq_fa_entrega = nr_seq_entrega_p
and	ie_status_medicacao <> 'AC'
and	coalesce(dt_cancelamento::text, '') = '';

if (qt_itens_w = 0) then
	ds_retorno_w := 'S';
end if;

ds_retorno_p := ds_retorno_w;
commit;


end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE fa_consistir_item_separacao (nr_seq_entrega_p bigint, ds_retorno_p INOUT text) FROM PUBLIC;

