-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_agenda_consulta_hist (cd_agenda_p bigint, nr_seq_agenda_p bigint, ie_historico_p text, nm_usuario_hist_p text, ds_historico_p text, cd_pessoa_hist_p text, nm_paciente_hist_p text, dt_agenda_hist_p timestamp) AS $body$
DECLARE


nr_seq_hist_w	bigint;
cd_protocolo_w  ageint_protocolo_atend.cd_protocolo%TYPE;


BEGIN

if (cd_agenda_p IS NOT NULL AND cd_agenda_p::text <> '') and (nr_seq_agenda_p IS NOT NULL AND nr_seq_agenda_p::text <> '') and (ie_historico_p IS NOT NULL AND ie_historico_p::text <> '') and
	--(nm_usuario_hist_p is not null) and
	(ds_historico_p IS NOT NULL AND ds_historico_p::text <> '') and
	--((cd_pessoa_hist_p is not null) or

	-- (nm_paciente_hist_p is not null)) and
	(dt_agenda_hist_p IS NOT NULL AND dt_agenda_hist_p::text <> '') then

	/* obter sequence */

	select	nextval('agenda_consulta_hist_seq')
	into STRICT	nr_seq_hist_w
	;

	/* Obter protocolo */

	cd_protocolo_w := ageint_obter_protocolo_atend(nm_usuario_hist_p);

	/* gerar historico */

	insert into agenda_consulta_hist(
		nr_sequencia,
		cd_agenda,
		nr_seq_agenda,
		ie_historico,
		dt_historico,
		nm_usuario_hist,
		ds_historico,
		cd_pessoa_hist,
		nm_paciente_hist,
		dt_agenda_hist,
		cd_protocolo,
		ds_stack
		)
	values (
		nr_seq_hist_w,
		cd_agenda_p,
		nr_seq_agenda_p,
		ie_historico_p,
		clock_timestamp(),
		nm_usuario_hist_p,
		ds_historico_p,
		cd_pessoa_hist_p,
		substr(nm_paciente_hist_p,1,60),
		dt_agenda_hist_p,
		cd_protocolo_w,
		substr(dbms_utility.format_call_stack,1,4000)
		);

end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_agenda_consulta_hist (cd_agenda_p bigint, nr_seq_agenda_p bigint, ie_historico_p text, nm_usuario_hist_p text, ds_historico_p text, cd_pessoa_hist_p text, nm_paciente_hist_p text, dt_agenda_hist_p timestamp) FROM PUBLIC;

