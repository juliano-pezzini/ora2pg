-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE alterar_motivo_transf_atend ( nr_seq_motivo_transf_p text, nr_seq_motivo_perm_p text, nr_seq_interno_p bigint) AS $body$
BEGIN

if	((coalesce(nr_seq_motivo_transf_p,0) > 0) or (coalesce(nr_seq_motivo_perm_p,0) > 0)) then

	update	atend_paciente_unidade
	set	nr_seq_motivo_transf = coalesce(nr_seq_motivo_transf_p, nr_seq_motivo_transf),
		nr_seq_motivo_perm   = coalesce(nr_seq_motivo_perm_p, nr_seq_motivo_perm)
	where	nr_seq_interno 	     = nr_seq_interno_p;

end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE alterar_motivo_transf_atend ( nr_seq_motivo_transf_p text, nr_seq_motivo_perm_p text, nr_seq_interno_p bigint) FROM PUBLIC;

