-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_alt_situ_regra_inc ( nr_seq_regra_p bigint, ie_opcao_p text, nm_usuario_p text) AS $body$
BEGIN

if (ie_opcao_p = 'A') then
	update	PLS_MOV_BENEF_REGRA_INCONS
	set	ie_situacao = 'A',
		nm_usuario = nm_usuario_p,
		dt_atualizacao = clock_timestamp()
	where	nr_sequencia = nr_seq_regra_p;
elsif (ie_opcao_p = 'I') then
	update	PLS_MOV_BENEF_REGRA_INCONS
	set	ie_situacao = 'I',
		nm_usuario = nm_usuario_p,
		dt_atualizacao = clock_timestamp()
	where	nr_sequencia = nr_seq_regra_p;
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_alt_situ_regra_inc ( nr_seq_regra_p bigint, ie_opcao_p text, nm_usuario_p text) FROM PUBLIC;

