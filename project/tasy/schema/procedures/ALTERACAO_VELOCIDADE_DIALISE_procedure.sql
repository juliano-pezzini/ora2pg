-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE alteracao_velocidade_dialise (nr_prescricao_p bigint, nr_seq_solucao_p bigint, nm_usuario_p text, qt_dosagem_p bigint, ie_unid_vel_inf_p text) AS $body$
DECLARE


nr_seq_evento_w	bigint;


BEGIN
IF (nr_prescricao_p IS NOT NULL AND nr_prescricao_p::text <> '') AND (nr_seq_solucao_p IS NOT NULL AND nr_seq_solucao_p::text <> '') AND (nm_usuario_p IS NOT NULL AND nm_usuario_p::text <> '') THEN

	SELECT	nextval('hd_prescricao_evento_seq')
	INTO STRICT	nr_seq_evento_w
	;

	INSERT INTO hd_prescricao_evento(
		nr_sequencia,
		dt_atualizacao_nrec,
		nm_usuario_nrec,
		dt_atualizacao,
		nm_usuario,
		nr_prescricao,
		nr_seq_solucao,
		ie_evento,
		dt_evento,
		cd_pessoa_evento,
		qt_volume,
		qt_dosagem,
		ie_unid_vel_inf)
	VALUES (
		nr_seq_evento_w,
		clock_timestamp(),
		nm_usuario_p,
		clock_timestamp(),
		nm_usuario_p,
		nr_prescricao_p,
		nr_seq_solucao_p,
		'AV',
		clock_timestamp(),
		SUBSTR(obter_dados_usuario_opcao(nm_usuario_p,'C'),1,10),
		NULL,
		coalesce(qt_dosagem_p,0),
		ie_unid_vel_inf_p);

END IF;

COMMIT;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE alteracao_velocidade_dialise (nr_prescricao_p bigint, nr_seq_solucao_p bigint, nm_usuario_p text, qt_dosagem_p bigint, ie_unid_vel_inf_p text) FROM PUBLIC;

