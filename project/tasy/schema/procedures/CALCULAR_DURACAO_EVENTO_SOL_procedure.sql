-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE calcular_duracao_evento_sol (ie_tipo_solucao_p bigint, nr_prescricao_p bigint, nr_seq_solucao_p bigint) AS $body$
DECLARE


nr_seq_evento_w	bigint;
dt_evento_w		timestamp;
nr_seq_ant_w		bigint := 0;
dt_ant_w		timestamp;
qt_duracao_w		double precision;

c01 CURSOR FOR
SELECT	nr_sequencia,
	dt_alteracao
from	prescr_solucao_evento
where	ie_tipo_solucao = ie_tipo_solucao_p
and	nr_prescricao = nr_prescricao_p
and	nr_seq_solucao = nr_seq_solucao_p
and	ie_evento_valido = 'S'
and	ie_alteracao <> 6
order by
	nr_sequencia;

c02 CURSOR FOR
SELECT	nr_sequencia,
	dt_alteracao
from	prescr_solucao_evento
where	ie_tipo_solucao = ie_tipo_solucao_p
and	nr_prescricao = nr_prescricao_p
and	nr_seq_material = nr_seq_solucao_p
and	ie_evento_valido = 'S'
and	ie_alteracao <> 6
order by
	nr_sequencia;

c03 CURSOR FOR
SELECT	nr_sequencia,
	dt_alteracao
from	prescr_solucao_evento
where	ie_tipo_solucao = ie_tipo_solucao_p
and	nr_prescricao = nr_prescricao_p
and	nr_seq_procedimento = nr_seq_solucao_p
and	ie_evento_valido = 'S'
and	ie_alteracao <> 6
order by
	nr_sequencia;

c04 CURSOR FOR
SELECT	nr_sequencia,
	dt_alteracao
from	prescr_solucao_evento
where	ie_tipo_solucao = ie_tipo_solucao_p
and	nr_prescricao = nr_prescricao_p
and	nr_seq_nut = nr_seq_solucao_p
and	ie_evento_valido = 'S'
and	ie_alteracao <> 6
order by
	nr_sequencia;

c05 CURSOR FOR
SELECT	nr_sequencia,
	dt_alteracao
from	prescr_solucao_evento
where	ie_tipo_solucao = ie_tipo_solucao_p
and	nr_prescricao = nr_prescricao_p
and	nr_seq_nut_neo = nr_seq_solucao_p
and	ie_evento_valido = 'S'
and	ie_alteracao <> 6
order by
	nr_sequencia;


BEGIN
if (ie_tipo_solucao_p IS NOT NULL AND ie_tipo_solucao_p::text <> '') and (nr_prescricao_p IS NOT NULL AND nr_prescricao_p::text <> '') and (nr_seq_solucao_p IS NOT NULL AND nr_seq_solucao_p::text <> '') then
	if (ie_tipo_solucao_p = 1) then -- solucoes
		open c01;
		loop
		fetch c01 into	nr_seq_evento_w,
					dt_evento_w;
		EXIT WHEN NOT FOUND; /* apply on c01 */
			begin
			if (nr_seq_ant_w > 0) then
				qt_duracao_w := round((dt_evento_w - dt_ant_w) * 24,2);
				if (qt_duracao_w < 1) then
					update	prescr_solucao_evento
					set	ds_duracao = '0' || to_char(qt_duracao_w) || ' h'
					where	nr_sequencia = nr_seq_ant_w;
				else
					update	prescr_solucao_evento
					set	ds_duracao = qt_duracao_w || 'h'
					where	nr_sequencia = nr_seq_ant_w;
				end if;
			end if;
			nr_seq_ant_w	:= nr_seq_evento_w;
			dt_ant_w	:= dt_evento_w;
			end;
		end loop;
		close c01;

	elsif (ie_tipo_solucao_p = 2) then -- sne
		open c02;
		loop
		fetch c02 into	nr_seq_evento_w,
					dt_evento_w;
		EXIT WHEN NOT FOUND; /* apply on c02 */
			begin
			if (nr_seq_ant_w > 0) then
				qt_duracao_w := round((dt_evento_w - dt_ant_w) * 24,2);
				if (qt_duracao_w < 1) then
					update	prescr_solucao_evento
					set	ds_duracao = '0' || to_char(qt_duracao_w) || ' h'
					where	nr_sequencia = nr_seq_ant_w;
				else
					update	prescr_solucao_evento
					set	ds_duracao = qt_duracao_w || 'h'
					where	nr_sequencia = nr_seq_ant_w;
				end if;
			end if;
			nr_seq_ant_w	:= nr_seq_evento_w;
			dt_ant_w	:= dt_evento_w;
			end;
		end loop;
		close c02;

	elsif (ie_tipo_solucao_p = 3) then -- hemocomponentes
		open c03;
		loop
		fetch c03 into	nr_seq_evento_w,
					dt_evento_w;
		EXIT WHEN NOT FOUND; /* apply on c03 */
			begin
			if (nr_seq_ant_w > 0) then
				qt_duracao_w := round((dt_evento_w - dt_ant_w) * 24,2);
				if (qt_duracao_w < 1) then
					update	prescr_solucao_evento
					set	ds_duracao = '0' || to_char(qt_duracao_w) || ' h'
					where	nr_sequencia = nr_seq_ant_w;
				else
					update	prescr_solucao_evento
					set	ds_duracao = qt_duracao_w || 'h'
					where	nr_sequencia = nr_seq_ant_w;
				end if;
			end if;
			nr_seq_ant_w	:= nr_seq_evento_w;
			dt_ant_w	:= dt_evento_w;
			end;
		end loop;
		close c03;

	elsif (ie_tipo_solucao_p = 4) then -- npt
		open c04;
		loop
		fetch c04 into	nr_seq_evento_w,
					dt_evento_w;
		EXIT WHEN NOT FOUND; /* apply on c04 */
			begin
			if (nr_seq_ant_w > 0) then
				qt_duracao_w := round((dt_evento_w - dt_ant_w) * 24,2);
				if (qt_duracao_w < 1) then
					update	prescr_solucao_evento
					set	ds_duracao = '0' || to_char(qt_duracao_w) || ' h'
					where	nr_sequencia = nr_seq_ant_w;
				else
					update	prescr_solucao_evento
					set	ds_duracao = qt_duracao_w || 'h'
					where	nr_sequencia = nr_seq_ant_w;
				end if;
			end if;
			nr_seq_ant_w	:= nr_seq_evento_w;
			dt_ant_w	:= dt_evento_w;
			end;
		end loop;
		close c04;

	elsif (ie_tipo_solucao_p = 5) then -- npt neo
		open c05;
		loop
		fetch c05 into	nr_seq_evento_w,
					dt_evento_w;
		EXIT WHEN NOT FOUND; /* apply on c05 */
			begin
			if (nr_seq_ant_w > 0) then
				qt_duracao_w := round((dt_evento_w - dt_ant_w) * 24,2);
				if (qt_duracao_w < 1) then
					update	prescr_solucao_evento
					set	ds_duracao = '0' || to_char(qt_duracao_w) || ' h'
					where	nr_sequencia = nr_seq_ant_w;
				else
					update	prescr_solucao_evento
					set	ds_duracao = qt_duracao_w || 'h'
					where	nr_sequencia = nr_seq_ant_w;
				end if;
			end if;
			nr_seq_ant_w	:= nr_seq_evento_w;
			dt_ant_w	:= dt_evento_w;
			end;
		end loop;
		close c05;
	end if;
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE calcular_duracao_evento_sol (ie_tipo_solucao_p bigint, nr_prescricao_p bigint, nr_seq_solucao_p bigint) FROM PUBLIC;

