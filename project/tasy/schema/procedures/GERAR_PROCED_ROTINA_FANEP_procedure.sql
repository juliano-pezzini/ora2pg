-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_proced_rotina_fanep ( nr_seq_pepo_p bigint, nr_seq_proc_interno_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) AS $body$
DECLARE

										 
cd_procedimento_w	bigint;		
ie_origem_proced_w	bigint;		
 

BEGIN 
 
SELECT * FROM Obter_Proc_Tab_Interno(nr_seq_proc_interno_p, 0, null, null, cd_procedimento_w, ie_origem_proced_w, null, null) INTO STRICT cd_procedimento_w, ie_origem_proced_w;
 
if (nr_seq_pepo_p IS NOT NULL AND nr_seq_pepo_p::text <> '') and (nr_seq_proc_interno_p IS NOT NULL AND nr_seq_proc_interno_p::text <> '') then 
	insert	into	pepo_cirurgia_proc( 
			nr_sequencia, 
			dt_atualizacao, 
			nm_usuario, 
			dt_atualizacao_nrec, 
			nm_usuario_nrec, 
			nr_seq_pepo, 
			nr_seq_proc_interno, 
			cd_procedimento, 
			ie_origem_proced) 
	values (			 
			nextval('pepo_cirurgia_proc_seq'), 
			clock_timestamp(), 
			nm_usuario_p, 
			clock_timestamp(), 
			nm_usuario_p, 
			nr_seq_pepo_p, 
			nr_seq_proc_interno_p, 
			cd_procedimento_w, 
			ie_origem_proced_w);	
	commit;			
end if;	
										 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_proced_rotina_fanep ( nr_seq_pepo_p bigint, nr_seq_proc_interno_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) FROM PUBLIC;

