-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_atualizar_status_int_a500 ( nr_seq_protocolo_p bigint, nr_seq_fatura_p bigint, nm_usuario_p text) AS $body$
DECLARE


vl_glosa_w			double precision;
vl_total_fatura_w		double precision;
ie_status_w			varchar(10);


BEGIN
/*select	nvl(sum(vl_glosa),0)
into	vl_glosa_w
from	pls_conta
where	nr_seq_protocolo	= nr_seq_protocolo_p;

select	dividir(nvl(sum(vl_total_fatura),0) + nvl(sum(vl_total_ndc),0),2)
into	vl_total_fatura_w
from	ptu_fatura
where	nr_sequencia	= nr_seq_fatura_p;

-- Caso o valor da glosa for maior que a metade do valor da fatura, a fatura será rejeitado e permite desfazer a importação
if	(vl_glosa_w > vl_total_fatura_w) then
	ie_status_w	:= 'R';		OS 648138 / 44171 - WCBERNARDINO - O valor de GLOSA nunca poderá ser maior que o valor TOTAL da FATURA
end if;*/
select	max(ie_status)
into STRICT	ie_status_w
from	ptu_fatura
where	nr_sequencia	= nr_seq_fatura_p;

CALL ptu_atualizar_status_fatura( nr_seq_fatura_p, ie_status_w, null, nm_usuario_p);

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_atualizar_status_int_a500 ( nr_seq_protocolo_p bigint, nr_seq_fatura_p bigint, nm_usuario_p text) FROM PUBLIC;

