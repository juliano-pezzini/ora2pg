-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_dado_estat_checkup ( nr_atendimento_p bigint, nr_seq_protocolo_p bigint, nm_usuario_p text, cd_tipo_p bigint default 0) AS $body$
DECLARE


cd_pessoa_usuario_w	varchar(10);
cd_empresa_w		smallint;

BEGIN

if (nr_atendimento_p > 0) and (nr_seq_protocolo_p IS NOT NULL AND nr_seq_protocolo_p::text <> '') then


	If (nr_seq_protocolo_p = -1 ) then
	
		Select	Obter_Pf_Usuario(nm_usuario_p,'C'),
				obter_empresa_estab(wheb_usuario_pck.get_cd_estabelecimento)
		into STRICT	cd_pessoa_usuario_w,
				cd_empresa_w
		;
	
		CALL gerar_dados_aval_checkup(nr_atendimento_p, cd_pessoa_usuario_w, cd_empresa_w, coalesce(cd_tipo_p,0), clock_timestamp(), nm_usuario_p);
	
	else
	
		CALL gerar_dados_checkup(nr_atendimento_p, nr_seq_protocolo_p, nm_usuario_p);
	
	end if;
	
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_dado_estat_checkup ( nr_atendimento_p bigint, nr_seq_protocolo_p bigint, nm_usuario_p text, cd_tipo_p bigint default 0) FROM PUBLIC;

