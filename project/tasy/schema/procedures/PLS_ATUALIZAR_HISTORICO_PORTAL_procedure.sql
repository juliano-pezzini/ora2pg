-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_atualizar_historico_portal ( nr_seq_atendimento_p bigint, nm_usuario_p text) AS $body$
DECLARE


dt_envio_mensagem_w	varchar(20);
ds_mensagem_w		varchar(255);
nm_usuario_envio_w	varchar(255);
ds_mensagem_final_w	text;

c01 CURSOR FOR
	SELECT	to_char(dt_envio_mensagem, 'dd/mm/yyyy hh24:mi:ss'),
		nm_usuario_envio,
		ds_mensagem
	from 	pls_atendimento_chat
	where 	nr_seq_atendimento = nr_seq_atendimento_p
	order by nr_sequencia;

BEGIN
open c01;
loop
fetch c01 into	dt_envio_mensagem_w,
		nm_usuario_envio_w,
		ds_mensagem_w;
EXIT WHEN NOT FOUND; /* apply on c01 */

	if (coalesce(ds_mensagem_final_w::text, '') = '') then
		ds_mensagem_final_w := '(' || dt_envio_mensagem_w || ') - ' || nm_usuario_envio_w || chr(10) || ds_mensagem_w;
	else
		ds_mensagem_final_w := ds_mensagem_final_w || chr(10) || '(' || dt_envio_mensagem_w || ') - ' || nm_usuario_envio_w || chr(10) ||ds_mensagem_w;
	end if;
end loop;
close c01;

update	pls_atendimento
set	ds_historico_atend_chat = ds_mensagem_final_w
where	nr_sequencia = nr_seq_atendimento_p;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_atualizar_historico_portal ( nr_seq_atendimento_p bigint, nm_usuario_p text) FROM PUBLIC;

