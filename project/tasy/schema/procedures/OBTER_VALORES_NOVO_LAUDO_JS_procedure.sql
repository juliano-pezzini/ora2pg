-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE obter_valores_novo_laudo_js ( cd_estabelecimento_p bigint, nr_atendimento_p bigint, cd_medico_auditor_p INOUT text, nr_laudo_sus_p INOUT bigint) AS $body$
DECLARE

 
cd_medico_auditor_w	varchar(10);
nr_laudo_sus_w		smallint;	
				 

BEGIN 
 
if (coalesce(cd_estabelecimento_p,0) > 0 ) and (coalesce(nr_atendimento_p,0) > 0) then 
	begin 
	 
	begin 
		select 	cd_medico_auditor 
		into STRICT	cd_medico_auditor_w 
		from 	sus_parametros 
		where 	cd_estabelecimento = cd_estabelecimento_p;
	exception 
	when others then 
		cd_medico_auditor_w := null;
	end;
	 
	select  max(nr_laudo_sus) 
	into STRICT	 nr_laudo_sus_w 
	from   sus_laudo_paciente 
	where  nr_atendimento = nr_atendimento_p;
	 
	end;
	 
	cd_medico_auditor_p := cd_medico_auditor_w;
	nr_laudo_sus_p := nr_laudo_sus_w;
	 
end if;
 
commit;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE obter_valores_novo_laudo_js ( cd_estabelecimento_p bigint, nr_atendimento_p bigint, cd_medico_auditor_p INOUT text, nr_laudo_sus_p INOUT bigint) FROM PUBLIC;

