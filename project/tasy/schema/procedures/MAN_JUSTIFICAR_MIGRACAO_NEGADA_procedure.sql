-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE man_justificar_migracao_negada ( nr_seq_ordem_serv_p bigint, ds_just_migr_neg_p text, nm_usuario_p text ) AS $body$
BEGIN

if (nr_seq_ordem_serv_p IS NOT NULL AND nr_seq_ordem_serv_p::text <> '') and (ds_just_migr_neg_p IS NOT NULL AND ds_just_migr_neg_p::text <> '') and (nm_usuario_p IS NOT NULL AND nm_usuario_p::text <> '') then

	update	man_ordem_servico
	set	ds_just_migr_neg = ds_just_migr_neg_p,
		nm_usuario_migr_neg = nm_usuario_p,
		dt_migr_neg = clock_timestamp()
	where	nr_sequencia = nr_seq_ordem_serv_p;

end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE man_justificar_migracao_negada ( nr_seq_ordem_serv_p bigint, ds_just_migr_neg_p text, nm_usuario_p text ) FROM PUBLIC;

