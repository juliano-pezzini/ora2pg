-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE deletar_orcamento_pac_vinc ( nr_seq_proc_princ_p bigint) AS $body$
DECLARE


nr_seq_proc_princ_w	bigint;

C01 CURSOR FOR
	SELECT	nr_sequencia
	from	orcamento_paciente_proc
	where	nr_seq_proc_princ = nr_seq_proc_princ_p;


BEGIN

if (coalesce(nr_seq_proc_princ_p,0) <> 0) then

	open C01;
	loop
	fetch C01 into
		nr_seq_proc_princ_w;
	EXIT WHEN NOT FOUND; /* apply on C01 */
		begin

		delete	from orcamento_paciente_proc
		where	nr_seq_proc_princ = nr_seq_proc_princ_w;

		delete	from orcamento_paciente_mat
		where	nr_seq_proc_princ = nr_seq_proc_princ_w;

		end;
	end loop;
	close C01;

	delete	from orcamento_paciente_proc
	where	nr_seq_proc_princ = nr_seq_proc_princ_p;

	delete	from orcamento_paciente_mat
	where	nr_seq_proc_princ = nr_seq_proc_princ_p;

end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE deletar_orcamento_pac_vinc ( nr_seq_proc_princ_p bigint) FROM PUBLIC;

