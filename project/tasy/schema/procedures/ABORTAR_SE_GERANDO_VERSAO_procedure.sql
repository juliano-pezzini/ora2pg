-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE abortar_se_gerando_versao () AS $body$
DECLARE


ie_status_aplicacao_w varchar(1);
qt_reg_w	bigint;


BEGIN


select 	ie_status_aplicacao
into STRICT	ie_status_aplicacao_w
from 	aplicacao_tasy
where 	cd_aplicacao_tasy = 'Tasy';

select	count(*)
into STRICT	qt_reg_w
from	v$session
where	audsid = (
   SELECT userenv('sessionid') )
and (upper(machine) like '%WHEBD12%'
or	upper(machine) like '%WHEBT13%');

if ( ie_status_aplicacao_w = 'U' ) and (qt_reg_w = 0) then
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(279952);
end if;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE abortar_se_gerando_versao () FROM PUBLIC;

