-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_integracao_tit_pagar ( nr_titulo_p bigint, nm_usuario_p text, nr_seq_evento_p text, nr_seq_log_p INOUT text) AS $body$
DECLARE


ds_erro_w			varchar(4000);


BEGIN

begin

nr_seq_log_p := gravar_log_integracao(nm_usuario_p, nr_seq_evento_p, 'NR_TITULO=' || nr_titulo_p || ';', 'N', 'S', nr_seq_log_p);

exception when others then
	ds_erro_w	:= sqlerrm(SQLSTATE);
	/*ERRO AO GERAR O XML:
	#@ds_erro_w#@*/
	CALL wheb_mensagem_pck.exibir_mensagem_abort(265106,'ds_erro_w=' || ds_erro_w);
end;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_integracao_tit_pagar ( nr_titulo_p bigint, nm_usuario_p text, nr_seq_evento_p text, nr_seq_log_p INOUT text) FROM PUBLIC;

