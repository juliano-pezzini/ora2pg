-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE update_medico_laudos (cd_medico_resp_p bigint, nr_seq_laudos_p text) AS $body$
DECLARE

  item RECORD;

BEGIN
  for  item in (WITH RECURSIVE cte AS (
SELECT regexp_substr(nr_seq_laudos_p,'[^,]+', 1, level) nr_seq
                 (regexp_substr(nr_seq_laudos_p, '[^,]+', 1, level) IS NOT NULL AND (regexp_substr(nr_seq_laudos_p, '[^,]+', 1, level))::text <> '')  UNION ALL
SELECT regexp_substr(nr_seq_laudos_p,'[^,]+', 1, level) nr_seq
                 (regexp_substr(nr_seq_laudos_p, '[^,]+', 1, level) IS NOT NULL AND (regexp_substr(nr_seq_laudos_p, '[^,]+', 1, level))::text <> '') JOIN cte c ON ()

) SELECT * FROM cte;
)
  loop
      update laudo_paciente
      set cd_medico_resp = cd_medico_resp_p
      where nr_sequencia = item.nr_seq;
  end loop;

commit;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE update_medico_laudos (cd_medico_resp_p bigint, nr_seq_laudos_p text) FROM PUBLIC;

