-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE copia_localizacao_mat ( cd_material_p bigint, cd_material_novo_p bigint, nm_usuario_p text) AS $body$
DECLARE


cd_estabelecimento_w		smallint;
cd_local_estoque_w		smallint;
ds_localizacao_w			varchar(80);
nr_seq_local_w			bigint;
nr_seq_estrut_mat_w		bigint;
nr_sequencia_w			bigint;

C01 CURSOR FOR
SELECT	cd_estabelecimento,
	cd_local_estoque,
	ds_localizacao,
	nr_seq_local,
	nr_seq_estrut_mat
from	localizacao_estoque_local
where	cd_material = cd_material_p;


BEGIN

open C01;
loop
fetch C01 into
	cd_estabelecimento_w,
	cd_local_estoque_w,
	ds_localizacao_w,
	nr_seq_local_w,
	nr_seq_estrut_mat_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin

	select	nextval('localizacao_estoque_local_seq')
	into STRICT	nr_sequencia_w
	;

	insert into localizacao_estoque_local(
		nr_sequencia,
		dt_atualizacao,
		nm_usuario,
		cd_estabelecimento,
		cd_local_estoque,
		cd_material,
		ds_localizacao,
		nr_seq_local,
		nr_seq_estrut_mat)
	values (	nr_sequencia_w,
		clock_timestamp(),
		nm_usuario_p,
		cd_estabelecimento_w,
		cd_local_estoque_w,
		cd_material_novo_p,
		ds_localizacao_w,
		nr_seq_local_w,
		nr_seq_estrut_mat_w);
	end;
end loop;
close C01;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE copia_localizacao_mat ( cd_material_p bigint, cd_material_novo_p bigint, nm_usuario_p text) FROM PUBLIC;

