-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE atualiza_proc_regra_glosa ( nr_sequencia_p bigint, nr_seq_agenda_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint) AS $body$
DECLARE

				 
 
 
					 
cd_procedimento_w		bigint;
ie_origem_proced_w		bigint;
nr_seq_exame_w			bigint;
nr_seq_proc_interno_w		bigint;
nr_seq_proc_adic_w		bigint;
					
					 
C01 CURSOR FOR 
	SELECT	a.cd_procedimento, 
		a.ie_origem_proced, 
		a.nr_seq_exame, 
		a.nr_seq_proc_interno, 
		a.nr_sequencia 
	from	agenda_consulta_proc a 
	where	a.nr_seq_agenda = nr_sequencia_p;
					

BEGIN 
 
 
if (nr_sequencia_p IS NOT NULL AND nr_sequencia_p::text <> '')	then 
	open C01;
	loop 
	fetch C01 into	 
		cd_procedimento_w, 
		ie_origem_proced_w, 
		nr_seq_exame_w, 
		nr_seq_proc_interno_w, 
		nr_seq_proc_adic_w;
	EXIT WHEN NOT FOUND; /* apply on C01 */
		begin 
		CALL Agserv_cons_proc_regra_glosa(	nr_sequencia_p, 
						nr_seq_proc_adic_w, 
						cd_procedimento_w, 
						ie_origem_proced_w, 
						nr_seq_exame_w, 
						nr_seq_proc_interno_w, 
						nm_usuario_p, 
						cd_estabelecimento_p 
						);
		end;
	end loop;
	close C01;	
 
end if;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE atualiza_proc_regra_glosa ( nr_sequencia_p bigint, nr_seq_agenda_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint) FROM PUBLIC;

