-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE desvincular_paciente_grupo ( cd_pessoa_fisica_p text, nm_usuario_p text, nr_seq_grupo_p bigint default null) AS $body$
BEGIN

if (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') then
	update	pac_grupo_atend
	set	dt_saida = clock_timestamp(),
		nm_usuario = nm_usuario_p
	where	cd_pessoa_fisica = cd_pessoa_fisica_p
	and		nr_seq_grupo = coalesce(nr_seq_grupo_p,nr_seq_grupo)
	and	coalesce(nr_atendimento::text, '') = '';
	commit;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE desvincular_paciente_grupo ( cd_pessoa_fisica_p text, nm_usuario_p text, nr_seq_grupo_p bigint default null) FROM PUBLIC;

