-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE atualizar_ultima_medicacao_js ( dt_ultimo_dose_p timestamp, qt_ult_dose_p bigint, cd_unidade_med_p text, cd_procedimento_p bigint, nr_prescricao_p bigint) AS $body$
BEGIN

if (qt_ult_dose_p <> 0)then
	begin

	if (cd_unidade_med_p IS NOT NULL AND cd_unidade_med_p::text <> '')then
		begin

		update 	prescr_procedimento
		set 	dt_ult_dose_medic 		= dt_ultimo_dose_p,
			qt_ult_dose_medic 		= qt_ult_dose_p,
			cd_unidade_ult_dose_medic 	= cd_unidade_med_p
		where 	cd_procedimento 		= cd_procedimento_p
		and   	nr_prescricao  			= nr_prescricao_p;

		end;
	else
		begin

		update 	prescr_procedimento
		set 	dt_ult_dose_medic 		= dt_ultimo_dose_p,
			qt_ult_dose_medic 		= qt_ult_dose_p
		where 	cd_procedimento 		= cd_procedimento_p
		and   	nr_prescricao  			= nr_prescricao_p;

		end;
	end if;

	end;
else
	begin

	if (cd_unidade_med_p IS NOT NULL AND cd_unidade_med_p::text <> '')then
		begin

		update 	prescr_procedimento
		set 	dt_ult_dose_medic 		= dt_ultimo_dose_p,
			cd_unidade_ult_dose_medic 	= cd_unidade_med_p
		where 	cd_procedimento 		= cd_procedimento_p
		and   	nr_prescricao  			= nr_prescricao_p;


		end;
	else
		begin

		update 	prescr_procedimento
		set 	dt_ult_dose_medic 		= dt_ultimo_dose_p
		where 	cd_procedimento 		= cd_procedimento_p
		and   	nr_prescricao  			= nr_prescricao_p;

		end;
	end if;

	end;
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE atualizar_ultima_medicacao_js ( dt_ultimo_dose_p timestamp, qt_ult_dose_p bigint, cd_unidade_med_p text, cd_procedimento_p bigint, nr_prescricao_p bigint) FROM PUBLIC;

