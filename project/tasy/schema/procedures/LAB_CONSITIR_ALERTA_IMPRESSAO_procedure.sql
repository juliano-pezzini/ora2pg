-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE lab_consitir_alerta_impressao ( nr_prescricao_p bigint, nr_seq_prescr_p text, nm_usuario_p text, cd_estabelecimento_p bigint, cd_perfil_p bigint, ds_retorno_p INOUT text) AS $body$
DECLARE


qt_registro_w bigint;


BEGIN

ds_retorno_p	:= '';

if (nr_prescricao_p IS NOT NULL AND nr_prescricao_p::text <> '') and (nr_seq_prescr_p IS NOT NULL AND nr_seq_prescr_p::text <> '') then

	select	count(*)
	into STRICT	qt_registro_w
	from (SELECT	c.nr_sequencia
			from	lab_regra_imp_lab_externo a,
					prescr_medica b,
					prescr_procedimento c,
					exame_laboratorio d
			where	c.nr_prescricao = nr_prescricao_p
			and		c.nr_prescricao = b.nr_prescricao
			and		coalesce(a.cd_estabelecimento,coalesce(b.cd_estabelecimento,0)) = coalesce(b.cd_estabelecimento,0)
			and		c.nr_seq_exame = a.nr_seq_exame
			and		d.nr_seq_exame = c.nr_seq_exame
			and		(d.cd_cgc_externo IS NOT NULL AND d.cd_cgc_externo::text <> '')
			and		coalesce(a.ie_situacao,'A') = 'A') a
	where	obter_se_contido_char(a.nr_sequencia, coalesce(nr_seq_prescr_p,a.nr_sequencia)) = 'S'
	order by 1;

	if (qt_registro_w = 0) then
		ds_retorno_p	:=  '';
	else
		ds_retorno_p	:=  obter_texto_tasy(259881, wheb_usuario_pck.get_nr_seq_idioma);
	end if;

end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE lab_consitir_alerta_impressao ( nr_prescricao_p bigint, nr_seq_prescr_p text, nm_usuario_p text, cd_estabelecimento_p bigint, cd_perfil_p bigint, ds_retorno_p INOUT text) FROM PUBLIC;

