-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE trocar_sala_cirurgia_html5 (nr_cirurgia_p bigint, dt_entrada_unidade_p timestamp, dt_inicio_real_p timestamp, nr_seq_unidade_p bigint, nm_usuario_p text, cd_funcao_p bigint, cd_estabelecimento_p bigint) AS $body$
DECLARE

 
cd_setor_atendimento_w	unidade_atendimento.cd_setor_atendimento%type;
cd_unidade_basica_w	unidade_atendimento.cd_unidade_basica%type;
cd_unidade_compl_w	unidade_atendimento.cd_unidade_compl%type;
cd_tipo_acomodacao_w	unidade_atendimento.cd_tipo_acomodacao%type;


BEGIN 
 
select	max(cd_setor_atendimento), 
    max(cd_unidade_basica), 
    max(cd_unidade_compl), 
    max(cd_tipo_acomodacao) 
into STRICT	cd_setor_atendimento_w, 
    cd_unidade_basica_w, 
    cd_unidade_compl_w, 
    cd_tipo_acomodacao_w 
from	unidade_atendimento 
where	nr_seq_interno = nr_seq_unidade_p;
 
CALL trocar_sala_cirurgia(	nr_cirurgia_p, 
			dt_entrada_unidade_p, 
			dt_inicio_real_p, 
			cd_setor_atendimento_w, 
			cd_unidade_basica_w, 
			cd_unidade_compl_w, 
			cd_tipo_acomodacao_w, 
			nm_usuario_p, 
			cd_funcao_p, 
			cd_estabelecimento_p);
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE trocar_sala_cirurgia_html5 (nr_cirurgia_p bigint, dt_entrada_unidade_p timestamp, dt_inicio_real_p timestamp, nr_seq_unidade_p bigint, nm_usuario_p text, cd_funcao_p bigint, cd_estabelecimento_p bigint) FROM PUBLIC;

