-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consiste_lib_visit_setor_post (cd_pessoa_fisica_p text, cd_setor_atendimento_p bigint, nr_seq_liberacao_p bigint, nm_usuario_p text) AS $body$
DECLARE


ie_existe_lib_w	varchar(1);


BEGIN

ie_existe_lib_w := 1;

begin
	select	1
	into STRICT	ie_existe_lib_w
	from	ATEND_SETOR_LIB
	where	coalesce(cd_pessoa_fisica, 'X') = coalesce(cd_pessoa_fisica_p, 'X')
	and	coalesce(cd_setor_atendimento, 0) = coalesce(cd_setor_atendimento_p, 0)
	and	ie_situacao = 'A'
	and	nr_sequencia <> nr_seq_liberacao_p  LIMIT 1;
exception
	when	others then
		ie_existe_lib_w := 0;
end;

if (ie_existe_lib_w = 1) then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(272639);
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consiste_lib_visit_setor_post (cd_pessoa_fisica_p text, cd_setor_atendimento_p bigint, nr_seq_liberacao_p bigint, nm_usuario_p text) FROM PUBLIC;

