-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_imp_nota_debito_a560 ( ds_conteudo_p text, nm_usuario_p text, nr_seq_camara_contest_p bigint, nr_seq_referencia_p INOUT bigint, ie_versao_p INOUT text, cd_estabelecimento_p bigint) AS $body$
DECLARE


/*++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
OPS - Controle de Contestações
Finalidade: Importar o arquivo A560.
-------------------------------------------------------------------------------------------------------------------
Locais de chamada direta:
[  ]  Objetos do dicionário [X] Tasy (Delphi/Java) [  ] Portal [  ]  Relatórios [ ] Outros:
 ------------------------------------------------------------------------------------------------------------------
Pontos de atenção:

06 - 5.0a, 07 - 6.0, 08 - 7.0, 09 - 8.0
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++*/BEGIN
-- OBTER A VERSÃO DE TRANSAÇÃO
if (coalesce(ie_versao_p::text, '') = '') then
	select	substr(ds_conteudo_p,45,2)
	into STRICT	ie_versao_p
	;
end if;

if	((trim(both substr(ds_conteudo_p,9,2)) is not null) and ((substr(ds_conteudo_p,9,2) <> '56') and (substr(ds_conteudo_p,9,2) <> '99'))) then
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(276588);
end if;

-- VERSÃO PTU 5.0a
if (ie_versao_p in ('04','05','06')) then
	CALL pls_importar_arquivo_a560( ds_conteudo_p, nr_seq_camara_contest_p, nm_usuario_p, cd_estabelecimento_p );

-- VERSÃO PTU 6.0
elsif (ie_versao_p in ('07')) then
	CALL pls_importar_arquivo_a560_60( ds_conteudo_p, nr_seq_camara_contest_p, nm_usuario_p, cd_estabelecimento_p );

-- VERSÃO PTU 7.0
elsif (ie_versao_p in ('08')) then
	CALL pls_importar_arquivo_a560_70( ds_conteudo_p, nr_seq_camara_contest_p, nm_usuario_p, cd_estabelecimento_p );

-- VERSÃO PTU 8.0
elsif (ie_versao_p in ('09')) then
	CALL pls_importar_arquivo_a560_80( ds_conteudo_p, nr_seq_camara_contest_p, nm_usuario_p, cd_estabelecimento_p );
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_imp_nota_debito_a560 ( ds_conteudo_p text, nm_usuario_p text, nr_seq_camara_contest_p bigint, nr_seq_referencia_p INOUT bigint, ie_versao_p INOUT text, cd_estabelecimento_p bigint) FROM PUBLIC;

