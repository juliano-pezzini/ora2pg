-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE inserir_qua_doc_setor_adic ( nr_seq_doc_p bigint, cd_setor_atendimento_p bigint, nm_usuario_p text) AS $body$
DECLARE


qt_setor_w		bigint;


BEGIN

if (nr_seq_doc_p IS NOT NULL AND nr_seq_doc_p::text <> '') and (cd_setor_atendimento_p IS NOT NULL AND cd_setor_atendimento_p::text <> '') and (nm_usuario_p IS NOT NULL AND nm_usuario_p::text <> '') then
	begin
	select	count(1)
	into STRICT	qt_setor_w
	from	qua_doc_setor_adic
	where	nr_seq_doc = nr_seq_doc_p
	and	cd_setor_atendimento = cd_setor_atendimento_p;

	if (qt_setor_w = 0) then
		begin
		insert	into qua_doc_setor_adic(
				nr_sequencia,
				nr_seq_doc,
				dt_atualizacao,
				nm_usuario,
				cd_setor_atendimento,
				dt_atualizacao_nrec,
				nm_usuario_nrec)
			values (nextval('qua_doc_setor_adic_seq'),
				nr_seq_doc_p,
				clock_timestamp(),
				nm_usuario_p,
				cd_setor_atendimento_p,
				clock_timestamp(),
				nm_usuario_p);
		commit;
		end;
	end if;
	end;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE inserir_qua_doc_setor_adic ( nr_seq_doc_p bigint, cd_setor_atendimento_p bigint, nm_usuario_p text) FROM PUBLIC;

