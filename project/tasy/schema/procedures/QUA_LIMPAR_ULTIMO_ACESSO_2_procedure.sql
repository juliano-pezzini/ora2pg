-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE qua_limpar_ultimo_acesso_2 ( nr_seq_doc_p bigint, nm_usuario_p text) AS $body$
DECLARE


nr_seq_doc_log_w	bigint;


BEGIN

if ((nr_seq_doc_p IS NOT NULL AND nr_seq_doc_p::text <> '') or (nr_seq_doc_p <> 0)) then
	select	coalesce(max(nr_sequencia), 0)
	into STRICT	nr_seq_doc_log_w
	from	qua_doc_log_acesso
	where	nr_seq_doc	= nr_seq_doc_p;
end if;

if ((nr_seq_doc_log_w IS NOT NULL AND nr_seq_doc_log_w::text <> '') or (nr_seq_doc_log_w <> 0)) then
	update	qua_doc_log_acesso
	set	dt_leitura	 = NULL,
		dt_atualizacao	= clock_timestamp(),
		nm_usuario	= nm_usuario_p
	where	nr_sequencia	= nr_seq_doc_log_w;
end if;

CALL limpar_ultimo_acesso(nr_seq_doc_p, nm_usuario_p);

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE qua_limpar_ultimo_acesso_2 ( nr_seq_doc_p bigint, nm_usuario_p text) FROM PUBLIC;

