-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE atualiza_dados_nfse_integracao ( nr_nfe_imp_p text, cd_verificacao_nfse_p text, dt_emissao_nfe_p text, cd_serie_nf_p text, nr_nota_fiscal_p text, cd_cgc_emitente_p text, dt_emissao_rps_p text, nm_usuario_p text, cd_estabelecimento_p bigint, cd_integracao_p bigint) AS $body$
DECLARE


dt_emissao_nfe_aux_w	varchar(50);
dt_emissao_nfe_w	timestamp;
ie_situacao_w		varchar(1);
dt_emissao_rps_w	timestamp;
ie_notas_estornadas_w	varchar(1);


BEGIN

ie_notas_estornadas_w	:= Obter_Valor_Param_Usuario(9909,13,obter_perfil_ativo,nm_usuario_p,cd_estabelecimento_p);

if (dt_emissao_rps_p <> 'X') then
	begin
	dt_emissao_rps_w := replace(dt_emissao_rps_p, 'T', ' ');

	exception when others then
		begin
		dt_emissao_rps_w		:= to_date(replace(dt_emissao_rps_p, 'T', ' '), 'yyyy-mm-dd hh24:mi:ss');
		exception when others then
			begin
			dt_emissao_rps_w	:= to_date(replace(dt_emissao_rps_p, 'T', ' '), 'dd-mm-yyyy hh24:mi:ss');
			exception when others then
			--Não foi possível converter a data de emissão enviada no arquivo:
				CALL Wheb_mensagem_pck.exibir_mensagem_abort(186114,'DT_EMISSAO_NFE=' || dt_emissao_rps_p);
			end;
		end;
	end;
end if;

if (cd_integracao_p = 100714) then
	begin

	if (coalesce(ie_notas_estornadas_w,'N') = 'S') then
		begin

		select	ie_situacao
		into STRICT	ie_situacao_w
		from 	nota_fiscal
		where 	nr_nota_fiscal  = nr_nota_fiscal_p
		and 	ie_tipo_nota in ('SE','SD','SF','ST')
		and	((to_char(dt_emissao,'dd/mm/yyyy') 	= to_char(dt_emissao_rps_w,'dd/mm/yyyy')) or (dt_emissao_rps_p = 'X'))
		and	((cd_cgc_emitente	= cd_cgc_emitente_p) or (cd_cgc_emitente_p = 'X'))
		and	not ie_situacao in ('2');

		end;
	else
		begin

		select	ie_situacao
		into STRICT	ie_situacao_w
		from 	nota_fiscal
		where 	nr_nota_fiscal  = nr_nota_fiscal_p
		and 	ie_tipo_nota in ('SE','SD','SF','ST')
		and	((to_char(dt_emissao,'dd/mm/yyyy') 	= to_char(dt_emissao_rps_w,'dd/mm/yyyy')) or (dt_emissao_rps_p = 'X'))
		and	((cd_cgc_emitente	= cd_cgc_emitente_p) or (cd_cgc_emitente_p = 'X'))
		and	not ie_situacao in ('2','3');

		end;
	end if;

	end;
else
	begin

	if (coalesce(ie_notas_estornadas_w,'N') = 'S') then
		begin

		select	ie_situacao
		into STRICT	ie_situacao_w
		from 	nota_fiscal
		where 	nr_nota_fiscal  = nr_nota_fiscal_p
		and	cd_serie_nf	= cd_serie_nf_p
		and 	ie_tipo_nota in ('SE','SD','SF','ST')
		and	((to_char(dt_emissao,'dd/mm/yyyy') 	= to_char(dt_emissao_rps_w,'dd/mm/yyyy')) or (dt_emissao_rps_p = 'X'))
		and	((cd_cgc_emitente	= cd_cgc_emitente_p) or (cd_cgc_emitente_p = 'X'))
		and	not ie_situacao in ('2');

		end;
	else
		begin

		select	ie_situacao
		into STRICT	ie_situacao_w
		from 	nota_fiscal
		where 	nr_nota_fiscal  = nr_nota_fiscal_p
		and	cd_serie_nf	= cd_serie_nf_p
		and 	ie_tipo_nota in ('SE','SD','SF','ST')
		and	((to_char(dt_emissao,'dd/mm/yyyy') 	= to_char(dt_emissao_rps_w,'dd/mm/yyyy')) or (dt_emissao_rps_p = 'X'))
		and	((cd_cgc_emitente	= cd_cgc_emitente_p) or (cd_cgc_emitente_p = 'X'))
		and	not ie_situacao in ('2','3');

		end;
	end if;

	end;
end if;


if (dt_emissao_nfe_p IS NOT NULL AND dt_emissao_nfe_p::text <> '') then
	begin
	dt_emissao_nfe_aux_w := dt_emissao_nfe_p;
	dt_emissao_nfe_w     := replace(dt_emissao_nfe_aux_w, 'T', ' ');

	exception when others then
		begin

		if (length(trim(both dt_emissao_nfe_aux_w)) > 19) then
			begin
			dt_emissao_nfe_aux_w := substr(dt_emissao_nfe_aux_w,1,19);
			end;
		end if;

		dt_emissao_nfe_w		:= to_date(replace(dt_emissao_nfe_aux_w, 'T', ' '), 'yyyy-mm-dd hh24:mi:ss');
		exception when others then
			begin
			dt_emissao_nfe_w	:= to_date(replace(dt_emissao_nfe_aux_w, 'T', ' '), 'dd-mm-yyyy hh24:mi:ss');
			exception when others then
			--Não foi possível converter a data de emissão enviada no arquivo:
				CALL Wheb_mensagem_pck.exibir_mensagem_abort(186114,'DT_EMISSAO_NFE=' || dt_emissao_nfe_aux_w);
			end;
		end;


	end;
end if;


if (ie_situacao_w <> '8') then
	if (cd_integracao_p = 100714) then
		begin

		if (coalesce(ie_notas_estornadas_w,'N') = 'S') then
			begin

			update	nota_fiscal
			set 	nr_nfe_imp		=  nr_nfe_imp_p,
				cd_verificacao_nfse 	= cd_verificacao_nfse_p,
				dt_emissao_nfe		= dt_emissao_nfe_w
			where	nr_nota_fiscal		= nr_nota_fiscal_p
			and 	ie_tipo_nota in ('SE','SD','SF','ST')
			and	((cd_cgc_emitente	= cd_cgc_emitente_p) or (cd_cgc_emitente_p = 'X'))
			and	ie_situacao in ('1','3')
			and	((to_char(dt_emissao,'dd/mm/yyyy') 	= to_char(dt_emissao_rps_w,'dd/mm/yyyy')) or (dt_emissao_rps_p = 'X'));

			end;
		else
			begin

			update	nota_fiscal
			set 	nr_nfe_imp		=  nr_nfe_imp_p,
				cd_verificacao_nfse 	= cd_verificacao_nfse_p,
				dt_emissao_nfe		= dt_emissao_nfe_w
			where	nr_nota_fiscal		= nr_nota_fiscal_p
			and 	ie_tipo_nota in ('SE','SD','SF','ST')
			and	((cd_cgc_emitente	= cd_cgc_emitente_p) or (cd_cgc_emitente_p = 'X'))
			and	ie_situacao in ('1')
			and	((to_char(dt_emissao,'dd/mm/yyyy') 	= to_char(dt_emissao_rps_w,'dd/mm/yyyy')) or (dt_emissao_rps_p = 'X'));

			end;

		end if;

		end;
	else
		begin

		if (coalesce(ie_notas_estornadas_w,'N') = 'S') then
			begin

			update	nota_fiscal
			set 	nr_nfe_imp		=  nr_nfe_imp_p,
				cd_verificacao_nfse 	= cd_verificacao_nfse_p,
				dt_emissao_nfe		= dt_emissao_nfe_w
			where	nr_nota_fiscal		= nr_nota_fiscal_p
			and	cd_serie_nf		= cd_serie_nf_p
			and 	ie_tipo_nota in ('SE','SD','SF','ST')
			and	((cd_cgc_emitente	= cd_cgc_emitente_p) or (cd_cgc_emitente_p = 'X'))
			and	ie_situacao in ('1','3')
			and	((to_char(dt_emissao,'dd/mm/yyyy') 	= to_char(dt_emissao_rps_w,'dd/mm/yyyy')) or (dt_emissao_rps_p = 'X'));

			end;
		else
			begin

			update	nota_fiscal
			set 	nr_nfe_imp		=  nr_nfe_imp_p,
				cd_verificacao_nfse 	= cd_verificacao_nfse_p,
				dt_emissao_nfe		= dt_emissao_nfe_w
			where	nr_nota_fiscal		= nr_nota_fiscal_p
			and	cd_serie_nf		= cd_serie_nf_p
			and 	ie_tipo_nota in ('SE','SD','SF','ST')
			and	((cd_cgc_emitente	= cd_cgc_emitente_p) or (cd_cgc_emitente_p = 'X'))
			and	ie_situacao in ('1')
			and	((to_char(dt_emissao,'dd/mm/yyyy') 	= to_char(dt_emissao_rps_w,'dd/mm/yyyy')) or (dt_emissao_rps_p = 'X'));

			end;

		end if;

		end;
	end if;
else
	if (cd_integracao_p = 100714) then
		begin

		update	nota_fiscal
		set 	nr_nfe_imp		= nr_nfe_imp_p,
			cd_verificacao_nfse 	= cd_verificacao_nfse_p,
			dt_emissao_nfe		= dt_emissao_nfe_w,
			dt_atualizacao_estoque	= clock_timestamp(),
			ie_situacao		= '1'
		where	nr_nota_fiscal		= nr_nota_fiscal_p
		and 	ie_tipo_nota in ('SE','SD','SF','ST')
		and	((cd_cgc_emitente	= cd_cgc_emitente_p) or (cd_cgc_emitente_p = 'X'))
		and	((to_char(dt_emissao,'dd/mm/yyyy') 	= to_char(dt_emissao_rps_w,'dd/mm/yyyy')) or (dt_emissao_rps_p = 'X'));

		end;
	else
		begin

		update	nota_fiscal
		set 	nr_nfe_imp		= nr_nfe_imp_p,
			cd_verificacao_nfse 	= cd_verificacao_nfse_p,
			dt_emissao_nfe		= dt_emissao_nfe_w,
			dt_atualizacao_estoque	= clock_timestamp(),
			ie_situacao		= '1'
		where	nr_nota_fiscal		= nr_nota_fiscal_p
		and	cd_serie_nf		= cd_serie_nf_p
		and 	ie_tipo_nota in ('SE','SD','SF','ST')
		and	((cd_cgc_emitente	= cd_cgc_emitente_p) or (cd_cgc_emitente_p = 'X'))
		and	((to_char(dt_emissao,'dd/mm/yyyy') 	= to_char(dt_emissao_rps_w,'dd/mm/yyyy')) or (dt_emissao_rps_p = 'X'));

		end;
	end if;

end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE atualiza_dados_nfse_integracao ( nr_nfe_imp_p text, cd_verificacao_nfse_p text, dt_emissao_nfe_p text, cd_serie_nf_p text, nr_nota_fiscal_p text, cd_cgc_emitente_p text, dt_emissao_rps_p text, nm_usuario_p text, cd_estabelecimento_p bigint, cd_integracao_p bigint) FROM PUBLIC;

