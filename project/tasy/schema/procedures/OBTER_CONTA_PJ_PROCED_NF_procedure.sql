-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE obter_conta_pj_proced_nf ( cd_estabelecimento_p bigint, cd_cgc_p text, cd_procedimento_p bigint, ie_origem_proced_p bigint, cd_operacao_nf_p bigint, cd_conta_contabil_p INOUT text, nr_seq_nota_fiscal_p bigint) AS $body$
DECLARE


cd_empresa_w		bigint;
cd_conta_contabil_w	varchar(20);
cd_area_w		integer;
cd_grupo_w		bigint;
cd_especialidade_w	bigint;
cd_procedimento_w	bigint;
ie_origem_proced_w	bigint;
dt_emissao_w		timestamp;

c01 CURSOR FOR
SELECT	a.cd_conta_contabil
from	pessoa_jur_conta_nf a
where	cd_empresa	= cd_empresa_w
and	a.cd_cgc	= cd_cgc_p
and	coalesce(cd_area_procedimento,cd_area_w)		= cd_area_w
and	coalesce(cd_especialidade,cd_especialidade_w)	= cd_especialidade_w
and	coalesce(cd_grupo_proc,cd_grupo_w)			= cd_grupo_w
and	coalesce(cd_procedimento, cd_procedimento_p)		= cd_procedimento_p
and	coalesce(ie_origem_proced, ie_origem_proced_p)	= ie_origem_proced_p
and	coalesce(cd_operacao_nf, cd_operacao_nf_p)		= cd_operacao_nf_p
and	coalesce(cd_material::text, '') = ''
and	substr(obter_se_periodo_vigente(a.dt_inicio_vigencia, a.dt_fim_vigencia, dt_emissao_w),1,1) = 'S'
order by 	coalesce(cd_operacao_nf,0),
	 coalesce(cd_procedimento,0),
	 coalesce(cd_grupo_proc,0),
	 coalesce(cd_especialidade,0),
	 coalesce(cd_area_procedimento,0);


BEGIN

cd_empresa_w	:= obter_empresa_estab(cd_estabelecimento_p);

select	max(cd_grupo_proc),
	max(cd_especialidade),
	max(cd_area_procedimento)
into STRICT	cd_grupo_w,
	cd_especialidade_w,
	cd_area_w
from	estrutura_procedimento_v
where	cd_procedimento 	= cd_procedimento_p
and	ie_origem_proced 	= ie_origem_proced_p;

select	max(dt_emissao)
into STRICT	dt_emissao_w
from	nota_fiscal
where	nr_sequencia = nr_seq_nota_fiscal_p;

open C01;
loop
fetch C01 into
	cd_conta_contabil_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin
	cd_conta_contabil_p	:= cd_conta_contabil_w;
	end;
end loop;
close C01;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE obter_conta_pj_proced_nf ( cd_estabelecimento_p bigint, cd_cgc_p text, cd_procedimento_p bigint, ie_origem_proced_p bigint, cd_operacao_nf_p bigint, cd_conta_contabil_p INOUT text, nr_seq_nota_fiscal_p bigint) FROM PUBLIC;

