-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_alta_apos_atend_agecons (nr_atendimento_p bigint, cd_agenda_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint) AS $body$
DECLARE

 
ie_alta_atendimento_w	varchar(1);					
cd_motivo_alta_w	smallint;
ds_erro_w		varchar(4000);
					

BEGIN 
 
if (nr_atendimento_p IS NOT NULL AND nr_atendimento_p::text <> '') and (cd_agenda_p IS NOT NULL AND cd_agenda_p::text <> '') then 
	select	coalesce(max(ie_alta_atendimento),'N'), 
		max(cd_motivo_alta) 
	into STRICT	ie_alta_atendimento_w, 
		cd_motivo_alta_w 
	from	agenda 
	where	cd_agenda = cd_agenda_p;
	 
	if (ie_alta_atendimento_w = 'S') and (cd_motivo_alta_w IS NOT NULL AND cd_motivo_alta_w::text <> '') then 
		ds_erro_w := gerar_estornar_alta(nr_atendimento_p, 'A', null, coalesce(cd_motivo_alta_w,1), clock_timestamp(), nm_usuario_p, ds_erro_w, null, null, null);
	end if;
	 
end if;
 
commit;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_alta_apos_atend_agecons (nr_atendimento_p bigint, cd_agenda_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint) FROM PUBLIC;

