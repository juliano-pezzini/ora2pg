-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE san_inserir_reagente_realizado (nr_seq_lote_exame_p bigint, nr_Seq_exame_p bigint, nm_usuario_p text) AS $body$
DECLARE



cd_material_w		bigint;
nr_seq_lote_fornec_w	bigint;

C01 CURSOR FOR
	SELECT	cd_material,
		nr_seq_lote_fornec
	from	san_reagentes_exame
	where	nr_seq_exame = nr_seq_exame_p
	and	ie_situacao = 'A'
	order by cd_material;

BEGIN
if (nr_seq_lote_exame_p IS NOT NULL AND nr_seq_lote_exame_p::text <> '') and (nr_seq_exame_p IS NOT NULL AND nr_seq_exame_p::text <> '') then

	open C01;
	loop
	fetch C01 into
		cd_material_w,
		nr_seq_lote_fornec_w;
	EXIT WHEN NOT FOUND; /* apply on C01 */
		begin
		CALL Inserir_san_exame_reagente(cd_material_w,nr_seq_lote_fornec_w,nr_seq_lote_exame_p,nr_Seq_exame_p,nm_usuario_p);
		end;
	end loop;
	close C01;

end if;

/*commit; tirado o commit pois esta procedure é chamda na gerar_exame_realizado, que é chamada na trigger San_Producao_Update*/

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE san_inserir_reagente_realizado (nr_seq_lote_exame_p bigint, nr_Seq_exame_p bigint, nm_usuario_p text) FROM PUBLIC;

