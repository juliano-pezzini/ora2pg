-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_gerencia_criacao_type (nm_objeto_p text) AS $body$
DECLARE


qt_registro_w	integer;

BEGIN

if (nm_objeto_p IS NOT NULL AND nm_objeto_p::text <> '') then
	select	count(1)
	into STRICT	qt_registro_w
	from	user_types a
	where	type_name = upper(nm_objeto_p);

	if (qt_registro_w > 0) then
		CALL exec_sql_dinamico('versao', 'drop type ' || upper(nm_objeto_p));
	end if;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_gerencia_criacao_type (nm_objeto_p text) FROM PUBLIC;

