-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE linkeddata_diagnostico_medico ( nr_atendimento_p bigint, ie_tipo_diagnostico_p bigint, dt_diagnostico_p timestamp, nm_usuario_p text, cd_medico_p text, ie_tipo_atendimento_p bigint ) AS $body$
DECLARE


nm_usuario_w                    diagnostico_medico.nm_usuario%type;
flag_w                          varchar(1);

BEGIN

  if (coalesce(nm_usuario_p::text, '') = '' or nm_usuario_p = '') then
    select obter_usuario_ativo
    into STRICT nm_usuario_w 
;
  else
    nm_usuario_w := nm_usuario_p;
  end if;

  select CASE WHEN count(1)=0 THEN  'S'  ELSE 'N' END 
  into STRICT flag_w
  from diagnostico_medico
  where nr_atendimento  = nr_atendimento_p and dt_diagnostico = dt_diagnostico_p;

  if (flag_w = 'S') then
    insert into diagnostico_medico(
          nr_atendimento, 
          dt_diagnostico, 
          ie_tipo_diagnostico, 
          cd_medico, 
          dt_atualizacao, 
          nm_usuario, 
          ie_tipo_atendimento
    ) values (
          nr_atendimento_p, 
          dt_diagnostico_p, 
          ie_tipo_diagnostico_p, 
          cd_medico_p, 
          clock_timestamp(), 
          nm_usuario_w, 
          ie_tipo_atendimento_p
    );
 end if;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE linkeddata_diagnostico_medico ( nr_atendimento_p bigint, ie_tipo_diagnostico_p bigint, dt_diagnostico_p timestamp, nm_usuario_p text, cd_medico_p text, ie_tipo_atendimento_p bigint ) FROM PUBLIC;

