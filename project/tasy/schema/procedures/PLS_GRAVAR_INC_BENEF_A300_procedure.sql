-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_gravar_inc_benef_a300 ( nr_seq_benef_p bigint, cd_inconsistencia_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) AS $body$
DECLARE


nr_seq_inconsistencia_w	ptu_inconsistencia_a300.nr_sequencia%type;


BEGIN

select	max(nr_sequencia)
into STRICT	nr_seq_inconsistencia_w
from	ptu_inconsistencia_a300
where	cd_inconsistencia = cd_inconsistencia_p
and	ie_situacao = 'A';

if (nr_seq_inconsistencia_w IS NOT NULL AND nr_seq_inconsistencia_w::text <> '') then
	insert	into	ptu_intercambio_consist(	nr_sequencia, nr_seq_mov_prod_benef, nr_seq_inconsistencia_a300,
			dt_atualizacao_nrec, dt_atualizacao, nm_usuario_nrec, nm_usuario,
			cd_estabelecimento)
		values (	nextval('ptu_intercambio_consist_seq'), nr_seq_benef_p, nr_seq_inconsistencia_w,
			clock_timestamp(), clock_timestamp(), nm_usuario_p, nm_usuario_p,
			cd_estabelecimento_p);
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_gravar_inc_benef_a300 ( nr_seq_benef_p bigint, cd_inconsistencia_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) FROM PUBLIC;

