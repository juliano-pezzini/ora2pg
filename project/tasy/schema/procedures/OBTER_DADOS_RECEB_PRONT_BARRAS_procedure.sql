-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE obter_dados_receb_pront_barras ( nr_sequencia_p bigint, ie_cont_trans_solic_p text, nm_usuario_p text, cd_setor_destino_p INOUT bigint) AS $body$
DECLARE


nr_sequencia_w 	bigint;
cd_setor_destino_w	integer;



BEGIN

select 	coalesce(max(a.nr_sequencia),0)
into STRICT	nr_sequencia_w
from 	transf_prontuario a,
	transf_prontuario_envelope b
where 	a.nr_sequencia = b.nr_seq_transf
and 	b.nr_seq_prontuario = nr_sequencia_p
and 	coalesce(a.dt_recebimento::text, '') = '';

if (ie_cont_trans_solic_p = 'S'
	and nr_sequencia_w = 0 ) then
	--(-20011, obter_texto_tasy(67885, wheb_usuario_pck.get_nr_seq_idioma)||'#@#@');
	CALL Wheb_mensagem_pck.exibir_mensagem_abort(67885);
end if;

select 	max(cd_setor_destino)
into STRICT	cd_setor_destino_w
from 	transf_prontuario
where  	nr_sequencia = nr_sequencia_w;

cd_setor_destino_p := cd_setor_destino_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE obter_dados_receb_pront_barras ( nr_sequencia_p bigint, ie_cont_trans_solic_p text, nm_usuario_p text, cd_setor_destino_p INOUT bigint) FROM PUBLIC;

