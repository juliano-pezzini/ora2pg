-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE tuss_carga_servico_item ( nr_seq_carga_tuss_p bigint, cd_procedimento_p bigint, ds_procedimento_p text, dt_inicio_vigencia_p timestamp, dt_fim_vigencia_p timestamp, dt_implantacao_p timestamp, nm_usuario_p text) AS $body$
DECLARE


ds_erro_w	varchar(4000);


BEGIN

begin
insert into tuss_servico_item(nr_sequencia,
	nr_seq_carga_tuss,
	dt_atualizacao,
	nm_usuario,
	dt_atualizacao_nrec,
	nm_usuario_nrec,
	cd_procedimento,
	ds_procedimento,
	ds_log,
	dt_inicio_vigencia,
	dt_fim_vigencia,
	dt_implantacao)
values (nextval('tuss_servico_item_seq'),
	nr_seq_carga_tuss_p,
	clock_timestamp(),
	substr(nm_usuario_p,1,15),
	clock_timestamp(),
	substr(nm_usuario_p,1,15),
	cd_procedimento_p,
	substr(ds_procedimento_p,1,240),
	'',
	dt_inicio_vigencia_p,
	dt_fim_vigencia_p,
	dt_implantacao_p);
exception
	when others then
	ds_erro_w:= substr(sqlerrm,1,4000);
	insert into tuss_servico_log(nr_sequencia,dt_atualizacao,nm_usuario,dt_atualizacao_nrec,nm_usuario_nrec,ds_erro,cd_item,ds_item,ie_terminologia,nr_seq_carga_tuss)
	values (nextval('tuss_servico_log_seq'),clock_timestamp(),nm_usuario_p,clock_timestamp(),nm_usuario_p,ds_erro_w,cd_procedimento_p,substr(ds_procedimento_p,1,240),'',nr_seq_carga_tuss_p);
	end;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE tuss_carga_servico_item ( nr_seq_carga_tuss_p bigint, cd_procedimento_p bigint, ds_procedimento_p text, dt_inicio_vigencia_p timestamp, dt_fim_vigencia_p timestamp, dt_implantacao_p timestamp, nm_usuario_p text) FROM PUBLIC;

