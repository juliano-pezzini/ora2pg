-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE obter_texto_padrao ( cd_estabelecimento_p bigint, nr_seq_item_p bigint, cd_especialidade_p bigint, ie_evolucao_p text, nm_usuario_p text, nr_seq_texto_p INOUT bigint) AS $body$
DECLARE


nr_sequencia_w	bigint;


BEGIN

select	coalesce(max(nr_sequencia),0)
into STRICT	nr_sequencia_w
from 	texto_padrao
where (to_char(cd_estabelecimento) = to_char(cd_estabelecimento_p) or coalesce(cd_estabelecimento::text, '') = '')
and 	((ie_evolucao_clinica = ie_evolucao_p)or((coalesce(ie_evolucao_clinica::text, '') = '')and (nr_seq_item_p = 83)))
and     	((cd_especialidade = cd_especialidade_p) or (coalesce(cd_especialidade::text, '') = '') or (cd_especialidade_p = 0))
and	((coalesce(cd_setor_atendimento::text, '') = '') or (obter_se_setor_usuario(cd_setor_atendimento,nm_usuario_p) = 'S'))
and	coalesce(ie_opcao_padrao,'S')	= 'S'
and	nr_seq_item_pront = nr_seq_item_p;

nr_seq_texto_p	:= null;
if (nr_sequencia_w > 0) then
	nr_seq_texto_p	:= nr_sequencia_w;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE obter_texto_padrao ( cd_estabelecimento_p bigint, nr_seq_item_p bigint, cd_especialidade_p bigint, ie_evolucao_p text, nm_usuario_p text, nr_seq_texto_p INOUT bigint) FROM PUBLIC;

