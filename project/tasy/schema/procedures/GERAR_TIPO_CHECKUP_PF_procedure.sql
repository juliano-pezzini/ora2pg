-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_tipo_checkup_pf ( ds_seq_tipo_checkup_p text, nr_seq_pessoa_checkup_p bigint, nm_usuario_p text) AS $body$
DECLARE

nr_seq_tipo_checkup_w	bigint;
ds_seq_tipo_checkup_w	varchar(1000);
tam_lista_w				bigint;
ie_pos_virgula_w		bigint;
contrato_tipo_checkup_pf_w	bigint;


BEGIN

if (ds_seq_tipo_checkup_p IS NOT NULL AND ds_seq_tipo_checkup_p::text <> '') and (nr_seq_pessoa_checkup_p IS NOT NULL AND nr_seq_pessoa_checkup_p::text <> '') then
	begin
	ds_seq_tipo_checkup_w := ds_seq_tipo_checkup_p;
	while (ds_seq_tipo_checkup_w IS NOT NULL AND ds_seq_tipo_checkup_w::text <> '') loop
		begin
		tam_lista_w := length(ds_seq_tipo_checkup_w);
		ie_pos_virgula_w := position(',' in ds_seq_tipo_checkup_w);
		if (ie_pos_virgula_w <> 0) then
			nr_seq_tipo_checkup_w := (substr(ds_seq_tipo_checkup_w, 1, (ie_pos_virgula_w - 1)))::numeric;
			ds_seq_tipo_checkup_w	:= substr(ds_seq_tipo_checkup_w, (ie_pos_virgula_w + 1), tam_lista_w);
		end if;

		if (nr_seq_tipo_checkup_w > 0) then
			begin
			select	nextval('contrato_tipo_checkup_pf_seq')
			into STRICT	contrato_tipo_checkup_pf_w
			;

			insert into contrato_tipo_checkup_pf(
				nr_sequencia,
				nr_seq_tipo_checkup,
				nr_seq_pessoa_checkup,
				dt_atualizacao,
				nm_usuario,
				dt_atualizacao_nrec,
				nm_usuario_nrec)
			values (
				contrato_tipo_checkup_pf_w,
				nr_seq_tipo_checkup_w,
				nr_seq_pessoa_checkup_p,
				clock_timestamp(),
				nm_usuario_p,
				clock_timestamp(),
				nm_usuario_p);
			end;
		end if;
		end;
	end loop;
	end;
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_tipo_checkup_pf ( ds_seq_tipo_checkup_p text, nr_seq_pessoa_checkup_p bigint, nm_usuario_p text) FROM PUBLIC;

