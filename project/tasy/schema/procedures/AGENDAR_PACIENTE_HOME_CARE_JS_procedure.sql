-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE agendar_paciente_home_care_js ( cd_pessoa_fisica_p text, dt_agenda_p timestamp, hr_agenda_p text, nm_usuario_p text) AS $body$
DECLARE


qt_agenda_selec_w integer;
ds_retorno_w	  varchar(1);

BEGIN

--Comentado pois não estava sendo utilizado em lugar nenhum e a passagem de parâmetros está incorreta
/*select	nvl(count(*), 0)
into	qt_agenda_selec_w
from	agenda_home_care
where	ie_controle = 'S';
if	(qt_agenda_selec_w > 0) then
	begin
	if	(cd_pessoa_fisica_p is not null) and
		(dt_agenda_p is not null) and
		(hr_agenda_p is not null) and
		(nm_usuario_p is not null) then
		begin

		--agendar_paciente_home_care(cd_pessoa_fisica_p, dt_agenda_p, hr_agenda_p, null, nm_usuario_p,null,null,null,null,null,'S',ds_retorno_w,null);
		end;
	end if;
	end;
else
	begin
	--(-20011, substr(obter_texto_tasy(53032, wheb_usuario_pck.get_nr_seq_idioma),1,255) || '#@#@');
	Wheb_mensagem_pck.exibir_mensagem_abort(53032);
	end;
end if;*/
commit;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE agendar_paciente_home_care_js ( cd_pessoa_fisica_p text, dt_agenda_p timestamp, hr_agenda_p text, nm_usuario_p text) FROM PUBLIC;

