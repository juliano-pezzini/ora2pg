-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_transferencia_area_alta ( nr_atendimento_p bigint DEFAULT NULL, cd_perfil_p bigint DEFAULT NULL, nm_usuario_p text DEFAULT NULL, cd_estabelecimento_p bigint  DEFAULT NULL) AS $body$
DECLARE


    ds_out_p                 varchar(1000);
    ie_out_warning_p         varchar(1000);
    nr_seq_interno_de_p      bigint;
    nr_seq_interno_para_p    bigint;
    nr_seq_gestao_vaga_p     bigint := NULL;
    cd_setor_atendimento_w   bigint;

BEGIN
    SELECT MAX(cd_setor_atendimento)
      INTO STRICT cd_setor_atendimento_w
      FROM setor_atendimento
     WHERE ie_area_de_alta = 'S';

    SELECT MAX(nr_seq_interno)
      INTO STRICT nr_seq_interno_de_p
      FROM unidade_atendimento
     WHERE nr_atendimento = nr_atendimento_p;

    SELECT MAX(nr_seq_interno)
      INTO STRICT nr_seq_interno_para_p
      FROM unidade_atendimento
     WHERE cd_setor_atendimento = cd_setor_atendimento_w;

    SELECT MAX(nr_sequencia)
      INTO STRICT nr_seq_gestao_vaga_p
      FROM gestao_vaga x
     WHERE x.nr_atendimento = nr_atendimento_p;

    SELECT * FROM gerar_transf_mapa(cd_perfil_p, nm_usuario_p, cd_estabelecimento_p, nr_seq_interno_de_p, nr_seq_interno_para_p, ds_out_p, nr_atendimento_p, 'N', 'N', NULL, nr_seq_gestao_vaga_p, ie_out_warning_p) INTO STRICT ds_out_p, ie_out_warning_p;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_transferencia_area_alta ( nr_atendimento_p bigint DEFAULT NULL, cd_perfil_p bigint DEFAULT NULL, nm_usuario_p text DEFAULT NULL, cd_estabelecimento_p bigint  DEFAULT NULL) FROM PUBLIC;

