-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE inserir_paciente_fila_espera (nr_seq_agenda_p bigint, dt_inclusao_p timestamp, dt_prevista_internacao_p timestamp, nm_usuario_p text) AS $body$
DECLARE


nr_seq_espera_w		bigint;
cd_pessoa_fisica_w	varchar(10);
cd_convenio_w		integer;
cd_medico_w		varchar(10);
cd_procedimento_w	bigint;
ie_origem_proced_w	bigint;



BEGIN

select	cd_pessoa_fisica,
	cd_convenio,
	cd_medico,
	cd_procedimento,
	ie_origem_proced
into STRICT	cd_pessoa_fisica_w,
	cd_convenio_w,
	cd_medico_w,
	cd_procedimento_w,
	ie_origem_proced_w
from	agenda_paciente
where	nr_sequencia	= nr_seq_agenda_p;


select	nextval('paciente_espera_seq')
into STRICT	nr_seq_espera_w
;

insert	into paciente_espera(nr_sequencia,
	cd_pessoa_fisica,
	nr_prioridade,
	dt_inclusao,
	dt_atualizacao,
	nm_usuario,
	cd_convenio,
	cd_setor_atendimento,
	cd_unidade_basica,
	cd_unidade_compl,
	ie_clinica,
	cd_medico,
	ie_situacao,
	cd_procedimento,
	ie_origem_proced,
	dt_prevista_internacao,
	cd_tipo_acomodacao,
	ds_observacao)
values (nr_seq_espera_w,
	cd_pessoa_fisica_w,
	1, dt_inclusao_p,
	clock_timestamp(),
	nm_usuario_p,
	cd_convenio_w,
	null, null,
	null, null,
	cd_medico_w,
	'A', cd_procedimento_w,
	ie_origem_proced_w,
	dt_prevista_internacao_p,
	null, null);

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE inserir_paciente_fila_espera (nr_seq_agenda_p bigint, dt_inclusao_p timestamp, dt_prevista_internacao_p timestamp, nm_usuario_p text) FROM PUBLIC;

