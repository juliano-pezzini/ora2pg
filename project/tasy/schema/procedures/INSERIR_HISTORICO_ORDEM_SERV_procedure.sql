-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE inserir_historico_ordem_serv (nr_seq_ordem_serv_p bigint, dt_historico_p timestamp, ds_relat_tecnico_p text, cd_estabelecimento_p text) AS $body$
DECLARE

nr_seq_hist_w	bigint;

BEGIN
if (nr_seq_ordem_serv_p IS NOT NULL AND nr_seq_ordem_serv_p::text <> '') then 
	select 	nextval('man_ordem_serv_tecnico_seq') 
	into STRICT	nr_seq_hist_w 
	;
	if (nr_seq_hist_w IS NOT NULL AND nr_seq_hist_w::text <> '')then 
		insert 
		into 	man_ordem_serv_tecnico(nr_sequencia, 
							nr_seq_ordem_serv, 
							dt_atualizacao, 
							dt_historico, 
							dt_liberacao, 
							nm_usuario, 
							nr_seq_tipo, 
							ie_origem, 
							ds_relat_tecnico) 
					values (nr_seq_hist_w, 
							nr_seq_ordem_serv_p, 
							clock_timestamp(), 
							dt_historico_p, 
							dt_historico_p, 
							'WebService', 
							obter_parametro_usuario_js(299, 200), 
							'E', 
							ds_relat_tecnico_p);
		if (obter_parametro_usuario_js(299, 142) = 'S') then 
			CALL man_comunicar_historico(nr_seq_hist_w, 'E', 'WebService', 'N', cd_estabelecimento_p);
		end if;
		commit;
	end if;
end if;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE inserir_historico_ordem_serv (nr_seq_ordem_serv_p bigint, dt_historico_p timestamp, ds_relat_tecnico_p text, cd_estabelecimento_p text) FROM PUBLIC;

