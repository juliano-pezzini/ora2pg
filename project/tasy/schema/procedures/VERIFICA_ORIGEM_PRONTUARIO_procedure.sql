-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE verifica_origem_prontuario ( cd_pessoa_fisica_p text, ds_retorno_p INOUT text) AS $body$
DECLARE


ds_setor_w	varchar(100);



BEGIN

begin
select	substr(obter_nome_setor(max(cd_setor_atendimento)),1,100)
into STRICT	ds_setor_w
from	pf_setor_prontuario
where	cd_pessoa_fisica = cd_pessoa_fisica_p
and	coalesce(dt_final_vinculo::text, '') = '';
exception
	when others then
	ds_setor_w	:= null;
end;

if (ds_setor_w IS NOT NULL AND ds_setor_w::text <> '') then
	ds_retorno_p	:= Wheb_mensagem_pck.get_texto(305970, 'DS_SETOR='||ds_setor_w); -- Paciente do '||ds_setor_w||' favor encaminhar à triagem.
end if;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE verifica_origem_prontuario ( cd_pessoa_fisica_p text, ds_retorno_p INOUT text) FROM PUBLIC;

