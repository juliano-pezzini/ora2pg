-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE baca_medico_convenio_uk () AS $body$
DECLARE


qt_col_w	bigint;
nr_sequencia_W	bigint;
ie_w		varchar(1);
qt_existe_w	bigint;


BEGIN

select 	count(*)
into STRICT	qt_col_w
from 	user_cons_columns
where 	constraint_name = 'MEDCONV_UK'
and	column_name	= 'IE_CARATER_INTER_SUS';

if (qt_col_w	= 0) then
	begin

	select	count(*)
	into STRICT	qt_existe_w
	from	user_constraints
	where	CONSTRAINT_NAME = 'MEDCONV_UK';

	if (qt_existe_w > 0) then
		begin
		CALL Exec_Sql_Dinamico('MEDCONV_UK',   ' alter table MEDICO_CONVENIO drop constraint MEDCONV_UK ');
		exception
			when others then
			ie_w:= 'S';
		end;
	end if;

	select	count(*)
	into STRICT	qt_existe_w
	from	user_indexes
	where	INDEX_NAME = 'MEDCONV_UK';

	if (qt_existe_w > 0) then

		begin
		CALL Exec_Sql_Dinamico('MEDCONV_UK',   ' drop index MEDCONV_UK ');
		exception
			when others then
			ie_w:= 'S';
		end;

	end if;

	select	count(*)
	into STRICT	qt_existe_w
	from	user_constraints
	where	CONSTRAINT_NAME = 'MEDCONV_UK';


	if (qt_existe_w = 0) THEN
		begin
		CALL Exec_sql_Dinamico('Tasy','ALTER TABLE MEDICO_CONVENIO ADD (  	'||
		      			' CONSTRAINT MEDCONV_UK Unique  (	'||
					'               CD_PESSOA_FISICA,	'||
					'               CD_CONVENIO,		'||
					'               CD_ESTABELECIMENTO,	'||
					'               CD_PRESTADOR,		'||
					'               CD_ESPECIALIDADE,	'||
					'               CD_CATEGORIA,		'||
					'               CD_SETOR_ATENDIMENTO,	'||
					'               CD_PLANO_CONVENIO,	'||
					'		IE_TIPO_ATENDIMENTO,	'||
					'		DT_INICIO_VIGENCIA,	'||
					'		IE_FUNCAO_MEDICO,	'||
					'		IE_CARATER_INTER_SUS))	');
		exception
			when others then
			ie_w:= 'S';
		end;
	END IF;

	end;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE baca_medico_convenio_uk () FROM PUBLIC;

