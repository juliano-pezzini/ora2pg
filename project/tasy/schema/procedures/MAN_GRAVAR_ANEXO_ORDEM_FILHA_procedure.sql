-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE man_gravar_anexo_ordem_filha ( nr_seq_filha_p bigint, ds_arquivo_p text, nm_usuario_p text ) AS $body$
DECLARE


nr_seq_filha_w	man_ordem_serv_filha.nr_sequencia%type;


BEGIN

select	max(nr_sequencia)
into STRICT	nr_seq_filha_w
from	man_ordem_serv_filha
where	nr_sequencia = nr_seq_filha_p;

if (ds_arquivo_p IS NOT NULL AND ds_arquivo_p::text <> '' AND nr_seq_filha_w IS NOT NULL AND nr_seq_filha_w::text <> '') then
	insert into man_ordem_serv_filha_anexo(
		nr_sequencia,
		nr_seq_filha,
		ds_arquivo,
		dt_atualizacao,
		nm_usuario,
		dt_atualizacao_nrec,
		nm_usuario_nrec )
	values (	nextval('man_ordem_serv_filha_anexo_seq'),
		nr_seq_filha_p,
		ds_arquivo_p,
		clock_timestamp(),
		nm_usuario_p,
		clock_timestamp(),
		nm_usuario_p);
	commit;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE man_gravar_anexo_ordem_filha ( nr_seq_filha_p bigint, ds_arquivo_p text, nm_usuario_p text ) FROM PUBLIC;

