-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consiste_regra_agecons_conv_js ( dt_agenda_p timestamp, cd_convenio_p bigint, cd_agenda_p bigint, cd_setor_atendimento_p bigint, cd_plano_convenio_p text, cd_pessoa_fisica_p text, cd_categoria_p text, ie_consiste_regra_conv_p text, ie_consiste_turno_p text, nm_usuario_p text, ds_msg_p INOUT text, cd_estabelecimento_p bigint) AS $body$
DECLARE

 
ie_existe_turno_w		varchar(1) := 'N';

BEGIN
if (nm_usuario_p IS NOT NULL AND nm_usuario_p::text <> '') then 
	begin 
	 
	if (ie_consiste_regra_conv_p = 'S') then 
		begin 
		CALL consiste_regra_agecons_conv(cd_convenio_p, cd_categoria_p, cd_agenda_p, cd_setor_atendimento_p, cd_plano_convenio_p, cd_pessoa_fisica_p, dt_agenda_p, cd_estabelecimento_p, null, null);
		end;
	end if;
	 
	if (ie_consiste_turno_p = 'S') and (cd_agenda_p IS NOT NULL AND cd_agenda_p::text <> '') and (dt_agenda_p IS NOT NULL AND dt_agenda_p::text <> '') then 
		begin 
		ie_existe_turno_w := substr(obter_existe_agend_turno_cons(cd_agenda_p, dt_agenda_p, null),1);
 
		if (ie_existe_turno_w = 'N') then 
			begin 
			ds_msg_p := obter_texto_dic_objeto(182449, wheb_usuario_pck.get_nr_seq_idioma, null);
			end;
		end if;
		end;
	end if;
	end;
end if;
commit;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consiste_regra_agecons_conv_js ( dt_agenda_p timestamp, cd_convenio_p bigint, cd_agenda_p bigint, cd_setor_atendimento_p bigint, cd_plano_convenio_p text, cd_pessoa_fisica_p text, cd_categoria_p text, ie_consiste_regra_conv_p text, ie_consiste_turno_p text, nm_usuario_p text, ds_msg_p INOUT text, cd_estabelecimento_p bigint) FROM PUBLIC;

