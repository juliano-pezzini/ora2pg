-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE duplicar_mat_atend_pac_conv ( nr_sequencia_p bigint, nr_seq_nova_p bigint, nm_usuario_p text) AS $body$
BEGIN

if (coalesce(nr_sequencia_p,0) > 0) and (coalesce(nr_seq_nova_p,0) > 0) then

	insert into mat_atend_pac_convenio(
		nr_seq_material,
		dt_atualizacao,
		nm_usuario,
		cd_material,
		ds_material,
		cd_unidade_medida,
		tx_conversao_qtde,
		cd_grupo,
		ie_tipo_material_conv
	)
	SELECT	nr_seq_nova_p,
		clock_timestamp(),
		nm_usuario_p,
		cd_material,
		ds_material,
		cd_unidade_medida,
		tx_conversao_qtde,
		cd_grupo,
		ie_tipo_material_conv
	from	mat_atend_pac_convenio
	where	nr_seq_material = nr_sequencia_p;

end if;

if (coalesce(wheb_usuario_pck.get_ie_commit, 'S') = 'S') then commit; end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE duplicar_mat_atend_pac_conv ( nr_sequencia_p bigint, nr_seq_nova_p bigint, nm_usuario_p text) FROM PUBLIC;

