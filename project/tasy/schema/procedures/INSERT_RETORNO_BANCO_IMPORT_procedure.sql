-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE insert_retorno_banco_import ( nm_tabela_retorno_p text, nm_usuario_p text, nr_seq_cobranca_p bigint, ds_string_p text) AS $body$
DECLARE


ds_sep_bv_w	varchar(10);


BEGIN
	if (coalesce(nr_seq_cobranca_p, 0) > 0) then
		begin

		select 	obter_separador_bv
		into STRICT	ds_sep_bv_w
		;

		CALL exec_sql_dinamico_bv('', 'insert into ' || nm_tabela_retorno_p || ' (	NR_SEQUENCIA, ' ||
								'DT_ATUALIZACAO, ' ||
								'NM_USUARIO, ' ||
								'DT_ATUALIZACAO_NREC, ' ||
								'NM_USUARIO_NREC, ' ||
								'NR_SEQ_COBR_ESCRIT, ' ||
								'DS_STRING) ' ||
								'values( ' || nm_tabela_retorno_p || '_seq.nextval' || ', ' ||
								'sysdate, ' ||
								':nm_usuario, ' ||
								'sysdate, ' ||
								':nm_usuario, ' ||
								':nr_seq_cobranca, ' ||
								':ds_string) ',
								'nm_usuario='|| nm_usuario_p || ds_sep_bv_w ||
								'nm_usuario='|| nm_usuario_p || ds_sep_bv_w ||
								'nr_seq_cobranca='|| nr_seq_cobranca_p || ds_sep_bv_w ||
								'ds_string='|| ds_string_p || ds_sep_bv_w);
		commit;
		end;
	end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE insert_retorno_banco_import ( nm_tabela_retorno_p text, nm_usuario_p text, nr_seq_cobranca_p bigint, ds_string_p text) FROM PUBLIC;

