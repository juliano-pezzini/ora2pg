-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_alt_plano_benef_prop_lista ( nr_seq_beneficiario_lista_p text, nr_seq_plano_p bigint, nr_seq_tabela_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) AS $body$
DECLARE

 
nr_seq_beneficiario_lista_w	varchar(2000);
nr_seq_beneficiario_w	bigint;


BEGIN 
 
nr_seq_beneficiario_lista_w	:= nr_seq_beneficiario_lista_p;
 
while (nr_seq_beneficiario_lista_w IS NOT NULL AND nr_seq_beneficiario_lista_w::text <> '') loop 
	begin 
	nr_seq_beneficiario_w	:= substr(nr_seq_beneficiario_lista_w, 1, position(',' in nr_seq_beneficiario_lista_w) - 1);
	nr_seq_beneficiario_lista_w	:= substr(nr_seq_beneficiario_lista_w, position(',' in nr_seq_beneficiario_lista_w) + 1, length(nr_seq_beneficiario_lista_w));
 
	CALL pls_alterar_plano_benef_prop( 
			nr_seq_beneficiario_w, 
			nr_seq_plano_p, 
			nr_seq_tabela_p, 
			cd_estabelecimento_p, 
			nm_usuario_p);
	end;
end loop;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_alt_plano_benef_prop_lista ( nr_seq_beneficiario_lista_p text, nr_seq_plano_p bigint, nr_seq_tabela_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) FROM PUBLIC;

