-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE inserir_imagem ( nr_seq_img_p bigint, nr_seq_ext_p bigint, ie_classificacao_p text, cd_pessoa_fisica_p text default null, nr_atendimento_p bigint default null) AS $body$
DECLARE


ds_titulo_w	varchar(100);


BEGIN

select	ds_titulo
into STRICT	ds_titulo_w
from	imagem_banco
where	nr_sequencia = nr_seq_img_p;

if (ie_classificacao_p = 'PE_PRESCR_IMAGEM') then
	insert into pe_prescr_imagem(NR_SEQUENCIA,
		DS_TITULO,
		NR_SEQ_IMG,
		DT_ATUALIZACAO,
		NM_USUARIO,
		NR_SEQ_PRESCR,
		CD_PESSOA_FISICA,
		NR_ATENDIMENTO)
	values (nextval('pe_prescr_imagem_seq'),
		ds_titulo_w,
		nr_seq_img_p,
		clock_timestamp(),
		WHEB_USUARIO_PCK.get_nm_usuario,
		nr_seq_ext_p,
		cd_pessoa_fisica_p,
		nr_atendimento_p);
end	if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE inserir_imagem ( nr_seq_img_p bigint, nr_seq_ext_p bigint, ie_classificacao_p text, cd_pessoa_fisica_p text default null, nr_atendimento_p bigint default null) FROM PUBLIC;

