-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE atualizar_nr_seq_assinatura ( nr_sequencia_p bigint, nr_parecer_p bigint, nr_seq_assinatura_p bigint, nm_usuario_p text) AS $body$
BEGIN

if ((nr_sequencia_p IS NOT NULL AND nr_sequencia_p::text <> '' AND  nr_sequencia_p > 0)	and
   (nr_parecer_p IS NOT NULL AND nr_parecer_p::text <> '' AND  nr_parecer_p > 0)	and
   (nr_seq_assinatura_p IS NOT NULL AND nr_seq_assinatura_p::text <> '' AND  nr_seq_assinatura_p > 0))	then

	update 	parecer_medico
	set		nr_seq_assinatura = nr_seq_assinatura_p,
			dt_atualizacao = clock_timestamp(),
			nm_usuario = nm_usuario_p
	where 	nr_sequencia = nr_sequencia_p
	and 	nr_parecer = nr_parecer_p;

end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE atualizar_nr_seq_assinatura ( nr_sequencia_p bigint, nr_parecer_p bigint, nr_seq_assinatura_p bigint, nm_usuario_p text) FROM PUBLIC;

