-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_oc_cta_tratar_val_75 ( dados_regra_p pls_tipos_ocor_pck.dados_regra, nr_id_transacao_p pls_selecao_ocor_cta.nr_id_transacao%type, nm_usuario_p usuario.nm_usuario%type) AS $body$
DECLARE


bind_sql_valor_w	sql_pck.t_dado_bind;
cursor_w		sql_pck.t_cursor;
ds_sql_w		varchar(3000);
ds_restr_w		varchar(1000);

nr_seq_selecao_w	dbms_sql.number_table;
ds_observacao_w		dbms_sql.varchar2_table;
ie_valido_w		dbms_sql.varchar2_table;

C01 CURSOR(	nr_seq_oc_cta_comb_p	dados_regra_p.nr_sequencia%type) FOR
	SELECT	a.ie_exigencia_regra
	from	pls_oc_cta_val_exig_audit a
	where	a.nr_seq_oc_cta_comb	= nr_seq_oc_cta_comb_p;

BEGIN

if (dados_regra_p.nr_sequencia IS NOT NULL AND dados_regra_p.nr_sequencia::text <> '')  then
	for r_C01_w in C01(dados_regra_p.nr_sequencia) loop

		CALL pls_tipos_ocor_pck.atualiza_campo_auxiliar('V', nr_id_transacao_p, null, dados_regra_p);

		if (coalesce(r_C01_w.ie_exigencia_regra,0) > 1)	then

			if (r_C01_w.ie_exigencia_regra = 2) then
				ds_restr_w := 	'	and  (	b.nr_crm_auditor is null				' || pls_tipos_ocor_pck.enter_w ||
						'	or	b.nm_medico_auditor is null				' || pls_tipos_ocor_pck.enter_w ||
						'	or	b.nr_coren_auditor is null				' || pls_tipos_ocor_pck.enter_w ||
						'	or	b.nm_enfer_auditor is null				' || pls_tipos_ocor_pck.enter_w ||
						'	or	b.cd_uf_crm is null					' || pls_tipos_ocor_pck.enter_w ||
						'	or	b.cd_uf_coren is null  )				';
			elsif (r_C01_w.ie_exigencia_regra = 3) then
				ds_restr_w := 	'	and  (	b.nr_coren_auditor is null				' || pls_tipos_ocor_pck.enter_w ||
						'	or	b.nm_enfer_auditor is null				' || pls_tipos_ocor_pck.enter_w ||
						'	or	b.cd_uf_coren is null  )				';
			elsif (r_C01_w.ie_exigencia_regra = 4) then
				ds_restr_w := 	'	and  (	b.nr_crm_auditor is null				' || pls_tipos_ocor_pck.enter_w ||
						'	or	b.nm_medico_auditor is null				' || pls_tipos_ocor_pck.enter_w ||
						'	or	b.cd_uf_crm is null  )					';
			elsif (r_C01_w.ie_exigencia_regra = 5) then
				ds_restr_w := 	'	and  ((	b.nr_coren_auditor is null				' || pls_tipos_ocor_pck.enter_w ||
						'	or	b.nm_enfer_auditor is null				' || pls_tipos_ocor_pck.enter_w ||
						'	or	b.cd_uf_coren is null  )				' || pls_tipos_ocor_pck.enter_w ||
						'	and   (	b.nr_crm_auditor is null				' || pls_tipos_ocor_pck.enter_w ||
						'	or	b.nm_medico_auditor is null				' || pls_tipos_ocor_pck.enter_w ||
						'	or	b.cd_uf_crm is null  ))					';
			end if;

			ds_sql_w := 	'	select 	x.nr_sequencia, 						' || pls_tipos_ocor_pck.enter_w ||
					'		''S'' ie_valido, 						' || pls_tipos_ocor_pck.enter_w ||
					'		null ds_observacao 						' || pls_tipos_ocor_pck.enter_w ||
					'	from	pls_oc_cta_selecao_ocor_v	x, 				' || pls_tipos_ocor_pck.enter_w ||
					'		pls_conta			a,				' || pls_tipos_ocor_pck.enter_w ||
					'		ptu_nota_hospitalar		b				' || pls_tipos_ocor_pck.enter_w ||
					'	where	x.ie_valido		= ''S'' 				' || pls_tipos_ocor_pck.enter_w ||
					'	and	x.nr_id_transacao	= :nr_id_transacao_pc 			' || pls_tipos_ocor_pck.enter_w ||
					'	and	a.nr_sequencia		= x.nr_seq_conta 			' || pls_tipos_ocor_pck.enter_w ||
					'	and	b.nr_sequencia		= a.nr_seq_nota_hospitalar		' || pls_tipos_ocor_pck.enter_w;

			ds_sql_w := ds_sql_w || ds_restr_w;

			bind_sql_valor_w := sql_pck.bind_variable(':nr_id_transacao_pc', nr_id_transacao_p, bind_sql_valor_w);

			begin
				bind_sql_valor_w := sql_pck.executa_sql_cursor(ds_sql_w, bind_sql_valor_w);
				nr_seq_selecao_w	:= pls_util_cta_pck.num_table_vazia_w;
				ie_valido_w		:= pls_util_cta_pck.vchr2_table_vazia_w;
				ds_observacao_w		:= pls_util_cta_pck.vchr2_table_vazia_w;

				loop
					fetch cursor_w bulk collect
					into nr_seq_selecao_w, ie_valido_w, ds_observacao_w
					limit pls_util_cta_pck.qt_registro_transacao_w;
					exit when nr_seq_selecao_w.count = 0;

					CALL pls_tipos_ocor_pck.gerencia_selecao_validacao(	nr_seq_selecao_w, pls_util_cta_pck.clob_table_vazia_w,
											'SEQ', ds_observacao_w, ie_valido_w, nm_usuario_p);

				end loop;

				close cursor_w;

			exception
			when others then
				CALL pls_tipos_ocor_pck.trata_erro_sql_dinamico(dados_regra_p, ds_sql_w, nr_id_transacao_p, nm_usuario_p);
			end;
		end if;
	end loop;

	CALL pls_tipos_ocor_pck.atualiza_campo_valido('V', nr_id_transacao_p, null, dados_regra_p);

end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_oc_cta_tratar_val_75 ( dados_regra_p pls_tipos_ocor_pck.dados_regra, nr_id_transacao_p pls_selecao_ocor_cta.nr_id_transacao%type, nm_usuario_p usuario.nm_usuario%type) FROM PUBLIC;

