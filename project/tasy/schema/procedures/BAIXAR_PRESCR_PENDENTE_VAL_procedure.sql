-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE baixar_prescr_pendente_val ( qt_hora_adic_mat_p bigint, qt_hora_adic_proc_p bigint, cd_motivo_baixa_p bigint, dt_parametro_p timestamp) AS $body$
BEGIN
/*
update	prescr_procedimento a
set	cd_motivo_baixa = cd_motivo_baixa_p
where	cd_motivo_baixa = 0
and	nvl(ie_status_atend,1) < 35
and	DT_CANCELAMENTO is null
and	exists
	(select	1
	from	Procedimento c,
		prescr_medica b
	where	a.nr_prescricao	= b.nr_prescricao
	and	(a.dt_prev_execucao + (nvl(c.qt_hora_baixar_prescr,qt_hora_adic_proc_p) / 24) < dt_parametro_p)
	and	(b.dt_prescricao + ((b.nr_horas_validade + nvl(c.qt_hora_baixar_prescr,qt_hora_adic_proc_p)) /24) < dt_parametro_p)
	and	c.cd_procedimento = a.cd_procedimento
	and	c.ie_origem_proced = a.ie_origem_proced)
and not exists(
		select	1
		from	cirurgia w
		where	w.nr_prescricao = a.nr_prescricao)
and not exists(
		select	1
		from	prescr_medica w
		where	w.nr_prescricao = a.nr_prescricao
		and	w.nr_cirurgia is not null)
and not exists
	(select	1
	from	procedimento_paciente x
	where	x.nr_prescricao = a.nr_prescricao
	and	x.nr_sequencia_prescricao = a.nr_sequencia);
commit;*/
update	prescr_material a
set	cd_motivo_baixa = cd_motivo_baixa_p
where	cd_motivo_baixa = 0
and not exists (
		SELECT	1
		from	cirurgia w
		where	w.nr_prescricao = a.nr_prescricao)
and exists
	(SELECT	1
	from 	prescr_medica b
	where	a.nr_prescricao = b.nr_prescricao
	and	(b.dt_validade_prescr + (qt_hora_adic_mat_p/24)) < dt_parametro_p)
and not exists (
		select	1
		from	prescr_medica w
		where	w.nr_prescricao = a.nr_prescricao
		and	(w.nr_cirurgia IS NOT NULL AND w.nr_cirurgia::text <> ''))
and not exists (select	1
	from	conclusao_recom_apae w
	where	w.nr_prescricao = a.nr_prescricao);
commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE baixar_prescr_pendente_val ( qt_hora_adic_mat_p bigint, qt_hora_adic_proc_p bigint, cd_motivo_baixa_p bigint, dt_parametro_p timestamp) FROM PUBLIC;

