-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE atualiza_estab_mat_estoque ( cd_material_p bigint, cd_material_estoque_p bigint, nm_campo_p text, cd_estabelecimento_p bigint, nm_usuario_p text, ie_dias_intervalo_p text default 'X', ie_dias_fornecedor_p text default 'X', ie_dias_estoque_p text default 'X') AS $body$
DECLARE


qt_dia_interv_ressup_w		smallint;
qt_dia_ressup_forn_w		smallint;
qt_dia_estoque_minimo_w		smallint;


BEGIN

if (cd_material_estoque_p IS NOT NULL AND cd_material_estoque_p::text <> '') then
	select	coalesce(qt_dia_interv_ressup,0),
		coalesce(qt_dia_ressup_forn,0),
		coalesce(qt_dia_estoque_minimo,0)
	into STRICT	qt_dia_interv_ressup_w,
		qt_dia_ressup_forn_w,
		qt_dia_estoque_minimo_w
	from	material_estab
	where	cd_material = cd_material_estoque_p
	and	cd_estabelecimento = cd_estabelecimento_p;
end if;

if (nm_campo_p = wheb_mensagem_pck.get_texto(312072)) or (ie_dias_intervalo_p = 'S') then
	begin
	update	material_estab
	set	qt_dia_interv_ressup = qt_dia_interv_ressup_w
	where	cd_material = cd_material_p
	and	cd_estabelecimento = cd_estabelecimento_p;
	end;
end if;

if (nm_campo_p = wheb_mensagem_pck.get_texto(312075)) or (ie_dias_fornecedor_p = 'S') then
	begin
	update	material_estab
	set	qt_dia_ressup_forn = qt_dia_ressup_forn_w
	where	cd_material = cd_material_p
	and	cd_estabelecimento = cd_estabelecimento_p;
	end;
end if;


if (nm_campo_p = wheb_mensagem_pck.get_texto(312081)) or (ie_dias_estoque_p = 'S') then
	begin
	update	material_estab
	set	qt_dia_estoque_minimo = qt_dia_estoque_minimo_w
	where	cd_material = cd_material_p
	and	cd_estabelecimento = cd_estabelecimento_p;
	end;
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE atualiza_estab_mat_estoque ( cd_material_p bigint, cd_material_estoque_p bigint, nm_campo_p text, cd_estabelecimento_p bigint, nm_usuario_p text, ie_dias_intervalo_p text default 'X', ie_dias_fornecedor_p text default 'X', ie_dias_estoque_p text default 'X') FROM PUBLIC;

