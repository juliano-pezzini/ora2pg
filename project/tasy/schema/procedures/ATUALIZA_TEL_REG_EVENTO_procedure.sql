-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE atualiza_tel_reg_evento ( nr_sequencia_p bigint, ie_processado_p bigint, ds_erro_p text, nm_usuario_p text) AS $body$
BEGIN

if ((nr_sequencia_p IS NOT NULL AND nr_sequencia_p::text <> '') and nr_sequencia_p > 0) then

	if	((coalesce(ds_erro_P::text, '') = '' or ds_erro_p = '')
		and (ie_processado_p IS NOT NULL AND ie_processado_p::text <> '') and ie_processado_p >= 0) then
		update 	tel_reg_evento
		set 	ie_processado = ie_processado_p
		where 	nr_sequencia = nr_sequencia_p;
	elsif ((ds_erro_P IS NOT NULL AND ds_erro_P::text <> '') or ds_erro_p <> '') then
		update 	tel_reg_evento
		set 	ds_erro	= ds_erro_p
		where 	nr_sequencia = nr_sequencia_p;
	end if;

end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE atualiza_tel_reg_evento ( nr_sequencia_p bigint, ie_processado_p bigint, ds_erro_p text, nm_usuario_p text) FROM PUBLIC;

