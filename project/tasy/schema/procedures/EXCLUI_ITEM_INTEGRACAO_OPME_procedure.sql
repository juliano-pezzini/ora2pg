-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE exclui_item_integracao_opme ( nr_seq_map_p bigint ) AS $body$
DECLARE


qt_pac_opme_w		bigint;
qt_pac_int_opme_w	bigint;


BEGIN

select	count(*)
into STRICT	qt_pac_opme_w
from	agenda_pac_opme
where	nr_seq_map = nr_seq_map_p;

select	count(*)
into STRICT	qt_pac_int_opme_w
from	atend_pac_int_opme_item
where	nr_seq_map = nr_seq_map_p;

if (coalesce(qt_pac_opme_w,0) > 0) then
	begin

	delete	FROM agenda_pac_opme
	where	nr_seq_map = nr_seq_map_p;

	end;
elsif (coalesce(qt_pac_int_opme_w,0) >0) then
	begin

	delete	FROM atend_pac_int_opme_item
	where	nr_seq_map = nr_seq_map_p;

	end;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE exclui_item_integracao_opme ( nr_seq_map_p bigint ) FROM PUBLIC;

