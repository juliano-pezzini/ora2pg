-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consistir_entregas_anteriores (nr_seq_paciente_entrega_p bigint, nr_seq_receita_p bigint, ds_erro_p INOUT text) AS $body$
DECLARE


nr_sequencia_w	bigint;

BEGIN


select	max(nr_sequencia)
into STRICT	nr_sequencia_w
from	fa_entrega_medicacao
where	ie_status_medicacao <> 'EM'
and	nr_seq_paciente_entrega = nr_seq_paciente_entrega_p
and	nr_seq_receita_amb = nr_seq_receita_p
and	coalesce(dt_cancelamento::text, '') = '';

if (nr_sequencia_w IS NOT NULL AND nr_sequencia_w::text <> '') then
	ds_erro_p := WHEB_MENSAGEM_PCK.get_texto(279280);
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consistir_entregas_anteriores (nr_seq_paciente_entrega_p bigint, nr_seq_receita_p bigint, ds_erro_p INOUT text) FROM PUBLIC;

