-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_demissao_html ( cd_pessoa_fisica_p text, nm_usuario_p text, ie_acao_p bigint, dt_referencia_p timestamp, cd_estabelecimento_p bigint, cd_cargo_p bigint, cd_cargo_ant_p bigint, cd_setor_ant_p bigint, ds_observacao_p text, ie_gerar_cargo_perfil_p text, ie_consiste_usuario_p text, ie_apagar_inf_demitir_p text, ie_regra_p text, ds_registro_novo_p text, ds_valor_atual_p text default null, ds_valor_ant_p text default null) AS $body$
DECLARE

 
C01 CURSOR FOR 
	SELECT	nm_usuario, 
		cd_setor_atendimento 
	from	usuario 
	where	cd_pessoa_fisica = cd_pessoa_fisica_p 
	
union
 
	SELECT	null, 
		null 
	from	pessoa_fisica a 
	where	cd_pessoa_fisica = cd_pessoa_fisica_p 
	and	not exists (	select	1 
				from	usuario b 
				where	b.cd_pessoa_fisica = a.cd_pessoa_fisica);

c01_w	c01%rowtype;


BEGIN 
 
open C01;
loop 
fetch C01 into	 
	c01_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	 
	 
	CALL enviar_comunic_regra_func_usu(	ie_regra_p, 
				ds_registro_novo_p, 
				nm_usuario_p, 
				cd_pessoa_fisica_p, 
				ds_valor_atual_p, 
				ds_valor_ant_p);
 
	CALL Gravar_movto_funcionario(	cd_pessoa_fisica_p, 
					nm_usuario_p, 
					ie_acao_p, 
					dt_referencia_p, 
					cd_estabelecimento_p, 
					0, 
					cd_cargo_ant_p, 
					0, 
					c01_w.cd_setor_atendimento, 
					ds_observacao_p, 
					c01_w.nm_usuario, 
					ie_gerar_cargo_perfil_p, 
					ie_consiste_usuario_p, 
					ie_apagar_inf_demitir_p);
end loop;
close C01;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_demissao_html ( cd_pessoa_fisica_p text, nm_usuario_p text, ie_acao_p bigint, dt_referencia_p timestamp, cd_estabelecimento_p bigint, cd_cargo_p bigint, cd_cargo_ant_p bigint, cd_setor_ant_p bigint, ds_observacao_p text, ie_gerar_cargo_perfil_p text, ie_consiste_usuario_p text, ie_apagar_inf_demitir_p text, ie_regra_p text, ds_registro_novo_p text, ds_valor_atual_p text default null, ds_valor_ant_p text default null) FROM PUBLIC;

