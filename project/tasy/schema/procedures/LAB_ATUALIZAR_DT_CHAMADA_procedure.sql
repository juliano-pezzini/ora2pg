-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE lab_atualizar_dt_chamada ( nr_prescricao_p bigint, nr_seq_prescr_p bigint, nm_usuario_p text, nr_seq_local_pa_p bigint DEFAULT NULL) AS $body$
BEGIN
  if (nr_prescricao_p IS NOT NULL AND nr_prescricao_p::text <> '') and (nr_seq_prescr_p IS NOT NULL AND nr_seq_prescr_p::text <> '') then
    update prescr_procedimento c
       set dt_chamada_lab = clock_timestamp(),
           nm_usuario = nm_usuario_p,
	         dt_atualizacao = clock_timestamp()
     where nr_prescricao = nr_prescricao_p
       and nr_sequencia = nr_seq_prescr_p;

    if (nr_seq_local_pa_p IS NOT NULL AND nr_seq_local_pa_p::text <> '') then
      CALL lab_atualiza_local_atendimento(nr_prescricao_p, nr_seq_local_pa_p, nm_usuario_p);
    end if;
  end if;

  commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE lab_atualizar_dt_chamada ( nr_prescricao_p bigint, nr_seq_prescr_p bigint, nm_usuario_p text, nr_seq_local_pa_p bigint DEFAULT NULL) FROM PUBLIC;

