-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE atualiza_status_pac_js ( nr_seq_status_p bigint, ds_lista_agenda_p text, nm_usuario_p text) AS $body$
DECLARE


ds_lista_agenda_cons_w		varchar(4000);
ds_lista_agenda_w		varchar(4000);
ds_lista_agenda_serv_w		varchar(4000);
ds_lista_agenda_quimio_w 	varchar(4000);

BEGIN
if (nm_usuario_p IS NOT NULL AND nm_usuario_p::text <> '') then
	begin
	SELECT * FROM obter_seq_agendas_lista(ds_lista_agenda_p, ds_lista_agenda_w, ds_lista_agenda_cons_w, ds_lista_agenda_serv_w, ds_lista_agenda_quimio_w) INTO STRICT ds_lista_agenda_w, ds_lista_agenda_cons_w, ds_lista_agenda_serv_w, ds_lista_agenda_quimio_w;
	CALL atualiza_status_pac_agexame(null, ds_lista_agenda_w, nr_seq_status_p, nm_usuario_p);
	end;
end if;
commit;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE atualiza_status_pac_js ( nr_seq_status_p bigint, ds_lista_agenda_p text, nm_usuario_p text) FROM PUBLIC;

