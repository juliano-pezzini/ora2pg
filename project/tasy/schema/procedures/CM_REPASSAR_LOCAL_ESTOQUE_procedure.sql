-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE cm_repassar_local_estoque ( nr_seq_conjunto_p bigint, cd_local_estoque_p bigint, cd_pessoa_receb_p text, nm_pessoa_receb_p text, nm_usuario_p text) AS $body$
DECLARE


nr_seq_conjunto_w	bigint;
cd_estabelecimento_w	integer;


BEGIN

if (coalesce(cd_pessoa_receb_p,'0') <> '0') then
	begin

	update	cm_conjunto_cont
	set	cd_local_estoque = cd_local_estoque_p,
		cd_pessoa_receb = cd_pessoa_receb_p,
		nm_usuario = nm_usuario_p,
		dt_atualizacao = clock_timestamp()
	where	nr_sequencia = nr_seq_conjunto_p;

	end;
elsif (nm_pessoa_receb_p IS NOT NULL AND nm_pessoa_receb_p::text <> '') then
	begin

	update	cm_conjunto_cont
	set	cd_local_estoque = cd_local_estoque_p,
		nm_pessoa_receb = nm_pessoa_receb_p,
		nm_usuario = nm_usuario_p,
		dt_atualizacao = clock_timestamp()
	where	nr_sequencia = nr_seq_conjunto_p;

	end;
else
	begin

	update	cm_conjunto_cont
	set	cd_local_estoque = cd_local_estoque_p,
		nm_usuario = nm_usuario_p,
		dt_atualizacao = clock_timestamp()
	where	nr_sequencia = nr_seq_conjunto_p;

	end;
end if;

select	nr_seq_conjunto,
	cd_estabelecimento
into STRICT	nr_seq_conjunto_w,
	cd_estabelecimento_w
from	cm_conjunto_cont
where	nr_sequencia = nr_seq_conjunto_p;

CALL cm_gera_expurgo_saldo(nr_seq_conjunto_w, cd_local_estoque_p, 'D', cd_estabelecimento_w, nm_usuario_p);

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE cm_repassar_local_estoque ( nr_seq_conjunto_p bigint, cd_local_estoque_p bigint, cd_pessoa_receb_p text, nm_pessoa_receb_p text, nm_usuario_p text) FROM PUBLIC;

