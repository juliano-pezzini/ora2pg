-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_atualizar_data_hist_inco ( cd_pessoa_fisica_p text, cd_cgc_p text, dt_historico_p timestamp, nm_usuario_p text, cd_estabelecimento_p bigint) AS $body$
BEGIN

if (cd_pessoa_fisica_p <>	' ') then
	update	pls_pessoa_inconsistente
	set	dt_ultimo_historico	= to_char(dt_historico_p,'dd/mm/yyyy'),
		nm_usuario		= nm_usuario_p
	where	cd_pessoa_fisica	= cd_pessoa_fisica_p;
end if;

if (cd_cgc_p <> ' ') then
	update	pls_pessoa_inconsistente
	set	dt_ultimo_historico	= to_char(dt_historico_p,'dd/mm/yyyy'),
		nm_usuario		= nm_usuario_p
	where	cd_cgc			= cd_cgc_p;
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_atualizar_data_hist_inco ( cd_pessoa_fisica_p text, cd_cgc_p text, dt_historico_p timestamp, nm_usuario_p text, cd_estabelecimento_p bigint) FROM PUBLIC;

