-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_parametro_secundario_at ( cd_operador_p text, nm_usuario_p text, nr_seq_operador_p INOUT bigint, nr_seq_grupo_atend_p INOUT bigint, ie_operador_cadastrado_p INOUT text) AS $body$
BEGIN

if (cd_operador_p IS NOT NULL AND cd_operador_p::text <> '') then


	begin
		select	coalesce(max(nr_sequencia),0)
		into STRICT	nr_seq_operador_p
		from	pls_operador
		where	cd_pessoa_fisica = cd_operador_p
		and	ie_situacao = 'A';
	exception
	when others then
		nr_seq_operador_p := null;
	end;

	begin
		select	nr_seq_grupo_atend
		into STRICT	nr_seq_grupo_atend_p
		from	pls_operador
		where	nr_sequencia = nr_seq_operador_p;
	exception
	when others then
		nr_seq_grupo_atend_p := null;
	end;

	if (coalesce(nr_seq_operador_p,0) <> 0) then
		ie_operador_cadastrado_p := 'S';
	else
		ie_operador_cadastrado_p := 'N';
	end if;
end if;


commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_parametro_secundario_at ( cd_operador_p text, nm_usuario_p text, nr_seq_operador_p INOUT bigint, nr_seq_grupo_atend_p INOUT bigint, ie_operador_cadastrado_p INOUT text) FROM PUBLIC;

