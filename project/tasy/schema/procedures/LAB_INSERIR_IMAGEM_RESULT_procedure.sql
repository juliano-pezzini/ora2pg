-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE lab_inserir_imagem_result ( nr_seq_prescr_p bigint, nr_seq_resultado_p bigint, ds_imagem_p text, nm_usuario_p text) AS $body$
DECLARE


nr_sequencia_w	bigint;


BEGIN

if (nr_seq_resultado_p IS NOT NULL AND nr_seq_resultado_p::text <> '') and (ds_imagem_p IS NOT NULL AND ds_imagem_p::text <> '') then

	select	nextval('exame_lab_result_imagem_seq')
	into STRICT	nr_sequencia_w
	;

	insert into exame_lab_result_imagem(
		nr_sequencia,
		nr_seq_resultado,
		nr_seq_prescr,
		ds_imagem,
		dt_atualizacao,
		nm_usuario)
	values (
		nr_sequencia_w,
		nr_seq_resultado_p,
		nr_seq_prescr_p,
		substr(ds_imagem_p,1,255),
		clock_timestamp(),
		nm_usuario_p);

	commit;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE lab_inserir_imagem_result ( nr_seq_prescr_p bigint, nr_seq_resultado_p bigint, ds_imagem_p text, nm_usuario_p text) FROM PUBLIC;

