-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE alterar_status_grupo_agecons ( cd_agenda_p bigint, nr_atendimento_p bigint, ie_status_p text, cd_motivo_p text, ds_motivo_p text, ie_agenda_dia_p text, nm_usuario_p text, nr_seq_forma_conf_p bigint) AS $body$
DECLARE


nr_seq_age_cons_w	agenda_consulta.nr_sequencia%type;
nr_seq_agenda_int_w	bigint;

C01 CURSOR FOR
	SELECT	a.nr_sequencia
	from	agenda_consulta a
	where	a.nr_seq_agend_coletiva in (	SELECT	b.nr_sequencia
					from	agendamento_coletivo b
					where	b.nr_seq_agenda_int = nr_seq_agenda_int_w);


BEGIN

select	max(a.nr_seq_agenda_int)
into STRICT	nr_seq_agenda_int_w
from	agendamento_coletivo a
where	a.nr_sequencia = (	SELECT	max(b.nr_seq_agend_coletiva)
			from	agenda_consulta b
			where	b.nr_atendimento = nr_atendimento_p);

open C01;
loop
fetch C01 into	
	nr_seq_age_cons_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin
	CALL alterar_status_agecons(cd_agenda_p, nr_seq_age_cons_w, ie_status_p, cd_motivo_p, ds_motivo_p, ie_agenda_dia_p, nm_usuario_p, nr_seq_forma_conf_p);
	end;
end loop;
close C01;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE alterar_status_grupo_agecons ( cd_agenda_p bigint, nr_atendimento_p bigint, ie_status_p text, cd_motivo_p text, ds_motivo_p text, ie_agenda_dia_p text, nm_usuario_p text, nr_seq_forma_conf_p bigint) FROM PUBLIC;

