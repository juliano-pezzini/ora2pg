-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_prescricoes_paciente ( cd_protocolo_p bigint, nr_atendimento_p bigint, ie_liberado_p text, ds_lista_atend_pac_p text, cd_estabelecimento_p bigint, nm_usuario_p text, ie_gerar_todas_prescr_p text default 'N') AS $body$
DECLARE

 
ie_situacao_w		varchar(1);
ds_lista_atend_pac_w	varchar(2000);
nr_seq_paciente_w		bigint;
nr_pos_virgula_w		bigint;
ds_retorno_ww		varchar(255);
		

BEGIN 
if (ds_lista_atend_pac_p IS NOT NULL AND ds_lista_atend_pac_p::text <> '') and (nm_usuario_p IS NOT NULL AND nm_usuario_p::text <> '') then 
	begin 
	 
	if (ds_lista_atend_pac_p IS NOT NULL AND ds_lista_atend_pac_p::text <> '') and (cd_estabelecimento_p IS NOT NULL AND cd_estabelecimento_p::text <> '') then 
		begin 
		ds_lista_atend_pac_w	:= ds_lista_atend_pac_p;
		 
		while(ds_lista_atend_pac_w IS NOT NULL AND ds_lista_atend_pac_w::text <> '') loop 
			begin 
			nr_pos_virgula_w	:= position(',' in ds_lista_atend_pac_w);
			if (nr_pos_virgula_w > 0) then 
				begin 
				nr_seq_paciente_w	:= (substr(ds_lista_atend_pac_w,1,nr_pos_virgula_w-1))::numeric;
				ds_lista_atend_pac_w	:= substr(ds_lista_atend_pac_w,nr_pos_virgula_w+1,length(ds_lista_atend_pac_w));
				if (nr_seq_paciente_w > 0) then 
					begin 
					ds_retorno_ww := gerar_prescricao_paciente(nr_seq_paciente_w, nm_usuario_p, cd_estabelecimento_p, nr_atendimento_p, '', ie_liberado_p, ds_retorno_ww, ie_gerar_todas_prescr_p);
					end;
				end if;
				end;
			else 
				begin 
				ds_lista_atend_pac_w	:= null;
				end;
			end if;
			end;
		end loop;
		end;
	end if;
	end;
end if;
commit;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_prescricoes_paciente ( cd_protocolo_p bigint, nr_atendimento_p bigint, ie_liberado_p text, ds_lista_atend_pac_p text, cd_estabelecimento_p bigint, nm_usuario_p text, ie_gerar_todas_prescr_p text default 'N') FROM PUBLIC;

