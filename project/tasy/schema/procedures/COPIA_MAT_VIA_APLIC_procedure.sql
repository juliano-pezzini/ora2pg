-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE copia_mat_via_aplic ( cd_estabelecimento_p bigint, cd_material_p bigint, cd_material_novo_p bigint, nm_usuario_p text) AS $body$
DECLARE


/* Campos da tabela LOOP */

ie_via_aplicacao_w		varchar(5);
ds_recomendacao_w	varchar(2000);
ie_diluicao_w		varchar(1);
cd_setor_atendimento_w	integer;
cd_setor_excluir_w		integer;
cd_estabelecimento_w    mat_via_aplic.cd_estabelecimento%type;
ie_recomendada_w        mat_via_aplic.ie_recomendada%type;
nr_seq_prioridade_w     mat_via_aplic.nr_seq_prioridade%type;

/*  Sequencia da tabela */

nr_sequencia_w		bigint;

C01 CURSOR FOR
	SELECT	ie_via_aplicacao,
		ds_recomendacao,
		ie_diluicao,
        cd_estabelecimento,
		cd_setor_atendimento,
		cd_setor_excluir,
        nr_seq_prioridade,
        ie_recomendada
	from	mat_via_aplic
	where	cd_material = cd_material_p;


BEGIN

open C01;
loop
fetch C01 into	
	ie_via_aplicacao_w,
	ds_recomendacao_w,
	ie_diluicao_w,
    cd_estabelecimento_w,
	cd_setor_atendimento_w,
	cd_setor_excluir_w,
    nr_seq_prioridade_w,
    ie_recomendada_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin

	select	nextval('mat_via_aplic_seq')
	into STRICT	nr_sequencia_w
	;

	insert into mat_via_aplic(		nr_sequencia,
					cd_material,
					dt_atualizacao,
					nm_usuario,
					ie_via_aplicacao,
					ds_recomendacao,
					ie_diluicao,
                    cd_estabelecimento,
					cd_setor_atendimento,
					cd_setor_excluir,
                    nr_seq_prioridade,
                    ie_recomendada)
	values (	nr_sequencia_w,
					cd_material_novo_p,
					clock_timestamp(),
					nm_usuario_p,
					ie_via_aplicacao_w,
					ds_recomendacao_w,
					ie_diluicao_w,
                    cd_estabelecimento_w,
					cd_setor_atendimento_w,
					cd_setor_excluir_w,
                    nr_seq_prioridade_w,
                    ie_recomendada_w
					);


	end;
end loop;
close C01;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE copia_mat_via_aplic ( cd_estabelecimento_p bigint, cd_material_p bigint, cd_material_novo_p bigint, nm_usuario_p text) FROM PUBLIC;

