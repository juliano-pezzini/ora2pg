-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE ptu_consistir_complemento ( nr_seq_nota_cobr_p bigint, nr_seq_fatura_p bigint, nm_usuario_p text) AS $body$
DECLARE


nr_lote_w			integer;
nr_nota_w			ptu_nota_complemento.nr_nota%type;
ie_tipo_complemento_w		smallint;
ds_complemento_w		varchar(100);
nr_sequencia_w			bigint;
C01 CURSOR FOR
	SELECT	nr_sequencia,
		nr_lote,
		nr_nota,
		ie_tipo_complemento,
		ds_complemento
	from	ptu_nota_complemento
	where	nr_seq_nota_cobr	= nr_seq_nota_cobr_p;

BEGIN
/*
open C01;
loop
fetch C01 into
	nr_sequencia_w,
	nr_lote_w,
	nr_nota_w,
	ie_tipo_complemento_w,
	ds_complemento_w;
exit when C01%notfound;
	begin
	if	(nr_lote_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '505', 'NR_LOTE', null, null, nr_sequencia_w,nm_usuario_p);
	end if;
	if	(nr_nota_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '505', 'NR_NOTA', null, null, nr_sequencia_w,nm_usuario_p);
	end if;
	if	(ie_tipo_complemento_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '505', 'TP_REG_CPL', null, null, nr_sequencia_w,nm_usuario_p);
	end if;
	if	(ds_complemento_w is null) then
		ptu_inserir_consist_fatura(nr_seq_fatura_p, 1, '505', 'NM_DESC_CPL', null, null, nr_sequencia_w,nm_usuario_p);
	end if;
	end;
end loop;
close C01;*/
ds_complemento_w	:= 'A';
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE ptu_consistir_complemento ( nr_seq_nota_cobr_p bigint, nr_seq_fatura_p bigint, nm_usuario_p text) FROM PUBLIC;

