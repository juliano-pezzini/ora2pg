-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE agendamento_externo_finalizado (nr_seq_lista_espera_p bigint, nm_usuario_p text) AS $body$
DECLARE


nr_seq_regulacao_atend_w		regulacao_atend.nr_sequencia%type;


BEGIN

	if (nr_seq_lista_espera_p IS NOT NULL AND nr_seq_lista_espera_p::text <> '') then

		select max(nr_seq_regulacao)
		into STRICT nr_seq_regulacao_atend_w
		from agenda_lista_espera a
		where nr_sequencia = nr_seq_lista_espera_p;

		if (nr_seq_regulacao_atend_w IS NOT NULL AND nr_seq_regulacao_atend_w::text <> '') then
			CALL alterar_status_regulacao(nr_seq_regulacao_atend_w, 'FI', obter_expressao_dic_objeto(1099915), obter_pessoa_fisica_usuario(nm_usuario_p, null));
		end if;

	end if;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE agendamento_externo_finalizado (nr_seq_lista_espera_p bigint, nm_usuario_p text) FROM PUBLIC;

