-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_entrada_real_mapa ( nr_atendimento_p bigint, nr_seq_interno_p bigint, cd_setor_atendimento_p bigint, cd_unidade_basica_p text, cd_unidade_compl_p text) AS $body$
DECLARE

		 
nr_atendimento_w	bigint	:= 0;
	

BEGIN 
 
if (nr_atendimento_p IS NOT NULL AND nr_atendimento_p::text <> '') then 
	begin 
	 
	select	nr_atendimento 
	into STRICT	nr_atendimento_w 
	from	atendimento_paciente 
	where	nr_atendimento	= nr_atendimento_p;
	 
	if (nr_atendimento_w > 0) and (nr_seq_interno_p > 0) then 
		begin 
			 
		update	atend_paciente_unidade 
		set	dt_entrada_real		= clock_timestamp() 
		where	nr_atendimento		= nr_atendimento_w 
		and	cd_setor_atendimento	= cd_setor_atendimento_p 
		and	cd_unidade_basica	= cd_unidade_basica_p 
		and	cd_unidade_compl	= cd_unidade_compl_p 
		and	coalesce(dt_saida_unidade::text, '') = '';
		 
		commit;
 
		end;
	end if;
	 
	end;
end if;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_entrada_real_mapa ( nr_atendimento_p bigint, nr_seq_interno_p bigint, cd_setor_atendimento_p bigint, cd_unidade_basica_p text, cd_unidade_compl_p text) FROM PUBLIC;

