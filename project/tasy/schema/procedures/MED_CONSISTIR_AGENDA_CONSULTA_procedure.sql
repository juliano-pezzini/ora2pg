-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE med_consistir_agenda_consulta ( nr_seq_agenda_p bigint, cd_agenda_p bigint, cd_pessoa_fisica_p text, dt_agenda_p timestamp, nm_usuario_p text, ds_erro_p INOUT text) AS $body$
DECLARE

 
ds_erro_w	varchar(255);


BEGIN 
 
if (cd_agenda_p IS NOT NULL AND cd_agenda_p::text <> '') and (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') and (dt_agenda_p IS NOT NULL AND dt_agenda_p::text <> '') then 
	begin 
	ds_erro_w := Consiste_Agenda_Duplic_Horario(cd_pessoa_fisica_p, cd_agenda_p, dt_agenda_p, nm_usuario_p, ds_erro_w);
 
	if (coalesce(ds_erro_w::text, '') = '') then 
		ds_erro_w := Consiste_Agenda_Consulta(nr_seq_agenda_p, ds_erro_w);
	end if;
	end;
end if;
 
ds_erro_p	:= substr(ds_erro_w,1,255);
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE med_consistir_agenda_consulta ( nr_seq_agenda_p bigint, cd_agenda_p bigint, cd_pessoa_fisica_p text, dt_agenda_p timestamp, nm_usuario_p text, ds_erro_p INOUT text) FROM PUBLIC;

