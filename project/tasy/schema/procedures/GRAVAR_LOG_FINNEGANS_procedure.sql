-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gravar_log_finnegans ( ie_integracao_p log_finnegans.ie_integracao%TYPE, nm_tabela_p log_finnegans.nm_tabela%TYPE, nm_atributo_p log_finnegans.nm_atributo%TYPE, nr_seq_atributo_p log_finnegans.nr_seq_atributo%TYPE, cd_erro_p log_finnegans.cd_erro%TYPE, ds_erro_p log_finnegans.ds_erro%TYPE, ds_detalhe_p log_finnegans.ds_detalhe%TYPE, nm_usuario_p log_finnegans.nm_usuario%TYPE) AS $body$
DECLARE


ie_integracao_w     log_finnegans.ie_integracao%TYPE := ie_integracao_p;
nm_tabela_w         log_finnegans.nm_tabela%TYPE := nm_tabela_p;
nm_atributo_w       log_finnegans.nm_atributo%TYPE := nm_atributo_p;
nr_seq_atributo_w   log_finnegans.nr_seq_atributo%TYPE := nr_seq_atributo_p;
cd_erro_w           log_finnegans.cd_erro%TYPE := cd_erro_p;
ds_erro_w           log_finnegans.ds_erro%TYPE := ds_erro_p;
ds_detalhe_w        log_finnegans.ds_detalhe%TYPE := ds_detalhe_p;
nm_usuario_w        log_finnegans.nm_usuario%TYPE := nm_usuario_p;
ds_erro_insert_w    log_finnegans.ds_erro%TYPE;


BEGIN
    <<bloco_insert>>
    BEGIN
        INSERT INTO log_finnegans(
            nr_sequencia,
            ie_integracao,
            nm_tabela,
            nm_atributo,
            nr_seq_atributo,
            ds_detalhe,
            nm_usuario,
            dt_atualizacao,
            cd_erro,
            ds_erro
        ) VALUES (
            nextval('log_finnegans_seq'),
            ie_integracao_w,
            nm_tabela_w,
            nm_atributo_w,
            nr_seq_atributo_w,
            ds_detalhe_w,
            nm_usuario_w,
            clock_timestamp(),
            cd_erro_w,
            ds_erro_w
        );

        COMMIT;
    EXCEPTION
        WHEN data_exception OR program_error THEN
            ds_erro_insert_w := sqlerrm;
    END;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gravar_log_finnegans ( ie_integracao_p log_finnegans.ie_integracao%TYPE, nm_tabela_p log_finnegans.nm_tabela%TYPE, nm_atributo_p log_finnegans.nm_atributo%TYPE, nr_seq_atributo_p log_finnegans.nr_seq_atributo%TYPE, cd_erro_p log_finnegans.cd_erro%TYPE, ds_erro_p log_finnegans.ds_erro%TYPE, ds_detalhe_p log_finnegans.ds_detalhe%TYPE, nm_usuario_p log_finnegans.nm_usuario%TYPE) FROM PUBLIC;

