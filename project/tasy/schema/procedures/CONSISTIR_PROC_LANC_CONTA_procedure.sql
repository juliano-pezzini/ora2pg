-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE consistir_proc_lanc_conta ( nr_prescricao_p bigint, ds_erro_p INOUT text, nm_usuario_p text, cd_estabelecimento_p bigint) AS $body$
DECLARE


ds_erro_w		varchar(255) := null;
ie_consiste_w		varchar(1);
ie_consiste_via_w	varchar(1) := 'N';


BEGIN

ie_consiste_via_w := obter_param_usuario(900, 500, wheb_usuario_pck.get_cd_perfil, nm_usuario_p, cd_estabelecimento_p, ie_consiste_via_w);


if (ie_consiste_via_w = 'S') or (ie_consiste_via_w = 'A') then
	select	coalesce(max('S'),'N')
	into STRICT	ie_consiste_w
	from	prescr_procedimento
	where	nr_prescricao 		= nr_prescricao_p
	and	coalesce(cd_motivo_baixa,0) 	= 0
	and	coalesce(ie_via_acesso::text, '') = '';

	if (ie_consiste_w = 'S') then
		ds_erro_w := wheb_mensagem_pck.get_texto(278170);
	end if;
end if;

ds_erro_p := ds_erro_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE consistir_proc_lanc_conta ( nr_prescricao_p bigint, ds_erro_p INOUT text, nm_usuario_p text, cd_estabelecimento_p bigint) FROM PUBLIC;

