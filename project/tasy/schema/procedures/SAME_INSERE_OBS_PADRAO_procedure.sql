-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE same_insere_obs_padrao (nr_seq_observ_p bigint, nr_seq_solicitacao_p bigint, nm_usuario_p text) AS $body$
BEGIN
if (nr_seq_observ_p IS NOT NULL AND nr_seq_observ_p::text <> '') and (nr_seq_solicitacao_p IS NOT NULL AND nr_seq_solicitacao_p::text <> '') then
	update	same_solic_pront
	set	ds_observacao	= (SELECT max(x.ds_observacao) from same_observacao_padrao x where x.nr_sequencia = nr_seq_observ_p),
		nm_usuario	= nm_usuario_p,
		dt_atualizacao	= clock_timestamp()
	where	nr_sequencia	= nr_seq_solicitacao_p;
end if;
commit;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE same_insere_obs_padrao (nr_seq_observ_p bigint, nr_seq_solicitacao_p bigint, nm_usuario_p text) FROM PUBLIC;

