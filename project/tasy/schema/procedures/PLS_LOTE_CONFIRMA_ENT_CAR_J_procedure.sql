-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE pls_lote_confirma_ent_car_j ( nr_sequencia_lista_p text, nm_usuario_p text, dt_entrega_p timestamp, nm_pessoa_entrega_p text) AS $body$
DECLARE


nr_sequencia_lista_w	varchar(1000);
tam_lista_w		bigint;
ie_pos_virgula_w		smallint;
nr_sequencia_w		varchar(10);
nr_sequencia_final		bigint;


BEGIN

nr_sequencia_lista_w	:= nr_sequencia_lista_p;

while(nr_sequencia_lista_w IS NOT NULL AND nr_sequencia_lista_w::text <> '') loop
	begin
	tam_lista_w	:= length(nr_sequencia_lista_w);
	ie_pos_virgula_w	:= position(',' in nr_sequencia_lista_w);

	if (ie_pos_virgula_w <> 0) then
		begin
		nr_sequencia_w	:= substr(nr_sequencia_lista_w, 1, (ie_pos_virgula_w - 1));
		nr_sequencia_lista_w	:= substr(nr_sequencia_lista_w, (ie_pos_virgula_w + 1), tam_lista_w);

		CALL pls_lote_confirmar_entrega_car(
			nr_sequencia_final,
			dt_entrega_p,
			nm_pessoa_entrega_p,
			nm_usuario_p);
		end;
	end if;
	end;
end loop;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_lote_confirma_ent_car_j ( nr_sequencia_lista_p text, nm_usuario_p text, dt_entrega_p timestamp, nm_pessoa_entrega_p text) FROM PUBLIC;

