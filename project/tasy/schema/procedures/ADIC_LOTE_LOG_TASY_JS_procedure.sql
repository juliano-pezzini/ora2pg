-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE adic_lote_log_tasy_js ( nr_seq_lote_externo_p bigint, nm_usuario_p text) AS $body$
DECLARE


qt_exames_lote_w	bigint;


BEGIN

if (nr_seq_lote_externo_p IS NOT NULL AND nr_seq_lote_externo_p::text <> '')then
	begin

	select 	count(*)
	into STRICT	qt_exames_lote_w
	from 	prescr_procedimento
	where 	nr_seq_lote_externo = nr_seq_lote_externo_p;

	/*Insert into logxxxxxx_tasy(	dt_atualizacao,
				nm_usuario,
				cd_log,
				ds_log)
	Values(	sysdate,
		nm_usuario_p,
		55779,
		'Gerado arquivo XML. Lote: ' || nr_seq_lote_externo_p || ' Qt Exames arquivo: ' || qt_exames_lote_w);*/
	end;
end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE adic_lote_log_tasy_js ( nr_seq_lote_externo_p bigint, nm_usuario_p text) FROM PUBLIC;

