-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';



CREATE TYPE campos AS (nr_interno_conta bigint);


CREATE OR REPLACE PROCEDURE baca_eis_conta_paciente (dt_parametro_P timestamp) AS $body$
DECLARE

type Vetor is table of campos index by integer;

dt_parametro_inicio_w       	timestamp;
dt_parametro_fim_w        	timestamp;
NR_INTERNO_CONTA_W         	bigint;
i					integer;
k					integer;
Vetor_Proc_w				Vetor;

 
C01 CURSOR FOR 
SELECT a.nr_interno_conta 
from 	conta_paciente a 
where a.dt_mesano_referencia between dt_parametro_inicio_w and dt_parametro_fim_w 
 and a.ie_status_acerto 		= 2;


BEGIN 
 
dt_parametro_fim_w         := last_day(Trunc(dt_parametro_p,'dd')) + 86399/86400;
dt_parametro_Inicio_w		 	:= trunc(dt_parametro_p,'month');
 
i	:= 1;
OPEN C01;
LOOP 
FETCH C01 into	nr_interno_conta_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin 
	Vetor_Proc_w[i].nr_interno_conta := nr_interno_conta_w;
	i := i + 1;
	end;
END LOOP;
close c01;
 
i := Vetor_Proc_w.count;
for k in 1.. i loop 
	begin 
	nr_interno_conta_w	:= Vetor_Proc_w[k].nr_interno_conta;
	/*if	(trunc(k /1000,0) = (k / 1000)) then 
		insert into logxxxxx_tasy values(sysdate, 'Tasy', 999, 'Conta EIS: ' || k || '/' || i); 
	 	commit; 
	end if;*/
 
	CALL Gerar_eis_conta_Paciente(nr_interno_conta_w);
	end;
end loop;
/*insert into logxxxxx_tasy values(sysdate, 'Tasy', 999, 'Conta EIS: ' || k || '/' || i);*/
 
commit;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE baca_eis_conta_paciente (dt_parametro_P timestamp) FROM PUBLIC;

