-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE alterar_paciente_triagem ( nr_sequencia_p text, cd_pessoa_fisica_p text ) AS $body$
DECLARE

nr_seq_fila_senha_w	bigint;


BEGIN
if (nr_sequencia_p IS NOT NULL AND nr_sequencia_p::text <> '') and (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') then
	begin
	update	TRIAGEM_PRONTO_ATEND
	set	cd_pessoa_fisica = cd_pessoa_fisica_p
	where	nr_sequencia	= nr_sequencia_p;

	select	nr_seq_fila_senha
	into STRICT	nr_seq_fila_senha_w
	from	triagem_pronto_atend
	where	nr_sequencia	= nr_sequencia_p;

	IF (coalesce(nr_seq_fila_senha_w,0) > 0) then
		update	paciente_senha_fila
		set	cd_pessoa_fisica = cd_pessoa_fisica_p
		where	nr_sequencia = nr_seq_fila_senha_w;
	end if;

	end;
end if;
commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE alterar_paciente_triagem ( nr_sequencia_p text, cd_pessoa_fisica_p text ) FROM PUBLIC;

