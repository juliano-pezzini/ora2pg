-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE transf_gastos_materiais ( cd_setor_atendimento_p bigint, dt_entrada_unidade_p timestamp, dt_conta_p timestamp, dt_atendimento_p timestamp, nr_seq_atepacu_p bigint, nr_atendimento_P bigint, nr_sequencia_p bigint) AS $body$
BEGIN

if (nr_sequencia_p IS NOT NULL AND nr_sequencia_p::text <> '') then
 begin
 if (coalesce(nr_atendimento_p::text, '') = '') then
  begin
  update material_atend_paciente
  set cd_setor_atendimento = cd_setor_atendimento_p ,
   dt_entrada_unidade  = dt_entrada_unidade_p,
   dt_conta   = coalesce(dt_conta_p, dt_conta),
   dt_atendimento   = coalesce(dt_atendimento_p, dt_atendimento),
   nr_seq_atepacu  = nr_seq_atepacu_p,
   dt_atualizacao  = clock_timestamp(),
   nm_usuario  = coalesce(substr(wheb_usuario_pck.get_nm_usuario,1,15),nm_usuario)
  where nr_sequencia  = nr_sequencia_p;
  end;
 else
  begin
  update material_atend_paciente
  set cd_setor_atendimento = cd_setor_atendimento_p ,
   dt_entrada_unidade  = dt_entrada_unidade_p,
   dt_conta   = coalesce(dt_conta_p, dt_conta),
   dt_atendimento   = coalesce(dt_atendimento_p, dt_atendimento),
   nr_seq_atepacu  = nr_seq_atepacu_p,
   nr_atendimento  = nr_atendimento_p,
   dt_atualizacao  = clock_timestamp(),
   nm_usuario  = coalesce(substr(wheb_usuario_pck.get_nm_usuario,1,15),nm_usuario)
  where nr_sequencia  = nr_sequencia_p;
  end;
 end if;
 end;
 commit;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE transf_gastos_materiais ( cd_setor_atendimento_p bigint, dt_entrada_unidade_p timestamp, dt_conta_p timestamp, dt_atendimento_p timestamp, nr_seq_atepacu_p bigint, nr_atendimento_P bigint, nr_sequencia_p bigint) FROM PUBLIC;

