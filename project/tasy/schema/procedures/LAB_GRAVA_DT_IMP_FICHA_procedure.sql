-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE lab_grava_dt_imp_ficha ( nr_prescricao_p bigint, nr_seq_prescr_p bigint) AS $body$
DECLARE


nr_seq_resultado_w	bigint;


BEGIN

if (nr_prescricao_p IS NOT NULL AND nr_prescricao_p::text <> '') then

	select	max(nr_seq_resultado)
	into STRICT	nr_seq_resultado_w
	from	exame_lab_resultado
	where	nr_prescricao = nr_prescricao_p;

	if (coalesce(nr_seq_prescr_p,0) = 0)	then

		update	exame_lab_result_item
		set		dt_impressao = clock_timestamp()
		where	nr_seq_resultado = nr_seq_resultado_w;

	else

		update	exame_lab_result_item
		set		dt_impressao = clock_timestamp()
		where	nr_seq_resultado = nr_seq_resultado_w
		and		nr_seq_prescr = nr_seq_prescr_p;

	end if;

end if;

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE lab_grava_dt_imp_ficha ( nr_prescricao_p bigint, nr_seq_prescr_p bigint) FROM PUBLIC;

