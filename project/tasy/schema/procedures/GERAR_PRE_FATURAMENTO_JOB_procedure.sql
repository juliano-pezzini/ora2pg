-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE gerar_pre_faturamento_job ( dt_referencia_p timestamp, nm_usuario_p text, cd_estabelecimento_p bigint) AS $body$
DECLARE

			
l_jobid			bigint;
ds_mask_w 		varchar(255)	:= pkg_date_formaters.localize_mask('timestamp', pkg_date_formaters.getUserLanguageTag(WHEB_USUARIO_PCK.GET_CD_ESTABELECIMENTO, WHEB_USUARIO_PCK.GET_NM_USUARIO));
dt_referencia_varchar_w	varchar(255);
ds_what_w		varchar(255);

BEGIN

dt_referencia_varchar_w := to_char(dt_referencia_p, ds_mask_w);

ds_what_w := 'gerar_pre_faturamento(to_date(' || '''' || dt_referencia_varchar_w || '''' || ', ' || '''' || ds_mask_w || '''' || '), ' || '''' || nm_usuario_p || '''' || ', ' || cd_estabelecimento_p || ');';

dbms_job.submit(
l_jobid,
ds_what_w
);
commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_pre_faturamento_job ( dt_referencia_p timestamp, nm_usuario_p text, cd_estabelecimento_p bigint) FROM PUBLIC;

