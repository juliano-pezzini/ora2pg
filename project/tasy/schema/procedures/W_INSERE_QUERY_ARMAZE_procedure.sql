-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE PROCEDURE w_insere_query_armaze ( ds_query_p w_query_armazenado.ds_query%type, nm_usuario_p text) AS $body$
DECLARE


qt_consulta_w 	bigint;


BEGIN

select count(1)
into STRICT qt_consulta_w
from w_query_armazenado
where nm_usuario = nm_usuario_p;

if (qt_consulta_w > 0 ) then
	delete from w_query_armazenado
	where nm_usuario = nm_usuario_p;
end if;

insert into w_query_armazenado( ds_query, nm_usuario, dt_atualizacao, nr_sequencia )
values ( ds_query_p, nm_usuario_p, clock_timestamp(), nextval('w_query_armazenado_seq') );

commit;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE w_insere_query_armaze ( ds_query_p w_query_armazenado.ds_query%type, nm_usuario_p text) FROM PUBLIC;

