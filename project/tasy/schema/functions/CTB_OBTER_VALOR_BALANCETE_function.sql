-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION ctb_obter_valor_balancete ( dt_referencia_p timestamp, cd_conta_contabil_p text, ie_opcao_p text, cd_empresa_p bigint) RETURNS bigint AS $body$
DECLARE


vl_retorno_w		ctb_balancete_v.vl_saldo%type;
nr_seq_mes_ref_w	ctb_mes_ref.nr_sequencia%type;


BEGIN

select	max(a.nr_sequencia)
into STRICT	nr_seq_mes_ref_w
from	ctb_mes_ref a
where	trunc(a.dt_referencia,'month')	= dt_referencia_p
and	a.cd_empresa = cd_empresa_p;

if (nr_seq_mes_ref_w IS NOT NULL AND nr_seq_mes_ref_w::text <> '') then
	
	if (ie_opcao_p = 'S') then
		select	coalesce(sum(b.vl_saldo),0)
		into STRICT	vl_retorno_w
		from	ctb_balancete_v b
		where	b.nr_seq_mes_ref 	= nr_seq_mes_ref_w
		and	b.cd_conta_contabil	= cd_conta_contabil_p
		and	ie_normal_encerramento	<> 'E';
	elsif (ie_opcao_p = 'C') then
		select	coalesce(sum(b.vl_credito),0)
		into STRICT	vl_retorno_w
		from	ctb_balancete_v b
		where	b.nr_seq_mes_ref 	= nr_seq_mes_ref_w
		and	b.cd_conta_contabil	= cd_conta_contabil_p
		and	ie_normal_encerramento	<> 'E';
	elsif (ie_opcao_p = 'D') then
		select	coalesce(sum(b.vl_debito),0)
		into STRICT	vl_retorno_w
		from	ctb_balancete_v b
		where	b.nr_seq_mes_ref 	= nr_seq_mes_ref_w
		and	b.cd_conta_contabil	= cd_conta_contabil_p
		and	ie_normal_encerramento	<> 'E';
	elsif (ie_opcao_p = 'M') then
		select	coalesce(sum(b.vl_movimento),0)
		into STRICT	vl_retorno_w
		from	ctb_balancete_v b
		where	b.nr_seq_mes_ref 	= nr_seq_mes_ref_w
		and	b.cd_conta_contabil	= cd_conta_contabil_p
		and	ie_normal_encerramento	<> 'E';	
	end if;
	
else
	vl_retorno_w := 0;
end if;

return coalesce(vl_retorno_w,0);
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION ctb_obter_valor_balancete ( dt_referencia_p timestamp, cd_conta_contabil_p text, ie_opcao_p text, cd_empresa_p bigint) FROM PUBLIC;

