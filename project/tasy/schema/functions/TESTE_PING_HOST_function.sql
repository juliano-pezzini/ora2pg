-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION teste_ping_host ( p_host_name text, p_port bigint) RETURNS varchar AS $body$
DECLARE

tcpconnection utl_tcp.connection;

BEGIN
/*Se retornar "S" o sistema conseguiu pingar no IP passado*/

begin
	tcpconnection := utl_tcp.open_connection(p_host_name, p_port);
	utl_tcp.close_connection(tcpconnection);
	return 'S';
exception
when utl_tcp.network_error then

if (upper(sqlerrm) like '%HOST%') then
		return 'N';
	elsif (upper(sqlerrm) like '%LISTENER%') then
		return 'S';
	else
		return 'N';
	end if;
end;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION teste_ping_host ( p_host_name text, p_port bigint) FROM PUBLIC;

