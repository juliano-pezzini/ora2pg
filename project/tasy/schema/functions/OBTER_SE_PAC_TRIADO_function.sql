-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_pac_triado (nr_atendimento_p bigint) RETURNS varchar AS $body$
DECLARE

 
ds_retorno_w	varchar(5) := 'N';			
dt_inicio_w	timestamp;
dt_fim_w	timestamp;
			

BEGIN 
 
select	max(dt_inicio_atendimento), 
	max(dt_fim_triagem) 
into STRICT	dt_inicio_w, 
	dt_fim_w 
from	atendimento_paciente 
where	nr_atendimento = nr_atendimento_p;
 
if (dt_inicio_w IS NOT NULL AND dt_inicio_w::text <> '') and (coalesce(dt_fim_w::text, '') = '') then 
	ds_retorno_w := 'S';
end if;
 
 
return	ds_retorno_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_pac_triado (nr_atendimento_p bigint) FROM PUBLIC;

