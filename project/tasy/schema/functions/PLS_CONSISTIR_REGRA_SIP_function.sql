-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_consistir_regra_sip ( ie_materiais_p text, cd_procedimento_p text, cd_area_procedimento_p text, cd_especialidade_p text, cd_grupo_proc_p text) RETURNS varchar AS $body$
DECLARE


ds_mensagem_w			varchar(255);

BEGIN
if (ie_materiais_p = 'S')			and
	((cd_procedimento_p IS NOT NULL AND cd_procedimento_p::text <> '')	or (cd_area_procedimento_p IS NOT NULL AND cd_area_procedimento_p::text <> '')	or (cd_especialidade_p IS NOT NULL AND cd_especialidade_p::text <> '')	or (cd_grupo_proc_p IS NOT NULL AND cd_grupo_proc_p::text <> ''))		then
	ds_mensagem_w	:= 'É permitido informar somente a estrutura de procedimento ou o campo materiais.';
end if;
if (coalesce(ds_mensagem_w::text, '') = '') then
	ds_mensagem_w	:= 'X';

end if;
return ds_mensagem_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_consistir_regra_sip ( ie_materiais_p text, cd_procedimento_p text, cd_area_procedimento_p text, cd_especialidade_p text, cd_grupo_proc_p text) FROM PUBLIC;

