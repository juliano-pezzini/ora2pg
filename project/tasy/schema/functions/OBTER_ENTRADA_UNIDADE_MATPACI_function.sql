-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_entrada_unidade_matpaci (nr_seq_matpaci_p bigint) RETURNS timestamp AS $body$
DECLARE


dt_entrada_unidade_w	timestamp;



BEGIN
begin
if (coalesce(nr_seq_matpaci_p,0) > 0) then
	select 	dt_entrada_unidade
	into STRICT	dt_entrada_unidade_w
	from	material_atend_paciente
	where	nr_sequencia = nr_seq_matpaci_p;
end if;
exception
when others then
	dt_entrada_unidade_w := clock_timestamp();
end;

return	dt_entrada_unidade_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_entrada_unidade_matpaci (nr_seq_matpaci_p bigint) FROM PUBLIC;

