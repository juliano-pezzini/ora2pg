-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION get_data_external_refferal ( nr_atendimento_p bigint, si_type_record_p bigint, si_option_return_p text) RETURNS varchar AS $body$
DECLARE


/*si_type_record_p
1 - RL_REF_FROM_HOSPITAL table
2 - RL_REP_TO_HOSPITAL table
3 - RL_REF_TO_HOSPITAL table
4 - RL_REP_FROM_HOSPITAL table*/
			
			
/*si_option_return_p
SEQ	 NR_SEQUENCIA
CDD	CD_DEPARTMENT
DSD	DS_DEPARTMENT
CDW	CD_WARD
DSW	DS_WARD
CDAD	CD_ATTENDING_DOCTOR
NMAD	NM_ATTENDING_DOCTOR
IEST	IE_STATUS
DSST	DS_STATUS*/
nr_sequencia_w			RL_REF_FROM_HOSPITAL.nr_sequencia%type;
cd_departamento_w		departamento_medico.cd_departamento%type;
ds_departmento_w		departamento_medico.ds_departamento%type;
cd_setor_atendimento_w		setor_atendimento.cd_setor_atendimento%type;
ds_setor_atendimento_w		setor_atendimento.ds_setor_atendimento%type;
cd_physician_w			pessoa_fisica.cd_pessoa_fisica%type;
nm_physician_w			pessoa_fisica.nm_pessoa_fisica%type;
ie_status_w			valor_dominio.vl_dominio%type;
ds_status_w			dic_expressao.ds_expressao_br%type;
ds_return_w			varchar(255);
			

BEGIN

if (si_type_record_p = 1) then
	select	max(nr_sequencia)
	into STRICT	nr_sequencia_w
	from	RL_REF_FROM_HOSPITAL
	where	nr_atendimento = nr_atendimento_p;


	/*SEQ	 NR_SEQUENCIA*/

	if (si_option_return_p = 'SEQ') and (nr_sequencia_w > 0) then
		begin
		ds_return_w := nr_sequencia_w;
		end;
		
	/*CDD	CD_DEPARTMENT*/

	elsif (si_option_return_p = 'CDD') and (nr_sequencia_w > 0) then
		begin
		select	CD_REFERRED_DEPARTMENT
		into STRICT	cd_departamento_w
		from	RL_REF_FROM_HOSPITAL
		where	nr_sequencia = nr_sequencia_w;	
		
		ds_return_w := cd_departamento_w;
		end;
		
	/*DSD	DS_DEPARTMENT*/

	elsif (si_option_return_p = 'DSD') and (nr_sequencia_w > 0) then
		begin
		select	substr(obter_nome_departamento_medico(CD_REFERRED_DEPARTMENT),1,255)
		into STRICT	ds_departmento_w
		from	RL_REF_FROM_HOSPITAL
		where	nr_sequencia = nr_sequencia_w;	
		
		ds_return_w := ds_departmento_w;
		end;
		
	/*CDW	CD_WARD*/

	elsif (si_option_return_p = 'CDW') and (nr_sequencia_w > 0) then
		begin
		select	cd_setor_atendimento
		into STRICT	cd_setor_atendimento_w
		from	RL_REF_FROM_HOSPITAL
		where	nr_sequencia = nr_sequencia_w;	
		
		ds_return_w := cd_setor_atendimento_w;
		end;

	/*DSW	DS_WARD*/

	elsif (si_option_return_p = 'DSW') and (nr_sequencia_w > 0) then
		begin	
		select	substr(obter_nome_setor(cd_setor_atendimento),1,255)
		into STRICT	ds_setor_atendimento_w
		from	RL_REF_FROM_HOSPITAL
		where	nr_sequencia = nr_sequencia_w;	
		
		ds_return_w := ds_setor_atendimento_w;
		end;

	/*CDAD	CD_ATTENDING_DOCTOR*/

	elsif (si_option_return_p = 'CDAD') and (nr_sequencia_w > 0) then
		begin	
		
		select	CD_REFERRED_DOCTOR
		into STRICT	cd_physician_w
		from	RL_REF_FROM_HOSPITAL
		where	nr_sequencia = nr_sequencia_w;	
			
		ds_return_w := cd_physician_w;
		end;
		
	/*NMAD	NM_ATTENDING_DOCTOR*/

	elsif (si_option_return_p = 'NMAD') and (nr_sequencia_w > 0) then
		begin
		
		select	substr(max(obter_nome_medico(CD_REFERRED_DOCTOR,'N')),1,255)
		into STRICT	nm_physician_w
		from	RL_REF_FROM_HOSPITAL
		where	nr_sequencia = nr_sequencia_w;	
		
		ds_return_w := nm_physician_w;
		end;
		
	/*IEST	IE_STATUS
	DSST	DS_STATUS*/
	elsif (si_option_return_p in ('IEST','DSST')) then
		begin	
		
		if (coalesce(nr_sequencia_w,0) = 0) then
			ie_status_w := 'N'; /*Not created*/
			ds_status_w := wheb_mensagem_pck.get_Texto(1147367); /*Not created*/
		else						
			select	CASE WHEN coalesce(dt_liberacao::text, '') = '' THEN 'C'  ELSE 'F' END  /*Created or Finished*/
,
				CASE WHEN coalesce(dt_liberacao::text, '') = '' THEN wheb_mensagem_pck.get_Texto(1147368)  ELSE wheb_mensagem_pck.get_Texto(1163070) END  /*Created or Finished*/
			into STRICT	ie_status_w,
				ds_status_w
			from	rl_ref_from_hospital
			where	nr_sequencia = nr_sequencia_w;	
			
			--(A,N,C,F,D,O)(All,Not created,Created,Finished,Delivered,Other than finished and delivered)
			
		end if;	
		
		if (si_option_return_p = 'IEST') then
			ds_return_w := ie_status_w;
		elsif (si_option_return_p = 'DSST') then
			ds_return_w := ds_status_w;
		end if;	
		end;	
	end if;

	
	
elsif (si_type_record_p = 2) then
	select	max(nr_sequencia)
	into STRICT	nr_sequencia_w
	from	RL_REP_TO_HOSPITAL
	where	nr_atendimento = nr_atendimento_p;


	/*SEQ	 NR_SEQUENCIA*/

	if (si_option_return_p = 'SEQ') and (nr_sequencia_w > 0) then
		begin
		ds_return_w := nr_sequencia_w;
		end;
		
	/*CDD	CD_DEPARTMENT*/

	elsif (si_option_return_p = 'CDD') and (nr_sequencia_w > 0) then
		begin
		select	CD_REFERRED_DEPARTMENT
		into STRICT	cd_departamento_w
		from	RL_REP_TO_HOSPITAL
		where	nr_sequencia = nr_sequencia_w;	
		
		ds_return_w := cd_departamento_w;
		end;
		
	/*DSD	DS_DEPARTMENT*/

	elsif (si_option_return_p = 'DSD') and (nr_sequencia_w > 0) then
		begin
		select	substr(obter_nome_departamento_medico(CD_REFERRED_DEPARTMENT),1,255)
		into STRICT	ds_departmento_w
		from	RL_REP_TO_HOSPITAL
		where	nr_sequencia = nr_sequencia_w;	
		
		ds_return_w := ds_departmento_w;
		end;
		
	/*CDW	CD_WARD*/

	elsif (si_option_return_p = 'CDW') and (nr_sequencia_w > 0) then
		begin
		select	cd_setor_atendimento
		into STRICT	cd_setor_atendimento_w
		from	RL_REP_TO_HOSPITAL
		where	nr_sequencia = nr_sequencia_w;	
		
		ds_return_w := cd_setor_atendimento_w;
		end;

	/*DSW	DS_WARD*/

	elsif (si_option_return_p = 'DSW') and (nr_sequencia_w > 0) then
		begin	
		select	substr(obter_nome_setor(cd_setor_atendimento),1,255)
		into STRICT	ds_setor_atendimento_w
		from	RL_REP_TO_HOSPITAL
		where	nr_sequencia = nr_sequencia_w;	
		
		ds_return_w := ds_setor_atendimento_w;
		end;

	/*CDAD	CD_ATTENDING_DOCTOR*/

	elsif (si_option_return_p = 'CDAD') and (nr_sequencia_w > 0) then
		begin	
		
		select	CD_REFERRING_DOCTOR
		into STRICT	cd_physician_w
		from	RL_REP_TO_HOSPITAL
		where	nr_sequencia = nr_sequencia_w;	
			
		ds_return_w := cd_physician_w;
		end;
		
	/*NMAD	NM_ATTENDING_DOCTOR*/

	elsif (si_option_return_p = 'NMAD') and (nr_sequencia_w > 0) then
		begin
		
		select	substr(max(obter_nome_medico(CD_REFERRING_DOCTOR,'N')),1,255)
		into STRICT	nm_physician_w
		from	RL_REP_TO_HOSPITAL
		where	nr_sequencia = nr_sequencia_w;	
		
		ds_return_w := nm_physician_w;
		end;
		
	/*IEST	IE_STATUS
	DSST	DS_STATUS*/
	elsif (si_option_return_p in ('IEST','DSST')) then
		begin	
		
		if (coalesce(nr_sequencia_w,0) = 0) then
			ie_status_w := 'N'; /*Not created*/
			ds_status_w := wheb_mensagem_pck.get_Texto(1147367); /*Not created*/
		else						
			select	CASE WHEN coalesce(IE_DELIVERY_TYPE::text, '') = '' THEN CASE WHEN coalesce(dt_liberacao::text, '') = '' THEN 'C'  ELSE 'F' END   ELSE 'D' END  /*Created or Finished*/
,
				CASE WHEN coalesce(IE_DELIVERY_TYPE::text, '') = '' THEN CASE WHEN coalesce(dt_liberacao::text, '') = '' THEN wheb_mensagem_pck.get_Texto(1147368)  ELSE wheb_mensagem_pck.get_Texto(1163070) END   ELSE wheb_mensagem_pck.get_Texto(1163111) END  /*Created or Finished or Deliveried*/
			into STRICT	ie_status_w,
				ds_status_w
			from	RL_REP_TO_HOSPITAL
			where	nr_sequencia = nr_sequencia_w;
			
			--(A,N,C,F,D,O)(All,Not created,Created,Finished,Delivered,Other than finished and delivered)
			
		end if;	
		
		if (si_option_return_p = 'IEST') then
			ds_return_w := ie_status_w;
		elsif (si_option_return_p = 'DSST') then
			ds_return_w := ds_status_w;
		end if;	
		end;	
	end if;
	
	
elsif (si_type_record_p = 3) then
	select	max(nr_sequencia)
	into STRICT	nr_sequencia_w
	from	RL_REF_TO_HOSPITAL
	where	nr_atendimento = nr_atendimento_p;


	/*SEQ	 NR_SEQUENCIA*/

	if (si_option_return_p = 'SEQ') and (nr_sequencia_w > 0) then
		begin
		ds_return_w := nr_sequencia_w;
		end;
		
	/*CDD	CD_DEPARTMENT*/

	elsif (si_option_return_p = 'CDD') and (nr_sequencia_w > 0) then
		begin
		select	CD_REFERRED_DEPARTMENT
		into STRICT	cd_departamento_w
		from	RL_REF_TO_HOSPITAL
		where	nr_sequencia = nr_sequencia_w;	
		
		ds_return_w := cd_departamento_w;
		end;
		
	/*DSD	DS_DEPARTMENT*/

	elsif (si_option_return_p = 'DSD') and (nr_sequencia_w > 0) then
		begin
		select	substr(obter_nome_departamento_medico(CD_REFERRED_DEPARTMENT),1,255)
		into STRICT	ds_departmento_w
		from	RL_REF_TO_HOSPITAL
		where	nr_sequencia = nr_sequencia_w;	
		
		ds_return_w := ds_departmento_w;
		end;
		
	/*CDW	CD_WARD*/

	elsif (si_option_return_p = 'CDW') and (nr_sequencia_w > 0) then
		begin
		select	cd_setor_atendimento
		into STRICT	cd_setor_atendimento_w
		from	RL_REF_TO_HOSPITAL
		where	nr_sequencia = nr_sequencia_w;	
		
		ds_return_w := cd_setor_atendimento_w;
		end;

	/*DSW	DS_WARD*/

	elsif (si_option_return_p = 'DSW') and (nr_sequencia_w > 0) then
		begin	
		select	substr(obter_nome_setor(cd_setor_atendimento),1,255)
		into STRICT	ds_setor_atendimento_w
		from	RL_REF_TO_HOSPITAL
		where	nr_sequencia = nr_sequencia_w;	
		
		ds_return_w := ds_setor_atendimento_w;
		end;

	/*CDAD	CD_ATTENDING_DOCTOR*/

	elsif (si_option_return_p = 'CDAD') and (nr_sequencia_w > 0) then
		begin	
		
		select	CD_REFERRING_DOCTOR
		into STRICT	cd_physician_w
		from	RL_REF_TO_HOSPITAL
		where	nr_sequencia = nr_sequencia_w;	
			
		ds_return_w := cd_physician_w;
		end;
		
	/*NMAD	NM_ATTENDING_DOCTOR*/

	elsif (si_option_return_p = 'NMAD') and (nr_sequencia_w > 0) then
		begin
		
		select	substr(max(obter_nome_medico(CD_REFERRING_DOCTOR,'N')),1,255)
		into STRICT	nm_physician_w
		from	RL_REF_TO_HOSPITAL
		where	nr_sequencia = nr_sequencia_w;	
		
		ds_return_w := nm_physician_w;
		end;
		
	/*IEST	IE_STATUS
	DSST	DS_STATUS*/
	elsif (si_option_return_p in ('IEST','DSST')) then
		begin	
		
		if (coalesce(nr_sequencia_w,0) = 0) then
			ie_status_w := 'N'; /*Not created*/
			ds_status_w := wheb_mensagem_pck.get_Texto(1147367); /*Not created*/
		else						
			select	CASE WHEN coalesce(dt_liberacao::text, '') = '' THEN 'C'  ELSE 'F' END  /*Created or Finished*/
,
				CASE WHEN coalesce(dt_liberacao::text, '') = '' THEN wheb_mensagem_pck.get_Texto(1147368)  ELSE wheb_mensagem_pck.get_Texto(1163070) END  /*Created or Finished*/
			into STRICT	ie_status_w,
				ds_status_w
			from	RL_REF_TO_HOSPITAL
			where	nr_sequencia = nr_sequencia_w;	
			
			--(A,N,C,F,D,O)(All,Not created,Created,Finished,Delivered,Other than finished and delivered)
			
		end if;	
		
		if (si_option_return_p = 'IEST') then
			ds_return_w := ie_status_w;
		elsif (si_option_return_p = 'DSST') then
			ds_return_w := ds_status_w;
		end if;	
		end;	
	end if;	
	
elsif (si_type_record_p = 4) then
	select	max(nr_sequencia)
	into STRICT	nr_sequencia_w
	from	RL_REP_FROM_HOSPITAL
	where	nr_atendimento = nr_atendimento_p;


	/*SEQ	 NR_SEQUENCIA*/

	if (si_option_return_p = 'SEQ') and (nr_sequencia_w > 0) then
		begin
		ds_return_w := nr_sequencia_w;
		end;
		
	/*CDD	CD_DEPARTMENT*/

	elsif (si_option_return_p = 'CDD') and (nr_sequencia_w > 0) then
		begin
		select	CD_REFERRED_DEPARTMENT
		into STRICT	cd_departamento_w
		from	RL_REP_FROM_HOSPITAL
		where	nr_sequencia = nr_sequencia_w;	
		
		ds_return_w := cd_departamento_w;
		end;
		
	/*DSD	DS_DEPARTMENT*/

	elsif (si_option_return_p = 'DSD') and (nr_sequencia_w > 0) then
		begin
		select	substr(obter_nome_departamento_medico(CD_REFERRED_DEPARTMENT),1,255)
		into STRICT	ds_departmento_w
		from	RL_REP_FROM_HOSPITAL
		where	nr_sequencia = nr_sequencia_w;	
		
		ds_return_w := ds_departmento_w;
		end;
		
	/*CDW	CD_WARD*/

	elsif (si_option_return_p = 'CDW') and (nr_sequencia_w > 0) then
		begin
		select	cd_setor_atendimento
		into STRICT	cd_setor_atendimento_w
		from	RL_REP_FROM_HOSPITAL
		where	nr_sequencia = nr_sequencia_w;	
		
		ds_return_w := cd_setor_atendimento_w;
		end;

	/*DSW	DS_WARD*/

	elsif (si_option_return_p = 'DSW') and (nr_sequencia_w > 0) then
		begin	
		select	substr(obter_nome_setor(cd_setor_atendimento),1,255)
		into STRICT	ds_setor_atendimento_w
		from	RL_REP_FROM_HOSPITAL
		where	nr_sequencia = nr_sequencia_w;	
		
		ds_return_w := ds_setor_atendimento_w;
		end;

	/*CDAD	CD_ATTENDING_DOCTOR*/

	elsif (si_option_return_p = 'CDAD') and (nr_sequencia_w > 0) then
		begin	
		
		select	CD_REFERRED_DOCTOR
		into STRICT	cd_physician_w
		from	RL_REP_FROM_HOSPITAL
		where	nr_sequencia = nr_sequencia_w;	
			
		ds_return_w := cd_physician_w;
		end;
		
	/*NMAD	NM_ATTENDING_DOCTOR*/

	elsif (si_option_return_p = 'NMAD') and (nr_sequencia_w > 0) then
		begin
		
		select	substr(max(obter_nome_medico(CD_REFERRED_DOCTOR,'N')),1,255)
		into STRICT	nm_physician_w
		from	RL_REP_FROM_HOSPITAL
		where	nr_sequencia = nr_sequencia_w;	
		
		ds_return_w := nm_physician_w;
		end;
		
	/*IEST	IE_STATUS
	DSST	DS_STATUS*/
	elsif (si_option_return_p in ('IEST','DSST')) then
		begin	
		
		if (coalesce(nr_sequencia_w,0) = 0) then
			ie_status_w := 'N'; /*Not created*/
			ds_status_w := wheb_mensagem_pck.get_Texto(1147367); /*Not created*/
		else						
			select	CASE WHEN coalesce(dt_liberacao::text, '') = '' THEN 'C'  ELSE 'F' END  /*Created or Finished*/
,
				CASE WHEN coalesce(dt_liberacao::text, '') = '' THEN wheb_mensagem_pck.get_Texto(1147368)  ELSE wheb_mensagem_pck.get_Texto(1163070) END  /*Created or Finished*/
			into STRICT	ie_status_w,
				ds_status_w
			from	RL_REP_FROM_HOSPITAL
			where	nr_sequencia = nr_sequencia_w;	
			
			--(A,N,C,F,D,O)(All,Not created,Created,Finished,Delivered,Other than finished and delivered)
			
		end if;	
		
		if (si_option_return_p = 'IEST') then
			ds_return_w := ie_status_w;
		elsif (si_option_return_p = 'DSST') then
			ds_return_w := ds_status_w;
		end if;	
		end;	
	end if;	
	
end if;

return	ds_return_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION get_data_external_refferal ( nr_atendimento_p bigint, si_type_record_p bigint, si_option_return_p text) FROM PUBLIC;

