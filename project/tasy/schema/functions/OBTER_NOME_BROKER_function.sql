-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_nome_broker (nr_seq_broker_p bigint) RETURNS varchar AS $body$
DECLARE


nm_broker_w		convenio_broker.nm_broker%type;


BEGIN

if (nr_seq_broker_p IS NOT NULL AND nr_seq_broker_p::text <> '') then
	begin
	select  nm_broker
	into STRICT	nm_broker_w
	from    convenio_broker
	where   nr_sequencia = nr_seq_broker_p;

	end;
end if;

return	nm_broker_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_nome_broker (nr_seq_broker_p bigint) FROM PUBLIC;

