-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_estab_ciclo (nr_seq_config_ciclo_p bigint) RETURNS bigint AS $body$
DECLARE


cd_estabelecimento_w	bigint;


BEGIN

select	max(cd_estabelecimento)
into STRICT	cd_estabelecimento_w
from	CONFIG_CICLO_CHAMADA
where	nr_sequencia = nr_seq_config_ciclo_p;

if (coalesce(cd_estabelecimento_w::text, '') = '') then
	cd_estabelecimento_w := obter_estabelecimento_ativo;
end if;

return	coalesce(cd_estabelecimento_w, 1);

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_estab_ciclo (nr_seq_config_ciclo_p bigint) FROM PUBLIC;

