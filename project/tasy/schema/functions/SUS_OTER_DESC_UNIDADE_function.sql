-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION sus_oter_desc_unidade (cd_unidade_p text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(200);


BEGIN

if (cd_unidade_p IS NOT NULL AND cd_unidade_p::text <> '') then
	begin

	select	ds_razao_social
	into STRICT	ds_retorno_w
	from	sus_s_ups
	where	cd_ups = cd_unidade_p;

	end;
end if;

RETURN ds_retorno_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION sus_oter_desc_unidade (cd_unidade_p text) FROM PUBLIC;

