-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION fin_obter_destino_gv ( nr_seq_viagem_p bigint) RETURNS varchar AS $body$
DECLARE

 
ds_retorno_w		varchar(255);


BEGIN 
select	coalesce(substr(obter_dados_pf_pj(null,a.cd_cnpj,'N'),1,60),a.ds_destino) 
into STRICT	ds_retorno_w 
from	via_destino a 
where	a.nr_sequencia = (	SELECT	max(x.nr_sequencia) 
				from	via_destino x 
				where	x.nr_seq_viagem = nr_seq_viagem_p);
 
return	ds_retorno_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION fin_obter_destino_gv ( nr_seq_viagem_p bigint) FROM PUBLIC;

