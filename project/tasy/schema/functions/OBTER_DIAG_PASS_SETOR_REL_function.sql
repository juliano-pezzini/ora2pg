-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_diag_pass_setor_rel (ds_filtro_p text) RETURNS varchar AS $body$
DECLARE


ds_filtro_w	varchar(4000) := '';
qt_tamanho_w	bigint;
qt_inicial_w	bigint;
qt_final_w	bigint;


BEGIN

SELECT	LENGTH(ds_filtro_p)
INTO STRICT	qt_tamanho_w
;

SELECT	position('w' in ds_filtro_p)
INTO STRICT	qt_inicial_w
;

IF (qt_inicial_w > 0) THEN
    SELECT	SUBSTR(ds_filtro_p,qt_inicial_w,qt_tamanho_w)
    INTO STRICT		ds_filtro_w
;

    SELECT	position(')' in ds_filtro_w)
    INTO STRICT		qt_final_w
;

    SELECT	SUBSTR(ds_filtro_w,1,qt_final_w)
    INTO STRICT		ds_filtro_w
;

END IF;

RETURN	ds_filtro_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_diag_pass_setor_rel (ds_filtro_p text) FROM PUBLIC;

