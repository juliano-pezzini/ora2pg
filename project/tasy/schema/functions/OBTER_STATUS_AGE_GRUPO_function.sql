-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_status_age_grupo (nr_seq_horario_p bigint, cd_agenda_p bigint, dt_agenda_p timestamp) RETURNS varchar AS $body$
DECLARE



ds_retorno_w				varchar(1) := 'L';	


BEGIN


if (coalesce(nr_seq_horario_p,0) <> 0) and (coalesce(cd_agenda_p, 0) <> 0) and (dt_agenda_p IS NOT NULL AND dt_agenda_p::text <> '') then
	
	select coalesce(max('L'),'N')
	into STRICT	ds_retorno_w
	from	agenda_paciente
	where   cd_agenda = cd_agenda_p
	and		nr_seq_horario = nr_seq_horario_p
	and     pkg_date_utils.start_of(dt_agenda, 'dd') = dt_agenda_p
	and		ie_status_agenda = 'L';
end if;

return ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_status_age_grupo (nr_seq_horario_p bigint, cd_agenda_p bigint, dt_agenda_p timestamp) FROM PUBLIC;

