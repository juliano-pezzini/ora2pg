-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_desc_tipo_consulta (nr_seq_tipo_consulta_p bigint) RETURNS varchar AS $body$
DECLARE

ds_tipo_consulta_w	varchar(255);

BEGIN

if (coalesce(nr_seq_tipo_consulta_p,0) > 0) then
	Select	substr(MAX(ds_tipo_consulta),1,255)
	into STRICT	ds_tipo_consulta_w
	from 	oft_tipo_consulta
	where 	nr_sequencia = nr_seq_tipo_consulta_p;

end if;

return	ds_tipo_consulta_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_desc_tipo_consulta (nr_seq_tipo_consulta_p bigint) FROM PUBLIC;

