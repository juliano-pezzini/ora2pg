-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_setor_sv_esp_ocup ( cd_setor_atendimento_p text, cd_unidade_basica_p text, cd_unidade_compl_p text) RETURNS varchar AS $body$
DECLARE


ie_ocup    varchar(1) := 'N';
nr_count_temp  integer;
nr_pac_temp  integer;


BEGIN

SELECT   count(*)
into STRICT     nr_count_temp
FROM     ocupacao_serv_esp_v
where  cd_setor_atendimento = cd_setor_atendimento_p
and  cd_unidade_basica = cd_unidade_basica_p
and  cd_unidade_compl = cd_unidade_compl_p;

select max(coalesce(QT_PAC_UNIDADE,-1))
into STRICT nr_pac_temp
from unidade_atendimento
where cd_setor_atendimento = cd_setor_atendimento_p
and cd_unidade_basica = cd_unidade_basica_p
and cd_unidade_compl = cd_unidade_compl_p;

if (nr_pac_temp > -1) and (nr_count_temp >= nr_pac_temp) then
	ie_ocup:= 'S';
else
	ie_ocup := 'N';
end if;

return ie_ocup;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_setor_sv_esp_ocup ( cd_setor_atendimento_p text, cd_unidade_basica_p text, cd_unidade_compl_p text) FROM PUBLIC;

