-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_tabela_proc_guia ( nr_seq_guia_p bigint, cd_estabelecimento_p bigint, nr_seq_prestador_p bigint, cd_procedimento_p bigint, ie_origem_proced_p bigint, dt_vigencia_p timestamp) RETURNS varchar AS $body$
DECLARE


cd_retorno_w			varchar(5);
nr_seq_pacote_w			bigint;


BEGIN

cd_retorno_w	:= substr(pls_obter_tabela_edicao(cd_estabelecimento_p, nr_seq_prestador_p, cd_procedimento_p, ie_origem_proced_p, dt_vigencia_p),1,2);

begin
select	nr_seq_pacote
into STRICT	nr_seq_pacote_w
from	pls_guia_plano_proc
where	nr_seq_guia		= nr_seq_guia_p
and	cd_procedimento		= cd_procedimento_p
and	ie_origem_proced	= ie_origem_proced_p;
exception
when others then
	nr_seq_pacote_w	:= null;
end;

if (nr_seq_pacote_w IS NOT NULL AND nr_seq_pacote_w::text <> '') then
	cd_retorno_w	:= '98';
end if;

return	cd_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_tabela_proc_guia ( nr_seq_guia_p bigint, cd_estabelecimento_p bigint, nr_seq_prestador_p bigint, cd_procedimento_p bigint, ie_origem_proced_p bigint, dt_vigencia_p timestamp) FROM PUBLIC;

