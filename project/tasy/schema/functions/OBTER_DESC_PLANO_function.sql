-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_desc_plano ( cd_convenio_p bigint, cd_plano_p text) RETURNS varchar AS $body$
DECLARE



ds_plano_w		varchar(80);


BEGIN

--729347 = Sem Plano
select	coalesce(max(ds_plano),obter_desc_expressao(729347))
into STRICT	ds_plano_w
from	Convenio_Plano
where	cd_convenio	= coalesce(cd_convenio_p,0)
and	cd_plano	= coalesce(cd_plano_p,'0');

return	ds_plano_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_desc_plano ( cd_convenio_p bigint, cd_plano_p text) FROM PUBLIC;

