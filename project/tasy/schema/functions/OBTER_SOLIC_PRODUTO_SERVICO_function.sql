-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_solic_produto_servico ( nr_solic_compra_p bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w		varchar(80);
qt_registros_w		bigint;


BEGIN

ds_retorno_w	:= wheb_mensagem_pck.get_Texto(736046); /*Produto*/
select	count(*)
into STRICT	qt_registros_w
from	solic_compra_item a,
	material b
where	a.cd_material = b.cd_material
and	a.nr_solic_compra = nr_solic_compra_p
and	b.ie_tipo_material = 4;

if (qt_registros_w > 0) then
	ds_retorno_w	:= wheb_mensagem_pck.get_Texto(736047); /*Serviço*/
end if;


return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_solic_produto_servico ( nr_solic_compra_p bigint) FROM PUBLIC;

