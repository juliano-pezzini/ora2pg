-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION sus_obter_cbo_propaci ( nr_sequencia_p bigint, ie_opcao_p text) RETURNS varchar AS $body$
DECLARE


cd_cbo_executor_w		varchar(6);

BEGIN
select	max(coalesce(cd_cbo, coalesce(sus_obter_cbo_medico(coalesce(cd_medico_executor,cd_pessoa_fisica), cd_procedimento, dt_procedimento, 0),'0')))
into STRICT	cd_cbo_executor_w
from	procedimento_paciente
where	nr_sequencia	= nr_sequencia_p;

if (ie_opcao_p	= 'C') then
	return		cd_cbo_executor_w;
else
	return		coalesce(sus_obter_desc_cbo(cd_cbo_executor_w),'Não informado');

end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION sus_obter_cbo_propaci ( nr_sequencia_p bigint, ie_opcao_p text) FROM PUBLIC;

