-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_tipo_regra_data_preco ( ie_tipo_preco_p text, cd_convenio_p bigint) RETURNS varchar AS $body$
DECLARE


ie_regra_w			varchar(1);

c01 CURSOR FOR
	SELECT coalesce(ie_regra,'X')
	from	Convenio_regra_Data_Preco
	where 	coalesce(cd_convenio, cd_convenio_p) = cd_convenio_p
	and	ie_tipo_preco			= ie_tipo_preco_p
	order by 	coalesce(cd_convenio, 0),
		1 desc;


BEGIN

open	c01;
loop
fetch	c01 into ie_regra_w;
	EXIT WHEN NOT FOUND; /* apply on c01 */
	begin

	ie_regra_w	:= ie_regra_w;

	end;
end loop;
close c01;

RETURN ie_regra_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_tipo_regra_data_preco ( ie_tipo_preco_p text, cd_convenio_p bigint) FROM PUBLIC;

