-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_regra_uso_qtde_mat (nr_sequencia_p bigint, ie_opcao_p text) RETURNS varchar AS $body$
DECLARE

 
/* 
ie_opcao_p		1 - IE_ACAO_EXCESSO 
			2 - QT_EXCEDIDA 
			3 - DS_ERRO 
*/
 
 
ds_retorno_w		varchar(255);
nr_atendimento_w	bigint;
CD_MATERIAL_w		bigint;
ie_acao_excesso_w	varchar(5);
ds_erro_w		varchar(255);
qt_excedida_w		double precision;

 

BEGIN 
 
select	nr_atendimento, 
	cd_material 
into STRICT	nr_atendimento_w, 
	cd_material_w 
from	material_atend_paciente 
where	nr_sequencia	= nr_sequencia_p;
 
SELECT * FROM obter_regra_qtde_mat(NR_ATENDIMENTO_w, CD_MATERIAL_w, NR_SEQUENCIA_p, IE_ACAO_EXCESSO_w, DS_ERRO_w, QT_EXCEDIDA_w) INTO STRICT IE_ACAO_EXCESSO_w, DS_ERRO_w, QT_EXCEDIDA_w;
 
if (ie_opcao_p	= '1') then 
	ds_retorno_w	:= IE_ACAO_EXCESSO_w;
elsif (ie_opcao_p	= '2') then 
	ds_retorno_w	:= qt_excedida_w;
else 
	ds_retorno_w	:= ds_erro_w;
end if;
 
return	ds_retorno_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_regra_uso_qtde_mat (nr_sequencia_p bigint, ie_opcao_p text) FROM PUBLIC;

