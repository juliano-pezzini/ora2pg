-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_qt_titulo_lote_mens (nr_seq_lote_p bigint) RETURNS bigint AS $body$
DECLARE


qt_retorno_w	bigint	:= null;


BEGIN

if (nr_seq_lote_p IS NOT NULL AND nr_seq_lote_p::text <> '') then
	select	count(*)
	into STRICT	qt_retorno_w
	from	titulo_receber b,
		pls_mensalidade a
	where	a.nr_sequencia = b.nr_seq_mensalidade
	and	a.nr_seq_lote = nr_seq_lote_p;
end if;

return	qt_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_qt_titulo_lote_mens (nr_seq_lote_p bigint) FROM PUBLIC;

