-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_setor_prescr_regra (cd_perfil_p bigint) RETURNS varchar AS $body$
DECLARE


cd_setor_atendimento_w	rep_regra_setor_prescr.cd_setor_atendimento%type;


BEGIN

select	max(cd_setor_atendimento)
into STRICT	cd_setor_atendimento_w
from	rep_regra_setor_prescr
where	coalesce(cd_perfil, cd_perfil_p) = cd_perfil_p
and 	coalesce(cd_estabelecimento, wheb_usuario_pck.get_cd_estabelecimento) = wheb_usuario_pck.get_cd_estabelecimento;

return	cd_setor_atendimento_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_setor_prescr_regra (cd_perfil_p bigint) FROM PUBLIC;

