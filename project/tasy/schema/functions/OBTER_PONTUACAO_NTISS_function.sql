-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_pontuacao_ntiss (nr_atendimento_p bigint) RETURNS bigint AS $body$
DECLARE


qt_pontuacao_w		numeric(20);

BEGIN

select	max(a.qt_pto_total)
into STRICT	qt_pontuacao_w
from	escala_ntiss a
where	a.nr_sequencia = ( SELECT max(b.nr_sequencia)
			   from	  escala_ntiss b
			   where  b.nr_atendimento = nr_atendimento_p);

return	qt_pontuacao_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_pontuacao_ntiss (nr_atendimento_p bigint) FROM PUBLIC;

