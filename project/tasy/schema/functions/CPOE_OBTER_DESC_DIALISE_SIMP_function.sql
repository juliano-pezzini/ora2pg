-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION cpoe_obter_desc_dialise_simp ( ie_tipo_dialise_p text, ie_hemodialise_p text, ie_tipo_peritoneal_p text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w				varchar(500);

	function desc_peritoneal return text is
		;
BEGIN
			if (ie_tipo_peritoneal_p = 'CAPD') then
				return ' ' || obter_desc_expressao(689353, null) || ' ';
			elsif (ie_tipo_peritoneal_p = 'DPA') then
				return ' ' || obter_desc_expressao(689355, null) || ' ';
			elsif (ie_tipo_peritoneal_p = 'DPI') then
				return ' ' || obter_desc_expressao(292175, null) || ' ';
			end if;

			return null;
		end;

begin

if (ie_tipo_dialise_p = 'DI') then
	-- Hemodiálise
	if (ie_hemodialise_p = 'S') then
		ds_retorno_w	:= obter_desc_expressao(489925, '') ||' ';
	else
		ds_retorno_w	:= obter_desc_expressao(489926, '') ||' ';
	end if;
elsif (ie_tipo_dialise_p = 'DP') then
	-- Diálise peritoneal
	ds_retorno_w	:= initcap(obter_desc_expressao(314156, null) || desc_peritoneal());
end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION cpoe_obter_desc_dialise_simp ( ie_tipo_dialise_p text, ie_hemodialise_p text, ie_tipo_peritoneal_p text) FROM PUBLIC;

