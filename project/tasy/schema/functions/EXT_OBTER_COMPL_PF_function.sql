-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';



--
-- dblink wrapper to call function ext_obter_compl_pf as an autonomous transaction
--
CREATE EXTENSION IF NOT EXISTS dblink;

CREATE OR REPLACE FUNCTION ext_obter_compl_pf (cd_pessoa_fisica_p text, ie_tipo_complemento_p bigint, ie_campo_p text) RETURNS varchar AS $body$
DECLARE
	-- Change this to reflect the dblink connection string
	v_conn_str  text := format('port=%s dbname=%s user=%s', current_setting('port'), current_database(), current_user);
	v_query     text;

	v_ret	varchar;
BEGIN
	v_query := 'SELECT * FROM ext_obter_compl_pf_atx ( ' || quote_nullable(cd_pessoa_fisica_p) || ',' || quote_nullable(ie_tipo_complemento_p) || ',' || quote_nullable(ie_campo_p) || ' )';
	SELECT * INTO v_ret FROM dblink(v_conn_str, v_query) AS p (ret varchar);
	RETURN v_ret;

END;
$body$ LANGUAGE plpgsql SECURITY DEFINER;




CREATE OR REPLACE FUNCTION ext_obter_compl_pf_atx (cd_pessoa_fisica_p text, ie_tipo_complemento_p bigint, ie_campo_p text) RETURNS varchar AS $body$
DECLARE
ds_retorno_w	varchar(2000);


BEGIN

if (ie_campo_p = 'NM_CONTATO') then

	select	max(a.nm_contato)
	into STRICT	ds_retorno_w
	from	compl_pessoa_fisica a
	where	a.cd_pessoa_fisica = cd_pessoa_fisica_p
	and		a.ie_tipo_complemento = ie_tipo_complemento_p;

end if;


return ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION ext_obter_compl_pf (cd_pessoa_fisica_p text, ie_tipo_complemento_p bigint, ie_campo_p text) FROM PUBLIC; -- REVOKE ALL ON FUNCTION ext_obter_compl_pf_atx (cd_pessoa_fisica_p text, ie_tipo_complemento_p bigint, ie_campo_p text) FROM PUBLIC;

