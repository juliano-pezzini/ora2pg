-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_copia_acm ( cd_setor_atendimento_p bigint) RETURNS varchar AS $body$
DECLARE


ie_copiar_w	varchar(1);

c01 CURSOR FOR
SELECT	max(ie_copia_acm)
from	regra_copia_rep_ACM
where	((cd_setor_atendimento = cd_setor_atendimento_p) or (coalesce(cd_setor_atendimento::text, '') = ''))
order by coalesce(cd_setor_atendimento,0);


BEGIN

open C01;
loop
fetch C01 into
	ie_copiar_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	ie_copiar_w	:= ie_copiar_w;
end loop;
close C01;

return	ie_copiar_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_copia_acm ( cd_setor_atendimento_p bigint) FROM PUBLIC;

