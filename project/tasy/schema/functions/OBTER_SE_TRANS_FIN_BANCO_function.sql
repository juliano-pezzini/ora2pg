-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_trans_fin_banco (nr_seq_conta_banco_p bigint, nr_seq_trans_financ_p bigint) RETURNS varchar AS $body$
DECLARE


cont_w		integer;
ds_retorno_w	varchar(2);


BEGIN

select	count(*)
into STRICT	cont_w
from	trans_financ_banco
where	nr_seq_trans_financ	= nr_seq_trans_financ_p;

ds_retorno_w	:= 'S';

if (cont_w > 0) then
	select	count(*)
	into STRICT	cont_w
	from	trans_financ_banco
	where	nr_seq_conta_banco	= nr_seq_conta_banco_p
	and	nr_seq_trans_financ	= nr_seq_trans_financ_p;

	if (cont_w = 0) then
		ds_retorno_w	:= 'N';
	end if;

end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_trans_fin_banco (nr_seq_conta_banco_p bigint, nr_seq_trans_financ_p bigint) FROM PUBLIC;

