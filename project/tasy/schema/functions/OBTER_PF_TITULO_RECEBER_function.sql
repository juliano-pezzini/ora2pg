-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_pf_titulo_receber (nr_titulo_p bigint, ie_opcao_p text) RETURNS varchar AS $body$
DECLARE

 
cd_pessoa_fisica_w	varchar(10);
ds_retorno_w		varchar(80);


BEGIN 
 
if (nr_titulo_p IS NOT NULL AND nr_titulo_p::text <> '') then 
	select	cd_pessoa_fisica 
	into STRICT	cd_pessoa_fisica_w 
	from	titulo_receber 
	where	nr_titulo	= nr_titulo_p;
 
	if (coalesce(ie_opcao_p,'C') = 'D') then 
		select	obter_nome_pf_pj(cd_pessoa_fisica_w, null) 
		into STRICT	ds_retorno_w 
		;
	else 
		ds_retorno_w	:= cd_pessoa_fisica_w;
	end if;
end if;
 
RETURN	ds_retorno_w;
 
END	;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_pf_titulo_receber (nr_titulo_p bigint, ie_opcao_p text) FROM PUBLIC;

