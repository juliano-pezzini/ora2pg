-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_mostra_medic_perfil ( nr_seq_medic_p bigint, cd_perfil_p bigint) RETURNS varchar AS $body$
DECLARE


qt_total_w	integer;
ie_mostra_w	varchar(1);


BEGIN

/* Verifica se tem algum perfil informada */

begin
select	count(*)
into STRICT	qt_total_w
from	med_medic_padrao_perfil
where	nr_seq_medic		= nr_seq_medic_p;
exception
	when others then
	qt_total_w 		:= 0;
end;

/* Caso não tenha nenhuma informada, sai liberando o medicamento para o medico */

if (qt_total_w = 0) then
	qt_total_w		:= 1;
else
	begin
	select	count(*)
	into STRICT	qt_total_w
	from	med_medic_padrao_perfil
	where	nr_seq_medic		= nr_seq_medic_p
	and	cd_perfil		= cd_perfil_p;
	exception
		when others then
		qt_total_w 		:= 0;
	end;
end if;

if (qt_total_w > 0) then
	ie_mostra_w		:= 'S';
else
	ie_mostra_w		:= 'N';
end if;

return ie_mostra_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_mostra_medic_perfil ( nr_seq_medic_p bigint, cd_perfil_p bigint) FROM PUBLIC;

