-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_conv_lib_agecons (cd_estabelecimento_p bigint, cd_setor_atendimento_p bigint, cd_convenio_p bigint) RETURNS varchar AS $body$
DECLARE

 
ie_liberado_w	varchar(1);
nr_seq_esp_w	bigint;


BEGIN 
if (cd_estabelecimento_p IS NOT NULL AND cd_estabelecimento_p::text <> '') and (cd_setor_atendimento_p IS NOT NULL AND cd_setor_atendimento_p::text <> '') and (cd_convenio_p IS NOT NULL AND cd_convenio_p::text <> '') then 
	/* estab x conv x setor */
 
	select	coalesce(max(nr_sequencia),0) 
	into STRICT	nr_seq_esp_w 
	from	regra_agecons_setor 
	where	cd_estabelecimento = cd_estabelecimento_p 
	and	cd_setor_atendimento = cd_setor_atendimento_p 
	and	cd_convenio = cd_convenio_p;
 
	if (nr_seq_esp_w > 0) then 
		select	coalesce(max(ie_regra),'S') 
		into STRICT	ie_liberado_w 
		from	regra_agecons_setor 
		where	nr_sequencia = nr_seq_esp_w;
	else 
		select	obter_valor_conv_estab(cd_convenio_p, cd_estabelecimento_p, 'ie_agenda_consulta') 
		into STRICT	ie_liberado_w 
		;
	end if;
end if;
 
return ie_liberado_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_conv_lib_agecons (cd_estabelecimento_p bigint, cd_setor_atendimento_p bigint, cd_convenio_p bigint) FROM PUBLIC;

