-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION lab_obter_valor_integracao (nr_seq_exame_p bigint, ds_valor_integ_p text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(255);


BEGIN

if (nr_seq_exame_p IS NOT NULL AND nr_seq_exame_p::text <> '')  and (ds_valor_integ_p IS NOT NULL AND ds_valor_integ_p::text <> '') then
	select 	coalesce(max(ds_valor),'')
	into STRICT	ds_retorno_w
	from 	exame_lab_valor
	where 	nr_seq_exame = nr_seq_exame_p
	and	upper(ds_valor_integracao) = upper(ds_valor_integ_p);
end if;

return ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION lab_obter_valor_integracao (nr_seq_exame_p bigint, ds_valor_integ_p text) FROM PUBLIC;

