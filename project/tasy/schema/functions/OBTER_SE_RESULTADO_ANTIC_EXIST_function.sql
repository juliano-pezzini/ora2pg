-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_resultado_antic_exist ( nr_seq_peca_p bigint, nr_seq_prescr_proc_p bigint ) RETURNS varchar AS $body$
DECLARE


ie_result_w		varchar(1) := 'N';
qt_result_atic_w	bigint;

BEGIN

select 	count(*)
into STRICT	qt_result_atic_w
from 	prescr_proc_result_antic
where 	nr_seq_prescr_proc_compl = nr_seq_prescr_proc_p
and 	nr_seq_prescr_proc_peca  = nr_seq_peca_p
and	coalesce(ie_resultado::text, '') = '';

if	not(qt_result_atic_w > 0) then

	select 	count(*)
	into STRICT	qt_result_atic_w
	from 	prescr_proc_result_antic
	where 	nr_seq_prescr_proc_compl = nr_seq_prescr_proc_p
	and 	nr_seq_prescr_proc_peca  = nr_seq_peca_p
	and 	coalesce(dt_liberacao::text, '') = '';

	if (qt_result_atic_w >0) then

		ie_result_w := 'S';

	end if;

end if;

return	ie_result_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_resultado_antic_exist ( nr_seq_peca_p bigint, nr_seq_prescr_proc_p bigint ) FROM PUBLIC;

