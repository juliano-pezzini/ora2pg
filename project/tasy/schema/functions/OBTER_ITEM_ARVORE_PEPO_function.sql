-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_item_arvore_pepo (nr_seq_pepo_item_p bigint, others_p text default null) RETURNS bigint AS $body$
DECLARE


nr_seq_schematic_w	bigint;
							

BEGIN

nr_seq_schematic_w := get_tree_item_ehro(nr_seq_pepo_item_p, others_p);

if (coalesce(nr_seq_schematic_w::text, '') = '') then
	if (nr_seq_pepo_item_p = 49) then
		/* Dispositivos */
		
		nr_seq_schematic_w	:= 122268;
	end if;
end if;

return	nr_seq_schematic_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_item_arvore_pepo (nr_seq_pepo_item_p bigint, others_p text default null) FROM PUBLIC;

