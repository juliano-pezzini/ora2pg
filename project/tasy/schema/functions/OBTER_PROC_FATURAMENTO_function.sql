-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_proc_faturamento (nr_seq_proc_interno_p bigint) RETURNS bigint AS $body$
DECLARE



cd_procedimento_w	bigint;


BEGIN

SELECT 	coalesce(MAX(cd_procedimento),0)
INTO STRICT	cd_procedimento_w
FROM	proc_interno
WHERE	nr_sequencia = nr_seq_proc_interno_p;

RETURN	cd_procedimento_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_proc_faturamento (nr_seq_proc_interno_p bigint) FROM PUBLIC;

