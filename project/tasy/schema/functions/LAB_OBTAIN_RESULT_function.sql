-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION lab_obtain_result ( QT_RESULTADO bigint, DS_RESULTADO text, PR_RESULTADO bigint ) RETURNS varchar AS $body$
DECLARE

  ds_refer_w varchar(4000):=NULL;

BEGIN
	IF (ds_resultado IS NOT NULL AND ds_resultado::text <> '') THEN
	ds_refer_w    :=obter_desc_expressao(970652)||': '||ds_resultado;
	END IF;
	IF (ds_refer_w IS NOT NULL AND ds_refer_w::text <> '' AND QT_RESULTADO IS NOT NULL AND QT_RESULTADO::text <> '') THEN
	  ds_refer_w      :=ds_refer_w||' / '||obter_desc_expressao(297049)||': '||QT_RESULTADO;
	elsif (coalesce(ds_refer_w::text, '') = '' AND (QT_RESULTADO IS NOT NULL AND QT_RESULTADO::text <> '')) THEN
	  ds_refer_w      :=obter_desc_expressao(297049)||': '||QT_RESULTADO;
	END IF;
	IF (ds_refer_w IS NOT NULL AND ds_refer_w::text <> '' AND PR_RESULTADO IS NOT NULL AND PR_RESULTADO::text <> '') THEN
	  ds_refer_w      :=ds_refer_w||' / '||obter_desc_expressao(492469)||': '||PR_RESULTADO;
	elsif (coalesce(ds_refer_w::text, '') = '' AND (PR_RESULTADO IS NOT NULL AND PR_RESULTADO::text <> '')) THEN
	  ds_refer_w      :=obter_desc_expressao(492469)||': '||PR_RESULTADO;
	END IF;
RETURN ds_refer_w;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION lab_obtain_result ( QT_RESULTADO bigint, DS_RESULTADO text, PR_RESULTADO bigint ) FROM PUBLIC;

