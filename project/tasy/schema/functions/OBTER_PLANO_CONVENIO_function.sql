-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_plano_convenio (CD_CONVENIO_P bigint, cd_plano_p text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w		varchar(100);


BEGIN
IF (cd_convenio_p IS NOT NULL AND cd_convenio_p::text <> '') AND (cd_plano_p IS NOT NULL AND cd_plano_p::text <> '') THEN
	BEGIN
	SELECT	MAX(ds_plano)
	INTO STRICT	ds_retorno_w
	FROM	convenio_plano
	WHERE	cd_convenio	= cd_convenio_p
	AND	cd_plano	= cd_plano_p;
	END;
END IF;

RETURN ds_retorno_w;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_plano_convenio (CD_CONVENIO_P bigint, cd_plano_p text) FROM PUBLIC;

