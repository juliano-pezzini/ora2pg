-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION man_obter_loc_lib_usuario ( nr_seq_localizacao_p bigint, nm_usuario_p text) RETURNS varchar AS $body$
DECLARE


ie_retorno_w		varchar(15) := 'S';
nr_seq_grupo_trab_w	bigint;
nr_seq_grupo_planej_w	bigint;

C01 CURSOR FOR
	SELECT	nr_seq_grupo_trabalho,
		nr_seq_grupo_planejamento,
		'N'
	from	man_localizacao_lib
	where	nr_seq_localizacao = nr_seq_localizacao_p
	order by dt_atualizacao_nrec;


BEGIN

open C01;
loop
fetch C01 into
	nr_seq_grupo_trab_w,
	nr_seq_grupo_planej_w,
	ie_retorno_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin
	if (nr_seq_grupo_trab_w IS NOT NULL AND nr_seq_grupo_trab_w::text <> '')and (ie_retorno_w = 'N') then

		ie_retorno_w := obter_se_usuario_grupo_trab(nr_seq_grupo_trab_w,nm_usuario_p);

	elsif (nr_seq_grupo_planej_w IS NOT NULL AND nr_seq_grupo_planej_w::text <> '')and (ie_retorno_w = 'N') then

		ie_retorno_w := obter_se_usuario_grupo_planej(nr_seq_grupo_planej_w, nm_usuario_p);

	end if;
	if (ie_retorno_w = 'S') then
		goto final;
	end if;
	end;
end loop;
close C01;
<<final>>
return	ie_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION man_obter_loc_lib_usuario ( nr_seq_localizacao_p bigint, nm_usuario_p text) FROM PUBLIC;

