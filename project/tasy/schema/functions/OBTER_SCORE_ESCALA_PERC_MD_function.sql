-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_score_escala_perc_md (IE_HEART_RATE_P text, IE_HEMOPTYSIS_P text, IE_HORMONE_USE_P text, IE_AGE_P text, IE_LEG_SWELLING_P text, IE_PRIOR_PE_P text, IE_RECENT_SURGERY_P text, IE_SATURATION_P text ) RETURNS bigint AS $body$
DECLARE


   qt_pontuacao_w	bigint := 0;

BEGIN
	--- Inicio MD1
	if (IE_HEART_RATE_P = 'S') then
		qt_pontuacao_w := qt_pontuacao_w + 1;
	end if;

	if (IE_HEMOPTYSIS_P = 'S') then
		qt_pontuacao_w := qt_pontuacao_w + 1;
	end if;

	if (IE_HORMONE_USE_P  = 'S') then
		qt_pontuacao_w := qt_pontuacao_w + 1;
	end if;

	if (IE_AGE_P = 'S') then
		qt_pontuacao_w := qt_pontuacao_w + 1;
	end if;

	if (IE_LEG_SWELLING_P  = 'S') then
		qt_pontuacao_w := qt_pontuacao_w + 1;
	end if;

	if (IE_PRIOR_PE_P  = 'S') then
		qt_pontuacao_w := qt_pontuacao_w + 1;
	end if;

	if (IE_RECENT_SURGERY_P = 'S') then
		qt_pontuacao_w := qt_pontuacao_w + 1;
	end if;

	if (IE_SATURATION_P  = 'S') then
		qt_pontuacao_w := qt_pontuacao_w + 1;
	end if;
	--- Fim MD1
    RETURN coalesce(qt_pontuacao_w,0);
   
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_score_escala_perc_md (IE_HEART_RATE_P text, IE_HEMOPTYSIS_P text, IE_HORMONE_USE_P text, IE_AGE_P text, IE_LEG_SWELLING_P text, IE_PRIOR_PE_P text, IE_RECENT_SURGERY_P text, IE_SATURATION_P text ) FROM PUBLIC;

