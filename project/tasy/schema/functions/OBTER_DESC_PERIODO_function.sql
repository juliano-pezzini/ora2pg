-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_desc_periodo (ie_opcao_p text) RETURNS varchar AS $body$
DECLARE

ds_periodo_w		varchar(50) := '';


BEGIN

/* Function utilizada na função agenda de exames e integrada,
nas regras de convênio x procedimento, quantidade x consistência */
if (ie_opcao_p IS NOT NULL AND ie_opcao_p::text <> '') then
	if (ie_opcao_p = 'D') then
		ds_periodo_w := WHEB_MENSAGEM_PCK.get_texto(386356);
	end if;

	if (ie_opcao_p = 'M') then
		ds_periodo_w := WHEB_MENSAGEM_PCK.get_texto(386357);
	end if;

	if (ie_opcao_p = 'A') then
		ds_periodo_w := WHEB_MENSAGEM_PCK.get_texto(386358);
	end if;

	if (ie_opcao_p = 'S') then
		ds_periodo_w := WHEB_MENSAGEM_PCK.get_texto(386359);
	end if;
end if;

return	ds_periodo_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_desc_periodo (ie_opcao_p text) FROM PUBLIC;

