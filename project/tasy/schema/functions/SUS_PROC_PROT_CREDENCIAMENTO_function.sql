-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION sus_proc_prot_credenciamento ( cd_procedimento_p bigint, ie_origem_proced_p bigint, nr_seq_protocolo_p bigint) RETURNS varchar AS $body$
DECLARE

 
 
ie_credenciamento_prot_w	varchar(1)	:= '';
ie_credenciamento_proc_w	varchar(1)	:= '';
ds_retorno_w			varchar(1)	:= 'S';


BEGIN 
 
select	coalesce(max(ie_credenciamento_sus),'') 
into STRICT	ie_credenciamento_prot_w 
from	protocolo_convenio 
where	nr_seq_protocolo	= nr_seq_protocolo_p;
 
select	coalesce(max(ie_credenciamento_sus),'') 
into STRICT	ie_credenciamento_proc_w 
from	procedimento 
where	cd_procedimento		= cd_procedimento_p 
and	ie_origem_proced	= ie_origem_proced_p;
 
if (ie_credenciamento_prot_w IS NOT NULL AND ie_credenciamento_prot_w::text <> '') and (ie_credenciamento_prot_w	<> ie_credenciamento_proc_w) then 
	ds_retorno_w		:= 'N';
end if;
 
return	ds_retorno_w;
 
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION sus_proc_prot_credenciamento ( cd_procedimento_p bigint, ie_origem_proced_p bigint, nr_seq_protocolo_p bigint) FROM PUBLIC;

