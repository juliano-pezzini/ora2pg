-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_ie_recurso_pendente ( nr_seq_lote_p bigint) RETURNS varchar AS $body$
DECLARE


ie_recurso_pend_w		varchar(1);


BEGIN

select coalesce(MAX('S'),'N')
into STRICT ie_recurso_pend_w
        from   lote_audit_hist x,
              TISS_REC_GLOSA_PROT_IMP y
        where  x.nr_seq_lote_audit = nr_seq_lote_p
        and    x.nr_sequencia = y.nr_seq_lote_hist
        and    x.nr_analise = (SELECT  max(w.nr_analise) from lote_audit_hist w where w.nr_seq_lote_audit = x.nr_seq_lote_audit);

return ie_recurso_pend_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_ie_recurso_pendente ( nr_seq_lote_p bigint) FROM PUBLIC;

