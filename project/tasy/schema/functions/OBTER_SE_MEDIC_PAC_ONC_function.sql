-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_medic_pac_onc ( cd_pessoa_fisica_p text, cd_material_p bigint) RETURNS varchar AS $body$
DECLARE


qt_lib_w		bigint;
qt_dias_tratamento_w	double precision;
qt_prescricao_w		double precision;
nm_usuario_w		varchar(50);
ie_usa_liberacao_w	varchar(50);
cd_estabelecimento_w	bigint;
dt_inicio_validade_w	timestamp;


BEGIN
select	count(*)
into STRICT	qt_lib_w
from	lib_material_paciente
where	cd_material	 = cd_material_p
and	cd_pessoa_fisica = cd_pessoa_fisica_p
and	coalesce(dt_suspenso::text, '') = ''
and	clock_timestamp() between trunc(dt_inicio_validade,'dd') and fim_dia(coalesce(dt_suspenso,clock_timestamp()));

if (qt_lib_w > 0) then
	return 'S';
else
	return 'N';
end if;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_medic_pac_onc ( cd_pessoa_fisica_p text, cd_material_p bigint) FROM PUBLIC;

