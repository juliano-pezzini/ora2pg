-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_aval_procedimento ( nr_seq_proc_interno_p bigint, cd_procedimento_p bigint) RETURNS bigint AS $body$
DECLARE


nr_seq_tipo_aval_w	bigint;


BEGIN
if ( (cd_procedimento_p IS NOT NULL AND cd_procedimento_p::text <> '') OR (nr_seq_proc_interno_p IS NOT NULL AND nr_seq_proc_interno_p::text <> '')) then
	select	max(nr_seq_tipo_aval)
	into STRICT	nr_seq_tipo_aval_w
	from	med_tipo_aval_proc
	where ((nr_seq_proc_interno IS NOT NULL AND nr_seq_proc_interno::text <> '') and nr_seq_proc_interno = nr_seq_proc_interno_p) or ((cd_procedimento IS NOT NULL AND cd_procedimento::text <> '') and cd_procedimento = cd_procedimento_p);
end if;

return nr_seq_tipo_aval_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_aval_procedimento ( nr_seq_proc_interno_p bigint, cd_procedimento_p bigint) FROM PUBLIC;

