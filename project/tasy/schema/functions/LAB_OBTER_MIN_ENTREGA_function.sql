-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION lab_obter_min_entrega (nr_seq_exame_p bigint, nr_seq_grupo_p bigint, ie_tipo_atendimento_p bigint) RETURNS varchar AS $body$
DECLARE


qt_min_entrega_w	bigint;


BEGIN

select	coalesce(max(qt_min_entrega),0)
into STRICT	qt_min_entrega_w
from	exame_lab_regra_setor
where	nr_seq_exame = nr_seq_exame_p
and		coalesce(ie_tipo_atendimento,ie_tipo_Atendimento_p) = ie_tipo_atendimento_p;

if (qt_min_entrega_w = 0) then

	select	coalesce(max(qt_min_entrega),0)
	into STRICT	qt_min_entrega_w
	from	exame_lab_regra_setor
	where 	nr_seq_grupo = nr_seq_grupo_p
	and		ie_tipo_atendimento = ie_tipo_atendimento_p;

end if;

return	to_char(qt_min_entrega_w);

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION lab_obter_min_entrega (nr_seq_exame_p bigint, nr_seq_grupo_p bigint, ie_tipo_atendimento_p bigint) FROM PUBLIC;

