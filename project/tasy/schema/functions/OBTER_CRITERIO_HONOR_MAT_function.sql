-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_criterio_honor_mat (nr_sequencia_p bigint) RETURNS bigint AS $body$
DECLARE

			 
cd_estabelecimento_w	smallint;
cd_convenio_w		integer;
cd_categoria_w		varchar(10);
cd_material_w		bigint;
dt_atendimento_w	timestamp;
ie_tipo_atendimento_w	smallint;
cd_setor_Atendimento_w	integer;
cd_medico_executor_w	varchar(10);
cd_cgc_prestador_w	varchar(14);
cd_regra_valida_w	varchar(5);
nr_seq_criterio_w	bigint;
ie_conta_honorario_w	varchar(1);
ie_calcula_honorario_w	varchar(1);
cd_cgc_honorario_w	varchar(14);
cd_pessoa_honorario_w	varchar(10);
cd_cgc_fornecedor_w	varchar(14);
ie_valor_autorizado_w	varchar(1);


BEGIN	 
select	a.cd_estabelecimento, 
	a.cd_convenio_parametro, 
	a.cd_categoria_parametro, 
	b.cd_material, 
	Obter_Tipo_Atendimento(a.nr_atendimento), 
	b.cd_setor_atendimento, 
	b.cd_cgc_prestador, 
	b.dt_atendimento, 
	b.ie_valor_autorizado 
into STRICT	cd_estabelecimento_w, 
	cd_convenio_w, 
	cd_categoria_w, 
	cd_material_w, 
	ie_tipo_atendimento_w, 
	cd_setor_Atendimento_w, 
	cd_cgc_prestador_w, 
	dt_atendimento_w, 
	ie_valor_autorizado_w 
from	material_atend_paciente b, 
	conta_paciente a 
where	a.nr_interno_conta	= b.nr_interno_conta 
and	b.nr_sequencia		= nr_sequencia_p;
 
 
 
 
SELECT * FROM obter_regra_material_crit(	cd_estabelecimento_w, cd_convenio_w, cd_categoria_w, cd_material_w, dt_atendimento_w, ie_tipo_atendimento_w, cd_setor_Atendimento_w, null, null, null, cd_cgc_prestador_w, cd_cgc_fornecedor_w, ie_valor_autorizado_w, cd_regra_valida_w, ie_conta_honorario_w, ie_calcula_honorario_w, cd_cgc_prestador_w, cd_pessoa_honorario_w, nr_seq_criterio_w) INTO STRICT cd_regra_valida_w, ie_conta_honorario_w, ie_calcula_honorario_w, cd_cgc_prestador_w, cd_pessoa_honorario_w, nr_seq_criterio_w;
 
return	nr_seq_criterio_w;
 
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_criterio_honor_mat (nr_sequencia_p bigint) FROM PUBLIC;

