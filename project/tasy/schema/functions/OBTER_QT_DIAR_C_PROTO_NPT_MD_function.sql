-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_qt_diar_c_proto_npt_md (ie_Calc_Conforme_Protocolo_p text, qt_Conversao_Ml_p bigint, qt_Elemento_p bigint, qt_Volume_p bigint ) RETURNS bigint AS $body$
DECLARE

    EXEC_W varchar(200);
    result_w bigint;

BEGIN
   		if (upper(ie_Calc_Conforme_Protocolo_p) = 'S' and qt_Conversao_Ml_p > 0) then
         if qt_Elemento_p > 0 then
            result_w := qt_Elemento_p;
         else
            result_w := 0;
         end if;
    		
			else
         begin
           EXEC_W := 'CALL OBTER_CHECK_DIV_DIVID_MD(:1,:2) INTO :result';

           EXECUTE EXEC_W USING IN qt_Volume_p,
                                          IN qt_Conversao_Ml_p,
                                          OUT result_w;
          exception
             when others then
                 result_w := null;
         end;
			end if;
	
    return coalesce(result_w,0);

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_qt_diar_c_proto_npt_md (ie_Calc_Conforme_Protocolo_p text, qt_Conversao_Ml_p bigint, qt_Elemento_p bigint, qt_Volume_p bigint ) FROM PUBLIC;

