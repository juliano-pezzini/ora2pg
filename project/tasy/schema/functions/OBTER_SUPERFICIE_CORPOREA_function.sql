-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_superficie_corporea ( qt_altura_cm_p bigint, qt_peso_p bigint, ie_formula_sup_corporea_p text default null) RETURNS bigint AS $body$
DECLARE


qt_resultado_w		        double precision;
cd_estabelecimento_w	    bigint;
ie_formula_sup_corporea_w	varchar(10);
nr_dec_sc_w               bigint;
qt_peso_gramas_w	        double precision;
EXEC_w                    varchar(200);

BEGIN
    cd_estabelecimento_w	:= wheb_usuario_pck.get_cd_estabelecimento;

    select coalesce(coalesce(ie_formula_sup_corporea_p,max(ie_formula_sup_corporea)),'M'),
           coalesce(max(nr_dec_sc),3)
    into STRICT   ie_formula_sup_corporea_w,
           nr_dec_sc_w
    from   parametro_medico
    where  cd_estabelecimento	= cd_estabelecimento_w;

    begin
      EXEC_w := 'CALL OBTER_SUPERFICIE_CORPOREA_MD(:1,:2,:3,:4) INTO :result';
			
      EXECUTE EXEC_w USING IN qt_altura_cm_p,
                                     IN qt_peso_p,
                                     IN ie_formula_sup_corporea_w,
                                     IN nr_dec_sc_w,
                                     OUT qt_resultado_w;
    exception
       when others then
          qt_resultado_w := null;
    end;

    RETURN qt_resultado_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_superficie_corporea ( qt_altura_cm_p bigint, qt_peso_p bigint, ie_formula_sup_corporea_p text default null) FROM PUBLIC;

