-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_desc_carater_cir (IE_CARATER_CIRURGIA_P text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(255);


BEGIN

if (IE_CARATER_CIRURGIA_P = 'M') then
	ds_retorno_w := obter_desc_expressao(289150);
elsif (IE_CARATER_CIRURGIA_P = 'U') then
	ds_retorno_w := obter_desc_expressao(300864);
elsif (IE_CARATER_CIRURGIA_P = 'E') then
	ds_retorno_w := obter_desc_expressao(315708);
elsif (IE_CARATER_CIRURGIA_P = 'A') then
	ds_retorno_w := obter_desc_expressao(283433);
end if;

RETURN	ds_retorno_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_desc_carater_cir (IE_CARATER_CIRURGIA_P text) FROM PUBLIC;

