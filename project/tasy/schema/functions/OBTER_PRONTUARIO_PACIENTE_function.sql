-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_prontuario_paciente (cd_pessoa_fisica_p text) RETURNS bigint AS $body$
DECLARE


nr_prontuario_w		bigint;


BEGIN

if (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') then
	begin
	select nr_prontuario
	into STRICT nr_prontuario_w
	from pessoa_fisica
	where cd_pessoa_fisica = cd_pessoa_fisica_p;
	exception
		when others then
			nr_prontuario_w := null;
	end;
end if;

return nr_prontuario_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_prontuario_paciente (cd_pessoa_fisica_p text) FROM PUBLIC;

