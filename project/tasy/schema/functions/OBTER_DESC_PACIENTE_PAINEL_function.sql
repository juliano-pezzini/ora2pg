-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_desc_paciente_painel ( DT_ANALISE_P timestamp, DT_CHEGADA_P timestamp, DT_PAC_AGUARDO_P timestamp, DT_ALTA_P timestamp, DT_PRE_INDUC_P timestamp, DT_SALA_REC_P timestamp, DT_CHEGADA_FIM_P timestamp, DT_SAIDA_CC_P timestamp, IE_STATUS_AGENDA text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(255);


BEGIN

if (IE_STATUS_AGENDA = 'C') then
	ds_retorno_w := obter_desc_expressao(302747);
elsif (DT_SAIDA_CC_P IS NOT NULL AND DT_SAIDA_CC_P::text <> '') then
	ds_retorno_w := obter_desc_expressao(854767);
elsif (DT_CHEGADA_FIM_P IS NOT NULL AND DT_CHEGADA_FIM_P::text <> '') then
	ds_retorno_w := obter_desc_expressao(854765);
elsif (DT_SALA_REC_P IS NOT NULL AND DT_SALA_REC_P::text <> '') then
	ds_retorno_w := obter_desc_expressao(854763);
elsif (DT_PRE_INDUC_P IS NOT NULL AND DT_PRE_INDUC_P::text <> '') then
	ds_retorno_w := obter_desc_expressao(854759);
elsif (DT_ALTA_P IS NOT NULL AND DT_ALTA_P::text <> '') then
	ds_retorno_w := obter_desc_expressao(854757);
elsif (DT_PAC_AGUARDO_P IS NOT NULL AND DT_PAC_AGUARDO_P::text <> '') then
	ds_retorno_w := obter_desc_expressao(854755);
elsif (DT_CHEGADA_P IS NOT NULL AND DT_CHEGADA_P::text <> '') then
	ds_retorno_w := obter_desc_expressao(284848);
elsif (DT_ANALISE_P IS NOT NULL AND DT_ANALISE_P::text <> '') then
	ds_retorno_w := obter_desc_expressao(318367);
end if;

RETURN	ds_retorno_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_desc_paciente_painel ( DT_ANALISE_P timestamp, DT_CHEGADA_P timestamp, DT_PAC_AGUARDO_P timestamp, DT_ALTA_P timestamp, DT_PRE_INDUC_P timestamp, DT_SALA_REC_P timestamp, DT_CHEGADA_FIM_P timestamp, DT_SAIDA_CC_P timestamp, IE_STATUS_AGENDA text) FROM PUBLIC;

