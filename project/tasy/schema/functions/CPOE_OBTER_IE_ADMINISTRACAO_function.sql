-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION cpoe_obter_ie_administracao ( nr_seq_proc_interno_p bigint, cd_setor_atendimento_p bigint) RETURNS varchar AS $body$
DECLARE

					
ie_administracao_w			exame_regra_cpoe.ie_administracao%type;

c01 CURSOR FOR
SELECT	max(ie_administracao)
from	exame_regra_cpoe
where	nr_seq_proc_interno = nr_seq_proc_interno_p
and		coalesce(cd_setor_atendimento,coalesce(cd_setor_atendimento_p,0)) = coalesce(cd_setor_atendimento_p,0)
order 	by	coalesce(cd_setor_atendimento,0);	


BEGIN

open c01;
loop
fetch c01 into ie_administracao_w;
EXIT WHEN NOT FOUND; /* apply on c01 */
end loop;
close c01;

return ie_administracao_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION cpoe_obter_ie_administracao ( nr_seq_proc_interno_p bigint, cd_setor_atendimento_p bigint) FROM PUBLIC;

