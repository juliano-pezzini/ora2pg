-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_prioridade (nr_prescricao_p bigint, nr_seq_proc_interno_p bigint) RETURNS REG_PRIOR_PROCED_EXAME.NR_SEQUENCIA%TYPE AS $body$
DECLARE


nr_seq_prioridade_w 	varchar(80);
ie_status_execucao_w 	varchar(80);

BEGIN

nr_seq_prioridade_w := null;
ie_status_execucao_w := null;

  begin

  SELECT  max(c.NR_SEQ_REG_PRIOR_PROCED_EXAME),
          max(a.ie_status_execucao)
	into STRICT	nr_seq_prioridade_w,
        ie_status_execucao_w
	 FROM   prescr_procedimento a,
			cpoe_procedimento b,
			protocolo_medic_proc c,
			prescr_medica d
	 WHERE  a.nr_seq_proc_cpoe = b.nr_sequencia
	 AND    a.nr_seq_proc_interno = b.nr_seq_proc_interno
	 AND    b.nr_seq_protocolo = c.nr_sequencia
	 AND    b.cd_protocolo = c.cd_protocolo 
	 AND    b.nr_seq_proc_interno = c.nr_seq_proc_interno
	 AND    a.nr_prescricao = nr_prescricao_p
	 AND    a.nr_seq_proc_interno = nr_seq_proc_interno_p
	 AND    d.nr_prescricao = a.NR_PRESCRICAO;

   exception
    when others then
      nr_seq_prioridade_w  := null;
  end;	

  if (ie_status_execucao_w = '40') then
    nr_seq_prioridade_w := null;
  end if;

return nr_seq_prioridade_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_prioridade (nr_prescricao_p bigint, nr_seq_proc_interno_p bigint) FROM PUBLIC;

