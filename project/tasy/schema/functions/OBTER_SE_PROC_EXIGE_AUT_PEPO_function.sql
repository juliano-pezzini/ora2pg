-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_proc_exige_aut_pepo ( nr_seq_proc_interno_w bigint, cd_procedimento_w bigint, ie_origem_proced_w bigint) RETURNS varchar AS $body$
DECLARE


ie_retorno_w		varchar(1);
cd_procedimento_tuss_w	bigint;
ie_origem_proc_tuss_w	bigint;


BEGIN

if (coalesce(nr_seq_proc_interno_w,0) > 0) then

	select	max(cd_procedimento_tuss),
		max(ie_origem_proc_tuss)
	into STRICT	cd_procedimento_tuss_w,
		ie_origem_proc_tuss_w
	from	proc_interno
	where	nr_sequencia = nr_seq_proc_interno_w;

end if;

select	coalesce(max('S'),'N')
into STRICT	ie_retorno_w
from	regra_proc_exige_autentic
where	coalesce(nr_seq_proc_interno, coalesce(nr_seq_proc_interno_w,0)) = coalesce(nr_seq_proc_interno_w,0)
and	coalesce(cd_procedimento_tuss, coalesce(cd_procedimento_tuss_w, 0)) = coalesce(cd_procedimento_tuss_w, 0)
and	((coalesce(cd_procedimento_tuss::text, '') = '') or (coalesce(ie_origem_proc_tuss, coalesce(ie_origem_proc_tuss_w, 0)) = coalesce(ie_origem_proc_tuss_w, 0)))
and	coalesce(cd_procedimento, coalesce(cd_procedimento_w, 0)) = coalesce(cd_procedimento_w, 0)
and	((coalesce(cd_procedimento::text, '') = '') or (coalesce(ie_origem_proced, coalesce(ie_origem_proced_w, 0)) = coalesce(ie_origem_proced_w, 0)));

return	ie_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_proc_exige_aut_pepo ( nr_seq_proc_interno_w bigint, cd_procedimento_w bigint, ie_origem_proced_w bigint) FROM PUBLIC;

