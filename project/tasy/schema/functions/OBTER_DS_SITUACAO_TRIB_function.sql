-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_ds_situacao_trib (nr_seq_trib_prest_p bigint) RETURNS varchar AS $body$
DECLARE


ds_situacao_w	varchar(255);

BEGIN
ds_situacao_w := '';

if (coalesce(nr_seq_trib_prest_p,'0') <> '0') then

	select	max(ds_situacao)
	into STRICT	ds_situacao_w
	from	situacao_trib_prest_serv
	where	nr_sequencia= nr_seq_trib_prest_p;

end if;

return	ds_situacao_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_ds_situacao_trib (nr_seq_trib_prest_p bigint) FROM PUBLIC;

