-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_volume_npt_adult_md ( ds_unidade_medida_usu_p text, ds_unidade_medida_p text, qt_diaria_p bigint, qt_conversao_ml_p bigint ) RETURNS bigint AS $body$
DECLARE

  qt_volume_w  bigint;

BEGIN
  if (ds_unidade_medida_usu_p = ds_unidade_medida_p) then
    qt_volume_w := qt_diaria_p;
  else
    qt_volume_w := coalesce(qt_diaria_p, 0) * coalesce(qt_conversao_ml_p, 0);
  end if;
  return qt_volume_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_volume_npt_adult_md ( ds_unidade_medida_usu_p text, ds_unidade_medida_p text, qt_diaria_p bigint, qt_conversao_ml_p bigint ) FROM PUBLIC;

