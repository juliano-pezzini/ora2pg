-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_itens_integ_telemedicina (nr_seq_ageint_p bigint, ie_acao_p text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(4000) := '';

  item RECORD;

BEGIN

for item in (SELECT nr_seq_agenda_cons from agenda_integrada_item where nr_seq_agenda_int = nr_seq_ageint_p and (IE_TIPO_AGENDAMENTO = 'C' OR IE_TIPO_AGENDAMENTO = 'S')  and (nr_seq_agenda_cons IS NOT NULL AND nr_seq_agenda_cons::text <> '')) loop
	if (OBTEM_INTEGRA_TELEMEDICINA(item.nr_seq_agenda_cons, ie_acao_p) = 'S') then
		ds_retorno_w := ds_retorno_w || item.nr_seq_agenda_cons || ',';
	end if;
end loop;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_itens_integ_telemedicina (nr_seq_ageint_p bigint, ie_acao_p text) FROM PUBLIC;

