-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_unidade_temporaria ( CD_SETOR_ATENDIMENTO_P bigint, CD_UNIDADE_BASICA_P text, CD_UNIDADE_COMPL_P text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w		varchar(14);


BEGIN
if (CD_SETOR_ATENDIMENTO_P IS NOT NULL AND CD_SETOR_ATENDIMENTO_P::text <> '')	and (CD_UNIDADE_BASICA_P IS NOT NULL AND CD_UNIDADE_BASICA_P::text <> '')		and (CD_UNIDADE_COMPL_P IS NOT NULL AND CD_UNIDADE_COMPL_P::text <> '')		then
	begin
	select	max(IE_TEMPORARIO)
	into STRICT	ds_retorno_w
	from	unidade_atendimento
	where	cd_setor_atendimento	= cd_setor_atendimento_p
	and	cd_unidade_basica	= cd_unidade_basica_p
	and	cd_unidade_compl	= cd_unidade_compl_p;
	end;
end if;

RETURN ds_retorno_w;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_unidade_temporaria ( CD_SETOR_ATENDIMENTO_P bigint, CD_UNIDADE_BASICA_P text, CD_UNIDADE_COMPL_P text) FROM PUBLIC;

