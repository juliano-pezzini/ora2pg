-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pfcs_get_battery_status ( nr_seq_patient_p text ) RETURNS varchar AS $body$
DECLARE


ds_battery_status 	varchar(15) := null;
ds_battery 			varchar(15) := null;

low_battery_code 	varchar(15) := 'D0401';
empty_battery_code	varchar(15) := 'D0402';
ds_active_status	varchar(15) := 'ACTIVE';


BEGIN

select cd_flag into STRICT ds_battery_status from (
	SELECT 	cd_flag
	from 	pfcs_patient_flag ppf
	where 	ppf.si_status = ds_active_status
			and (ppf.cd_flag = low_battery_code or ppf.cd_flag = empty_battery_code)
			and ppf.nr_seq_patient = nr_seq_patient_p
			and coalesce(ppf.period_end::text, '') = ''
			order by ppf.dt_atualizacao desc
            ) alias2 LIMIT 1;

  
  if (ds_battery_status = low_battery_code) then
		ds_battery := 'Low';
  elsif (ds_battery_status = empty_battery_code) then
		ds_battery := 'Empty';
  else
		ds_battery := null;
  end if;
	
return ds_battery;

exception

-- If there will be no record
when no_data_found then
  return ds_battery;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pfcs_get_battery_status ( nr_seq_patient_p text ) FROM PUBLIC;

