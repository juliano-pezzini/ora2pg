-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_data_formato ( dt_horario_p timestamp, cd_estabelecimento_p estabelecimento.cd_estabelecimento%type, nm_usuario_p usuario.nm_usuario%type, cd_formato_p locale_formats.id_mask%type default 'timestamp') RETURNS timestamp AS $body$
DECLARE


/*
exemplo formatos:
  longDate
  mediumDate
  short
  shortDate
  shortDayMonth
  shortThreeLetterMonth
  shortTime
  timestamp

  Os formatos podem ser encontrados na tabela locale_formats
*/
ds_mascara_w varchar(100);


BEGIN

if ((dt_horario_p IS NOT NULL AND dt_horario_p::text <> '')
	and (cd_estabelecimento_p IS NOT NULL AND cd_estabelecimento_p::text <> '')
	and (nm_usuario_p IS NOT NULL AND nm_usuario_p::text <> '')) then
	
	ds_mascara_w := pkg_date_formaters.localize_mask(coalesce(cd_formato_p, 'timestamp'), pkg_date_formaters.getUserLanguageTag(cd_estabelecimento_p, nm_usuario_p));

	return to_date(to_char(dt_horario_p, ds_mascara_w), ds_mascara_w);
	
end if;

return null;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_data_formato ( dt_horario_p timestamp, cd_estabelecimento_p estabelecimento.cd_estabelecimento%type, nm_usuario_p usuario.nm_usuario%type, cd_formato_p locale_formats.id_mask%type default 'timestamp') FROM PUBLIC;

