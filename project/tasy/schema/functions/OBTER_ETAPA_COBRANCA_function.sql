-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_etapa_cobranca (nr_seq_cobranca_p bigint) RETURNS bigint AS $body$
DECLARE


nr_seq_etapa_w	bigint;


BEGIN

begin
	select 	nr_seq_etapa
	into STRICT	nr_seq_etapa_w
	from (SELECT		a.nr_seq_etapa
			from		cobranca_etapa a
			where		a.nr_seq_cobranca = nr_seq_cobranca_p
			order by	a.dt_etapa desc) alias0 LIMIT 1;
exception
when others then
	nr_seq_etapa_w	:= null;
end;

return	nr_seq_etapa_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_etapa_cobranca (nr_seq_cobranca_p bigint) FROM PUBLIC;

