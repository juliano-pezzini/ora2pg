-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_desc_inf_sae_adep ( ie_sn_p text, ds_intervalo_p text, nr_seq_topografia_p bigint default 0, ie_lado_p text default null, nr_seq_modelo_p bigint default 0) RETURNS varchar AS $body$
DECLARE

		
ds_informacao_w	varchar(4000);
ie_exibir_sae_modelo_w	varchar(1);


BEGIN
ds_informacao_w	:= ds_intervalo_p;
if (ie_sn_p = 'S') then
	begin
	ds_informacao_w	:= obter_desc_expressao(309405) || ' ' || ds_informacao_w;
	end;
end if;

if (coalesce(nr_seq_topografia_p, 0) > 0) then
	ds_informacao_w	:= ds_informacao_w || ' - ' || obter_desc_expressao(343115) ||substr(Obter_desc_topografia_dor(nr_seq_topografia_p),1,150);
end if;

if (ie_lado_p IS NOT NULL AND ie_lado_p::text <> '') then
	ds_informacao_w	:= ds_informacao_w || obter_desc_expressao(347323) || ' ' ||substr(obter_valor_dominio(1372,ie_lado_p),1,150);
end if;

ie_exibir_sae_modelo_w := wheb_assist_pck.obterParametroFuncao(1113,703, wheb_usuario_pck.get_cd_perfil, wheb_usuario_pck.get_nm_usuario, wheb_usuario_pck.get_cd_estabelecimento);
if (coalesce(nr_seq_modelo_p, 0) > 0) and (ie_exibir_sae_modelo_w = 'S') then
	ds_informacao_w	:= obter_desc_expressao(330326) || ' ' ||obter_desc_mod_sae(nr_seq_modelo_p) || ' - ' || ds_informacao_w;
end if;

return ds_informacao_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_desc_inf_sae_adep ( ie_sn_p text, ds_intervalo_p text, nr_seq_topografia_p bigint default 0, ie_lado_p text default null, nr_seq_modelo_p bigint default 0) FROM PUBLIC;

