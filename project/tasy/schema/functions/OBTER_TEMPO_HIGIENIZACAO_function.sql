-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_tempo_higienizacao ( nr_seq_proc_interno_p bigint) RETURNS bigint AS $body$
DECLARE


qt_min_higienizacao_w	bigint:=0;


BEGIN

if (coalesce(nr_seq_proc_interno_p,0) > 0) then
	select	coalesce(max(qt_min_higienizacao),0)
	into STRICT	qt_min_higienizacao_w
	from	proc_interno
	where	nr_sequencia	= nr_seq_proc_interno_p;
end if;

return	qt_min_higienizacao_w;

end	;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_tempo_higienizacao ( nr_seq_proc_interno_p bigint) FROM PUBLIC;

