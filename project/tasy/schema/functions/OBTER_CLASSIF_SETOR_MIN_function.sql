-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_classif_setor_min ( nr_atendimento_p bigint ) RETURNS bigint AS $body$
DECLARE


cd_classif_setor_w		bigint;
cd_setor_atendimento_w		bigint;


C01 CURSOR FOR
	SELECT	cd_setor_atendimento
	from	atend_paciente_unidade
	where	nr_atendimento = nr_atendimento_p
	order by dt_entrada_unidade desc;


BEGIN

open C01;
loop
fetch C01 into
	cd_setor_atendimento_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin
	cd_setor_atendimento_w := cd_setor_atendimento_w;
	end;
end loop;
close C01;


select	max(cd_classif_setor)
into STRICT	cd_classif_setor_w
from	setor_atendimento
where	cd_setor_atendimento = cd_setor_atendimento_w;

return			cd_classif_setor_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_classif_setor_min ( nr_atendimento_p bigint ) FROM PUBLIC;

