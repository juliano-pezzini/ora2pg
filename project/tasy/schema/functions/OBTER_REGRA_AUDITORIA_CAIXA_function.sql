-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_regra_auditoria_caixa (nr_seq_caixa_p bigint) RETURNS bigint AS $body$
DECLARE



nr_sequencia_w	caixa_regra_auditoria.nr_sequencia%type;


BEGIN
	
	select	max(nr_sequencia)
	into STRICT	nr_sequencia_w
	from	caixa_regra_auditoria
	where	coalesce(coalesce(nr_seq_caixa,nr_seq_caixa_p), 0)	= coalesce(nr_seq_caixa_p,0)
	and trunc(clock_timestamp())		between dt_inicio_vigencia and coalesce(dt_fim_vigencia,trunc(clock_timestamp()));
	
return	nr_sequencia_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_regra_auditoria_caixa (nr_seq_caixa_p bigint) FROM PUBLIC;

