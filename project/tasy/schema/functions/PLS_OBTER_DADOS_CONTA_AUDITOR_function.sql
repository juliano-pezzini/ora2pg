-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_dados_conta_auditor ( nr_seq_conta_auditor_p pls_conta_auditor.nr_sequencia%type, ie_opcao_p text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w		varchar(255) := null;
nr_sequencia_w		pls_membro_grupo_aud.nr_sequencia%type;


BEGIN
if (ie_opcao_p = 'NAM') then
	select	max(nr_seq_grupo_membro_med)
	into STRICT	nr_sequencia_w
	from	pls_conta_auditor
	where	nr_sequencia	= nr_seq_conta_auditor_p;

	if (nr_sequencia_w IS NOT NULL AND nr_sequencia_w::text <> '') then
		select	substr(obter_nome_usuario(nm_usuario_exec),1,80)
		into STRICT	ds_retorno_w
		from	pls_membro_grupo_aud
		where	ie_auditor_ptu	= 'S'
		and	nr_sequencia	= nr_sequencia_w;
	end if;

elsif (ie_opcao_p = 'NAE') then
	select	max(nr_seq_grupo_membro_enf)
	into STRICT	nr_sequencia_w
	from	pls_conta_auditor
	where	nr_sequencia	= nr_seq_conta_auditor_p;

	if (nr_sequencia_w IS NOT NULL AND nr_sequencia_w::text <> '') then
		select	substr(obter_nome_usuario(nm_usuario_exec),1,80)
		into STRICT	ds_retorno_w
		from	pls_membro_grupo_aud
		where	ie_auditor_ptu	= 'S'
		and	nr_sequencia	= nr_sequencia_w;
	end if;

end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_dados_conta_auditor ( nr_seq_conta_auditor_p pls_conta_auditor.nr_sequencia%type, ie_opcao_p text) FROM PUBLIC;

