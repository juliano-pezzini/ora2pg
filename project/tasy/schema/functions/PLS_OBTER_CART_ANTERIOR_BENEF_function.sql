-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_cart_anterior_benef (nr_seq_segurado_p bigint) RETURNS varchar AS $body$
DECLARE


cd_usuario_ant_w	varchar(30)	:= '';


BEGIN

if (coalesce(nr_seq_segurado_p,0) <> 0) then

	select	max(cd_usuario_ant)
	into STRICT	cd_usuario_ant_w
	from	pls_segurado_cart_ant
	where	nr_seq_segurado	= nr_seq_segurado_p
	and	dt_alteracao	=
			(SELECT	max(dt_alteracao)
			from	pls_segurado_cart_ant
			where	nr_seq_segurado	= nr_seq_segurado_p);
end if;

return cd_usuario_ant_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_cart_anterior_benef (nr_seq_segurado_p bigint) FROM PUBLIC;

