-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_cd_procedimento ( nr_seq_proc_cpoe_p bigint, nr_atendimento_p bigint) RETURNS bigint AS $body$
DECLARE

nr_prescricao_w		prescr_procedimento.nr_prescricao%type;
cd_procedimento_w	proc_interno.ie_origem_proced%type;
ie_origem_w			proc_interno.ie_origem_proced%type;
						

BEGIN

select max(nr_prescricao)
into STRICT nr_prescricao_w
from prescr_procedimento
where nr_seq_proc_cpoe = nr_seq_proc_cpoe_p;

SELECT * FROM Obter_Proc_Tab_Interno(nr_seq_proc_cpoe_p, nr_prescricao_w, nr_atendimento_p, 0, cd_procedimento_w, ie_origem_w) INTO STRICT cd_procedimento_w, ie_origem_w;

return cd_procedimento_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_cd_procedimento ( nr_seq_proc_cpoe_p bigint, nr_atendimento_p bigint) FROM PUBLIC;

