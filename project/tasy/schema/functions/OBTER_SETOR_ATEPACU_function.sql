-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_setor_atepacu ( nr_seq_interno_p bigint, ie_opcao_p bigint) RETURNS varchar AS $body$
DECLARE



cd_setor_atendimento_w	integer;
ds_retorno_w		varchar(100);


BEGIN

if (coalesce(nr_seq_interno_p,0) <> 0) then
	select	cd_setor_atendimento
	into STRICT	cd_setor_atendimento_w
	from	atend_paciente_unidade
	where	nr_seq_interno = nr_seq_interno_p;

	if (ie_opcao_p = 0) then
		ds_retorno_w := cd_setor_Atendimento_w;
	elsif (ie_opcao_p = 1) then
		select	ds_setor_atendimento
		into STRICT	ds_retorno_w
		from setor_atendimento
		where cd_setor_atendimento = cd_setor_atendimento_w;
	end if;
end if;

RETURN ds_retorno_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_setor_atepacu ( nr_seq_interno_p bigint, ie_opcao_p bigint) FROM PUBLIC;

