-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION sus_obter_carater_inter_uf ( cd_pessoa_fisica_p text, cd_procedimento_p bigint, cd_estabelecimento_p bigint ) RETURNS varchar AS $body$
DECLARE


sg_uf_w			compl_pessoa_fisica.sg_estado%type;
ie_carater_inter_sus_w	varchar(2);


BEGIN

select	max(coalesce(sg_estado,''))
into STRICT	sg_uf_w
from 	compl_pessoa_fisica
where 	cd_pessoa_fisica	= cd_pessoa_fisica_p
and 	ie_tipo_complemento	= 1;

if (sg_uf_w IS NOT NULL AND sg_uf_w::text <> '') then
	begin
	select	ie_carater_inter_sus
	into STRICT	ie_carater_inter_sus_w
	from	sus_carater_uf
	where	cd_procedimento		= cd_procedimento_p
	and	ie_origem_proced	= 2
	and	((cd_unidade_federacao	= sg_uf_w)
	or 	((coalesce(cd_unidade_federacao::text, '') = '') and not exists (	SELECT	1
	   						  	 	from	sus_carater_uf
									where	cd_procedimento		= cd_procedimento_p
									and	ie_origem_proced	= 2
									and	cd_unidade_federacao	= sg_uf_w)))
	and	cd_estabelecimento	= cd_estabelecimento_p;
	exception
		when others then
		ie_carater_inter_sus_W	:= '';
	end;

end if;

return	ie_carater_inter_sus_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION sus_obter_carater_inter_uf ( cd_pessoa_fisica_p text, cd_procedimento_p bigint, cd_estabelecimento_p bigint ) FROM PUBLIC;

