-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_qt_senhas_aguar_fila (nr_seq_fila_p bigint, dt_inicio_p timestamp, dt_fim_p timestamp) RETURNS bigint AS $body$
DECLARE


qt_senha_fila_w	bigint;


BEGIN

select	count(*)
into STRICT	qt_senha_fila_w
from 	paciente_senha_fila
where	coalesce(dt_inicio_atendimento::text, '') = ''
and	coalesce(dt_fim_atendimento::text, '') = ''
and	coalesce(dt_inutilizacao::text, '') = ''
and	nr_seq_fila_senha = nr_seq_fila_p
and 	trunc(dt_entrada_fila) between trunc(dt_inicio_p) and trunc(dt_fim_p);


return	qt_senha_fila_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_qt_senhas_aguar_fila (nr_seq_fila_p bigint, dt_inicio_p timestamp, dt_fim_p timestamp) FROM PUBLIC;

