-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_sip_obter_se_sip_gerado ( nr_seq_lote_sip_p pls_lote_sip.nr_sequencia%Type) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(1);
qt_registro_w	bigint;

BEGIN

if (nr_seq_lote_sip_p IS NOT NULL AND nr_seq_lote_sip_p::text <> '') then
	begin
		select	count(1)
		into STRICT	qt_registro_w
		from	sip_lote_item_assistencial
		where	nr_seq_lote = nr_seq_lote_sip_p
		and	(nr_seq_lote IS NOT NULL AND nr_seq_lote::text <> '')  LIMIT 1;
	exception
	when no_data_found then
		qt_registro_w := null;
	end;

	if (qt_registro_w IS NOT NULL AND qt_registro_w::text <> '') and (qt_registro_w > 0) then
		ds_retorno_w := 'S';
	else
		ds_retorno_w := 'N';
	end if;
end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_sip_obter_se_sip_gerado ( nr_seq_lote_sip_p pls_lote_sip.nr_sequencia%Type) FROM PUBLIC;

