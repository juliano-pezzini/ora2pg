-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_dente_protese_odont ( nr_seq_area_p bigint, nr_seq_dente_p bigint, ie_protese_sup_p text, ie_protese_inf_p text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w 	varchar(1) := 'N';
nr_seq_dente_w	area_imagem_item.nr_seq_dente%type := nr_seq_dente_p;


BEGIN

if (coalesce(nr_seq_dente_w::text, '') = '') then
	select	max(nr_seq_dente)
	into STRICT	nr_seq_dente_w
	from	area_imagem_item
	where	nr_seq_area = nr_seq_area_p;
end if;

if (nr_seq_dente_w IS NOT NULL AND nr_seq_dente_w::text <> '') then

	select	coalesce(max('S'),'N')
	into STRICT	ds_retorno_w
	
	where	((ie_protese_sup_p = 'S' and (nr_seq_dente_w between 10 and 29 or nr_seq_dente_w between 50 and 66)) or (ie_protese_inf_p = 'S' and (nr_seq_dente_w between 30 and 49 or nr_seq_dente_w between 70 and 86)));

end if;

return ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_dente_protese_odont ( nr_seq_area_p bigint, nr_seq_dente_p bigint, ie_protese_sup_p text, ie_protese_inf_p text) FROM PUBLIC;

