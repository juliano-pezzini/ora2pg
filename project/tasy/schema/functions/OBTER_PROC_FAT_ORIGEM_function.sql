-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_proc_fat_origem ( nr_seq_proc_interno_p bigint, ie_origem_proced_p bigint) RETURNS varchar AS $body$
DECLARE


ds_resultado_w	varchar(20);



BEGIN

if (nr_seq_proc_interno_p IS NOT NULL AND nr_seq_proc_interno_p::text <> '') then
		select	max(cd_procedimento)
		into STRICT	ds_resultado_w
		from	proc_interno
		where	nr_sequencia	= nr_seq_proc_interno_p
		and 	ie_origem_proced  = coalesce(ie_origem_proced_p,ie_origem_proced);
end if;

return	ds_resultado_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_proc_fat_origem ( nr_seq_proc_interno_p bigint, ie_origem_proced_p bigint) FROM PUBLIC;

