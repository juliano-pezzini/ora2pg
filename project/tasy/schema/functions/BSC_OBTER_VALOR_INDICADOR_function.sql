-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION bsc_obter_valor_indicador ( nr_seq_ind_inf_p bigint, nr_seq_formula_p bigint) RETURNS bigint AS $body$
DECLARE


cd_ano_w			smallint;
cd_periodo_w			smallint;
nr_seq_indicador_w			bigint;
qt_real_w				double precision;


BEGIN
/* Indicador de referência*/

select	nr_seq_ind_ref
into STRICT	nr_seq_indicador_w
from	bsc_regra_formula
where	nr_sequencia	= nr_seq_formula_p;

/* Ano/período que está sendo calculado */

select	cd_ano,
	cd_periodo
into STRICT	cd_ano_w,
	cd_periodo_w
from	bsc_ind_inf
where	nr_sequencia 	= nr_seq_ind_inf_p;

/*Quantidade Real no período do Indicador de Referencia que está sendo utilizado para comparar os valores*/

select	coalesce(max(qt_real),0)
into STRICT	qt_real_w
from	bsc_ind_inf
where	nr_seq_indicador	= nr_seq_indicador_w
and	cd_ano		= cd_ano_w
and	cd_periodo	= cd_periodo_w;

return	qt_real_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION bsc_obter_valor_indicador ( nr_seq_ind_inf_p bigint, nr_seq_formula_p bigint) FROM PUBLIC;

