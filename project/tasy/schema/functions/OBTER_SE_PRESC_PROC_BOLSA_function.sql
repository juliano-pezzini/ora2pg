-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_presc_proc_bolsa (nr_seq_proc_bolsa_p bigint) RETURNS varchar AS $body$
DECLARE


ie_retorno_w	varchar(10);

BEGIN

select	CASE WHEN count(1)=0 THEN 'N'  ELSE 'S' END  ie_prescr_proc_bolsa
into STRICT	ie_retorno_w
from	prescr_proc_bolsa b,
	prescr_proc_hor c
where	b.nr_prescricao = c.nr_prescricao
and	b.nr_seq_procedimento = c.nr_seq_procedimento
and	b.nr_sequencia = nr_seq_proc_bolsa_p
and	obter_status_hor_sol_adep(c.dt_inicio_horario,c.dt_fim_horario,c.dt_suspensao,c.dt_interrupcao,c.ie_dose_especial,null,null,c.nr_prescricao,null) not in ('N','S')
and	coalesce(b.ie_bipado, 'N') = 'S';

return	ie_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_presc_proc_bolsa (nr_seq_proc_bolsa_p bigint) FROM PUBLIC;

