-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_codigo_simpro_tiss (cd_material_p bigint, cd_estabelecimento_p bigint, cd_convenio_p bigint, dt_referencia_p timestamp) RETURNS varchar AS $body$
DECLARE


/*
function duplicada da  TISS_Obter_Codigo_Simpro, a diferença é que esta busca o campo CD_SIMPRO_TISS
*/
cd_simpro_w		varchar(255) := null;
ie_tipo_convenio_w	smallint;

c01 CURSOR FOR
SELECT	lpad(cd_simpro,10,0)
from	material_simpro
where	cd_material					= cd_material_p
and	coalesce(cd_estabelecimento,coalesce(cd_estabelecimento_p,0))	= coalesce(cd_estabelecimento_p,0)
and	coalesce(cd_convenio,coalesce(cd_convenio_p,0))		= coalesce(cd_convenio_p,0)
and	coalesce(ie_tipo_convenio, coalesce(ie_tipo_convenio_w,0)) = coalesce(ie_tipo_convenio_w,0)
and	coalesce(dt_referencia_p,clock_timestamp()) between coalesce(dt_vigencia,to_date('01/01/1988','dd/mm/yyyy')) and trunc(coalesce(dt_final_vigencia,clock_timestamp())) + 89399/89400
order by 	coalesce(cd_convenio,0),
	coalesce(ie_tipo_convenio,0),
	coalesce(cd_estabelecimento,0);


BEGIN

if (coalesce(cd_convenio_p,0) > 0) then
	select	coalesce(max(ie_tipo_convenio),0)
	into STRICT	ie_tipo_convenio_w
	from	convenio
	where	cd_convenio = cd_convenio_p;
end if;

open c01;
loop
fetch c01 into
	cd_simpro_w;
EXIT WHEN NOT FOUND; /* apply on c01 */

end loop;
close c01;

return cd_simpro_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_codigo_simpro_tiss (cd_material_p bigint, cd_estabelecimento_p bigint, cd_convenio_p bigint, dt_referencia_p timestamp) FROM PUBLIC;

