-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_simpro_preco_ant (cd_simpro_p bigint, dt_vigencia_p timestamp, ie_tipo_preco_p text) RETURNS bigint AS $body$
DECLARE


vl_preco_venda_w	double precision;
vl_preco_fabrica_w	double precision;
nr_sequencia_w		bigint;
cd_estab_simpro_w	estabelecimento.cd_estabelecimento%type;


BEGIN

cd_estab_simpro_w := wheb_usuario_pck.get_cd_estabelecimento;

if (cd_simpro_p > 0) then
	select 	coalesce(max(vl_preco_venda),0),
		coalesce(max(vl_preco_fabrica),0)
	into STRICT	vl_preco_venda_w,
		vl_preco_fabrica_w
	from	simpro_preco
	where	cd_simpro = cd_simpro_p
	and	coalesce(cd_estabelecimento, coalesce(cd_estab_simpro_w, 0)) = coalesce(cd_estab_simpro_w, 0)
	and	dt_vigencia = (
				SELECT max(dt_vigencia)
				from simpro_preco
				where cd_simpro = cd_simpro_p
				and   coalesce(cd_estabelecimento, coalesce(cd_estab_simpro_w, 0)) = coalesce(cd_estab_simpro_w, 0)
				and   dt_vigencia < dt_vigencia_p);
end if;
if (ie_tipo_preco_p = 'V') then
	return	vl_preco_venda_w;
else
	return 	vl_preco_fabrica_w;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_simpro_preco_ant (cd_simpro_p bigint, dt_vigencia_p timestamp, ie_tipo_preco_p text) FROM PUBLIC;

