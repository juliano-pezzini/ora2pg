-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_elem_kgday_cpoenpte_md (tp_calc_p text, qt_nao_discontar_p bigint, qt_volume_p bigint, qt_diaria_p bigint, qt_discontar_p bigint, qt_peso_p bigint ) RETURNS bigint AS $body$
DECLARE

    result_w bigint := 0.0;

BEGIN
    if (tp_calc_p = 'E') then
      result_w := dividir_sem_round_md((qt_nao_discontar_p * qt_volume_p),qt_diaria_p) + dividir_sem_round_md(coalesce(qt_discontar_p,0),qt_peso_p);
    else
      result_w := dividir_md(qt_volume_p,qt_diaria_p);
    end if;
	
    return coalesce(result_w,0);

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_elem_kgday_cpoenpte_md (tp_calc_p text, qt_nao_discontar_p bigint, qt_volume_p bigint, qt_diaria_p bigint, qt_discontar_p bigint, qt_peso_p bigint ) FROM PUBLIC;

