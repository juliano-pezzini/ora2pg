-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_spa_pend_parecer (NR_SEQ_SPA_P bigint, IE_OPCAO_P bigint) RETURNS bigint AS $body$
DECLARE


IE_RESP_PARECER_W	varchar(1);


BEGIN

IF (IE_OPCAO_P = 1) THEN
begin
	SELECT	SUBSTR(DS_PEDIDO_PARECER,0,1)
	INTO STRICT	IE_RESP_PARECER_W
	FROM	SPA_PARECER
	WHERE	NR_SEQ_SPA = NR_SEQ_SPA_P;

	IF (IE_RESP_PARECER_W <> '') THEN
		IE_RESP_PARECER_W := 'S';
	else
		IE_RESP_PARECER_W := 'N';
	end if;
end;
end if;

IF (IE_OPCAO_P = 2) THEN
begin
	SELECT	SUBSTR(DS_RESPOSTA_PARECER,0,1)
	INTO STRICT	IE_RESP_PARECER_W
	FROM	SPA_PARECER
	WHERE	NR_SEQ_SPA = NR_SEQ_SPA_P;

	IF (IE_RESP_PARECER_W = '') THEN
		IE_RESP_PARECER_W := 'S';
	else
		IE_RESP_PARECER_W := 'N';
	end if;
end;
end if;

RETURN	IE_RESP_PARECER_W;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_spa_pend_parecer (NR_SEQ_SPA_P bigint, IE_OPCAO_P bigint) FROM PUBLIC;

