-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION tiss_obter_clinica_autor (ie_clinica_p bigint, nr_seq_classificacao_p text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(20);


C01 CURSOR FOR
	SELECT	ie_tipo_internacao_tiss
	from	tiss_tipo_internacao
	WHERE	coalesce(ie_clinica,coalesce(ie_clinica_p,0))	= coalesce(ie_clinica_p,0)
	AND    coalesce(nr_seq_classificacao,coalesce(nr_seq_classificacao_p,0)) = coalesce(nr_seq_classificacao_p,0)
	order by coalesce(nr_seq_classificacao,0),
		 coalesce(ie_clinica,0);


BEGIN
open C01;
loop
fetch C01 into
	ds_retorno_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin
	ds_retorno_w := ds_retorno_w;
	end;
end loop;
close C01;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION tiss_obter_clinica_autor (ie_clinica_p bigint, nr_seq_classificacao_p text) FROM PUBLIC;

