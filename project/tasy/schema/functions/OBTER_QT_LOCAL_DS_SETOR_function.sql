-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_qt_local_ds_setor (nr_seq_local_p bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(255);


BEGIN

if (nr_seq_local_p IS NOT NULL AND nr_seq_local_p::text <> '') then
	select 	substr(obter_desc_expressao(max(b.cd_exp_setor_atend),max(b.ds_setor_atendimento)),1,255)
	into STRICT	ds_retorno_w
	from 	qt_local a,
			setor_atendimento b
	where	a.nr_sequencia = nr_seq_local_p
	and 	a.cd_setor_atendimento = b.cd_setor_atendimento;
end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_qt_local_ds_setor (nr_seq_local_p bigint) FROM PUBLIC;

