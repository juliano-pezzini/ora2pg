-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_macro_ci_status () RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(2000);


BEGIN

ds_retorno_w	:= 	replace(wheb_mensagem_pck.get_texto(800229), '@paciente', obter_traducao_macro_pront('@paciente',820)) ||CHR(10)||
			replace(wheb_mensagem_pck.get_texto(800226), '@agenda', obter_traducao_macro_pront('@agenda',820)) ||CHR(10)||
			replace(wheb_mensagem_pck.get_texto(800228), '@horario', obter_traducao_macro_pront('@horario',820));

RETURN	ds_retorno_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_macro_ci_status () FROM PUBLIC;

