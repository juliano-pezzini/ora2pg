-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION man_obter_se_possui_val_prog ( nr_seq_os_p bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w		varchar(1) := 'N';
nm_usuario_w		varchar(15);
qt_hist_val_prog_w		bigint;
nr_seq_os_w		bigint;


BEGIN
/*
select	nvl(max(a.nr_seq_os),0)
into	nr_seq_os_w
from	w_migracao_ordem_servico a
where	a.nr_seq_os_migracao = nr_seq_os_p;

select	nvl(max(a.nm_usuario_exec),'')
into	nm_usuario_w
from	man_ordem_servico a
where	a.nr_sequencia = nr_seq_os_w;

select	count(*)
into	qt_hist_val_prog_w
from	man_ordem_serv_tecnico a
where	a.nr_seq_ordem_serv = nr_seq_os_p
and		a.nr_seq_tipo = 99
and		a.dt_liberacao is not null
and		a.nm_usuario = nm_usuario_w;

if	(qt_hist_val_prog_w > 0) then
	ds_retorno_w := 'S';
end if;*/
return	'S';

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION man_obter_se_possui_val_prog ( nr_seq_os_p bigint) FROM PUBLIC;

