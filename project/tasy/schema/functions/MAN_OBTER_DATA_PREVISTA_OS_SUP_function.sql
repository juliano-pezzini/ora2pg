-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION man_obter_data_prevista_os_sup ( nr_seq_ordem_serv_p bigint ) RETURNS timestamp AS $body$
DECLARE


dt_prevista_w			timestamp;


BEGIN

select	max(a.dt_prevista)
into STRICT	dt_prevista_w
from	man_ordem_ativ_prev a
where	a.nr_seq_ordem_serv = nr_seq_ordem_serv_p
and	a.nm_usuario_prev = (	SELECT	max(c.nm_usuario_exec)
				from	usuario_grupo_sup b,
					man_ordem_servico_exec c
				where	b.nm_usuario_grupo = c.nm_usuario_exec
				and	c.nr_seq_ordem = nr_seq_ordem_serv_p
				and	coalesce(c.dt_fim_execucao::text, '') = ''
			);

return	dt_prevista_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION man_obter_data_prevista_os_sup ( nr_seq_ordem_serv_p bigint ) FROM PUBLIC;

