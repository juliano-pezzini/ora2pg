-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION hsc_obter_saldo_mes_balancete ( cd_grupo_p bigint, ano_ref_p bigint, cd_classificacao_p text, cd_emp bigint, cd_estab_p bigint, ie_normal_encerramento_p text, nr_nivel_p bigint, ie_balancete_p text, mes_p text, ie_zero_p text, cd_conta_contabil_p text) RETURNS varchar AS $body$
DECLARE


vl_saldo_mes_w	 	double precision;
nr_seq_mes_ref_w	bigint;


BEGIN

select 	min(nr_seq_mes_ref)
into STRICT	nr_seq_mes_ref_w
from	ctb_balancete_v2
where	TO_CHAR(dt_referencia, 'mm') = mes_p
and	nr_seq_mes_ref >= ano_ref_p
and	cd_estabelecimento = cd_estab_p;


SELECT	coalesce(SUM(a.vl_movimento),0) vl_saldo
INTO STRICT	vl_saldo_mes_w
FROM 	ctb_balancete_v2 a
WHERE 	a.cd_grupo	= cd_grupo_p
AND	a.dt_referencia BETWEEN ctb_obter_mes_ref(ano_ref_p) AND fim_ano(ctb_obter_mes_ref(ano_ref_p))
AND (CTB_Obter_Se_Conta_Classif_Sup(a.cd_conta_contabil, cd_classificacao_p)	= 'S' OR cd_classificacao_p = '0')
AND	a.cd_empresa	= cd_emp
AND	((cd_estab_p = '0') OR (a.cd_estabelecimento = cd_estab_p))
AND (a.ie_normal_encerramento	= ie_normal_encerramento_p)
AND (SUBSTR(obter_se_conta_vigente(a.cd_conta_contabil, ctb_obter_mes_ref(a.nr_seq_mes_ref)),1,1) = 'S')
AND	ctb_obter_nivel_classif_conta(cd_classificacao) <= nr_nivel_p
AND	ie_balancete_p = ie_balancete_p
AND	a.nr_seq_mes_ref = nr_seq_mes_ref_w
AND	a.cd_conta_contabil = cd_conta_contabil_p
GROUP BY 	a.ie_tipo,
	a.cd_conta_contabil,
	a.cd_classificacao,
	a.ds_conta_apres,
	a.ie_centro_custo;



RETURN vl_saldo_mes_w;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION hsc_obter_saldo_mes_balancete ( cd_grupo_p bigint, ano_ref_p bigint, cd_classificacao_p text, cd_emp bigint, cd_estab_p bigint, ie_normal_encerramento_p text, nr_nivel_p bigint, ie_balancete_p text, mes_p text, ie_zero_p text, cd_conta_contabil_p text) FROM PUBLIC;

