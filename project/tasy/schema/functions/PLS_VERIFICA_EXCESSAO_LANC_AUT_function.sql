-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_verifica_excessao_lanc_aut (nr_seq_regra_p bigint, nr_seq_requisicao_p bigint, nr_seq_guia_p bigint, nr_seq_execucao_p bigint) RETURNS varchar AS $body$
DECLARE


/* +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
finalidade: verificar as regras de exceção da função ops - regras de lançamento automáticas.
---------------------------------------------------------------------------------------------------
locais de chamada direta:
[ ]  objetos do dicionário [x] tasy (delphi/java) [ ] portal [ ]  relatórios [ ] outros:
 --------------------------------------------------------------------------------------------------
pontos de atenção:performance.
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ */
cd_espec_regra_w		pls_regra_exc_lanc_guia.cd_especialidade%type;
nr_seq_cbo_saude_regra_w	pls_regra_exc_lanc_guia.nr_seq_cbo_saude%type;

cd_especialidade_w		pls_requisicao.cd_especialidade%type;
nr_seq_cbo_saude_w		pls_requisicao.nr_seq_cbo_saude%type;

ds_retorno_w			varchar(1);

c01 CURSOR FOR
	SELECT	cd_especialidade,
		nr_seq_cbo_saude
	from	pls_regra_exc_lanc_guia
	where	nr_seq_regra		= nr_seq_regra_p
	and (cd_especialidade 	= cd_especialidade_w or coalesce(cd_especialidade::text, '') = '')
	and (nr_seq_cbo_saude	= nr_seq_cbo_saude_w or coalesce(nr_seq_cbo_saude::text, '') = '');


BEGIN
ds_retorno_w	:= 'N';

if (nr_seq_requisicao_p IS NOT NULL AND nr_seq_requisicao_p::text <> '') then
	begin
		select	cd_especialidade,
			nr_seq_cbo_saude
		into STRICT	cd_especialidade_w,
			nr_seq_cbo_saude_w
		from	pls_requisicao
		where	nr_sequencia	= nr_seq_requisicao_p;
	exception
	when others then
		cd_especialidade_w	:= 0;
		nr_seq_cbo_saude_w	:= 0;
	end;
end if;

-- elsif	(nr_seq_guia_p	is not null) then
-- implementar quando houver necessidade
-- elsif	(nr_seq_execucao_p	is not null) then
-- implementar quando houver necessidade
open c01;
loop
fetch c01 into
	cd_espec_regra_w,
	nr_seq_cbo_saude_regra_w;
EXIT WHEN NOT FOUND; /* apply on c01 */
	begin
		ds_retorno_w	:= 'S';
		exit;
	end;
end loop;
close c01;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_verifica_excessao_lanc_aut (nr_seq_regra_p bigint, nr_seq_requisicao_p bigint, nr_seq_guia_p bigint, nr_seq_execucao_p bigint) FROM PUBLIC;

