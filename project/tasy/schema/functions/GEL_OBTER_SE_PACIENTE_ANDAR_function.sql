-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION gel_obter_se_paciente_andar (cd_pessoa_fisica_p bigint, cd_setor_p bigint) RETURNS varchar AS $body$
DECLARE

 
ds_retorno_w	varchar(255);
cd_setor_atendimento_w	integer;
ds_setor_atendimento_w	varchar(255);


BEGIN 
 
IF (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') THEN 
 
  SELECT MAX(obter_setor_atendimento(nr_atendimento)) 
  INTO STRICT cd_setor_atendimento_w 
  FROM atendimento_paciente 
  WHERE cd_pessoa_fisica = cd_pessoa_fisica_p;
 
  IF (cd_setor_atendimento_w IS NOT NULL AND cd_setor_atendimento_w::text <> '') AND (cd_setor_atendimento_w	= cd_setor_p) THEN 
 
	ds_retorno_w	:= 'S';
 
  ELSIF (cd_setor_atendimento_w IS NOT NULL AND cd_setor_atendimento_w::text <> '') AND (cd_setor_atendimento_w	<> cd_setor_p) THEN 
 
	SELECT	substr(obter_desc_expressao(max(cd_exp_setor_atend),max(ds_setor_atendimento)),1,100) 
	INTO STRICT	ds_setor_atendimento_w 
	FROM	setor_atendimento 
	WHERE	cd_setor_atendimento	= cd_setor_atendimento_w;
 
	ds_retorno_w	:= Wheb_mensagem_pck.get_texto(308444, 'DS_SETOR_ATENDIMENTO_W='||ds_setor_atendimento_w); --'Este paciente pertence ao andar '||ds_setor_atendimento_w||'!'; 
  END IF;
 
END IF;
 
RETURN	ds_retorno_w;
 
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION gel_obter_se_paciente_andar (cd_pessoa_fisica_p bigint, cd_setor_p bigint) FROM PUBLIC;

