-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION verifica_se_compila_proj_swing (nm_projeto_p text, dt_ult_commit_p timestamp) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(1);
dt_ult_commit_w	timestamp;

BEGIN

ds_retorno_w := 'N';

select	max(dt_ult_commit)
into STRICT	dt_ult_commit_w
from	aplicacao_swing_log
where	nm_projeto = nm_projeto_p;

if	((coalesce(dt_ult_commit_w::text, '') = '') or (dt_ult_commit_w < dt_ult_commit_p))  then
	ds_retorno_w := 'S';
end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION verifica_se_compila_proj_swing (nm_projeto_p text, dt_ult_commit_p timestamp) FROM PUBLIC;

