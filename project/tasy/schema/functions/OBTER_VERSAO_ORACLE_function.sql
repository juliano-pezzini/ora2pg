-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_versao_oracle () RETURNS bigint AS $body$
DECLARE


v1			varchar(1000);
v2			varchar(1000);
ov_w			bigint;


BEGIN

begin
dbms_utility.db_version(v1, v2);

v2	:=	coalesce(v2, v1);

ov_w	:=	somente_numero(substr(v2, 1, position('.' in v2)-1));
exception
when others then
	ov_w	:=	null;
end;

return	ov_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_versao_oracle () FROM PUBLIC;

