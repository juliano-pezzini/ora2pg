-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION lab_obter_se_gravar_result (nr_seq_exame_p bigint) RETURNS varchar AS $body$
DECLARE


nr_seq_grupo_w	bigint;
ds_retorno_w	varchar(1) := 'N';


BEGIN

select	max(nr_seq_grupo)
into STRICT	nr_seq_grupo_w
from	exame_laboratorio
where 	nr_seq_exame = nr_seq_exame_p;

select	CASE WHEN count(*)=0 THEN 'N'  ELSE 'S' END
into STRICT	ds_retorno_w
from	lab_regra_gravar_result
where	coalesce(nr_seq_exame,nr_seq_exame_p)	= nr_seq_exame_p
and	coalesce(nr_seq_grupo,nr_seq_grupo_w)	= nr_seq_grupo_w;

return ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION lab_obter_se_gravar_result (nr_seq_exame_p bigint) FROM PUBLIC;

