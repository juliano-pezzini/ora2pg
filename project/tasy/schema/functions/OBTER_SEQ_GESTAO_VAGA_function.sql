-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_seq_gestao_vaga (nr_seq_agenda_p bigint) RETURNS bigint AS $body$
DECLARE

 
ds_retorno_w	bigint;


BEGIN 
 
if (nr_seq_agenda_p IS NOT NULL AND nr_seq_agenda_p::text <> '') then 
	begin 
		select	max(nr_sequencia) 
		into STRICT	ds_retorno_w 
		from	gestao_vaga a 
		where	a.nr_seq_agenda = nr_seq_agenda_p;
	exception 
	when others then 
		ds_retorno_w := 0;
	end;
end if;
 
RETURN	ds_retorno_w;
 
END	;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_seq_gestao_vaga (nr_seq_agenda_p bigint) FROM PUBLIC;

