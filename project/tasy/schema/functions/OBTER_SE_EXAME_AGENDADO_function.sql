-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_exame_agendado (nr_prescricao_p bigint, nr_seq_prescr_p bigint) RETURNS varchar AS $body$
DECLARE


ie_agendado_w	varchar(1) := 'N';
nr_seq_proc_exclui_w bigint;
dt_hora_agenda_w	timestamp;


BEGIN


select 	coalesce(max(nr_seq_proc_excl),0)
into STRICT	nr_seq_proc_exclui_w
from	prescr_procedimento
where 	nr_prescricao = nr_prescricao_p
and		nr_sequencia = nr_seq_prescr_p;

if (nr_seq_proc_exclui_w = 0) then
	begin

	SELECT  max(coalesce(Obter_Horario_item_Ageint(a.nr_seq_agenda_cons, a.nr_Seq_Agenda_exame,a.nr_sequencia),qt_obter_horario_agendado(a.nr_sequencia)))
	into STRICT	dt_hora_agenda_w
	FROM	agenda_integrada_item a
	WHERE	a.nr_prescricao = nr_prescricao_p
	and		a.nr_sequencia_prescricao = nr_seq_prescr_p;

	if (coalesce(dt_hora_agenda_w::text, '') = '') then

		select 	max(dt_prevista)
		into STRICT	dt_hora_agenda_w
		from    ageint_exame_lab a,
			    agenda_integrada_item b
		where   a.nr_seq_ageint = b.nr_seq_agenda_int
		and	 	b.nr_prescricao = nr_prescricao_p;

	end if;

	end;
else
	begin

	SELECT  max(coalesce(Obter_Horario_item_Ageint(a.nr_seq_agenda_cons, a.nr_Seq_Agenda_exame,a.nr_sequencia),qt_obter_horario_agendado(a.nr_sequencia)))
	into STRICT	dt_hora_agenda_w
	FROM	agenda_integrada_item a,
			prescr_procedimento b
	WHERE	b.nr_seq_interno = nr_seq_proc_exclui_w
	and		b.nr_prescricao = a.nr_prescricao
	and		b.nr_sequencia = a.nr_sequencia_prescricao;

	end;
end if;

if (dt_hora_agenda_w IS NOT NULL AND dt_hora_agenda_w::text <> '') then

	ie_agendado_w := 'S';

end if;

return	ie_agendado_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_exame_agendado (nr_prescricao_p bigint, nr_seq_prescr_p bigint) FROM PUBLIC;

