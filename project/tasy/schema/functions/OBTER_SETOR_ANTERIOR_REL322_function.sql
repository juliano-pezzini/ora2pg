-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_setor_anterior_rel322 ( nr_atendimento_p bigint ) RETURNS bigint AS $body$
DECLARE

cd_setor_w	bigint;

BEGIN

if (coalesce(nr_atendimento_p,0) > 0) then

	select	max(cd_setor_atendimento)
	into STRICT	cd_setor_w
	from	atend_paciente_unidade
	where	(dt_saida_unidade IS NOT NULL AND dt_saida_unidade::text <> '')
	and	nr_atendimento = nr_atendimento_p
	and	dt_entrada_unidade  = 	(	SELECT 	max(b.dt_entrada_unidade)
						from	atend_paciente_unidade b
						where	b.nr_atendimento = nr_atendimento_p
						and	(b.dt_saida_unidade IS NOT NULL AND b.dt_saida_unidade::text <> '')
					);
end if;

return	cd_setor_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_setor_anterior_rel322 ( nr_atendimento_p bigint ) FROM PUBLIC;

