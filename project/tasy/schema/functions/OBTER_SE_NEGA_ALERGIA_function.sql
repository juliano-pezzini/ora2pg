-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_nega_alergia (cd_pessoa_fisica_p text) RETURNS varchar AS $body$
DECLARE


ie_nega_alergia_w 			varchar(1) := 'N';
ie_liberar_hist_saude_w		varchar(1);


BEGIN

if (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') then

	select	coalesce(max(ie_liberar_hist_saude),'N')
	into STRICT	ie_liberar_hist_saude_w
	from	parametro_medico
	where	cd_estabelecimento = wheb_usuario_pck.get_cd_estabelecimento;


	select	coalesce(max('S'),'N')
	into STRICT	ie_nega_alergia_w
	from	paciente_alergia a
	where	cd_pessoa_fisica = cd_pessoa_fisica_p
	and		coalesce(a.ie_nega_alergias,'N')	= 'S'
	and		((coalesce(a.dt_fim::text, '') = '') or (clock_timestamp() between coalesce(a.dt_inicio,clock_timestamp() - interval '1 days') and a.dt_fim))
	and		coalesce(a.dt_inativacao::text, '') = ''
	and 	((ie_liberar_hist_saude_w = 'N') or (dt_liberacao IS NOT NULL AND dt_liberacao::text <> ''));

end if;

return ie_nega_alergia_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_nega_alergia (cd_pessoa_fisica_p text) FROM PUBLIC;

