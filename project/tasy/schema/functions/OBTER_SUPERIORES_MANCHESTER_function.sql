-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_superiores_manchester (nr_seq_fluxo_p bigint, ie_leitura_obrigatoria_p text) RETURNS varchar AS $body$
DECLARE


ds_superiores_w varchar(255) := '';
ds_leitura_obr_w varchar(255);

c01 CURSOR FOR
	SELECT	ie_classificacao_risco||'/'||nr_seq_item||':'||coalesce(b.qt_valor_maximo,0)||','||coalesce(b.qt_valor_minimo,0) valores,
			case when b.ie_leitura_obrigatoria in ('PS','PD') then b.ie_leitura_obrigatoria||'=' else '' end identificador
	from    manchester_fluxograma_item a,
			manchester_item b
	where   nr_seq_fluxo = nr_seq_fluxo_p
	and     coalesce(a.ie_versao,'1') = '2'
	and     coalesce(b.ie_versao,'1') = '2'
	and     b.nr_sequencia = a.nr_seq_item
	and     obter_se_contido_char(b.ie_leitura_obrigatoria,ds_leitura_obr_w) = 'S'
	and     (b.ie_leitura_obrigatoria IS NOT NULL AND b.ie_leitura_obrigatoria::text <> '')
	order by nr_seq_apresentacao;

BEGIN

if (nr_seq_fluxo_p > 0) and (ie_leitura_obrigatoria_p <> ' ') then

	if (ie_leitura_obrigatoria_p = 'PS') then
		ds_leitura_obr_w := 'PS,PD';
	else
		ds_leitura_obr_w := ie_leitura_obrigatoria_p;
	end if;

	for itens in c01 loop
		ds_superiores_w := ds_superiores_w || itens.identificador || itens.valores || ';';
	end loop;

end if;

return ds_superiores_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_superiores_manchester (nr_seq_fluxo_p bigint, ie_leitura_obrigatoria_p text) FROM PUBLIC;

