-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION hd_obter_unid_escala ( CD_PESSOA_FISICA_P text) RETURNS bigint AS $body$
DECLARE


vl_retorno_w		bigint;
/*
Retorna a unidade da Escala com a data de início mais recente.
 */
BEGIN
if (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') then

	SELECT 	max(nr_seq_unid_dialise)
	INTO STRICT	vl_retorno_w
	FROM 	hd_escala_dialise
	WHERE 	dt_inicio = (SELECT MAX(dt_inicio)
			 FROM 	HD_ESCALA_DIALISE
			 WHERE 	cd_pessoa_fisica = cd_pessoa_fisica_p
			 AND 	coalesce(dt_fim::text, '') = '');

end if;

RETURN vl_retorno_w;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION hd_obter_unid_escala ( CD_PESSOA_FISICA_P text) FROM PUBLIC;

