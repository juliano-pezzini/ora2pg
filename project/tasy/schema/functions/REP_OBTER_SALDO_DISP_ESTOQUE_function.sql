-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION rep_obter_saldo_disp_estoque ( cd_setor_p bigint, cd_estab_p bigint, cd_material_p text) RETURNS bigint AS $body$
DECLARE


cd_local_estoque_w	bigint := 0;
qt_saldo_disp_estoque_w	bigint := 0;


BEGIN
if (cd_setor_p IS NOT NULL AND cd_setor_p::text <> '') and (cd_estab_p IS NOT NULL AND cd_estab_p::text <> '') and (cd_material_p IS NOT NULL AND cd_material_p::text <> '') then
	begin
	select	coalesce(obter_local_estoque_setor(cd_setor_p, cd_estab_p), 0)
	into STRICT	cd_local_estoque_w
	;


	select	obter_saldo_disp_estoque(cd_estab_p, cd_material_p, cd_local_estoque_w, clock_timestamp())
	into STRICT	qt_saldo_disp_estoque_w
	;
	end;
end if;
return	qt_saldo_disp_estoque_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION rep_obter_saldo_disp_estoque ( cd_setor_p bigint, cd_estab_p bigint, cd_material_p text) FROM PUBLIC;

