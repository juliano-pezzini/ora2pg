-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_dose_rediluiente_md (ie_separar_p text, qt_solucao_str_p text, ds_dose_diluicao_p text, ds_material_p text) RETURNS varchar AS $body$
DECLARE


  ds_retorno_w varchar(2000);

BEGIN
  if (ie_separar_p = 'S') then
    ds_retorno_w := wheb_mensagem_pck.get_texto(307089);
  else
    ds_retorno_w := wheb_mensagem_pck.get_texto(307184);
  end if;

  ds_retorno_w := ds_retorno_w || ' ' || qt_solucao_str_p || ' ' ||
                  wheb_mensagem_pck.get_texto(750517) || ' ' ||
                  wheb_mensagem_pck.get_texto(307105) || ' ' ||
                  ds_dose_diluicao_p || ' ' ||
                  wheb_mensagem_pck.get_texto(307090) || ' ' ||
                  ds_material_p || chr(10);

  return ds_retorno_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_dose_rediluiente_md (ie_separar_p text, qt_solucao_str_p text, ds_dose_diluicao_p text, ds_material_p text) FROM PUBLIC;

