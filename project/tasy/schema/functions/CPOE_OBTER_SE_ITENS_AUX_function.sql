-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION cpoe_obter_se_itens_aux (nr_atendimento_p bigint, cd_pessoa_fisica_p text, cd_pessoa_usuario_p text) RETURNS varchar AS $body$
DECLARE


ie_itens_aux_w		varchar(1);
					

BEGIN
if (coalesce(nr_atendimento_p::text, '') = '') then
	select	CASE WHEN count(1)=0 THEN 'N'  ELSE 'S' END
	into STRICT	ie_itens_aux_w
	from	(
		SELECT	nr_sequencia
		from	cpoe_material
		where	cd_pessoa_fisica = cd_pessoa_fisica_p
		and		coalesce(ie_prescritor_aux,'N') = 'S'
		and		coalesce(dt_liberacao::text, '') = ''
		and 	((cd_medico = cd_pessoa_usuario_p) or (cpoe_verifica_regra_aprovacao(obter_perfil_ativo, wheb_usuario_pck.get_nm_usuario, 'AX') = 'S'))
		and		(dt_liberacao_aux IS NOT NULL AND dt_liberacao_aux::text <> '')
		and		coalesce(dt_suspensao::text, '') = ''
		and 	coalesce(ie_exige_ciencia, 'XPTO') <> 'V'
		and (cpoe_reg_valido_ativacao( dt_fim,dt_inicio, clock_timestamp()) = 'S')
		
union

		SELECT	nr_sequencia
		from	cpoe_dieta
		where	cd_pessoa_fisica = cd_pessoa_fisica_p
		and	coalesce(ie_prescritor_aux,'N') = 'S'
		and	coalesce(dt_liberacao::text, '') = '' 
		and 	((cd_medico = cd_pessoa_usuario_p) or (cpoe_verifica_regra_aprovacao(obter_perfil_ativo, wheb_usuario_pck.get_nm_usuario, 'AX') = 'S'))
		and	(dt_liberacao_aux IS NOT NULL AND dt_liberacao_aux::text <> '')
		and	coalesce(dt_suspensao::text, '') = ''
		AND (cpoe_reg_valido_ativacao( dt_fim,dt_inicio, clock_timestamp()) = 'S')
		
union

		select	nr_sequencia
		from	cpoe_procedimento
		where	cd_pessoa_fisica = cd_pessoa_fisica_p
		and	coalesce(ie_prescritor_aux,'N') = 'S'
		and	coalesce(dt_liberacao::text, '') = '' 
		and 	((cd_medico = cd_pessoa_usuario_p) or (cpoe_verifica_regra_aprovacao(obter_perfil_ativo, wheb_usuario_pck.get_nm_usuario, 'AX') = 'S'))
		and	(dt_liberacao_aux IS NOT NULL AND dt_liberacao_aux::text <> '')
		and	coalesce(dt_suspensao::text, '') = ''
		AND (cpoe_reg_valido_ativacao( dt_fim,dt_inicio, clock_timestamp()) = 'S')
		
union

		select	nr_sequencia
		from	cpoe_gasoterapia
		where	cd_pessoa_fisica = cd_pessoa_fisica_p
		and	coalesce(ie_prescritor_aux,'N') = 'S'
		and	coalesce(dt_liberacao::text, '') = '' 
		and 	((cd_medico = cd_pessoa_usuario_p) or (cpoe_verifica_regra_aprovacao(obter_perfil_ativo, wheb_usuario_pck.get_nm_usuario, 'AX') = 'S'))
		and	(dt_liberacao_aux IS NOT NULL AND dt_liberacao_aux::text <> '')
		and	coalesce(dt_suspensao::text, '') = ''
		AND (cpoe_reg_valido_ativacao( dt_fim,dt_inicio, clock_timestamp()) = 'S')
		
union

		select	nr_sequencia
		from	cpoe_recomendacao
		where	cd_pessoa_fisica = cd_pessoa_fisica_p
		and	coalesce(ie_prescritor_aux,'N') = 'S'
		and	coalesce(dt_liberacao::text, '') = '' 
		and 	((cd_medico = cd_pessoa_usuario_p) or (cpoe_verifica_regra_aprovacao(obter_perfil_ativo, wheb_usuario_pck.get_nm_usuario, 'AX') = 'S'))
		and	(dt_liberacao_aux IS NOT NULL AND dt_liberacao_aux::text <> '')
		and	coalesce(dt_suspensao::text, '') = ''
		AND (cpoe_reg_valido_ativacao( dt_fim,dt_inicio, clock_timestamp()) = 'S')
		
union

		select	nr_sequencia
		from	cpoe_hemoterapia
		where	cd_pessoa_fisica = cd_pessoa_fisica_p
		and	coalesce(ie_prescritor_aux,'N') = 'S'
		and	coalesce(dt_liberacao::text, '') = '' 
		and 	((cd_medico = cd_pessoa_usuario_p) or (cpoe_verifica_regra_aprovacao(obter_perfil_ativo, wheb_usuario_pck.get_nm_usuario, 'AX') = 'S'))
		and	(dt_liberacao_aux IS NOT NULL AND dt_liberacao_aux::text <> '')
		and	coalesce(dt_suspensao::text, '') = ''
		AND (cpoe_reg_valido_ativacao( dt_fim,dt_inicio, clock_timestamp()) = 'S')
		
union

		select	nr_sequencia
		from	cpoe_dialise
		where	cd_pessoa_fisica = cd_pessoa_fisica_p
		and	coalesce(ie_prescritor_aux,'N') = 'S'
		and	coalesce(dt_liberacao::text, '') = '' 
		and 	((cd_medico = cd_pessoa_usuario_p) or (cpoe_verifica_regra_aprovacao(obter_perfil_ativo, wheb_usuario_pck.get_nm_usuario, 'AX') = 'S'))
		and	(dt_liberacao_aux IS NOT NULL AND dt_liberacao_aux::text <> '')
		and	coalesce(dt_suspensao::text, '') = ''
		AND (cpoe_reg_valido_ativacao( dt_fim,dt_inicio, clock_timestamp()) = 'S')
		
union

		select	nr_sequencia
		from	cpoe_anatomia_patologica
		where	cd_pessoa_fisica = cd_pessoa_fisica_p
		and	coalesce(ie_prescritor_aux,'N') = 'S'
		and	coalesce(dt_liberacao::text, '') = '' 
		and 	((cd_medico = cd_pessoa_usuario_p) or (cpoe_verifica_regra_aprovacao(obter_perfil_ativo, wheb_usuario_pck.get_nm_usuario, 'AX') = 'S'))
		and	(dt_liberacao_aux IS NOT NULL AND dt_liberacao_aux::text <> '')
		and	coalesce(dt_suspensao::text, '') = ''
		AND (cpoe_reg_valido_ativacao( dt_fim,dt_inicio, clock_timestamp()) = 'S')) alias92;
else 	
	select	CASE WHEN count(1)=0 THEN 'N'  ELSE 'S' END
	into STRICT	ie_itens_aux_w
	from	(
		SELECT	nr_sequencia
		from	cpoe_material
		where	nr_atendimento = nr_atendimento_p
		and		coalesce(ie_prescritor_aux,'N') = 'S'
		and		coalesce(dt_liberacao::text, '') = '' 
		and 	((cd_medico = cd_pessoa_usuario_p) or (cpoe_verifica_regra_aprovacao(obter_perfil_ativo, wheb_usuario_pck.get_nm_usuario, 'AX') = 'S'))
		and		(dt_liberacao_aux IS NOT NULL AND dt_liberacao_aux::text <> '')
		and		coalesce(dt_suspensao::text, '') = ''
		and 	coalesce(ie_exige_ciencia, 'XPTO') <> 'V'
		and (cpoe_reg_valido_ativacao( dt_fim,dt_inicio, clock_timestamp()) = 'S')
		
union

		SELECT	nr_sequencia
		from	cpoe_dieta
		where	nr_atendimento = nr_atendimento_p
		and	coalesce(ie_prescritor_aux,'N') = 'S'
		and	coalesce(dt_liberacao::text, '') = '' 
		and 	((cd_medico = cd_pessoa_usuario_p) or (cpoe_verifica_regra_aprovacao(obter_perfil_ativo, wheb_usuario_pck.get_nm_usuario, 'AX') = 'S'))
		and	(dt_liberacao_aux IS NOT NULL AND dt_liberacao_aux::text <> '')
		and	coalesce(dt_suspensao::text, '') = ''
		AND (cpoe_reg_valido_ativacao( dt_fim,dt_inicio, clock_timestamp()) = 'S')
		
union

		select	nr_sequencia
		from	cpoe_procedimento
		where	nr_atendimento = nr_atendimento_p
		and	coalesce(ie_prescritor_aux,'N') = 'S'
		and	coalesce(dt_liberacao::text, '') = '' 
		and 	((cd_medico = cd_pessoa_usuario_p) or (cpoe_verifica_regra_aprovacao(obter_perfil_ativo, wheb_usuario_pck.get_nm_usuario, 'AX') = 'S'))
		and	(dt_liberacao_aux IS NOT NULL AND dt_liberacao_aux::text <> '')
		and	coalesce(dt_suspensao::text, '') = ''
		AND (cpoe_reg_valido_ativacao( dt_fim,dt_inicio, clock_timestamp()) = 'S')
		
union

		select	nr_sequencia
		from	cpoe_gasoterapia
		where	nr_atendimento = nr_atendimento_p
		and	coalesce(ie_prescritor_aux,'N') = 'S'
		and	coalesce(dt_liberacao::text, '') = '' 
		and 	((cd_medico = cd_pessoa_usuario_p) or (cpoe_verifica_regra_aprovacao(obter_perfil_ativo, wheb_usuario_pck.get_nm_usuario, 'AX') = 'S'))
		and	(dt_liberacao_aux IS NOT NULL AND dt_liberacao_aux::text <> '')
		and	coalesce(dt_suspensao::text, '') = ''
		AND (cpoe_reg_valido_ativacao( dt_fim,dt_inicio, clock_timestamp()) = 'S')
		
union

		select	nr_sequencia
		from	cpoe_recomendacao
		where	nr_atendimento = nr_atendimento_p
		and	coalesce(ie_prescritor_aux,'N') = 'S'
		and	coalesce(dt_liberacao::text, '') = '' 
		and 	((cd_medico = cd_pessoa_usuario_p) or (cpoe_verifica_regra_aprovacao(obter_perfil_ativo, wheb_usuario_pck.get_nm_usuario, 'AX') = 'S'))
		and	(dt_liberacao_aux IS NOT NULL AND dt_liberacao_aux::text <> '')
		and	coalesce(dt_suspensao::text, '') = ''
		AND (cpoe_reg_valido_ativacao( dt_fim,dt_inicio, clock_timestamp()) = 'S')
		
union

		select	nr_sequencia
		from	cpoe_hemoterapia
		where	nr_atendimento = nr_atendimento_p
		and	coalesce(ie_prescritor_aux,'N') = 'S'
		and	coalesce(dt_liberacao::text, '') = '' 
		and 	((cd_medico = cd_pessoa_usuario_p) or (cpoe_verifica_regra_aprovacao(obter_perfil_ativo, wheb_usuario_pck.get_nm_usuario, 'AX') = 'S'))
		and	(dt_liberacao_aux IS NOT NULL AND dt_liberacao_aux::text <> '')
		and	coalesce(dt_suspensao::text, '') = ''
		AND (cpoe_reg_valido_ativacao( dt_fim,dt_inicio, clock_timestamp()) = 'S')
		
union

		select	nr_sequencia
		from	cpoe_dialise
		where	nr_atendimento = nr_atendimento_p
		and	coalesce(ie_prescritor_aux,'N') = 'S'
		and	coalesce(dt_liberacao::text, '') = '' 
		and 	((cd_medico = cd_pessoa_usuario_p) or (cpoe_verifica_regra_aprovacao(obter_perfil_ativo, wheb_usuario_pck.get_nm_usuario, 'AX') = 'S'))
		and	(dt_liberacao_aux IS NOT NULL AND dt_liberacao_aux::text <> '')
		and	coalesce(dt_suspensao::text, '') = ''
		AND (cpoe_reg_valido_ativacao( dt_fim,dt_inicio, clock_timestamp()) = 'S')
		
union

		select	nr_sequencia
		from	cpoe_anatomia_patologica
		where	nr_atendimento = nr_atendimento_p
		and	coalesce(ie_prescritor_aux,'N') = 'S'
		and	coalesce(dt_liberacao::text, '') = '' 
		and 	((cd_medico = cd_pessoa_usuario_p) or (cpoe_verifica_regra_aprovacao(obter_perfil_ativo, wheb_usuario_pck.get_nm_usuario, 'AX') = 'S'))
		and	(dt_liberacao_aux IS NOT NULL AND dt_liberacao_aux::text <> '')
		and	coalesce(dt_suspensao::text, '') = ''
		AND (cpoe_reg_valido_ativacao( dt_fim,dt_inicio, clock_timestamp()) = 'S')) alias90;
end if;
return	ie_itens_aux_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION cpoe_obter_se_itens_aux (nr_atendimento_p bigint, cd_pessoa_fisica_p text, cd_pessoa_usuario_p text) FROM PUBLIC;

