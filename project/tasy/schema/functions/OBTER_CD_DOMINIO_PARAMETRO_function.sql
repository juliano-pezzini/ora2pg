-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_cd_dominio_parametro (CD_FUNCAO_P bigint, CD_PARAMETRO_P bigint) RETURNS bigint AS $body$
DECLARE


cd_dominio_w	FUNCAO_PARAMETRO.CD_DOMINIO%TYPE;


BEGIN

IF (CD_FUNCAO_P IS NOT NULL AND CD_FUNCAO_P::text <> '' AND CD_PARAMETRO_P IS NOT NULL AND CD_PARAMETRO_P::text <> '') THEN
	BEGIN

	 select coalesce(max(CD_DOMINIO),0)
	 into STRICT	cd_dominio_w
	 from   FUNCAO_PARAMETRO
	 where  CD_FUNCAO = CD_FUNCAO_P
	 and    NR_SEQUENCIA = CD_PARAMETRO_P;

	END;
END IF;

RETURN	cd_dominio_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_cd_dominio_parametro (CD_FUNCAO_P bigint, CD_PARAMETRO_P bigint) FROM PUBLIC;

