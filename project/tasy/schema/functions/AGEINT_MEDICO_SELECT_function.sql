-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION ageint_medico_select (nr_seq_ageint_item_p bigint, cd_medico_p text) RETURNS varchar AS $body$
DECLARE


  ds_retorno_w					varchar(255) := '';
  cd_medico_w					agenda_integrada_item.cd_medico%type;
  ie_rodizio_especialidade_w	agenda_integrada_item.ie_rodizio_especialidade%type;


BEGIN

	SELECT coalesce(MAX(ie_rodizio_especialidade), 'N')
	INTO STRICT ie_rodizio_especialidade_w
	FROM agenda_integrada_item
	WHERE nr_sequencia = nr_seq_ageint_item_p;

	SELECT MAX(cd_medico)
	INTO STRICT cd_medico_w
	FROM agenda_integrada_item
	WHERE nr_sequencia = nr_seq_ageint_item_p;
	
	if (ie_rodizio_especialidade_w = 'S') and (cd_medico_w = cd_medico_p) then
		ds_retorno_w := '(' || obter_expressao_dic_objeto(1071024) || ') ';
	end if;

	return ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION ageint_medico_select (nr_seq_ageint_item_p bigint, cd_medico_p text) FROM PUBLIC;

