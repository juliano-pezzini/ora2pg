-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_ds_tipo_procedimento (vl_dominio_p bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(100);


BEGIN
	if (vl_dominio_p IS NOT NULL AND vl_dominio_p::text <> '') then
		SELECT	ds_valor_dominio
		into STRICT	ds_retorno_w
		FROM  	valor_dominio
		WHERE 	cd_dominio = 95
		AND     vl_dominio = vl_dominio_p;
	end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_ds_tipo_procedimento (vl_dominio_p bigint) FROM PUBLIC;

