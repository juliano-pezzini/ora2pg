-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION nicu_obter_qt_clinical_rule (ID_TABELA_P bigint) RETURNS bigint AS $body$
DECLARE

QT_REGISTROS_W	double precision;


BEGIN
	QT_REGISTROS_W	:= 0;
	IF (ID_TABELA_P = 1) THEN
		SELECT	count(*)
		INTO STRICT	QT_REGISTROS_W
		FROM	NICU_CLINICAL_RULE;
	ELSIF (ID_TABELA_P = 2) THEN
		SELECT	count(*)
		INTO STRICT	QT_REGISTROS_W
		FROM	NICU_CLINICAL_RULE_COLOR;
	ELSE
		SELECT	count(*)
		INTO STRICT	QT_REGISTROS_W
		FROM	NICU_CLINICAL_RULE_AGING;
END IF;

RETURN QT_REGISTROS_W;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION nicu_obter_qt_clinical_rule (ID_TABELA_P bigint) FROM PUBLIC;

