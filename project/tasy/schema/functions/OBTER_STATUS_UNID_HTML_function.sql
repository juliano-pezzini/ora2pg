-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_status_unid_html (ds_status_unidade_p text) RETURNS varchar AS $body$
DECLARE

				
ds_retorno_w	varchar(255) := '';				


BEGIN

IF (ds_status_unidade_p IS NOT NULL AND ds_status_unidade_p::text <> '') THEN
 CASE ds_status_unidade_p
   WHEN 'A' THEN
    ds_retorno_w := OBTER_DESC_EXPRESSAO(607201,NULL); -- Em processo de alta
   WHEN 'C' THEN
    ds_retorno_w := OBTER_DESC_EXPRESSAO(494926,NULL); -- Chamado manutencao
   WHEN 'E' THEN
    ds_retorno_w := OBTER_DESC_EXPRESSAO(740836,NULL); -- Manutencao
   WHEN 'G' THEN
    ds_retorno_w := OBTER_DESC_EXPRESSAO(307974,NULL); -- Aguardando higienizacao
   WHEN 'H' THEN
    ds_retorno_w := OBTER_DESC_EXPRESSAO(489110,NULL);  -- Em higienizacao
   WHEN 'I' THEN
    ds_retorno_w := OBTER_DESC_EXPRESSAO(307834,NULL); -- Interditado
   WHEN 'L' THEN
    ds_retorno_w := OBTER_DESC_EXPRESSAO(292634,NULL); -- Livre
   WHEN 'M' THEN
    ds_retorno_w := OBTER_DESC_EXPRESSAO(283172,NULL); -- Acompanhante
   WHEN 'O' THEN
    ds_retorno_w := OBTER_DESC_EXPRESSAO(292257,NULL); -- Isolamento
   WHEN 'P' THEN
    ds_retorno_w := OBTER_DESC_EXPRESSAO(295156,NULL); -- Paciente
   WHEN 'R' THEN
    ds_retorno_w := OBTER_DESC_EXPRESSAO(331547,NULL); -- Reservado
   WHEN 'S' THEN
    ds_retorno_w := OBTER_DESC_EXPRESSAO(491871,NULL); -- Saida de interdicao
   WHEN 'D' THEN
    ds_retorno_w := OBTER_DESC_EXPRESSAO(497438,NULL); -- Servico em pausa
   WHEN 'U' THEN
    ds_retorno_w := OBTER_DESC_EXPRESSAO(717806,NULL); -- Interditado Radiacao
  END CASE;
END IF;

RETURN ds_retorno_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_status_unid_html (ds_status_unidade_p text) FROM PUBLIC;

