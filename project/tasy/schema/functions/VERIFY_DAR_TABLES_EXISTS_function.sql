-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION verify_dar_tables_exists ( TABLE_NAME_P text ) RETURNS varchar AS $body$
DECLARE

TABLE_EXISTS_W varchar(1);

BEGIN

SELECT
	CASE
		WHEN COUNT(1) > 0 THEN
			'S'
		ELSE
			'N'
	END qtd
INTO STRICT TABLE_EXISTS_W
FROM
	DAR_TABLES_FK
WHERE DS_TABLE = TABLE_NAME_P;

  RETURN TABLE_EXISTS_W;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION verify_dar_tables_exists ( TABLE_NAME_P text ) FROM PUBLIC;

