-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_paciente_autorizacao (nr_sequencia_p bigint, nr_atendimento_p bigint, cd_pessoa_fisica_autor_p text, cd_pessoa_fisica_p text, nr_seq_agenda_p bigint, nr_seq_age_integ_p bigint, nr_seq_gestao_p bigint, nr_seq_agenda_consulta_p bigint, nr_seq_paciente_setor_p bigint) RETURNS varchar AS $body$
DECLARE

				 
ds_retorno_w			varchar(1) := 'N';


BEGIN 
 
if (nr_sequencia_p IS NOT NULL AND nr_sequencia_p::text <> '') and (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') then 
	begin 
	 
	if (coalesce(cd_pessoa_fisica_autor_p,'X') <> 'X') and (coalesce(cd_pessoa_fisica_p,'X') <> 'X') and (cd_pessoa_fisica_autor_p = cd_pessoa_fisica_p) then 
		ds_retorno_w := 'S';
	end if;
	 
	if (ds_retorno_w = 'N') then 
		begin 
		begin 
		select	'S' 
		into STRICT	ds_retorno_w 
		from	atendimento_paciente 
		where	nr_atendimento = nr_atendimento_p 
		and	cd_pessoa_fisica = cd_pessoa_fisica_p  LIMIT 1;
		exception 
		when others then 
			ds_retorno_w := 'N';
		end;		
		 
		if (ds_retorno_w = 'N') then 
			begin 
			begin 
			select	'S' 
			into STRICT	ds_retorno_w 
			from	agenda_paciente 
			where	nr_sequencia = nr_seq_agenda_p 
			and	cd_pessoa_fisica = cd_pessoa_fisica_p  LIMIT 1;
			exception 
			when others then 
				ds_retorno_w := 'N';
			end;
			 
			if (ds_retorno_w = 'N') then 
				begin 
				begin 
				select	'S' 
				into STRICT	ds_retorno_w 
				from	agenda_integrada 
				where	nr_sequencia = nr_seq_age_integ_p 
				and	cd_pessoa_fisica = cd_pessoa_fisica_p  LIMIT 1;
				exception 
				when others then 
					ds_retorno_w := 'N';
				end;
				 
				if (ds_retorno_w = 'N') then 
					begin 
					begin 
					select	'S' 
					into STRICT	ds_retorno_w 
					from	gestao_vaga 
					where	nr_sequencia = nr_seq_gestao_p 
					and	cd_pessoa_fisica = cd_pessoa_fisica_p  LIMIT 1;
					exception 
					when others then 
						ds_retorno_w := 'N';
					end;
					 
					if (ds_retorno_w = 'N') then 
						begin 
						begin 
						select	'S' 
						into STRICT	ds_retorno_w 
						from	agenda_consulta 
						where	nr_sequencia = nr_seq_agenda_consulta_p 
						and	cd_pessoa_fisica = cd_pessoa_fisica_p  LIMIT 1;
						exception 
						when others then 
							ds_retorno_w := 'N';
						end;
						 
						if (ds_retorno_w = 'N') then 
							begin 
							select	'S' 
							into STRICT	ds_retorno_w 
							from	paciente_setor 
							where	nr_seq_paciente = nr_seq_paciente_setor_p 
							and	cd_pessoa_fisica = cd_pessoa_fisica_p  LIMIT 1;
							exception 
							when others then 
								ds_retorno_w := 'N';
							end;
						end if;
						end;
					end if;
					end;
				end if;
				end;
			end if;
			end;
		end if;
		end;
	end if;
	end;
end if;
 
return	ds_retorno_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_paciente_autorizacao (nr_sequencia_p bigint, nr_atendimento_p bigint, cd_pessoa_fisica_autor_p text, cd_pessoa_fisica_p text, nr_seq_agenda_p bigint, nr_seq_age_integ_p bigint, nr_seq_gestao_p bigint, nr_seq_agenda_consulta_p bigint, nr_seq_paciente_setor_p bigint) FROM PUBLIC;

