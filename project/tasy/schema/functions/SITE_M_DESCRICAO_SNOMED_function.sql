-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION site_m_descricao_snomed (nr_sequencia_p bigint) RETURNS varchar AS $body$
DECLARE

ds_retorno_w		varchar(2000);


BEGIN

    SELECT c.ds_term
      INTO STRICT ds_retorno_w
      FROM sm_location e,
           res_cadastro_ontologia_cli b,
           snomed_descricao c
     WHERE e.nr_sequencia= b.cd_valor_tasy
       AND c.DS_ID  = b.CD_VALOR_ONTOLOGIA
       AND b.nm_tabela='SM_LOCATION'
       AND c.ds_effectivetime = (SELECT MAX(d.ds_effectivetime) 
                                   FROM snomed_descricao d 
                                  WHERE d.ds_id = c.ds_id)
       AND b.cd_valor_tasy = nr_sequencia_p;

    RETURN ds_retorno_w;
End;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION site_m_descricao_snomed (nr_sequencia_p bigint) FROM PUBLIC;

