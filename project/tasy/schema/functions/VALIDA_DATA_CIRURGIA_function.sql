-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION valida_data_cirurgia ( dt_analise_p timestamp, dt_entrada_p timestamp, dt_alta_p timestamp) RETURNS varchar AS $body$
DECLARE


ds_retorno_w varchar(255) := '';


BEGIN

IF (dt_analise_p < dt_entrada_p) THEN
 ds_retorno_w := Wheb_mensagem_pck.get_texto(305916); -- A data digitada deve ser maior que a data de entrada do paciente!
ELSIF (dt_analise_p > dt_alta_p AND dt_alta_p IS NOT NULL AND dt_alta_p::text <> '') THEN
 ds_retorno_w := Wheb_mensagem_pck.get_texto(305918); -- A data digitada deve ser menor que a data de saída do paciente!
ELSIF (dt_analise_p > clock_timestamp()) THEN
 ds_retorno_w := Wheb_mensagem_pck.get_texto(305919); -- A data digitada deve ser menor que a data atual!
END IF;

RETURN ds_retorno_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION valida_data_cirurgia ( dt_analise_p timestamp, dt_entrada_p timestamp, dt_alta_p timestamp) FROM PUBLIC;

