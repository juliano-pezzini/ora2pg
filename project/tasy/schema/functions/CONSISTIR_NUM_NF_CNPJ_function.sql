-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION consistir_num_nf_cnpj ( cd_cgc_p text, nr_nota_fiscal_p text, cd_estabelecimento_p bigint) RETURNS varchar AS $body$
DECLARE


ds_consiste_w		varchar(1);
nr_nota_fiscal_w	numeric(30);
nr_nf_atual_w		numeric(30);


BEGIN

nr_nf_atual_w := (nr_nota_fiscal_p)::numeric;

select	max((nr_nota_fiscal)::numeric )
into STRICT	nr_nota_fiscal_w
from	nota_fiscal
where	cd_cgc_emitente = cd_cgc_p
and	cd_estabelecimento = cd_estabelecimento_p
and	obter_dados_operacao_nota(cd_operacao_nf,9) = 'S'
and	ie_situacao = '1';

if (nr_nota_fiscal_w > nr_nf_atual_w) then
	ds_consiste_w := 'S';
else
	ds_consiste_w := 'N';
end if;

return	ds_consiste_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION consistir_num_nf_cnpj ( cd_cgc_p text, nr_nota_fiscal_p text, cd_estabelecimento_p bigint) FROM PUBLIC;

