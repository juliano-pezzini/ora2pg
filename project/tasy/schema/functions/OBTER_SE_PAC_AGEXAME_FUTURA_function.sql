-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_pac_agexame_futura (nr_seq_agenda_p bigint) RETURNS varchar AS $body$
DECLARE

 
ie_futuras_w		varchar(1) := 'N';
dt_agenda_w		timestamp;
cd_pessoa_fisica_w	varchar(10);
cd_tipo_agenda_w	bigint;
qt_agenda_w		bigint;


BEGIN 
if (nr_seq_agenda_p IS NOT NULL AND nr_seq_agenda_p::text <> '') then 
	select	b.hr_inicio, 
		b.cd_pessoa_fisica, 
		a.cd_tipo_agenda 
	into STRICT	dt_agenda_w, 
		cd_pessoa_fisica_w, 
		cd_tipo_agenda_w 
	from	agenda a, 
		agenda_paciente b 
	where	a.cd_agenda		= b.cd_agenda 
	and	b.nr_sequencia 	= nr_seq_agenda_p;
 
	select	coalesce(count(*),0) 
	into STRICT	qt_agenda_w 
	from	agenda a, 
		agenda_paciente b 
	where	a.cd_agenda 		= b.cd_agenda 
	and	a.cd_tipo_agenda	= cd_tipo_agenda_w 
	and	b.hr_inicio		> dt_agenda_w 
	and	b.cd_pessoa_fisica	= cd_pessoa_fisica_w 
	and	b.ie_status_agenda	<> 'C';
 
	if (coalesce(qt_agenda_w,0) > 0) then 
		ie_futuras_w	:= 'S';
	end if;
end if;
 
return	ie_futuras_w;		
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_pac_agexame_futura (nr_seq_agenda_p bigint) FROM PUBLIC;

