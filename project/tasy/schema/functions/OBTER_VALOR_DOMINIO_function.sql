-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_valor_dominio ( cd_dominio_p bigint, vl_dominio_p text) RETURNS varchar AS $body$
DECLARE

  ds_valor_dominio_w varchar(255) := null;

BEGIN

if (cd_dominio_p IS NOT NULL AND cd_dominio_p::text <> '') and (vl_dominio_p IS NOT NULL AND vl_dominio_p::text <> '') then
	ds_valor_dominio_w	:= dominio_pck.Obter_Valor_Dominio(cd_dominio_p,vl_dominio_p);
end if;

return ds_valor_dominio_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_valor_dominio ( cd_dominio_p bigint, vl_dominio_p text) FROM PUBLIC;

