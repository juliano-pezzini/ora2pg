-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_rodape_auditor ( nr_seq_conta_p pls_conta.nr_sequencia%type) RETURNS varchar AS $body$
DECLARE


ds_retorno_w			varchar(200);
qt_auditor_w			integer;

nr_crm_auditor_w		medico.nr_crm%type;
nm_medico_auditor_w		pessoa_fisica.nm_pessoa_fisica%type;
cd_uf_crm_w			medico.uf_crm%type;
nr_coren_auditor_w		medico.nr_crm%type;
nm_enfer_auditor_w		pessoa_fisica.nm_pessoa_fisica%type;
cd_uf_coren_w			medico.uf_crm%type;


BEGIN

select	count(1)
into STRICT	qt_auditor_w
from	pls_conta_auditor
where	nr_seq_conta = nr_seq_conta_p;

if (qt_auditor_w > 0) then

	SELECT * FROM ptu_obter_dados_aud_analise(	nr_seq_conta_p, nr_crm_auditor_w, nm_medico_auditor_w, cd_uf_crm_w, nr_coren_auditor_w, nm_enfer_auditor_w, cd_uf_coren_w) INTO STRICT nr_crm_auditor_w, nm_medico_auditor_w, cd_uf_crm_w, nr_coren_auditor_w, nm_enfer_auditor_w, cd_uf_coren_w;

	if (nr_crm_auditor_w IS NOT NULL AND nr_crm_auditor_w::text <> '') then

		ds_retorno_w := 'Méd.: ' || nm_medico_auditor_w || ' / CRM: ' ||  nr_crm_auditor_w;

	elsif (nr_coren_auditor_w IS NOT NULL AND nr_coren_auditor_w::text <> '') then

		ds_retorno_w := 'Enf.: ' || nm_enfer_auditor_w || ' / COREN: ' ||  nr_coren_auditor_w;
	end if;
end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_rodape_auditor ( nr_seq_conta_p pls_conta.nr_sequencia%type) FROM PUBLIC;

