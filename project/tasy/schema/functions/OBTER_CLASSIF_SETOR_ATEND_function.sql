-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_classif_setor_atend ( nr_atendimento_p bigint) RETURNS bigint AS $body$
DECLARE



cd_setor_atendimento_w	integer;
cd_classif_setor_w	varchar(2);


BEGIN

select	max(cd_setor_atendimento)
into STRICT	cd_setor_atendimento_w
from	atend_paciente_unidade
where	nr_seq_interno = (
	SELECT Obter_Atepacu_paciente(nr_atendimento_p, 'A')
	);

if (cd_setor_atendimento_w IS NOT NULL AND cd_setor_atendimento_w::text <> '') then
	select  max(cd_classif_setor)
	into STRICT	cd_classif_setor_w
	from	setor_atendimento
	where 	cd_setor_atendimento = cd_setor_atendimento_w;
end if;

RETURN cd_classif_setor_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_classif_setor_atend ( nr_atendimento_p bigint) FROM PUBLIC;

