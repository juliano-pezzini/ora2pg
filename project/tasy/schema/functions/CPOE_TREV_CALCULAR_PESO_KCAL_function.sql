-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION cpoe_trev_calcular_peso_kcal ( nr_atendimento_p atendimento_paciente.nr_atendimento%type, cd_pessoa_fisica_p atendimento_paciente.cd_pessoa_fisica%type, qt_peso_p bigint ) RETURNS bigint AS $body$
DECLARE


	cd_pessoa_fisica_w 	atendimento_paciente.cd_pessoa_fisica%type;
	qt_idade_paciente_w	integer;
	qt_peso_kcal_w		cpoe_trev.qt_peso_calorico%type;


BEGIN
	qt_idade_paciente_w := -1;
	qt_peso_kcal_w := 0;

	if (nr_atendimento_p IS NOT NULL AND nr_atendimento_p::text <> '') then
		cd_pessoa_fisica_w := obter_pessoa_atendimento(nr_atendimento_p, 'C');
	elsif (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') then
		cd_pessoa_fisica_w := cd_pessoa_fisica_p;
	end if;

	if (cd_pessoa_fisica_w IS NOT NULL AND cd_pessoa_fisica_w::text <> '') then
		qt_idade_paciente_w	:= obter_dados_pf(cd_pessoa_fisica_w, 'I');
	end if;

    if ((qt_idade_paciente_w >= 0) and (qt_idade_paciente_w <= 21) and (coalesce(qt_peso_p, 0) > 0) and (coalesce(qt_peso_p, 0) <= 80)) then
      if (qt_peso_p <= 10) then
        qt_peso_kcal_w := ((qt_peso_p * 100) / 100);
      elsif (qt_peso_p > 10 AND qt_peso_p <= 20) then
        qt_peso_kcal_w := ((1000 + ((qt_peso_p - 10) * 50)) / 100);
      elsif (qt_peso_p > 20) then
        qt_peso_kcal_w := ((1500 + ((qt_peso_p - 20) * 20)) / 100);
      end if;
	end if;

	return qt_peso_kcal_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION cpoe_trev_calcular_peso_kcal ( nr_atendimento_p atendimento_paciente.nr_atendimento%type, cd_pessoa_fisica_p atendimento_paciente.cd_pessoa_fisica%type, qt_peso_p bigint ) FROM PUBLIC;

