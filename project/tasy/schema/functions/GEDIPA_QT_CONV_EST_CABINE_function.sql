-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION gedipa_qt_conv_est_cabine ( cd_material_p bigint, nr_seq_gedipa_cabine_p bigint, cd_unidade_medida_dest_p text) RETURNS bigint AS $body$
DECLARE


qt_dose_disp_w              	double precision;
qt_estoque_w			double precision;
cd_unidade_medida_consumo_w 	varchar(30);


BEGIN

SELECT 	max(a.qt_estoque),
	max(b.cd_unidade_medida_consumo)
into STRICT 	qt_estoque_w,
	cd_unidade_medida_consumo_w
FROM 	gedipa_estoque_cabine a, material b
WHERE   a.cd_material = b.cd_material
AND 	a.nr_sequencia = nr_seq_gedipa_cabine_p;

qt_dose_disp_w := Obter_dose_convertida_quimio(cd_material_p, qt_estoque_w, cd_unidade_medida_consumo_w, cd_unidade_medida_dest_p);

return qt_dose_disp_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION gedipa_qt_conv_est_cabine ( cd_material_p bigint, nr_seq_gedipa_cabine_p bigint, cd_unidade_medida_dest_p text) FROM PUBLIC;

