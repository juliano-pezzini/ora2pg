-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION nut_obter_seq_prescr_nut (nr_atendimento_p bigint, cd_setor_atendimento_p bigint, dt_referencia_p timestamp) RETURNS bigint AS $body$
DECLARE


nr_sequencia_w		bigint;
dt_inicial_w		timestamp;
dt_final_w		timestamp;


BEGIN

dt_inicial_w		:= trunc(dt_referencia_p, 'dd');
dt_final_w		:= trunc(dt_referencia_p, 'dd') + 86399/86400;


select	coalesce(max(nr_sequencia), 0)
into STRICT	nr_sequencia_w
from	nut_prescricao a
where	a.nr_atendimento	= nr_atendimento_p
and	a.cd_setor_atendimento	= cd_setor_atendimento_p
and	a.dt_prescricao between dt_inicial_w and dt_final_w;

return	nr_sequencia_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION nut_obter_seq_prescr_nut (nr_atendimento_p bigint, cd_setor_atendimento_p bigint, dt_referencia_p timestamp) FROM PUBLIC;

