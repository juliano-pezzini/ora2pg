-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_conta_financ_titulo ( nr_titulo_p bigint, ie_tipo_titulo_p text) RETURNS varchar AS $body$
DECLARE


/* ie_tipo_titulo_p

R	Receber
P	Pagar

*/
ds_retorno_w		varchar(2000);
ds_conta_financ_w	varchar(60);

c01 CURSOR FOR
SELECT	b.ds_conta_financ
from	conta_financeira b,
	titulo_receber_classif a
where	a.cd_conta_financ	= b.cd_conta_financ
and	a.nr_titulo		= nr_titulo_p
group by b.ds_conta_financ;


BEGIN

if (ie_tipo_titulo_p	= 'R') then

	open	c01;
	loop
	fetch	c01 into
		ds_conta_financ_w;
	EXIT WHEN NOT FOUND; /* apply on c01 */

		if (coalesce(ds_retorno_w::text, '') = '') or (length(ds_retorno_w) < 1930) then

			if (coalesce(ds_retorno_w::text, '') = '') then
				ds_retorno_w	:= ds_conta_financ_w;
			else
				ds_retorno_w	:= ds_retorno_w || ', ' || ds_conta_financ_w;
			end if;

		end if;

	end	loop;
	close	c01;

end if;

RETURN ds_retorno_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_conta_financ_titulo ( nr_titulo_p bigint, ie_tipo_titulo_p text) FROM PUBLIC;

