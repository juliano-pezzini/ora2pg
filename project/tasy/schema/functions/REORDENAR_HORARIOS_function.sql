-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION reordenar_horarios (dt_Hora_Inicio_p timestamp, ds_horarios_p text ) RETURNS varchar AS $body$
DECLARE


hr_inicio_w    integer;
hr_prime_w     integer;
ie_minuto_w    smallint := 0;
hr_menor_w     integer := -1;
ds_validos_w   varchar(20) := '1234567890: ';
X              varchar(1);
i              integer;
ie_ok_w        varchar(1) := 'S';
ds_horarios_w  varchar(2000);
ds_horarios_ww varchar(2000);
ds_erro_w      varchar(2000);
sql_w          varchar(200);

BEGIN
  BEGIN
      sql_w := 'CALL REORDENAR_HORARIOS_MD(:1,:2) INTO :RESULT';

      EXECUTE sql_w USING IN dt_Hora_Inicio_p,
                                    IN ds_horarios_p,
                                    OUT ds_horarios_w;
  EXCEPTION
      WHEN OTHERS THEN
          ds_horarios_w := NULL;
  END;

  return ds_horarios_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION reordenar_horarios (dt_Hora_Inicio_p timestamp, ds_horarios_p text ) FROM PUBLIC;

