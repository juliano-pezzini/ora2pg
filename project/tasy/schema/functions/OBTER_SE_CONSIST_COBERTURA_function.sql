-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_consist_cobertura (cd_convenio_p bigint, cd_plano_p text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(10);


BEGIN
select	coalesce(max('S'), 'N')
into STRICT	ds_retorno_w
from	regra_consist_cobertura
where	coalesce(cd_convenio, coalesce(cd_convenio_p, 0)) = coalesce(cd_convenio_p, 0)
and	coalesce(cd_plano, coalesce(cd_plano_p, 'XPTO')) 	= coalesce(cd_plano_p, 'XPTO');

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_consist_cobertura (cd_convenio_p bigint, cd_plano_p text) FROM PUBLIC;

