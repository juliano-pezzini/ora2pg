-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION tx_obter_desc_local_perf (nr_Seq_local_p bigint) RETURNS varchar AS $body$
DECLARE


ds_local_perfusao_w	varchar(90);


BEGIN

if (coalesce(nr_Seq_local_p,0) > 0) then

	select 	max(ds_local_perfusao)
	into STRICT	ds_local_perfusao_w
	from	tx_local_perfusao
	where	nr_sequencia = nr_Seq_local_p;

end if;

return ds_local_perfusao_w;


end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION tx_obter_desc_local_perf (nr_Seq_local_p bigint) FROM PUBLIC;

