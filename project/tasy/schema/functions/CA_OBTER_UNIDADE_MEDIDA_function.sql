-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION ca_obter_unidade_medida (nr_seq_exame_p bigint) RETURNS varchar AS $body$
DECLARE


cd_unidade_medida_w	varchar(30);


BEGIN

if (coalesce(nr_seq_exame_p,0) > 0) then
	select	max(cd_unidade_medida)
	into STRICT	cd_unidade_medida_w
	from	ca_exame
	where	nr_sequencia = nr_seq_exame_p;
end if;
return	cd_unidade_medida_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION ca_obter_unidade_medida (nr_seq_exame_p bigint) FROM PUBLIC;

