-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_qt_item_atendimento ( nr_atendimento_p bigint, nm_tabela_p text) RETURNS bigint AS $body$
DECLARE


ds_sql_w			varchar(2000);
retorno_w			integer;
C01					integer;
qt_item_w			bigint;


BEGIN

ds_sql_w	:=	'	select   count(*) qt_item '||
				'	from     prescr_medica a, '||nm_tabela_p||' b '||
				'	where    a.nr_prescricao   = b.nr_prescricao '||
				'	and      a.nr_atendimento  = :NR_ATENDIMENTO ';

C01 := DBMS_SQL.OPEN_CURSOR;
DBMS_SQL.PARSE(C01, ds_sql_w, dbms_sql.Native);
DBMS_SQL.DEFINE_COLUMN(C01,1,qt_item_w);
DBMS_SQL.BIND_VARIABLE(C01,'NR_ATENDIMENTO', nr_atendimento_p);
retorno_w := DBMS_SQL.execute(C01);

while( DBMS_SQL.FETCH_ROWS(C01) > 0 ) loop
	begin
	DBMS_SQL.COLUMN_VALUE(C01,1,qt_item_w);
	end;
end loop;

DBMS_SQL.CLOSE_CURSOR(C01);

return	qt_item_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_qt_item_atendimento ( nr_atendimento_p bigint, nm_tabela_p text) FROM PUBLIC;

