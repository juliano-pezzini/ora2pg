-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';



--
-- dblink wrapper to call function consistir_acesso_pep_ios as an autonomous transaction
--
CREATE EXTENSION IF NOT EXISTS dblink;

CREATE OR REPLACE FUNCTION consistir_acesso_pep_ios (cd_estabelecimento_p bigint, nr_atendimento_p bigint, cd_paciente_p text, cd_pessoa_fisica_p text, nm_usuario_p text, dt_logon_p timestamp) RETURNS varchar AS $body$
DECLARE
	-- Change this to reflect the dblink connection string
	v_conn_str  text := format('port=%s dbname=%s user=%s', current_setting('port'), current_database(), current_user);
	v_query     text;

	v_ret	varchar;
BEGIN
	v_query := 'SELECT * FROM consistir_acesso_pep_ios_atx ( ' || quote_nullable(cd_estabelecimento_p) || ',' || quote_nullable(nr_atendimento_p) || ',' || quote_nullable(cd_paciente_p) || ',' || quote_nullable(cd_pessoa_fisica_p) || ',' || quote_nullable(nm_usuario_p) || ',' || quote_nullable(dt_logon_p) || ' )';
	SELECT * INTO v_ret FROM dblink(v_conn_str, v_query) AS p (ret varchar);
	RETURN v_ret;

END;
$body$ LANGUAGE plpgsql SECURITY DEFINER;




CREATE OR REPLACE FUNCTION consistir_acesso_pep_ios_atx (cd_estabelecimento_p bigint, nr_atendimento_p bigint, cd_paciente_p text, cd_pessoa_fisica_p text, nm_usuario_p text, dt_logon_p timestamp) RETURNS varchar AS $body$
DECLARE
ie_permite_w		varchar(1);
nr_seq_mensagem_w	bigint;
ds_mensagem_w		varchar(255);
nr_seq_lib_acesso_w		bigint;
ie_lib_acesso_w			varchar(1);


BEGIN 
ds_mensagem_w := '';
 
SELECT * FROM consistir_acesso_pep(cd_estabelecimento_p, nr_atendimento_p, cd_paciente_p, cd_pessoa_fisica_p, nm_usuario_p, dt_logon_p, ie_permite_w, nr_seq_mensagem_w, nr_seq_lib_acesso_w, ie_lib_acesso_w) INTO STRICT ie_permite_w, nr_seq_mensagem_w, nr_seq_lib_acesso_w, ie_lib_acesso_w;
 
 
if (coalesce(ie_permite_w,'S') = 'N') then 
	ds_mensagem_w := Obter_mensagem_acesso(nr_seq_mensagem_w);
end if;
 
 
return	ds_mensagem_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION consistir_acesso_pep_ios (cd_estabelecimento_p bigint, nr_atendimento_p bigint, cd_paciente_p text, cd_pessoa_fisica_p text, nm_usuario_p text, dt_logon_p timestamp) FROM PUBLIC; -- REVOKE ALL ON FUNCTION consistir_acesso_pep_ios_atx (cd_estabelecimento_p bigint, nr_atendimento_p bigint, cd_paciente_p text, cd_pessoa_fisica_p text, nm_usuario_p text, dt_logon_p timestamp) FROM PUBLIC;

