-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION ptu_obter_se_trans_retorno ( nr_seq_guia_p bigint, nr_seq_requisicao_p bigint, ie_tipo_p text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w			varchar(2);
qt_registros_w			smallint;
qt_registros_ww			smallint;
ie_estagio_w			smallint;


BEGIN
ds_retorno_w	:= 'N';

if (nr_seq_guia_p IS NOT NULL AND nr_seq_guia_p::text <> '') then
	ds_retorno_w	:= 'N';
	if (ie_tipo_p	= 'PC') then
		select	count(1)
		into STRICT	qt_registros_w
		from	ptu_pedido_compl_aut
		where	nr_seq_guia	= nr_seq_guia_p;

		if (qt_registros_w	<> 0) then
			select	count(1)
			into STRICT	qt_registros_ww
			from	ptu_resposta_autorizacao
			where	ie_tipo_resposta	= ie_tipo_p
			and	nr_seq_guia		= nr_seq_guia_p;

			if (qt_registros_ww	= qt_registros_w) then
				ds_retorno_w	:= 'S';
			end if;
		end if;
	elsif (ie_tipo_p	= 'PA') then
		select	count(1)
		into STRICT	qt_registros_w
		from	ptu_pedido_autorizacao
		where	nr_seq_guia	= nr_seq_guia_p;

		if (qt_registros_w	<> 0) then
			select	count(1)
			into STRICT	qt_registros_ww
			from	ptu_resposta_autorizacao
			where	ie_tipo_resposta	= ie_tipo_p
			and	nr_seq_guia		= nr_seq_guia_p;

			if (qt_registros_ww	= qt_registros_w) then
				ds_retorno_w	:= 'S';
			end if;
		end if;
	elsif (ie_tipo_p	= 'C') then
		select	count(1)
		into STRICT	qt_registros_w
		from	ptu_cancelamento
		where	nr_seq_guia	= nr_seq_guia_p;

		if (qt_registros_w	<> 0) then
			select	count(1)
			into STRICT	qt_registros_ww
			from	ptu_confirmacao
			where	ie_tipo_resposta	= ie_tipo_p
			and	nr_seq_guia		= nr_seq_guia_p;

			if (qt_registros_ww	= qt_registros_w) then
				ds_retorno_w	:= 'S';
			end if;
		end if;
	elsif (ie_tipo_p	= 'PI') then
		select	count(1)
		into STRICT	qt_registros_w
		from	ptu_pedido_insistencia
		where	nr_seq_guia	= nr_seq_guia_p;

		if (qt_registros_w	<> 0) then
			select	count(1)
			into STRICT	qt_registros_ww
			from	ptu_confirmacao
			where	ie_tipo_resposta	= ie_tipo_p
			and	nr_seq_guia		= nr_seq_guia_p;

			if (qt_registros_ww	= qt_registros_w) then
				ds_retorno_w	:= 'S';
			end if;
		end if;
	elsif (ie_tipo_p	= 'RA') then
		select	count(1)
		into STRICT	qt_registros_w
		from	ptu_resposta_auditoria
		where	nr_seq_guia	= nr_seq_guia_p;

		if (qt_registros_w	<> 0) then
			select	count(1)
			into STRICT	qt_registros_ww
			from	ptu_confirmacao
			where	ie_tipo_resposta	= ie_tipo_p
			and	nr_seq_guia		= nr_seq_guia_p;

			if (qt_registros_ww	= qt_registros_w) then
				ds_retorno_w	:= 'S';
			end if;
		end if;
	end if;
elsif (nr_seq_requisicao_p IS NOT NULL AND nr_seq_requisicao_p::text <> '') then
	if (ie_tipo_p	= 'PC') then
		select	count(1)
		into STRICT	qt_registros_w
		from	ptu_pedido_compl_aut
		where	nr_seq_requisicao	= nr_seq_requisicao_p;

		if (qt_registros_w	<> 0) then
			select	count(1)
			into STRICT	qt_registros_ww
			from	ptu_resposta_autorizacao
			where	ie_tipo_resposta	= ie_tipo_p
			and	nr_seq_requisicao	= nr_seq_requisicao_p;

			if (qt_registros_ww	= qt_registros_w) then
				ds_retorno_w	:= 'S';
			end if;
		end if;
	elsif (ie_tipo_p	= 'PA') then
		select	max(ie_estagio)
		into STRICT	ie_estagio_w
		from	pls_requisicao
		where	nr_sequencia = nr_seq_requisicao_p;

		if ((ie_estagio_w IS NOT NULL AND ie_estagio_w::text <> '') and ie_estagio_w <> 10) then
			select	count(1)
			into STRICT	qt_registros_w
			from	ptu_pedido_autorizacao
			where	nr_seq_requisicao	= nr_seq_requisicao_p;

			if (qt_registros_w	<> 0) then
				select	count(1)
				into STRICT	qt_registros_ww
				from	ptu_resposta_autorizacao
				where	ie_tipo_resposta	= ie_tipo_p
				and	nr_seq_requisicao	= nr_seq_requisicao_p;

				if (qt_registros_ww	= qt_registros_w) then
					ds_retorno_w	:= 'S';
				end if;
			end if;
		end if;
	elsif (ie_tipo_p	= 'C') then
		select	count(1)
		into STRICT	qt_registros_w
		from	ptu_cancelamento
		where	nr_seq_requisicao	= nr_seq_requisicao_p;

		if (qt_registros_w	<> 0) then
			select	count(1)
			into STRICT	qt_registros_ww
			from	ptu_confirmacao
			where	ie_tipo_resposta	= ie_tipo_p
			and	nr_seq_requisicao	= nr_seq_requisicao_p;

			if (qt_registros_ww	= qt_registros_w) then
				ds_retorno_w	:= 'S';
			end if;
		end if;
	elsif (ie_tipo_p	= 'PI') then
		select	count(1)
		into STRICT	qt_registros_w
		from	ptu_pedido_insistencia
		where	nr_seq_requisicao	= nr_seq_requisicao_p;

		if (qt_registros_w	<> 0) then
			select	count(1)
			into STRICT	qt_registros_ww
			from	ptu_confirmacao
			where	ie_tipo_resposta	= ie_tipo_p
			and	nr_seq_requisicao	= nr_seq_requisicao_p;

			if (qt_registros_ww	= qt_registros_w) then
				ds_retorno_w	:= 'S';
			end if;
		end if;
	elsif (ie_tipo_p	= 'RA') then
		select	count(1)
		into STRICT	qt_registros_w
		from	ptu_resposta_auditoria
		where	nr_seq_requisicao	= nr_seq_requisicao_p;

		if (qt_registros_w	<> 0) then
			select	count(1)
			into STRICT	qt_registros_ww
			from	ptu_confirmacao
			where	ie_tipo_resposta	= ie_tipo_p
			and	nr_seq_requisicao	= nr_seq_requisicao_p;

			if (qt_registros_ww	= qt_registros_w) then
				ds_retorno_w	:= 'S';
			end if;
		end if;
	end if;
end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION ptu_obter_se_trans_retorno ( nr_seq_guia_p bigint, nr_seq_requisicao_p bigint, ie_tipo_p text) FROM PUBLIC;

