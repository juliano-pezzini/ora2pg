-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION proj_obter_tot_viagem_geral (nr_seq_viagem_p bigint, dt_inicial_p timestamp, dt_final_p timestamp, ie_opcao_p text) RETURNS bigint AS $body$
DECLARE


vl_retorno_w			double precision;


BEGIN
if (ie_opcao_p	=	'G') then --Total geral
	select	sum(y.vl_total) vl_total
	into STRICT	vl_retorno_w
	from (	SELECT	coalesce(sum(b.vl_despesa), 0) vl_total
			from	via_despesa b,
				via_relat_desp a
			where	a.nr_sequencia	= b.nr_seq_relat
			and	a.nr_seq_viagem	= nr_seq_viagem_p
			and	b.dt_despesa	between dt_inicial_p and fim_dia(dt_final_p)
			having	coalesce(sum(b.vl_despesa), 0) > 0
			
union all

			SELECT	coalesce(sum(obter_valor_total_hotel(nr_sequencia)),0) vl_total_hotel
			from	via_reserva a
			where	a.nr_seq_viagem	= nr_seq_viagem_p
			having	coalesce(sum(obter_valor_total_hotel(nr_sequencia)),0) > 0
		) y
	where	1 = 1
	having	count(*) > 0;
end if;
if (ie_opcao_p	=	'N') then -- Nossa empresa
	select	sum(y.vl_total) vl_total
	into STRICT	vl_retorno_w
	from (	SELECT	coalesce(sum(b.vl_despesa), 0) vl_total
			from	via_despesa b,
				via_relat_desp a
			where	a.nr_sequencia	= b.nr_seq_relat
			and	a.nr_seq_viagem	= nr_seq_viagem_p
			and	b.ie_responsavel_custo = 'N'
			and	b.dt_despesa	between dt_inicial_p and fim_dia(dt_final_p)
			having	coalesce(sum(b.vl_despesa), 0) > 0
			
union all

			SELECT	coalesce(sum(obter_valor_total_hotel(nr_sequencia)),0) vl_total_hotel
			from	via_reserva a
			where	a.nr_seq_viagem	= nr_seq_viagem_p
			and	a.ie_responsavel_custo  = 'N'
			having	coalesce(sum(obter_valor_total_hotel(nr_sequencia)),0) > 0
		) y
	where	1 = 1
	having	count(*) > 0;
end if;
if (ie_opcao_p	=	'T') then -- Terceiro
	select	sum(y.vl_total) vl_total
	into STRICT	vl_retorno_w
	from (	SELECT	coalesce(sum(b.vl_despesa), 0) vl_total
			from	via_despesa b,
				via_relat_desp a
			where	a.nr_sequencia	= b.nr_seq_relat
			and	a.nr_seq_viagem	= nr_seq_viagem_p
			and	b.ie_responsavel_custo = 'T'
			and	b.dt_despesa	between dt_inicial_p and fim_dia(dt_final_p)
			having	coalesce(sum(b.vl_despesa), 0) > 0
			
union all

			SELECT	coalesce(sum(obter_valor_total_hotel(nr_sequencia)),0) vl_total_hotel
			from	via_reserva a
			where	a.nr_seq_viagem	= nr_seq_viagem_p
			and	a.ie_responsavel_custo  = 'T'
			having	coalesce(sum(obter_valor_total_hotel(nr_sequencia)),0) > 0
		) y
	where	1 = 1
	having	count(*) > 0;
end if;


return	vl_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION proj_obter_tot_viagem_geral (nr_seq_viagem_p bigint, dt_inicial_p timestamp, dt_final_p timestamp, ie_opcao_p text) FROM PUBLIC;

