-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_proced_hemoc ( nr_seq_derivado_p bigint, cd_procedimento_p bigint, ie_origem_proced_p bigint, nr_seq_proc_interno_p bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(4000);


BEGIN

ds_retorno_w := 'N';

SELECT	coalesce(MAX('S'), 'N')
INTO STRICT	ds_retorno_w
FROM	san_derivado
WHERE	nr_sequencia	= nr_seq_derivado_p
AND	(((cd_procedimento = coalesce(cd_procedimento_p,0)) AND (ie_origem_proced = coalesce(ie_origem_proced_p,0)))
OR (nr_seq_proc_interno = nr_seq_proc_interno_p));

IF (ds_retorno_w = 'N') THEN

	SELECT	coalesce(MAX('S'), 'N')
	INTO STRICT	ds_retorno_w
	FROM	san_derivado_convenio
	WHERE	nr_seq_derivado	= nr_seq_derivado_p
	AND	(((cd_procedimento = coalesce(cd_procedimento_p,0)) AND (ie_origem_proced = coalesce(ie_origem_proced_p,0)))
	OR (nr_seq_proc_interno = nr_seq_proc_interno_p));

END IF;

RETURN ds_retorno_w;

END	;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_proced_hemoc ( nr_seq_derivado_p bigint, cd_procedimento_p bigint, ie_origem_proced_p bigint, nr_seq_proc_interno_p bigint) FROM PUBLIC;

