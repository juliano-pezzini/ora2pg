-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_gera_nm_arq_anexo_ptu ( nr_lote_p ptu_nota_cobranca.nr_lote%type, nr_nota_p ptu_nota_cobranca.nr_nota%type, nr_ordem_arq_p bigint, ds_extensao_p text) RETURNS varchar AS $body$
DECLARE

ds_retorno_w	varchar(255);
ds_extensao_w	varchar(100) := '';

BEGIN

if	((ds_extensao_p IS NOT NULL AND ds_extensao_p::text <> '') and (position('.' in ds_extensao_p) < 1)) then

	ds_extensao_w := '.'||ds_extensao_p;
else

	ds_extensao_w := ds_extensao_p;
end if;

ds_retorno_w := lpad(substr(coalesce(to_char(nr_lote_p),'0'),1,8),8,'0') || substr(rpad(substr(coalesce(replace(to_char(nr_nota_p),'.',''),'_'),1,20),20,'_') || lpad(nr_ordem_arq_p,3,'0'), 1, 255)||ds_extensao_w;

return ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_gera_nm_arq_anexo_ptu ( nr_lote_p ptu_nota_cobranca.nr_lote%type, nr_nota_p ptu_nota_cobranca.nr_nota%type, nr_ordem_arq_p bigint, ds_extensao_p text) FROM PUBLIC;

