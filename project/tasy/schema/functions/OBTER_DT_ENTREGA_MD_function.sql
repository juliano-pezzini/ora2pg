-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_dt_entrega_md ( ie_data_resultado_p text, ie_atualizar_dt_prescr_p text, qt_dia_entrega_entrada_p bigint, dt_coleta_p timestamp, qt_dia_entrega_coleta_p bigint ) RETURNS bigint AS $body$
DECLARE


qt_dia_entrega_w smallint := 0;


BEGIN
	if ((ie_data_resultado_p = 'P') and (coalesce(ie_atualizar_dt_prescr_p,'S') = 'S')) then
		 qt_dia_entrega_w := coalesce(qt_dia_entrega_entrada_p,0);
	elsif(ie_data_resultado_p = 'C' AND dt_coleta_p IS NOT NULL AND dt_coleta_p::text <> '') then
		 qt_dia_entrega_w := coalesce(qt_dia_entrega_coleta_p,0);
	elsif (ie_data_resultado_p = 'N') then
		if (coalesce(ie_atualizar_dt_prescr_p,'S') = 'S') then
			 qt_dia_entrega_w := coalesce(qt_dia_entrega_entrada_p,0);
		elsif (dt_coleta_p IS NOT NULL AND dt_coleta_p::text <> '') then
			 qt_dia_entrega_w := coalesce(qt_dia_entrega_coleta_p,0);
		end if;
	end if;
    RETURN qt_dia_entrega_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_dt_entrega_md ( ie_data_resultado_p text, ie_atualizar_dt_prescr_p text, qt_dia_entrega_entrada_p bigint, dt_coleta_p timestamp, qt_dia_entrega_coleta_p bigint ) FROM PUBLIC;

