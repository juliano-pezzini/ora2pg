-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION get_sinal_vital_nm_atributo ( nr_atendimento_p bigint, nm_field_p text) RETURNS bigint AS $body$
DECLARE


ds_return_w		integer;
ds_sql			varchar(1000);


BEGIN

if (nm_field_p IS NOT NULL AND nm_field_p::text <> '') then

	ds_sql :=		' select ' || nm_field_p || ' from atendimento_sinal_vital ' ||
					' where nr_atendimento = :nr_atendimento_p ' ||
					' and rownum = 1 ' ||
					' order by dt_atualizacao desc ';

	EXECUTE
		ds_sql
	into STRICT
		ds_return_w
	using
		nr_atendimento_p;

end if;

return ds_return_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION get_sinal_vital_nm_atributo ( nr_atendimento_p bigint, nm_field_p text) FROM PUBLIC;

