-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION san_ultimo_fenotipo_receptor ( nr_seq_transfusao_p bigint, nr_seq_reserva_p bigint) RETURNS varchar AS $body$
DECLARE


nr_seq_fenotipagem_w	bigint;			
ds_fenotipo_w		varchar(255);


BEGIN
if (nr_seq_transfusao_p IS NOT NULL AND nr_seq_transfusao_p::text <> '') or (nr_seq_reserva_p IS NOT NULL AND nr_seq_reserva_p::text <> '') then
	select	max(nr_sequencia)
	into STRICT	nr_seq_fenotipagem_w
	from	san_result_fenotipagem
	where	nr_seq_transfusao = nr_seq_transfusao_p
	or	nr_seq_reserva = nr_seq_reserva_p
    and (dt_liberacao IS NOT NULL AND dt_liberacao::text <> '')
    and coalesce(dt_inativacao::text, '') = '';
end if;
if (nr_seq_fenotipagem_w IS NOT NULL AND nr_seq_fenotipagem_w::text <> '') then
	select	substr(san_obter_result_fenotipagem(nr_seq_fenotipagem_w),1,255)
	into STRICT	ds_fenotipo_w
	;
end if;


return	ds_fenotipo_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION san_ultimo_fenotipo_receptor ( nr_seq_transfusao_p bigint, nr_seq_reserva_p bigint) FROM PUBLIC;

