-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_prescricao_prot (cd_protocolo_p bigint, nr_seq_medicacao_p bigint) RETURNS bigint AS $body$
DECLARE

total_w   bigint;


BEGIN

select  distinct max(total)
into STRICT    total_w
from (SELECT  count(*)total
from    protocolo_medic_dieta
where   cd_protocolo = cd_protocolo_p
and     nr_sequencia     = nr_seq_medicacao_p

union all

select  count(*)total
from    protocolo_medic_material
where   cd_protocolo = cd_protocolo_p
and     nr_sequencia     = nr_seq_medicacao_p

union all

select  count(*)total
from    protocolo_medic_proc
where   cd_protocolo = cd_protocolo_p
and     nr_sequencia     = nr_seq_medicacao_p

union all

select  count(*)total
from    protocolo_medic_rec
where   cd_protocolo = cd_protocolo_p
and     nr_sequencia     = nr_seq_medicacao_p

union all

select  count(*)total
from    protocolo_medic_solucao
where   cd_protocolo = cd_protocolo_p
and     nr_sequencia     = nr_seq_medicacao_p) alias6;


return  total_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_prescricao_prot (cd_protocolo_p bigint, nr_seq_medicacao_p bigint) FROM PUBLIC;

