-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_evento_lib_perfil ( nr_cirurgia_p bigint, nr_seq_pepo_p bigint, nr_seq_evento_p bigint, cd_perfil_ativo_p bigint ) RETURNS varchar AS $body$
DECLARE


ds_retorno_w			varchar(1);


BEGIN

SELECT	CASE WHEN COUNT(*)=0 THEN 'S'  ELSE 'N' END
into STRICT	ds_retorno_w
from	evento_cirurgia_lib
where	nr_seq_evento = nr_seq_evento_p;

IF (ds_retorno_w = 'N') THEN

	select	coalesce(max('S'),'N')
	into STRICT	ds_retorno_w
	from	evento_cirurgia_lib
	where	nr_seq_evento	= nr_seq_evento_p
	and	coalesce(cd_perfil,coalesce(cd_perfil_ativo_p,0))	= coalesce(cd_perfil_ativo_p,0)
	and	coalesce(cd_estabelecimento,coalesce(wheb_usuario_pck.get_cd_estabelecimento,0))	= coalesce(wheb_usuario_pck.get_cd_estabelecimento,0);

end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_evento_lib_perfil ( nr_cirurgia_p bigint, nr_seq_pepo_p bigint, nr_seq_evento_p bigint, cd_perfil_ativo_p bigint ) FROM PUBLIC;

