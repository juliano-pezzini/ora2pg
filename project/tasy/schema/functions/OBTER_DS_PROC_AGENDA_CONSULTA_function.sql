-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_ds_proc_agenda_consulta ( nr_sequencia_p bigint) RETURNS varchar AS $body$
DECLARE

        ds_procedimento_w       procedimento.ds_procedimento%type;

BEGIN
        select  substr(max(coalesce(obter_desc_exame(nr_seq_exame), obter_desc_prescr_proc(cd_procedimento, ie_origem_proced, nr_seq_proc_interno))),1,240)
        into STRICT    ds_procedimento_w
        from    agenda_consulta_proc
        where   nr_sequencia = nr_sequencia_p;

        return ds_procedimento_w;
End;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_ds_proc_agenda_consulta ( nr_sequencia_p bigint) FROM PUBLIC;

