-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_conv_brasil (cd_convenio_p bigint) RETURNS varchar AS $body$
DECLARE



ie_brasil_w	varchar(01)	:= 'S';
nr_seq_pais_w	bigint;
CD_CODIGO_PAIS_w	varchar(10);


BEGIN

select	coalesce(max(nr_seq_pais),0)
into STRICT	nr_seq_pais_w
from	convenio a, pessoa_juridica b
where	a.cd_cgc	= b.cd_cgc
and	a.cd_convenio	= cd_convenio_p;

select	coalesce(max(CD_CODIGO_PAIS),'0')
into STRICT	CD_CODIGO_PAIS_w
from	pais
where	nr_sequencia	= nr_seq_pais_w;


if (CD_CODIGO_PAIS_w <> '0') and (CD_CODIGO_PAIS_w <> '55') then
	ie_brasil_w	:= 'N';
end if;


return	ie_brasil_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_conv_brasil (cd_convenio_p bigint) FROM PUBLIC;

