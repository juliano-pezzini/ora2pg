-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_etapa_checkup_proc (nr_seq_proc_p bigint) RETURNS bigint AS $body$
DECLARE


nr_seq_etapa_checkup_w	bigint;
nr_seq_proc_est_w	bigint;
nr_seq_procedimento_w	bigint;


BEGIN

select	max(a.nr_seq_proc_est)
into STRICT	nr_seq_proc_est_w
from	procedimento_paciente a
where	a.nr_sequencia	= nr_seq_proc_p;

nr_seq_procedimento_w	:= coalesce(nr_seq_proc_est_w, nr_seq_proc_p);

select	max(a.nr_seq_etapa_checkup)
into STRICT	nr_seq_etapa_checkup_w
from	procedimento_paciente a
where	a.nr_sequencia	= nr_seq_procedimento_w;

return	nr_seq_etapa_checkup_w;

end	;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_etapa_checkup_proc (nr_seq_proc_p bigint) FROM PUBLIC;

