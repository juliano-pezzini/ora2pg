-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_descricao_diagnostico ( nr_atendimento_p bigint) RETURNS varchar AS $body$
DECLARE


ds_diagnostico_w		varchar(2000);


BEGIN

begin
select 	max(ds_diagnostico)
into STRICT 	ds_diagnostico_w
from 	diagnostico_medico
where	nr_atendimento	= nr_atendimento_p
and 	dt_diagnostico = (SELECT max(dt_diagnostico)
			  from 	 diagnostico_medico
			  where  nr_atendimento = nr_atendimento_p);
exception
	when no_data_found then
		ds_diagnostico_w := null;
end;

RETURN ds_diagnostico_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_descricao_diagnostico ( nr_atendimento_p bigint) FROM PUBLIC;

