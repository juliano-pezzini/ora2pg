-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION verifica_med_proc_global ( nr_seq_proc_interno_p bigint, cd_medico_p text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(1) := 'S';
qt_reg_w	varchar(10);


BEGIN


select	count(*)
into STRICT	qt_reg_w
from 	agenda_global
where	nr_seq_proc_interno 	= nr_seq_proc_interno_p
and	(cd_medico_agenda IS NOT NULL AND cd_medico_agenda::text <> '');

if (qt_reg_w > 0) then

	ds_retorno_w := 'N';


	select	count(*)
	into STRICT	qt_reg_w
	from 	agenda_global
	where	nr_seq_proc_interno 	= nr_seq_proc_interno_p
	and	cd_medico_agenda	= cd_medico_p;

	if (qt_reg_w > 0) then
		ds_retorno_w	:= 'S';
	end if;

end if;


return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION verifica_med_proc_global ( nr_seq_proc_interno_p bigint, cd_medico_p text) FROM PUBLIC;

