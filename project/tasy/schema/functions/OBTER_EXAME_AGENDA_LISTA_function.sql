-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_exame_agenda_lista (nr_seq_agenda_p bigint) RETURNS varchar AS $body$
DECLARE


cd_procedimento_w	bigint;
ie_origem_proced_w	bigint;
nr_seq_proc_interno_w	bigint;
ds_exame_w		varchar(255);


BEGIN

if (nr_seq_agenda_p IS NOT NULL AND nr_seq_agenda_p::text <> '') then
	begin

	select	cd_procedimento,
		ie_origem_proced,
		nr_seq_proc_interno
	into STRICT	cd_procedimento_w,
		ie_origem_proced_w,
		nr_seq_proc_interno_w
	from	agenda_paciente
	where	nr_sequencia	= nr_seq_agenda_p;

	ds_exame_w	:= substr(obter_exame_agenda(cd_procedimento_w,ie_origem_proced_w,nr_seq_proc_interno_w),1,240);
	end;
end if;

return ds_exame_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_exame_agenda_lista (nr_seq_agenda_p bigint) FROM PUBLIC;

