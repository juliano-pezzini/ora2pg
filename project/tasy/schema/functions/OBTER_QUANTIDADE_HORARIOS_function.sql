-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_quantidade_horarios (ds_horarios_p text) RETURNS bigint AS $body$
DECLARE

quantidade_w	bigint := 0;
BEGIN

if	(ds_horarios_p IS NOT NULL AND ds_horarios_p::text <> '') then
	for i in 1.. length(ds_horarios_p) loop
		if substr(ltrim(rtrim(ds_horarios_p)), i, 1) = ' ' then
			quantidade_w := quantidade_w + 1;
		end if;
	end loop;
end if;

return quantidade_w + 1;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_quantidade_horarios (ds_horarios_p text) FROM PUBLIC;

