-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION qt_obter_dados_agequi ( nr_seq_agequi_p bigint, ie_opcao_p text ) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(255);
/*
	P - Código pessoa fisica
	L - nr seq local
*/
BEGIN

if (nr_seq_agequi_p	> 0) then
	if (ie_opcao_p	= 'P') then
		select	max(cd_pessoa_fisica)
		into STRICT	ds_retorno_w
		from	agenda_quimio
		where	nr_sequencia	= nr_seq_agequi_p;
	elsif (ie_opcao_p	= 'L') then
		select	max(nr_seq_local)
		into STRICT	ds_retorno_w
		from	paciente_atendimento
		where	nr_seq_Atendimento	= nr_seq_agequi_p;
	end if;
end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION qt_obter_dados_agequi ( nr_seq_agequi_p bigint, ie_opcao_p text ) FROM PUBLIC;

