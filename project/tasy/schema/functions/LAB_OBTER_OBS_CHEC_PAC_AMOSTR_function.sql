-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION lab_obter_obs_chec_pac_amostr ( nr_prescricao_p prescr_proc_checagem_pac.nr_prescricao%type, nr_seq_prescr_p prescr_proc_checagem_pac.nr_seq_prescr%type) RETURNS PRESCR_PROC_CHECAGEM_PAC.DS_OBSERVACAO%TYPE AS $body$
DECLARE


ds_observacao_p	prescr_proc_checagem_pac.ds_observacao%type;


BEGIN
	begin
		select	ds_observacao
		into STRICT	ds_observacao_p
		from (
			SELECT	ds_observacao
			from	prescr_proc_checagem_pac
			where	nr_prescricao = nr_prescricao_p and
					nr_seq_prescr = nr_seq_prescr_p
			order by dt_atualizacao desc) alias0 LIMIT 1;
	exception
		when no_data_found then
			ds_observacao_p := OBTER_DESC_EXPRESSAO(654177);--Item não checado
	end;

	return	ds_observacao_p;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION lab_obter_obs_chec_pac_amostr ( nr_prescricao_p prescr_proc_checagem_pac.nr_prescricao%type, nr_seq_prescr_p prescr_proc_checagem_pac.nr_seq_prescr%type) FROM PUBLIC;

