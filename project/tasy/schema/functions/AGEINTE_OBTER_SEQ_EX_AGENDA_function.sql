-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION ageinte_obter_seq_ex_agenda ( nr_seq_Ageint_item_p bigint, cd_estabelecimento_p bigint, ie_regr_tempo_zero_p text default null) RETURNS bigint AS $body$
DECLARE


vl_Retorno_W		bigint;
nr_seq_regra_w		bigint;
cd_procedimento_w	bigint;
ie_origem_proced_w	bigint;
cd_area_procedimento_w	bigint;
cd_especialidade_w	bigint;
cd_grupo_proc_w		bigint;
cd_tipo_procedimento_w	procedimento.cd_tipo_procedimento%type;

C01 CURSOR FOR
	SELECT	nr_sequencia
	from	ageint_exames_agenda
	where	cd_estabelecimento					= cd_estabelecimento_p
	and	coalesce(cd_area_procedimento, cd_area_procedimento_w)	= cd_area_procedimento_w
	and	coalesce(cd_especialidade, cd_especialidade_w)		= cd_especialidade_w
	and	coalesce(cd_grupo_proc, cd_grupo_proc_w)			= cd_grupo_proc_w
	and	coalesce(cd_tipo_procedimento, cd_tipo_procedimento_w)	= cd_tipo_procedimento_w
	and (coalesce(ie_regr_tempo_zero_p::text, '') = '' or coalesce(ie_agrupar_item_sugestao,'N') = 'S')
	order by coalesce(cd_area_procedimento,0),
		coalesce(cd_especialidade,0),
		coalesce(cd_grupo_proc,0),
		coalesce(cd_tipo_procedimento,0);


BEGIN

select	cd_procedimento,
	ie_origem_proced
into STRICT	cd_procedimento_w,
	ie_origem_proced_w
from	agenda_integrada_item
where	nr_sequencia	= nr_seq_Ageint_item_p;

if (cd_procedimento_w IS NOT NULL AND cd_procedimento_w::text <> '') and (ie_origem_proced_w IS NOT NULL AND ie_origem_proced_w::text <> '') then
	select	max(cd_area_procedimento),
		max(cd_especialidade),
		max(cd_grupo_proc),
		coalesce(max(cd_tipo_procedimento),0)
	into STRICT	cd_area_procedimento_w,
		cd_especialidade_w,
		cd_grupo_proc_w,
		cd_tipo_procedimento_w
	from	estrutura_procedimento_v
	where	cd_procedimento		= cd_procedimento_w
	and	ie_origem_proced	= ie_origem_proced_w;
	open C01;
	loop
	fetch C01 into
		nr_seq_regra_w;
	EXIT WHEN NOT FOUND; /* apply on C01 */
		begin
		nr_seq_regra_w	:= nr_seq_regra_w;
		end;
	end loop;
	close C01;

	vl_Retorno_W	:= nr_seq_regra_w;
end if;

return	vl_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION ageinte_obter_seq_ex_agenda ( nr_seq_Ageint_item_p bigint, cd_estabelecimento_p bigint, ie_regr_tempo_zero_p text default null) FROM PUBLIC;

