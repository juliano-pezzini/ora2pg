-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_des_result_pont_meld_md ( ie_peld_meld_p text, vl_result_creat_p bigint, vl_result_bil_p bigint, vl_result_rni_p bigint, vl_peld_p bigint, vl_result_albumina_p bigint ) RETURNS bigint AS $body$
DECLARE

    ds_resultado_w double precision;

BEGIN
    IF ( ie_peld_meld_p = 'M' ) THEN
        ds_resultado_w := round((ln(vl_result_creat_p) * 9.57) +(ln(vl_result_bil_p) * 3.78) +(ln(vl_result_rni_p) * 11.20) + 6.42);
    ELSE
        ds_resultado_w := round((ln(vl_result_bil_p) * 4.80) +(ln(vl_result_rni_p) * 18.57) -(ln(vl_result_albumina_p) * 6.87) + vl_peld_p);
    END IF;

    RETURN ds_resultado_w;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_des_result_pont_meld_md ( ie_peld_meld_p text, vl_result_creat_p bigint, vl_result_bil_p bigint, vl_result_rni_p bigint, vl_peld_p bigint, vl_result_albumina_p bigint ) FROM PUBLIC;

