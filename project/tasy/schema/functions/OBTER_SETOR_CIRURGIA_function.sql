-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_setor_cirurgia ( nr_atendimento_p bigint, dt_entrada_unidade_p timestamp) RETURNS bigint AS $body$
DECLARE


cd_setor_atendimento_w	integer:=0;


BEGIN

if	(nr_atendimento_p > 0 AND dt_entrada_unidade_p IS NOT NULL AND dt_entrada_unidade_p::text <> '') then
	select	coalesce(max(cd_setor_atendimento),0)
	into STRICT	cd_setor_atendimento_w
	from	atend_paciente_unidade
	where	nr_atendimento	 = nr_atendimento_p
	and	dt_entrada_unidade	 = dt_entrada_unidade_p;
end if;

return	cd_setor_atendimento_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_setor_cirurgia ( nr_atendimento_p bigint, dt_entrada_unidade_p timestamp) FROM PUBLIC;

