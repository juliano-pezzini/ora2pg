-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_situacao_coop_prest ( cd_pessoa_fisica_p text, cd_cgc_p text) RETURNS bigint AS $body$
DECLARE


nr_seq_situacao_w	bigint;


BEGIN

if (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') then
	select	max(a.nr_seq_situacao)
	into STRICT	nr_seq_situacao_w
	from	pls_cooperado a
	where	a.cd_pessoa_fisica	= cd_pessoa_fisica_p;
elsif (cd_cgc_p IS NOT NULL AND cd_cgc_p::text <> '') then
	select	max(a.nr_seq_situacao)
	into STRICT	nr_seq_situacao_w
	from	pls_cooperado a
	where	a.cd_cgc	= cd_cgc_p;
end if;

return	nr_seq_situacao_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_situacao_coop_prest ( cd_pessoa_fisica_p text, cd_cgc_p text) FROM PUBLIC;

