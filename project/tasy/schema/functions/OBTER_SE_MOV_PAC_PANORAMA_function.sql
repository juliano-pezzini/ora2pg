-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_mov_pac_panorama ( cd_perfil_p bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w		varchar(255);
ie_movimenta_paciente_w	regra_perm_panorama.ie_movimenta_paciente%type;


BEGIN

select	coalesce(max(ie_movimenta_paciente),'S')
into STRICT 	ie_movimenta_paciente_w
from	regra_perm_panorama
where	cd_perfil = cd_perfil_p;

if (coalesce(ie_movimenta_paciente_w,'S') = 'S') then  -- se não encontrou a regra para o perfil, busca uma regra sem perfil definido.
	select	coalesce(max(ie_movimenta_paciente),'S')
	into STRICT 	ie_movimenta_paciente_w
	from	regra_perm_panorama
	where	coalesce(cd_perfil::text, '') = '';
end if;

ds_retorno_w	:= coalesce(ie_movimenta_paciente_w,'S');

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_mov_pac_panorama ( cd_perfil_p bigint) FROM PUBLIC;

