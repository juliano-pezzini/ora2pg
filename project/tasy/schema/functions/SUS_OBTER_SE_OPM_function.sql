-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION sus_obter_se_opm (CD_PROCEDIMENTO_P bigint, IE_ORIGEM_PROCED_P bigint) RETURNS varchar AS $body$
DECLARE

ds_retorno_w		varchar(2);
qt_registros_w		bigint;


BEGIN
select	count(*)
into STRICT	qt_registros_w
from	sus_ortese_protese
where	cd_proc_principal	= cd_procedimento_p
and	ie_origem_proc_princ	= IE_ORIGEM_PROCED_P;
if (qt_registros_w		= 0 ) then
	ds_retorno_w	:= 'N';
else
	ds_retorno_w	:= 'S';
end if;
return ds_retorno_w;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION sus_obter_se_opm (CD_PROCEDIMENTO_P bigint, IE_ORIGEM_PROCED_P bigint) FROM PUBLIC;

