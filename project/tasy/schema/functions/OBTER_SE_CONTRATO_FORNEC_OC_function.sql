-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_contrato_fornec_oc ( nr_seq_contrato_p bigint, nr_ordem_compra_p bigint) RETURNS varchar AS $body$
DECLARE

 
ie_retorno_w			varchar(1) := 'N';
cd_cgc_fornecedor_w		ordem_compra.cd_cgc_fornecedor%type;
cd_pessoa_fisica_w		ordem_compra.cd_pessoa_fisica%type;
			

BEGIN 
 
select	cd_cgc_fornecedor, 
	cd_pessoa_fisica 
into STRICT	cd_cgc_fornecedor_w, 
	cd_pessoa_fisica_w 
from	ordem_compra 
where	nr_ordem_compra = nr_ordem_compra_p;
 
if (cd_cgc_fornecedor_w IS NOT NULL AND cd_cgc_fornecedor_w::text <> '') then 
	ie_retorno_w := obter_se_contrato_do_cnpj(nr_seq_contrato_p,cd_cgc_fornecedor_w,'OC');
elsif (cd_pessoa_fisica_w IS NOT NULL AND cd_pessoa_fisica_w::text <> '') then 
	ie_retorno_w := obter_se_contrato_pessoa(nr_seq_contrato_p,cd_pessoa_fisica_w,'OC');
end if;
 
return	ie_retorno_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_contrato_fornec_oc ( nr_seq_contrato_p bigint, nr_ordem_compra_p bigint) FROM PUBLIC;

