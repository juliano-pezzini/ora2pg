-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION cih_obter_dias_medic ( nr_ficha_ocorrencia_p bigint, nr_seq_medicamento_p bigint, dt_inicial_p timestamp, dt_final_p timestamp) RETURNS bigint AS $body$
DECLARE



dt_inicial_utilizacao_w		timestamp;
dt_final_utilizacao_w		timestamp;
dt_inicial_w			timestamp;
dt_final_w			timestamp;
qt_dias_w			bigint;


BEGIN

select	dt_inicial_utilizacao,
	dt_final_utilizacao
into STRICT	dt_inicial_utilizacao_w,
	dt_final_utilizacao_w
from	cih_medic_utilizado_v
where	nr_seq_medicamento	= nr_seq_medicamento_p
and	nr_ficha_ocorrencia	= nr_ficha_ocorrencia_p;

dt_inicial_w		:= dt_inicial_utilizacao_w;
if (dt_inicial_utilizacao_w < dt_inicial_p) then
	dt_inicial_w	:= dt_inicial_p;
end if;

dt_final_w			:= dt_final_utilizacao_w;
if (dt_final_utilizacao_w > dt_final_p) then
	dt_final_w		:= dt_final_p;
end if;

qt_dias_w	:= (dt_final_w - dt_inicial_w) + 1;

return qt_dias_w;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION cih_obter_dias_medic ( nr_ficha_ocorrencia_p bigint, nr_seq_medicamento_p bigint, dt_inicial_p timestamp, dt_final_p timestamp) FROM PUBLIC;

