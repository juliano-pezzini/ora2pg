-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_min_max_data_nascimento ( qt_idade_p bigint, qt_idade_mes_p bigint, qt_idade_dia_p bigint, dt_nascimento_p timestamp, tipo_p text) RETURNS timestamp AS $body$
DECLARE

date_w timestamp;


BEGIN

    if (coalesce(qt_idade_p::text, '') = '' and coalesce(qt_idade_mes_p::text, '') = '' and coalesce(qt_idade_dia_p::text, '') = '') then
        date_w := dt_nascimento_p;
        if (tipo_p = 'MAX') then
            date_w := date_w -1;
        end if;
    else
        date_w := pkg_date_utils.get_Date(
                        PKG_DATE_UTILS.extract_field('YEAR',clock_timestamp()) - coalesce(qt_idade_p,0),
                        PKG_DATE_UTILS.extract_field('MONTH',clock_timestamp()) - coalesce(qt_idade_mes_p,0),
                        PKG_DATE_UTILS.extract_field('DAY',clock_timestamp()) - coalesce(qt_idade_dia_p,0));
    end if;

    return date_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_min_max_data_nascimento ( qt_idade_p bigint, qt_idade_mes_p bigint, qt_idade_dia_p bigint, dt_nascimento_p timestamp, tipo_p text) FROM PUBLIC;

