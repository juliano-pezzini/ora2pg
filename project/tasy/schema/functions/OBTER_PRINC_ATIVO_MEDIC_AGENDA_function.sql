-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_princ_ativo_medic_agenda (nr_seq_medic_p bigint) RETURNS varchar AS $body$
DECLARE


ds_principio_ativo_w	varchar(100);


BEGIN

if (nr_seq_medic_p IS NOT NULL AND nr_seq_medic_p::text <> '') then

	select	max(ds_principio_ativo)
	into STRICT	ds_principio_ativo_w
	from	agenda_medic
	where	nr_sequencia = nr_seq_medic_p;

end if;

return ds_principio_ativo_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_princ_ativo_medic_agenda (nr_seq_medic_p bigint) FROM PUBLIC;

