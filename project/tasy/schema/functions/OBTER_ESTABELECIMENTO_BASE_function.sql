-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_estabelecimento_base (cd_pessoa_fisica_titular_p text) RETURNS bigint AS $body$
DECLARE


cd_estabelecimento_base_w		bigint;


BEGIN

select	max(b.cd_estabelecimento_base)
into STRICT	cd_estabelecimento_base_w
from	pessoa_fisica_loc_trab	a,
	setor_atendimento	b
where	a.cd_setor_atendimento	= b.cd_setor_atendimento
and	a.cd_pessoa_fisica	= cd_pessoa_fisica_titular_p
and	a.ie_local_principal	= 'S';

return	cd_estabelecimento_base_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_estabelecimento_base (cd_pessoa_fisica_titular_p text) FROM PUBLIC;

