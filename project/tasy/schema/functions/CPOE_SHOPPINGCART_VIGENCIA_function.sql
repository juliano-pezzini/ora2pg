-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION cpoe_shoppingcart_vigencia (cd_intervalo_p text, ie_administracao_p text, dt_inicio_p timestamp, ie_urgencia_p text, ie_duracao_p text, dt_fim_p timestamp, ie_evento_unico_p text, ie_retrogrado_p text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(1) := 'N';
ie_operacao_w	intervalo_prescricao.ie_operacao%type;
ie_acm_w	intervalo_prescricao.ie_acm%type;
ie_se_necessario_w	intervalo_prescricao.ie_se_necessario%type;
qt_operacao_w	intervalo_prescricao.qt_operacao%type;


BEGIN

select	max(ie_operacao),
	max(ie_acm),
	max(ie_se_necessario),
	max(qt_operacao)
into STRICT	ie_operacao_w,
	ie_acm_w,
	ie_se_necessario_w,
	qt_operacao_w
from	intervalo_prescricao
where	cd_intervalo = cd_intervalo_p;

if ((cd_intervalo_p IS NOT NULL AND cd_intervalo_p::text <> '') and ie_administracao_p = 'C' and coalesce(ie_acm_w,'N') = 'N') then
	ds_retorno_w := 'S';
	goto return_value;
end if;

if ((cd_intervalo_p IS NOT NULL AND cd_intervalo_p::text <> '') and ie_administracao_p = 'N' and coalesce(ie_se_necessario_w,'N') = 'N') then
	ds_retorno_w := 'S';
	goto return_value;
end if;

if (coalesce(ie_urgencia_p::text, '') = '' and trunc(dt_inicio_p, 'mi') < trunc(clock_timestamp(),'mi') and coalesce(ie_retrogrado_p,'N') = 'N') then
	ds_retorno_w := 'S';
	goto return_value;
end if;

if (ie_duracao_p = 'P' and coalesce(dt_fim_p::text, '') = '') then
	ds_retorno_w := 'S';
	goto return_value;
end if;

if (qt_operacao_w = 1 and coalesce(ie_evento_unico_p, 'N')  = 'S') then
	if (ie_duracao_p <> 'P') then
		ds_retorno_w := 'S';
		goto return_value;
	end if;
end if;

<<return_value>>		
 null;

 return ds_retorno_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION cpoe_shoppingcart_vigencia (cd_intervalo_p text, ie_administracao_p text, dt_inicio_p timestamp, ie_urgencia_p text, ie_duracao_p text, dt_fim_p timestamp, ie_evento_unico_p text, ie_retrogrado_p text) FROM PUBLIC;

