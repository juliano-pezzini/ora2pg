-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_ds_glosa (cd_motivo_glosa_p text) RETURNS varchar AS $body$
DECLARE


nr_seq_motivo_glosa_w	bigint := 0;
ds_glosa_w		varchar(255);


BEGIN
if (cd_motivo_glosa_p IS NOT NULL AND cd_motivo_glosa_p::text <> '') then
	begin

	select	coalesce(substr(pls_obter_seq_motivo_glosa(cd_motivo_glosa_p),1,255), 0)
	into STRICT	nr_seq_motivo_glosa_w
	;

	if (nr_seq_motivo_glosa_w <> 0) then
		begin

		select	substr(tiss_obter_motivo_glosa(nr_seq_motivo_glosa_w,'D'),1,255) ds_glosa
		into STRICT	ds_glosa_w
		;

		end;
	end if;

	end;
end if;

return	ds_glosa_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_ds_glosa (cd_motivo_glosa_p text) FROM PUBLIC;

