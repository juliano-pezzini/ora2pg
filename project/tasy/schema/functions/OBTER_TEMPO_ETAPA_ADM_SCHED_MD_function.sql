-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_tempo_etapa_adm_sched_md (ie_Calc_Hora_Fase_Direnciada_p text, Qt_Hora_Fase_Diferenciada_p bigint, qt_Nr_Etapas_p bigint, qt_Tempo_Etapa_p bigint ) RETURNS bigint AS $body$
DECLARE

   result_w bigint;

BEGIN
    if (ie_Calc_Hora_Fase_Direnciada_p = 'S') then
        if coalesce(Qt_Hora_Fase_Diferenciada_p::text, '') = '' then
            result_w := null;
        else
           if (coalesce(qt_Nr_Etapas_p::text, '') = '' or qt_Nr_Etapas_p = 0) then
             result_w := dividir_sem_round_md(24,1);
           else
             result_w := dividir_sem_round_md(24,qt_Nr_Etapas_p);
           end if;
        end if;
    else
        if (coalesce(qt_Tempo_Etapa_p::text, '') = '' or qt_Tempo_Etapa_p = 0) then
            result_w := null;
        end if;
    end if;

    return result_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_tempo_etapa_adm_sched_md (ie_Calc_Hora_Fase_Direnciada_p text, Qt_Hora_Fase_Diferenciada_p bigint, qt_Nr_Etapas_p bigint, qt_Tempo_Etapa_p bigint ) FROM PUBLIC;

