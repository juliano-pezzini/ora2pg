-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_prescr_compl_laudo_lib ( nr_prescricao_p bigint, nr_seq_prescr_p bigint) RETURNS varchar AS $body$
DECLARE


nr_seq_proc_compl_w 	bigint;
ie_laudo_liberado_int_w	varchar(1);


BEGIN

ie_laudo_liberado_int_w := 'N';

if 	((coalesce(nr_prescricao_p,0) > 0) and (coalesce(nr_seq_prescr_p,0) > 0)) then

	select 	coalesce(nr_seq_proc_compl,0)
	into STRICT	nr_seq_proc_compl_w
	from 	prescr_procedimento
	where	nr_prescricao = nr_prescricao_p
	and	nr_sequencia = nr_seq_prescr_p;

	if (coalesce(nr_seq_proc_compl_w,0) > 0) then

		select 	max(coalesce(ie_laudo_liberado_int,'N'))
		into STRICT	ie_laudo_liberado_int_w
		from	prescr_procedimento_compl
		where	nr_sequencia = nr_seq_proc_compl_w;

	end if;

end if;

return	ie_laudo_liberado_int_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_prescr_compl_laudo_lib ( nr_prescricao_p bigint, nr_seq_prescr_p bigint) FROM PUBLIC;

