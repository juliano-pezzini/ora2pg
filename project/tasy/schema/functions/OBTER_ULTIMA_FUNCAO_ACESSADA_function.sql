-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_ultima_funcao_acessada ( nr_seq_acesso_p text, ie_opcao_p bigint) RETURNS varchar AS $body$
DECLARE


nr_sequencia_w	bigint;
ds_funcao_w	varchar(80) := '';


BEGIN

if (nr_seq_acesso_p IS NOT NULL AND nr_seq_acesso_p::text <> '') then
	select	max(nr_sequencia)
	into STRICT	nr_sequencia_w
	from	log_Acesso_funcao
	where	nr_seq_acesso = nr_seq_acesso_p
	   and	coalesce(dt_fim_acesso::text, '') = '';

	if (coalesce(nr_sequencia_w::text, '') = '') then
		select	max(nr_sequencia)
		into STRICT	nr_sequencia_w
		from	log_Acesso_funcao
		where	nr_seq_acesso = nr_seq_acesso_p;
	end if;

	if (nr_sequencia_w IS NOT NULL AND nr_sequencia_w::text <> '') then
		select	ds_funcao
		into STRICT	ds_funcao_w
		from	funcao b,
			log_acesso_funcao a
		where a.cd_funcao = b.cd_funcao
		  and a.nr_sequencia = nr_sequencia_w;
	end if;

end if;

return ds_funcao_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_ultima_funcao_acessada ( nr_seq_acesso_p text, ie_opcao_p bigint) FROM PUBLIC;

