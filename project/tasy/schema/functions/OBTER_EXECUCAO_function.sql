-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_execucao ( nr_atendimento_p bigint ) RETURNS varchar AS $body$
DECLARE

  existe bigint;

BEGIN
  select count(1)
    into STRICT existe
    from imagem_pac_prot_externo
   where nr_atendimento = nr_atendimento_p;
  if existe > 0 then
     RETURN obter_desc_expressao(718223); -- Externo;
   else
     RETURN obter_desc_expressao(697085); -- Interno
   end if;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_execucao ( nr_atendimento_p bigint ) FROM PUBLIC;

