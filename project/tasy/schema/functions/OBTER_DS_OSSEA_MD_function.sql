-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_ds_ossea_md ( qt_ossea_p bigint, ds_sexo_p text ) RETURNS varchar AS $body$
DECLARE

  ds_result_w	varchar(25);

BEGIN
  if (ds_sexo_p = 'M')then 
    if (qt_ossea_p > 10.4)then 
      ds_result_w := obter_desc_expressao(762831);
    elsif (qt_ossea_p >= 9.6) then 
      ds_result_w := obter_desc_expressao(762833);
    else 
      ds_result_w := obter_desc_expressao(762835);
    end if;
  else 
    if (qt_ossea_p > 11)then 
      ds_result_w := obter_desc_expressao(762831);
    elsif (qt_ossea_p >= 10.1) then 
      ds_result_w := obter_desc_expressao(762833);
    else 
      ds_result_w := obter_desc_expressao(762835);
    end if;
  end if;

  return ds_result_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_ds_ossea_md ( qt_ossea_p bigint, ds_sexo_p text ) FROM PUBLIC;

