-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';



--
-- dblink wrapper to call function is_duplic_regra_leitor_cart as an autonomous transaction
--
CREATE EXTENSION IF NOT EXISTS dblink;

CREATE OR REPLACE FUNCTION is_duplic_regra_leitor_cart ( nr_sequencia_p bigint, nr_endereco_ip_p text, nm_computador_p text, ie_tipo_cartao_p text, ie_fabricante_p text, ie_interface_p text, nr_seq_regra_conv_p bigint ) RETURNS varchar AS $body$
DECLARE
	-- Change this to reflect the dblink connection string
	v_conn_str  text := format('port=%s dbname=%s user=%s', current_setting('port'), current_database(), current_user);
	v_query     text;

	v_ret	varchar;
BEGIN
	v_query := 'SELECT * FROM is_duplic_regra_leitor_cart_atx ( ' || quote_nullable(nr_sequencia_p) || ',' || quote_nullable(nr_endereco_ip_p) || ',' || quote_nullable(nm_computador_p) || ',' || quote_nullable(ie_tipo_cartao_p) || ',' || quote_nullable(ie_fabricante_p) || ',' || quote_nullable(ie_interface_p) || ',' || quote_nullable(nr_seq_regra_conv_p) || ' )';
	SELECT * INTO v_ret FROM dblink(v_conn_str, v_query) AS p (ret varchar);
	RETURN v_ret;

END;
$body$ LANGUAGE plpgsql SECURITY DEFINER;




CREATE OR REPLACE FUNCTION is_duplic_regra_leitor_cart_atx ( nr_sequencia_p bigint, nr_endereco_ip_p text, nm_computador_p text, ie_tipo_cartao_p text, ie_fabricante_p text, ie_interface_p text, nr_seq_regra_conv_p bigint ) RETURNS varchar AS $body$
DECLARE
is_duplicate_w smallint;


BEGIN
	select count(1)
	into STRICT    is_duplicate_w
	from    regra_leitor_cartao_pac z
	where   z.nr_sequencia  <>  nr_sequencia_p
	and     coalesce(z.nr_endereco_ip, 'null')   =   coalesce(nr_endereco_ip_p, 'null')
	and     coalesce(z.nm_computador, 'null')    =  coalesce(nm_computador_p, 'null')
        and     z.ie_tipo_cartao    =   ie_tipo_cartao_p
        and     z.ie_fabricante     =   ie_fabricante_p
        and     z.ie_interface      =   ie_interface_p
        and     z.nr_seq_regra_conv   =   nr_seq_regra_conv_p;

    if (is_duplicate_w > 0) then
        return 'S';
    else
        return 'N';
    end if;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION is_duplic_regra_leitor_cart ( nr_sequencia_p bigint, nr_endereco_ip_p text, nm_computador_p text, ie_tipo_cartao_p text, ie_fabricante_p text, ie_interface_p text, nr_seq_regra_conv_p bigint ) FROM PUBLIC; -- REVOKE ALL ON FUNCTION is_duplic_regra_leitor_cart_atx ( nr_sequencia_p bigint, nr_endereco_ip_p text, nm_computador_p text, ie_tipo_cartao_p text, ie_fabricante_p text, ie_interface_p text, nr_seq_regra_conv_p bigint ) FROM PUBLIC;

