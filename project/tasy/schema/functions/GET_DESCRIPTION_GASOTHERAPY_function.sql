-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION get_description_gasotherapy ( nr_seq_gas_cpoe_p bigint) RETURNS varchar AS $body$
DECLARE

ds_text_w varchar(2000) := '';
ds_new_line_w varchar(10) := ' ' || chr(13) || chr(10);
ds_separator_w varchar(10) := ': ';
cpoe_gas_w cpoe_gasoterapia%rowtype;

BEGIN
if (nr_seq_gas_cpoe_p IS NOT NULL AND nr_seq_gas_cpoe_p::text <> '') then
	begin	
		begin
		select 	*
		into STRICT	cpoe_gas_w
		from 	cpoe_gasoterapia
		where 	nr_sequencia = nr_seq_gas_cpoe_p  LIMIT 1;
		exception when others then
		ds_text_w := '';
		end;
		
		if (cpoe_gas_w.qt_freq_vent IS NOT NULL AND cpoe_gas_w.qt_freq_vent::text <> '') then
			begin
			ds_text_w := ds_text_w || obter_desc_expressao(756812, null) || ds_separator_w || cpoe_gas_w.qt_freq_vent || ds_new_line_w;
			end;
		end if;
		if (cpoe_gas_w.qt_vc_prog IS NOT NULL AND cpoe_gas_w.qt_vc_prog::text <> '') then
			begin
			ds_text_w := ds_text_w || obter_desc_expressao(755224, null) || ds_separator_w || cpoe_gas_w.qt_vc_prog || ds_new_line_w;
			end;
		end if;
		if (cpoe_gas_w.qt_pip IS NOT NULL AND cpoe_gas_w.qt_pip::text <> '') then
			begin
			ds_text_w := ds_text_w || obter_desc_expressao(314664, null) || ds_separator_w || cpoe_gas_w.qt_pip || ds_new_line_w;
			end;
		end if;
		if (cpoe_gas_w.qt_epap IS NOT NULL AND cpoe_gas_w.qt_epap::text <> '') then
			begin
			ds_text_w := ds_text_w || obter_desc_expressao(950775, null) || ds_separator_w || cpoe_gas_w.qt_epap || ds_new_line_w;
			end;
		end if;
		if (cpoe_gas_w.qt_peep IS NOT NULL AND cpoe_gas_w.qt_peep::text <> '') then
			begin
			ds_text_w := ds_text_w || obter_desc_expressao(314679, null) || ds_separator_w || cpoe_gas_w.qt_peep || ds_new_line_w;
			end;
		end if;
		if (cpoe_gas_w.qt_cpap IS NOT NULL AND cpoe_gas_w.qt_cpap::text <> '') then
			begin
			ds_text_w := ds_text_w || obter_desc_expressao(950817, null) || ds_separator_w || cpoe_gas_w.qt_cpap || ds_new_line_w;
			end;
		end if;
		if (cpoe_gas_w.qt_ps IS NOT NULL AND cpoe_gas_w.qt_ps::text <> '') then
			begin
			ds_text_w := ds_text_w || obter_desc_expressao(742999, null) || ds_separator_w || cpoe_gas_w.qt_ps || ds_new_line_w;
			end;
		end if;
		if (cpoe_gas_w.qt_abaixo_peep IS NOT NULL AND cpoe_gas_w.qt_abaixo_peep::text <> '') then
			begin
			ds_text_w := ds_text_w || obter_desc_expressao(950755, null) || ds_separator_w || cpoe_gas_w.qt_abaixo_peep || ds_new_line_w;
			end;
		end if;
		if (cpoe_gas_w.qt_acima_peep IS NOT NULL AND cpoe_gas_w.qt_acima_peep::text <> '') then
			begin
			ds_text_w := ds_text_w || obter_desc_expressao(756990, null) || ds_separator_w || cpoe_gas_w.qt_acima_peep || ds_new_line_w;
			end;
		end if;
		if (cpoe_gas_w.qt_fluxo_insp IS NOT NULL AND cpoe_gas_w.qt_fluxo_insp::text <> '') then
			begin
			ds_text_w := ds_text_w || obter_desc_expressao(741701, null) || ds_separator_w || cpoe_gas_w.qt_fluxo_insp || ds_new_line_w;
			end;
		end if;
		if (cpoe_gas_w.qt_tempo_insp IS NOT NULL AND cpoe_gas_w.qt_tempo_insp::text <> '') then
			begin
			ds_text_w := ds_text_w || obter_desc_expressao(741705, null) || ds_separator_w || cpoe_gas_w.qt_tempo_insp || ds_new_line_w;
			end;
		end if;
		if (cpoe_gas_w.qt_ti_te IS NOT NULL AND cpoe_gas_w.qt_ti_te::text <> '') then
			begin
			ds_text_w := ds_text_w || obter_desc_expressao(756988, null) || ds_separator_w || cpoe_gas_w.qt_ti_te || ds_new_line_w;
			end;
		end if;
		if (cpoe_gas_w.qt_tempo_baixo IS NOT NULL AND cpoe_gas_w.qt_tempo_baixo::text <> '') then
			begin
			ds_text_w := ds_text_w || obter_desc_expressao(950761, null) || ds_separator_w || cpoe_gas_w.qt_tempo_baixo || ds_new_line_w;
			end;
		end if;
		if (cpoe_gas_w.qt_tempo_alto IS NOT NULL AND cpoe_gas_w.qt_tempo_alto::text <> '') then
			begin
			ds_text_w := ds_text_w || obter_desc_expressao(950765, null) || ds_separator_w || cpoe_gas_w.qt_tempo_alto || ds_new_line_w;
			end;
		end if;
		if (cpoe_gas_w.qt_sensib_resp IS NOT NULL AND cpoe_gas_w.qt_sensib_resp::text <> '') then
			begin
			ds_text_w := ds_text_w || obter_desc_expressao(756994, null) || ds_separator_w || cpoe_gas_w.qt_sensib_resp || ds_new_line_w;
			end;
		end if;
		if (cpoe_gas_w.ie_tipo_onda IS NOT NULL AND cpoe_gas_w.ie_tipo_onda::text <> '') then
			begin
			ds_text_w := ds_text_w || obter_desc_expressao(299524, null) || ds_separator_w || obter_valor_dominio(3503,cpoe_gas_w.ie_tipo_onda) || ds_new_line_w;
			end;
		end if;
		if (cpoe_gas_w.qt_freq_vent_hz IS NOT NULL AND cpoe_gas_w.qt_freq_vent_hz::text <> '') then
			begin
			ds_text_w := ds_text_w || obter_desc_expressao(1038729, null) || ds_separator_w || cpoe_gas_w.qt_freq_vent_hz || ds_new_line_w;
			end;
		end if;
	end;
end if;
return ds_text_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION get_description_gasotherapy ( nr_seq_gas_cpoe_p bigint) FROM PUBLIC;

