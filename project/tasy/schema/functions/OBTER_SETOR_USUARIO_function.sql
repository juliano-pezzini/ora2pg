-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_setor_usuario ( nm_usuario_p text) RETURNS bigint AS $body$
DECLARE


cd_setor_atendimento_w		integer;

nm_usuario_w varchar(15);


BEGIN

	begin
	select	cd_setor_atendimento
	into STRICT	cd_setor_atendimento_w
	from	usuario
	where	nm_usuario = nm_usuario_p;
	exception
		when no_data_found then
			nm_usuario_w := upper(nm_usuario_p);
			select	max(cd_setor_atendimento)
			into STRICT	cd_setor_atendimento_w
			from	usuario
			where	upper(nm_usuario) = nm_usuario_w;
	end;

return	cd_setor_atendimento_w;

End;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_setor_usuario ( nm_usuario_p text) FROM PUBLIC;

