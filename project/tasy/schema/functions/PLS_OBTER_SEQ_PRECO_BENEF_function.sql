-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_seq_preco_benef ( nr_seq_segurado_p bigint, dt_preco_p timestamp, cd_estabelecimento_p bigint) RETURNS bigint AS $body$
DECLARE


nr_seq_preco_w		bigint;


BEGIN

select	max(a.nr_sequencia)
into STRICT	nr_seq_preco_w
from	table(pls_segurado_preco_pck.obter_preco_segurado(nr_seq_segurado_p,null,cd_estabelecimento_p)) a
where	ie_situacao = 'A'
and	dt_preco <= dt_preco_p;

return	nr_seq_preco_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_seq_preco_benef ( nr_seq_segurado_p bigint, dt_preco_p timestamp, cd_estabelecimento_p bigint) FROM PUBLIC;

