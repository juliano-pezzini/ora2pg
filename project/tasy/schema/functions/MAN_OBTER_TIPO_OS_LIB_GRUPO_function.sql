-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION man_obter_tipo_os_lib_grupo ( nr_seq_grupo_planej_p bigint, ie_tipo_ordem_p text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w		varchar(1) := 'S';
qt_existe_w		bigint;


BEGIN

select	count(*)
into STRICT	qt_existe_w
from	man_grupo_planej_tp_os_lib
where	nr_seq_grupo_planej = nr_seq_grupo_planej_p;

if (qt_existe_w > 0) then
	begin
	select	'S'
	into STRICT	ds_retorno_w
	from	man_grupo_planej_tp_os_lib
	where	nr_seq_grupo_planej = nr_seq_grupo_planej_p
	and	ie_tipo_ordem = ie_tipo_ordem_p  LIMIT 1;
	exception
	when others then
		ds_retorno_w := 'N';
	end;
end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION man_obter_tipo_os_lib_grupo ( nr_seq_grupo_planej_p bigint, ie_tipo_ordem_p text) FROM PUBLIC;

