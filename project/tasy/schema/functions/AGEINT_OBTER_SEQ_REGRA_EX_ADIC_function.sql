-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION ageint_obter_seq_regra_ex_adic ( nr_seq_proc_interno_p bigint, cd_convenio_p bigint, cd_categoria_p text, cd_estabelecimento_p bigint, nm_usaurio_p text) RETURNS bigint AS $body$
DECLARE


vl_Retorno_w		bigint;
cd_area_procedimento_w	bigint;
cd_especialidade_w	bigint;
cd_grupo_proc_w		bigint;
nr_seq_regra_w		bigint;
cd_procedimento_w	bigint;
ie_origem_proced_w	bigint;

C01 CURSOR FOR
	SELECT	nr_sequencia
	from	ageint_qtd_Exames_adic
	where	cd_estabelecimento					= cd_estabelecimento_p
	and	coalesce(cd_area_procedimento, cd_area_procedimento_w)	= cd_area_procedimento_w
	and	coalesce(cd_especialidade, cd_especialidade_w)		= cd_especialidade_w
	and	coalesce(cd_grupo_proc, cd_grupo_proc_w)			= cd_grupo_proc_w
	order by coalesce(cd_area_procedimento,0),
		coalesce(cd_especialidade,0),
		coalesce(cd_grupo_proc,0);


BEGIN

SELECT * FROM obter_proc_tab_interno_conv(
						nr_seq_proc_interno_p, cd_estabelecimento_p, cd_convenio_p, cd_categoria_p, null, null, cd_procedimento_w, ie_origem_proced_w, null, clock_timestamp(), null, null, null, null, null, null, null, null) INTO STRICT cd_procedimento_w, ie_origem_proced_w;

if (cd_procedimento_w IS NOT NULL AND cd_procedimento_w::text <> '') and (ie_origem_proced_w IS NOT NULL AND ie_origem_proced_w::text <> '') then
	select	max(cd_area_procedimento),
		max(cd_especialidade),
		max(cd_grupo_proc)
	into STRICT	cd_area_procedimento_w,
		cd_especialidade_w,
		cd_grupo_proc_w
	from	estrutura_procedimento_v
	where	cd_procedimento		= cd_procedimento_w
	and	ie_origem_proced	= ie_origem_proced_w;

	open C01;
	loop
	fetch C01 into
		nr_seq_regra_w;
	EXIT WHEN NOT FOUND; /* apply on C01 */
		begin
		vl_retorno_w	:= nr_seq_regra_w;
		end;
	end loop;
	close C01;
end if;

return	vl_Retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION ageint_obter_seq_regra_ex_adic ( nr_seq_proc_interno_p bigint, cd_convenio_p bigint, cd_categoria_p text, cd_estabelecimento_p bigint, nm_usaurio_p text) FROM PUBLIC;

