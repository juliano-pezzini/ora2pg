-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_proc_interno_edicao ( cd_estabelecimento_p bigint, nr_seq_interno_p bigint, cd_convenio_p bigint, cd_categoria_p text, dt_conta_p timestamp) RETURNS varchar AS $body$
DECLARE

 
cd_procedimento_w		bigint;
ie_origem_proced_w		bigint;
ds_retorno_w			varchar(2);


BEGIN 
 
SELECT * FROM Obter_Proc_Tab_Interno_Conv(	nr_seq_interno_p, cd_estabelecimento_p, cd_convenio_p, cd_categoria_p, null, null, cd_procedimento_w, ie_origem_proced_w, null, dt_conta_p, null, null, null, null, null, null, null, null) INTO STRICT cd_procedimento_w, ie_origem_proced_w;
				 
 
select	obter_se_proc_edicao(cd_estabelecimento_p,cd_convenio_p,cd_categoria_p,dt_conta_p,cd_procedimento_w, null) 
into STRICT	ds_retorno_w
;
 
 
 
return	ds_retorno_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_proc_interno_edicao ( cd_estabelecimento_p bigint, nr_seq_interno_p bigint, cd_convenio_p bigint, cd_categoria_p text, dt_conta_p timestamp) FROM PUBLIC;

