-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_regra_acesso_func (cd_perfil_p text, nr_seq_regra_p bigint) RETURNS varchar AS $body$
DECLARE


ie_possui_regra_w	varchar(1);

BEGIN


select 	CASE WHEN count(*)=0 THEN 'S'  ELSE 'N' END
into STRICT	ie_possui_regra_w
from	atend_regra_acesso_func
where 	cd_perfil = cd_perfil_p;

if (ie_possui_regra_w = 'N') then

	select 	CASE WHEN count(*)=0 THEN 'N'  ELSE 'S' END
	into STRICT	ie_possui_regra_w
	from	atend_regra_acesso_func
	where (cd_perfil = cd_perfil_p)
	and (nr_seq_regra = nr_seq_regra_p);

end if;

return	ie_possui_regra_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_regra_acesso_func (cd_perfil_p text, nr_seq_regra_p bigint) FROM PUBLIC;

