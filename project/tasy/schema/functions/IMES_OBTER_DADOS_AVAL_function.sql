-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION imes_obter_dados_aval ( cd_pessoa_fisica_p bigint, dt_inicio_p timestamp, dt_fim_p timestamp, nr_seq_aval_p bigint, ie_opcao_p text) RETURNS varchar AS $body$
DECLARE

	 		 
ds_retorno_dados_w   varchar(2000);
ds_retorno_datas_w   varchar(2000);

ds_dados_w   	varchar(100);
ds_datas_w   	varchar(100);

C01 CURSOR FOR 
	SELECT 	RPAD(substr(obter_desc_result_avaliacao(c.nr_sequencia,nr_seq_aval_p),1,20),18,' '), 
		RPAD(SUBSTR(c.dt_avaliacao,1,10),18,' ') 
	FROM 	med_avaliacao_paciente c, 
		med_tipo_avaliacao b, 
		med_item_avaliar a 
	WHERE  c.nr_seq_tipo_avaliacao = b.nr_sequencia 
	AND	a.nr_seq_tipo = b.nr_sequencia 
	AND 	a.nr_seq_tipo = 643 
	AND   a.nr_sequencia = nr_seq_aval_p 
	AND	c.cd_pessoa_fisica = cd_pessoa_fisica_p 
	AND	c.dt_avaliacao BETWEEN dt_inicio_p AND fim_dia(dt_fim_p);

 

BEGIN 
ds_retorno_dados_w   := null;
ds_retorno_datas_w   := null;
 
ds_dados_w   	:= null;
ds_datas_w   	:= null;
 
open C01;
loop 
fetch C01 into	 
	ds_dados_w, 
	ds_datas_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin 
	 
	ds_retorno_dados_w := ds_retorno_dados_w || ds_dados_w;
	ds_retorno_datas_w := ds_retorno_datas_w || ds_datas_w;
	 
	end;
end loop;
close C01;
 
if (ie_opcao_p = 'DT') then 
	return	ds_retorno_datas_w;
end if;
if (ie_opcao_p = 'DD') then 
	return	ds_retorno_dados_w;
end if;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION imes_obter_dados_aval ( cd_pessoa_fisica_p bigint, dt_inicio_p timestamp, dt_fim_p timestamp, nr_seq_aval_p bigint, ie_opcao_p text) FROM PUBLIC;

