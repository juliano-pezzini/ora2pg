-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION qua_obter_se_classif_ata_lib ( nr_sequencia_p bigint, nm_usuario_p text) RETURNS varchar AS $body$
DECLARE


ie_retorno_w		varchar(1) := 'S';
qt_reg_w			bigint;
cd_perfil_w		integer := obter_perfil_ativo;

C01 CURSOR FOR
	SELECT	ie_visualiza
	from	proj_ata_classif_lib
	where	coalesce(nm_usuario_lib,nm_usuario_p) = nm_usuario_p
	and	coalesce(cd_perfil_lib,cd_perfil_w) = cd_perfil_w
	and	nr_seq_classificacao = nr_sequencia_p
	order by nm_usuario_lib desc, coalesce(cd_perfil_lib,0), nr_sequencia;


BEGIN

select	count(*)
into STRICT	qt_reg_w
from	proj_ata_classif_lib
where	nr_seq_classificacao = nr_sequencia_p;

if (qt_reg_w > 0) then
	begin
	ie_retorno_w := 'N';

	open C01;
	loop
	fetch C01 into
		ie_retorno_w;
	EXIT WHEN NOT FOUND; /* apply on C01 */
	end loop;
	close C01;
	end;
end if;

return	ie_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION qua_obter_se_classif_ata_lib ( nr_sequencia_p bigint, nm_usuario_p text) FROM PUBLIC;

