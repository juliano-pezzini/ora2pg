-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_perc_prev_real_proj_migr ( nr_seq_projeto_p bigint, dt_previsao_p timestamp) RETURNS bigint AS $body$
DECLARE


pr_prev_real_w		double precision;
qt_horas_prev_real_w	double precision;
qt_horas_prev_proj_w	double precision;


BEGIN
if (nr_seq_projeto_p IS NOT NULL AND nr_seq_projeto_p::text <> '') and (dt_previsao_p IS NOT NULL AND dt_previsao_p::text <> '') then
	begin
	qt_horas_prev_real_w := obter_sent_real_proj_migr(nr_seq_projeto_p, dt_previsao_p);

	select	sum(coalesce(proj_obter_qt_total_horas_cron(nr_seq_projeto_p, 0, 'T', 'E'), 0))
	into STRICT	qt_horas_prev_proj_w
	;

	pr_prev_real_w := obter_perc_valor(qt_horas_prev_real_w, qt_horas_prev_proj_w);

	if (pr_prev_real_w > 100) then
		begin
		pr_prev_real_w := 100;
		end;
	end if;
	end;
end if;
return pr_prev_real_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_perc_prev_real_proj_migr ( nr_seq_projeto_p bigint, dt_previsao_p timestamp) FROM PUBLIC;

