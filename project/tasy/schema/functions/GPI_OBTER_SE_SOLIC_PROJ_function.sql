-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION gpi_obter_se_solic_proj ( nr_seq_proj_gpi_p bigint, nr_seq_solic_p bigint) RETURNS varchar AS $body$
DECLARE


ie_vinculo_solic_proj_w varchar(1);


BEGIN
SELECT CASE
         WHEN Count(1) > 0 THEN 'S'
         ELSE 'N'
       END AS ie_possui
INTO STRICT   ie_vinculo_solic_proj_w
FROM   solic_compra
WHERE  nr_seq_proj_gpi = nr_seq_proj_gpi_p
AND    nr_solic_compra = nr_seq_solic_p;

RETURN ie_vinculo_solic_proj_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION gpi_obter_se_solic_proj ( nr_seq_proj_gpi_p bigint, nr_seq_solic_p bigint) FROM PUBLIC;

