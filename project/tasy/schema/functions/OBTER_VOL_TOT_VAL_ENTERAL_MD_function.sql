-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_vol_tot_val_enteral_md ( ie_ref_calculo_p text, qt_volume_p bigint, nr_etapas_p bigint, qt_volume_total_p bigint ) RETURNS bigint AS $body$
DECLARE

    qt_volume_total_w double precision := qt_volume_total_p;

BEGIN
    IF ( ie_ref_calculo_p = 'VI' ) THEN
        qt_volume_total_w := ( qt_volume_p * nr_etapas_p );
    ELSIF ( ie_ref_calculo_p = 'VT' ) THEN
        IF ( qt_volume_total_p = 0 ) THEN
            qt_volume_total_w := ( qt_volume_p * nr_etapas_p );
        END IF;
    END IF;
    RETURN qt_volume_total_w;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_vol_tot_val_enteral_md ( ie_ref_calculo_p text, qt_volume_p bigint, nr_etapas_p bigint, qt_volume_total_p bigint ) FROM PUBLIC;

