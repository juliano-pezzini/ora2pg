-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_desc_regra_param (ie_regra_p text) RETURNS varchar AS $body$
DECLARE


ds_regra_w	varchar(255);


BEGIN

if (ie_regra_p = 'N') then
	ds_regra_w	:= wheb_mensagem_pck.get_texto(301069);
elsif (ie_regra_p = 'S') then
	ds_regra_w	:= wheb_mensagem_pck.get_texto(301070);
elsif (ie_regra_p = 'U') then
	ds_regra_w	:= wheb_mensagem_pck.get_texto(301071);
elsif (ie_regra_p = 'P') then
	ds_regra_w	:= wheb_mensagem_pck.get_texto(301072);
elsif (ie_regra_p = 'E') then
	ds_regra_w	:= wheb_mensagem_pck.get_texto(301073);
elsif (ie_regra_p = 'O') then
	ds_regra_w	:= wheb_mensagem_pck.get_texto(301074);
elsif (ie_regra_p = 'A') then
	ds_regra_w	:= wheb_mensagem_pck.get_texto(301075);
end if;

return ds_regra_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_desc_regra_param (ie_regra_p text) FROM PUBLIC;

