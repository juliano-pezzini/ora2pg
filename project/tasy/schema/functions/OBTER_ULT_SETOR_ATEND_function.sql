-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_ult_setor_atend (nr_atendimento_p bigint) RETURNS varchar AS $body$
DECLARE


nr_seq_interno_w	bigint;
cd_setor_atendimento_w	integer;
ds_setor_atendimento_w	varchar(100);


BEGIN
ds_setor_atendimento_w := '';

if (coalesce(nr_atendimento_p,0) > 0) then

	select	coalesce(max(nr_seq_interno),0)
	into STRICT	nr_seq_interno_w
	from 	atend_paciente_unidade a
	where	a.nr_atendimento 	= nr_atendimento_p
	and 	dt_entrada_unidade	=  (SELECT max(dt_entrada_unidade)
		                           from atend_paciente_unidade b
		                           where nr_atendimento = nr_atendimento_p
					   and	 (dt_saida_unidade IS NOT NULL AND dt_saida_unidade::text <> ''));

	if (nr_seq_interno_w > 0) then
		select 	coalesce(max(cd_setor_atendimento),0)
		into STRICT	cd_setor_atendimento_w
		from 	atend_paciente_unidade
		where 	nr_seq_interno = nr_seq_interno_w;

		if (cd_setor_atendimento_w > 0) then

			select	substr(max(ds_setor_atendimento),1,100)
			into STRICT	ds_setor_atendimento_w
			from	setor_atendimento
			where	cd_setor_atendimento	= cd_setor_atendimento_w;
		end if;
	end if;
end if;

return	ds_setor_atendimento_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_ult_setor_atend (nr_atendimento_p bigint) FROM PUBLIC;

