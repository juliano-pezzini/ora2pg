-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION cpoe_shoppingcart_regra_cid ( nr_atendimento_p atendimento_paciente.nr_atendimento%type, cd_estabelecimento_p estabelecimento.cd_estabelecimento%type, cd_perfil_p perfil.cd_perfil%type, cd_material_p material.cd_material%type) RETURNS varchar AS $body$
DECLARE


ie_consiste_cid_w       varchar(1);
ie_permite_justificar_w varchar(1);
ds_doenca_cid_w         varchar(4000);
si_form_validation_p    varchar(1);
cd_pessoa_fisica_w  atendimento_paciente.cd_pessoa_fisica%type;


BEGIN

select  max(cd_pessoa_fisica)
into STRICT    cd_pessoa_fisica_w
from    atendimento_paciente
where   nr_atendimento = nr_atendimento_p;

SELECT * FROM cpoe_obter_se_consiste_diag(nr_atendimento_p, cd_estabelecimento_p, cd_perfil_p, cd_material_p, wheb_usuario_pck.get_nm_usuario, ie_consiste_cid_w, ie_permite_justificar_w, ds_doenca_cid_w, cd_pessoa_fisica_w, si_form_validation_p) INTO STRICT ie_consiste_cid_w, ie_permite_justificar_w, ds_doenca_cid_w, si_form_validation_p;

return coalesce(ie_consiste_cid_w, 'N');

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION cpoe_shoppingcart_regra_cid ( nr_atendimento_p atendimento_paciente.nr_atendimento%type, cd_estabelecimento_p estabelecimento.cd_estabelecimento%type, cd_perfil_p perfil.cd_perfil%type, cd_material_p material.cd_material%type) FROM PUBLIC;

