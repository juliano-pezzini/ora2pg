-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION psa_is_configured_datasource () RETURNS varchar AS $body$
DECLARE

  v_id_datasource   datasource.id%TYPE;
  v_nm_datasource   datasource.nm_datasource%TYPE;

BEGIN
  v_nm_datasource := obter_valor_param_usuario(0, 231, 0, null, 0);
  BEGIN
    SELECT
      id
    INTO STRICT
      v_id_datasource
    FROM
      datasource
    WHERE
        id_application = psa_is_configured
      AND
        nm_datasource = v_nm_datasource;

    RETURN v_id_datasource;
  EXCEPTION
    WHEN no_data_found THEN
      RETURN NULL;
  END;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION psa_is_configured_datasource () FROM PUBLIC;

