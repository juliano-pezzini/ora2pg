-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_nr_exame_sigla ( cd_exame_p text, cd_estabelecimento_p bigint) RETURNS bigint AS $body$
DECLARE


nr_seq_exame_w	bigint := null;

c01 CURSOR FOR
	SELECT	nr_seq_exame
	from	exame_laboratorio
	where	upper(cd_exame) = cd_exame_p
	and	lab_obter_se_exame_lib(nr_seq_exame, cd_estabelecimento_p) = 'S'
	and	ie_solicitacao = 'S';


BEGIN
if (cd_exame_p IS NOT NULL AND cd_exame_p::text <> '') and (cd_estabelecimento_p IS NOT NULL AND cd_estabelecimento_p::text <> '') then
	begin
	open c01;
	loop
	fetch c01 into nr_seq_exame_w;
	EXIT WHEN NOT FOUND; /* apply on c01 */
		begin
		if (nr_seq_exame_w IS NOT NULL AND nr_seq_exame_w::text <> '') then
			begin
			exit;
			end;
		end if;
		end;
	end loop;
	close c01;
	end;
end if;

return nr_seq_exame_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_nr_exame_sigla ( cd_exame_p text, cd_estabelecimento_p bigint) FROM PUBLIC;

