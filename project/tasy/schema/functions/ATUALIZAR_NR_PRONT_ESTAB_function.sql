-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION atualizar_nr_pront_estab (cd_pessoa_fisica_p text) RETURNS bigint AS $body$
DECLARE


nr_same_w	bigint;

BEGIN

if (coalesce(cd_pessoa_fisica_p,0) > 0) then

	select	max(nr_sequencia)
	into STRICT	nr_same_w
	from	prontuario_estab
	where	cd_pessoa_fisica = cd_pessoa_fisica_p;

end if;

return	nr_same_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION atualizar_nr_pront_estab (cd_pessoa_fisica_p text) FROM PUBLIC;

