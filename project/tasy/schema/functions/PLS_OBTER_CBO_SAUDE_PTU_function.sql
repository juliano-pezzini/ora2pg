-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_cbo_saude_ptu ( nr_seq_cbo_saude_p bigint, nr_versao_ptu_p text, ie_tipo_transacao_p text, cd_interface_p bigint) RETURNS bigint AS $body$
DECLARE


/* +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Finalidade:
-------------------------------------------------------------------------------------------------------------------
Locais de chamada direta:
[ ]  Objetos do dicionário [ ] Tasy (Delphi/Java) [  ] Portal [  ]  Relatórios [ ] Outros:
 ------------------------------------------------------------------------------------------------------------------
Pontos de atenção:
	NO PARAMETRO "NR_VERSAO_PTU_P" INFORMAR APENAS:
		040	041	050	060
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ */
cd_cbo_ptu_w		bigint;
nr_versao_ptu_w		varchar(10);


BEGIN
nr_versao_ptu_w := coalesce( nr_versao_ptu_p, ptu_obter_versao_dominio( ie_tipo_transacao_p, cd_interface_p));

if (nr_seq_cbo_saude_p IS NOT NULL AND nr_seq_cbo_saude_p::text <> '') then
	if (nr_versao_ptu_w IS NOT NULL AND nr_versao_ptu_w::text <> '') then
		select	max(cd_cbo_ptu)
		into STRICT	cd_cbo_ptu_w
		from	cbo_saude_ptu
		where	nr_seq_cbo_saude	= nr_seq_cbo_saude_p
		and	nr_versao_ptu		= nr_versao_ptu_w;
	end if;

	if (coalesce(cd_cbo_ptu_w::text, '') = '') then
		select	max(cd_cbo_ptu)
		into STRICT	cd_cbo_ptu_w
		from	cbo_saude
		where	nr_sequencia	= nr_seq_cbo_saude_p;
	end if;
end if;

return	cd_cbo_ptu_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_cbo_saude_ptu ( nr_seq_cbo_saude_p bigint, nr_versao_ptu_p text, ie_tipo_transacao_p text, cd_interface_p bigint) FROM PUBLIC;

