-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_proced_mat_esp_sus ( cd_material_p bigint) RETURNS bigint AS $body$
DECLARE


nr_sequencia_w		bigint;
cd_procedimento_w	bigint;


BEGIN

select	max(nr_sequencia)
into STRICT	nr_sequencia_w
from	sus_material_opm
where	cd_material		= cd_material_p
and	ie_origem_proced	= 7;

if (nr_sequencia_w > 0) then

	select	cd_procedimento
	into STRICT	cd_procedimento_w
	from	sus_material_opm
	where	nr_sequencia	= nr_sequencia_w;

end if;

return cd_procedimento_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_proced_mat_esp_sus ( cd_material_p bigint) FROM PUBLIC;

