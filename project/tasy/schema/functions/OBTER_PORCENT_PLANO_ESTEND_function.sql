-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_porcent_plano_estend (nr_atendimento_p bigint, cd_perfil_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) RETURNS bigint AS $body$
DECLARE

 
pr_estendido_w		bigint;
qt_total_w		bigint;
qt_estendidos_w		bigint;
					

BEGIN 
 
if (nr_atendimento_p IS NOT NULL AND nr_atendimento_p::text <> '') then 
 
	qt_total_w	:= Obter_quant_itens_plano(null,nr_atendimento_p,'T',nm_usuario_p,cd_perfil_p,cd_estabelecimento_p);
	qt_estendidos_w	:= Obter_quant_itens_plano(null,nr_atendimento_p,'E',nm_usuario_p,cd_perfil_p,cd_estabelecimento_p);
 
	pr_estendido_w	:= trunc(dividir((qt_estendidos_w * 100),qt_total_w));
	 
end if;
 
return	pr_estendido_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_porcent_plano_estend (nr_atendimento_p bigint, cd_perfil_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) FROM PUBLIC;

