-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_proc_int_lib_estab ( nr_seq_proc_interno_p bigint, cd_estabelecimento_p bigint) RETURNS varchar AS $body$
DECLARE



qt_registro_w	integer;
ds_retorno_w	varchar(1) := 'N';


BEGIN

begin
select	count(*)
into STRICT	qt_registro_w
from	proc_interno_estab
where	nr_seq_proc_interno	=	nr_seq_proc_interno_p;
exception
	when others then
		qt_registro_w	:= 0;
end;

if (qt_registro_w = 0) then
	ds_retorno_w	:= 'S';
else
	begin
	select	count(*)
	into STRICT	qt_registro_w
	from	proc_interno_estab
	where	nr_seq_proc_interno	=	nr_seq_proc_interno_p
	and	cd_estabelecimento	=	cd_estabelecimento_p;
	exception
		when others then
			qt_registro_w	:= 0;
	end;

	if (qt_registro_w > 0) then
		ds_retorno_w	:= 'S';
	end if;
end if;

return ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_proc_int_lib_estab ( nr_seq_proc_interno_p bigint, cd_estabelecimento_p bigint) FROM PUBLIC;

