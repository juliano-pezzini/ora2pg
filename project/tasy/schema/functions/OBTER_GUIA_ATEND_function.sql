-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_guia_atend ( nr_atendimento_p bigint, cd_convenio_p bigint, cd_categoria_p text) RETURNS varchar AS $body$
DECLARE

nr_guia_w	varchar(20);
ds_retorno_w	varchar(20) := '';


BEGIN
select	coalesce(max(nr_doc_convenio),'')
into STRICT	nr_guia_w
from	atend_categoria_convenio
where	nr_atendimento		= nr_atendimento_p
and	cd_convenio		= cd_convenio_p
and	cd_categoria		= cd_categoria_p
and	dt_inicio_vigencia	= (	SELECT	max(dt_inicio_vigencia)
					from	atend_categoria_convenio
					where	nr_atendimento		= nr_atendimento_p
					and	cd_convenio		= cd_convenio_p
					and	cd_categoria		= cd_categoria_p);
if (nr_guia_w IS NOT NULL AND nr_guia_w::text <> '') then
	ds_retorno_w := nr_guia_w;
end if;

return ds_retorno_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_guia_atend ( nr_atendimento_p bigint, cd_convenio_p bigint, cd_categoria_p text) FROM PUBLIC;

