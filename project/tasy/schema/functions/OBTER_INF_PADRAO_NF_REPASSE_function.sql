-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_inf_padrao_nf_repasse ( cd_estabelecimento_p bigint, ie_retorno_p text) RETURNS varchar AS $body$
DECLARE



/*ie_retorno_p
SE	Serie
PR	Procedimento
OR	Origem procedimento
MA	Material
LO	Local estoque
OP	Operacao nota
CC	Centro custo
CP	Condição pagamento*/
ds_retorno_w				varchar(255);
cd_serie_nf_w				regra_campos_nf_repasse.cd_serie_nf%type;
cd_procedimento_w			regra_campos_nf_repasse.cd_procedimento%type;
ie_origem_proced_w			regra_campos_nf_repasse.ie_origem_proced%type;
cd_material_w				regra_campos_nf_repasse.cd_material%type;
cd_local_estoque_w			regra_campos_nf_repasse.cd_local_estoque%type;
cd_operacao_nf_w				regra_campos_nf_repasse.cd_operacao_nf%type;
cd_centro_custo_w				regra_campos_nf_repasse.cd_centro_custo%type;
cd_condicao_pagamento_w			regra_campos_nf_repasse.cd_condicao_pagamento%type;

c01 CURSOR FOR
SELECT	cd_serie_nf,
	cd_procedimento,
	ie_origem_proced,
	cd_material,
	cd_local_estoque,
	cd_operacao_nf,
	cd_centro_custo,
	cd_condicao_pagamento
from	regra_campos_nf_repasse
where	cd_estabelecimento = cd_estabelecimento_p;


BEGIN

open C01;
loop
fetch C01 into
	cd_serie_nf_w,
	cd_procedimento_w,
	ie_origem_proced_w,
	cd_material_w,
	cd_local_estoque_w,
	cd_operacao_nf_w,
	cd_centro_custo_w,
	cd_condicao_pagamento_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin
	cd_serie_nf_w		:= cd_serie_nf_w;
	cd_procedimento_w	:= cd_procedimento_w;
	ie_origem_proced_w	:= ie_origem_proced_w;
	cd_material_w		:= cd_material_w;
	cd_local_estoque_w	:= cd_local_estoque_w;
	cd_operacao_nf_w		:= cd_operacao_nf_w;
	cd_centro_custo_w		:= cd_centro_custo_w;
	cd_condicao_pagamento_w	:= cd_condicao_pagamento_w;
	end;
end loop;
close C01;

if (ie_retorno_p = 'SE') then
	ds_retorno_w := cd_serie_nf_w;
elsif (ie_retorno_p = 'PR') then
	ds_retorno_w := cd_procedimento_w;
elsif (ie_retorno_p = 'OR') then
	ds_retorno_w := ie_origem_proced_w;
elsif (ie_retorno_p = 'MA') then
	ds_retorno_w := cd_material_w;
elsif (ie_retorno_p = 'LO') then
	ds_retorno_w := cd_local_estoque_w;
elsif (ie_retorno_p = 'OP') then
	ds_retorno_w := cd_operacao_nf_w;
elsif (ie_retorno_p = 'CC') then
	ds_retorno_w := cd_centro_custo_w;
elsif (ie_retorno_p = 'CP') then
	ds_retorno_w := cd_condicao_pagamento_w;
end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_inf_padrao_nf_repasse ( cd_estabelecimento_p bigint, ie_retorno_p text) FROM PUBLIC;

