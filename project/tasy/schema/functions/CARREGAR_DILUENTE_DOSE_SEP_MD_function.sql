-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION carregar_diluente_dose_sep_md (qt_dose_mat_str_p bigint, nr_casas_diluicao_p bigint, ds_unidade_separar_p text, ds_aux_diluir_p text, ds_additional_item_p text, ds_dose_diluicao_p text, ds_material_p text) RETURNS varchar AS $body$
DECLARE


  ds_retorno_w varchar(5000);

BEGIN

  ds_retorno_w := wheb_mensagem_pck.get_texto(307089) || ' ' ||
                  round(qt_dose_mat_str_p, nr_casas_diluicao_p) || ' ' ||
                  ds_unidade_separar_p || ds_aux_diluir_p ||
                  ds_additional_item_p || ' ' ||
                  wheb_mensagem_pck.get_texto(307087) || ' ' ||
                  ds_dose_diluicao_p || ' ' ||
                  wheb_mensagem_pck.get_texto(307090) || ds_material_p || chr(10);

  return ds_retorno_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION carregar_diluente_dose_sep_md (qt_dose_mat_str_p bigint, nr_casas_diluicao_p bigint, ds_unidade_separar_p text, ds_aux_diluir_p text, ds_additional_item_p text, ds_dose_diluicao_p text, ds_material_p text) FROM PUBLIC;

