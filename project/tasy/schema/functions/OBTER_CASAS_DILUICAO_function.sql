-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_casas_diluicao (cd_material_p bigint, cd_setor_p bigint) RETURNS bigint AS $body$
DECLARE


nr_casas_diluicao_w	bigint;


BEGIN

select	max(nr_casas_diluicao)
into STRICT	nr_casas_diluicao_w
from	rep_arredonda_diluicao
where	coalesce(cd_material, cd_material_p) = cd_material_p
and	coalesce(cd_setor_atendimento, coalesce(cd_setor_p,0)) = coalesce(cd_setor_p,0);

return	nr_casas_diluicao_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_casas_diluicao (cd_material_p bigint, cd_setor_p bigint) FROM PUBLIC;

