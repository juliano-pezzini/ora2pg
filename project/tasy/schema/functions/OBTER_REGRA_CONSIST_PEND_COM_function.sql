-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_regra_consist_pend_com ( cd_estabelecimento_p bigint, nr_seq_forma_compra_p bigint) RETURNS bigint AS $body$
DECLARE


nr_seq_forma_compra_w		regra_consist_pend_compra.nr_seq_forma_compra%type;
nr_seq_regra_w			regra_consist_pend_compra.nr_sequencia%type;

c01 CURSOR FOR
SELECT	nr_sequencia
from 	regra_consist_pend_compra
where	(((nr_seq_forma_compra_w = 0) and (coalesce(nr_seq_forma_compra::text, '') = '')) or
     	((nr_seq_forma_compra_w > 0) and ((nr_seq_forma_compra = nr_seq_forma_compra_w) or (coalesce(nr_seq_forma_compra::text, '') = ''))))
order by
	coalesce(nr_seq_forma_compra,999999);


BEGIN

nr_seq_forma_compra_w		:= coalesce(nr_seq_forma_compra_p,0);

open C01;
loop
fetch C01 into
	nr_seq_regra_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin
	nr_seq_regra_w := nr_seq_regra_w;
	exit;
	end;
end loop;
close C01;

return	coalesce(nr_seq_regra_w,0);

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_regra_consist_pend_com ( cd_estabelecimento_p bigint, nr_seq_forma_compra_p bigint) FROM PUBLIC;

