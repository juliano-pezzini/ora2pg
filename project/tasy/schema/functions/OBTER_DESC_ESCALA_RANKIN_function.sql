-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_desc_escala_rankin (qt_pontuacao_p bigint) RETURNS varchar AS $body$
DECLARE

ds_escala_rankin_w	 varchar(100);

BEGIN
		if (qt_pontuacao_p = 0) then
		ds_escala_rankin_w := wheb_mensagem_pck.get_texto(308848); -- Sem sintomas
	 elsif (qt_pontuacao_p = 1) then
	 	ds_escala_rankin_w := wheb_mensagem_pck.get_texto(307931); -- Independente
	 elsif ((qt_pontuacao_p = 2) or (qt_pontuacao_p = 3)) then
	 	ds_escala_rankin_w := wheb_mensagem_pck.get_texto(307932); -- Parcialmente dependente
	 elsif ((qt_pontuacao_p = 4) or (qt_pontuacao_p = 5) or (qt_pontuacao_p = 6)) then
	  	ds_escala_rankin_w := wheb_mensagem_pck.get_texto(307933); -- Totalmente dependente
	 else
	    	ds_escala_rankin_w := ' ';
	 end if;

return ds_escala_rankin_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_desc_escala_rankin (qt_pontuacao_p bigint) FROM PUBLIC;

