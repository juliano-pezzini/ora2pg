-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_acesso_rapido_web_boleto ( nr_seq_grupo_relat_p bigint) RETURNS bigint AS $body$
DECLARE


qt_relat_acesso_rapido_w	bigint;


BEGIN

select	count(1)
into STRICT	qt_relat_acesso_rapido_w
from   	pls_regra_relatorio a,
        pls_regra_relat_grupo b
where   a.nr_seq_grupo = b.nr_sequencia
and     b.nr_seq_grupo_relat = nr_seq_grupo_relat_p;

return	qt_relat_acesso_rapido_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_acesso_rapido_web_boleto ( nr_seq_grupo_relat_p bigint) FROM PUBLIC;

