-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_contrato_vencido (nr_seq_contrato bigint) RETURNS varchar AS $body$
DECLARE


qt_registros_w		integer;


BEGIN

select	count(*)
into STRICT	qt_registros_w
from	contrato
where	nr_sequencia = nr_seq_contrato
and	clock_timestamp() > coalesce(dt_fim,clock_timestamp());

if (qt_registros_w > 0) then
	return	'S'; /* Vencido */
else
	return	'N'; /* Não vencido */
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_contrato_vencido (nr_seq_contrato bigint) FROM PUBLIC;

