-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_nome_cooperado (nr_seq_cooperado_p bigint) RETURNS varchar AS $body$
DECLARE

								 
ds_retorno_w			varchar(4000)	:= '';
cd_pessoa_fisica_w		pls_cooperado.cd_pessoa_fisica%type;
cd_cgc_w			pls_cooperado.cd_cgc%type;


BEGIN 
 
select	max(cd_pessoa_fisica), 
	max(cd_cgc) 
into STRICT	cd_pessoa_fisica_w, 
	cd_cgc_w 
from	pls_cooperado 
where	nr_sequencia 	= nr_seq_cooperado_p;
 
if (cd_pessoa_fisica_w IS NOT NULL AND cd_pessoa_fisica_w::text <> '') or (cd_cgc_w IS NOT NULL AND cd_cgc_w::text <> '') then 
	ds_retorno_w 		:= obter_nome_pf_pj(cd_pessoa_fisica_w, cd_cgc_w);
end if;
 
return	ds_retorno_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_nome_cooperado (nr_seq_cooperado_p bigint) FROM PUBLIC;

