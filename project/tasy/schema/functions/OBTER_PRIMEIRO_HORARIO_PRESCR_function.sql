-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';



--
-- dblink wrapper to call function obter_primeiro_horario_prescr as an autonomous transaction
--
CREATE EXTENSION IF NOT EXISTS dblink;

CREATE OR REPLACE FUNCTION obter_primeiro_horario_prescr (nr_prescricao_p bigint) RETURNS varchar AS $body$
DECLARE
	-- Change this to reflect the dblink connection string
	v_conn_str  text := format('port=%s dbname=%s user=%s', current_setting('port'), current_database(), current_user);
	v_query     text;

	v_ret	varchar;
BEGIN
	v_query := 'SELECT * FROM obter_primeiro_horario_prescr_atx ( ' || quote_nullable(nr_prescricao_p) || ' )';
	SELECT * INTO v_ret FROM dblink(v_conn_str, v_query) AS p (ret varchar);
	RETURN v_ret;

END;
$body$ LANGUAGE plpgsql SECURITY DEFINER;




CREATE OR REPLACE FUNCTION obter_primeiro_horario_prescr_atx (nr_prescricao_p bigint) RETURNS varchar AS $body$
DECLARE


ds_prim_horario_w		varchar(255);
BEGIN
select	max(to_char(dt_primeiro_horario,'hh24:mi'))
into STRICT	ds_prim_horario_w
from	prescr_medica
where	nr_prescricao	= nr_prescricao_p;

RETURN ds_prim_horario_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_primeiro_horario_prescr (nr_prescricao_p bigint) FROM PUBLIC; -- REVOKE ALL ON FUNCTION obter_primeiro_horario_prescr_atx (nr_prescricao_p bigint) FROM PUBLIC;

