-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION tev_obter_dados_cad (nm_campo_p text, ie_opcao_p text) RETURNS varchar AS $body$
DECLARE


nr_seq_cad_w	bigint;
ds_contraind_w	varchar(255);


BEGIN
if ((trim(both nm_campo_p) IS NOT NULL AND (trim(both nm_campo_p))::text <> '')) then
	if (nm_campo_p = 'IE_HIPERSENSIBILIDADE_HEPARINA') then
		select	max(nr_sequencia),
			max(coalesce(ds_contraindicacao_estab,ds_contraindicacao))
		into STRICT	nr_seq_cad_w,
			ds_contraind_w
		from	tev_contraindicacao
		where	nr_sequencia = 12
		and	coalesce(ie_exibir_estab,ie_exibir) = 'S'
		and	(ie_tipo_contraindicacao_estab IS NOT NULL AND ie_tipo_contraindicacao_estab::text <> '');
	elsif (nm_campo_p = 'IE_PLAQUETOPENIA_HEPARINA') then
		select	max(nr_sequencia),
			max(coalesce(ds_contraindicacao_estab,ds_contraindicacao))
		into STRICT	nr_seq_cad_w,
			ds_contraind_w
		from	tev_contraindicacao
		where	nr_sequencia = 13
		and	coalesce(ie_exibir_estab,ie_exibir) = 'S'
		and	(ie_tipo_contraindicacao_estab IS NOT NULL AND ie_tipo_contraindicacao_estab::text <> '');
	elsif (nm_campo_p = 'IE_SANGRAMENTO_ULCERA') then
		select	max(nr_sequencia),
			max(coalesce(ds_contraindicacao_estab,ds_contraindicacao))
		into STRICT	nr_seq_cad_w,
			ds_contraind_w
		from	tev_contraindicacao
		where	nr_sequencia = 14
		and	coalesce(ie_exibir_estab,ie_exibir) = 'S'
		and	(ie_tipo_contraindicacao_estab IS NOT NULL AND ie_tipo_contraindicacao_estab::text <> '');
	elsif (nm_campo_p = 'IE_ULCERA_ATIVA') then
		select	max(nr_sequencia),
			max(coalesce(ds_contraindicacao_estab,ds_contraindicacao))
		into STRICT	nr_seq_cad_w,
			ds_contraind_w
		from	tev_contraindicacao
		where	nr_sequencia = 15
		and	coalesce(ie_exibir_estab,ie_exibir) = 'S'
		and	(ie_tipo_contraindicacao_estab IS NOT NULL AND ie_tipo_contraindicacao_estab::text <> '');
	elsif (nm_campo_p = 'IE_CIRURGIA_INTRACRANIANA') then
		select	max(nr_sequencia),
			max(coalesce(ds_contraindicacao_estab,ds_contraindicacao))
		into STRICT	nr_seq_cad_w,
			ds_contraind_w
		from	tev_contraindicacao
		where	nr_sequencia = 16
		and	coalesce(ie_exibir_estab,ie_exibir) = 'S'
		and	(ie_tipo_contraindicacao_estab IS NOT NULL AND ie_tipo_contraindicacao_estab::text <> '');
	elsif (nm_campo_p = 'IE_COAGULOPATIA') then
		select	max(nr_sequencia),
			max(coalesce(ds_contraindicacao_estab,ds_contraindicacao))
		into STRICT	nr_seq_cad_w,
			ds_contraind_w
		from	tev_contraindicacao
		where	nr_sequencia = 17
		and	coalesce(ie_exibir_estab,ie_exibir) = 'S'
		and	(ie_tipo_contraindicacao_estab IS NOT NULL AND ie_tipo_contraindicacao_estab::text <> '');
	elsif (nm_campo_p = 'IE_PUNCAO_LIQUORICA') then
		select	max(nr_sequencia),
			max(coalesce(ds_contraindicacao_estab,ds_contraindicacao))
		into STRICT	nr_seq_cad_w,
			ds_contraind_w
		from	tev_contraindicacao
		where	nr_sequencia = 18
		and	coalesce(ie_exibir_estab,ie_exibir) = 'S'
		and	(ie_tipo_contraindicacao_estab IS NOT NULL AND ie_tipo_contraindicacao_estab::text <> '');
	elsif (nm_campo_p = 'IE_HIPERTENSAO') then
		select	max(nr_sequencia),
			max(coalesce(ds_contraindicacao_estab,ds_contraindicacao))
		into STRICT	nr_seq_cad_w,
			ds_contraind_w
		from	tev_contraindicacao
		where	nr_sequencia = 19
		and	coalesce(ie_exibir_estab,ie_exibir) = 'S'
		and	(ie_tipo_contraindicacao_estab IS NOT NULL AND ie_tipo_contraindicacao_estab::text <> '');
	elsif (nm_campo_p = 'IE_INSUF_RENAL') then
		select	max(nr_sequencia),
			max(coalesce(ds_contraindicacao_estab,ds_contraindicacao))
		into STRICT	nr_seq_cad_w,
			ds_contraind_w
		from	tev_contraindicacao
		where	nr_sequencia = 20
		and	coalesce(ie_exibir_estab,ie_exibir) = 'S'
		and	(ie_tipo_contraindicacao_estab IS NOT NULL AND ie_tipo_contraindicacao_estab::text <> '');
	elsif (nm_campo_p = 'IE_CONT_POLITRAUMA') then
		select	max(nr_sequencia),
			max(coalesce(ds_contraindicacao_estab,ds_contraindicacao))
		into STRICT	nr_seq_cad_w,
			ds_contraind_w
		from	tev_contraindicacao
		where	nr_sequencia = 24
		and	coalesce(ie_exibir_estab,ie_exibir) = 'S'
		and	(ie_tipo_contraindicacao_estab IS NOT NULL AND ie_tipo_contraindicacao_estab::text <> '');
	elsif (nm_campo_p = 'IE_TRAUMA_CRANIANO') then
		select	max(nr_sequencia),
			max(coalesce(ds_contraindicacao_estab,ds_contraindicacao))
		into STRICT	nr_seq_cad_w,
			ds_contraind_w
		from	tev_contraindicacao
		where	nr_sequencia = 25
		and	coalesce(ie_exibir_estab,ie_exibir) = 'S'
		and	(ie_tipo_contraindicacao_estab IS NOT NULL AND ie_tipo_contraindicacao_estab::text <> '');
	elsif (nm_campo_p = 'IE_CI_INESP_1') then
		select	max(nr_sequencia),
			max(coalesce(ds_contraindicacao_estab,ds_contraindicacao))
		into STRICT	nr_seq_cad_w,
			ds_contraind_w
		from	tev_contraindicacao
		where	nr_sequencia = 27
		and	coalesce(ie_exibir_estab,ie_exibir) = 'S'
		and	(ie_tipo_contraindicacao_estab IS NOT NULL AND ie_tipo_contraindicacao_estab::text <> '');
	elsif (nm_campo_p = 'IE_CI_INESP_2') then
		select	max(nr_sequencia),
			max(coalesce(ds_contraindicacao_estab,ds_contraindicacao))
		into STRICT	nr_seq_cad_w,
			ds_contraind_w
		from	tev_contraindicacao
		where	nr_sequencia = 28
		and	coalesce(ie_exibir_estab,ie_exibir) = 'S'
		and	(ie_tipo_contraindicacao_estab IS NOT NULL AND ie_tipo_contraindicacao_estab::text <> '');
	elsif (nm_campo_p = 'IE_CI_INESP_3') then
		select	max(nr_sequencia),
			max(coalesce(ds_contraindicacao_estab,ds_contraindicacao))
		into STRICT	nr_seq_cad_w,
			ds_contraind_w
		from	tev_contraindicacao
		where	nr_sequencia = 29
		and	coalesce(ie_exibir_estab,ie_exibir) = 'S'
		and	(ie_tipo_contraindicacao_estab IS NOT NULL AND ie_tipo_contraindicacao_estab::text <> '');
	end if;
end if;

if (ie_opcao_p = '1') then
	if (coalesce(nr_seq_cad_w,0) > 0) then
		return 'S';
	else
		return 'N';
	end if;
elsif (ie_opcao_p = '2') then
	return nr_seq_cad_w;
elsif (ie_opcao_p = '3') then
	return ds_contraind_w;
else
	return null;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION tev_obter_dados_cad (nm_campo_p text, ie_opcao_p text) FROM PUBLIC;

