-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_qt_peso_sv (nr_atendimento_p bigint) RETURNS bigint AS $body$
DECLARE

qt_peso_w		double precision;
nr_sequencia_w	bigint;


BEGIN

select  coalesce(min(nr_sequencia),0)
into STRICT	nr_sequencia_w
from	atendimento_sinal_vital
where	ie_situacao = 'A'
and 	(qt_peso IS NOT NULL AND qt_peso::text <> '')
and	nr_atendimento = nr_atendimento_p;

if (nr_sequencia_w > 0)	then
	select	max(round((qt_peso)::numeric,2))
	into STRICT	qt_peso_w
	from	atendimento_sinal_vital
	where	nr_sequencia = nr_sequencia_w;
end if;


return	qt_peso_w;


end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_qt_peso_sv (nr_atendimento_p bigint) FROM PUBLIC;

