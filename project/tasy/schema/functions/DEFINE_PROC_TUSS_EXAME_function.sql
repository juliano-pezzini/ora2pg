-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION define_proc_tuss_exame (nr_seq_exame_p bigint, cd_convenio_p bigint, cd_categoria_p text, nr_seq_material_p bigint, cd_edicao_amb_p bigint) RETURNS bigint AS $body$
DECLARE


ds_retorno_w		bigint;
cd_procedimento_tuss_w	bigint;
qt_regra_w		bigint;
ie_origem_proc_tuss_w	bigint;

C01 CURSOR FOR
	SELECT	cd_procedimento
	from	exame_lab_conv_tuss
	where	ie_situacao = 'A'
	and 	nr_seq_exame = nr_seq_exame_p
	and	((cd_convenio = cd_convenio_p) OR (coalesce(cd_convenio::text, '') = ''))
	and	((cd_categoria = coalesce(cd_categoria_p,cd_categoria)) OR (coalesce(cd_categoria::text, '') = ''))
	and	coalesce(nr_seq_material, coalesce(nr_seq_material_p,0)) = coalesce(nr_seq_material_p,0)
	AND	((cd_edicao_amb = coalesce(cd_edicao_amb_p,cd_edicao_amb)) OR (coalesce(cd_edicao_amb::text, '') = ''))
	order by coalesce(cd_convenio,0), coalesce(nr_seq_material,0);


BEGIN

ds_retorno_w:= null;

select 	coalesce(max(cd_procedimento_tuss),0),
	coalesce(max(ie_origem_proc_tuss),0)
into STRICT	cd_procedimento_tuss_w,
	ie_origem_proc_tuss_w
from 	exame_laboratorio
where 	nr_seq_exame = nr_seq_exame_p;

if (ie_origem_proc_tuss_w = 8) then
	ds_retorno_w := cd_procedimento_tuss_w;
end if;

select 	count(*)
into STRICT	qt_regra_w
from 	exame_lab_conv_tuss
where	nr_seq_exame = nr_seq_exame_p;

if (qt_regra_w > 0) then

	OPEN C01;
	LOOP
	FETCH C01 into
		cd_procedimento_tuss_w;
	EXIT WHEN NOT FOUND; /* apply on c01 */
		cd_procedimento_tuss_w := cd_procedimento_tuss_w;
	END LOOP;
	CLOSE C01;

	ds_retorno_w := cd_procedimento_tuss_w;

end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION define_proc_tuss_exame (nr_seq_exame_p bigint, cd_convenio_p bigint, cd_categoria_p text, nr_seq_material_p bigint, cd_edicao_amb_p bigint) FROM PUBLIC;

