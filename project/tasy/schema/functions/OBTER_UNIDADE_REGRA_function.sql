-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';



--
-- dblink wrapper to call function obter_unidade_regra as an autonomous transaction
--
CREATE EXTENSION IF NOT EXISTS dblink;

CREATE OR REPLACE FUNCTION obter_unidade_regra (nr_seq_interno_p bigint) RETURNS varchar AS $body$
DECLARE
	-- Change this to reflect the dblink connection string
	v_conn_str  text := format('port=%s dbname=%s user=%s', current_setting('port'), current_database(), current_user);
	v_query     text;

	v_ret	varchar;
BEGIN
	v_query := 'SELECT * FROM obter_unidade_regra_atx ( ' || quote_nullable(nr_seq_interno_p) || ' )';
	SELECT * INTO v_ret FROM dblink(v_conn_str, v_query) AS p (ret varchar);
	RETURN v_ret;

END;
$body$ LANGUAGE plpgsql SECURITY DEFINER;




CREATE OR REPLACE FUNCTION obter_unidade_regra_atx (nr_seq_interno_p bigint) RETURNS varchar AS $body$
DECLARE


cd_unidade_w	varchar(255);
BEGIN

select	cd_unidade_basica || ' ' || cd_unidade_compl
into STRICT	cd_unidade_w
from 	unidade_atendimento
where 	nr_seq_interno = nr_seq_interno_p;

return	cd_unidade_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_unidade_regra (nr_seq_interno_p bigint) FROM PUBLIC; -- REVOKE ALL ON FUNCTION obter_unidade_regra_atx (nr_seq_interno_p bigint) FROM PUBLIC;

