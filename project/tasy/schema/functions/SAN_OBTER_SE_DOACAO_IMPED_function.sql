-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION san_obter_se_doacao_imped ( nr_seq_doacao_p bigint, ie_codigo_descricao_p text) RETURNS varchar AS $body$
DECLARE


ds_resultado_w			varchar(2000) := '';
ds_imp_w				varchar(2000);

c01 CURSOR FOR

	SELECT CASE WHEN upper(substr(ie_codigo_descricao_p,1,1))='C' THEN to_char(a.nr_seq_impedimento) || ','  ELSE b.ds_impedimento || chr(13) || chr(10) END
	from	san_impedimento b,
		san_questionario a
	where 	a.nr_seq_impedimento 	= b.nr_sequencia
	and 	a.nr_seq_doacao 	= nr_seq_doacao_p
	and     coalesce(b.ie_impede_doacao,'N') = 'S'
	and	coalesce(b.ie_resposta_correta,'N') <> a.ie_resposta
	order by 1;


BEGIN

open c01;
loop
fetch c01 into ds_imp_w;
EXIT WHEN NOT FOUND; /* apply on c01 */

ds_resultado_w := ds_resultado_w || ds_imp_w;

end loop;
close c01;

return substr(ds_resultado_w,1,length(ds_resultado_w) - 1);
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION san_obter_se_doacao_imped ( nr_seq_doacao_p bigint, ie_codigo_descricao_p text) FROM PUBLIC;

