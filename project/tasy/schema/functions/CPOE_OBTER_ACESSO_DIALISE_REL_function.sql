-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION cpoe_obter_acesso_dialise_rel ( ie_cateter_p text, nr_seq_diametro_agulha_p bigint, nr_seq_tipo_agulha_p bigint, nr_seq_tipo_agulha_art_p bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w		varchar(4000);

	function ie_cateter return text is
	;
BEGIN
		return obter_desc_expressao(299373, null) || ': '|| obter_valor_dominio(5466,ie_cateter_p) ||' ';
	end;

	function nr_seq_diametro_agulha return varchar2 is
	begin
		return ' ' || obter_desc_expressao(286471, null) || ': '|| obter_desc_diametro_agulha(nr_seq_diametro_agulha_p) ||' ';
	end;

	function nr_seq_tipo_agulha return varchar2 is
	begin
		return ' ' || obter_desc_expressao(701790, null) || ': '|| obter_desc_tipo_agulha(nr_seq_tipo_agulha_p) ||'	';
	end;

	function nr_seq_tipo_agulha_art return varchar2 is
	begin
		return ' ' || obter_desc_expressao(701794, null) || ': '|| obter_desc_tipo_agulha(nr_seq_tipo_agulha_art_p) ||'	';
	end;

begin

	ds_retorno_w := ie_cateter();
	--Inserido espaços pois futuramente relatório wate que usa isto se tornará um cate onde será melhorado isso.
	if (coalesce(nr_seq_diametro_agulha_p,0) <> 0) then
		ds_retorno_w := ds_retorno_w || '													' || nr_seq_diametro_agulha();
	end if;
		if (coalesce(nr_seq_tipo_agulha_p,0) <> 0) then
		ds_retorno_w := ds_retorno_w || '													' || nr_seq_tipo_agulha();
	end if;
		if (coalesce(nr_seq_tipo_agulha_art_p,0) <> 0) then
		ds_retorno_w := ds_retorno_w || '													' || nr_seq_tipo_agulha_art();
	end if;

	return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION cpoe_obter_acesso_dialise_rel ( ie_cateter_p text, nr_seq_diametro_agulha_p bigint, nr_seq_tipo_agulha_p bigint, nr_seq_tipo_agulha_art_p bigint) FROM PUBLIC;

