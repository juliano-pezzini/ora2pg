-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION tre_controle_inscrito (cd_pessoa_fisica_p text, nr_seq_mod_evento_p bigint, nr_seq_evento_p bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w			varchar(1);


BEGIN
if (coalesce(nr_seq_mod_evento_p::text, '') = '') then
	select	CASE WHEN count(*)=0 THEN 'S'  ELSE 'N' END
	into STRICT	ds_retorno_w
	from   	tre_inscrito
	where (cd_pessoa_fisica = cd_pessoa_fisica_p)
	and		coalesce(nr_seq_mod_evento::text, '') = ''
	and		nr_seq_evento = nr_seq_evento_p;
else
	select	CASE WHEN count(*)=0 THEN 'S'  ELSE 'N' END
	into STRICT	ds_retorno_w
	from   	tre_inscrito
	where (cd_pessoa_fisica = cd_pessoa_fisica_p)
	and		nr_seq_mod_evento = nr_seq_mod_evento_p
	and		nr_seq_evento = nr_seq_evento_p;
end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION tre_controle_inscrito (cd_pessoa_fisica_p text, nr_seq_mod_evento_p bigint, nr_seq_evento_p bigint) FROM PUBLIC;

