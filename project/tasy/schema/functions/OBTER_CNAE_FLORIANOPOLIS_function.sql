-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_cnae_florianopolis (cd_estabelecimento_p bigint, nr_seq_nota_p text) RETURNS varchar AS $body$
DECLARE


nr_seq_cnae_w	bigint;
id_cnae_w	varchar(255);
retorno_w	varchar(255);


BEGIN

select	max(nr_seq_cnae)
into STRICT	nr_seq_cnae_w
from	nota_fiscal_item n
where	n.nr_sequencia = nr_seq_nota_p;

if (nr_seq_cnae_w > 0) then

	select	id_cnae
	into STRICT	id_cnae_w
	from	pls_cnae
	where	nr_Sequencia = nr_seq_cnae_w;

end if;

retorno_w:= id_cnae_w;

return  retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_cnae_florianopolis (cd_estabelecimento_p bigint, nr_seq_nota_p text) FROM PUBLIC;

