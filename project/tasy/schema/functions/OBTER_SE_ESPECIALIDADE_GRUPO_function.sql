-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_especialidade_grupo ( cd_especialidade_p bigint, cd_perfil_p bigint, nr_seq_grupo_p bigint, cd_estabelecimento_p bigint default 0) RETURNS varchar AS $body$
DECLARE


ie_liberado_w	varchar(10) := 'S';


BEGIN

select	coalesce(max('N'), 'S')
into STRICT	ie_liberado_w
from	lib_grupo_medic_rotina where		nr_seq_grupo	= nr_seq_grupo_p LIMIT 1;

if (ie_liberado_w = 'N') then
	select	coalesce(max('S'),'N')
	into STRICT	ie_liberado_w
	from	lib_grupo_medic_rotina where		nr_seq_grupo			= nr_seq_grupo_p
	and		coalesce(cd_especialidade,coalesce(cd_especialidade_p,0))	= coalesce(cd_especialidade_p,0)
	and		coalesce(cd_estabelecimento, cd_estabelecimento_p)	= cd_estabelecimento_p
	and		coalesce(cd_perfil,cd_perfil_p)	= cd_perfil_p LIMIT 1;
end if;

return ie_liberado_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_especialidade_grupo ( cd_especialidade_p bigint, cd_perfil_p bigint, nr_seq_grupo_p bigint, cd_estabelecimento_p bigint default 0) FROM PUBLIC;

