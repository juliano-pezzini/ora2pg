-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_valor_atrib_ca_exame_js ( nr_seq_exame_p bigint, nm_atributo_p text) RETURNS varchar AS $body$
DECLARE



vl_atributo_retorno_w			varchar(255) := '';

BEGIN

if ('IE_RESULTADO' = upper(nm_atributo_p)) then
	begin

	select	max(ie_tipo_resultado)
	into STRICT	vl_atributo_retorno_w
	from	ca_exame
	where	nr_sequencia	= nr_seq_exame_p;

	end;
elsif ('CD_TIPO_RESULTADO' = upper(nm_atributo_p)) then
	begin

	select	max(cd_tipo_resultado)
	into STRICT	vl_atributo_retorno_w
	from	ca_exame
	where	nr_sequencia	= nr_seq_exame_p;

	end;
end if;

return	vl_atributo_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_valor_atrib_ca_exame_js ( nr_seq_exame_p bigint, nm_atributo_p text) FROM PUBLIC;

