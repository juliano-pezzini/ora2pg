-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_prescr_medico (nr_prescricao_p bigint) RETURNS varchar AS $body$
DECLARE


ie_medico_w		varchar(01)	:= 'N';
nm_usuario_w		varchar(15);
cd_pessoa_fisica_w	varchar(10);


BEGIN

select	nm_usuario_original
into STRICT	nm_usuario_w
from	prescr_medica
where	nr_prescricao	= nr_prescricao_p;

select	max(cd_pessoa_fisica)
into STRICT	cd_pessoa_fisica_w
from 	usuario
where	nm_usuario	= nm_usuario_w;

if (cd_pessoa_fisica_w IS NOT NULL AND cd_pessoa_fisica_w::text <> '') then
	begin

	select	coalesce(max('S'),'N')
	into STRICT	ie_medico_w
	from	medico
	where	cd_pessoa_fisica	= cd_pessoa_fisica_w;

	end;
end if;

return	ie_medico_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_prescr_medico (nr_prescricao_p bigint) FROM PUBLIC;

