-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION ageint_obter_obs_proc_conv (cd_convenio_p bigint, nr_seq_proc_interno_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint, cd_funcao_p bigint, cd_categoria_p text, cd_plano_p text, cd_pessoa_fisica_p text) RETURNS varchar AS $body$
DECLARE


ds_orientacao_w	varchar(4000) := '';
ds_orient_convenio_w	varchar(2000) := '';
ds_orientacao_usuario_w	varchar(2000) := '';
ie_orientacao_w		varchar(01);
ie_tipo_orientacao_w	varchar(01);
qt_idade_w		integer;


BEGIN

ie_tipo_orientacao_w := Obter_Param_Usuario(820, 67, obter_perfil_ativo, nm_usuario_p, cd_estabelecimento_p, ie_tipo_orientacao_w);

if (coalesce(nr_seq_proc_interno_p,0) > 0) then
	select	max(ie_orientacao),
		max(ds_orientacao_pac),
		max(ds_orientacao_usuario)
	into STRICT	ie_orientacao_w,
		ds_orientacao_w,
		ds_orientacao_usuario_w
	from	proc_interno
	where	nr_sequencia = nr_seq_proc_interno_p;

	qt_idade_w	:= obter_idade_pf(cd_pessoa_fisica_p, clock_timestamp(),'A');

	select	max(ds_orientacao)
	into STRICT	ds_orient_convenio_w
	from	proc_interno_conv_orient
	where	nr_seq_proc_interno = nr_seq_proc_interno_p
	and	((cd_convenio = cd_convenio_p) or (coalesce(cd_convenio::text, '') = ''))
	and	coalesce(cd_categoria, cd_categoria_p) 	= cd_categoria_p
	and	coalesce(cd_plano, cd_plano_p)		= cd_plano_p
	and	((((qt_idade_w >= qt_idade_min) or (coalesce(qt_idade_min::text, '') = ''))
	and	((qt_idade_w <= qt_idade_max) or (coalesce(qt_idade_max::text, '') = '')))
	or (coalesce(cd_pessoa_fisica_p::text, '') = ''));

	--ation_error(-20011,cd_funcao_w || '#@#@');
	if (ie_orientacao_w = 'C') then
		ds_orientacao_w := ds_orient_convenio_w;
	elsif (ie_orientacao_w = 'A') and ((ie_tipo_orientacao_w = 'P') or (cd_funcao_p <> 820)) then
		ds_orientacao_w := ds_orientacao_w || chr(13) || chr(10) || ds_orient_convenio_w;
	elsif (ie_orientacao_w = 'A') and (ie_tipo_orientacao_w = 'U') and (cd_funcao_p = 820) then
		ds_orientacao_w := ds_orientacao_usuario_w || chr(13) || chr(10) || ds_orient_convenio_w;
	elsif (ie_tipo_orientacao_w = 'U') and (cd_funcao_p = 820) then
		ds_orientacao_w := ds_orientacao_usuario_w;
	end if;
end if;

return ds_orientacao_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION ageint_obter_obs_proc_conv (cd_convenio_p bigint, nr_seq_proc_interno_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint, cd_funcao_p bigint, cd_categoria_p text, cd_plano_p text, cd_pessoa_fisica_p text) FROM PUBLIC;

