-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION ageint_perm_marcar_agendado ( cd_procedimento_p bigint, nr_seq_proc_interno_p bigint, ie_origem_proced_p bigint) RETURNS varchar AS $body$
DECLARE


cd_area_proc_w		bigint;
cd_especialidade_w	bigint;
cd_grupo_proc_w		bigint;
nr_seq_regra_w		bigint;
ds_retorno_w		varchar(1)	:= 'N';

C01 CURSOR FOR
	SELECT	nr_sequencia
	from	ageint_exame_perm_agendado
	where	coalesce(nr_seq_proc_interno, nr_seq_proc_interno_p)	= nr_seq_proc_interno_p
	and	coalesce(cd_area_procedimento, cd_area_proc_w)	= cd_area_proc_w
	and	coalesce(cd_especialidade, cd_especialidade_w)	= cd_especialidade_w
	and	coalesce(cd_grupo_proc, cd_grupo_proc_w)		= cd_grupo_proc_w
	and	coalesce(ie_situacao, 'A') = 'A'
	order by
		coalesce(nr_seq_proc_interno,0),
		coalesce(cd_area_procedimento,0),
		coalesce(cd_especialidade,0),
		coalesce(cd_grupo_proc,0);


BEGIN
select	max(cd_area_procedimento),
	max(cd_especialidade),
	max(cd_grupo_proc)
into STRICT	cd_area_proc_w,
	cd_especialidade_w,
	cd_grupo_proc_w
from	estrutura_procedimento_v
where	cd_procedimento	= cd_procedimento_p
and	ie_origem_proced = ie_origem_proced_p;

open C01;
loop
fetch C01 into
	nr_seq_regra_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin
	nr_seq_regra_w	:= nr_seq_regra_w;
	end;
end loop;
close C01;

if (nr_seq_regra_w IS NOT NULL AND nr_seq_regra_w::text <> '') then
	ds_retorno_w	:= 'S';
end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION ageint_perm_marcar_agendado ( cd_procedimento_p bigint, nr_seq_proc_interno_p bigint, ie_origem_proced_p bigint) FROM PUBLIC;

