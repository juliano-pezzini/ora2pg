-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_local_gera_lote ( cd_local_estoque_p bigint) RETURNS varchar AS $body$
DECLARE


ie_gera_lote_w	varchar(1);


BEGIN

select	coalesce(max(ie_gera_lote),'S')
into STRICT	ie_gera_lote_w
from	local_estoque
where	ie_situacao = 'A'
and	cd_local_estoque = cd_local_estoque_p;

return	ie_gera_lote_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_local_gera_lote ( cd_local_estoque_p bigint) FROM PUBLIC;

