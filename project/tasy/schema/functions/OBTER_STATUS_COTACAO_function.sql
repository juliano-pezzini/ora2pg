-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_status_cotacao ( cd_status_p bigint, ie_contrato_p text, ie_exclusivo_p text) RETURNS varchar AS $body$
DECLARE


ie_status_cotacao_w	agenda_pac_opme.ie_status_cotacao%type;


BEGIN

if (coalesce(cd_status_p, 0) <> 30) then
	ie_status_cotacao_w := 'C';
elsif (coalesce(ie_contrato_p, 'N') = 'S') and (coalesce(cd_status_p, 0) = 30) then
	ie_status_cotacao_w := 'A';
elsif (coalesce(ie_contrato_p, 'N') = 'N') and (coalesce(cd_status_p, 0) = 30) and (coalesce(ie_exclusivo_p, 'N') = 'S') then
	ie_status_cotacao_w := 'A';
elsif (coalesce(ie_contrato_p, 'N') = 'N') and (coalesce(cd_status_p, 0) = 30) and (coalesce(ie_exclusivo_p, 'N') = 'N') then
	ie_status_cotacao_w := 'P';
end if;

return ie_status_cotacao_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_status_cotacao ( cd_status_p bigint, ie_contrato_p text, ie_exclusivo_p text) FROM PUBLIC;

