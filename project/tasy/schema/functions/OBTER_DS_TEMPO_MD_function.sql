-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_ds_tempo_md (qt_min_aplicacao_p bigint, qt_hora_aplicacao_p bigint) RETURNS varchar AS $body$
DECLARE


  ds_tempo_w varchar(100);


BEGIN
  if (qt_min_aplicacao_p IS NOT NULL AND qt_min_aplicacao_p::text <> '') and (qt_hora_aplicacao_p IS NOT NULL AND qt_hora_aplicacao_p::text <> '') then
    ds_tempo_w := ' ' || Wheb_mensagem_pck.get_texto(307087) || ' '
                  || to_char(qt_hora_aplicacao_p) || ' ' ||
                  Wheb_mensagem_pck.get_texto(309411) || ' ' || 
                  Wheb_mensagem_pck.get_texto(309413) || ' ' ||
                  to_char(qt_min_aplicacao_p) || ' ' ||
                  lower(Wheb_mensagem_pck.get_texto(309401));
  elsif (coalesce(qt_min_aplicacao_p::text, '') = '') and (qt_hora_aplicacao_p IS NOT NULL AND qt_hora_aplicacao_p::text <> '') then
    ds_tempo_w := ' ' || Wheb_mensagem_pck.get_texto(307087) || ' ' 
                  || to_char(qt_hora_aplicacao_p) || ' ' ||
                  Wheb_mensagem_pck.get_texto(309411);
  elsif (qt_min_aplicacao_p IS NOT NULL AND qt_min_aplicacao_p::text <> '') and (coalesce(qt_hora_aplicacao_p::text, '') = '') then
    ds_tempo_w := ' ' || Wheb_mensagem_pck.get_texto(307087) || ' ' 
                  || to_char(qt_min_aplicacao_p) || ' ' ||
                  Wheb_mensagem_pck.get_texto(309408) || '.';
  end if;
  return ds_tempo_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_ds_tempo_md (qt_min_aplicacao_p bigint, qt_hora_aplicacao_p bigint) FROM PUBLIC;

