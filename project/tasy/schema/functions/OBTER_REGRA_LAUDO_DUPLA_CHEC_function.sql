-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_regra_laudo_dupla_chec ( cd_estabelecimento_p bigint, cd_perfil_p bigint) RETURNS bigint AS $body$
DECLARE


nr_seq_regra_dupla_chec_w	bigint;
nr_seq_retorno_w		bigint := 0;

C01 CURSOR FOR
	SELECT  nr_sequencia
	from    regra_laudo_dupla_chec
	where   cd_estabelecimento = coalesce(cd_estabelecimento_p,1)
	and	coalesce(cd_perfil,coalesce(cd_perfil_p,0)) = coalesce(cd_perfil_p,0)
	order   by coalesce(cd_perfil, 0);


BEGIN

open C01;
loop
fetch C01 into
	nr_seq_regra_dupla_chec_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin
	nr_seq_retorno_w := nr_seq_regra_dupla_chec_w;
	end;
end loop;
close C01;


return	nr_seq_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_regra_laudo_dupla_chec ( cd_estabelecimento_p bigint, cd_perfil_p bigint) FROM PUBLIC;

