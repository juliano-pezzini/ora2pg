-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_agenda_int_opme ( cd_agenda_p bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w		varchar(1) := 'N';
cd_evento_log_w		varchar(15);

c01 CURSOR FOR
	SELECT	cd_evento_log
	from	agenda_pac_int_opme_log
	where	nr_seq_agenda = cd_agenda_p
	and	cd_evento_log in ('2','3')
	order by dt_atualizacao,
		cd_evento_log;


BEGIN

open c01;
loop
fetch c01 into
	cd_evento_log_w;
EXIT WHEN NOT FOUND; /* apply on c01 */
	begin
	cd_evento_log_w := cd_evento_log_w;
	end;
end loop;
close c01;

if (cd_evento_log_w = '2') then
	ds_retorno_w := 'S';
end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_agenda_int_opme ( cd_agenda_p bigint) FROM PUBLIC;

