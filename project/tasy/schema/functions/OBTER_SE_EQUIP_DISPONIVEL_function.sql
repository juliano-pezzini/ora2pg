-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_equip_disponivel ( nr_seq_agenda_p bigint, nr_seq_proc_interno_p bigint, cd_medico_p text, qt_tempo_p bigint) RETURNS varchar AS $body$
DECLARE


ie_disponivel_w		varchar(1) := 'S';
qt_tempo_total_w	bigint;

c01 CURSOR FOR
	SELECT	obter_disp_equip_agenda_cirur(nr_seq_agenda_p, coalesce(nr_seq_classif_equip,0), qt_tempo_total_w)
	from	proc_interno_equip
	where	nr_seq_proc_interno	=	nr_seq_proc_interno_p;


BEGIN

select	coalesce(qt_tempo_p,obter_tempo_total_proced(nr_seq_proc_interno_p, cd_medico_p))
into STRICT	qt_tempo_total_w
;

OPEN C01;
LOOP
FETCH C01 into
	ie_disponivel_w;
EXIT WHEN NOT FOUND; /* apply on c01 */
	begin
	if (ie_disponivel_w = 'N') then
		exit;
	end if;

	end;
END LOOP;
CLOSE C01;

return ie_disponivel_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_equip_disponivel ( nr_seq_agenda_p bigint, nr_seq_proc_interno_p bigint, cd_medico_p text, qt_tempo_p bigint) FROM PUBLIC;

