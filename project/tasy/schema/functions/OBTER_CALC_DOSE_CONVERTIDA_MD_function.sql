-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_calc_dose_convertida_md (qt_dose_origem_p bigint, cd_unidade_medida_origem_p text, cd_unidade_medida_destino_p text, cd_unidade_medida_consumo_p text, qt_conversao_p bigint, qt_reconversao_p bigint default null) RETURNS bigint AS $body$
DECLARE


  qt_dose_retorno_w bigint;


BEGIN
  if (cd_unidade_medida_consumo_p = cd_unidade_medida_destino_p) then
    qt_dose_retorno_w:=  dividir_sem_round_md(qt_dose_origem_p, qt_conversao_p);

  elsif (cd_unidade_medida_consumo_p = cd_unidade_medida_origem_p) then
    qt_dose_retorno_w:= (coalesce(qt_dose_origem_p, 0) * coalesce(qt_conversao_p, 0));
  else
    qt_dose_retorno_w:= dividir_sem_round_md(qt_dose_origem_p, qt_conversao_p);
    qt_dose_retorno_w:= (coalesce(qt_dose_retorno_w, 0) * coalesce(qt_reconversao_p, 0));
  end if;

  return qt_dose_retorno_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_calc_dose_convertida_md (qt_dose_origem_p bigint, cd_unidade_medida_origem_p text, cd_unidade_medida_destino_p text, cd_unidade_medida_consumo_p text, qt_conversao_p bigint, qt_reconversao_p bigint default null) FROM PUBLIC;

