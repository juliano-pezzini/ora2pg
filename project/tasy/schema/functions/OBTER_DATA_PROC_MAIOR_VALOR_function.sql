-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_data_proc_maior_valor (nr_interno_conta_p bigint) RETURNS timestamp AS $body$
DECLARE


dt_procedimento_w	timestamp;

c01 CURSOR FOR
	SELECT	a.dt_procedimento
	from	procedimento_paciente a,
		procedimento b
	where	a.cd_procedimento = b.cd_procedimento
	and	a.ie_origem_proced = b.ie_origem_proced
	and	a.nr_interno_conta = nr_interno_conta_p
	and	coalesce(a.cd_motivo_exc_conta::text, '') = ''
	and	(a.nr_seq_proc_interno IS NOT NULL AND a.nr_seq_proc_interno::text <> '')
	and	b.ie_classificacao = 1
	order by	vl_procedimento asc;


BEGIN

dt_procedimento_w := null;

open c01;
loop
fetch c01 into
	dt_procedimento_w;
EXIT WHEN NOT FOUND; /* apply on c01 */
	begin
	dt_procedimento_w := dt_procedimento_w;
	end;
end loop;
close c01;

return	dt_procedimento_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_data_proc_maior_valor (nr_interno_conta_p bigint) FROM PUBLIC;

