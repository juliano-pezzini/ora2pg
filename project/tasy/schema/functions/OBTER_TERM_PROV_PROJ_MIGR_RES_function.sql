-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_term_prov_proj_migr_res ( nr_seq_projeto_p bigint, dt_resumo_p timestamp) RETURNS timestamp AS $body$
DECLARE


nr_seq_resumo_w		bigint;
dt_provavel_term_w	timestamp;


BEGIN
if (nr_seq_projeto_p IS NOT NULL AND nr_seq_projeto_p::text <> '') and (dt_resumo_p IS NOT NULL AND dt_resumo_p::text <> '') then
	begin
	select	coalesce(max(nr_sequencia),0)
	into STRICT	nr_seq_resumo_w
	from	w_resumo_migracao
	where	nr_seq_projeto = nr_seq_projeto_p
	and	trunc(dt_resumo,'dd') = trunc(dt_resumo_p,'dd');

	if (nr_seq_resumo_w > 0) then
		begin
		select	max(dt_provavel_termino)
		into STRICT	dt_provavel_term_w
		from	w_resumo_migracao
		where	nr_sequencia = nr_seq_resumo_w;
		end;
	end if;
	end;
end if;
return dt_provavel_term_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_term_prov_proj_migr_res ( nr_seq_projeto_p bigint, dt_resumo_p timestamp) FROM PUBLIC;

