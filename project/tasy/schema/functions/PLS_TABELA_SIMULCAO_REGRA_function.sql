-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_tabela_simulcao_regra ( nr_seq_plano_p bigint) RETURNS bigint AS $body$
DECLARE


nr_seq_tabela_w		bigint;


BEGIN

select	max(nr_seq_tabela)
into STRICT	nr_seq_tabela_w
from	pls_regra_simulador_web
where	nr_seq_plano	= nr_seq_plano_p
and	ie_tabela_padrao = 'S'
and	clock_timestamp() between dt_inicio_vigencia and coalesce(dt_fim_vigencia,clock_timestamp());

if (coalesce(nr_seq_tabela_w::text, '') = '') then
	select	max(nr_seq_tabela)
	into STRICT	nr_seq_tabela_w
	from	pls_regra_simulador_web
	where	nr_seq_plano	= nr_seq_plano_p
	and	clock_timestamp() between dt_inicio_vigencia and coalesce(dt_fim_vigencia,clock_timestamp());
end if;

return	nr_seq_tabela_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_tabela_simulcao_regra ( nr_seq_plano_p bigint) FROM PUBLIC;

