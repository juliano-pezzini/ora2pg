-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION fa_obter_se_entrega_pend (cd_pessoa_fisica_p text, nr_sequencia_p bigint) RETURNS varchar AS $body$
DECLARE


ie_retorno_w	varchar(1);


BEGIN

if (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') then

	select  CASE WHEN count(*)=0 THEN 'N'  ELSE 'S' END
	into STRICT	ie_retorno_w
	from    FA_entrega_medicacao a
	where	a.cd_pessoa_fisica = cd_pessoa_fisica_p
	and		a.nr_sequencia <> nr_sequencia_p
	and		a.ie_status_medicacao <> 'EM'
	and		coalesce(a.dt_cancelamento::text, '') = '';
end if;

return	ie_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION fa_obter_se_entrega_pend (cd_pessoa_fisica_p text, nr_sequencia_p bigint) FROM PUBLIC;

