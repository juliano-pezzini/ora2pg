-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_desc_repasse_pontuacao ( nr_sequencia_p bigint, ds_tipo_p text) RETURNS varchar AS $body$
DECLARE




ds_regra_pontuacao_w	regra_repasse_pontuacao.ds_regra_pontuacao%type;
vl_unitario_ponto_w	regra_repasse_pontuacao.vl_unitario_ponto%type;
ie_situacao_w		regra_repasse_pontuacao.ie_situacao%type;
dt_inicio_vigencia_w	regra_repasse_pontuacao.dt_inicio_vigencia%type;
dt_fim_vigencia_w		regra_repasse_pontuacao.dt_fim_vigencia%type;
ds_retorno_w		varchar(255);


BEGIN


select	ds_regra_pontuacao,
	vl_unitario_ponto ,
	ie_situacao,
	dt_inicio_vigencia,
	dt_fim_vigencia  
into STRICT	 
	ds_regra_pontuacao_w,
	vl_unitario_ponto_w,  
	ie_situacao_w,   
	dt_inicio_vigencia_w,  
	dt_fim_vigencia_w  
from	regra_repasse_pontuacao
where	nr_sequencia = nr_sequencia_p;

if (ds_tipo_p = 'DS') then
	ds_retorno_w := ds_regra_pontuacao_w;
elsif (ds_tipo_p = 'VL') then
	ds_retorno_w := vl_unitario_ponto_W;
elsif (ds_tipo_p = 'STATUS') then
	ds_retorno_w := ie_situacao_w;
elsif (ds_tipo_p = 'DT_INICIO') then
	ds_retorno_w := to_char(dt_inicio_vigencia_w, 'dd/mm/yyyy');
elsif (ds_tipo_p = 'DT_FIM') then
	ds_retorno_w :=  to_char(dt_fim_vigencia_w, 'dd/mm/yyyy');
end if;
	
return ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_desc_repasse_pontuacao ( nr_sequencia_p bigint, ds_tipo_p text) FROM PUBLIC;

