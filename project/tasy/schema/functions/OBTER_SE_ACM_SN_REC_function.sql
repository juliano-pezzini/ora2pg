-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_acm_sn_rec ( ds_horarios_p text, ie_se_necessario_p text) RETURNS varchar AS $body$
DECLARE


ie_acm_sn_w	varchar(1) := 'N';
ds_horarios_w	varchar(2000);


BEGIN
if (ds_horarios_p IS NOT NULL AND ds_horarios_p::text <> '') then
	begin
	ds_horarios_w	:= padroniza_horario(ds_horarios_p);
	if	((ds_horarios_w = '') or (coalesce(ds_horarios_w::text, '') = '')) then
		begin
		ie_acm_sn_w	:= 'S';
		end;
	end if;
	end;
end if;

if (ie_acm_sn_w		= 'N') and (ie_se_necessario_p	= 'S') then

	ie_acm_sn_w	:= 'S';

end if;

return ie_acm_sn_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_acm_sn_rec ( ds_horarios_p text, ie_se_necessario_p text) FROM PUBLIC;

