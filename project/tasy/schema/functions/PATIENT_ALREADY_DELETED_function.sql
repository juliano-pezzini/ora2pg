-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION patient_already_deleted ( cd_pessoa_fisica_p text) RETURNS varchar AS $body$
DECLARE


ie_deleted_w	varchar(1);


BEGIN

select	CASE WHEN count(1)=0 THEN 'N'  ELSE 'Y' END
into STRICT	ie_deleted_w
from	pessoa_fisica_deleted
where	cd_pessoa_fisica = cd_pessoa_fisica_p;

return	ie_deleted_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION patient_already_deleted ( cd_pessoa_fisica_p text) FROM PUBLIC;

