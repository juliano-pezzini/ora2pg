-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_obser_regra_onco ( nr_seq_Atendimento_p bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w         varchar(255) := '';
contador_w	bigint;

BEGIN

if (nr_seq_Atendimento_p IS NOT NULL AND nr_seq_Atendimento_p::text <> '')then

	select 	count(*)
	into STRICT 	contador_w
	from 	regra_observacao_oncologia;

	if (contador_w > 0) then
	   select	   max(a.ds_observacao)
	   into STRICT          ds_retorno_w
	   from  	   paciente_Setor b,
	           	   paciente_atendimento c,
		   regra_observacao_oncologia a
	   where	   b.cd_protocolo = a.cd_protocolo
	   and	   a.nr_seq_medicacao = b.nr_seq_medicacao
	   and	   c.nr_Seq_paciente = b.nr_seq_paciente
	   and	   nr_seq_atendimento = nr_seq_Atendimento_p
	   and	   a.nr_ciclo = c.nr_ciclo
	   and	   a.ds_dia_ciclo = c.ds_dia_ciclo;
	end if;
end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_obser_regra_onco ( nr_seq_Atendimento_p bigint) FROM PUBLIC;

