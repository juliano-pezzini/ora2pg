-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_lista_dados_classif_hsl (cd_pessoa_fisica_p text, ie_campo_retorno_p text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w		varchar(2000) := '';
ds_classif_w		varchar(80);
ds_observacao_w		varchar(255);

C01 CURSOR FOR
	SELECT	substr(obter_classif_pessoa(nr_seq_classif),1,80) ds_classif,
			substr(ds_observacao,1,255) ds_observacao
	from    pessoa_classif
	where cd_pessoa_fisica = cd_pessoa_fisica_p
	group by substr(obter_classif_pessoa(nr_seq_classif),1,80),
		substr(ds_observacao,1,255)
	order by 1;


BEGIN

open C01;
loop
fetch C01 into
	ds_classif_w,
	ds_observacao_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin

	if (ie_campo_retorno_p = 'O') then
		ds_retorno_w := ds_retorno_w || ', ' || ds_observacao_w;
	elsif (ie_campo_retorno_p = 'D') then
		ds_retorno_w := ds_retorno_w || ', ' || ds_classif_w;
	end if;

	end;
end loop;
close C01;

ds_retorno_w := substr(ds_retorno_w, 2, Length(ds_retorno_w) - 1);

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_lista_dados_classif_hsl (cd_pessoa_fisica_p text, ie_campo_retorno_p text) FROM PUBLIC;

