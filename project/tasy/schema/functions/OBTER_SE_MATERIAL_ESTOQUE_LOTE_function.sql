-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_material_estoque_lote ( cd_estabelecimento_p bigint, cd_material_p bigint) RETURNS varchar AS $body$
DECLARE


ie_estoque_lote_w		varchar(1);
cd_material_estoque_w	integer;


BEGIN
select	coalesce(max(cd_material_estoque),cd_material_p)
into STRICT	cd_material_estoque_w
from	material
where	cd_material = cd_material_p;

select	coalesce(max(ie_estoque_lote),'N')
into STRICT	ie_estoque_lote_w
from	material_estab
where	cd_material = cd_material_estoque_w
and	cd_estabelecimento = cd_estabelecimento_p;

RETURN ie_estoque_lote_w;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_material_estoque_lote ( cd_estabelecimento_p bigint, cd_material_p bigint) FROM PUBLIC;

