-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_cd_unidade_ibge ( cd_unidade_federacao_p unidade_federacao.cd_unidade_federacao%type, cd_unidade_ibge_p unidade_federacao.cd_unidade_ibge%type default null) RETURNS varchar AS $body$
DECLARE


cd_unidade_ibge_w	unidade_federacao.cd_unidade_ibge%type;
cd_unidade_federacao_w	unidade_federacao.cd_unidade_federacao%type;
	

BEGIN

if (cd_unidade_federacao_p IS NOT NULL AND cd_unidade_federacao_p::text <> '') then

	select	max(cd_unidade_ibge)
	into STRICT	cd_unidade_ibge_w
	from	unidade_federacao
	where	cd_unidade_federacao = cd_unidade_federacao_p;

	return	cd_unidade_ibge_w;
	
elsif (cd_unidade_ibge_p IS NOT NULL AND cd_unidade_ibge_p::text <> '') then
	select	max(cd_unidade_federacao)
	into STRICT	cd_unidade_federacao_w
	from	unidade_federacao
	where	cd_unidade_ibge = cd_unidade_ibge_p;
	
	return	cd_unidade_federacao_w;
else
	return null;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_cd_unidade_ibge ( cd_unidade_federacao_p unidade_federacao.cd_unidade_federacao%type, cd_unidade_ibge_p unidade_federacao.cd_unidade_ibge%type default null) FROM PUBLIC;

