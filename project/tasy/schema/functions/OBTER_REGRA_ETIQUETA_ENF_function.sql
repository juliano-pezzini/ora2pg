-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_regra_etiqueta_enf (cd_material_p bigint) RETURNS varchar AS $body$
DECLARE


ie_emite_w				varchar(10)	:= null;
cd_grupo_material_w		smallint;
cd_subgrupo_material_w		smallint;
cd_classe_material_w		integer;

c01 CURSOR FOR
SELECT 	ie_emite
from 		Regra_Etiqueta_enf
where 	coalesce(cd_material,cd_material_p)	   		= cd_material_p
  and		coalesce(cd_classe_material,cd_classe_material_w)	= cd_classe_material_w
  and		coalesce(cd_subgrupo_material,cd_subgrupo_material_w)= cd_subgrupo_material_w
  and		coalesce(cd_grupo_material,cd_grupo_material_w) 	= cd_grupo_material_w
order by
		coalesce(cd_material,0),
		coalesce(cd_classe_material,0),
		coalesce(cd_subgrupo_material,0),
		coalesce(cd_grupo_material,0);


BEGIN

select 	cd_grupo_material,
		cd_subgrupo_material,
		cd_classe_material
into STRICT		cd_grupo_material_w,
		cd_subgrupo_material_w,
		cd_classe_material_w
from 		estrutura_material_v
where 	cd_material		= cd_material_p;

OPEN C01;
LOOP
	FETCH C01 into
		ie_emite_w;
	EXIT WHEN NOT FOUND; /* apply on c01 */
		ie_emite_w		:= ie_emite_w;
END LOOP;
CLOSE C01;

if (coalesce(ie_emite_w::text, '') = '') then
	ie_emite_w	:= 'N';
end if;
RETURN ie_emite_w;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_regra_etiqueta_enf (cd_material_p bigint) FROM PUBLIC;

