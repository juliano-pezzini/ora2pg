-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_relatorio_exame_convenio (cd_convenio_p bigint) RETURNS bigint AS $body$
DECLARE


cd_relatorio_w		bigint;
cd_empresa_w			smallint;
cd_estabelecimento_w	smallint;


BEGIN

cd_estabelecimento_w	:= coalesce(wheb_usuario_pck.get_cd_estabelecimento,0);
cd_empresa_w			:= coalesce(OBTER_EMPRESA_ESTAB(cd_estabelecimento_w),0);

select	max(cd_relatorio)
into STRICT	cd_relatorio_w
from	convenio_regra_copia_pep
where	cd_convenio = cd_convenio_p
and		coalesce(cd_empresa,cd_empresa_w)  = cd_empresa_w
and		coalesce(cd_estabelecimento,cd_estabelecimento_w)	= cd_estabelecimento_w
and		coalesce(ie_regra,'C') = 'R'
and		coalesce(NR_SEQ_ITEM,385) = 385
order by coalesce(cd_empresa,0),
		 coalesce(cd_estabelecimento,0);

return cd_relatorio_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_relatorio_exame_convenio (cd_convenio_p bigint) FROM PUBLIC;

