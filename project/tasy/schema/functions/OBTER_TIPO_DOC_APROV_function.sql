-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_tipo_doc_aprov (CD_DESC_NODE bigint, NR_SEQ_ITEM_PRONT_P bigint) RETURNS varchar AS $body$
DECLARE


DS_TIPO_COLUNA_W varchar(50);
DS_TIPO_TEMPLATE varchar(50);



BEGIN
    SELECT
        obter_desc_exp_idioma(CD_DESC_NODE,wheb_usuario_pck.get_nr_seq_idioma,null)
    INTO STRICT
        DS_TIPO_COLUNA_W
;

    IF (CD_DESC_NODE = 307982) THEN
        IF (NR_SEQ_ITEM_PRONT_P = 5) THEN
            SELECT 
                obter_desc_exp_idioma(792013,wheb_usuario_pck.get_nr_seq_idioma,null)
            INTO STRICT
                DS_TIPO_TEMPLATE
;
            DS_TIPO_COLUNA_W := DS_TIPO_TEMPLATE || ' (' || DS_TIPO_COLUNA_W || ')';
        END IF;
        IF (NR_SEQ_ITEM_PRONT_P = 7) THEN
            SELECT
                obter_desc_exp_idioma(284113,wheb_usuario_pck.get_nr_seq_idioma,null)
            INTO STRICT
                DS_TIPO_TEMPLATE
;
            DS_TIPO_COLUNA_W := DS_TIPO_TEMPLATE || ' (' || DS_TIPO_COLUNA_W || ')';
        END IF;
    END IF;

    RETURN INITCAP(DS_TIPO_COLUNA_W);
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_tipo_doc_aprov (CD_DESC_NODE bigint, NR_SEQ_ITEM_PRONT_P bigint) FROM PUBLIC;

