-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION dt_inicio_higienizacao (nr_atendimento_p bigint, nr_unidade_p bigint, dt_alta_p timestamp) RETURNS timestamp AS $body$
DECLARE


dt_inicio_hig_w      	timestamp;


BEGIN

	if (coalesce(nr_atendimento_p::text, '') = '') then
		begin
			select coalesce(max(b.dt_inicio), clock_timestamp())
			into STRICT dt_inicio_hig_w
			from unidade_atendimento a,sl_unid_atend b
			where b.nr_seq_unidade = a.nr_seq_interno
				and a.nr_atendimento = nr_atendimento_p
				and b.nr_seq_unidade = nr_unidade_p
				and b.dt_saida_real = dt_alta_p;
		end;
	else
		dt_inicio_hig_w := clock_timestamp();
	end if;

	return dt_inicio_hig_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION dt_inicio_higienizacao (nr_atendimento_p bigint, nr_unidade_p bigint, dt_alta_p timestamp) FROM PUBLIC;

