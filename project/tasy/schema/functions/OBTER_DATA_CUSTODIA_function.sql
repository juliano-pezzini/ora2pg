-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_data_custodia (nr_seq_compl_p bigint, cd_pessoa_fisica_p text default null) RETURNS timestamp AS $body$
DECLARE


dt_revisao_w		compl_pessoa_fisica_cust.dt_revisao%type;
ds_retorno_w		timestamp;


BEGIN

if (coalesce(nr_seq_compl_p, 0) <> 0) then
    if (PKG_I18N.get_user_locale in ('de_DE', 'de_AT')) then
	select	max(dt_revisao)
        into STRICT	dt_revisao_w
        from    compl_pessoa_fisica_cust
        where   nr_sequencia = (SELECT	max(nr_sequencia)
                        from    compl_pessoa_fisica_cust
                        where 	nr_seq_compl = nr_seq_compl_p
                        and	cd_pessoa_fisica = cd_pessoa_fisica_p);	
    else
        select	max(dt_revisao)
        into STRICT	dt_revisao_w
        from    compl_pessoa_fisica_cust
        where   nr_sequencia = (SELECT	max(nr_sequencia)
                        from    compl_pessoa_fisica_cust
                        where 	nr_seq_compl = nr_seq_compl_p);
    end if;

	if (dt_revisao_w IS NOT NULL AND dt_revisao_w::text <> '')then
		ds_retorno_w := dt_revisao_w;
	end if;		

end if;

return ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_data_custodia (nr_seq_compl_p bigint, cd_pessoa_fisica_p text default null) FROM PUBLIC;

