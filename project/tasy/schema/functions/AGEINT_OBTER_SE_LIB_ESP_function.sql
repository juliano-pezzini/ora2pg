-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION ageint_obter_se_lib_esp (nr_seq_proc_interno_p bigint, nr_seq_ageint_p bigint, cd_agenda_p bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w		varchar(1)	:= 'N';
cd_agenda_w		bigint;
nr_seq_proc_interno_w	bigint;
qt_exame_w		bigint;

C01 CURSOR FOR
	SELECT	a.cd_agenda,
		a.nr_seq_proc_interno
	from	ageint_lib_exame_pri a,
		ageint_lib_exames b
	where	b.nr_seq_proc_interno	= nr_seq_proc_interno_p
	and	a.nr_sequencia		= b.nr_ageint_lib_exame
	and	a.cd_agenda		= cd_agenda_p
	and	ds_retorno_w	= 'N';


BEGIN

open C01;
loop
fetch C01 into
	cd_Agenda_w,
	nr_seq_proc_interno_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin
	if (cd_agenda_w IS NOT NULL AND cd_agenda_w::text <> '') and (nr_seq_proc_interno_w IS NOT NULL AND nr_seq_proc_interno_w::text <> '') then
		select	count(*)
		into STRICT	qt_exame_w
		from	agenda_integrada_item
		where	nr_seq_proc_interno	= nr_seq_proc_interno_w
		and	nr_seq_agenda_int	= nr_seq_ageint_p;

		if (qt_exame_w	> 0) then
			ds_retorno_w	:= 'S';
		end if;
	end if;
	end;
end loop;
close C01;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION ageint_obter_se_lib_esp (nr_seq_proc_interno_p bigint, nr_seq_ageint_p bigint, cd_agenda_p bigint) FROM PUBLIC;

