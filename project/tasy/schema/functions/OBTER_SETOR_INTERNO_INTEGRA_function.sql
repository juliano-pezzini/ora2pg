-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_setor_interno_integra ( cd_estabelecimento_p bigint, cd_setor_atendimento_p bigint, nr_seq_sistema_integ_p bigint) RETURNS varchar AS $body$
DECLARE


CD_INTEGRACAO_w		REGRA_SETOR_INTEGRACAO.CD_INTEGRACAO%type;


BEGIN

select	max(a.CD_INTEGRACAO)
into STRICT	CD_INTEGRACAO_w
from	REGRA_SETOR_INTEGRACAO a
where	coalesce(a.cd_estabelecimento,coalesce(cd_estabelecimento_p, 0)) = coalesce(cd_estabelecimento_p, 0)
and		coalesce(a.cd_setor_atendimento,coalesce(cd_setor_atendimento_p,0)) = coalesce(cd_setor_atendimento_p,0)
and		a.nr_seq_sistema_integ 	= nr_seq_sistema_integ_p
and		coalesce(a.ie_situacao,'A') = 'A';

return CD_INTEGRACAO_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_setor_interno_integra ( cd_estabelecimento_p bigint, cd_setor_atendimento_p bigint, nr_seq_sistema_integ_p bigint) FROM PUBLIC;

