-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_proc_autor_concatenado (nr_sequencia_autor_p bigint) RETURNS varchar AS $body$
DECLARE

ds_retorno_w		varchar(4000) := '';
cd_procedimento_w	bigint;
ds_procedimento_w	varchar(255);

C01 CURSOR FOR
	SELECT	p.cd_procedimento cd_proced,
		substr(obter_descricao_procedimento(p.cd_procedimento,p.ie_origem_proced),1,100) ds_proced
	from	procedimento_autorizado p
	where	p.nr_sequencia_autor = nr_sequencia_autor_p;


BEGIN

open C01;
loop
fetch C01 into
	cd_procedimento_w,
	ds_procedimento_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin
	if (coalesce(ds_retorno_w::text, '') = '') then
		ds_retorno_w := substr(ds_retorno_w || cd_procedimento_w || ' - ' || ds_procedimento_w,1,4000);
	else
		ds_retorno_w := substr(ds_retorno_w || chr(13)|| chr(10) || cd_procedimento_w || ' - ' || ds_procedimento_w,1,4000);
	end if;
	end;
end loop;
close C01;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_proc_autor_concatenado (nr_sequencia_autor_p bigint) FROM PUBLIC;

