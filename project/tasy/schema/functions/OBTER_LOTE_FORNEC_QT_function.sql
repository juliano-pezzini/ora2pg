-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_lote_fornec_qt ( nr_seq_ordem_p bigint, cd_material_p bigint) RETURNS bigint AS $body$
DECLARE


nr_seq_cabine_w		bigint;
nr_seq_lote_fornec_w		bigint;


BEGIN

select	b.nr_seq_cabine
into STRICT	nr_seq_cabine_w
from	far_etapa_producao b,
	can_ordem_prod a
where	a.nr_sequencia = nr_seq_ordem_p
and	b.nr_sequencia = a.nr_seq_etapa_prod;


select	coalesce(max(nr_seq_lote_fornec),0)
into STRICT	nr_seq_lote_fornec_w
from	far_estoque_cabine
where	nr_seq_cabine	= nr_seq_cabine_w
and	cd_material	= cd_material_p;

return	nr_seq_lote_fornec_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_lote_fornec_qt ( nr_seq_ordem_p bigint, cd_material_p bigint) FROM PUBLIC;

