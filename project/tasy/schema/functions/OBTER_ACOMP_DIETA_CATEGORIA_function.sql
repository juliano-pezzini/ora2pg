-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_acomp_dieta_categoria ( cd_convenio_p bigint, cd_categoria_p text, nr_seq_servico_p bigint) RETURNS bigint AS $body$
DECLARE


qt_dieta_acomp_w			smallint;
nr_seq_regra_acomp_w		bigint;
qt_regra_w			bigint;


BEGIN

select	coalesce(qt_dieta_acomp,0),
	coalesce(nr_seq_regra_acomp,0)
into STRICT	qt_dieta_acomp_w,
	nr_seq_regra_acomp_w
from	categoria_convenio
where	cd_convenio = cd_convenio_p
and	cd_categoria = cd_categoria_p;

if (nr_seq_regra_acomp_w > 0) then
	select	count(*)
	into STRICT	qt_regra_w
	from	nut_regra_cons_acomp_item
	where	nr_seq_regra_acomp = nr_seq_regra_acomp_w
	and	nr_seq_servico = nr_seq_servico_p;

	if (qt_regra_w = 0) then
		qt_dieta_Acomp_w := 0;
	end if;
end if;

return	qt_dieta_acomp_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_acomp_dieta_categoria ( cd_convenio_p bigint, cd_categoria_p text, nr_seq_servico_p bigint) FROM PUBLIC;

