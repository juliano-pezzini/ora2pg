-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_relatorio_proced ( nr_seq_proc_p bigint) RETURNS varchar AS $body$
DECLARE

 
ds_resultado_w 			varchar(255) := '';
cd_procedimento_w 		bigint;
ie_origem_procedimento_w 	bigint;
cd_setor_atendimento_w 		integer;


BEGIN 
if (nr_seq_proc_p IS NOT NULL AND nr_seq_proc_p::text <> '') then 
	begin 
	select 	cd_procedimento, 
		ie_origem_proced, 
		coalesce(cd_setor_atendimento, 0) 
	into STRICT	cd_procedimento_w, 
		ie_origem_procedimento_w, 
		cd_setor_atendimento_w 
	from 	procedimento_paciente 
	where 	nr_sequencia	= nr_seq_proc_p;
 
	ds_resultado_w	:= obter_relat_proc(cd_procedimento_w, ie_origem_procedimento_w, 28, cd_setor_atendimento_w);
	end;
end if;
return ds_resultado_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_relatorio_proced ( nr_seq_proc_p bigint) FROM PUBLIC;

