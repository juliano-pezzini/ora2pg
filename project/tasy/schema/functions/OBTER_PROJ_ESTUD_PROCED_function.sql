-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_proj_estud_proced ( nr_seq_proc_interno_p bigint) RETURNS bigint AS $body$
DECLARE


nr_seq_proj_estud_w     bigint;
cd_projeto_w            integer;


BEGIN

select  max(nr_sequencia)
into STRICT    nr_seq_proj_estud_w
from    projeto_estudo_proced
where   nr_seq_proc_interno	= nr_seq_proc_interno_p;

select  max(cd_projeto)
into STRICT    cd_projeto_w
from    projeto_estudo_proced
where   nr_sequencia        = nr_seq_proj_estud_w;

return  cd_projeto_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_proj_estud_proced ( nr_seq_proc_interno_p bigint) FROM PUBLIC;

