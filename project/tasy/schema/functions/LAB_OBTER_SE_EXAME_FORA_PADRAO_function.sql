-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION lab_obter_se_exame_fora_padrao (nr_seq_prescr_p bigint, nr_prescricao_p bigint) RETURNS bigint AS $body$
DECLARE


ds_retorno_w	bigint;


BEGIN

select	distinct
	1
into STRICT	ds_retorno_w
from	exame_lab_resultado b,
	exame_lab_result_item a
where	a.nr_seq_resultado	= b.nr_seq_resultado
and	((a.qt_resultado	< a.qt_minima) or (a.qt_resultado > a.qt_maxima) or (a.pr_resultado	< a.pr_minimo) or (a.pr_resultado > a.pr_maximo))
and	b.nr_prescricao		= nr_prescricao_p
and	a.nr_seq_prescr		= nr_seq_prescr_p;

return coalesce(ds_retorno_w,0);

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION lab_obter_se_exame_fora_padrao (nr_seq_prescr_p bigint, nr_prescricao_p bigint) FROM PUBLIC;

