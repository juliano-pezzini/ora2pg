-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_evento_regra ( nr_seq_evento_p bigint, cd_tipo_procedimento_p bigint) RETURNS bigint AS $body$
DECLARE


nr_seq_evento_conv_w bigint;

c01 CURSOR FOR
	SELECT a.nr_sequencia
	from	evento_convenio_regra b,
		evento_convenio a
	where a.nr_sequencia      = b.nr_seq_evento_conv
	   and a.nr_seq_evento      = nr_seq_evento_p
	   and coalesce(b.cd_tipo_procedimento, cd_tipo_procedimento_p)  = cd_tipo_procedimento_p
	   and	a.cd_estabelecimento = wheb_usuario_pck.get_cd_estabelecimento
	order by coalesce(b.cd_tipo_procedimento, 0);

BEGIN

open c01;
loop
	fetch c01 into nr_seq_evento_conv_w;
	EXIT WHEN NOT FOUND; /* apply on c01 */
end loop;
close c01;

return nr_seq_evento_conv_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_evento_regra ( nr_seq_evento_p bigint, cd_tipo_procedimento_p bigint) FROM PUBLIC;

