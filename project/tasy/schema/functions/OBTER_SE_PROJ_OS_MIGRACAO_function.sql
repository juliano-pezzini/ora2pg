-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_proj_os_migracao (nr_seq_ativ_proj_p bigint) RETURNS varchar AS $body$
DECLARE


ie_proj_migracao_w	varchar(1) := 'N';


BEGIN
if (nr_seq_ativ_proj_p IS NOT NULL AND nr_seq_ativ_proj_p::text <> '') then
	begin
	select	CASE WHEN count(*)=0 THEN 'N'  ELSE 'S' END
	into STRICT	ie_proj_migracao_w
	from	proj_projeto p,
		proj_cronograma c,
		proj_cron_etapa e
	where	p.nr_sequencia = c.nr_seq_proj
	and	c.nr_sequencia = e.nr_seq_cronograma
	and	e.nr_sequencia = nr_seq_ativ_proj_p
	and	p.nr_seq_classif = 14;
	end;
end if;
return ie_proj_migracao_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_proj_os_migracao (nr_seq_ativ_proj_p bigint) FROM PUBLIC;

