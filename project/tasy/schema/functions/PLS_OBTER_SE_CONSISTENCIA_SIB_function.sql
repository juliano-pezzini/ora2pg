-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_se_consistencia_sib (nr_seq_segurado_p bigint, nr_seq_lote_sib_p bigint) RETURNS varchar AS $body$
DECLARE


qt_consistencia_w integer;
ds_retorno_w  varchar(1);

BEGIN

select  count(*)
into STRICT qt_consistencia_w
from  pls_lote_consistencia_sib
where nr_seq_segurado = nr_seq_segurado_p
and nr_seq_lote_sib = nr_seq_lote_sib_p;

ds_retorno_w := 'N';
if (qt_consistencia_w > 0) then
 ds_retorno_w := 'S';
end if;


return ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_se_consistencia_sib (nr_seq_segurado_p bigint, nr_seq_lote_sib_p bigint) FROM PUBLIC;

