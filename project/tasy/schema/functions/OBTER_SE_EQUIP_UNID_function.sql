-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_equip_unid (nr_seq_unidade_w bigint, cd_equipamento_w bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(1);
qt_w		bigint;

BEGIN

select 	max(1)
into STRICT	qt_w
from	unidade_atendimento a,
	UNIDADE_ATEND_EQUIP b
where	a.cd_setor_atendimento = b.cd_setor_atendimento
and	a.cd_unidade_basica = b.cd_unidade_basica
and	a.cd_unidade_compl = b.cd_unidade_compl
and	b.cd_equipamento	= cd_equipamento_w
and	nr_seq_interno	= nr_seq_unidade_w  LIMIT 1;

ds_retorno_w	:= 'N';

if (qt_w = 1) then
	ds_retorno_w := 'S';
end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_equip_unid (nr_seq_unidade_w bigint, cd_equipamento_w bigint) FROM PUBLIC;

