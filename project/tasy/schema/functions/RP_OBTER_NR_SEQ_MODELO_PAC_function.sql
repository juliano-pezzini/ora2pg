-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION rp_obter_nr_seq_modelo_pac (nr_seq_pac_reab_p bigint) RETURNS varchar AS $body$
DECLARE


nr_seq_modelo_agendamento_w	bigint;
nr_seq_pac_modelo_w		bigint;
ds_retorno_w			varchar(80);


BEGIN

select	max(nr_sequencia)
into STRICT	nr_seq_pac_modelo_w
from	rp_pac_modelo_agendamento
where	nr_seq_pac_reab = nr_seq_pac_reab_p
and	coalesce(dt_fim_tratamento::text, '') = ''
and	coalesce(dt_inativacao::text, '') = '';

select	max(nr_seq_modelo_agendamento)
into STRICT	nr_seq_modelo_agendamento_w
from	rp_pac_modelo_agendamento
where	nr_sequencia = nr_seq_pac_modelo_w
and	coalesce(dt_fim_tratamento::text, '') = ''
and	coalesce(dt_inativacao::text, '') = '';

if (nr_seq_modelo_agendamento_w IS NOT NULL AND nr_seq_modelo_agendamento_w::text <> '') then
	ds_retorno_w := nr_seq_modelo_agendamento_w;
end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION rp_obter_nr_seq_modelo_pac (nr_seq_pac_reab_p bigint) FROM PUBLIC;

