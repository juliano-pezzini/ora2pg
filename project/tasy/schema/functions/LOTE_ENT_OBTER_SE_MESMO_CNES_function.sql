-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION lote_ent_obter_se_mesmo_cnes (nr_seq_instituicao_p bigint, cd_cns_inst_p text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(1);


BEGIN

if (cd_cns_inst_p IS NOT NULL AND cd_cns_inst_p::text <> '') then

	select	CASE WHEN count(*)=0 THEN 'N'  ELSE 'S' END
	into STRICT	ds_retorno_w
	from	lote_ent_instituicao
	where	CD_CNES_INST = cd_cns_inst_p
	and		nr_sequencia <> nr_seq_instituicao_p;

end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION lote_ent_obter_se_mesmo_cnes (nr_seq_instituicao_p bigint, cd_cns_inst_p text) FROM PUBLIC;

