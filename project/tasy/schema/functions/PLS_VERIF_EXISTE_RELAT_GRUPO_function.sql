-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_verif_existe_relat_grupo ( ie_grupo_p text, cd_estabelecimento_p bigint) RETURNS varchar AS $body$
DECLARE


nr_seq_grupo_w		bigint;
nr_contador_w		bigint;
ie_existe_relat_p   varchar(1) := 'N';


BEGIN

select 	max(coalesce(b.nr_sequencia,0))
into STRICT 	nr_seq_grupo_w
from   	pls_grupo_relatorio a,
		pls_regra_relat_grupo b
where  	a.nr_Sequencia = b.nr_seq_grupo_relat
and		a.cd_grupo = ie_grupo_p;


if (nr_seq_grupo_w > 0 ) then
	select 	count(nr_sequencia)
	into STRICT	nr_contador_w
	from	pls_regra_relatorio
	where   nr_seq_grupo = nr_seq_grupo_w
	and 	ie_padrao = 'S';

	if (nr_contador_w > 0) then
		ie_existe_relat_p := 'S';
	end if;

end if;

return ie_existe_relat_p;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_verif_existe_relat_grupo ( ie_grupo_p text, cd_estabelecimento_p bigint) FROM PUBLIC;

