-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_birads_obrigatorio ( nr_seq_proc_p bigint, cd_tipo_procedimento_p bigint, cd_procedimentos_p text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w       varchar(1);



BEGIN

select CASE WHEN count(1)=1 THEN 'S'  ELSE 'N' END
into STRICT ds_retorno_w
from procedimento_paciente proc_pac,
procedimento proc
where proc_pac.nr_sequencia = nr_seq_proc_p
and proc_pac.cd_procedimento = proc.cd_procedimento
and proc_pac.ie_origem_proced = proc.ie_origem_proced
and ((proc.cd_tipo_procedimento = cd_tipo_procedimento_p) or 'S' = Obter_Se_Contido(proc_pac.cd_procedimento,cd_procedimentos_p));

return ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_birads_obrigatorio ( nr_seq_proc_p bigint, cd_tipo_procedimento_p bigint, cd_procedimentos_p text) FROM PUBLIC;

