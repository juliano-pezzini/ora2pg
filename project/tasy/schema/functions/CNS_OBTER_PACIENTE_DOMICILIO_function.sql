-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION cns_obter_paciente_domicilio ( nr_seq_usuario_p bigint) RETURNS varchar AS $body$
DECLARE


cd_pessoa_fisica_w	varchar(10);
ds_retorno_w		varchar(10);



BEGIN

begin
select	coalesce(max(a.cd_pessoa_fisica),'')
into STRICT	cd_pessoa_fisica_w
from	cns_domicilio_usuario	b,
	cns_usuario		a
where	a.nr_sequencia	= b.nr_seq_usuario
and	a.nr_sequencia	= nr_seq_usuario_p;
exception
	when others then
		cd_pessoa_fisica_w	:= null;
end;

ds_retorno_w	:= cd_pessoa_fisica_w;

return	ds_retorno_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION cns_obter_paciente_domicilio ( nr_seq_usuario_p bigint) FROM PUBLIC;

