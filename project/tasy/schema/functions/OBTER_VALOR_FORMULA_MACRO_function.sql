-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_valor_formula_macro (ds_formula_p text) RETURNS varchar AS $body$
DECLARE


c01	  		integer;
ds_comando_w		varchar(2000);
ds_retorno_w		varchar(255);
retorno_w		integer;


BEGIN
begin
ds_comando_w := ' select	'||ds_formula_p	||
		' from dual	';

C01 := DBMS_SQL.OPEN_CURSOR;
DBMS_SQL.PARSE(C01, ds_comando_w, dbms_sql.Native);
DBMS_SQL.DEFINE_COLUMN(C01, 1, ds_retorno_w,255);
retorno_w := DBMS_SQL.execute(C01);

while( DBMS_SQL.FETCH_ROWS(C01) > 0 ) loop
	BEGIN
	DBMS_SQL.COLUMN_VALUE(C01, 1, ds_retorno_w);
	end;
END LOOP;
exception
	when others then
	null;
end;
DBMS_SQL.CLOSE_CURSOR(C01);


return ds_retorno_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_valor_formula_macro (ds_formula_p text) FROM PUBLIC;

