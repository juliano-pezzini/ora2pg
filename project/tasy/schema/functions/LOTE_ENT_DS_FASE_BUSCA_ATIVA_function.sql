-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION lote_ent_ds_fase_busca_ativa (nr_seq_fase_p bigint) RETURNS varchar AS $body$
DECLARE


ds_fase_w		varchar(255);


BEGIN

if (nr_seq_fase_p IS NOT NULL AND nr_seq_fase_p::text <> '') then

	SELECT	MAX(SUBSTR(obter_valor_dominio(5576,nr_seq_fase_p),1,255))
	INTO STRICT	ds_fase_w
	;

end if;

return	ds_fase_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION lote_ent_ds_fase_busca_ativa (nr_seq_fase_p bigint) FROM PUBLIC;

