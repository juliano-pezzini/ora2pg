-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_dose_convertida_md (qt_dose_origem_p bigint, cd_unid_medida_ori_p text, cd_unid_medida_dest_p text, cd_unid_med_cons_p text, qt_conversao_und_orig_p bigint, qt_conversao_und_dest_p bigint ) RETURNS bigint AS $body$
DECLARE


cd_unid_medida_ori_w		varchar(30);
cd_unid_medida_dest_w		varchar(30);
qt_dose_convertida_w		double precision := 0;
cd_unid_med_cons_w			varchar(30);

BEGIN
  cd_unid_medida_ori_w	:= upper(cd_unid_medida_ori_p);
  cd_unid_medida_dest_w	:= upper(cd_unid_medida_dest_p);
  cd_unid_med_cons_w		:= upper(cd_unid_med_cons_p);
  if (qt_dose_origem_p IS NOT NULL AND qt_dose_origem_p::text <> '') then
    if (cd_unid_medida_ori_w = cd_unid_medida_dest_w) then
      qt_dose_convertida_w := qt_dose_origem_p;
    else
      if (cd_unid_med_cons_w = cd_unid_medida_dest_w) then
        qt_dose_convertida_w := dividir_md(qt_dose_origem_p, qt_conversao_und_orig_p);
      elsif (cd_unid_med_cons_w <> cd_unid_medida_ori_w) then
        qt_dose_convertida_w := dividir_md(qt_dose_origem_p, qt_conversao_und_orig_p);
        qt_dose_convertida_w := qt_dose_convertida_w * qt_conversao_und_dest_p;
      else
        qt_dose_convertida_w := qt_dose_origem_p * qt_conversao_und_dest_p;
      end if;
    end if;
  end if;

  return	qt_dose_convertida_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_dose_convertida_md (qt_dose_origem_p bigint, cd_unid_medida_ori_p text, cd_unid_medida_dest_p text, cd_unid_med_cons_p text, qt_conversao_und_orig_p bigint, qt_conversao_und_dest_p bigint ) FROM PUBLIC;

