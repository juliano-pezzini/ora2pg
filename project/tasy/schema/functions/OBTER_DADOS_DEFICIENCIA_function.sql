-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_dados_deficiencia ( cd_pessoa_fisica_p text, tipo_dado_p text) RETURNS bigint AS $body$
DECLARE


retorno_w	bigint;


BEGIN

  if (upper(tipo_dado_p) = 'TIPO') then

	select max(CASE WHEN ptd.nr_seq_tipo_def=48 THEN 1 WHEN ptd.nr_seq_tipo_def=49 THEN 2 WHEN ptd.nr_seq_tipo_def=50 THEN 3 WHEN ptd.nr_seq_tipo_def=51 THEN 4 WHEN ptd.nr_seq_tipo_def=52 THEN 5 WHEN ptd.nr_seq_tipo_def=53 THEN 6 WHEN ptd.nr_seq_tipo_def=54 THEN 7 WHEN ptd.nr_seq_tipo_def=55 THEN 8 WHEN ptd.nr_seq_tipo_def=56 THEN 9 END )
	into STRICT retorno_w
	from pf_tipo_deficiencia ptd
	where ptd.cd_pessoa_fisica = cd_pessoa_fisica_p
	and coalesce(ptd.dt_inativacao::text, '') = '' and (ptd.dt_liberacao IS NOT NULL AND ptd.dt_liberacao::text <> ''); -- tipodificultad
  elsif (upper(tipo_dado_p) = 'GRAU') then
	select max(CASE WHEN ptd.nr_seq_tipo_def=56 THEN 9  ELSE ptd.ie_grau_deficiencia END )
	into STRICT retorno_w
	from pf_tipo_deficiencia ptd
	where ptd.cd_pessoa_fisica = cd_pessoa_fisica_p
	and coalesce(ptd.dt_inativacao::text, '') = '' and (ptd.dt_liberacao IS NOT NULL AND ptd.dt_liberacao::text <> ''); --gradodificultad
  elsif (upper(tipo_dado_p) = 'ORIGEM') then
	select max(CASE WHEN PTD.NR_SEQ_TIPO_DEF=56 THEN 9  ELSE CASE WHEN ptd.nr_seq_causa_lesao=6 THEN 1 WHEN ptd.nr_seq_causa_lesao=8 THEN 2 WHEN ptd.nr_seq_causa_lesao=9 THEN 3 WHEN ptd.nr_seq_causa_lesao=10 THEN 4 WHEN ptd.nr_seq_causa_lesao=11 THEN 5 WHEN ptd.nr_seq_causa_lesao=12 THEN 6 WHEN ptd.nr_seq_causa_lesao=13 THEN 9 END  END )
	into STRICT retorno_w
	from pf_tipo_deficiencia ptd
	where ptd.cd_pessoa_fisica = cd_pessoa_fisica_p
	and coalesce(ptd.dt_inativacao::text, '') = '' and (ptd.dt_liberacao IS NOT NULL AND ptd.dt_liberacao::text <> '');  -- origendificultad
  else
      retorno_w := 0;
  end if;

return	retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_dados_deficiencia ( cd_pessoa_fisica_p text, tipo_dado_p text) FROM PUBLIC;

