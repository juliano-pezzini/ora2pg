-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_unid_atend_adep (nr_atendimento_p bigint, dt_unidade_p timestamp, ie_informacao_p text) RETURNS varchar AS $body$
DECLARE


ds_unid_adep_w		varchar(255);
nr_seq_interno_w		bigint;
cd_setor_atendimento_w	integer;
ds_setor_atendimento_w	varchar(100);
cd_unidade_basica_w		varchar(10);
cd_unidade_compl_w		varchar(10);


BEGIN
if (nr_atendimento_p IS NOT NULL AND nr_atendimento_p::text <> '') and (dt_unidade_p IS NOT NULL AND dt_unidade_p::text <> '') and (ie_informacao_p IS NOT NULL AND ie_informacao_p::text <> '') then

	/* obter atend_paciente_unidade x data */

	select	coalesce(max(a.nr_seq_interno),0)
	into STRICT	nr_seq_interno_w
	from	atend_paciente_unidade a
	where	a.nr_atendimento	= 	nr_atendimento_p
	and	a.nr_seq_interno	=	(
						SELECT	coalesce(max(x.nr_seq_interno),0)
						from	setor_atendimento y,
							atend_paciente_unidade x
						where	y.cd_classif_setor			in (1,3,4,8)
						and	y.cd_setor_atendimento		= x.cd_setor_atendimento
						and	coalesce(x.ie_passagem_setor,'N')	<> 'S'
						and	x.dt_entrada_unidade			<= dt_unidade_p
						and	x.nr_atendimento			= nr_atendimento_p
						);

	/* obter informacao */

	if (nr_seq_interno_w > 0) then

		select	cd_setor_atendimento,
			substr(obter_nome_setor(cd_setor_atendimento),1,100) ds_setor_atendimento,
			cd_unidade_basica,
			cd_unidade_compl
		into STRICT	cd_setor_atendimento_w,
			ds_setor_atendimento_w,
			cd_unidade_basica_w,
			cd_unidade_compl_w
		from	atend_paciente_unidade
		where	nr_atendimento = nr_atendimento_p
		and	nr_seq_interno = nr_seq_interno_w;

		if (ie_informacao_p = 'SU') then
			ds_unid_adep_w := ds_setor_atendimento_w || ' ' || cd_unidade_basica_w || ' ' || cd_unidade_compl_w;
		elsif (ie_informacao_p = 'S') then
			ds_unid_adep_w := ds_setor_atendimento_w;
		elsif (ie_informacao_p = 'U') then
			ds_unid_adep_w := cd_unidade_basica_w || ' ' || cd_unidade_compl_w;
		end if;

	end if;

end if;

return ds_unid_adep_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_unid_atend_adep (nr_atendimento_p bigint, dt_unidade_p timestamp, ie_informacao_p text) FROM PUBLIC;

