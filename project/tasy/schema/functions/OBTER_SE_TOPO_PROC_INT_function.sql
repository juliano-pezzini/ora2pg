-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_topo_proc_int ( nr_seq_topografia_p bigint, nr_seq_proc_interno_p bigint) RETURNS varchar AS $body$
DECLARE


cont_w		bigint;
ds_retorno_w	varchar(1);


BEGIN

ds_retorno_w	:= 'S';

select	count(*)
into STRICT	cont_w
from	proc_interno_topografia
where	nr_seq_proc_interno = nr_seq_proc_interno_p;

if (cont_w > 0) then

	ds_retorno_w	:= 'N';

	select	count(*)
	into STRICT	cont_w
	from	proc_interno_topografia
	where	nr_seq_proc_interno = nr_seq_proc_interno_p
	and	nr_seq_topografia	= nr_seq_topografia_p;

	if (cont_w > 0) then
		ds_retorno_w := 'S';
	end if;
end if;

return ds_retorno_w;

end	;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_topo_proc_int ( nr_seq_topografia_p bigint, nr_seq_proc_interno_p bigint) FROM PUBLIC;

