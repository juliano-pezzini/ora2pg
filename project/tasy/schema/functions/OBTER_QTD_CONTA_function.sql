-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_qtd_conta (nr_cirurgia_p bigint, cd_material_p bigint, nr_seq_lote_fornec_p bigint ) RETURNS bigint AS $body$
DECLARE


qt_material_w	bigint := 0;


BEGIN

	SELECT		SUM(qt_material)
	INTO STRICT		qt_material_w
	FROM 		material_atend_paciente
	WHERE 		nr_seq_atepacu = obter_unid_setor_cirurgia(nr_cirurgia_p,'N')
	AND 		cd_material    = cd_material_p
	AND 		coalesce(nr_seq_lote_fornec,coalesce(nr_seq_lote_fornec_p,0)) = coalesce(nr_seq_lote_fornec_p,0)
	AND 		coalesce(CD_MOTIVO_EXC_CONTA::text, '') = '';

return	coalesce(qt_material_w,0);

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_qtd_conta (nr_cirurgia_p bigint, cd_material_p bigint, nr_seq_lote_fornec_p bigint ) FROM PUBLIC;

