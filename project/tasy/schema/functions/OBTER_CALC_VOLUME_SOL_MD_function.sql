-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_calc_volume_sol_md (cd_valores_p text) RETURNS bigint AS $body$
DECLARE

  v_result_w bigint;

BEGIN
  select CASE WHEN sum(linha)=0 THEN  1  ELSE sum(linha) END
    into STRICT v_result_w
    from (WITH RECURSIVE cte AS (
SELECT regexp_substr(cd_valores_p, '[^-]+', 1, level) as linha
            
          (regexp_substr(cd_valores_p, '[^-]+', 1, level) IS NOT NULL AND (regexp_substr(cd_valores_p, '[^-]+', 1, level))::text <> '')  UNION ALL
SELECT regexp_substr(cd_valores_p, '[^-]+', 1, level) as linha
            
          (regexp_substr(cd_valores_p, '[^-]+', 1, level) IS NOT NULL AND (regexp_substr(cd_valores_p, '[^-]+', 1, level))::text <> '') JOIN cte c ON ()

) SELECT * FROM cte;
) alias7;

  return v_result_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_calc_volume_sol_md (cd_valores_p text) FROM PUBLIC;

