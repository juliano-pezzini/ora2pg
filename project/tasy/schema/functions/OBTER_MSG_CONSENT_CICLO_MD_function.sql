-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_msg_consent_ciclo_md ( ds_textos_necessarios_p text, ie_tipo_regra_p text ) RETURNS varchar AS $body$
DECLARE

    ds_txt_neces_w       varchar(2000);

BEGIN
	if (ds_textos_necessarios_p IS NOT NULL AND ds_textos_necessarios_p::text <> '') then
		if (ie_tipo_regra_p	= 'P') then
            ds_txt_neces_w := wheb_mensagem_pck.get_texto(276462, 'DS_CONSENT='||ds_textos_necessarios_p);
		else
            ds_txt_neces_w := wheb_mensagem_pck.get_texto(199318, 'DS_CONSENT='||ds_textos_necessarios_p);
		end if;
	end if;

    return ds_txt_neces_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_msg_consent_ciclo_md ( ds_textos_necessarios_p text, ie_tipo_regra_p text ) FROM PUBLIC;

