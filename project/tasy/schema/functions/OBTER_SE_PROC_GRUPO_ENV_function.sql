-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_proc_grupo_env ( cd_procedimento_p bigint, ie_origem_proced_p bigint, nr_seq_proc_interno_p bigint, nr_seq_exame_p bigint, nr_seq_grupo_p bigint) RETURNS varchar AS $body$
DECLARE


ie_evento_w			varchar(1);


BEGIN

if (coalesce(nr_seq_grupo_p::text, '') = '') then
	ie_evento_w	:= 'S';
else
	select	coalesce(max('S'),'N')
	into STRICT	ie_evento_w
	from	grupo_env_alerta_evento a,
			proc_grupo_alerta_evento d
	where	a.nr_sequencia	= d.nr_seq_grupo
	and		a.nr_sequencia	= nr_seq_grupo_p
	and		d.nr_seq_grupo	= nr_seq_grupo_p
	AND		coalesce(d.nr_seq_proc_interno,coalesce(nr_seq_proc_interno_p,0))	= coalesce(nr_seq_proc_interno_p,0)
	AND		coalesce(d.cd_procedimento,coalesce(cd_procedimento_p,0))		= coalesce(cd_procedimento_p,0)
	AND		coalesce(d.ie_origem_proced,coalesce(ie_origem_proced_p,0))	= coalesce(ie_origem_proced_p,0)
	AND		coalesce(d.nr_seq_exame,coalesce(nr_seq_exame_p,0))			= coalesce(nr_seq_exame_p,0);
end if;

return	ie_evento_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_proc_grupo_env ( cd_procedimento_p bigint, ie_origem_proced_p bigint, nr_seq_proc_interno_p bigint, nr_seq_exame_p bigint, nr_seq_grupo_p bigint) FROM PUBLIC;

