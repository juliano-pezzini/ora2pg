-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_min_permanencia_setor (nr_atendimento_p bigint, cd_setor_atendimento_p bigint, cd_unidade_basica_p text, cd_unidade_compl_p text) RETURNS bigint AS $body$
DECLARE


qt_min_setor_w	numeric(20);
dt_entrada_unidade_w	timestamp;


BEGIN

select	max(dt_entrada_unidade)
into STRICT	dt_entrada_unidade_w
from	atend_paciente_unidade
where	nr_atendimento = nr_atendimento_p
and	cd_setor_atendimento = cd_setor_atendimento_p
and	cd_unidade_basica    = cd_unidade_basica_p
and 	cd_unidade_compl     = cd_unidade_compl_p;


if (dt_entrada_unidade_w IS NOT NULL AND dt_entrada_unidade_w::text <> '') then

   qt_min_setor_w := ((clock_timestamp() - dt_entrada_unidade_w) * 1440);

end if;

return	qt_min_setor_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_min_permanencia_setor (nr_atendimento_p bigint, cd_setor_atendimento_p bigint, cd_unidade_basica_p text, cd_unidade_compl_p text) FROM PUBLIC;

