-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';



--
-- dblink wrapper to call function verifica_sla_removido as an autonomous transaction
--
CREATE EXTENSION IF NOT EXISTS dblink;

CREATE OR REPLACE FUNCTION verifica_sla_removido (nr_seq_ordem_p bigint) RETURNS varchar AS $body$
DECLARE
	-- Change this to reflect the dblink connection string
	v_conn_str  text := format('port=%s dbname=%s user=%s', current_setting('port'), current_database(), current_user);
	v_query     text;

	v_ret	varchar;
BEGIN
	v_query := 'SELECT * FROM verifica_sla_removido_atx ( ' || quote_nullable(nr_seq_ordem_p) || ' )';
	SELECT * INTO v_ret FROM dblink(v_conn_str, v_query) AS p (ret varchar);
	RETURN v_ret;

END;
$body$ LANGUAGE plpgsql SECURITY DEFINER;




CREATE OR REPLACE FUNCTION verifica_sla_removido_atx (nr_seq_ordem_p bigint) RETURNS varchar AS $body$
DECLARE
ie_removeu_sla_w	varchar(1);


BEGIN

	select	CASE WHEN count(*)=0 THEN  'N'  ELSE 'S' END
	into STRICT	ie_removeu_sla_w
	from	man_ordem_serv_tecnico
	where	nr_seq_ordem_serv = nr_seq_ordem_p
	and	nr_seq_tipo = 148;

return	ie_removeu_sla_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION verifica_sla_removido (nr_seq_ordem_p bigint) FROM PUBLIC; -- REVOKE ALL ON FUNCTION verifica_sla_removido_atx (nr_seq_ordem_p bigint) FROM PUBLIC;

