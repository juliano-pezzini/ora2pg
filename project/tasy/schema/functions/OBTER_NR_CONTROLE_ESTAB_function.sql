-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';



--
-- dblink wrapper to call function obter_nr_controle_estab as an autonomous transaction
--
CREATE EXTENSION IF NOT EXISTS dblink;

CREATE OR REPLACE FUNCTION obter_nr_controle_estab (cd_estabelecimento_p bigint) RETURNS bigint AS $body$
DECLARE
	-- Change this to reflect the dblink connection string
	v_conn_str  text := format('port=%s dbname=%s user=%s', current_setting('port'), current_database(), current_user);
	v_query     text;

	v_ret	bigint;
BEGIN
	v_query := 'SELECT * FROM obter_nr_controle_estab_atx ( ' || quote_nullable(cd_estabelecimento_p) || ' )';
	SELECT * INTO v_ret FROM dblink(v_conn_str, v_query) AS p (ret bigint);
	RETURN v_ret;

END;
$body$ LANGUAGE plpgsql SECURITY DEFINER;




CREATE OR REPLACE FUNCTION obter_nr_controle_estab_atx (cd_estabelecimento_p bigint) RETURNS bigint AS $body$
DECLARE



nr_seq_controle_w		bigint;
BEGIN


SELECT	MAX(nr_seq_controle) + 1
into STRICT 	nr_seq_controle_w
FROM	atendimento_visita a,
	atendimento_paciente b
WHERE 	a.nr_atendimento = b.nr_atendimento
AND	b.cd_estabelecimento = cd_estabelecimento_p;

return	nr_seq_controle_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_nr_controle_estab (cd_estabelecimento_p bigint) FROM PUBLIC; -- REVOKE ALL ON FUNCTION obter_nr_controle_estab_atx (cd_estabelecimento_p bigint) FROM PUBLIC;

