-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_cor_isolamento_leito (cd_perfil_p bigint, cd_estabelecimento_p bigint) RETURNS varchar AS $body$
DECLARE

				
ds_cor_fundo_w		varchar(50);
ds_cor_fonte_w		varchar(50);
ds_cor_selecao_w	varchar(50);

BEGIN

SELECT * FROM tasy_obter_cor(10361, cd_perfil_p, cd_estabelecimento_p, ds_cor_fundo_w, ds_cor_fonte_w, ds_cor_selecao_w) INTO STRICT ds_cor_fundo_w, ds_cor_fonte_w, ds_cor_selecao_w;

return converte_Cor_Delphi_RGB(ds_cor_fundo_w);

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_cor_isolamento_leito (cd_perfil_p bigint, cd_estabelecimento_p bigint) FROM PUBLIC;

