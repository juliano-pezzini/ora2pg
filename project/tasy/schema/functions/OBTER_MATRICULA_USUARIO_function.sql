-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_matricula_usuario ( CD_CONVENIO_P bigint, NR_ATENDIMENTO_P bigint) RETURNS varchar AS $body$
DECLARE


CD_USUARIO_W		varchar(60);


BEGIN
SELECT MAX(CD_USUARIO_CONVENIO)
INTO STRICT CD_USUARIO_W
FROM ATEND_CATEGORIA_CONVENIO
WHERE NR_ATENDIMENTO 	= NR_ATENDIMENTO_P
     AND CD_CONVENIO		= CD_CONVENIO_P
     AND DT_INICIO_VIGENCIA	= (SELECT MAX(X.DT_INICIO_VIGENCIA)
				    FROM ATEND_CATEGORIA_CONVENIO X
				    WHERE X.NR_ATENDIMENTO	= NR_ATENDIMENTO_P
				         AND X.CD_CONVENIO		= CD_CONVENIO_P);

RETURN CD_USUARIO_W;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_matricula_usuario ( CD_CONVENIO_P bigint, NR_ATENDIMENTO_P bigint) FROM PUBLIC;

