-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION via_obter_aprov_sub ( cd_pessoa_p text) RETURNS varchar AS $body$
DECLARE

 
ds_retorno_w	varchar(255);
qt_diretor_w	bigint;
qt_gerente_w	bigint;


BEGIN 
select	count(*) 
into STRICT	qt_diretor_w 
from	PESSOA_FISICA_DELEGACAO a 
where	CD_PESSOA_SUBSTITUTA = cd_pessoa_p 
and	IE_OBJETIVO = 'AV' 
and DT_LIMITE >= clock_timestamp();
 
select	count(*) 
into STRICT	qt_gerente_w 
from	depto_gerencia_philips a 
where	obter_cd_pessoa_substituta(obter_cd_pessoa_responsavel(a.nr_seq_gerencia)) = cd_pessoa_p;
 
if (cd_pessoa_p = '0') then 
	ds_retorno_w := cd_pessoa_p;
elsif (qt_diretor_w > 0) then 
	ds_retorno_w := cd_pessoa_p;
elsif (qt_gerente_w > 0) then 
	ds_retorno_w := cd_pessoa_p;
else 
	ds_retorno_w := 'X';
end if;
/* 
	begin 
	select	nvl(max(a.cd_aprov_sub),cd_pessoa_p) 
	into	ds_retorno_w 
	from	fin_gv_pend_aprov a 
	where	a.cd_pessoa_aprov = cd_pessoa_p; 
	end; 
end if;*/
 
 
return	ds_retorno_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION via_obter_aprov_sub ( cd_pessoa_p text) FROM PUBLIC;

