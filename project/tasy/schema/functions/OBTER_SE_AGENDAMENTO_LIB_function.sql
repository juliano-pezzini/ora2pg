-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_agendamento_lib ( cd_pessoa_fisica_p text, cd_medico_p text, cd_perfil_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint, ie_todos_p text) RETURNS varchar AS $body$
DECLARE


--ie_permite_todos_w	varchar2(1);
ie_permite_w		varchar(1);


BEGIN
--obter_param_usuario(870, 11, obter_perfil_ativo, nm_usuario_p, cd_estabelecimento_p, ie_permite_todos_w);
if (ie_todos_p = 'N') then
	select	coalesce(max('S'),'N')
	into STRICT	ie_permite_w
	from	agenda_lib_uso_cc
	where	coalesce(cd_pessoa_fisica,cd_pessoa_fisica_p)	=	cd_pessoa_fisica_p
	and	coalesce(cd_perfil,cd_perfil_p)			=	cd_perfil_p
	and	cd_medico					=	cd_medico_p;
else
	select	coalesce(max('S'),'N')
	into STRICT	ie_permite_w
	from	agenda_lib_uso_cc
	where	cd_medico					=	cd_medico_p;
end if;

return	ie_permite_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_agendamento_lib ( cd_pessoa_fisica_p text, cd_medico_p text, cd_perfil_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint, ie_todos_p text) FROM PUBLIC;

