-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION tiss_obter_pr_desc ( nr_seq_procedimento_p bigint) RETURNS bigint AS $body$
DECLARE


pr_valor_w	bigint;


BEGIN

select	coalesce(max(a.pr_valor),0)
into STRICT	pr_valor_w
from	proc_paciente_valor a,
	procedimento_paciente b
where	a.nr_seq_procedimento	= b.nr_sequencia
and	b.nr_sequencia		= nr_seq_procedimento_p
and	a.ie_tipo_valor		= 3;

return	pr_valor_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION tiss_obter_pr_desc ( nr_seq_procedimento_p bigint) FROM PUBLIC;

