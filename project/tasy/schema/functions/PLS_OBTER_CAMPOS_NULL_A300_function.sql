-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_campos_null_a300 ( nr_seq_empresa_p ptu_mov_produto_benef.nr_seq_empresa%type, nr_seq_segurado_p ptu_mov_produto_benef.nr_seq_segurado%type, ie_opcao_p text) RETURNS varchar AS $body$
DECLARE

/* +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Finalidade: 	Verificar se algum dos campos 'nr_rg', 'sg_uf_rg', 'ds_orgao_emissor', 'cd_pais' do
	beneficiario do a300 estão nulos, caso um deles esteja nulo, todos devem receber nulo,
	caso contrário, recebem seu devido valor.
-------------------------------------------------------------------------------------------------------------------
Locais de chamada direta:
[  X]  Objetos do dicionário [ ] Tasy (Delphi/Java) [  ] Portal [  ]  Relatórios [ ] Outros:
 ------------------------------------------------------------------------------------------------------------------
Pontos de atenção:

- Function chamda dentro das views do a300

-ie_opcao_p
- 'RG' - Código do RG do beneficiario
- 'UF' - Sigla do UF do RG do beneficiário
- 'OE' - Orgão emissor do RG do beneficiário
- 'P' - Código do país do beneficiario
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ */
nr_identidade_w			ptu_mov_produto_benef.nr_identidade%type;
cd_uf_rg_w			ptu_mov_produto_benef.sg_uf_rg%type;
ds_orgao_emissor_w		ptu_mov_produto_benef.ds_orgao_emissor%type;
cd_pais_w			ptu_mov_produto_benef.cd_pais%type;
ds_retorno_w			varchar(50);


BEGIN
ds_retorno_w := null;

select	max(nr_identidade),
	max(sg_uf_rg),
	max(ds_orgao_emissor),
	max(cd_pais)
into STRICT	nr_identidade_w,
	cd_uf_rg_w,
	ds_orgao_emissor_w,
	cd_pais_w
from	ptu_mov_produto_benef
where	nr_seq_empresa 	= nr_seq_empresa_p
and	nr_seq_segurado	= nr_seq_segurado_p;

if (coalesce(nr_identidade_w,'0') = '0') or (coalesce(cd_uf_rg_w,'0') = '0') or (coalesce(ds_orgao_emissor_w,'0') = '0') or (coalesce(cd_pais_w,'0') = '0') then

	ds_retorno_w := null;

elsif (ie_opcao_p = 'RG') then
	ds_retorno_w := nr_identidade_w;

elsif (ie_opcao_p = 'UF') then
	ds_retorno_w := cd_uf_rg_w;

elsif (ie_opcao_p = 'OE') then
	ds_retorno_w := ds_orgao_emissor_w;

elsif (ie_opcao_p = 'P') then
	ds_retorno_w := cd_pais_w;
end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_campos_null_a300 ( nr_seq_empresa_p ptu_mov_produto_benef.nr_seq_empresa%type, nr_seq_segurado_p ptu_mov_produto_benef.nr_seq_segurado%type, ie_opcao_p text) FROM PUBLIC;

