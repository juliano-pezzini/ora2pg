-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION tiss_obter_desc_proc_convenio (nr_seq_propaci_p bigint) RETURNS varchar AS $body$
DECLARE


ds_procedimento_w	varchar(255);


BEGIN

select	max(c.ds_procedimento)
into STRICT	ds_procedimento_w
from	proc_paciente_convenio c
where	c.nr_seq_procedimento	= nr_seq_propaci_p;

return	ds_procedimento_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION tiss_obter_desc_proc_convenio (nr_seq_propaci_p bigint) FROM PUBLIC;

