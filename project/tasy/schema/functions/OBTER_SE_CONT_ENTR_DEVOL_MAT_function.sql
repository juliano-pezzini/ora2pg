-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_cont_entr_devol_mat ( cd_material_p bigint) RETURNS varchar AS $body$
DECLARE


ie_retorno_w			varchar(1);
cd_classe_material_w		integer;
cd_subgrupo_material_w		smallint;
cd_grupo_material_w		smallint;
cd_perfil_ativo_w		bigint;


BEGIN

cd_perfil_ativo_w	:= substr(obter_perfil_ativo,1,10);

if (coalesce(cd_material_p,0) > 0) then

	SELECT	MAX(cd_classe_material),
		MAX(cd_subgrupo_material),
		MAX(cd_grupo_material)
	INTO STRICT	cd_classe_material_w,
		cd_subgrupo_material_w,
		cd_grupo_material_w
	FROM	estrutura_material_v
	WHERE	cd_material	= cd_material_p;

end if;

select	coalesce(max('S'),'N')
into STRICT	ie_retorno_w
from	controle_entrega_devol_mat
where	coalesce(cd_classe_material,coalesce(cd_classe_material_w,0))  	   = coalesce(cd_classe_material_w,0)
and	coalesce(cd_subgrupo_material,coalesce(cd_subgrupo_material_w,0))    = coalesce(cd_subgrupo_material_w,0)
and	coalesce(cd_grupo_material,coalesce(cd_grupo_material_w,0))	   = coalesce(cd_grupo_material_w,0)
and	coalesce(cd_perfil_ativo,coalesce(cd_perfil_ativo_w,0))	  	   = coalesce(cd_perfil_ativo_w,0)
and	coalesce(cd_material,coalesce(cd_material_p,0))			   = coalesce(cd_material_p,0);

return	ie_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_cont_entr_devol_mat ( cd_material_p bigint) FROM PUBLIC;

