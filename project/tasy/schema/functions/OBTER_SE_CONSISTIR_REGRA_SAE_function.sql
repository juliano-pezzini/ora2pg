-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_consistir_regra_sae ( nr_seq_regra_p bigint, cd_perfil_p bigint) RETURNS varchar AS $body$
DECLARE


ie_regra_existe_w		varchar(1);
ie_consistir_w		varchar(1);
ie_forma_consistencia_w	varchar(15);
ie_regra_lib_perfil_w	varchar(15);

C01 CURSOR FOR
	SELECT	ie_forma_consistencia
	from	regra_consiste_sae_par
	where	nr_seq_regra	= nr_seq_regra_p
	and	coalesce(cd_perfil,cd_perfil_p)	= cd_perfil_p
	order by coalesce(cd_perfil,0);



BEGIN

open C01;
loop
fetch C01 into
	ie_forma_consistencia_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
end loop;
close C01;

return coalesce(ie_forma_consistencia_w,'X');

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_consistir_regra_sae ( nr_seq_regra_p bigint, cd_perfil_p bigint) FROM PUBLIC;

