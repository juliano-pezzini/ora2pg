-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';



--
-- dblink wrapper to call function obter_laudo_paciente_sequencia as an autonomous transaction
--
CREATE EXTENSION IF NOT EXISTS dblink;

CREATE OR REPLACE FUNCTION obter_laudo_paciente_sequencia ( nr_prescricao_p bigint, nr_seq_interno_p bigint) RETURNS bigint AS $body$
DECLARE
	-- Change this to reflect the dblink connection string
	v_conn_str  text := format('port=%s dbname=%s user=%s', current_setting('port'), current_database(), current_user);
	v_query     text;

	v_ret	bigint;
BEGIN
	v_query := 'SELECT * FROM obter_laudo_paciente_sequencia_atx ( ' || quote_nullable(nr_prescricao_p) || ',' || quote_nullable(nr_seq_interno_p) || ' )';
	SELECT * INTO v_ret FROM dblink(v_conn_str, v_query) AS p (ret bigint);
	RETURN v_ret;

END;
$body$ LANGUAGE plpgsql SECURITY DEFINER;




CREATE OR REPLACE FUNCTION obter_laudo_paciente_sequencia_atx ( nr_prescricao_p bigint, nr_seq_interno_p bigint) RETURNS bigint AS $body$
DECLARE

nr_seq_retorno_w			bigint := 0;
BEGIN

if (nr_prescricao_p IS NOT NULL AND nr_prescricao_p::text <> '' AND nr_seq_interno_p IS NOT NULL AND nr_seq_interno_p::text <> '') then
begin
select	nr_sequencia
into STRICT	nr_seq_retorno_w
from	laudo_paciente
where	nr_prescricao = nr_prescricao_p and substr(obter_dado_proc_laudo(NR_SEQUENCIA,3),1,10) = nr_seq_interno_p;
end;
end if;

return	nr_seq_retorno_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_laudo_paciente_sequencia ( nr_prescricao_p bigint, nr_seq_interno_p bigint) FROM PUBLIC; -- REVOKE ALL ON FUNCTION obter_laudo_paciente_sequencia_atx ( nr_prescricao_p bigint, nr_seq_interno_p bigint) FROM PUBLIC;

