-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_cor_proc_cirurgia (cd_procedimento_p bigint, ie_origem_proced_p bigint) RETURNS bigint AS $body$
DECLARE


nr_seq_cor_w bigint;


BEGIN

if (cd_procedimento_p IS NOT NULL AND cd_procedimento_p::text <> '') and (ie_origem_proced_p IS NOT NULL AND ie_origem_proced_p::text <> '') then
	select	max(nr_sequencia)
	into STRICT	nr_seq_cor_w
	from 	regra_cor_proced_agenda
	where (cd_procedimento = cd_procedimento_p)
	and (ie_origem_proced = ie_origem_proced_p);

end if;

return	nr_seq_cor_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_cor_proc_cirurgia (cd_procedimento_p bigint, ie_origem_proced_p bigint) FROM PUBLIC;

