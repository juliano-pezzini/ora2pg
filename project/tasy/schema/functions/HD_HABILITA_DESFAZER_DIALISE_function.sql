-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION hd_habilita_desfazer_dialise (nr_seq_dialise_p bigint) RETURNS varchar AS $body$
DECLARE

qt_registro_w	bigint;

BEGIN
select	count(*) 
into STRICT	qt_registro_w 
from	hd_dialise_dialisador 
where	nr_sequencia = hd_obter_dados_dialise(nr_seq_dialise_p,'DS') 
and	(dt_retirada IS NOT NULL AND dt_retirada::text <> '');
 
if (qt_registro_w > 0) then 
	return 'N';
end if;
 
select	count(*) 
into STRICT	qt_registro_w 
from	hd_controle 
where	nr_seq_dialise = nr_seq_dialise_p;
 
if (qt_registro_w > 0) then 
	return 'N';
end if;
 
select	count(*) 
into STRICT	qt_registro_w 
from	hd_dialise_evento 
where	nr_seq_dialise = nr_seq_dialise_p;
 
if (qt_registro_w > 0) then 
	return 'N';
end if;
 
select	count(*) 
into STRICT	qt_registro_w 
from	hd_dialise_concentrado 
where	nr_seq_dialise = nr_seq_dialise_p;
 
if (qt_registro_w > 0) then 
	return 'N';
end if;
 
return	'S';
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION hd_habilita_desfazer_dialise (nr_seq_dialise_p bigint) FROM PUBLIC;

