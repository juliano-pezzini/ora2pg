-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION rp_obtr_list_espr_html5 (cd_pessoa_fisica_p text, ie_tipo_p text) RETURNS timestamp AS $body$
DECLARE

dt_retorno_w	timestamp;

BEGIN

if (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') and (cd_pessoa_fisica_p > 0) then
	select 	max(CASE WHEN ie_tipo_p='DTI' THEN dt_inclusao_lista  ELSE null END )
	into STRICT	dt_retorno_w
	from	rp_lista_espera_modelo
	where	cd_pessoa_fisica = cd_pessoa_fisica_p
	and	ie_status = 'A';

end if;

return	dt_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION rp_obtr_list_espr_html5 (cd_pessoa_fisica_p text, ie_tipo_p text) FROM PUBLIC;

