-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_desc_vol_etapa_adep2 ( nr_prescricao_p bigint, nr_seq_solucao_p bigint, ie_acm_p text, ie_sn_p text, ie_solucao_agrupada_p text default 'N') RETURNS varchar AS $body$
DECLARE

		
ds_vol_etapa_w	varchar(255);


BEGIN
if (nr_prescricao_p IS NOT NULL AND nr_prescricao_p::text <> '') and (nr_seq_solucao_p IS NOT NULL AND nr_seq_solucao_p::text <> '') then
	begin
	ds_vol_etapa_w	:= substr(obter_desc_vol_sol_etapa_adep(nr_prescricao_p,nr_seq_solucao_p,ie_solucao_agrupada_p),1,255);

	if (coalesce(ie_acm_p,'N') = 'S') or (coalesce(ie_sn_p,'N') = 'S') then
		begin
		if (ie_sn_p = 'S') then
			begin
			ds_vol_etapa_w	:= substr(obter_desc_expressao(309405) || ' '  || ds_vol_etapa_w,1,255);
			end;
		elsif (ie_acm_p = 'S') then
			begin
			ds_vol_etapa_w	:= substr(obter_desc_expressao(283158) || ' ' || ds_vol_etapa_w,1,255);
			end;
		end if;
		end;
	end if;
	end;
end if;
return ds_vol_etapa_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_desc_vol_etapa_adep2 ( nr_prescricao_p bigint, nr_seq_solucao_p bigint, ie_acm_p text, ie_sn_p text, ie_solucao_agrupada_p text default 'N') FROM PUBLIC;

