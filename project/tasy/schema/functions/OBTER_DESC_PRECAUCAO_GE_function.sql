-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_desc_precaucao_ge (nr_atendimento_p bigint) RETURNS varchar AS $body$
DECLARE


ds_precaucao_w 			varchar(255);
ds_precaucao_concat_w 		varchar(4000);
ie_consiste_isolamento_w	varchar(1);
ie_isolamento_w			varchar(1);

c01 CURSOR FOR
        SELECT  distinct
				b.ds_precaucao
        from    cih_precaucao b,
                atendimento_precaucao a
        where   a.nr_seq_precaucao = b.nr_sequencia
        and     a.nr_atendimento = nr_atendimento_p
	and		coalesce(a.dt_inativacao::text, '') = ''
	and		((coalesce(a.dt_termino::text, '') = '') or (clock_timestamp() between coalesce(a.DT_INICIO,clock_timestamp() - interval '1 days') and a.DT_TERMINO))
	and		(a.dt_liberacao IS NOT NULL AND a.dt_liberacao::text <> '')
	and	coalesce(a.ie_situacao,'A') = 'A'
        order by 1;


BEGIN

open c01;
loop
fetch c01 into
        ds_precaucao_w;
EXIT WHEN NOT FOUND; /* apply on c01 */
        begin

		if (ds_precaucao_concat_w IS NOT NULL AND ds_precaucao_concat_w::text <> '') then
			ds_precaucao_concat_w := substr(ds_precaucao_concat_w || ', ',1,4000);
		end if;
		ds_precaucao_concat_w := substr(ds_precaucao_concat_w || ds_precaucao_w,1,4000);

		end;
end loop;
close c01;

return ds_precaucao_concat_w;


end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_desc_precaucao_ge (nr_atendimento_p bigint) FROM PUBLIC;

