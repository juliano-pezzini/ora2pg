-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_setor_mat_auditoria ( nr_seq_interno_p bigint, nr_seq_item_p bigint) RETURNS bigint AS $body$
DECLARE


cd_setor_atendimento_w	bigint := 0;


BEGIN

if (coalesce(nr_seq_interno_p,0) <> 0) then
	select	cd_setor_atendimento
	into STRICT	cd_setor_atendimento_w
	from	atend_paciente_unidade
	where	nr_seq_interno = nr_seq_interno_p;
end if;

if (coalesce(cd_setor_atendimento_w,0) = 0) then
	select	cd_setor_atendimento
	into STRICT	cd_setor_atendimento_w
	from	material_atend_paciente
	where	nr_sequencia	= nr_seq_item_p;
end if;

return	cd_setor_atendimento_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_setor_mat_auditoria ( nr_seq_interno_p bigint, nr_seq_item_p bigint) FROM PUBLIC;

