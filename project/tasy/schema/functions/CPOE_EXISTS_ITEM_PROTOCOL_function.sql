-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION cpoe_exists_item_protocol ( cd_protocolo_p bigint, nr_seq_protocolo_p bigint) RETURNS varchar AS $body$
DECLARE



ds_return_w	varchar(2000);

	function get_exists_item(ds_initials_p text) return text is

	ie_return_w varchar(2000);

	
BEGIN

	select	coalesce(max('S'),'N')
	into STRICT	ie_return_w
	from	cpoe_itens_protocolo_v a
	where	a.cd_protocolo = cd_protocolo_p
	and		a.nr_seq_protocolo = nr_seq_protocolo_p
	and		(a.ds_item IS NOT NULL AND a.ds_item::text <> '')
	and		a.ds_sigla = ds_initials_p;

	return ie_return_w;

	end;
begin

ds_return_w := get_exists_item('M');

	if (ds_return_w = 'N') then
		ds_return_w := get_exists_item('S');
		if (ds_return_w = 'N') then
			ds_return_w := get_exists_item('N');
			if (ds_return_w = 'N') then
				ds_return_w := get_exists_item('P');
				if (ds_return_w = 'N') then
					ds_return_w := get_exists_item('R');
					if (ds_return_w = 'N') then
						ds_return_w := get_exists_item('G');
						if (ds_return_w = 'N') then
							ds_return_w := get_exists_item('DI');
							if (ds_return_w = 'N') then
								ds_return_w := get_exists_item('MA');
								if (ds_return_w = 'N') then
									ds_return_w := get_exists_item('H');
								end if;
							end if;
						end if;
					end if;
				end if;
			end if;
		end if;
	end if;

	return ds_return_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION cpoe_exists_item_protocol ( cd_protocolo_p bigint, nr_seq_protocolo_p bigint) FROM PUBLIC;

