-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION plt_obter_max_nrprescricao () RETURNS bigint AS $body$
DECLARE


nr_prescricao_w 	bigint;


BEGIN

select	max(plt_obter_max_nr_prescricao(nr_prescricoes))
into STRICT	nr_prescricao_w
from	w_rep_t
where	coalesce(dt_atualizacao,clock_timestamp()) between(clock_timestamp() - interval '1 days') and (clock_timestamp() + interval '1 days');

return	nr_prescricao_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION plt_obter_max_nrprescricao () FROM PUBLIC;

