-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_convenio_agenda_paciente (nr_seq_agenda_p text) RETURNS varchar AS $body$
DECLARE

cd_convenio_w			bigint;


BEGIN

select	max(cd_convenio)
into STRICT 	cd_convenio_w
from	agenda_paciente
where  	nr_sequencia = nr_seq_agenda_p
and		(cd_convenio IS NOT NULL AND cd_convenio::text <> '')
order 	by dt_agenda;

return cd_convenio_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_convenio_agenda_paciente (nr_seq_agenda_p text) FROM PUBLIC;

