-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_qt_unitaria_md (qt_unitaria_p bigint, qt_unitaria_horario_p bigint, ds_dose_diferenciada_p text, ie_regra_disp_p text) RETURNS bigint AS $body$
DECLARE

  qt_unitaria_w				double precision;

BEGIN

  if (qt_unitaria_p = 0) then
    qt_unitaria_w := qt_unitaria_horario_p;
  elsif (ds_dose_diferenciada_p IS NOT NULL AND ds_dose_diferenciada_p::text <> '') then
    if (ie_regra_disp_p = 'S') then
      qt_unitaria_w := ceil(qt_unitaria_horario_p);
    else
      qt_unitaria_w := qt_unitaria_horario_p;
    end if;
  else
    qt_unitaria_w := qt_unitaria_p;
  end if;
  return qt_unitaria_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_qt_unitaria_md (qt_unitaria_p bigint, qt_unitaria_horario_p bigint, ds_dose_diferenciada_p text, ie_regra_disp_p text) FROM PUBLIC;

