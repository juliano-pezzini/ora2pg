-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_seq_laudo_padrao_js ( nr_prescricao_p bigint, nr_seq_prescricao_p bigint, nr_seq_laudo_p bigint) RETURNS bigint AS $body$
DECLARE


nr_seq_proc_interno_w	bigint;
nr_seq_laudo_padrao_w	bigint;


BEGIN

if (coalesce(nr_prescricao_p,0) > 0) and (coalesce(nr_seq_prescricao_p,0) > 0) then

	select	coalesce(max(nr_seq_proc_interno),0)
	into STRICT	nr_seq_proc_interno_w
	from	prescr_procedimento
	where	nr_prescricao = nr_prescricao_p
	and	nr_sequencia = nr_seq_prescricao_p;

	select	obter_laudo_padrao_montagem(nr_seq_laudo_p, nr_seq_proc_interno_w)
	into STRICT	nr_seq_laudo_padrao_w
	;

end if;

return	nr_seq_laudo_padrao_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_seq_laudo_padrao_js ( nr_prescricao_p bigint, nr_seq_prescricao_p bigint, nr_seq_laudo_p bigint) FROM PUBLIC;

