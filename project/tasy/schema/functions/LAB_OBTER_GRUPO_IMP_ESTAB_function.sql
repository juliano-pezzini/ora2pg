-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION lab_obter_grupo_imp_estab ( cd_estabelecimento_p bigint, nr_seq_exame_p bigint, cd_convenio_p bigint default null) RETURNS bigint AS $body$
DECLARE


nr_seq_grupo_imp_w	bigint;


BEGIN

select 	max(nr_seq_grupo_imp)
into STRICT	nr_seq_grupo_imp_w
from 	exame_lab_grupo_imp
where	nr_seq_exame = nr_seq_exame_p
and	coalesce(cd_estabelecimento, coalesce(cd_estabelecimento_p, 0)) = coalesce(cd_estabelecimento_p, 0)
and	coalesce(cd_convenio, coalesce(cd_convenio_p, 0)) = coalesce(cd_convenio_p, 0);

return nr_seq_grupo_imp_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION lab_obter_grupo_imp_estab ( cd_estabelecimento_p bigint, nr_seq_exame_p bigint, cd_convenio_p bigint default null) FROM PUBLIC;

