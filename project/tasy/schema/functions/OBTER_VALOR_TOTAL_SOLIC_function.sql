-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_valor_total_solic (nr_solic_compra_p bigint, nr_seq_proj_rec_p bigint) RETURNS bigint AS $body$
DECLARE


vl_retorno_w	bigint := 0;


BEGIN
select	sum(coalesce(qt_material, 0) * coalesce(vl_unit_previsto, 0))
into STRICT	vl_retorno_w
from	solic_compra_item
where	nr_solic_compra = nr_solic_compra_p
and	nr_seq_proj_rec = nr_seq_proj_rec_p;

return	vl_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_valor_total_solic (nr_solic_compra_p bigint, nr_seq_proj_rec_p bigint) FROM PUBLIC;

