-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_desc_classe_titulo_pagar (NR_SEQUENCIA_P bigint) RETURNS varchar AS $body$
DECLARE


DS_RETORNO_W	varchar(250);


BEGIN

IF (NR_SEQUENCIA_P IS NOT NULL AND NR_SEQUENCIA_P::text <> '') THEN
	SELECT	DS_CLASSE
	INTO STRICT		DS_RETORNO_W
	FROM		CLASSE_TITULO_PAGAR
	WHERE		NR_SEQUENCIA	= NR_SEQUENCIA_P;
END IF;

RETURN	DS_RETORNO_W;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_desc_classe_titulo_pagar (NR_SEQUENCIA_P bigint) FROM PUBLIC;

