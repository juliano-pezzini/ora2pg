-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_prontuario_sinan (nr_seq_notificacao_p bigint) RETURNS varchar AS $body$
DECLARE


nr_prontuario_w		bigint;
cd_paciente_w		varchar(10);


BEGIN

select	max(cd_paciente)
into STRICT	cd_paciente_w
from	notificacao_sinan
where	nr_sequencia = nr_seq_notificacao_p;

if (cd_paciente_w IS NOT NULL AND cd_paciente_w::text <> '') then
	nr_prontuario_w := obter_prontuario_pf(wheb_usuario_pck.get_cd_estabelecimento,cd_paciente_w);
end if;

return	nr_prontuario_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_prontuario_sinan (nr_seq_notificacao_p bigint) FROM PUBLIC;

