-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_setor_libera ( cd_setor_atendimento_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint, cd_perfil_p bigint, ds_setores_p text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w		varchar(1) := 'S';
VarSetoreNaoLibera_w	varchar(255);

BEGIN
if (ds_setores_p IS NOT NULL AND ds_setores_p::text <> '') then
	select	CASE WHEN obter_se_contido_char(cd_setor_atendimento_p, ds_setores_p)='S' THEN 'N' WHEN obter_se_contido_char(cd_setor_atendimento_p, ds_setores_p)='N' THEN 'S' END
	into STRICT	ds_retorno_w
	;
end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_setor_libera ( cd_setor_atendimento_p bigint, nm_usuario_p text, cd_estabelecimento_p bigint, cd_perfil_p bigint, ds_setores_p text) FROM PUBLIC;

