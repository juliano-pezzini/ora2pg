-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_origem_operador ( nr_seq_operador_p bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w			varchar(2);


BEGIN

select	max(ie_origem_atendimento)
into STRICT	ds_retorno_w
from	pls_operador_tipo_atend
where	nr_seq_operador = nr_seq_operador_p
and	ie_padrao = 'S';

if (coalesce(ds_retorno_w::text, '') = '') then
	select	max(ie_origem_atendimento)
	into STRICT	ds_retorno_w
	from	pls_operador_tipo_atend
	where	nr_seq_operador = nr_seq_operador_p;
end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_origem_operador ( nr_seq_operador_p bigint) FROM PUBLIC;

