-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_legenda_grupo_exames_js ( nr_seq_grupo_p bigint) RETURNS bigint AS $body$
DECLARE


nr_seq_legenda_w	bigint;
ie_possui_exames_w	varchar(1);


BEGIN


select	coalesce(max('S'),'N')
into STRICT	ie_possui_exames_w
from    exame_laboratorio
where   nr_seq_grupo = nr_seq_grupo_p;

if (ie_possui_exames_w = 'S') then
	nr_seq_legenda_w := 269;
else
	nr_seq_legenda_w := 271;
end if;

return	nr_seq_legenda_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_legenda_grupo_exames_js ( nr_seq_grupo_p bigint) FROM PUBLIC;

