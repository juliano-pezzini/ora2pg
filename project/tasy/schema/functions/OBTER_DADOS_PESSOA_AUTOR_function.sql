-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_dados_pessoa_autor ( cd_pessoa_fisica_p text, nr_sequencia_autor_p bigint, ie_opcao_p text) RETURNS varchar AS $body$
DECLARE

 
dt_obito_w			timestamp;
nr_atendimento_w		bigint;
nr_seq_gestao_w			bigint;
nr_seq_autor_cirurgia_w		bigint;
nr_seq_agenda_w			bigint;
nr_seq_agenda_consulta_w	bigint;
nr_seq_age_integ_w		bigint;
nr_seq_paciente_setor_w		bigint;
cd_pessoa_fisica_w		varchar(10);
ds_retorno_w			varchar(255);

/* 
DO - Data do óbito 
 
*/
 
 

BEGIN 
 
if (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') then 
 
	select 	to_char(dt_obito,'dd/mm/yyyy hh24:mi:ss') 
	into STRICT	ds_retorno_w 
	from	pessoa_fisica 
	where	cd_pessoa_fisica = cd_pessoa_fisica_p;
	 
elsif (nr_sequencia_autor_p IS NOT NULL AND nr_sequencia_autor_p::text <> '') then 
	select	nr_atendimento, 
		nr_seq_gestao, 
		nr_seq_autor_cirurgia, 
		nr_seq_agenda, 
		nr_seq_agenda_consulta, 
		nr_seq_age_integ, 
		nr_seq_paciente_setor 
	into STRICT	nr_atendimento_w, 
		nr_seq_gestao_w, 
		nr_seq_autor_cirurgia_w, 
		nr_seq_agenda_w, 
		nr_seq_agenda_consulta_w, 
		nr_seq_age_integ_w, 
		nr_seq_paciente_setor_w 
	from	autorizacao_convenio 
	where	nr_sequencia = nr_sequencia_autor_p;
	 
	if (nr_atendimento_w IS NOT NULL AND nr_atendimento_w::text <> '') then 
		select	cd_pessoa_fisica 
		into STRICT	cd_pessoa_fisica_w 
		from	atendimento_paciente 
		where	nr_atendimento = nr_atendimento_w;
	elsif (nr_seq_agenda_w IS NOT NULL AND nr_seq_agenda_w::text <> '') then 
		select	cd_pessoa_fisica 
		into STRICT	cd_pessoa_fisica_w 
		from	agenda_paciente 
		where	nr_sequencia = nr_seq_agenda_w;
	elsif (nr_seq_agenda_consulta_w IS NOT NULL AND nr_seq_agenda_consulta_w::text <> '') then 
		select	cd_pessoa_fisica 
		into STRICT	cd_pessoa_fisica_w 
		from	agenda_consulta 
		where	nr_sequencia = nr_seq_agenda_consulta_w;
	elsif (nr_seq_age_integ_w IS NOT NULL AND nr_seq_age_integ_w::text <> '') then 
		select	cd_pessoa_fisica 
		into STRICT	cd_pessoa_fisica_w 
		from	agenda_integrada 
		where	nr_sequencia = nr_seq_age_integ_w;
	elsif (nr_seq_gestao_w IS NOT NULL AND nr_seq_gestao_w::text <> '') then 
		select	cd_pessoa_fisica 
		into STRICT	cd_pessoa_fisica_w 
		from	gestao_vaga 
		where	nr_sequencia = nr_seq_gestao_w;
	elsif (nr_seq_paciente_setor_w IS NOT NULL AND nr_seq_paciente_setor_w::text <> '') then 
		select	cd_pessoa_fisica 
		into STRICT	cd_pessoa_fisica_w 
		from	paciente_setor 
		where	nr_seq_paciente = nr_seq_paciente_setor_w;
	end if;
	 
	if (cd_pessoa_fisica_w IS NOT NULL AND cd_pessoa_fisica_w::text <> '') and (ie_opcao_p = 'DO')then 
		begin 
		select 	to_char(dt_obito,'dd/mm/yyyy hh24:mi:ss') 
		into STRICT	ds_retorno_w 
		from	pessoa_fisica 
		where	cd_pessoa_fisica = cd_pessoa_fisica_w;
		end;
	end if;
end if;
 
return	ds_retorno_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_dados_pessoa_autor ( cd_pessoa_fisica_p text, nr_sequencia_autor_p bigint, ie_opcao_p text) FROM PUBLIC;

