-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_exibe_sala_vis_inte (cd_agenda_p bigint, cd_perfil_p bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(1) := 'N';
qt_agendas_w	bigint  := 0;


BEGIN

Select	count(*)
into STRICT	qt_agendas_w
from	agenda_visualizacao_integ;

if (qt_agendas_w > 0) then
	select 	count(*)
	into STRICT	qt_agendas_w
	from	agenda_visualizacao_integ
	where	cd_agenda = cd_agenda_p
	and	coalesce(cd_perfil, cd_perfil_p)  = cd_perfil_p;


	if (qt_agendas_w > 0) then

		ds_retorno_w := 'S';

	end if;

else
	ds_retorno_w := 'S';

end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_exibe_sala_vis_inte (cd_agenda_p bigint, cd_perfil_p bigint) FROM PUBLIC;

