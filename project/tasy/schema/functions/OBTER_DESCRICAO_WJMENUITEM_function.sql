-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_descricao_wjmenuitem ( ds_texto_p text, ie_atalho_p text, ie_ctrl_p text, ie_shift_p text, ie_alt_p text) RETURNS varchar AS $body$
DECLARE


ds_descricao_w	varchar(255);


BEGIN
if (ds_texto_p IS NOT NULL AND ds_texto_p::text <> '') then
	begin
	ds_descricao_w	:= ds_texto_p;
	if (ie_atalho_p IS NOT NULL AND ie_atalho_p::text <> '') then
		begin
		if (coalesce(ie_ctrl_p,'N') = 'N') and (coalesce(ie_shift_p,'N') = 'N') and (coalesce(ie_alt_p,'N') = 'N') then
			begin
			ds_descricao_w	:= ds_descricao_w || ' ' || ie_atalho_p;
			end;
		elsif (coalesce(ie_ctrl_p,'N') = 'S') and (coalesce(ie_shift_p,'N') = 'S') and (coalesce(ie_alt_p,'N') = 'S') then
			begin
			ds_descricao_w	:= ds_descricao_w || ' Ctrl+Shift+Alt' || ie_atalho_p;
			end;
		elsif (coalesce(ie_ctrl_p,'N') = 'S') and (coalesce(ie_shift_p,'N') = 'S') and (coalesce(ie_alt_p,'N') = 'N') then
			begin
			ds_descricao_w	:= ds_descricao_w || ' Ctrl+Shift+' || ie_atalho_p;
			end;
		elsif (coalesce(ie_ctrl_p,'N') = 'S') and (coalesce(ie_shift_p,'N') = 'N') and (coalesce(ie_alt_p,'N') = 'S') then
			begin
			ds_descricao_w	:= ds_descricao_w || ' Ctrl+Alt+' || ie_atalho_p;
			end;
		elsif (coalesce(ie_ctrl_p,'N') = 'S') and (coalesce(ie_shift_p,'N') = 'N') and (coalesce(ie_alt_p,'N') = 'N') then
			begin
			ds_descricao_w	:= ds_descricao_w || ' Ctrl+' || ie_atalho_p;
			end;
		elsif (coalesce(ie_ctrl_p,'N') = 'N') and (coalesce(ie_shift_p,'N') = 'S') and (coalesce(ie_alt_p,'N') = 'S') then
			begin
			ds_descricao_w	:= ds_descricao_w || ' Shift+Alt' || ie_atalho_p;
			end;
		elsif (coalesce(ie_ctrl_p,'N') = 'N') and (coalesce(ie_shift_p,'N') = 'S') and (coalesce(ie_alt_p,'N') = 'N') then
			begin
			ds_descricao_w	:= ds_descricao_w || ' Shift+' || ie_atalho_p;
			end;
		elsif (coalesce(ie_ctrl_p,'N') = 'N') and (coalesce(ie_shift_p,'N') = 'N') and (coalesce(ie_alt_p,'N') = 'S') then
			begin
			ds_descricao_w	:= ds_descricao_w || ' Alt+' || ie_atalho_p;
			end;
		end if;
		end;
	end if;
	end;
end if;
return ds_descricao_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_descricao_wjmenuitem ( ds_texto_p text, ie_atalho_p text, ie_ctrl_p text, ie_shift_p text, ie_alt_p text) FROM PUBLIC;

