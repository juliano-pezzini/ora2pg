-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_qt_proj_grupo (nr_seq_gerencia_p bigint, nr_seq_grupo_des_p bigint) RETURNS bigint AS $body$
DECLARE


qt_projeto_w			bigint;


BEGIN

select	count(*)
into STRICT	qt_projeto_w
from	proj_projeto
where	((nr_seq_grupo_des_p = 0) or (nr_seq_grupo_des		= nr_seq_grupo_des_p))
and		((nr_seq_gerencia_p = 0) or (NR_SEQ_GERENCIA		= nr_seq_gerencia_p))
and	not ie_status in ('F','T');

return	qt_projeto_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_qt_proj_grupo (nr_seq_gerencia_p bigint, nr_seq_grupo_des_p bigint) FROM PUBLIC;

