-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION replace_clob2 (ds_valor_p text, ds_localizar_p text, ds_substituir_p text) RETURNS text AS $body$
DECLARE

  l_pos integer;

BEGIN
  l_pos := position(ds_localizar_p in ds_valor_p);
  IF l_pos > 0 THEN
    RETURN SUBSTR(ds_valor_p, 1, l_pos-1)
        || ds_substituir_p
        || SUBSTR(ds_valor_p, l_pos+LENGTH(ds_localizar_p));
  END IF;
  RETURN ds_valor_p;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION replace_clob2 (ds_valor_p text, ds_localizar_p text, ds_substituir_p text) FROM PUBLIC;

