-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION buscar_campo_tiss_env_doc (nr_sequencia_p bigint, ie_tabela_origem_p text, ds_atributo_p text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w		varchar(255);


BEGIN


if (ie_tabela_origem_p = 'A') then
	if (ds_atributo_p = 'NR_PROTOC_REC_OPERADORA') then
		select max(a.NR_PROTOC_REC_OPERADORA)
		into STRICT ds_retorno_w
		from tiss_envio_documento a
		where a.nr_seq_autor_conv_anexo = nr_sequencia_p;
	end if;

	if (ds_atributo_p = 'DS_ERRO_COMUNICACAO') then
		select max(a.DS_ERRO_COMUNICACAO)
		into STRICT ds_retorno_w
		from tiss_envio_documento a
		where a.nr_seq_autor_conv_anexo = nr_sequencia_p;
	end if;

	if (ds_atributo_p = 'DS_OBSERVACAO_OPERADORA') then
		select max(a.DS_OBSERVACAO_OPERADORA)
		into STRICT ds_retorno_w
		from tiss_envio_documento a
		where a.nr_seq_autor_conv_anexo = nr_sequencia_p;
	end if;
end if;

if (ie_tabela_origem_p = 'P') then
	if (ds_atributo_p = 'NR_PROTOC_REC_OPERADORA') then
		select max(a.NR_PROTOC_REC_OPERADORA)
		into STRICT ds_retorno_w
		from tiss_envio_documento a
		where a.nr_seq_protoc_conv_anexo  = nr_sequencia_p;
	end if;

	if (ds_atributo_p = 'DS_ERRO_COMUNICACAO') then
		select max(a.DS_ERRO_COMUNICACAO)
		into STRICT ds_retorno_w
		from tiss_envio_documento a
		where a.nr_seq_protoc_conv_anexo  = nr_sequencia_p;
	end if;

	if (ds_atributo_p = 'DS_OBSERVACAO_OPERADORA') then
		select max(a.DS_OBSERVACAO_OPERADORA)
		into STRICT ds_retorno_w
		from tiss_envio_documento  a
		where a.nr_seq_protoc_conv_anexo  = nr_sequencia_p;
	end if;
end if;

if (ie_tabela_origem_p = 'G') then
	if (ds_atributo_p = 'NR_PROTOC_REC_OPERADORA') then
		select max(a.NR_PROTOC_REC_OPERADORA)
		into STRICT ds_retorno_w
		from tiss_envio_documento a
		where a.nr_seq_lote_hist_anexo  = nr_sequencia_p;
	end if;

	if (ds_atributo_p = 'DS_ERRO_COMUNICACAO') then
		select max(a.DS_ERRO_COMUNICACAO)
		into STRICT ds_retorno_w
		from tiss_envio_documento a
		where a.nr_seq_lote_hist_anexo  = nr_sequencia_p;
	end if;

	if (ds_atributo_p = 'DS_OBSERVACAO_OPERADORA') then
		select max(a.DS_OBSERVACAO_OPERADORA)
		into STRICT ds_retorno_w
		from tiss_envio_documento a
		where a.nr_seq_lote_hist_anexo  = nr_sequencia_p;
	end if;
end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION buscar_campo_tiss_env_doc (nr_sequencia_p bigint, ie_tabela_origem_p text, ds_atributo_p text) FROM PUBLIC;

