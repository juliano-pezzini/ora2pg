-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_exame_lido (nr_seq_prescr_p bigint, nr_seq_prescr_item_p bigint, ds_login_p text, ie_tipo_item_p text) RETURNS varchar AS $body$
DECLARE


nr_seq_view_w	wsuite_result_view.nr_sequencia%type;
ie_retorno_w		varchar(1) := 'N';


BEGIN

if (nr_seq_prescr_p > 0) and (nr_seq_prescr_item_p > 0) and (ds_login_p IS NOT NULL AND ds_login_p::text <> '') then

    select 	coalesce(max(nr_sequencia),0)
    into STRICT	nr_seq_view_w
    from	wsuite_result_view
    where 	nr_seq_prescr = nr_seq_prescr_p
    and		nr_seq_prescr_item = nr_seq_prescr_item_p
    and		ds_login = ds_login_p
    and		ie_tipo_item = ie_tipo_item_p;
    
    if (nr_seq_view_w IS NOT NULL AND nr_seq_view_w::text <> '') and (nr_seq_view_w > 0) then
        ie_retorno_w := 'S';
    end if;
end if;

return ie_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_exame_lido (nr_seq_prescr_p bigint, nr_seq_prescr_item_p bigint, ds_login_p text, ie_tipo_item_p text) FROM PUBLIC;

