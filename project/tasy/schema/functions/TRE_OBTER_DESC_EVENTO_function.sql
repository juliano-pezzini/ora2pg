-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION tre_obter_desc_evento (nr_seq_evento_p bigint) RETURNS varchar AS $body$
DECLARE


ds_evento_w	varchar(80);


BEGIN
if (nr_seq_evento_p IS NOT NULL AND nr_seq_evento_p::text <> '') then
	select	ds_evento
	into STRICT	ds_evento_w
	from	tre_evento
	where	nr_sequencia = nr_seq_evento_p;
end if;
return	ds_evento_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION tre_obter_desc_evento (nr_seq_evento_p bigint) FROM PUBLIC;

