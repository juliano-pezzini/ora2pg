-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION sup_obter_valor_tit_nf ( nr_seq_nota_fiscal_p bigint, dt_vencimento_p timestamp) RETURNS bigint AS $body$
DECLARE

 
nr_titulo_w	bigint;
nr_nota_fiscal_w	varchar(255);
vl_retorno_w	double precision;


BEGIN 
 
select	nr_nota_fiscal 
into STRICT	nr_nota_fiscal_w 
from	nota_fiscal 
where	nr_sequencia = nr_seq_nota_fiscal_p;
 
nr_titulo_w	:= null;
 
/* primeiro busca nos títulos a pagar */
 
select	max(nr_titulo) 
into STRICT	nr_titulo_w 
from	titulo_pagar 
where	nr_seq_nota_fiscal		= nr_seq_nota_fiscal_p 
and	trunc(dt_vencimento_original, 'dd')	= trunc(dt_vencimento_p, 'dd');
 
 
/* se não achou procura nos títulos a receber pela sequencia da nota */
 
if (coalesce(nr_titulo_w::text, '') = '') then 
	begin 
 
	select	max(nr_titulo) 
	into STRICT	nr_titulo_w 
	from	titulo_receber 
	where	nr_seq_nf_saida			= nr_seq_nota_fiscal_p 
	and	trunc(dt_vencimento, 'dd')	= trunc(dt_vencimento_p, 'dd');
 
	/* se não achou, busca então pelo número da nota */
 
	if (coalesce(nr_titulo_w::text, '') = '') then 
 
		select	max(nr_titulo) 
		into STRICT	nr_titulo_w 
		from	titulo_receber 
		where	nr_nota_fiscal			= nr_nota_fiscal_w 
		and	(nr_nota_fiscal IS NOT NULL AND nr_nota_fiscal::text <> '') 
		and	coalesce(nr_seq_nf_saida::text, '') = '' 
		and	trunc(dt_vencimento, 'dd')	= trunc(dt_vencimento_p, 'dd');
		 
	end if;
 
	if (nr_titulo_w IS NOT NULL AND nr_titulo_w::text <> '') then 
		begin 
 
		select	(obter_dados_titulo_receber(nr_titulo_w,'V'))::numeric  
		into STRICT	vl_retorno_w 
		;
 
		end;
	end if;
 
	end;
else 
	begin 
 
	select	(obter_dados_tit_pagar(nr_titulo_w,'V'))::numeric  
	into STRICT	vl_retorno_w 
	;
 
	end;
end if;
 
return	vl_retorno_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION sup_obter_valor_tit_nf ( nr_seq_nota_fiscal_p bigint, dt_vencimento_p timestamp) FROM PUBLIC;

