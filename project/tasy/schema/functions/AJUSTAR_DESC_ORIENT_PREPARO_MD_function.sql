-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION ajustar_desc_orient_preparo_md ( ds_dose_diferenciada_p text, nr_posicao_p bigint, nr_horarios_pos_p bigint, ds_horarios_p text, ds_orientacao_preparo_p text, ds_separar_p text, nr_rediluente_pos_p bigint, ds_rediluente_p text, nr_administrar_pos_p bigint, ie_aplicar_material_p text, ds_administrar_p text ) RETURNS varchar AS $body$
DECLARE

  ds_orientacao_preparo_w varchar(4000) := ds_orientacao_preparo_p;

BEGIN
	if ((ds_dose_diferenciada_p IS NOT NULL AND ds_dose_diferenciada_p::text <> '') and nr_posicao_p <= nr_horarios_pos_p and (ds_horarios_p IS NOT NULL AND ds_horarios_p::text <> '')) then
		ds_orientacao_preparo_w := ds_orientacao_preparo_w || ds_horarios_p;
	end if;

	if (ds_separar_p IS NOT NULL AND ds_separar_p::text <> '') then
		ds_orientacao_preparo_w := ds_orientacao_preparo_w || ds_separar_p;
	end if;

	if (nr_posicao_p <= nr_rediluente_pos_p and (ds_rediluente_p IS NOT NULL AND ds_rediluente_p::text <> '')) then
		ds_orientacao_preparo_w := ds_orientacao_preparo_w || ds_rediluente_p;
	end if;

	if ((ds_administrar_p IS NOT NULL AND ds_administrar_p::text <> '') and ie_aplicar_material_p <> 'N' and nr_posicao_p <= nr_administrar_pos_p) then
		ds_orientacao_preparo_w := ds_orientacao_preparo_w || ds_administrar_p;
	end if;
	
	return ds_orientacao_preparo_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION ajustar_desc_orient_preparo_md ( ds_dose_diferenciada_p text, nr_posicao_p bigint, nr_horarios_pos_p bigint, ds_horarios_p text, ds_orientacao_preparo_p text, ds_separar_p text, nr_rediluente_pos_p bigint, ds_rediluente_p text, nr_administrar_pos_p bigint, ie_aplicar_material_p text, ds_administrar_p text ) FROM PUBLIC;

