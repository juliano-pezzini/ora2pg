-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_regra_agenda_sus (nr_Seq_regra_p bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(1);


BEGIN

select	coalesce(max('S'),'N')
into STRICT	ds_retorno_w
from	agenda_Regra
where	nr_sequencia		= nr_Seq_regra_p
and	((ie_origem_proced	= 7 AND cd_procedimento IS NOT NULL AND cd_procedimento::text <> '')
or (nr_seq_grupo IS NOT NULL AND nr_seq_grupo::text <> '')
or (nr_seq_subgrupo IS NOT NULL AND nr_seq_subgrupo::text <> '')
or (nr_seq_forma_org IS NOT NULL AND nr_seq_forma_org::text <> ''));

return	ds_Retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_regra_agenda_sus (nr_Seq_regra_p bigint) FROM PUBLIC;

