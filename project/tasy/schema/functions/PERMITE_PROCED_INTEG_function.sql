-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION permite_proced_integ ( nr_seq_interno_p bigint, nr_seq_sistema_integ_p bigint ) RETURNS varchar AS $body$
DECLARE



ds_retorno_w	varchar(1);


BEGIN

select	CASE WHEN count(*)=0 THEN 'N'  ELSE 'S' END
into STRICT	ds_retorno_w
from	prescr_procedimento a,
	procedimento b,
	proc_interno_integracao c,
	prescr_medica d
where	a.cd_procedimento  = b.cd_procedimento
and	a.ie_origem_proced = b.ie_origem_proced
and	d.nr_prescricao	   = a.nr_prescricao
and	coalesce(c.nr_seq_proc_interno, coalesce(a.nr_seq_proc_interno,0)) = coalesce(a.nr_seq_proc_interno,0)
and	coalesce(c.cd_procedimento, coalesce(a.cd_procedimento,0)) = coalesce(a.cd_procedimento,0)
and	coalesce(c.ie_origem_proced, coalesce(a.ie_origem_proced,0)) = coalesce(a.ie_origem_proced,0)
and	coalesce(c.cd_tipo_procedimento, coalesce(b.cd_tipo_procedimento,0)) = coalesce(b.cd_tipo_procedimento,0)
and	coalesce(c.cd_estabelecimento, coalesce(d.cd_estabelecimento,0)) = coalesce(d.cd_estabelecimento,0)
and	a.nr_seq_interno = nr_seq_interno_p
and	c.nr_seq_sistema_integ 	= nr_seq_sistema_integ_p;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION permite_proced_integ ( nr_seq_interno_p bigint, nr_seq_sistema_integ_p bigint ) FROM PUBLIC;

