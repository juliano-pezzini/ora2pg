-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION calcular_score_rev_geneva_md ( ie_idade_maior65_p text, ie_tvp_tep_previo_p text, ie_cirurgia_ult_mes_p text, ie_neoplasia_maligna_p text, ie_dor_unilateral_p text, ie_hemoptise_p text, ie_fc_7594_bpm_p text, ie_fc_maior94_bpm_p text, ie_edema_unilateral_p text ) RETURNS bigint AS $body$
DECLARE

    qt_score_w smallint := 0;

BEGIN
    if coalesce(ie_idade_maior65_p, 'N') = 'S' then
        qt_score_w := qt_score_w + 1;
    end if;

    if coalesce(ie_tvp_tep_previo_p, 'N') = 'S' then 
        qt_score_w := qt_score_w + 3;
    end if;

    if coalesce(ie_cirurgia_ult_mes_p, 'N') = 'S' then 
        qt_score_w := qt_score_w + 2;
    end if;

    if coalesce(ie_neoplasia_maligna_p, 'N') = 'S' then 
        qt_score_w := qt_score_w + 2;
    end if;

    if coalesce(ie_dor_unilateral_p, 'N') = 'S' then 
        qt_score_w := qt_score_w + 3;
    end if;

    if coalesce(ie_hemoptise_p, 'N') = 'S' then 
        qt_score_w := qt_score_w + 2;
    end if;

    if coalesce(ie_fc_7594_bpm_p, 'N') = 'S' then 
        qt_score_w := qt_score_w + 3;
    end if;

    if coalesce(ie_fc_maior94_bpm_p, 'N') = 'S' then 
        qt_score_w := qt_score_w + 5;
    end if;

    if coalesce(ie_edema_unilateral_p, 'N') = 'S' then 
        qt_score_w := qt_score_w + 4;
    end if;

    return qt_score_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION calcular_score_rev_geneva_md ( ie_idade_maior65_p text, ie_tvp_tep_previo_p text, ie_cirurgia_ult_mes_p text, ie_neoplasia_maligna_p text, ie_dor_unilateral_p text, ie_hemoptise_p text, ie_fc_7594_bpm_p text, ie_fc_maior94_bpm_p text, ie_edema_unilateral_p text ) FROM PUBLIC;

