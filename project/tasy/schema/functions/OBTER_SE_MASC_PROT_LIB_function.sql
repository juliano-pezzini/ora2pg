-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_masc_prot_lib ( nr_sequencia_p bigint, cd_perfil_p bigint, cd_estabelecimento_p bigint, cd_setor_atendimento_p bigint) RETURNS varchar AS $body$
DECLARE

ds_retorno_w	varchar(1) := 'S';
qt_registro_w	bigint;

BEGIN

select	count(*)
into STRICT	qt_registro_w
from	med_mascara_prot_regra
where	nr_seq_protocolo = nr_sequencia_p;

if (qt_registro_w > 0) then
	select	coalesce(max('S'),'N')
	into STRICT	ds_retorno_w
	from	med_mascara_prot_regra
	where	nr_seq_protocolo 				= nr_sequencia_p
	and	coalesce(cd_perfil,cd_perfil_p)			= cd_perfil_p
	and	coalesce(cd_estabelecimento,cd_estabelecimento_p) 	= cd_estabelecimento_p
	and	coalesce(cd_setor_atendimento,cd_setor_atendimento_p)= cd_setor_atendimento_p;

end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_masc_prot_lib ( nr_sequencia_p bigint, cd_perfil_p bigint, cd_estabelecimento_p bigint, cd_setor_atendimento_p bigint) FROM PUBLIC;

