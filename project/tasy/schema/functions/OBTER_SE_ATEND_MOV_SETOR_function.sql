-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_atend_mov_setor ( nr_atendimento_p bigint, cd_setor_atendimento_p bigint, dt_inicial_p timestamp, dt_final_p timestamp) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(2);

BEGIN

select	 CASE WHEN count(*)=0 THEN 'N'  ELSE 'S' END
into STRICT	 ds_retorno_w
from	 atend_paciente_unidade a
where	 a.nr_atendimento = nr_atendimento_p
and	 a.cd_setor_atendimento = cd_setor_atendimento_p
and	 trunc(dt_entrada_unidade) >= trunc(dt_inicial_p)
and	 trunc(coalesce(dt_saida_unidade,dt_final_p)) <= trunc(dt_final_p);

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_atend_mov_setor ( nr_atendimento_p bigint, cd_setor_atendimento_p bigint, dt_inicial_p timestamp, dt_final_p timestamp) FROM PUBLIC;

