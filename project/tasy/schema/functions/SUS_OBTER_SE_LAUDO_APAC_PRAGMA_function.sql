-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';



--
-- dblink wrapper to call function sus_obter_se_laudo_apac_pragma as an autonomous transaction
--
CREATE EXTENSION IF NOT EXISTS dblink;

CREATE OR REPLACE FUNCTION sus_obter_se_laudo_apac_pragma (nr_atendimento_p atendimento_paciente.nr_atendimento%type) RETURNS bigint AS $body$
DECLARE
	-- Change this to reflect the dblink connection string
	v_conn_str  text := format('port=%s dbname=%s user=%s', current_setting('port'), current_database(), current_user);
	v_query     text;

	v_ret	bigint;
BEGIN
	v_query := 'SELECT * FROM sus_obter_se_laudo_apac_pragma_atx ( ' || quote_nullable(nr_atendimento_p) || ' )';
	SELECT * INTO v_ret FROM dblink(v_conn_str, v_query) AS p (ret bigint);
	RETURN v_ret;

END;
$body$ LANGUAGE plpgsql SECURITY DEFINER;




CREATE OR REPLACE FUNCTION sus_obter_se_laudo_apac_pragma_atx (nr_atendimento_p atendimento_paciente.nr_atendimento%type) RETURNS bigint AS $body$
DECLARE


qt_reg_apac_w		bigint := 0;
BEGIN

begin
select	count(*)
into STRICT	qt_reg_apac_w
from	sus_laudo_paciente
where	nr_atendimento = nr_atendimento_p
and	ie_classificacao in (11,12,13,14);
exception
when others then
	qt_reg_apac_w := 0;
end;

return	qt_reg_apac_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION sus_obter_se_laudo_apac_pragma (nr_atendimento_p atendimento_paciente.nr_atendimento%type) FROM PUBLIC; -- REVOKE ALL ON FUNCTION sus_obter_se_laudo_apac_pragma_atx (nr_atendimento_p atendimento_paciente.nr_atendimento%type) FROM PUBLIC;

