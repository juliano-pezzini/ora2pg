-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_desc_proc_pesq (nr_seq_proc_p bigint) RETURNS varchar AS $body$
DECLARE


nr_seq_proc_interno_w	bigint;
ds_procedimento_w		varchar(255);


BEGIN
if (nr_seq_proc_p IS NOT NULL AND nr_seq_proc_p::text <> '') then
	select	nr_seq_proc_interno
	into STRICT	nr_seq_proc_interno_w
	from	pq_procedimento
	where	nr_sequencia = nr_seq_proc_p;

	if (coalesce(nr_seq_proc_interno_w,0) > 0) then
		select	obter_desc_proc_interno(nr_seq_proc_interno_w)
		into STRICT	ds_procedimento_w
		;
	end if;
end if;

RETURN	ds_procedimento_w;

END	;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_desc_proc_pesq (nr_seq_proc_p bigint) FROM PUBLIC;

