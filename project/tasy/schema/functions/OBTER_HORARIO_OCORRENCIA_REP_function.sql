-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_horario_ocorrencia_rep ( ds_horarios_p text, nr_ocorrencia_p bigint) RETURNS varchar AS $body$
DECLARE



ds_horarios_w varchar(2000);
hr_horario_w varchar(7);
i  integer;
nr_ocorrencia_w double precision := 0;
hr_horario_ww varchar(7);


BEGIN

if (ds_horarios_p IS NOT NULL AND ds_horarios_p::text <> '') then
	begin
	ds_horarios_w	:= ds_horarios_p;
	ds_horarios_w	:= ds_horarios_w || ' ';

	while(ds_horarios_w IS NOT NULL AND ds_horarios_w::text <> '') loop
		begin
		select position(' ' in ds_horarios_w)
		into STRICT i
		;

		if (i > 1) and ((substr(ds_horarios_w, 1, i-1) IS NOT NULL AND (substr(ds_horarios_w, 1, i-1))::text <> '')) then
			hr_horario_w	:= substr(ds_horarios_w, 1, i-1);
			hr_horario_w 	:= replace(hr_horario_w, ' ', '');
			ds_horarios_w 	:= substr(ds_horarios_w, i+1, 2000);

			nr_ocorrencia_w := nr_ocorrencia_w + 1;


			if (nr_ocorrencia_w = nr_ocorrencia_p) then
				hr_horario_ww	:= hr_horario_w;
			end if;

		elsif (ds_horarios_w IS NOT NULL AND ds_horarios_w::text <> '') then
			ds_horarios_w := '';
		end if;
		end;
	end loop;
	exception
	when others then
	nr_ocorrencia_w := 0;
	end;
end if;

return hr_horario_ww;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_horario_ocorrencia_rep ( ds_horarios_p text, nr_ocorrencia_p bigint) FROM PUBLIC;

