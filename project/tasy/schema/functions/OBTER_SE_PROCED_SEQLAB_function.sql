-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_proced_seqlab (nr_seq_lab_p text, nr_seq_proced_p bigint, nr_prescricao_p bigint) RETURNS varchar AS $body$
DECLARE

nr_quantidade_w		bigint;
ds_retorno_w		varchar(50);

BEGIN

select 	count(*)
into STRICT	nr_quantidade_w
from	prescr_procedimento
where	nr_sequencia = nr_seq_proced_p
and	nr_prescricao = nr_prescricao_p
and	nr_seq_lab = nr_seq_lab_p;

if (nr_quantidade_w > 0) then
	ds_retorno_w := 'S';
else
	ds_retorno_w := 'N';
end if;


return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_proced_seqlab (nr_seq_lab_p text, nr_seq_proced_p bigint, nr_prescricao_p bigint) FROM PUBLIC;

