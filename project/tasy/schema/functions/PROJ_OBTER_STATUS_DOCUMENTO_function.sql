-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION proj_obter_status_documento ( ie_status_w text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w		varchar(1);


BEGIN
select	max(a.ds_estagio)
into STRICT	ds_retorno_w
from	proj_estagio_projetos a
where	exists (SELECT	1
		from	proj_doc_obrigatorio b
		where	b.nr_seq_estagio = a.nr_sequencia
		and	b.ds_documento = ie_status_w);

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION proj_obter_status_documento ( ie_status_w text) FROM PUBLIC;

