-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_se_regra_preco ( nr_seq_regra_p pls_regra_preco_proc.nr_sequencia%type, ie_tipo_regra_p text) RETURNS varchar AS $body$
DECLARE

 
/* 
Retorna S caso existir a regra de preço e N caso contrário. 
*/
 
									 
ds_retorno_w		varchar(255);


BEGIN 
 
if (ie_tipo_regra_p = 'P') then 
	Select	CASE WHEN count(1)=0 THEN 'N'  ELSE 'S' END  
	into STRICT	ds_retorno_w 
	from	pls_regra_preco_proc 
	where	nr_sequencia = nr_seq_regra_p;
	 
elsif (ie_tipo_regra_p = 'M') then 
	Select	CASE WHEN count(1)=0 THEN 'N'  ELSE 'S' END  
	into STRICT	ds_retorno_w 
	from	pls_regra_preco_mat 
	where	nr_sequencia = nr_seq_regra_p;
	 
else 
	Select	CASE WHEN count(1)=0 THEN 'N'  ELSE 'S' END  
	into STRICT	ds_retorno_w 
	from	pls_regra_preco_servico 
	where	nr_sequencia = nr_seq_regra_p;
end if;
 
Return	ds_retorno_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_se_regra_preco ( nr_seq_regra_p pls_regra_preco_proc.nr_sequencia%type, ie_tipo_regra_p text) FROM PUBLIC;

