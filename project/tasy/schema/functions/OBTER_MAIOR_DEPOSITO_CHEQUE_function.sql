-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_maior_deposito_cheque ( nr_seq_cheque_p bigint) RETURNS bigint AS $body$
DECLARE


nr_seq_deposito_w	deposito.nr_sequencia%type;
nr_seq_conta_w		deposito.nr_conta%type;


BEGIN

select	max(coalesce(b.nr_sequencia,0))
into STRICT	nr_seq_deposito_w
from    deposito b,
        deposito_cheque a
where   a.nr_seq_cheque		= nr_seq_cheque_p
and     a.nr_seq_deposito	= b.nr_sequencia;

if (nr_seq_deposito_w > 0) then
	select	max(coalesce(b.nr_conta,0))
	into STRICT	nr_seq_conta_w
	from	deposito b,
		deposito_cheque a
	where	a.nr_seq_cheque		= nr_seq_cheque_p
	and	b.nr_sequencia		= nr_seq_deposito_w
	and	a.nr_seq_deposito	= b.nr_sequencia;
end if;

return	nr_seq_conta_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_maior_deposito_cheque ( nr_seq_cheque_p bigint) FROM PUBLIC;

