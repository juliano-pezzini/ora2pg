-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_c301_parametro (cd_estabelecimento_p bigint, cd_convenio_p bigint, dt_referencia_p timestamp, ds_atributo_p text) RETURNS varchar AS $body$
DECLARE


nr_sequencia_w	bigint;
vl_retorno_w	varchar(255);
vl_resultado_w	varchar(255);
ds_sql_w	varchar(500);
c001		integer;


c01 CURSOR FOR
SELECT	nr_sequencia
from	c301_parametro
where	coalesce(cd_estabelecimento,cd_estabelecimento_p) = cd_estabelecimento_p
and	coalesce(cd_convenio,cd_convenio_p) = cd_convenio_p
and	trunc(dt_referencia_p) >= trunc(coalesce(dt_inicio_vigencia,clock_timestamp() - interval '1 days'))
and	trunc(dt_referencia_p) <= trunc(coalesce(dt_fim_vigencia,clock_timestamp() + interval '1 days'))
order by coalesce(DT_INICIO_VIGENCIA,clock_timestamp()),
	cd_estabelecimento desc,
	cd_convenio desc;

c01_w	c01%rowtype;


BEGIN

open C01;
loop
fetch C01 into
	c01_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	nr_sequencia_w := c01_w.nr_sequencia;
end loop;
close C01;

if (nr_sequencia_w IS NOT NULL AND nr_sequencia_w::text <> '') and (ds_atributo_p IS NOT NULL AND ds_atributo_p::text <> '') then

	ds_sql_w	:= 'select '||upper(DS_ATRIBUTO_P)||'  FROM C301_PARAMETRO WHERE NR_SEQUENCIA = '||nr_sequencia_w;

	c001	:= dbms_sql.open_cursor;
	dbms_sql.parse(c001,ds_sql_w,dbms_sql.native);
	dbms_sql.define_column(c001, 1, vl_retorno_w, 255);
	vl_resultado_w	:= dbms_sql.execute(c001);
	vl_resultado_w	:= dbms_sql.fetch_rows(c001);
	dbms_sql.column_value(c001,1,vl_retorno_w);
	dbms_sql.close_cursor(c001);

end if;

return vl_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_c301_parametro (cd_estabelecimento_p bigint, cd_convenio_p bigint, dt_referencia_p timestamp, ds_atributo_p text) FROM PUBLIC;

