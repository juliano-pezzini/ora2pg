-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_os_fora_desenv_data ( nr_ordem_servico_p bigint, dt_referencia_p timestamp ) RETURNS varchar AS $body$
DECLARE


ie_retorno_w		varchar(15);
nr_seq_estagio_w	bigint;



BEGIN

ie_retorno_w	:= 'N';

if (trunc(dt_referencia_p,'dd') <> trunc(clock_timestamp(),'dd')) then
begin
	select	max(a.nr_seq_estagio)
	into STRICT	nr_seq_estagio_w
	from	man_ordem_serv_estagio a
	where	a.nr_seq_ordem	= nr_ordem_servico_p
	and		a.dt_atualizacao	=	(	SELECT	max(y.dt_atualizacao)
										from	man_ordem_serv_estagio y
										where   y.nr_seq_ordem	= nr_ordem_servico_p
										and		y.dt_atualizacao <= fim_dia(dt_referencia_p)
									);

	select	coalesce(max('S'),'N')
	into STRICT	ie_retorno_w
	from	man_estagio_processo b
	where   b.nr_sequencia = nr_seq_estagio_w
	and ( coalesce(b.ie_desenv,'N') = 'N' and coalesce(b.ie_tecnologia,'N') = 'N' and coalesce(b.ie_design,'N') = 'N' );
end;
else
	select	coalesce(max('S'),'N')
	into STRICT	ie_retorno_w
	from 	man_estagio_processo b,
			man_ordem_servico a
	where	a.nr_sequencia = nr_ordem_servico_p
	and		a.nr_seq_estagio = b.nr_sequencia
	and ( coalesce(b.ie_desenv,'N') = 'N' and coalesce(b.ie_tecnologia,'N') = 'N' and coalesce(b.ie_design,'N') = 'N' );

end if;

return ie_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_os_fora_desenv_data ( nr_ordem_servico_p bigint, dt_referencia_p timestamp ) FROM PUBLIC;

