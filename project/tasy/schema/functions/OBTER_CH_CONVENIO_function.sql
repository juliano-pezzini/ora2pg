-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_ch_convenio ( cd_convenio_p bigint, cd_categoria_p text, cd_estabelecimento_p bigint) RETURNS bigint AS $body$
DECLARE

vl_retorno_w		double precision;
ie_prioridade_edicao_w	varchar(2);

BEGIN
begin
select	vl_ch_honorarios
into STRICT	vl_retorno_w
from	convenio_amb
where	cd_convenio		= cd_convenio_p
and	cd_categoria		= cd_categoria_p
and	ie_situacao		= 'A'
and	cd_estabelecimento	= cd_estabelecimento_p
and	dt_inicio_vigencia	= (	SELECT	max(dt_inicio_vigencia)
					from	convenio_amb
					where	cd_convenio		= cd_convenio_p
					and	cd_categoria		= cd_categoria_p
					and	ie_situacao		= 'A'
					and	cd_estabelecimento	= cd_estabelecimento_p);
exception
when others then
	vl_retorno_w	:= 0;
end;
return	vl_retorno_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_ch_convenio ( cd_convenio_p bigint, cd_categoria_p text, cd_estabelecimento_p bigint) FROM PUBLIC;

