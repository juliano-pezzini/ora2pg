-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_dias_internacao_drg ( nr_atendimento_p bigint) RETURNS bigint AS $body$
DECLARE


 cd_setor_atendimento_w	atend_paciente_unidade.cd_setor_atendimento%type;
 cd_unidade_basica_w	atend_paciente_unidade.cd_unidade_basica%type;
 cd_unidade_compl_w		atend_paciente_unidade.cd_unidade_compl%type;
 dt_entrada_unidade_w	atend_paciente_unidade.dt_entrada_unidade%type;
 dt_saida_unidade_w		atend_paciente_unidade.dt_saida_unidade%type;
 cd_tipo_acomodacao_w	atend_paciente_unidade.cd_tipo_acomodacao%type;
 qt_priv_acc_w			bigint;
 qt_retorno				bigint :=0;		

 C01 CURSOR FOR
	SELECT	a.cd_setor_atendimento,
			a.cd_unidade_basica,
			a.cd_unidade_compl,
			a.dt_entrada_unidade,
			a.dt_saida_unidade,
			a.cd_tipo_acomodacao
	from atend_paciente_unidade a, atend_categoria_convenio b
	where a.nr_atendimento=nr_atendimento_p
	and a.nr_atendimento=b.nr_atendimento
	and a.dt_entrada_unidade between b.dt_inicio_vigencia and coalesce(b.dt_final_vigencia,clock_timestamp())
	order by nr_sequencia;
	


BEGIN

if (nr_atendimento_p IS NOT NULL AND nr_atendimento_p::text <> '') then

	open c01;
	loop
	fetch c01 into
			cd_setor_atendimento_w,
			cd_unidade_basica_w,
			cd_unidade_compl_w,
			dt_entrada_unidade_w,
			dt_saida_unidade_w,
			cd_tipo_acomodacao_w;
	EXIT WHEN NOT FOUND; /* apply on c01 */
		begin
			qt_retorno := qt_retorno + trunc(coalesce(dt_saida_unidade_w,clock_timestamp()) + (1/24*60*60)) - trunc((dt_entrada_unidade_w) + (1/24*60*60));
		end;
	end loop;
	close c01;

end if;

return qt_retorno;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_dias_internacao_drg ( nr_atendimento_p bigint) FROM PUBLIC;

