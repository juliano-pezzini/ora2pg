-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION sl_obter_ult_serv_prof_dt (cd_profissional_p text, dt_referencia_p timestamp, nr_seq_unidade_p bigint, ie_opcao_p text) RETURNS timestamp AS $body$
DECLARE


nr_seq_sl_w		bigint;
dt_fim_w		timestamp;
dt_inicio_w		timestamp;
cd_unidade_basica_w	varchar(10);
cd_unidade_compl_w	varchar(10);
cd_setor_atendimento_w	integer;
dt_retorno_w		timestamp;
nr_seq_interno_w	bigint;


BEGIN

select 	max(nr_sequencia)
into STRICT	nr_seq_sl_w
from	sl_unid_atend
where	cd_executor = cd_profissional_p
and	trunc(dt_prevista) = trunc(dt_referencia_p);

select	max(dt_fim),
	max(dt_inicio),
	max(cd_unidade_basica),
	max(cd_unidade_compl),
	max(cd_setor_atendimento),
	max(a.nr_seq_unidade)
into STRICT	dt_fim_w,
	dt_inicio_w,
	cd_unidade_basica_w,
	cd_unidade_compl_w,
	cd_setor_atendimento_w,
	nr_seq_interno_w
from	sl_unid_atend a,
	unidade_atendimento b
where	b.nr_seq_interno = a.nr_seq_unidade
and	a.nr_sequencia = nr_seq_sl_w;


if (ie_opcao_p = 'DF') then
	dt_retorno_w	:= dt_fim_w;
elsif (ie_opcao_p = 'DI') then
	dt_retorno_w	:= dt_inicio_w;
end if;

return	dt_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION sl_obter_ult_serv_prof_dt (cd_profissional_p text, dt_referencia_p timestamp, nr_seq_unidade_p bigint, ie_opcao_p text) FROM PUBLIC;

