-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION man_obter_data_prevista_os ( nr_seq_ordem_serv_p bigint) RETURNS timestamp AS $body$
DECLARE


nm_usuario_exec_w		varchar(15);
dt_prevista_w			timestamp;


BEGIN

/*select	max(b.nm_usuario_exec)
into	nm_usuario_exec_w
from	usuario_grupo_des	a,
	man_ordem_servico_exec 	b
where	b.nm_usuario_exec	= a.nm_usuario_grupo
and	b.nr_seq_ordem		= nr_seq_ordem_serv_p
and	b.dt_fim_execucao is null;*/
--if	(nm_usuario_exec_w is not null) then
select	max(a.dt_prevista)
into STRICT	dt_prevista_w
from	man_ordem_ativ_prev a
where	a.nr_seq_ordem_serv	= nr_seq_ordem_serv_p
and	a.nm_usuario_prev	= (SELECT	max(b.nm_usuario_exec)
					from	usuario_grupo_des	a,
						man_ordem_servico_exec 	b
					where	b.nm_usuario_exec	= a.nm_usuario_grupo
					and	b.nr_seq_ordem		= nr_seq_ordem_serv_p
					and	coalesce(b.dt_fim_execucao::text, '') = '');
--end if;
return	dt_prevista_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION man_obter_data_prevista_os ( nr_seq_ordem_serv_p bigint) FROM PUBLIC;

