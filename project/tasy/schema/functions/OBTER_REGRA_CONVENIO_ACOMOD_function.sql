-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_regra_convenio_acomod ( cd_convenio_p bigint) RETURNS bigint AS $body$
DECLARE

cd_tipo_acomodacao_w bigint;

BEGIN

select	coalesce(max(cd_tipo_acomodacao),0)
into STRICT	cd_tipo_acomodacao_w
from	regra_convenio_tipo_acomod
where	cd_convenio = cd_convenio_p;

return	cd_tipo_acomodacao_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_regra_convenio_acomod ( cd_convenio_p bigint) FROM PUBLIC;

