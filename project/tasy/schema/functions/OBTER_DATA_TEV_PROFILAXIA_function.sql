-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_data_tev_profilaxia ( nr_atendimento_p bigint, ie_risco_alto_p text default null, ie_risco_baixo_p text default null, ie_risco_intermed_p text default null, ie_risco_nao_aplica_p text default null) RETURNS timestamp AS $body$
DECLARE


ds_retorno_w		timestamp := clock_timestamp();
nr_seq_tev_min_w	bigint;
dt_avaliacao_tev_w	timestamp;
qt_proc_tev_w		bigint;
qt_mat_tev_w		bigint;
qt_recomend_tev_w	bigint;


BEGIN

select	coalesce(min(nr_sequencia),0)
into STRICT	nr_seq_tev_min_w
from	escala_tev
where	nr_atendimento	= nr_atendimento_p
and		((ie_risco  = ie_risco_alto_p AND ie_risco_alto_p IS NOT NULL AND ie_risco_alto_p::text <> '') or
		(ie_risco  = ie_risco_baixo_p AND ie_risco_baixo_p IS NOT NULL AND ie_risco_baixo_p::text <> '') or
		(ie_risco  = ie_risco_intermed_p AND ie_risco_intermed_p IS NOT NULL AND ie_risco_intermed_p::text <> '') or
		(ie_risco  = ie_risco_nao_aplica_p AND ie_risco_nao_aplica_p IS NOT NULL AND ie_risco_nao_aplica_p::text <> '') or
		((coalesce(ie_risco_alto_p::text, '') = '') and (coalesce(ie_risco_baixo_p::text, '') = '') and (coalesce(ie_risco_intermed_p::text, '') = '') and (coalesce(ie_risco_nao_aplica_p::text, '') = '')))
and	coalesce(dt_inativacao::text, '') = '';


if (nr_seq_tev_min_w > 0 ) and (nr_atendimento_p > 0)then

		Select	max(dt_avaliacao)
		into STRICT	dt_avaliacao_tev_w
		from	escala_tev
		where	nr_sequencia = 	nr_seq_tev_min_w;

		ds_retorno_w	:= dt_avaliacao_tev_w;

end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_data_tev_profilaxia ( nr_atendimento_p bigint, ie_risco_alto_p text default null, ie_risco_baixo_p text default null, ie_risco_intermed_p text default null, ie_risco_nao_aplica_p text default null) FROM PUBLIC;

