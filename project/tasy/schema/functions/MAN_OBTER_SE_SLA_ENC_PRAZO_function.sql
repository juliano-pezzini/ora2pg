-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION man_obter_se_sla_enc_prazo ( nr_sequencia_p bigint) RETURNS varchar AS $body$
DECLARE


cd_estabelecimento_w	smallint;
ie_tempo_w		varchar(15);
dt_inicio_w		timestamp;
dt_termino_w	timestamp;
qt_min_termino_w	bigint;
ds_retorno_w		varchar(1);
dt_fim_real_w		timestamp;


BEGIN

/* Utilizada no relatório de SLAs */

if (nr_sequencia_p IS NOT NULL AND nr_sequencia_p::text <> '') then
	begin

	select	cd_estabelecimento,
		ie_tempo,
		dt_inicio,
		qt_min_termino
	into STRICT	cd_estabelecimento_w,
		ie_tempo_w,
		dt_inicio_w,
		qt_min_termino_w
	from	man_ordem_serv_sla
	where	nr_seq_ordem	= nr_sequencia_p;

	if (ie_tempo_w = 'COR') then
		dt_termino_w	:= dt_inicio_w + (qt_min_termino_w / 1440);
	else
		dt_termino_w	:= man_obter_hor_com(cd_estabelecimento_w, dt_inicio_w, qt_min_termino_w);
	end if;

	select	coalesce(dt_fim_real,clock_timestamp())
	into STRICT	dt_fim_real_w
	from	man_ordem_servico
	where	nr_sequencia = nr_sequencia_p;

	if (dt_termino_w >= dt_fim_real_w) then /* SLA encerrada no prazo*/
		ds_retorno_w := 'S';
	elsif (dt_termino_w < dt_fim_real_w) then /* SLA encerrada fora do prazo */
		ds_retorno_w := 'N';
	end if;

	end;
end if;

return ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION man_obter_se_sla_enc_prazo ( nr_sequencia_p bigint) FROM PUBLIC;

