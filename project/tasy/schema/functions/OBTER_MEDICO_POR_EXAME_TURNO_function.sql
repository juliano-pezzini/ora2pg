-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_medico_por_exame_turno (cd_procedimento_p bigint, ie_origem_proced_p text, nr_seq_proc_interno_p bigint, cd_agenda_p bigint, hr_agenda_p timestamp, nm_usuario_p text, cd_estabelecimento_p bigint) RETURNS bigint AS $body$
DECLARE

 
retorno_w	bigint;

BEGIN
 
SELECT	coalesce(MAX(cd_medico),0) 
INTO STRICT 	retorno_w 
FROM	agenda_medico_proced 
WHERE	((cd_procedimento = cd_procedimento_p) or (coalesce(cd_procedimento::text, '') = '')) and 
		((coalesce(cd_procedimento::text, '') = '') or ((ie_origem_proced = ie_origem_proced_p) or (coalesce(ie_origem_proced::text, '') = ''))) and 
		((nr_seq_proc_interno = nr_seq_proc_interno_p) or (coalesce(nr_seq_proc_interno::text, '') = '')) and 
    ( (cd_area_procedimento = obter_area_procedimento( cd_procedimento_p, ie_origem_proced_p)) or (coalesce(cd_area_procedimento::text, '') = '') ) and 
    ( (cd_especialidade = obter_especialidade_proced( cd_procedimento_p, ie_origem_proced_p)) or (coalesce(cd_especialidade::text, '') = '') ) and 
    ( (cd_grupo_proc = obter_grupo_procedimento( cd_procedimento_p, ie_origem_proced_p,'C')) or (coalesce(cd_grupo_proc::text, '') = '') ) and ( cd_agenda = cd_agenda_p) 
	and	Ageint_Obter_Se_Turno_Medico(cd_medico,hr_agenda_p,cd_agenda_p,nm_usuario_p,null,cd_estabelecimento_p) = 'S';
 
 
RETURN	retorno_w;
 
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_medico_por_exame_turno (cd_procedimento_p bigint, ie_origem_proced_p text, nr_seq_proc_interno_p bigint, cd_agenda_p bigint, hr_agenda_p timestamp, nm_usuario_p text, cd_estabelecimento_p bigint) FROM PUBLIC;

