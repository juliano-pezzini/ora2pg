-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_senha_autor_agecons ( nr_seq_agenda_p bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w		varchar(4000)	:= NULL;
nr_sequencia_autor_w	bigint;


BEGIN

IF (nr_seq_agenda_p IS NOT NULL AND nr_seq_agenda_p::text <> '') THEN

	SELECT	MAX(nr_sequencia)
	INTO STRICT	nr_sequencia_autor_w
	FROM	autorizacao_convenio
	WHERE	nr_seq_agenda_consulta	= nr_seq_agenda_p;

	IF (nr_sequencia_autor_w IS NOT NULL AND nr_sequencia_autor_w::text <> '') THEN

		SELECT	MAX(a.cd_senha)
		INTO STRICT	ds_retorno_w
		FROM	estagio_autorizacao b,
			autorizacao_convenio a
		WHERE	a.nr_seq_estagio	= b.nr_sequencia
		AND	a.nr_sequencia		= nr_sequencia_autor_w;

	END IF;

END IF;

RETURN ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_senha_autor_agecons ( nr_seq_agenda_p bigint) FROM PUBLIC;

