-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_data_nutricao_cpoe (NR_SEQ_DIETA_CPOE_P bigint, IE_DATA_FIM_P text) RETURNS timestamp AS $body$
DECLARE


DT_NUTRICAO_W  timestamp;


BEGIN
IF (NR_SEQ_DIETA_CPOE_P IS NOT NULL AND NR_SEQ_DIETA_CPOE_P::text <> '')
AND (IE_DATA_FIM_P = 'S') THEN

SELECT  MAX(DT_FIM)
INTO STRICT DT_NUTRICAO_W
FROM  CPOE_DIETA
WHERE  NR_SEQUENCIA = NR_SEQ_DIETA_CPOE_P;

ELSIF (NR_SEQ_DIETA_CPOE_P IS NOT NULL AND NR_SEQ_DIETA_CPOE_P::text <> '')
AND (IE_DATA_FIM_P = 'N') THEN

SELECT  MAX(DT_INICIO)
INTO STRICT DT_NUTRICAO_W
FROM  CPOE_DIETA
WHERE  NR_SEQUENCIA = NR_SEQ_DIETA_CPOE_P;

END IF;

RETURN DT_NUTRICAO_W;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_data_nutricao_cpoe (NR_SEQ_DIETA_CPOE_P bigint, IE_DATA_FIM_P text) FROM PUBLIC;

