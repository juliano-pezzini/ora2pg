-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION contr_temp_item_duplic (nr_seq_setor_temp_p bigint, nr_seq_item_p bigint) RETURNS bigint AS $body$
DECLARE


qt_registro_w		bigint;
cd_setor_w		bigint;
dt_hora_inicio_w	timestamp;
nr_seq_turno_w		bigint;


BEGIN

select	cd_setor_atendimento,
	dt_hora_inicio,
	nr_seq_turno
into STRICT	cd_setor_w,
	dt_hora_inicio_w,
	nr_seq_turno_w
from	setor_temperatura
where	nr_sequencia = nr_seq_setor_temp_p;

select  count(*)
into STRICT	qt_registro_w
from    setor_temperatura a,
        setor_temperatura_item b
where   b.nr_seq_setor_temp = a.nr_sequencia
and     a.cd_setor_atendimento = cd_setor_w
and     trunc(a.dt_hora_inicio,'dd') = trunc(dt_hora_inicio_w,'dd')
and     a.nr_seq_turno =  nr_seq_turno_w
and     b.nr_seq_item  =  nr_seq_item_p;



return	qt_registro_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION contr_temp_item_duplic (nr_seq_setor_temp_p bigint, nr_seq_item_p bigint) FROM PUBLIC;

