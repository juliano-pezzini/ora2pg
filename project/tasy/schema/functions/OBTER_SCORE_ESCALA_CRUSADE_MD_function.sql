-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_score_escala_crusade_md ( ie_sexo_p text, qt_hematocrito_p bigint, qt_clearance_p bigint, qt_freq_cardiaca_p bigint, qt_pa_sistolica_p bigint, ie_diabete_p text, ie_doenca_vasc_previa_p text, ie_sinais_ic_p text ) RETURNS bigint AS $body$
DECLARE

  qt_score_w double precision;

BEGIN
  qt_score_w := 0;

  if (ie_sexo_p = 'F') then
    qt_score_w := qt_score_w + 8;
  end if;

  if (qt_hematocrito_p > 39.9) then
    qt_score_w := qt_score_w;
  elsif (qt_hematocrito_p > 37) then
    qt_score_w := qt_score_w + 2;
  elsif (qt_hematocrito_p > 34) then
    qt_score_w := qt_score_w + 3;
  elsif (qt_hematocrito_p > 31) then
    qt_score_w := qt_score_w + 7;
  else
    qt_score_w := qt_score_w + 9;
  end if;	

  if (qt_clearance_p > 120) then
    qt_score_w := qt_score_w;
  elsif (qt_clearance_p > 91) then
    qt_score_w := qt_score_w + 7;
  elsif (qt_clearance_p > 61) then
    qt_score_w := qt_score_w + 17;
  elsif (qt_clearance_p > 31) then
    qt_score_w := qt_score_w + 28;
  elsif (qt_clearance_p > 16) then
    qt_score_w := qt_score_w + 35;
  else
    qt_score_w := qt_score_w + 39;
  end if;	

  if (qt_freq_cardiaca_p > 120) then
    qt_score_w := qt_score_w + 11;
  elsif (qt_freq_cardiaca_p > 111) then
    qt_score_w := qt_score_w + 10;
  elsif (qt_freq_cardiaca_p > 101) then
    qt_score_w := qt_score_w + 8;
  elsif (qt_freq_cardiaca_p > 91) then
    qt_score_w := qt_score_w + 6;
  elsif (qt_freq_cardiaca_p > 81) then
    qt_score_w := qt_score_w + 3;
  elsif (qt_freq_cardiaca_p > 71) then
    qt_score_w := qt_score_w + 1;
  end if;

  if (qt_pa_sistolica_p > 200) then
    qt_score_w := qt_score_w + 5;
  elsif (qt_pa_sistolica_p > 181) then
    qt_score_w := qt_score_w + 3;
  elsif (qt_pa_sistolica_p > 121) then
    qt_score_w := qt_score_w + 1;
  elsif (qt_pa_sistolica_p > 101) then
    qt_score_w := qt_score_w + 5;
  elsif (qt_pa_sistolica_p > 91) then
    qt_score_w := qt_score_w + 8;
  else
    qt_score_w := qt_score_w + 10;
  end if;

  if (ie_diabete_p = 'S') then
    qt_score_w := qt_score_w + 6;
  end if;

  if (ie_doenca_vasc_previa_p = 'S') then
    qt_score_w := qt_score_w + 6;
  end  if;

  if (ie_sinais_ic_p = 'S') then
    qt_score_w := qt_score_w + 7;
  end if;

  return qt_score_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_score_escala_crusade_md ( ie_sexo_p text, qt_hematocrito_p bigint, qt_clearance_p bigint, qt_freq_cardiaca_p bigint, qt_pa_sistolica_p bigint, ie_diabete_p text, ie_doenca_vasc_previa_p text, ie_sinais_ic_p text ) FROM PUBLIC;

