-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_nm_paciente_prescricao ( nr_prescricao_p bigint) RETURNS varchar AS $body$
DECLARE

    nm_paciente_w varchar(255);

BEGIN
    IF (nr_prescricao_p IS NOT NULL AND nr_prescricao_p::text <> '') THEN
      SELECT pes_fis.nm_pessoa_fisica
      INTO STRICT nm_paciente_w
      FROM pessoa_fisica pes_fis
      WHERE cd_pessoa_fisica = obter_cd_paciente_prescricao(nr_prescricao_p);
    END IF;
    RETURN nm_paciente_w;
  END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_nm_paciente_prescricao ( nr_prescricao_p bigint) FROM PUBLIC;

