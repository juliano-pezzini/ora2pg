-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION bsc_obter_valor_inf_ind ( nr_seq_indicador_p bigint, cd_ano_p bigint, cd_periodo_p bigint) RETURNS bigint AS $body$
DECLARE


qt_real_w	double precision;


BEGIN
if (nr_seq_indicador_p IS NOT NULL AND nr_seq_indicador_p::text <> '') and (cd_ano_p IS NOT NULL AND cd_ano_p::text <> '') and (cd_periodo_p IS NOT NULL AND cd_periodo_p::text <> '') then
	begin
	select	coalesce(max(qt_real),0)
	into STRICT	qt_real_w
	from	bsc_ind_inf
	where	nr_seq_indicador = nr_seq_indicador_p
	and	cd_ano = cd_ano_p
	and	cd_periodo = cd_periodo_p;
	end;
end if;
return qt_real_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION bsc_obter_valor_inf_ind ( nr_seq_indicador_p bigint, cd_ano_p bigint, cd_periodo_p bigint) FROM PUBLIC;

