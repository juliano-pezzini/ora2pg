-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_soma ( tp_item_p text, proc_mat_p bigint, tp_item_or_p text, proc_mat_or_p bigint, vl_item_p bigint) RETURNS bigint AS $body$
DECLARE


vl_retorno_w	double precision := 0;


BEGIN


if (proc_mat_p = 1) and (proc_mat_p = proc_mat_or_p) then
	begin
	if (tp_item_p = '1') and (tp_item_p = tp_item_or_p) then
		vl_retorno_w := vl_item_p;
	elsif (tp_item_p = '2') and (tp_item_p = tp_item_or_p) then
		vl_retorno_w := vl_item_p;
	elsif (tp_item_p = '3') and (tp_item_p = tp_item_or_p) then
		vl_retorno_w := vl_item_p;
	end if;
	end;
elsif (proc_mat_p = 2) and (proc_mat_p = proc_mat_or_p) then
	begin
	if (tp_item_p = '1') and (tp_item_p = tp_item_or_p) then
		vl_retorno_w := vl_item_p;
	elsif (tp_item_p = '2') and (tp_item_p = tp_item_or_p) then
		vl_retorno_w := vl_item_p;
	elsif (tp_item_p = '5') and (tp_item_p = tp_item_or_p) then
		vl_retorno_w := vl_item_p;
	end if;
	end;
end if;

RETURN vl_retorno_w;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_soma ( tp_item_p text, proc_mat_p bigint, tp_item_or_p text, proc_mat_or_p bigint, vl_item_p bigint) FROM PUBLIC;

