-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_guia_convenio (nr_atendimento_p bigint) RETURNS varchar AS $body$
DECLARE


nr_doc_convenio_w	varchar(20);


BEGIN

SELECT	max(nr_doc_convenio)
INTO STRICT	nr_doc_convenio_w
FROM	atend_categoria_convenio
WHERE	nr_seq_interno = obter_atecaco_atendimento(nr_atendimento_p);

RETURN	nr_doc_convenio_w;

END	;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_guia_convenio (nr_atendimento_p bigint) FROM PUBLIC;

