-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_qt_valor_amputado (ie_membro_amputado_p text, qt_membro_amputado_p text) RETURNS bigint AS $body$
DECLARE


ds_result_w	double precision;


BEGIN

	IF (ie_membro_amputado_p = 'C')THEN
		ds_result_w := qt_membro_amputado_p * 8;
	ELSIF (ie_membro_amputado_p = 'T')THEN
		ds_result_w := qt_membro_amputado_p * 50;
	ELSIF (ie_membro_amputado_p = 'B')THEN
		ds_result_w := qt_membro_amputado_p * 2.7;
	ELSIF (ie_membro_amputado_p = 'A')THEN
		ds_result_w := qt_membro_amputado_p * 1.6;
	ELSIF (ie_membro_amputado_p = 'M')THEN
		ds_result_w := qt_membro_amputado_p * 0.7;
	ELSIF (ie_membro_amputado_p = 'S')THEN
		ds_result_w := qt_membro_amputado_p * 5;
	ELSIF (ie_membro_amputado_p = 'CX')THEN
		ds_result_w := qt_membro_amputado_p * 10.1;
	ELSIF (ie_membro_amputado_p = 'P')THEN
		ds_result_w := qt_membro_amputado_p * 4.4;
	ELSIF (ie_membro_amputado_p = 'PE')THEN
		ds_result_w := qt_membro_amputado_p * 1.5;
	ELSE
		ds_result_w := qt_membro_amputado_p * 16;
	END IF;

RETURN ds_result_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_qt_valor_amputado (ie_membro_amputado_p text, qt_membro_amputado_p text) FROM PUBLIC;

