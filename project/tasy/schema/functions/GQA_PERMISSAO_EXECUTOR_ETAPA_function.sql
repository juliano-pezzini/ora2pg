-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION gqa_permissao_executor_etapa (nr_seq_etapa_p bigint ,nr_seq_prot_pac_p bigint ,nr_atendimento_p bigint) RETURNS varchar AS $body$
BEGIN
  IF gqa_obter_se_executor_etapa(nr_seq_etapa_p, nr_seq_prot_pac_p) = 'S' THEN

    IF obter_se_gqa_regra_liberada(nr_seq_etapa_p) = 'S' THEN

      IF gqa_regra_complementar(wheb_usuario_pck.get_nm_usuario
                               ,nr_atendimento_p
                               ,nr_seq_etapa_p) = 'X' THEN
        RETURN 'S';
      END IF;
    END IF;
  END IF;

  RETURN 'N';
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION gqa_permissao_executor_etapa (nr_seq_etapa_p bigint ,nr_seq_prot_pac_p bigint ,nr_atendimento_p bigint) FROM PUBLIC;

