-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_instituicao_lote_ent (nr_seq_instituicao_p bigint) RETURNS varchar AS $body$
DECLARE

ds_razao_social_w	varchar(80);

BEGIN

if (nr_seq_instituicao_p IS NOT NULL AND nr_seq_instituicao_p::text <> '') then
	select	max(a.DS_RAZAO_SOCIAL_INST)
	into STRICT	ds_razao_social_w
	from	lote_ent_instituicao a
	where	a.nr_sequencia = nr_seq_instituicao_p;
end if;

return	ds_razao_social_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_instituicao_lote_ent (nr_seq_instituicao_p bigint) FROM PUBLIC;

