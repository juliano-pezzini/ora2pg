-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_dose_diferenciada_md ( qt_fase_npt_p bigint, qt_vol_1_fase_p bigint, qt_vol_2_fase_p bigint, qt_vol_3_fase_p bigint, qt_vol_4_fase_p bigint ) RETURNS varchar AS $body$
BEGIN
  if (qt_fase_npt_p > 3) then
    return coalesce(qt_vol_1_fase_p, 0) || '-' || coalesce(qt_vol_2_fase_p, 0) || '-' || coalesce(qt_vol_3_fase_p, 0) || '-' ||coalesce(qt_vol_4_fase_p, 0);
  elsif (qt_fase_npt_p > 2) then
    return coalesce(qt_vol_1_fase_p, 0) || '-' || coalesce(qt_vol_2_fase_p, 0) || '-' || coalesce(qt_vol_3_fase_p, 0);
  elsif (qt_fase_npt_p > 1) then
    return coalesce(qt_vol_1_fase_p, 0) || '-' ||coalesce(qt_vol_2_fase_p, 0);
  else
    return coalesce(qt_vol_1_fase_p, 0);
  end if;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_dose_diferenciada_md ( qt_fase_npt_p bigint, qt_vol_1_fase_p bigint, qt_vol_2_fase_p bigint, qt_vol_3_fase_p bigint, qt_vol_4_fase_p bigint ) FROM PUBLIC;

