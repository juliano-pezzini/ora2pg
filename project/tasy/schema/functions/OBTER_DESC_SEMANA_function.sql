-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_desc_semana ( dt_referencia_p timestamp, ie_mascara_p text, ie_idioma_p text) RETURNS varchar AS $body$
DECLARE


ds_semana_w	varchar(15);
ds_ate_w	varchar(5);


BEGIN
if (dt_referencia_p IS NOT NULL AND dt_referencia_p::text <> '') and (ie_mascara_p IS NOT NULL AND ie_mascara_p::text <> '') then
	begin
	if (ie_idioma_p = 'I') then
		begin
		ds_ate_w := ' ' || Wheb_mensagem_pck.get_texto(309245) || ' '; --' to ';
		end;
	else
		begin
		ds_ate_w := ' ' || Wheb_mensagem_pck.get_texto(309245) || ' '; --' à ';
		end;
	end if;

	ds_semana_w :=	to_char(to_date(obter_inicio_fim_semana(dt_referencia_p,'I'),'dd/mm/yyyy hh24:mi:ss'),ie_mascara_p) || ds_ate_w ||
			to_char(to_date(obter_inicio_fim_semana(dt_referencia_p,'F'),'dd/mm/yyyy hh24:mi:ss'),ie_mascara_p);
	end;
end if;
return ds_semana_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_desc_semana ( dt_referencia_p timestamp, ie_mascara_p text, ie_idioma_p text) FROM PUBLIC;

