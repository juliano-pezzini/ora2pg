-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION cp_get_old_display_name ( nr_sequencia_p bigint, nm_tabela_p text, ds_unique_value_p text default null) RETURNS varchar AS $body$
DECLARE


nr_seq_care_plan_w		care_plan.nr_sequencia%type;
ds_unique_value_w		varchar(255);
ds_retorno_w			varchar(255);
ds_display_name_w		varchar(255);
nr_sequencia_w			bigint;
ie_population_w			cp_population.ie_population%type;
nr_seq_goal_w			cp_problem_goal.nr_seq_goal%type;
nr_seq_intervention_w	cp_interv_plan_assoc.nr_seq_intervention%type;
ds_keyword_w			cp_keyword.ds_keyword%type;


BEGIN

ds_unique_value_w := ds_unique_value_p;

if (nm_tabela_p IS NOT NULL AND nm_tabela_p::text <> '') then

	if (nm_tabela_p = 'CARE_PLAN') then
		
		if (ds_unique_value_w IS NOT NULL AND ds_unique_value_w::text <> '') then
		
			select	max(nr_sequencia)
			into STRICT	nr_sequencia_w
			from	care_plan
			where	ds_unique_value = ds_unique_value_w
			and		nr_sequencia < nr_sequencia_p;
			
			if (nr_sequencia_w IS NOT NULL AND nr_sequencia_w::text <> '') then
				select	ds_display_name
				into STRICT	ds_retorno_w
				from	care_plan
				where 	nr_sequencia = nr_sequencia_w;	
			end if;
			
		end if;

	elsif (nm_tabela_p = 'CP_PROBLEM') then	
		
		if (coalesce(ds_unique_value_w::text, '') = '') then
			select 	ds_unique_value
			into STRICT	ds_unique_value_w
			from 	cp_problem
			where	nr_sequencia = nr_sequencia_p;
		end if;
		
		if (ds_unique_value_w IS NOT NULL AND ds_unique_value_w::text <> '') then
		
			select	max(nr_sequencia)
			into STRICT	nr_sequencia_w
			from	cp_problem
			where	ds_unique_value = ds_unique_value_w
			and		nr_sequencia < nr_sequencia_p;
			
			if (nr_sequencia_w IS NOT NULL AND nr_sequencia_w::text <> '') then
				select	ds_display_name
				into STRICT	ds_retorno_w
				from	cp_problem
				where 	nr_sequencia = nr_sequencia_w;	
			end if;
			
		end if;
		
	elsif (nm_tabela_p = 'CP_MEASURE') then	
		
		if (coalesce(ds_unique_value_w::text, '') = '') then
			select 	ds_unique_value
			into STRICT	ds_unique_value_w
			from 	cp_measure
			where	nr_sequencia = nr_sequencia_p;
		end if;
		
		if (ds_unique_value_w IS NOT NULL AND ds_unique_value_w::text <> '') then
		
			select	max(nr_sequencia)
			into STRICT	nr_sequencia_w
			from	cp_measure
			where	ds_unique_value = ds_unique_value_w
			and		nr_sequencia < nr_sequencia_p;
			
			if (nr_sequencia_w IS NOT NULL AND nr_sequencia_w::text <> '') then
				select	ds_display_name
				into STRICT	ds_retorno_w
				from	cp_measure
				where 	nr_sequencia = nr_sequencia_w;	
			end if;
			
		end if;
		
	elsif (nm_tabela_p = 'CP_INDICATOR') then	
		
		if (coalesce(ds_unique_value_w::text, '') = '') then
			select 	ds_unique_value
			into STRICT	ds_unique_value_w
			from 	cp_indicator
			where	nr_sequencia = nr_sequencia_p;
		end if;
		
		if (ds_unique_value_w IS NOT NULL AND ds_unique_value_w::text <> '') then
		
			select	max(nr_sequencia)
			into STRICT	nr_sequencia_w
			from	cp_indicator
			where	ds_unique_value = ds_unique_value_w
			and		nr_sequencia < nr_sequencia_p;
			
			if (nr_sequencia_w IS NOT NULL AND nr_sequencia_w::text <> '') then
				select	ds_display_name
				into STRICT	ds_retorno_w
				from	cp_indicator
				where 	nr_sequencia = nr_sequencia_w;	
			end if;
			
		end if;
		
	elsif (nm_tabela_p = 'CP_GOAL') then	
	
		if (coalesce(ds_unique_value_w::text, '') = '') then
			select 	ds_unique_value
			into STRICT	ds_unique_value_w
			from 	cp_goal
			where	nr_sequencia = nr_sequencia_p;
		end if;
		
		if (ds_unique_value_w IS NOT NULL AND ds_unique_value_w::text <> '') then

			select	max(nr_sequencia)
			into STRICT	nr_sequencia_w
			from	cp_goal
			where	ds_unique_value = ds_unique_value_w
			and		nr_sequencia < nr_sequencia_p;
			
			if (nr_sequencia_w IS NOT NULL AND nr_sequencia_w::text <> '') then
				select	ds_display_name
				into STRICT	ds_retorno_w
				from	cp_goal
				where 	nr_sequencia = nr_sequencia_w;	
			end if;
		end if;
		
	elsif (nm_tabela_p = 'CP_INTERV_PLAN') then	
	
		if (ds_unique_value_w IS NOT NULL AND ds_unique_value_w::text <> '') then
		
			select	max(nr_sequencia)
			into STRICT	nr_sequencia_w
			from	cp_interv_plan
			where	ds_unique_value = ds_unique_value_w
			and		nr_sequencia < nr_sequencia_p;
			
			if (nr_sequencia_w IS NOT NULL AND nr_sequencia_w::text <> '') then
				select	ds_display_name
				into STRICT	ds_retorno_w
				from	cp_interv_plan
				where 	nr_sequencia = nr_sequencia_w;	
			end if;
			
		end if;
		
	elsif (nm_tabela_p = 'CP_INTERVENTION') then	
	
		if (coalesce(ds_unique_value_w::text, '') = '') then
			select 	ds_unique_value
			into STRICT	ds_unique_value_w
			from 	cp_intervention
			where	nr_sequencia = nr_sequencia_p;
		end if;
		
		if (ds_unique_value_w IS NOT NULL AND ds_unique_value_w::text <> '') then

			select	max(nr_sequencia)
			into STRICT	nr_sequencia_w
			from	cp_intervention
			where	ds_unique_value = ds_unique_value_w
			and		nr_sequencia < nr_sequencia_p;
			
			if (nr_sequencia_w IS NOT NULL AND nr_sequencia_w::text <> '') then
				select	ds_display_name
				into STRICT	ds_retorno_w
				from	cp_intervention
				where 	nr_sequencia = nr_sequencia_w;	
			end if;
			
		end if;
		
	end if;
	
end if;

return substr(ds_retorno_w,1,255);

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION cp_get_old_display_name ( nr_sequencia_p bigint, nm_tabela_p text, ds_unique_value_p text default null) FROM PUBLIC;

