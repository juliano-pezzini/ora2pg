-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION cpoe_get_proc_lib ( cd_area_proced_p bigint, cd_espec_proced_p bigint, cd_grupo_proced_p bigint, cd_procedimento_p bigint, ie_origem_proced_p bigint, nr_seq_pro_interno_p bigint) RETURNS varchar AS $body$
DECLARE


/* +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Finality:
---------------------------------------------------------------------------------------------------
Direct calls:
[ X ]  Object dictionary [  ] Tasy (Delphi/Java) [  ] Portal [  ]  Reports [ ] Others:
---------------------------------------------------------------------------------------------------
Attention points:
---------------------------------------------------------------------------------------------------
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ */
ds_retorno_w			varchar(255);
cd_procedimento_w       proc_interno.nr_sequencia%type;
ie_origem_proced_w      proc_interno.ie_origem_proced%type;
cd_area_proced_w        agenda_regra.cd_area_proc%type;
cd_espec_proced_w       agenda_regra.cd_especialidade%type;
cd_grupo_proced_w       agenda_regra.cd_grupo_proc%type;


BEGIN
select  a.cd_procedimento,
        a.ie_origem_proced
into STRICT    cd_procedimento_w,
        ie_origem_proced_w
from    proc_interno    a
where   a.nr_sequencia  = nr_seq_pro_interno_p;

SELECT	coalesce(MAX(cd_area_procedimento),0),
        coalesce(MAX(cd_especialidade),0),
        coalesce(MAX(cd_grupo_proc),0)
INTO STRICT	cd_area_proced_w,
        cd_espec_proced_w,
        cd_grupo_proced_w
FROM	estrutura_procedimento_v
WHERE	cd_procedimento = cd_procedimento_w
AND     ie_origem_proced = ie_origem_proced_w;

select  CASE WHEN count(1)=0 THEN 'N'  ELSE 'S' END
into STRICT    ds_retorno_w

where   ((cd_area_proced_p = cd_area_proced_w) OR (coalesce(cd_area_proced_p::text, '') = ''))
AND     ((cd_espec_proced_p = cd_espec_proced_w) OR (coalesce(cd_espec_proced_p::text, '') = ''))
AND     ((cd_grupo_proced_p = cd_grupo_proced_w) OR (coalesce(cd_grupo_proced_p::text, '') = ''))
AND     ((cd_procedimento_p = cd_procedimento_w) OR (coalesce(cd_procedimento_p::text, '') = ''))
AND     ((coalesce(cd_procedimento_p::text, '') = '') OR ((ie_origem_proced_p = ie_origem_proced_w) OR (coalesce(ie_origem_proced_p::text, '') = '')));

return ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION cpoe_get_proc_lib ( cd_area_proced_p bigint, cd_espec_proced_p bigint, cd_grupo_proced_p bigint, cd_procedimento_p bigint, ie_origem_proced_p bigint, nr_seq_pro_interno_p bigint) FROM PUBLIC;

