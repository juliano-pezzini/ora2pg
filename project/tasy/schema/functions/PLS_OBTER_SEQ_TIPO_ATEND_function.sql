-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_seq_tipo_atend ( ie_tipo_atend_tiss_p text, cd_estabelecimento_p bigint default null) RETURNS bigint AS $body$
DECLARE


/* +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Finalidade: Converter o tipo de atendimento da guia, que depende do dominio 1761, para a conta, que depende de cadastro
			no shift+f1 / Plano de Saude / OPS - Contas Médicas / OPS - Tipos de Atendimento
-------------------------------------------------------------------------------------------------------------------
Locais de chamada direta:
[ ]  Objetos do dicionário [ ] Tasy (Delphi/Java) [X] Portal [  ]  Relatórios [ ] Outros:
 ------------------------------------------------------------------------------------------------------------------
Pontos de atenção:
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ */
nr_sequencia_w		bigint;


BEGIN
if (ie_tipo_atend_tiss_p IS NOT NULL AND ie_tipo_atend_tiss_p::text <> '') then

	if (cd_estabelecimento_p IS NOT NULL AND cd_estabelecimento_p::text <> '') then
		select	max(nr_sequencia)
		into STRICT	nr_sequencia_w
		from	pls_tipo_atendimento
		where	(cd_tiss)::numeric  	= (ie_tipo_atend_tiss_p)::numeric
		and	cd_estabelecimento	= cd_estabelecimento_p;
	end if;

	if (coalesce(nr_sequencia_w::text, '') = '') then
		select	max(nr_sequencia)
		into STRICT	nr_sequencia_w
		from	pls_tipo_atendimento
		where	(cd_tiss)::numeric  	= (ie_tipo_atend_tiss_p)::numeric;
	end if;

end if;

return	nr_sequencia_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_seq_tipo_atend ( ie_tipo_atend_tiss_p text, cd_estabelecimento_p bigint default null) FROM PUBLIC;

