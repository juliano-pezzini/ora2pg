-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION verifica_proced_integracao ( nr_seq_proc_interno_p bigint, cd_procedimento_p bigint, ie_origem_proced_p bigint, nr_seq_sistema_integ_p bigint) RETURNS varchar AS $body$
DECLARE




ie_resultado_w	varchar(2);




BEGIN



select 	CASE WHEN count(*)=0 THEN 'N'  ELSE 'S' END

into STRICT	ie_resultado_w

from 	proc_interno_integracao b

where 	((b.nr_seq_proc_interno 	= nr_seq_proc_interno_p) or

	(b.cd_procedimento = cd_procedimento_p AND b.ie_origem_proced = ie_origem_proced_p))

and	b.nr_seq_sistema_integ 	= nr_seq_sistema_integ_p
and (
      ( coalesce(b.IE_UTILIZA_ESTAB, 'N') = 'S' and b.CD_ESTABELECIMENTO = WHEB_USUARIO_PCK.get_cd_estabelecimento) or ( coalesce(b.IE_UTILIZA_ESTAB, 'N') = 'N')
      );


return	ie_resultado_w;



end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION verifica_proced_integracao ( nr_seq_proc_interno_p bigint, cd_procedimento_p bigint, ie_origem_proced_p bigint, nr_seq_sistema_integ_p bigint) FROM PUBLIC;

