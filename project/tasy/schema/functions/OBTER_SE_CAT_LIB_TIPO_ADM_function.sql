-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_cat_lib_tipo_adm (nr_seq_tipo_admissao_fat_p bigint, cd_convenio_p bigint, cd_categoria_p text, nr_seq_tipo_case_p bigint default null) RETURNS varchar AS $body$
DECLARE


ie_liberado_w		varchar(01)	:= 'S';
qt_regra_w		bigint;


BEGIN

select	count(*)
into STRICT	qt_regra_w
from	categ_tipo_admissao_fat
where	cd_convenio = cd_convenio_p
and	cd_categoria = cd_categoria_p;


if (qt_regra_w	<> 0) then
	select	coalesce(max('S'), 'N')
	into STRICT	ie_liberado_w
	from	categ_tipo_admissao_fat
	where	cd_convenio = cd_convenio_p
	and	cd_categoria = cd_categoria_p
	and	coalesce(nr_seq_tipo_admissao_fat,coalesce(nr_seq_tipo_admissao_fat_p,0)) = coalesce(nr_seq_tipo_admissao_fat_p,0)
	and	coalesce(nr_seq_tipo_episodio,coalesce(nr_seq_tipo_case_p,0)) = coalesce(nr_seq_tipo_case_p,0);

end if;

return	ie_liberado_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_cat_lib_tipo_adm (nr_seq_tipo_admissao_fat_p bigint, cd_convenio_p bigint, cd_categoria_p text, nr_seq_tipo_case_p bigint default null) FROM PUBLIC;

