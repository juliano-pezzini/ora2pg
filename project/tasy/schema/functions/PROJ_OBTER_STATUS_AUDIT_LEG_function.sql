-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION proj_obter_status_audit_leg (nr_seq_etapa_p bigint) RETURNS bigint AS $body$
DECLARE

	nr_seq_leg_w	bigint;
	ie_status_w 	proj_cron_etapa_audit.ie_status%type;

BEGIN
	ie_status_w := proj_obter_status_audit(nr_seq_etapa_p);

	if (ie_status_w = 'OK') then
		nr_seq_leg_w := 10847;	
	end if;

	if (ie_status_w = 'AP') then
		nr_seq_leg_w := 10849;	
	end if;

	if (ie_status_w = 'NA') then
		nr_seq_leg_w := 10848;	
	end if;

	return	nr_seq_leg_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION proj_obter_status_audit_leg (nr_seq_etapa_p bigint) FROM PUBLIC;

