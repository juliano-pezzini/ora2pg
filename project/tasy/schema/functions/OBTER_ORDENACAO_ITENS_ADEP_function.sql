-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_ordenacao_itens_adep ( cd_material_p bigint, ie_via_aplicacao_p text, nr_regra_p bigint) RETURNS bigint AS $body$
DECLARE


nr_seq_apres_w	integer := 999;
cd_subgrupo_w	smallint;
cd_classe_material_w	integer;
cd_grupo_mat_w		integer;


BEGIN
/*
select	nvl(Obter_estrutura_material(cd_material_p, 'S'),0),
	nvl(Obter_estrutura_material(cd_material_p, 'C'),0),
	nvl(Obter_estrutura_material(cd_material_p, 'G'),0)
into	cd_subgrupo_w,
	cd_classe_material_w,
	cd_grupo_mat_w
from	dual;

if	(ie_via_aplicacao_p is not null) then
	begin
	select	nvl(max(nr_seq_apres),999)
	into	nr_seq_apres_w
	from	regra_ordem_medic_rep
	where	nvl(cd_material,cd_material_p) = cd_material_p
	and	nr_seq_regra 	= nr_regra_p
	and	nvl(ie_via_aplicacao, ie_via_aplicacao_p) = ie_via_aplicacao_p
	and	nvl(cd_subgrupo_material,cd_subgrupo_w) = cd_subgrupo_w
	and	nvl(cd_classe_material,cd_classe_material_w) = cd_classe_material_w
	and	nvl(cd_grupo_material, cd_grupo_mat_w)  = cd_grupo_mat_w;
	end;
end if;
*/
nr_seq_apres_w := 999;

return nr_seq_apres_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_ordenacao_itens_adep ( cd_material_p bigint, ie_via_aplicacao_p text, nr_regra_p bigint) FROM PUBLIC;

