-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_diagnostico_status ( ie_status_p text) RETURNS varchar AS $body$
DECLARE

				
ds_status_w diagnostico_status.ds_status%type;


BEGIN

  select coalesce(max(ie_status), '-1')
  into STRICT ds_status_w
  from diagnostico_status
  where ie_status = ie_status_p;

  if (ds_status_w = '-1' and ie_status_p = 'PR') then
    Insert into DIAGNOSTICO_STATUS(DT_ATUALIZACAO,NM_USUARIO,DT_ATUALIZACAO_NREC,NM_USUARIO_NREC,IE_STATUS,DS_STATUS,IE_SITUACAO,DS_COR) values (clock_timestamp(),'TASY',clock_timestamp(),'TASY',ie_status_p,obter_desc_expressao(489312),'A',null);
  elsif (ds_status_w = '-1' and ie_status_p = 'D') then
    Insert into DIAGNOSTICO_STATUS(DT_ATUALIZACAO,NM_USUARIO,DT_ATUALIZACAO_NREC,NM_USUARIO_NREC,IE_STATUS,DS_STATUS,IE_SITUACAO,DS_COR) values (clock_timestamp(),'TASY',clock_timestamp(),'TASY',ie_status_p,obter_desc_expressao(489313),'A',null);
  end if;

  if (ds_status_w = '-1') then
    select coalesce(max(ie_status), '-1')
    into STRICT ds_status_w
    from diagnostico_status
    where ie_status = ie_status_p;
  end if;

  return ds_status_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION obter_diagnostico_status ( ie_status_p text) FROM PUBLIC;

