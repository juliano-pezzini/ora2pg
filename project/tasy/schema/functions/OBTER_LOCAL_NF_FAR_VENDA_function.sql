-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_local_nf_far_venda (cd_estabelecimento_p text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w			parametro_estoque.cd_local_nf_far_venda%type;
cd_local_nf_far_venda_w		parametro_estoque.cd_local_nf_far_venda%type;


BEGIN

select 	coalesce(cd_local_nf_far_venda,0)
into STRICT  	ds_retorno_w
from 	parametro_estoque
where 	cd_estabelecimento = cd_estabelecimento_p;

begin
	select obter_valor_param_usuario(1608, 5, obter_perfil_ativo, wheb_usuario_pck.get_nm_usuario, cd_estabelecimento_p)
	into STRICT   cd_local_nf_far_venda_w
	;
exception
when others then
	cd_local_nf_far_venda_w := null;
end;

if (cd_local_nf_far_venda_w IS NOT NULL AND cd_local_nf_far_venda_w::text <> '') then
	ds_retorno_w := cd_local_nf_far_venda_w;
end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_local_nf_far_venda (cd_estabelecimento_p text) FROM PUBLIC;

