-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_transf_caixa_dest (nr_seq_trans_financ_p bigint, nr_seq_caixa_p bigint) RETURNS varchar AS $body$
DECLARE


cont_w		bigint;
ie_retorno_w	varchar(255) := 'S';


BEGIN

select	count(*)
into STRICT	cont_w
from	trans_financ_caixa_dest
where	nr_seq_trans_financ	= nr_seq_trans_financ_p;

if (cont_w > 0) then
	select	count(*)
	into STRICT	cont_w
	from	trans_financ_caixa_dest
	where	nr_seq_trans_financ	= nr_seq_trans_financ_p
	and	nr_seq_caixa_dest	= nr_seq_caixa_p;

	ie_retorno_w	:= 'N';
	if (cont_w > 0) then
		ie_retorno_w	:= 'S';
	end if;
end if;

return	ie_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_transf_caixa_dest (nr_seq_trans_financ_p bigint, nr_seq_caixa_p bigint) FROM PUBLIC;

