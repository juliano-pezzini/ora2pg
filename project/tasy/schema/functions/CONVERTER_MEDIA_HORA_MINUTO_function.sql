-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION converter_media_hora_minuto (qt_hora_p bigint) RETURNS varchar AS $body$
DECLARE


qt_horas_w		bigint;
qt_min_w		varchar(2000);
qt_dias_w		bigint;
ds_minutos 		varchar(2000);


BEGIN
qt_dias_w		:= TRUNC(qt_hora_p / 24);
qt_horas_w		:= TRUNC(((qt_hora_p / 24) - qt_dias_w) * 24);
qt_min_w		:= TRUNC(((((qt_hora_p / 24) - qt_dias_w) * 24) - qt_horas_w) * 60 );

ds_minutos := '';

if (qt_dias_w > 0) then
	ds_minutos := to_char(TRUNC(qt_dias_w))|| obter_desc_expressao(287752);
end if;
if (qt_horas_w > 0) then
	ds_minutos := ds_minutos || to_char(TRUNC(qt_horas_w))|| obter_desc_expressao(653219);
end if;

ds_minutos := ds_minutos || coalesce(qt_min_w,00) ||obter_desc_expressao(619557);


return ds_minutos;


end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION converter_media_hora_minuto (qt_hora_p bigint) FROM PUBLIC;

