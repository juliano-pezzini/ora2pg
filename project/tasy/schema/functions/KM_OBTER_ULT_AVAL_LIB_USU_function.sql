-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION km_obter_ult_aval_lib_usu ( cd_funcao_p funcao.cd_funcao%type default null, nr_seq_habilidade_p km_habilidade_tecnica.nr_sequencia%type default null, cd_setor_atendimento_p setor_atendimento.cd_setor_atendimento%type DEFAULT NULL, nm_usuario_p usuario.nm_usuario%type DEFAULT NULL) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	km_avaliacao.nr_sequencia%type;


BEGIN

select	max(x.nr_sequencia)
into STRICT	ds_retorno_w
from (SELECT	a.nr_sequencia
	from	km_questionario q,
		km_avaliacao a
	where	q.nr_sequencia = a.nr_seq_questionario
	and	coalesce(q.cd_funcao,0) = coalesce(cd_funcao_p,0)
	and	coalesce(q.nr_seq_habilidade,0) = coalesce(nr_seq_habilidade_p,0)
	and	a.nm_usuario_nrec = nm_usuario_p
	and	a.cd_setor_atendimento = cd_setor_atendimento_p
	and	(a.dt_liberacao IS NOT NULL AND a.dt_liberacao::text <> '')
	and	a.ie_situacao = 'A'
	order by a.dt_liberacao desc) x LIMIT 1;

return ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION km_obter_ult_aval_lib_usu ( cd_funcao_p funcao.cd_funcao%type default null, nr_seq_habilidade_p km_habilidade_tecnica.nr_sequencia%type default null, cd_setor_atendimento_p setor_atendimento.cd_setor_atendimento%type DEFAULT NULL, nm_usuario_p usuario.nm_usuario%type DEFAULT NULL) FROM PUBLIC;

