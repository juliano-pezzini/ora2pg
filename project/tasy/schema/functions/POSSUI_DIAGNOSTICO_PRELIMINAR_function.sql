-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION possui_diagnostico_preliminar ( nr_atendimento_p bigint) RETURNS varchar AS $body$
DECLARE


ie_existe_w		varchar(10);


BEGIN

select	CASE WHEN count(*)=0 THEN 'N'  ELSE 'S' END
into STRICT	ie_existe_w
from	diagnostico_medico
where	nr_atendimento = nr_atendimento_p
and	ie_tipo_diagnostico = 1
and	trunc(dt_diagnostico) = trunc(clock_timestamp());

return	ie_existe_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION possui_diagnostico_preliminar ( nr_atendimento_p bigint) FROM PUBLIC;

