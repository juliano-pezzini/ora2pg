-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_regra_obriga_campo_gas (cd_perfil_p bigint) RETURNS varchar AS $body$
DECLARE


ie_obriga_modalidade_w	varchar(1) := 'N';

C01 CURSOR FOR
	SELECT coalesce(ie_obriga_modalidade,'N')
	from regra_obriga_campo_gas
	where cd_perfil = cd_perfil_p or (coalesce(cd_perfil,0) = 0)
	order by cd_perfil desc;


BEGIN
	open C01;
	loop
	fetch C01 into
		ie_obriga_modalidade_w;
	EXIT WHEN NOT FOUND; /* apply on C01 */
		begin
			ie_obriga_modalidade_w := ie_obriga_modalidade_w;
		end;
	end loop;
	close C01;

return	ie_obriga_modalidade_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_regra_obriga_campo_gas (cd_perfil_p bigint) FROM PUBLIC;

