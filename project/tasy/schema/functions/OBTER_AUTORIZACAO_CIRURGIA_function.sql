-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_autorizacao_cirurgia (nr_seq_agenda_p bigint) RETURNS varchar AS $body$
DECLARE


nr_autorizacao_w		varchar(50);


BEGIN

if (nr_seq_agenda_p > 0) then
	select	to_char(max(nr_sequencia))
	into STRICT	nr_autorizacao_w
	from	autorizacao_cirurgia
	where	nr_seq_agenda	= nr_seq_agenda_p;
end if;

return	nr_autorizacao_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_autorizacao_cirurgia (nr_seq_agenda_p bigint) FROM PUBLIC;

