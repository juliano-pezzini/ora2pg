-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_dados_partic_cart (cd_pessoa_fisica_p mprev_participante.cd_pessoa_fisica%type default null) RETURNS varchar AS $body$
DECLARE

ds_retorno_w	varchar(255);					
ie_origem_dados_w    varchar(1);

BEGIN
	ie_origem_dados_w := mprev_obter_origem_dados;
	if (ie_origem_dados_w = 'O') then 
	   ds_retorno_w := pls_obter_dados_segurado(mprev_obter_benef_partic(null,cd_pessoa_fisica_p),'C');
	else 
		SELECT 	MAX(CD_USUARIO_CONVENIO) 
		INTO STRICT  ds_retorno_w 		 
		FROM 	pessoa_titular_convenio 
		WHERE 	cd_convenio = obter_convenio_estab(wheb_usuario_pck.get_cd_estabelecimento) 
		AND	cd_pessoa_fisica = cd_pessoa_fisica_p;
	end if;
return	ds_retorno_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_dados_partic_cart (cd_pessoa_fisica_p mprev_participante.cd_pessoa_fisica%type default null) FROM PUBLIC;

