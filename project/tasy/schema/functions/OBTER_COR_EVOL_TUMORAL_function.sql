-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_cor_evol_tumoral ( ie_ordem_p bigint, ds_valor_p text) RETURNS bigint AS $body$
BEGIN

if (ie_ordem_p	= -7) then

	if (ds_valor_p	= 'RB') then
		return	4439;
	elsif (ds_valor_p	= 'RC') then
		return	4437;
	elsif (ds_valor_p	= 'RP') then
		return 4438;
	elsif (ds_valor_p	= 'DE') then
		return 4436;
	elsif (ds_valor_p	= 'PD') then
		return 4435;
	end if;

end if;


return	null;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_cor_evol_tumoral ( ie_ordem_p bigint, ds_valor_p text) FROM PUBLIC;

