-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_permite_man_carencia ( nr_seq_proposta_p bigint) RETURNS varchar AS $body$
DECLARE


ie_parametro_w	varchar(2);
ds_parametro_w	varchar(255);


BEGIN

if (coalesce(nr_seq_proposta_p,0) > 0) then

	ds_parametro_w := obter_param_usuario(1232, 53, obter_perfil_ativo, wheb_usuario_pck.get_nm_usuario, wheb_usuario_pck.get_cd_estabelecimento, ds_parametro_w);

	select	max(pls_obter_permite_man_carencia(nr_seq_proposta_p, ds_parametro_w))
	into STRICT	ie_parametro_w
	;

end if;

return	ie_parametro_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_permite_man_carencia ( nr_seq_proposta_p bigint) FROM PUBLIC;

