-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_data_prox_exam_md ( qt_ponto_meld_p bigint, dt_exame_p timestamp ) RETURNS timestamp AS $body$
DECLARE

    dt_prox_exame_w timestamp;

BEGIN
    IF ( qt_ponto_meld_p > 0 )
        AND (dt_exame_p IS NOT NULL AND dt_exame_p::text <> '')
    THEN
        IF ( qt_ponto_meld_p <= 10 ) THEN
            dt_prox_exame_w := dt_exame_p + 360;
        ELSIF ( qt_ponto_meld_p <= 18 ) THEN
            dt_prox_exame_w := dt_exame_p + 90;
        ELSIF ( qt_ponto_meld_p <= 24 ) THEN
            dt_prox_exame_w := dt_exame_p + 30;
        ELSE
            dt_prox_exame_w := dt_exame_p + 7;
        END IF;
    END IF;

    RETURN dt_prox_exame_w;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_data_prox_exam_md ( qt_ponto_meld_p bigint, dt_exame_p timestamp ) FROM PUBLIC;

