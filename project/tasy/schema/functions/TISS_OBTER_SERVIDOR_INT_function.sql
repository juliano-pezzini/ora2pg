-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION tiss_obter_servidor_int (cd_estabelecimento_p bigint, cd_convenio_p bigint) RETURNS varchar AS $body$
DECLARE


c01 CURSOR FOR
SELECT	ds_endereco_servico
from	tiss_servidor_integracao
where	cd_estabelecimento	= cd_estabelecimento_p
and	coalesce(cd_convenio,cd_convenio_p) = cd_convenio_p
order by coalesce(cd_convenio,0);

ds_endereco_servico_w	tiss_servidor_integracao.ds_endereco_servico%type := '';


BEGIN

open C01;
loop
fetch C01 into
	ds_endereco_servico_w;
EXIT WHEN NOT FOUND; /* apply on C01 */

end loop;
close C01;

return	ds_endereco_servico_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION tiss_obter_servidor_int (cd_estabelecimento_p bigint, cd_convenio_p bigint) FROM PUBLIC;

