-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_motivo_audit_item (nr_sequencia_p bigint, nr_atendimento_p bigint, ie_item_p text) RETURNS varchar AS $body$
DECLARE


/* ie_item_p              P - Procedimento     M - Material*/

ds_retorno_w	varchar(100);


BEGIN

if (ie_item_p = 'P') then

    select	max(a.nr_seq_motivo)
    into STRICT	ds_retorno_w
    from	auditoria_propaci a,
		auditoria_conta_paciente b
    where	a.nr_seq_propaci		= nr_sequencia_p
    and		a.nr_seq_auditoria 		= b.nr_sequencia
    and		b.nr_atendimento		= nr_atendimento_p;

elsif (ie_item_p = 'M') then

    select	max(a.nr_seq_motivo)
    into STRICT	ds_retorno_w
    from	auditoria_matpaci a,
		auditoria_conta_paciente b
    where	a.nr_seq_matpaci		= nr_sequencia_p
    and		a.nr_seq_auditoria 		= b.nr_sequencia
    and		b.nr_atendimento		= nr_atendimento_p;

end if;

RETURN	ds_retorno_w;

END	;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_motivo_audit_item (nr_sequencia_p bigint, nr_atendimento_p bigint, ie_item_p text) FROM PUBLIC;

