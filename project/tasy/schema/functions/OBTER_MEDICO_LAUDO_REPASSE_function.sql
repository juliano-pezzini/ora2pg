-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_medico_laudo_repasse (nr_seq_proc_p bigint) RETURNS varchar AS $body$
DECLARE


cd_medico_w		varchar(10);
nr_seq_proc_princ_w	bigint;
nr_atendimento_w	bigint;
nr_sequencia_w		bigint;


BEGIN

select	max(cd_medico_resp)
into STRICT	cd_medico_w
from	laudo_paciente
where	nr_seq_proc	= nr_seq_proc_p;

if (coalesce(cd_medico_w,'0') = '0') then
	select	max(a.nr_seq_proc_princ)
	into STRICT	nr_seq_proc_princ_w
	from	procedimento_paciente  a
	where	a.nr_sequencia	= nr_seq_proc_p;

	select	max(cd_medico_resp)
	into STRICT	cd_medico_w
	from	laudo_paciente
	where	nr_seq_proc	= nr_seq_proc_princ_w;
end if;

return	cd_medico_w;


end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_medico_laudo_repasse (nr_seq_proc_p bigint) FROM PUBLIC;

