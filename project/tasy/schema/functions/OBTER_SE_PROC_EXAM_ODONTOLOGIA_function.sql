-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_proc_exam_odontologia (nr_seq_exame_p bigint, cd_procedimento_p bigint, ie_origem_proced_p bigint, nr_proc_interno_p bigint) RETURNS varchar AS $body$
DECLARE


cd_procedimento_w		med_exame_padrao.cd_procedimento%type := 0;
ie_origem_proced_w		med_exame_padrao.ie_origem_proced%type := 0;
nr_proc_interno_w		med_exame_padrao.nr_proc_interno%type := 0;
ds_retorno_w			varchar(1) := 'N';
ds_retorno1_w			varchar(1) := 'N';
ds_retorno2_w			varchar(1) := 'N';		


BEGIN

if (coalesce(nr_seq_exame_p, 0) > 0) then

	select 	coalesce(max(cd_procedimento), 0),
			coalesce(max(ie_origem_proced), 0),
			coalesce(max(nr_proc_interno), 0)	
	into STRICT 	cd_procedimento_w,
			ie_origem_proced_w,
			nr_proc_interno_w
	from 	med_exame_padrao
	where 	nr_sequencia = nr_seq_exame_p;
	
	ds_retorno1_w := obter_se_proc_odontologia(cd_procedimento_w, ie_origem_proced_w, nr_proc_interno_w);

end if;

ds_retorno2_w := obter_se_proc_odontologia(cd_procedimento_p, ie_origem_proced_p, nr_proc_interno_p);


if (ds_retorno1_w = 'S') or (ds_retorno2_w = 'S') then

	ds_retorno_w := 'S';

end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_proc_exam_odontologia (nr_seq_exame_p bigint, cd_procedimento_p bigint, ie_origem_proced_p bigint, nr_proc_interno_p bigint) FROM PUBLIC;

