-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_regra_cad_web ( ie_tipo_segurado_p text, cd_funcao_p bigint, nm_campo_p text, nm_tabela_p text) RETURNS varchar AS $body$
DECLARE


/* +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Finalidade: verificar regra em OPS - Cadastro de regras / OPS - Cadastro de pessoas / Regra solicitação de alteração pessoa física.

Caso o campo esteja vinculado a esta regra, será criada uma solicitação de alteração cadastral, se não estiver vinculado será alterado diretamente no cadastro de pessoa física.
-------------------------------------------------------------------------------------------------------------------
Locais de chamada direta:
[  ]  Objetos do dicionário [  ] Tasy (Delphi/Java) [X] Portal [  ]  Relatórios [ ] Outros:

Esta function é chamada ao finalizar a requisição, e a mesma ficar em análise, conforme parâmetro[11] - Ao finalizar a requisição se a mesma ficar em análise exigir informações de contato.
 ------------------------------------------------------------------------------------------------------------------
Pontos de atenção:
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ */
ds_retorno_w		varchar(20);
qt_registros_w		integer;



BEGIN

ds_retorno_w := 'UPDATE';

select 	count(1)
into STRICT	qt_registros_w
from 	pls_regra_solic_alt_atrib a,
	pls_regra_solic_alt_pf b
where 	a.nr_seq_regra = b.nr_sequencia
and	b.ie_situacao = 'A'
and	b.ie_tipo_segurado = ie_tipo_segurado_p
and	b.cd_funcao = cd_funcao_p
and	b.nm_tabela = nm_tabela_p
and	a.nm_atributo = nm_campo_p;

if (coalesce(qt_registros_w,0) <> 0) then
	ds_retorno_w := 'CRIA_SOLICITACAO';
end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_regra_cad_web ( ie_tipo_segurado_p text, cd_funcao_p bigint, nm_campo_p text, nm_tabela_p text) FROM PUBLIC;

