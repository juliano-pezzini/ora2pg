-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_perc_tot_escala_crib_md ( qt_temp_p bigint, qt_resultado_pp bigint, qt_resultado_p bigint, qt_base_p bigint ) RETURNS bigint AS $body$
DECLARE


    pr_score_w      double precision := 0;
    qt_total_w      double precision := 0;

BEGIN

qt_total_w 	:= coalesce(qt_total_w,0) + coalesce(qt_temp_P,0)  + coalesce(qt_resultado_pp,0) + coalesce(qt_resultado_P,0) + coalesce(qt_base_P,0);

qt_total_w 	:= qt_total_w * 0.45;
qt_total_w 	:= qt_total_w - 6.476;
qt_total_w 	:= dividir_md(exp(qt_total_w),(1 + exp(qt_total_w)));
qt_total_w	:= (100 * qt_total_w);

if ( qt_total_w >=0) then
	pr_score_w	:= trunc(qt_total_w+0.05,1);
else
	pr_score_w	:= trunc(qt_total_w-0.05,1);	
end if;

    RETURN pr_score_w;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_perc_tot_escala_crib_md ( qt_temp_p bigint, qt_resultado_pp bigint, qt_resultado_p bigint, qt_base_p bigint ) FROM PUBLIC;

