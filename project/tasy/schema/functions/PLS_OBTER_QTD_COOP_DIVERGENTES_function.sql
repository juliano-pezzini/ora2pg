-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_qtd_coop_divergentes (nr_seq_lote_p bigint) RETURNS bigint AS $body$
DECLARE

vl_divergentes_w	bigint := 0;


BEGIN
	select  count(distinct b.nr_sequencia)
	into STRICT	vl_divergentes_w
	from    pls_w_import_cad_unimed b,
		pls_cad_unimed_inconsist a
	where   a.nr_seq_cad_unimed  = b.nr_sequencia
	and	a.ie_tipo_inconsistencia  = 3
	and     b.nr_seq_lote = nr_seq_lote_p;

return	vl_divergentes_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_qtd_coop_divergentes (nr_seq_lote_p bigint) FROM PUBLIC;

