-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_mat_setor ( nr_seq_regra_p bigint, cd_setor_p bigint, cd_convenio_p bigint, ie_tipo_atendimento_p bigint) RETURNS char AS $body$
DECLARE


ie_liberado_w	char(1) := 'S';


BEGIN

select	coalesce(max('N'),'S')
into STRICT	ie_liberado_w
from	proc_int_mat_prescr_setor where		nr_seq_regra = nr_seq_regra_p LIMIT 1;

if (ie_liberado_w = 'N') then
	select	coalesce(max('N'),'S')
	into STRICT	ie_liberado_w
	from	proc_int_mat_prescr_setor where		nr_seq_regra = nr_seq_regra_p
	and		((coalesce(cd_setor_atendimento::text, '') = '') or (cd_setor_atendimento = cd_setor_p))
	and		((coalesce(ie_tipo_atendimento::text, '') = '') or (ie_tipo_atendimento = ie_tipo_atendimento_p))
	and		((coalesce(cd_convenio_exc::text, '') = '') or (cd_convenio_exc = cd_convenio_p)) LIMIT 1;
end if;

return	ie_liberado_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_mat_setor ( nr_seq_regra_p bigint, cd_setor_p bigint, cd_convenio_p bigint, ie_tipo_atendimento_p bigint) FROM PUBLIC;

