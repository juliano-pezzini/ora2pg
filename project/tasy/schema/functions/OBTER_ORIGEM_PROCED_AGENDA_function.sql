-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_origem_proced_agenda (cd_estabelecimento_p bigint, ie_tipo_convenio_p bigint, cd_convenio_p bigint) RETURNS bigint AS $body$
DECLARE


ie_origem_proced_w		bigint;

c01 CURSOR FOR
SELECT ie_origem_proced
from 	regra_origem_proced
where 	coalesce(ie_tipo_convenio, coalesce(ie_tipo_convenio_p,0)) 	= coalesce(ie_tipo_convenio_p,0)
and 	coalesce(cd_convenio, coalesce(cd_convenio_p,0)) 	= coalesce(cd_convenio_p,0)
and	cd_estabelecimento				= coalesce(cd_estabelecimento_p, cd_estabelecimento)
order by
	coalesce(cd_convenio,0),
	coalesce(ie_tipo_convenio,0),
	coalesce(ie_tipo_atendimento,0);


BEGIN

ie_origem_proced_w	:= 1;
OPEN C01;
LOOP
FETCH C01 into
	ie_origem_proced_w;
EXIT WHEN NOT FOUND; /* apply on c01 */
	ie_origem_proced_w	:= ie_origem_proced_w;
END LOOP;
CLOSE C01;


if (ie_tipo_convenio_p = 3) and (not ie_origem_proced_w in (2,3)) then
	ie_origem_proced_w	:= 3;
end if;

RETURN ie_origem_proced_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_origem_proced_agenda (cd_estabelecimento_p bigint, ie_tipo_convenio_p bigint, cd_convenio_p bigint) FROM PUBLIC;

