-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION get_ext_access_dynamic_report (cd_estabelecimento_p estabelecimento.cd_estabelecimento%type, cd_perfil_p perfil.cd_perfil%type, nm_usuario_p usuario.nm_usuario%type) RETURNS varchar AS $body$
DECLARE


ie_resultado_w			varchar(1) := 'S';


BEGIN

begin
	select	'S'
	into STRICT	ie_resultado_w
	from	regra_gerar_rel_dinamico
	where	ie_situacao = 'A'
	and	((nm_usuario_regra_rel = nm_usuario_p) or (coalesce(nm_usuario_regra_rel::text, '') = ''))
	and	((cd_perfil = cd_perfil_p) or (coalesce(cd_perfil::text, '') = ''))
	and	((cd_estabelecimento = cd_estabelecimento_p) or (coalesce(cd_estabelecimento::text, '') = ''));
exception
when no_data_found then
	ie_resultado_w := 'N';
end;

return ie_resultado_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION get_ext_access_dynamic_report (cd_estabelecimento_p estabelecimento.cd_estabelecimento%type, cd_perfil_p perfil.cd_perfil%type, nm_usuario_p usuario.nm_usuario%type) FROM PUBLIC;

