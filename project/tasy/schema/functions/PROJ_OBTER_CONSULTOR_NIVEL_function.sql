-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION proj_obter_consultor_nivel (nr_seq_nivel_p bigint) RETURNS bigint AS $body$
DECLARE

qt_consultores_w	bigint;


BEGIN

select	count(*)
into STRICT	qt_consultores_w
from (SELECT	z.cd_consultor,
	 max(z.dt_atualizacao) dt_atualizacao
	 from 	proj_consultor_nivel z
	 group by z.cd_consultor) a,
	proj_consultor_nivel b,
	com_canal_consultor c
where	a.dt_atualizacao = b.dt_atualizacao
and	b.cd_consultor = c.cd_pessoa_fisica
and	b.nr_seq_nivel = nr_seq_nivel_p
and	c.ie_situacao = 'A'
and	c.ie_regime_contr = 'CLT'
and	c.ie_consultor_wheb = 'S';

return	qt_consultores_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION proj_obter_consultor_nivel (nr_seq_nivel_p bigint) FROM PUBLIC;

