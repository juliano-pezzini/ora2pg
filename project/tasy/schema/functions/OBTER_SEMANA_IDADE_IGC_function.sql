-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_semana_idade_igc ( dt_atual_p timestamp, dt_nascimento_p timestamp, dt_nascimento_ig_p timestamp, qt_dias_ig_p bigint, qt_semanas_ig_p bigint) RETURNS bigint AS $body$
DECLARE



qt_retorno_w	bigint;
qt_diferenca_w	bigint;
qt_dias_total_w	bigint;
qt_semanas_w	bigint;
qt_dias_w		bigint;

BEGIN

if (dt_nascimento_ig_p IS NOT NULL AND dt_nascimento_ig_p::text <> '') and (qt_dias_ig_p IS NOT NULL AND qt_dias_ig_p::text <> '') and (qt_semanas_ig_p IS NOT NULL AND qt_semanas_ig_p::text <> '') then

	qt_diferenca_w	:= trunc(dt_atual_p - dt_nascimento_p);
	qt_dias_total_w:= coalesce(qt_dias_ig_p,0) + coalesce(qt_semanas_ig_p,0) * 7;
	qt_dias_total_w	:= qt_dias_total_w + qt_diferenca_w;
	qt_semanas_w	:= trunc(dividir(qt_dias_total_w,7));
	qt_dias_w		:= mod(qt_dias_total_w,7);
	qt_retorno_w	:= coalesce(qt_semanas_w,0);

end if;

return	qt_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_semana_idade_igc ( dt_atual_p timestamp, dt_nascimento_p timestamp, dt_nascimento_ig_p timestamp, qt_dias_ig_p bigint, qt_semanas_ig_p bigint) FROM PUBLIC;

