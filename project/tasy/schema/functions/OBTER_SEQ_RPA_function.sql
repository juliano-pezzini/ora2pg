-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_seq_rpa (cd_pessoa_fisica_p text) RETURNS varchar AS $body$
DECLARE

ds_erro_p	varchar(100);
nr_pis_pasep_w	varchar(11);

BEGIN
if (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') then
	begin
	select 	nr_pis_pasep
	into STRICT 	nr_pis_pasep_w
	from 	pessoa_fisica
	where  	cd_pessoa_fisica = cd_pessoa_fisica_p;
	if (nr_pis_pasep_w = '') then
		begin
		ds_erro_p := wheb_mensagem_pck.get_texto(802899);
		end;
	end if;
	end;
end if;

return	ds_erro_p;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_seq_rpa (cd_pessoa_fisica_p text) FROM PUBLIC;

