-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION qt_obter_box_agendado (nr_seq_ageint_item_p bigint, nr_seq_pend_quimio_p bigint, dt_agenda_p timestamp) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(255);


BEGIN

if (nr_seq_ageint_item_p > 0) then
	select	max(b.ds_local)
	into STRICT	ds_retorno_w
	from	agenda_quimio_marcacao a,
		qt_local b
	where	a.nr_seq_local = b.nr_sequencia
	and	a.nr_seq_ageint_item = nr_seq_ageint_item_p;
elsif (nr_seq_pend_quimio_p > 0) then
	select	max(b.ds_local)
	into STRICT	ds_retorno_w
	from	agenda_quimio_marcacao a,
		qt_local b
	where	a.nr_seq_local = b.nr_sequencia
	and	a.nr_seq_pend_agenda	= nr_seq_pend_quimio_p
	and	trunc(a.dt_agenda)	= trunc(dt_agenda_p);
end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION qt_obter_box_agendado (nr_seq_ageint_item_p bigint, nr_seq_pend_quimio_p bigint, dt_agenda_p timestamp) FROM PUBLIC;

