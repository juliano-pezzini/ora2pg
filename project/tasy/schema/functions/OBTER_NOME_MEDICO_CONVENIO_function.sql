-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_nome_medico_convenio ( cd_estabelecimento_p bigint, cd_pessoa_fisica_p text, cd_convenio_p bigint, cd_prestador_p text, cd_especialidade_p bigint, cd_categoria_p text, cd_setor_atendimento_p bigint, ie_tipo_atendimento_p bigint) RETURNS varchar AS $body$
DECLARE


cd_medico_convenio_w		varchar(255);

C01 CURSOR FOR
	SELECT	nm_medico_convenio
	from	medico_convenio
	where	cd_pessoa_fisica		= cd_pessoa_fisica_p
	and	cd_convenio			= cd_convenio_p
	and	((cd_estabelecimento = cd_estabelecimento_p) or (coalesce(cd_estabelecimento::text, '') = ''))
	and 	((coalesce(cd_prestador,'0')	= coalesce(cd_prestador_p, '0')) or (coalesce(cd_prestador::text, '') = ''))
	and 	((coalesce(cd_especialidade,0) = coalesce(cd_especialidade_p,0)) or (coalesce(cd_especialidade::text, '') = ''))
	and 	((coalesce(cd_categoria::text, '') = '') or (coalesce(cd_categoria,'0')  = coalesce(cd_categoria_p,'0')))
	and	coalesce(cd_setor_atendimento, coalesce(cd_setor_atendimento_p,0)) = coalesce(cd_setor_atendimento_p,0)
	and	coalesce(ie_tipo_atendimento, coalesce(ie_tipo_atendimento_p,0))	= coalesce(ie_tipo_atendimento_p,0)
	order by
		coalesce(cd_estabelecimento,0),
		coalesce(cd_especialidade,0),
		somente_numero(coalesce(cd_prestador,'0')),
		somente_numero(coalesce(cd_categoria,'0')),
		coalesce(cd_setor_atendimento,0),
		coalesce(ie_tipo_atendimento,0);


BEGIN

cd_medico_convenio_w	:= '';

OPEN C01;
LOOP
FETCH C01 into
	cd_medico_convenio_w;
EXIT WHEN NOT FOUND; /* apply on c01 */
	cd_medico_convenio_w		:= cd_medico_convenio_w;
END LOOP;
CLOSE C01;


RETURN cd_medico_convenio_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_nome_medico_convenio ( cd_estabelecimento_p bigint, cd_pessoa_fisica_p text, cd_convenio_p bigint, cd_prestador_p text, cd_especialidade_p bigint, cd_categoria_p text, cd_setor_atendimento_p bigint, ie_tipo_atendimento_p bigint) FROM PUBLIC;

