-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION hd_obter_atend_ultima_dialise (cd_pessoa_fisica_p text) RETURNS bigint AS $body$
DECLARE


nr_atendimento_w	bigint;
nr_sequencia_W		bigint;


BEGIN

SELECT	nr_atendimento
into STRICT	nr_atendimento_w
FROM	hd_dialise
WHERE	nr_sequencia = (SELECT MAX(nr_sequencia)
					   FROM	  hd_dialise
					   WHERE cd_pessoa_fisica = cd_pessoa_fisica_p);

return	nr_atendimento_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION hd_obter_atend_ultima_dialise (cd_pessoa_fisica_p text) FROM PUBLIC;

