-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_unidade_livre (nr_seq_interno_p bigint) RETURNS varchar AS $body$
DECLARE

ie_unidade_livre_w	varchar(1);
ie_status_unidade_w	varchar(3);


BEGIN

ie_unidade_livre_w := 'S';

if (coalesce(nr_seq_interno_p,0) > 0) then

	select	max(ie_status_unidade)
	into STRICT	ie_status_unidade_w
	from	unidade_atendimento
	where	nr_seq_interno = nr_seq_interno_p;

	if (ie_status_unidade_w <> 'R') then
		ie_unidade_livre_w := 'S';
	else
		ie_unidade_livre_w := 'N';
	end if;

end if;

return	ie_unidade_livre_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_unidade_livre (nr_seq_interno_p bigint) FROM PUBLIC;

