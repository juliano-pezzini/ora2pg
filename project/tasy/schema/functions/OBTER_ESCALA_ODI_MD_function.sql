-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_escala_odi_md ( qt_dor_p bigint, qt_cuidado_pessoal_p bigint, qt_levantamento_p bigint, qt_marcha_p bigint, qt_sentar_p bigint, qt_ficar_em_pe_p bigint, qt_dormir_p bigint, qt_vida_social_p bigint, qt_sexualidade_P bigint, qt_viagem_P bigint ) RETURNS bigint AS $body$
DECLARE


    qt_preenchido_w  bigint := 0;
    qt_pontuacao_w   bigint;
    pr_resultado_w   double precision;

BEGIN
    IF (qt_dor_p IS NOT NULL AND qt_dor_p::text <> '') THEN
        qt_preenchido_w := qt_preenchido_w + 1;
    END IF;
    IF (qt_cuidado_pessoal_p IS NOT NULL AND qt_cuidado_pessoal_p::text <> '') THEN
        qt_preenchido_w := qt_preenchido_w + 1;
    END IF;
    IF (qt_levantamento_p IS NOT NULL AND qt_levantamento_p::text <> '') THEN
        qt_preenchido_w := qt_preenchido_w + 1;
    END IF;
    IF (qt_marcha_p IS NOT NULL AND qt_marcha_p::text <> '') THEN
        qt_preenchido_w := qt_preenchido_w + 1;
    END IF;
    IF (qt_sentar_p IS NOT NULL AND qt_sentar_p::text <> '') THEN
        qt_preenchido_w := qt_preenchido_w + 1;
    END IF;
    IF (qt_ficar_em_pe_p IS NOT NULL AND qt_ficar_em_pe_p::text <> '') THEN
        qt_preenchido_w := qt_preenchido_w + 1;
    END IF;
    IF (qt_dormir_p IS NOT NULL AND qt_dormir_p::text <> '') THEN
        qt_preenchido_w := qt_preenchido_w + 1;
    END IF;
    IF (qt_vida_social_p IS NOT NULL AND qt_vida_social_p::text <> '') THEN
        qt_preenchido_w := qt_preenchido_w + 1;
    END IF;
    IF (qt_sexualidade_P IS NOT NULL AND qt_sexualidade_P::text <> '') THEN
        qt_preenchido_w := qt_preenchido_w + 1;
    END IF;
    IF (qt_viagem_P IS NOT NULL AND qt_viagem_P::text <> '') THEN
        qt_preenchido_w := qt_preenchido_w + 1;
    END IF;
    qt_pontuacao_w := (coalesce(qt_dor_p, 0) + coalesce(qt_cuidado_pessoal_p, 0) +
                      coalesce(qt_levantamento_p, 0) + coalesce(qt_marcha_p, 0) +
                      coalesce(qt_sentar_p,0) + coalesce(qt_ficar_em_pe_p, 0) + 
                      coalesce(qt_dormir_p, 0) + coalesce(qt_vida_social_p, 0) + 
                      coalesce(qt_sexualidade_P, 0) + coalesce(qt_viagem_P, 0));

    BEGIN

        pr_resultado_w := dividir_md(qt_pontuacao_w ,5 * qt_preenchido_w) * 100;

    EXCEPTION
        WHEN OTHERS THEN
            NULL;
    END;

    RETURN pr_resultado_w;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_escala_odi_md ( qt_dor_p bigint, qt_cuidado_pessoal_p bigint, qt_levantamento_p bigint, qt_marcha_p bigint, qt_sentar_p bigint, qt_ficar_em_pe_p bigint, qt_dormir_p bigint, qt_vida_social_p bigint, qt_sexualidade_P bigint, qt_viagem_P bigint ) FROM PUBLIC;

