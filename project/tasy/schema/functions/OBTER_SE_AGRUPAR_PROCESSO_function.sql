-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_agrupar_processo ( nr_atendimento_p bigint, dt_horario_p timestamp, ie_status_p text, nr_seq_processo_p bigint) RETURNS varchar AS $body$
DECLARE


ie_agrupar_w	varchar(1);


BEGIN
if (nr_atendimento_p IS NOT NULL AND nr_atendimento_p::text <> '') and (dt_horario_p IS NOT NULL AND dt_horario_p::text <> '') and (ie_status_p IS NOT NULL AND ie_status_p::text <> '') and (nr_seq_processo_p IS NOT NULL AND nr_seq_processo_p::text <> '') then

	select	CASE WHEN count(*)=0 THEN 'N'  ELSE 'S' END
	into STRICT	ie_agrupar_w
	from    	adep_processo
	where   	nr_atendimento       		= nr_atendimento_p
	and     	dt_horario_processo	= dt_horario_p
	and     	ie_status_processo   	= ie_status_p
	and     	nr_sequencia         		<> nr_seq_processo_p;

end if;

return ie_agrupar_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_agrupar_processo ( nr_atendimento_p bigint, dt_horario_p timestamp, ie_status_p text, nr_seq_processo_p bigint) FROM PUBLIC;

