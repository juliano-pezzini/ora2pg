-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_dt_fim_teste_prev_proj ( DT_INI_P timestamp, IE_TAMANHO_P text) RETURNS timestamp AS $body$
DECLARE

  PR_RETORNO_W	timestamp  := NULL;

BEGIN
  IF (coalesce(DT_INI_P::text, '') = '') THEN
    PR_RETORNO_W := NULL;
  ELSIF (IE_TAMANHO_P = 'XS') THEN
    PR_RETORNO_W := TRUNC(OBTER_DIA_UTIL_PERIODO(1, DT_INI_P, 1));
  ELSIF (IE_TAMANHO_P = 'S') THEN
    PR_RETORNO_W := TRUNC(OBTER_DIA_UTIL_PERIODO(1, DT_INI_P, 1));
  ELSIF (IE_TAMANHO_P = 'M') THEN
    PR_RETORNO_W := TRUNC(OBTER_DIA_UTIL_PERIODO(1, DT_INI_P, 3));
  ELSIF (IE_TAMANHO_P = 'L') THEN
    PR_RETORNO_W := TRUNC(OBTER_DIA_UTIL_PERIODO(1, DT_INI_P, 5));
  ELSIF (IE_TAMANHO_P = 'XL') THEN
    PR_RETORNO_W := TRUNC(OBTER_DIA_UTIL_PERIODO(1, DT_INI_P, 10));
  END IF;

  RETURN	PR_RETORNO_W;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_dt_fim_teste_prev_proj ( DT_INI_P timestamp, IE_TAMANHO_P text) FROM PUBLIC;

