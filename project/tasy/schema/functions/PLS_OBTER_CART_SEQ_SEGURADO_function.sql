-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_cart_seq_segurado (cd_usuario_plano_p text, nr_seq_segurado_p bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w		varchar(30) := null;
nr_seq_segurado_w	bigint;
cd_pessoa_fisica_w	varchar(10);


BEGIN
if (cd_usuario_plano_p IS NOT NULL AND cd_usuario_plano_p::text <> '') then
	select	max(nr_seq_segurado)
	into STRICT	ds_retorno_w
	from	pls_segurado_carteira
	where	cd_usuario_plano = cd_usuario_plano_p;
elsif (nr_seq_segurado_p IS NOT NULL AND nr_seq_segurado_p::text <> '') then
	select	max(cd_usuario_plano)
	into STRICT	ds_retorno_w
	from	pls_segurado_carteira
	where	nr_seq_segurado = nr_seq_segurado_p;
end if;

return ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_cart_seq_segurado (cd_usuario_plano_p text, nr_seq_segurado_p bigint) FROM PUBLIC;

