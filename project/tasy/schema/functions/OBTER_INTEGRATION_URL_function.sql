-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_integration_url ( nr_sequencia_p bigint) RETURNS varchar AS $body$
DECLARE


--ds_return_w			varchar2();
ie_evento_w			intpd_eventos.ie_evento%type;
NR_SEQ_SISTEMA_w	INTPD_EVENTOS_SISTEMA.NR_SEQ_SISTEMA%type;


BEGIN

select	max(b.ie_evento),
		max(a.nr_seq_sistema)
into STRICT	ie_evento_w,
        nr_seq_sistema_w
from	intpd_eventos_sistema a,
		intpd_eventos b,
		INTPD_CONFIG_EVENTOS c
where	a.nr_seq_evento = b.nr_sequencia
and		c.ie_evento = b.ie_evento
and		a.nr_sequencia = nr_sequencia_p
and		c.IE_ENVIA_RECEBE = 'R';

if (ie_evento_w IS NOT NULL AND ie_evento_w::text <> '') then
	return '/WhebServidorIntegracao/philips_rest/endpoint?event='||ie_evento_w||chr(38)||'system='||nr_seq_sistema_w;
end if;

return null;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_integration_url ( nr_sequencia_p bigint) FROM PUBLIC;

