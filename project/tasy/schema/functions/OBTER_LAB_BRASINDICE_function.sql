-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_lab_brasindice ( CD_ESTABELECIMENTO_P bigint, CD_MATERIAL_P bigint, DT_VIGENCIA_P timestamp, IE_OPCAO_P text) RETURNS varchar AS $body$
DECLARE



cd_laboratorio_w		varchar(06);
dt_vigencia_w			timestamp;

ds_laboratorio_w		varchar(40) := '';

C01 CURSOR FOR
	SELECT	CD_LABORATORIO
	FROM	MATERIAL_BRASINDICE
	WHERE	CD_MATERIAL						= CD_MATERIAL_P
	and	coalesce(IE_SITUACAO, 'A')					= 'A'
	and	coalesce(dt_vigencia,dt_vigencia_w)				<= dt_vigencia_w
	and	coalesce(cd_estabelecimento, coalesce(cd_estabelecimento_p, 0))	= coalesce(cd_estabelecimento_p, 0)
	order by coalesce(dt_vigencia,clock_timestamp() - interval '1000 days');


BEGIN

DT_VIGENCIA_W 	:= coalesce(DT_VIGENCIA_P,clock_timestamp());

OPEN C01;
LOOP
	FETCH C01 into	cd_laboratorio_w;
	EXIT WHEN NOT FOUND; /* apply on C01 */
	cd_laboratorio_w	:= cd_laboratorio_w;
END LOOP;
CLOSE C01;


if (ie_opcao_p = 'C') then
	Return cd_laboratorio_w;
else
	if (cd_laboratorio_w IS NOT NULL AND cd_laboratorio_w::text <> '') then
		select coalesce(max(ds_laboratorio), wheb_mensagem_pck.get_texto(799515,'CD_LABORATORIO=' || cd_laboratorio_w))
		into STRICT ds_laboratorio_w
		from brasindice_laboratorio
		where cd_laboratorio = cd_laboratorio_w;
	end if;

	Return ds_laboratorio_w;
end if;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_lab_brasindice ( CD_ESTABELECIMENTO_P bigint, CD_MATERIAL_P bigint, DT_VIGENCIA_P timestamp, IE_OPCAO_P text) FROM PUBLIC;

