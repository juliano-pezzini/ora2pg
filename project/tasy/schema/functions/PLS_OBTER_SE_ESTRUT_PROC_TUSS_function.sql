-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_se_estrut_proc_tuss ( cd_procedimento_p bigint, ie_origem_proced_p bigint, cd_area_p bigint, cd_especialidade_p bigint, cd_grupo_p bigint) RETURNS varchar AS $body$
DECLARE


ie_retorno_w			varchar(1)	:= 'S';
ie_origem_proced_w		bigint;
cd_grupo_proc_w			bigint;
cd_especialidade_w		bigint;
cd_area_procedimento_w		bigint;
qt_registros_w			bigint;


BEGIN
SELECT * FROM pls_obter_estrut_proc(	cd_procedimento_p, ie_origem_proced_p, cd_area_procedimento_w, cd_especialidade_w, cd_grupo_proc_w, ie_origem_proced_w) INTO STRICT cd_area_procedimento_w, cd_especialidade_w, cd_grupo_proc_w, ie_origem_proced_w;

if (cd_area_p IS NOT NULL AND cd_area_p::text <> '') and (cd_area_p <> cd_area_procedimento_w) then
	ie_retorno_w	:= 'N';
end if;

if (cd_especialidade_p IS NOT NULL AND cd_especialidade_p::text <> '') and (cd_especialidade_p <> cd_especialidade_w) then
	ie_retorno_w	:= 'N';
end if;

if (cd_grupo_p IS NOT NULL AND cd_grupo_p::text <> '') and (cd_grupo_p <> cd_grupo_proc_w) then
	ie_retorno_w	:= 'N';
end if;

if (ie_origem_proced_p IS NOT NULL AND ie_origem_proced_p::text <> '') and (ie_origem_proced_p <> ie_origem_proced_w) then
	ie_retorno_w	:= 'N';
end if;

return	ie_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_se_estrut_proc_tuss ( cd_procedimento_p bigint, ie_origem_proced_p bigint, cd_area_p bigint, cd_especialidade_p bigint, cd_grupo_p bigint) FROM PUBLIC;

