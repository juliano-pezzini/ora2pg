-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_preco_proc_sus ( dt_procedimento_p timestamp, cd_procedimento_p bigint, ie_origem_proced_p bigint, cd_estabelecimento_p bigint) RETURNS bigint AS $body$
DECLARE

vl_total_sus_w	double precision := 0;
vl_aux_num_w		bigint;
dt_aux_date_w		timestamp;
ds_aux_varch_w	varchar(255);
vl_procedimento_w	bigint;


BEGIN

/*select	(A.VL_DIARIA + A.VL_TAXAS + A.VL_SADT + A.VL_TPH + A.VL_DIALISE + A.VL_SADT_RX + A.VL_SADT_PC + A.VL_MATMED + 	A.VL_CONTRASTE +

A.VL_FILME_RX + A.VL_GESSO + A.VL_QUIMIOTERAPIA + A.VL_FILME_RESSONANCIA + A.VL_OUTROS + 		A.VL_MEDICO) VL_TOTAL_SUS
into	vl_total_sus_w
from	SUS_VALOR_PROC_PACIENTE A,
	procedimento_paciente b
where	a.nr_sequencia = b.nr_sequencia
and	b.cd_procedimento = cd_procedimento_p
and	b.ie_origem_proced = ie_origem_proced_p
and	a.dt_competencia =	(Select	max(x.dt_competencia)
				 from	SUS_VALOR_PROC_PACIENTE x,

				procedimento_paciente y
				 where	x.nr_sequencia = y.nr_sequencia
				 and	y.cd_procedimento = cd_procedimento_p
				 and	y.ie_origem_proced = ie_origem_proced
				 and	x.dt_competencia <= dt_procedimento_p);*/
if	vl_total_sus_w = 0 then
	SELECT * FROM DEFINE_PRECO_PROC_SUS(dt_procedimento_p, cd_procedimento_p, ie_origem_proced_p, cd_estabelecimento_p, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_procedimento_w, vl_aux_num_w, dt_aux_date_w, ds_aux_varch_w, vl_aux_num_w) INTO STRICT vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_procedimento_w, vl_aux_num_w, dt_aux_date_w, ds_aux_varch_w, vl_aux_num_w;

	vl_total_sus_w	:= vl_procedimento_w;
end if;

return vl_total_sus_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_preco_proc_sus ( dt_procedimento_p timestamp, cd_procedimento_p bigint, ie_origem_proced_p bigint, cd_estabelecimento_p bigint) FROM PUBLIC;

