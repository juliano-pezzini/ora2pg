-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_estrutura_proc (cd_area_procedimento_p bigint, cd_especialidade_p bigint, cd_grupo_proc_p bigint, cd_procedimento_p bigint, cd_area_filtro_p bigint, cd_espec_filtro_p bigint, cd_grupo_filtro_p bigint, cd_proc_filtro_p bigint) RETURNS varchar AS $body$
DECLARE


cont_w		bigint;
ds_retorno_w	varchar(10);


BEGIN

ds_retorno_w := 'N';

if (cd_area_filtro_p IS NOT NULL AND cd_area_filtro_p::text <> '') then

	if (cd_area_filtro_p = cd_area_procedimento_p) then
		ds_retorno_w := 'S';
	end if;

	if (cd_especialidade_p IS NOT NULL AND cd_especialidade_p::text <> '') then
		select	count(*)
		into STRICT	cont_w
		from	especialidade_proc
		where	cd_area_procedimento	= cd_area_filtro_p
		and	cd_especialidade	= cd_especialidade_p;
	end if;

	if (cd_grupo_proc_p IS NOT NULL AND cd_grupo_proc_p::text <> '') then
		select	count(*)
		into STRICT	cont_w
		from	grupo_proc a,
			especialidade_proc b
		where	a.cd_especialidade	= b.cd_especialidade
		and	b.cd_area_procedimento	= cd_area_filtro_p
		and	a.cd_grupo_proc		= cd_grupo_proc_p;
	end if;

	if (cd_procedimento_p IS NOT NULL AND cd_procedimento_p::text <> '') then
		select	count(*)
		into STRICT	cont_w
		from	grupo_proc a,
			especialidade_proc b,
			procedimento c
		where	a.cd_especialidade	= b.cd_especialidade
		and	c.cd_grupo_proc		= a.cd_grupo_proc
		and	b.cd_area_procedimento	= cd_area_filtro_p
		and	c.cd_procedimento	= cd_procedimento_p;

	end if;

	if (cont_w > 0) then
		ds_retorno_w := 'S';
	end if;
end if;

if (cd_espec_filtro_p IS NOT NULL AND cd_espec_filtro_p::text <> '') then

	if (cd_espec_filtro_p = cd_especialidade_p) then
		ds_retorno_w := 'S';
	end if;

	if (cd_grupo_proc_p IS NOT NULL AND cd_grupo_proc_p::text <> '') then
		select	count(*)
		into STRICT	cont_w
		from	grupo_proc
		where	cd_especialidade	= cd_espec_filtro_p
		and	cd_grupo_proc		= cd_grupo_proc_p;
	end if;

	if (cd_procedimento_p IS NOT NULL AND cd_procedimento_p::text <> '') then
		select	count(*)
		into STRICT	cont_w
		from	grupo_proc a,
			procedimento c
		where	a.cd_grupo_proc		= c.cd_grupo_proc
		and	a.cd_especialidade	= cd_espec_filtro_p
		and	c.cd_procedimento	= cd_procedimento_p;

	end if;

	if (cont_w > 0) then
		ds_retorno_w := 'S';
	end if;
end if;

if (cd_grupo_filtro_p IS NOT NULL AND cd_grupo_filtro_p::text <> '') then

	if (cd_grupo_filtro_p = cd_grupo_proc_p) then
		ds_retorno_w := 'S';
	end if;

	if (cd_procedimento_p IS NOT NULL AND cd_procedimento_p::text <> '') then
		select	count(*)
		into STRICT	cont_w
		from	procedimento c
		where	c.cd_grupo_proc		= cd_grupo_filtro_p
		and	c.cd_procedimento	= cd_procedimento_p;
	end if;

	if (cont_w > 0) then
		ds_retorno_w := 'S';
	end if;
end if;

if (cd_proc_filtro_p IS NOT NULL AND cd_proc_filtro_p::text <> '') then

	if (cd_proc_filtro_p = cd_procedimento_p) then
		ds_retorno_w  := 'S';
	end if;

end if;

return ds_retorno_w;

end	;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_estrutura_proc (cd_area_procedimento_p bigint, cd_especialidade_p bigint, cd_grupo_proc_p bigint, cd_procedimento_p bigint, cd_area_filtro_p bigint, cd_espec_filtro_p bigint, cd_grupo_filtro_p bigint, cd_proc_filtro_p bigint) FROM PUBLIC;

