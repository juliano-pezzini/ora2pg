-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_seq_complemento (cd_pessoa_fisica_p text, ie_tipo_complemento_p bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w varchar(255);

BEGIN

if (coalesce(ie_tipo_complemento_p::text, '') = '') then
	select 	max(nr_sequencia)
	into STRICT	ds_retorno_w
	from	compl_pessoa_fisica
	where	cd_pessoa_fisica = cd_pessoa_fisica_p;
else
	select 	max(nr_sequencia)
	into STRICT	ds_retorno_w
	from	compl_pessoa_fisica
	where	ie_tipo_complemento = ie_tipo_complemento_p
	and	cd_pessoa_fisica = cd_pessoa_fisica_p;
end if;

return ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_seq_complemento (cd_pessoa_fisica_p text, ie_tipo_complemento_p bigint) FROM PUBLIC;

