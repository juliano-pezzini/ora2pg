-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_vl_conta_mat_aprovacao ( nr_atendimento_p bigint, cd_material_p bigint) RETURNS bigint AS $body$
DECLARE


vl_material_w		double precision := 0;
nr_interno_conta_w		bigint;



BEGIN

select	coalesce(max(nr_interno_conta),0)
into STRICT	nr_interno_conta_w
from	material_atend_paciente
where	nr_atendimento = nr_atendimento_p
and	cd_material = cd_material_p;

if (nr_interno_conta_w > 0) then

	select	max(vl_unitario)
	into STRICT	vl_material_w
	from	material_atend_paciente
	where	nr_atendimento = nr_atendimento_p
	and	nr_interno_conta = nr_interno_conta_w
	and	cd_material = cd_material_p;

end if;

return	vl_material_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_vl_conta_mat_aprovacao ( nr_atendimento_p bigint, cd_material_p bigint) FROM PUBLIC;

