-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';



--
-- dblink wrapper to call function l10n_obter_min_seq_case as an autonomous transaction
--
CREATE EXTENSION IF NOT EXISTS dblink;

CREATE OR REPLACE FUNCTION l10n_obter_min_seq_case ( nr_seq_episodio_p bigint, nr_episodio_p text) RETURNS bigint AS $body$
DECLARE
	-- Change this to reflect the dblink connection string
	v_conn_str  text := format('port=%s dbname=%s user=%s', current_setting('port'), current_database(), current_user);
	v_query     text;

	v_ret	bigint;
BEGIN
	v_query := 'SELECT * FROM l10n_obter_min_seq_case_atx ( ' || quote_nullable(nr_seq_episodio_p) || ',' || quote_nullable(nr_episodio_p) || ' )';
	SELECT * INTO v_ret FROM dblink(v_conn_str, v_query) AS p (ret bigint);
	RETURN v_ret;

END;
$body$ LANGUAGE plpgsql SECURITY DEFINER;




CREATE OR REPLACE FUNCTION l10n_obter_min_seq_case_atx ( nr_seq_episodio_p bigint, nr_episodio_p text) RETURNS bigint AS $body$
DECLARE


nr_seq_episodio_w	episodio_paciente.nr_sequencia%type;

c01 CURSOR FOR
SELECT	nr_sequencia
from	episodio_paciente
where	nr_episodio = nr_episodio_p

union all

select	nr_sequencia
from	episodio_paciente
where	nr_sequencia = nr_episodio_p

union all

select	nr_sequencia
from	episodio_paciente
where	nr_episodio = nr_seq_episodio_p
order by 1 desc;
BEGIN

open c01;
loop
fetch c01 into	
	nr_seq_episodio_w;
EXIT WHEN NOT FOUND; /* apply on c01 */
end loop;
close c01;

if (coalesce(nr_seq_episodio_w::text, '') = '') then
	nr_seq_episodio_w	:=	nr_seq_episodio_p;
end if;

return	nr_seq_episodio_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 IMMUTABLE;
-- REVOKE ALL ON FUNCTION l10n_obter_min_seq_case ( nr_seq_episodio_p bigint, nr_episodio_p text) FROM PUBLIC; -- REVOKE ALL ON FUNCTION l10n_obter_min_seq_case_atx ( nr_seq_episodio_p bigint, nr_episodio_p text) FROM PUBLIC;

