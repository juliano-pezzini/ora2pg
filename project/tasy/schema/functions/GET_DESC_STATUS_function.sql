-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION get_desc_status (IE_STATUS_P bigint) RETURNS varchar AS $body$
DECLARE

  ds_result_w   varchar(255)  := '';
  ds_expression_w bigint;

BEGIN

    SELECT CASE WHEN IE_STATUS_P=1 THEN 330131 WHEN IE_STATUS_P=2 THEN 330733 WHEN IE_STATUS_P=3 THEN 487167 WHEN IE_STATUS_P=4 THEN 330498 WHEN IE_STATUS_P=5 THEN 494374 WHEN IE_STATUS_P=6 THEN 517410 WHEN IE_STATUS_P=7 THEN 283761 END
    INTO STRICT ds_expression_w
;

    SELECT expressao_pck.obter_desc_expressao(ds_expression_w)
    INTO STRICT ds_result_w
;
    RETURN ds_result_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION get_desc_status (IE_STATUS_P bigint) FROM PUBLIC;

