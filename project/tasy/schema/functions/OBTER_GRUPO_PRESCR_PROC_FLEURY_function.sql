-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_grupo_prescr_proc_fleury (nr_prescricao_p bigint) RETURNS bigint AS $body$
DECLARE


nr_grupo_integracao_w	bigint;

ie_grupa_horario_w	lab_parametro.ie_agrupa_ficha_fleury%type;
cd_estabelecimento_w	estabelecimento.cd_estabelecimento%type;


BEGIN

if (nr_prescricao_p IS NOT NULL AND nr_prescricao_p::text <> '') then

	select	coalesce(max(a.cd_estabelecimento),wheb_usuario_pck.get_cd_estabelecimento)
	into STRICT	cd_estabelecimento_w
	from	prescr_medica a
	where	a.nr_prescricao = nr_prescricao_p;

	ie_grupa_horario_w	:= obter_dados_lab_parametro('AF', cd_estabelecimento_w);

	if (ie_grupa_horario_w <> 'H') then
		select 	min(nr_grupo_integracao)
		into STRICT	nr_grupo_integracao_w
		from	prescr_procedimento
		where	nr_prescricao = nr_prescricao_p
		and	(nr_grupo_integracao IS NOT NULL AND nr_grupo_integracao::text <> '')
		and	coalesce(dt_integracao::text, '') = '';
	else
		select 	min(nr_grupo_integracao)
		into STRICT	nr_grupo_integracao_w
		from	prescr_proc_hor
		where	nr_prescricao = nr_prescricao_p
		and	(nr_grupo_integracao IS NOT NULL AND nr_grupo_integracao::text <> '')
		and	coalesce(dt_integracao::text, '') = '';
	end if;

end if;

return 	nr_grupo_integracao_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_grupo_prescr_proc_fleury (nr_prescricao_p bigint) FROM PUBLIC;

