-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION lab_obter_valor_campo_tabela ( nm_tabela text, nm_campo text, ds_restricao text) RETURNS varchar AS $body$
DECLARE


query		varchar(4000);
resultado	varchar(4000);


BEGIN

query := 'SELECT '||nm_campo||' FROM '||nm_tabela||' WHERE '||ds_restricao;
RAISE NOTICE 'Query: %', query;
EXECUTE query INTO STRICT resultado;
--DBMS_OUTPUT.PUT_LINE('Record: ' || record);
RETURN resultado;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION lab_obter_valor_campo_tabela ( nm_tabela text, nm_campo text, ds_restricao text) FROM PUBLIC;

