-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_entrada_clinica (cd_pessoa_fisica_p bigint) RETURNS varchar AS $body$
DECLARE

    data_dialise_w varchar(255);

BEGIN

    SELECT to_char(MIN(dt_dialise), 'dd/mm/yyyy HH24:MI')
      INTO STRICT data_dialise_w
      FROM hd_dialise
     WHERE cd_pessoa_fisica = cd_pessoa_fisica_p;

    RETURN data_dialise_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_entrada_clinica (cd_pessoa_fisica_p bigint) FROM PUBLIC;

