-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION ptu_obter_inconsist_trans_v50 ( nr_seq_guia_p bigint, nr_seq_requisicao_p bigint, nr_seq_consulta_p bigint, cd_transacao_p text, nr_qt_reg_p bigint, nr_seq_proc_p bigint, nr_seq_mat_p bigint) RETURNS bigint AS $body$
DECLARE

/* +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Finalidade:  Obter inconsistências transações v50.

ROTINA UTILIZADA NAS TRANSAÇÕES PTU VIA SCS HOMOLOGADAS COM A UNIMED BRASIL.
QUANDO FOR ALTERAR, FAVOR VERIFICAR COM O ANÁLISTA RESPONSÁVEL PARA A REALIZAÇÃO DE TESTES.
-------------------------------------------------------------------------------------------------------------------
Locais de chamada direta:
[ X ]  Objetos do dicionário [  ] Tasy (Delphi/Java) [  ] Portal [  ]  Relatórios [ ] Outros:
 ------------------------------------------------------------------------------------------------------------------
Pontos de atenção:

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ */
cd_inconsistencia_w		smallint	:= 0;
qt_registro_w			smallint;
nr_seq_inconsist_w		bigint;
count_registro_w		bigint := 1;
nr_seq_servico_w		bigint := 0;

C01 CURSOR FOR
	SELECT	nr_seq_inconsistencia
	from (
		SELECT	a.nr_seq_inconsistencia
		from	ptu_intercambio_consist	a,
			ptu_inconsistencia	b
		where	a.nr_seq_inconsistencia	= b.nr_sequencia
		and	b.cd_inconsistencia	between 2001 and 2999
		and (a.nr_seq_guia		= nr_seq_guia_p)
		and (coalesce(a.nr_seq_procedimento::text, '') = '')
		and	a.cd_transacao		= cd_transacao_p
		and (exists (	select	1
					from	pls_guia_plano_proc	x
					where	x.ie_status	in ('N','M','K')
					and	x.nr_sequencia	= nr_seq_proc_p))
		
union all

		select	a.nr_seq_inconsistencia
		from	ptu_intercambio_consist	a,
			ptu_inconsistencia	b
		where	a.nr_seq_inconsistencia	= b.nr_sequencia
		and	b.cd_inconsistencia	between 2001 and 2999
		and (a.nr_seq_guia		= nr_seq_guia_p)
		and (a.nr_seq_procedimento	= nr_seq_proc_p)
		and	a.cd_transacao		= cd_transacao_p
		and (exists (	select	1
					from	pls_guia_plano_proc	x
					where	x.ie_status	in ('N','M','K')
					and	x.nr_sequencia	= nr_seq_proc_p))) alias11
	group by nr_seq_inconsistencia;

C02 CURSOR FOR
	SELECT	nr_seq_inconsistencia
	from (
		SELECT	a.nr_seq_inconsistencia
		from	ptu_intercambio_consist	a,
			ptu_inconsistencia	b
		where	a.nr_seq_inconsistencia	= b.nr_sequencia
		and	b.cd_inconsistencia	between 2001 and 2999
		and (a.nr_seq_requisicao	= nr_seq_requisicao_p)
		and (coalesce(a.nr_seq_procedimento::text, '') = '')
		and	a.cd_transacao		= cd_transacao_p
		and (exists (	select	1
					from	pls_requisicao_proc	x
					where	x.ie_status	in ('N','G')
					and	x.nr_sequencia	= nr_seq_proc_p))
		
union all

		select	a.nr_seq_inconsistencia
		from	ptu_intercambio_consist	a,
			ptu_inconsistencia	b
		where	a.nr_seq_inconsistencia	= b.nr_sequencia
		and	b.cd_inconsistencia	between 2001 and 2999
		and (a.nr_seq_requisicao	= nr_seq_requisicao_p)
		and (a.nr_seq_procedimento	= nr_seq_proc_p)
		and	a.cd_transacao		= cd_transacao_p
		and (exists (	select	1
					from	pls_requisicao_proc	x
					where	x.ie_status	in ('N','G')
					and	x.nr_sequencia	= nr_seq_proc_p))) alias11
	group by nr_seq_inconsistencia;

C03 CURSOR FOR
	SELECT	nr_seq_inconsistencia
	from (
		SELECT	a.nr_seq_inconsistencia
		from	ptu_intercambio_consist	a,
			ptu_inconsistencia	b
		where	a.nr_seq_inconsistencia	= b.nr_sequencia
		and	b.cd_inconsistencia	between 2001 and 2999
		and (a.nr_seq_guia		= nr_seq_guia_p)
		and (coalesce(a.nr_seq_material::text, '') = '')
		and	a.cd_transacao		= cd_transacao_p
		and (exists (	select	1
					from	pls_guia_plano_mat	x
					where	x.ie_status	in ('N','M','K')
					and	x.nr_sequencia	= nr_seq_mat_p))
		
union all

		select	a.nr_seq_inconsistencia
		from	ptu_intercambio_consist	a,
			ptu_inconsistencia	b
		where	a.nr_seq_inconsistencia	= b.nr_sequencia
		and	b.cd_inconsistencia	between 2001 and 2999
		and (a.nr_seq_guia		= nr_seq_guia_p)
		and (a.nr_seq_material	= nr_seq_mat_p)
		and	a.cd_transacao		= cd_transacao_p
		and (exists (	select	1
					from	pls_guia_plano_mat	x
					where	x.ie_status	in ('N','M','K')
					and	x.nr_sequencia	= nr_seq_mat_p))) alias11
	group by nr_seq_inconsistencia;

C04 CURSOR FOR
	SELECT	nr_seq_inconsistencia
	from (
		SELECT	a.nr_seq_inconsistencia
		from	ptu_intercambio_consist	a,
			ptu_inconsistencia	b
		where	a.nr_seq_inconsistencia	= b.nr_sequencia
		and	b.cd_inconsistencia	between 2001 and 2999
		and (a.nr_seq_requisicao	= nr_seq_requisicao_p)
		and (coalesce(a.nr_seq_material::text, '') = '')
		and	a.cd_transacao		= cd_transacao_p
		and (exists (	select	1
					from	pls_requisicao_mat	x
					where	x.ie_status	in ('N','G')
					and	x.nr_sequencia	= nr_seq_mat_p))
		
union all

		select	a.nr_seq_inconsistencia
		from	ptu_intercambio_consist	a,
			ptu_inconsistencia	b
		where	a.nr_seq_inconsistencia	= b.nr_sequencia
		and	b.cd_inconsistencia	between 2001 and 2999
		and (a.nr_seq_requisicao	= nr_seq_requisicao_p)
		and (a.nr_seq_material	= nr_seq_mat_p)
		and	a.cd_transacao		= cd_transacao_p
		and (exists (	select	1
					from	pls_requisicao_mat	x
					where	x.ie_status	in ('N','G')
					and	x.nr_sequencia	= nr_seq_mat_p))) alias11
	group by nr_seq_inconsistencia;

C05 CURSOR FOR
	SELECT	distinct(nr_seq_inconsistencia)
	from	ptu_intercambio_consist
	where	((nr_seq_consulta_benef	= nr_seq_consulta_p)
	or (nr_seq_consulta_prest	= nr_seq_consulta_p))
	and	cd_transacao		= cd_transacao_p;

C06 CURSOR FOR
	SELECT	nr_seq_inconsistencia
	from (
		SELECT	nr_seq_inconsistencia
		from	ptu_intercambio_consist
		where	nr_seq_guia		= nr_seq_guia_p
		and	coalesce(nr_seq_requisicao::text, '') = ''
		and	coalesce(nr_seq_procedimento::text, '') = ''
		and	coalesce(nr_seq_material::text, '') = ''
		and	cd_transacao		= cd_transacao_p
		
union all

		select	nr_seq_inconsistencia
		from	ptu_intercambio_consist
		where	nr_seq_guia		= nr_seq_guia_p
		and	coalesce(nr_seq_requisicao::text, '') = ''
		and	nr_seq_procedimento	= nr_seq_consulta_p
		and	coalesce(nr_seq_material::text, '') = ''
		and	cd_transacao		= cd_transacao_p
		
union all

		select	nr_seq_inconsistencia
		from	ptu_intercambio_consist
		where	nr_seq_guia		= nr_seq_guia_p
		and	coalesce(nr_seq_requisicao::text, '') = ''
		and	coalesce(nr_seq_procedimento::text, '') = ''
		and	nr_seq_material		= nr_seq_consulta_p
		and	cd_transacao		= cd_transacao_p
		
union all

		select	nr_seq_inconsistencia
		from	ptu_intercambio_consist
		where	coalesce(nr_seq_guia::text, '') = ''
		and	nr_seq_requisicao	= nr_seq_requisicao_p
		and	coalesce(nr_seq_procedimento::text, '') = ''
		and	coalesce(nr_seq_material::text, '') = ''
		and	cd_transacao		= cd_transacao_p
		
union all

		select	nr_seq_inconsistencia
		from	ptu_intercambio_consist
		where	coalesce(nr_seq_guia::text, '') = ''
		and	nr_seq_requisicao	= nr_seq_requisicao_p
		and	nr_seq_procedimento	= nr_seq_consulta_p
		and	coalesce(nr_seq_material::text, '') = ''
		and	cd_transacao		= cd_transacao_p
		
union all

		select	nr_seq_inconsistencia
		from	ptu_intercambio_consist
		where	coalesce(nr_seq_guia::text, '') = ''
		and	nr_seq_requisicao	= nr_seq_requisicao_p
		and	coalesce(nr_seq_procedimento::text, '') = ''
		and	nr_seq_material		= nr_seq_consulta_p
		and	cd_transacao		= cd_transacao_p) alias14
	group by nr_seq_inconsistencia;
BEGIN

if (coalesce(nr_seq_consulta_p,0)	= 0) then
	if (nr_seq_requisicao_p IS NOT NULL AND nr_seq_requisicao_p::text <> '') then
		for r_C02_w in C02 loop
			begin
			cd_inconsistencia_w	:= 0;

			select	count(distinct(nr_seq_inconsistencia))
			into STRICT	qt_registro_w
			from (
				SELECT	nr_seq_inconsistencia
				from	ptu_intercambio_consist
				where	nr_seq_requisicao	= nr_seq_requisicao_p
				and	coalesce(nr_seq_procedimento::text, '') = ''
				and	cd_transacao		= cd_transacao_p
				
union all

				SELECT	nr_seq_inconsistencia
				from	ptu_intercambio_consist
				where	nr_seq_requisicao	= nr_seq_requisicao_p
				and	nr_seq_procedimento	= nr_seq_proc_p
				and	cd_transacao		= cd_transacao_p) alias3;

			select	max(cd_inconsistencia)
			into STRICT	cd_inconsistencia_w
			from	ptu_inconsistencia
			where	nr_sequencia	= r_C02_w.nr_seq_inconsistencia;

			if (nr_qt_reg_p	<= qt_registro_w) then
				if (count_registro_w  =  nr_qt_reg_p) then
					return	cd_inconsistencia_w;
				else
					count_registro_w := count_registro_w  + 1;
				end if;
			else
				cd_inconsistencia_w	:= 0;
			end if;
			end;
		end loop; -- C02
		for r_C04_w in C04 loop
			begin
			cd_inconsistencia_w	:= 0;

			select	count(distinct(nr_seq_inconsistencia))
			into STRICT	qt_registro_w
			from (
				SELECT	nr_seq_inconsistencia
				from	ptu_intercambio_consist
				where	nr_seq_requisicao	= nr_seq_requisicao_p
				and	coalesce(nr_seq_material::text, '') = ''
				and	cd_transacao		= cd_transacao_p
				
union all

				SELECT	nr_seq_inconsistencia
				from	ptu_intercambio_consist
				where	nr_seq_requisicao	= nr_seq_requisicao_p
				and	nr_seq_material	= nr_seq_mat_p
				and	cd_transacao		= cd_transacao_p) alias3;

			select	max(cd_inconsistencia)
			into STRICT	cd_inconsistencia_w
			from	ptu_inconsistencia
			where	nr_sequencia	= r_C04_w.nr_seq_inconsistencia;

			if (nr_qt_reg_p	<= qt_registro_w) then
				if (count_registro_w  =  nr_qt_reg_p) then
					return	cd_inconsistencia_w;
				else
					count_registro_w 	:= count_registro_w  + 1;
				end if;
			else
				cd_inconsistencia_w	:= 0;
			end if;
			end;
		end loop; -- C04
	elsif (nr_seq_guia_p IS NOT NULL AND nr_seq_guia_p::text <> '') then

		for r_C01_w in C01 loop
			begin
			cd_inconsistencia_w	:= 0;

			select	count(distinct(nr_seq_inconsistencia))
			into STRICT	qt_registro_w
			from (
				SELECT	nr_seq_inconsistencia
				from	ptu_intercambio_consist
				where	nr_seq_guia		= nr_seq_guia_p
				and	coalesce(nr_seq_procedimento::text, '') = ''
				and	cd_transacao		= cd_transacao_p
				
union all

				SELECT	nr_seq_inconsistencia
				from	ptu_intercambio_consist
				where	nr_seq_guia		= nr_seq_guia_p
				and	nr_seq_procedimento	= nr_seq_proc_p
				and	cd_transacao		= cd_transacao_p) alias3;

			select	max(cd_inconsistencia)
			into STRICT	cd_inconsistencia_w
			from	ptu_inconsistencia
			where	nr_sequencia	= r_C01_w.nr_seq_inconsistencia;

			if (nr_qt_reg_p	<= qt_registro_w) then
				if (count_registro_w  =  nr_qt_reg_p) then
					return	cd_inconsistencia_w;
				else
					count_registro_w := count_registro_w  + 1;
				end if;
			else
				cd_inconsistencia_w	:= 0;
			end if;
			end;
		end loop; -- C01
		for r_C03_w in C03 loop
			begin
			cd_inconsistencia_w	:= 0;

			select	count(distinct(nr_seq_inconsistencia))
			into STRICT	qt_registro_w
			from (
				SELECT	nr_seq_inconsistencia
				from	ptu_intercambio_consist
				where	nr_seq_guia		= nr_seq_guia_p
				and	coalesce(nr_seq_material::text, '') = ''
				and	cd_transacao		= cd_transacao_p
				
union all

				SELECT	nr_seq_inconsistencia
				from	ptu_intercambio_consist
				where	nr_seq_guia		= nr_seq_guia_p
				and	nr_seq_material		= nr_seq_mat_p
				and	cd_transacao		= cd_transacao_p) alias3;

			select	max(cd_inconsistencia)
			into STRICT	cd_inconsistencia_w
			from	ptu_inconsistencia
			where	nr_sequencia	= r_C03_w.nr_seq_inconsistencia;

			if (nr_qt_reg_p	<= qt_registro_w) then
				if (count_registro_w  =  nr_qt_reg_p) then
					return	cd_inconsistencia_w;
				else
					count_registro_w 	:= count_registro_w  + 1;
				end if;
			else
				cd_inconsistencia_w	:= 0;
			end if;
			end;
		end loop;

	elsif (coalesce(nr_seq_requisicao_p::text, '') = '') and (coalesce(nr_seq_guia_p::text, '') = '') then
		if (nr_qt_reg_p	= 1) then
			cd_inconsistencia_w	:= 2002;
		end if;
	end if;
elsif (cd_transacao_p	= '00361') then

	for r_C06_w in C06 loop
		begin

		select	count(distinct(nr_seq_inconsistencia))
		into STRICT	qt_registro_w
		from (
			SELECT	nr_seq_inconsistencia
			from	ptu_intercambio_consist
			where	nr_seq_guia		= nr_seq_guia_p
			and	coalesce(nr_seq_requisicao::text, '') = ''
			and	coalesce(nr_seq_procedimento::text, '') = ''
			and	coalesce(nr_seq_material::text, '') = ''
			and	cd_transacao		= cd_transacao_p
			
union all

			SELECT	nr_seq_inconsistencia
			from	ptu_intercambio_consist
			where	nr_seq_guia		= nr_seq_guia_p
			and	coalesce(nr_seq_requisicao::text, '') = ''
			and	nr_seq_procedimento	= nr_seq_consulta_p
			and	coalesce(nr_seq_material::text, '') = ''
			and	cd_transacao		= cd_transacao_p
			
union all

			select	nr_seq_inconsistencia
			from	ptu_intercambio_consist
			where	nr_seq_guia		= nr_seq_guia_p
			and	coalesce(nr_seq_requisicao::text, '') = ''
			and	coalesce(nr_seq_procedimento::text, '') = ''
			and	nr_seq_material		= nr_seq_consulta_p
			and	cd_transacao		= cd_transacao_p
			
union all

			select	nr_seq_inconsistencia
			from	ptu_intercambio_consist
			where	coalesce(nr_seq_guia::text, '') = ''
			and	nr_seq_requisicao	= nr_seq_requisicao_p
			and	coalesce(nr_seq_procedimento::text, '') = ''
			and	coalesce(nr_seq_material::text, '') = ''
			and	cd_transacao		= cd_transacao_p
			
union all

			select	nr_seq_inconsistencia
			from	ptu_intercambio_consist
			where	coalesce(nr_seq_guia::text, '') = ''
			and	nr_seq_requisicao	= nr_seq_requisicao_p
			and	nr_seq_procedimento	= nr_seq_consulta_p
			and	coalesce(nr_seq_material::text, '') = ''
			and	cd_transacao		= cd_transacao_p
			
union all

			select	nr_seq_inconsistencia
			from	ptu_intercambio_consist
			where	coalesce(nr_seq_guia::text, '') = ''
			and	nr_seq_requisicao	= nr_seq_requisicao_p
			and	coalesce(nr_seq_procedimento::text, '') = ''
			and	nr_seq_material		= nr_seq_consulta_p
			and	cd_transacao		= cd_transacao_p) alias17;

		select	max(cd_inconsistencia)
		into STRICT	cd_inconsistencia_w
		from	ptu_inconsistencia
		where	nr_sequencia	= r_C06_w.nr_seq_inconsistencia;

		if (nr_qt_reg_p	<= qt_registro_w) then
			if (count_registro_w  =  nr_qt_reg_p) then
				return	cd_inconsistencia_w;
			else
				count_registro_w := count_registro_w  + 1;
			end if;
		else
			cd_inconsistencia_w	:= 0;
		end if;
		end;
	end loop;
else

	for r_C05_w in C05 loop
		begin
		select	count(distinct(nr_seq_inconsistencia))
		into STRICT	qt_registro_w
		from	ptu_intercambio_consist
		where	((nr_seq_consulta_benef	= nr_seq_consulta_p)
		or (nr_seq_consulta_prest	= nr_seq_consulta_p))
		and	cd_transacao		= cd_transacao_p;

		select	max(cd_inconsistencia)
		into STRICT	cd_inconsistencia_w
		from	ptu_inconsistencia
		where	nr_sequencia	= r_C05_w.nr_seq_inconsistencia;

		if (nr_qt_reg_p	<= qt_registro_w) then
			if (count_registro_w  =  nr_qt_reg_p) then
				return	cd_inconsistencia_w;
			else
				count_registro_w := count_registro_w  + 1;
			end if;
		else
			cd_inconsistencia_w	:= 0;
		end if;
		end;
	end loop;
end if;


return	cd_inconsistencia_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION ptu_obter_inconsist_trans_v50 ( nr_seq_guia_p bigint, nr_seq_requisicao_p bigint, nr_seq_consulta_p bigint, cd_transacao_p text, nr_qt_reg_p bigint, nr_seq_proc_p bigint, nr_seq_mat_p bigint) FROM PUBLIC;

