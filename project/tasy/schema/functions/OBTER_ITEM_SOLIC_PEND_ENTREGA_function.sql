-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_item_solic_pend_entrega (nr_solic_compra_p bigint) RETURNS varchar AS $body$
DECLARE


ie_entrega_pendente_w		varchar(1) := 'N';
nr_ordem_compra_w		bigint;
nr_item_oci_w			integer;

c02 CURSOR FOR
SELECT	nr_ordem_compra,
	nr_item_oci
from	ordem_compra_item
where	nr_solic_compra = nr_solic_compra_p;


BEGIN

open c02;
loop
fetch c02 into
	nr_ordem_compra_w,
	nr_item_oci_w;
EXIT WHEN NOT FOUND; /* apply on c02 */
	begin
	if (ie_entrega_pendente_w = 'N') then
		ie_entrega_pendente_w := obter_se_oci_pend_entrega(nr_ordem_compra_w,nr_item_oci_w);
	end if;
	end;
end loop;
close c02;

return ie_entrega_pendente_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_item_solic_pend_entrega (nr_solic_compra_p bigint) FROM PUBLIC;

