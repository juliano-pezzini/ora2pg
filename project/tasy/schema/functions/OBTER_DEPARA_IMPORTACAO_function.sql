-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_depara_importacao ( nm_tabela_p text, ds_atributo_p text, ds_valor_arquivo_p text) RETURNS varchar AS $body$
DECLARE


ds_valor_tabela_w varchar(100);


BEGIN
    begin
        select	imp.ds_valor_tabela
        into STRICT	ds_valor_tabela_w
        from	reg_depara_importacao imp
        where	imp.nm_tabela = nm_tabela_p
        and	imp.ds_atributo = ds_atributo_p
        and	imp.ds_valor_arquivo = ds_valor_arquivo_p;
    exception
        when no_data_found then
        ds_valor_tabela_w := ds_valor_arquivo_p;
    end;

    return ds_valor_tabela_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_depara_importacao ( nm_tabela_p text, ds_atributo_p text, ds_valor_arquivo_p text) FROM PUBLIC;

