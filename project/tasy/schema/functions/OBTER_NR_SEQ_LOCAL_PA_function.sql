-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_nr_seq_local_pa ( nr_atendimento_p bigint) RETURNS bigint AS $body$
DECLARE

 
 
nr_seq_local_pa_w	bigint := 0;

 

BEGIN 
 
if (coalesce(nr_atendimento_p,0) > 0) then 
	begin 
	select	coalesce(max(nr_seq_local_pa),0) 
	into STRICT	nr_seq_local_pa_w 
	from	atendimento_paciente 
	where	nr_atendimento = nr_atendimento_p 
	and	1 = ( 
			SELECT	x.cd_classif_setor 
			from	setor_atendimento x, 
				atend_paciente_unidade y 
			where	y.nr_atendimento = nr_atendimento_p 
			and	y.cd_setor_atendimento	= x.cd_setor_atendimento 
			and	y.NR_SEQ_INTERNO = (	SELECT max(c.NR_SEQ_INTERNO) 
				 			from	setor_atendimento b, 
								atend_paciente_unidade c 
							where	c.nr_atendimento = nr_atendimento_p 
							and	c.cd_setor_atendimento	= b.cd_setor_atendimento));	
	end;
end if;
 
return nr_seq_local_pa_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_nr_seq_local_pa ( nr_atendimento_p bigint) FROM PUBLIC;

