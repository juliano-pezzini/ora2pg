-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_unid_medida_sepse_ped ( nr_seq_atributo_p bigint, vl_atributo_p bigint default null) RETURNS varchar AS $body$
DECLARE

	ds_unid_medida_w varchar(100);

BEGIN
	case nr_seq_atributo_p
		when 80 then ds_unid_medida_w := obter_desc_expressao(727864);
		when 81 then ds_unid_medida_w := obter_desc_expressao(292520);		
		when 83 then ds_unid_medida_w := obter_desc_expressao(727866);
		when 84 then ds_unid_medida_w := obter_desc_expressao(761785);
		when 88 then
			if (vl_atributo_p IS NOT NULL AND vl_atributo_p::text <> '') then 
				ds_unid_medida_w := obter_desc_expressao(890201);
			else
				ds_unid_medida_w := null;
			end if;	
		when 89 then ds_unid_medida_w := obter_desc_expressao(309212);
		when 90 then ds_unid_medida_w := obter_desc_expressao(792358);
		else ds_unid_medida_w := null;
	end case;
	return ds_unid_medida_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_unid_medida_sepse_ped ( nr_seq_atributo_p bigint, vl_atributo_p bigint default null) FROM PUBLIC;

