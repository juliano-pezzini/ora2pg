-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';



--
-- dblink wrapper to call function gerint_obter_prim_mov as an autonomous transaction
--
CREATE EXTENSION IF NOT EXISTS dblink;

CREATE OR REPLACE FUNCTION gerint_obter_prim_mov (nr_atendimento_p bigint) RETURNS varchar AS $body$
DECLARE
	-- Change this to reflect the dblink connection string
	v_conn_str  text := format('port=%s dbname=%s user=%s', current_setting('port'), current_database(), current_user);
	v_query     text;

	v_ret	varchar;
BEGIN
	v_query := 'SELECT * FROM gerint_obter_prim_mov_atx ( ' || quote_nullable(nr_atendimento_p) || ' )';
	SELECT * INTO v_ret FROM dblink(v_conn_str, v_query) AS p (ret varchar);
	RETURN v_ret;

END;
$body$ LANGUAGE plpgsql SECURITY DEFINER;




CREATE OR REPLACE FUNCTION gerint_obter_prim_mov_atx (nr_atendimento_p bigint) RETURNS varchar AS $body$
DECLARE


ie_prim_movimentacao_w		varchar(1);
BEGIN

select	CASE WHEN count(*)=0 THEN 'S'  ELSE 'N' END
into STRICT	ie_prim_movimentacao_w
from 	atend_paciente_unidade
where	nr_atendimento = nr_atendimento_p;

return	ie_prim_movimentacao_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION gerint_obter_prim_mov (nr_atendimento_p bigint) FROM PUBLIC; -- REVOKE ALL ON FUNCTION gerint_obter_prim_mov_atx (nr_atendimento_p bigint) FROM PUBLIC;

