-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_nr_aidf_nota_fiscal ( nr_sequencia_p bigint) RETURNS varchar AS $body$
DECLARE


nr_nota_fiscal_w		varchar(255);
cd_serie_nf_w			nota_fiscal.cd_serie_nf%type;
cd_estabelecimento_w	smallint;
nr_aidf_w				varchar(20);


BEGIN

/*Utilizada para buscar o numero da autorizacao de impressão de documento fiscal - AIDF da nota fiscal*/

select	nr_nota_fiscal,
	cd_estabelecimento,
	cd_serie_nf
into STRICT	nr_nota_fiscal_w,
	cd_estabelecimento_w,
	cd_serie_nf_w
from	nota_fiscal
where	nr_sequencia = nr_sequencia_p;

select	max(nr_id_dief)
into STRICT	nr_aidf_w
from	nota_fiscal_aidf
where	cd_serie_nf = cd_serie_nf_w
and	(nr_nota_fiscal_w)::numeric  between nr_nota_ini and nr_nota_fim;

return	nr_aidf_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_nr_aidf_nota_fiscal ( nr_sequencia_p bigint) FROM PUBLIC;

