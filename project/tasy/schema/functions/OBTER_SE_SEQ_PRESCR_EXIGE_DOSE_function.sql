-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_seq_prescr_exige_dose (nr_prescricao_p bigint, nr_seq_prescr_p bigint) RETURNS bigint AS $body$
DECLARE


qt_retorno_w		bigint := 0;


BEGIN

Select	count(*)
into STRICT	qt_retorno_w
from	prescr_procedimento a, exame_laboratorio b
where	a.nr_seq_exame = b.nr_seq_exame
and	coalesce(b.ie_informar_ult_dose,'N') <> 'N'
and	((coalesce(a.dt_ult_dose_medic::text, '') = '') or (coalesce(a.qt_ult_dose_medic::text, '') = ''))
and	a.nr_prescricao	= nr_prescricao_p
and	a.nr_sequencia = nr_seq_prescr_p;

return qt_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_seq_prescr_exige_dose (nr_prescricao_p bigint, nr_seq_prescr_p bigint) FROM PUBLIC;

