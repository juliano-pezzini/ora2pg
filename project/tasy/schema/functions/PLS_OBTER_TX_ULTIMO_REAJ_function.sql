-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_tx_ultimo_reaj (nr_seq_faixa_etaria_p bigint) RETURNS bigint AS $body$
DECLARE


pr_reajustado_w		double precision	:= 0;
nr_seq_reajuste_w	bigint;

BEGIN

if (nr_seq_faixa_etaria_p <> 0) then
	select	coalesce(max(nr_sequencia),0)
	into STRICT	nr_seq_reajuste_w
	from	pls_reajuste_preco
	where	nr_seq_preco	= nr_seq_faixa_etaria_p
	and	dt_reajuste	= (	SELECT	max(dt_reajuste)
					from	pls_reajuste_preco
					where	nr_seq_preco	= nr_seq_faixa_etaria_p);
	if (nr_seq_reajuste_w > 0) then
		select	coalesce(pr_reajustado,0)
		into STRICT	pr_reajustado_w
		from	pls_reajuste_preco
		where	nr_sequencia	= nr_seq_reajuste_w;
	else
		pr_reajustado_w := 0;
	end if;
end if;

return pr_reajustado_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_tx_ultimo_reaj (nr_seq_faixa_etaria_p bigint) FROM PUBLIC;

