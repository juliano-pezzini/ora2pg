-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION lic_obter_prioridade_fornec ( nr_seq_fornec_p bigint, nr_seq_lic_item_p bigint) RETURNS bigint AS $body$
DECLARE


nr_prioridade_w		bigint;
qt_registro_w		bigint;


BEGIN

select	count(*)
into STRICT	qt_registro_w
from	reg_lic_fornec_lance
where	nr_seq_fornec = nr_seq_fornec_p;

if (qt_registro_w > 0) then

	select	max(nr_prioridade)
	into STRICT	nr_prioridade_w
	from	reg_lic_fornec_lance
	where	nr_seq_fornec = nr_seq_fornec_p
	and	nr_seq_lic_item = nr_seq_lic_item_p;

end if;

return	nr_prioridade_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION lic_obter_prioridade_fornec ( nr_seq_fornec_p bigint, nr_seq_lic_item_p bigint) FROM PUBLIC;

