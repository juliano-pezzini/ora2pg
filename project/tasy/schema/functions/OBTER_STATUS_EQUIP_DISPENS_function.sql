-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_status_equip_dispens (nr_seq_equipamento_p bigint) RETURNS varchar AS $body$
DECLARE


/* +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Finalidade:
-------------------------------------------------------------------------------------------------------------------
Locais de chamada direta:
[  ]  Objetos do dicionário [ ] Tasy (Delphi/Java) [  ] Portal [  ]  Relatórios [ ] Outros:
 ------------------------------------------------------------------------------------------------------------------
Pontos de atenção:
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ */
ie_status_w		varchar(10)	:= 'I';
nr_seq_reg_w	bigint	:= null;


BEGIN

if (nr_seq_equipamento_p IS NOT NULL AND nr_seq_equipamento_p::text <> '') then
	select	max(nr_sequencia)
	into STRICT	nr_seq_reg_w
	from	equip_disp_elet_status a
	where	nr_seq_equipamento = nr_seq_equipamento_p;

	if (nr_seq_reg_w IS NOT NULL AND nr_seq_reg_w::text <> '') then
		select	coalesce(ie_status,'I')
		into STRICT	ie_status_w
		from	equip_disp_elet_status a
		where	nr_sequencia	= nr_seq_reg_w;
	end if;
end if;

return	ie_status_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_status_equip_dispens (nr_seq_equipamento_p bigint) FROM PUBLIC;

