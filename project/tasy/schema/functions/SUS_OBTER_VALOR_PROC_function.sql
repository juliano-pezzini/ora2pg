-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION sus_obter_valor_proc ( dt_procedimento_p timestamp, cd_procedimento_p bigint, ie_origem_proced_p bigint, cd_estabelecimento_p bigint) RETURNS bigint AS $body$
DECLARE

vl_total_sus_w	double precision := 0;
vl_aux_num_w		bigint;
dt_aux_date_w		timestamp;
ds_aux_varch_w	varchar(255);
vl_procedimento_w	bigint;


BEGIN

SELECT * FROM DEFINE_PRECO_PROC_SUS(dt_procedimento_p, cd_procedimento_p, ie_origem_proced_p, cd_estabelecimento_p, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_procedimento_w, vl_aux_num_w, dt_aux_date_w, ds_aux_varch_w, vl_aux_num_w) INTO STRICT vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_aux_num_w, vl_procedimento_w, vl_aux_num_w, dt_aux_date_w, ds_aux_varch_w, vl_aux_num_w;

vl_total_sus_w	:= vl_procedimento_w;

return vl_total_sus_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION sus_obter_valor_proc ( dt_procedimento_p timestamp, cd_procedimento_p bigint, ie_origem_proced_p bigint, cd_estabelecimento_p bigint) FROM PUBLIC;

