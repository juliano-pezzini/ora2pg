-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION lote_ent_retorna_urgente (nr_seq_ficha_p bigint, nr_seq_exame_p bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w		varchar(1);
nr_seq_prescr_w		bigint;
nr_prescricao_w		bigint;
ie_tipo_valor_w		varchar(10);


BEGIN

select 	max(nr_prescricao)
into STRICT	nr_prescricao_w
from	lote_ent_sec_ficha
where	nr_sequencia = nr_seq_ficha_p;

select	max(a.nr_sequencia)
into STRICT	nr_seq_prescr_w
from	prescr_procedimento a,
		lote_ent_sec_ficha b
where	a.nr_prescricao = b.nr_prescricao
and		a.nr_prescricao = nr_prescricao_w
and		a.nr_seq_exame = nr_seq_exame_p;

if (nr_prescricao_w IS NOT NULL AND nr_prescricao_w::text <> '') and (nr_seq_exame_p IS NOT NULL AND nr_seq_exame_p::text <> '') and (nr_seq_prescr_w IS NOT NULL AND nr_seq_prescr_w::text <> '') then

	SELECT	max(coalesce(a.ie_urgencia,'N'))
	into STRICT	ds_retorno_w
	FROM	prescr_procedimento a
	WHERE	a.nr_sequencia = nr_seq_prescr_w
	AND		a.nr_seq_exame 		= nr_seq_exame_p
	AND 	a.nr_prescricao 	= nr_prescricao_w;

end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION lote_ent_retorna_urgente (nr_seq_ficha_p bigint, nr_seq_exame_p bigint) FROM PUBLIC;

