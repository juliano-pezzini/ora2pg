-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_w_imp_conversao (nr_seq_nota_p bigint, nr_seq_item_p bigint, nm_atributo_p text) RETURNS varchar AS $body$
DECLARE


cd_interno_w	w_imp_conversao.cd_interno%type;


BEGIN

select	max(a.cd_interno)
into STRICT	cd_interno_w
from	w_imp_conversao a
where	upper(a.nm_atributo)	= upper(nm_atributo_p)
and	coalesce(a.nr_seq_item,0)	= coalesce(nr_seq_item_p,coalesce(a.nr_seq_item,0))
and	coalesce(a.nr_seq_nota,0)	= coalesce(nr_seq_nota_p,coalesce(a.nr_seq_nota,0));

return cd_interno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_w_imp_conversao (nr_seq_nota_p bigint, nr_seq_item_p bigint, nm_atributo_p text) FROM PUBLIC;

