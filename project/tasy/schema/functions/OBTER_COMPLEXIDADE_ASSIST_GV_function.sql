-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_complexidade_assist_gv (nr_atendimento_p bigint) RETURNS varchar AS $body$
DECLARE

 
ds_compexidade_w		varchar(255):= '';
cd_setor_atendimento_w	bigint;

BEGIN
 
if (coalesce(nr_atendimento_p,0) > 0)	then 
	select	max(cd_setor_atendimento) 
	into STRICT	cd_setor_atendimento_w 
	from	unidade_atendimento 
	where	nr_atendimento = nr_atendimento_p;
	 
	if (coalesce(cd_setor_atendimento_w,0) > 0)	then 
		ds_compexidade_w	:=	SUBSTR(obter_complexidade_assist(nr_atendimento_p, cd_setor_atendimento_w, 'G'),1,100);
	end if;
end if;
 
return	ds_compexidade_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_complexidade_assist_gv (nr_atendimento_p bigint) FROM PUBLIC;

