-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_conta_cont_item_cot ( nr_cot_compra_p bigint, nr_item_cot_compra_p bigint) RETURNS varchar AS $body$
DECLARE


cd_conta_contabil_w			varchar(20);
nr_solic_compra_w				bigint;
nr_item_solic_compra_w			integer;

c01 CURSOR FOR
SELECT	a.nr_solic_compra,
	a.nr_item_solic_compra
from	cot_compra_item a
where	a.nr_cot_compra		= nr_cot_compra_p
and	a.nr_item_cot_compra	= nr_item_cot_compra_p
and	(a.nr_solic_compra IS NOT NULL AND a.nr_solic_compra::text <> '')

union

select	b.nr_solic_compra,
	b.nr_item_solic_compra
from	cot_compra_item a,
	cot_compra_solic_agrup b
where	a.nr_cot_compra		= b.nr_cot_compra
and	a.nr_item_cot_compra	= b.nr_item_cot_compra
and	a.nr_cot_compra		= nr_cot_compra_p
and	a.nr_item_cot_compra	= nr_item_cot_compra_p
and	(b.nr_solic_compra IS NOT NULL AND b.nr_solic_compra::text <> '');


BEGIN

open C01;
loop
fetch C01 into
	nr_solic_compra_w,
	nr_item_solic_compra_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin
	select	cd_conta_contabil
	into STRICT	cd_conta_contabil_w
	from	solic_compra_item
	where	nr_solic_compra		= nr_solic_compra_w
	and	nr_item_solic_compra	= nr_item_solic_compra_w;
	end;
end loop;
close C01;

return	cd_conta_contabil_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_conta_cont_item_cot ( nr_cot_compra_p bigint, nr_item_cot_compra_p bigint) FROM PUBLIC;

