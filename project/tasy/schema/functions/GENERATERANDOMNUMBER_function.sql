-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION generaterandomnumber () RETURNS varchar AS $body$
DECLARE


w		varchar(1);
a		varchar(10);
b		varchar(1);
x		varchar(10);
y		varchar(10);
z		varchar(2);
c		varchar(6);
locationId 	varchar(10);  -- Location Id
t		varchar(10);
result_w 	varchar(50);


BEGIN


select	substr('abcdef', dbms_random.value(1,6), 1) ||
	substr('abcdef', dbms_random.value(1,6), 1)
into STRICT a
;

select	Lpad(round(dbms_random.value(1,9)),1,'0')
into STRICT	w
;

select	substr('abcdef', dbms_random.value(1,6), 1) 
into STRICT 	x 
;

select	lpad(round(dbms_random.value(1, 9)),1,'0')
into STRICT	b	
;

select	substr('abcdef', dbms_random.value(1,6), 1) ||
	substr('abcdef', dbms_random.value(1,6), 1)
into STRICT	y
;

select	lpad(round(dbms_random.value(6, 999999)),6,'0')
into STRICT 	c 
;

select	substr('abcdef', dbms_random.value(1,6), 1) 
into STRICT	t
;

select	lpad(round(dbms_random.value(3, 99)),2,'0')
into STRICT 	z	 
;

select  max(CD_LOCATION)
into STRICT 	locationId
from 	ECLIPSE_PARAMETERS;

result_w	:=( locationId || a || to_char(w) || x || to_char(b) || y || to_char(c) || t || to_char(z)  );
return	result_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION generaterandomnumber () FROM PUBLIC;

