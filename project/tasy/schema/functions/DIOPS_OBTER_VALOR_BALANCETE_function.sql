-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION diops_obter_valor_balancete ( nr_seq_periodo_p bigint, ds_conta_p text, ie_tipo_balancete_p text) RETURNS bigint AS $body$
DECLARE


vl_retorno_w	double precision;


BEGIN

if (ie_tipo_balancete_p = 'A') then
	select	max(vl_saldo_final)
	into STRICT	vl_retorno_w
	from	diops_fin_mov_ativo
	where	nr_seq_periodo	= nr_seq_periodo_p
	and	ds_conta = ds_conta_p;
elsif (ie_tipo_balancete_p = 'P') then
	select	max(vl_saldo_final)
	into STRICT	vl_retorno_w
	from	diops_fin_mov_passivo
	where	nr_seq_periodo	= nr_seq_periodo_p
	and	ds_conta = ds_conta_p;
elsif (ie_tipo_balancete_p = 'R') then
	select	max(vl_saldo_final)
	into STRICT	vl_retorno_w
	from	diops_fin_mov_receita
	where	nr_seq_periodo	= nr_seq_periodo_p
	and	ds_conta = ds_conta_p;
elsif (ie_tipo_balancete_p = 'D') then
	select	max(vl_saldo_final)
	into STRICT	vl_retorno_w
	from	diops_fin_mov_despesa
	where	nr_seq_periodo	= nr_seq_periodo_p
	and	ds_conta = ds_conta_p;
end if;

return	coalesce(vl_retorno_w,0);

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION diops_obter_valor_balancete ( nr_seq_periodo_p bigint, ds_conta_p text, ie_tipo_balancete_p text) FROM PUBLIC;

