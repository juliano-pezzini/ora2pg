-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_grupo_usuario (nm_usuario_p text) RETURNS bigint AS $body$
DECLARE


nr_seq_grupo_w		bigint;


BEGIN

	select	max(nr_seq_grupo)
	into STRICT	nr_seq_grupo_w
	from	usuario_grupo_des
	where	nm_usuario_grupo = nm_usuario_p;

	if (coalesce(nr_seq_grupo_w::text, '') = '') then

		select	max(nr_seq_grupo)
		into STRICT	nr_seq_grupo_w
		from	usuario_grupo_sup
		where	nm_usuario_grupo = nm_usuario_p;

	end if;

	return	nr_seq_grupo_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_grupo_usuario (nm_usuario_p text) FROM PUBLIC;

