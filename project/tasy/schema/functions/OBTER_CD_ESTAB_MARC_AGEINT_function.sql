-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_cd_estab_marc_ageint ( nr_seq_agenda_cons_p bigint, nr_seq_agenda_exame_p bigint) RETURNS bigint AS $body$
DECLARE


cd_estab_w	smallint;
vl_retorno_w	bigint := '';

BEGIN

if (coalesce(nr_seq_agenda_cons_p,0) > 0) then
	begin
	select	max(b.cd_estabelecimento)
	into STRICT	cd_estab_w
	from	agenda_consulta a,
		agenda b
	where	a.cd_agenda = b.cd_agenda
	and	a.nr_sequencia = nr_seq_agenda_cons_p;
	end;
elsif (coalesce(nr_seq_agenda_exame_p, 0) > 0) then
	begin
	select	max(b.cd_estabelecimento)
	into STRICT	cd_estab_w
	from	agenda_paciente a,
		agenda b
	where	a.cd_agenda = b.cd_agenda
	and	a.nr_sequencia = nr_seq_agenda_exame_p;
	end;
end if;

return	cd_estab_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_cd_estab_marc_ageint ( nr_seq_agenda_cons_p bigint, nr_seq_agenda_exame_p bigint) FROM PUBLIC;

