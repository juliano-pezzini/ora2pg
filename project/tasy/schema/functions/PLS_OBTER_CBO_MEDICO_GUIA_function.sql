-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_cbo_medico_guia ( cd_pessoa_fisica_p bigint, cd_especialidade_p bigint) RETURNS bigint AS $body$
DECLARE

 
nr_seq_cbo_w		tiss_cbo_saude.nr_seq_cbo_saude%type;


BEGIN 
 
select max(nr_seq_cbo_saude) 
into STRICT	nr_seq_cbo_w 
from	tiss_cbo_saude 
where	cd_pessoa_fisica = cd_pessoa_fisica_p 
and	cd_especialidade = cd_especialidade_p 
order by ie_versao desc;
 
if (coalesce(nr_seq_cbo_w::text, '') = '') then 
	select max(nr_seq_cbo_saude) 
	into STRICT	nr_seq_cbo_w 
	from	medico_especialidade 
	where	(nr_seq_cbo_saude IS NOT NULL AND nr_seq_cbo_saude::text <> '') 
	and	cd_pessoa_fisica = cd_pessoa_fisica_p 
	and	cd_especialidade = cd_especialidade_p;
end if;
 
return	nr_seq_cbo_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_cbo_medico_guia ( cd_pessoa_fisica_p bigint, cd_especialidade_p bigint) FROM PUBLIC;

