-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_qt_os_grupo_inicio_dia ( nr_seq_grupo_des_p bigint, nm_usuario_p text) RETURNS bigint AS $body$
DECLARE


/* Utilizada para os calculos das cores de metas*/

qt_ordem_inicio_dia_w	integer;
nr_seq_grupo_w		bigint;


BEGIN

if (nr_seq_grupo_des_p > 0) then
	nr_seq_grupo_w		:= nr_seq_grupo_des_p;
else
	nr_seq_grupo_w		:= obter_grupo_usuario_wheb(nm_usuario_p);
end if;

select	coalesce(sum(qt_ordem_servico), 0)
into STRICT	qt_ordem_inicio_dia_w
from	man_regra_resumo_meta
where	dt_referencia = Obter_inicio_semana(trunc(clock_timestamp(), 'dd'))
and	nr_seq_grupo_des = nr_seq_grupo_w;




return qt_ordem_inicio_dia_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_qt_os_grupo_inicio_dia ( nr_seq_grupo_des_p bigint, nm_usuario_p text) FROM PUBLIC;

