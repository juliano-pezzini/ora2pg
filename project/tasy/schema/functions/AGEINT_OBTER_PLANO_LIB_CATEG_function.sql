-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION ageint_obter_plano_lib_categ (cd_convenio_p bigint, cd_categoria_p text, cd_plano_p text, cd_estabelecimento_p bigint, dt_atendimento_p timestamp) RETURNS varchar AS $body$
DECLARE


  ie_liberado_w varchar(01) := 'S';
  ie_parametro  varchar(01) := 'S';


BEGIN
  SELECT coalesce(MAX(pa.ie_ativa_categ_plano), 'N')
    INTO STRICT ie_parametro
    FROM parametro_agenda pa
   WHERE pa.cd_estabelecimento = cd_estabelecimento_p;

  --  obter_param_usuario(869, 187, obter_perfil_ativo, obter_usuario_ativo, cd_estabelecimento_p, ie_parametro);
  IF ie_parametro = 'S' AND (cd_plano_p IS NOT NULL AND cd_plano_p::text <> '') THEN
    ie_liberado_w := obter_plano_lib_categoria(cd_convenio_p,
                                               cd_categoria_p,
                                               cd_plano_p,
                                               cd_estabelecimento_p,
                                               dt_atendimento_p);
  END IF;
  RETURN ie_liberado_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION ageint_obter_plano_lib_categ (cd_convenio_p bigint, cd_categoria_p text, cd_plano_p text, cd_estabelecimento_p bigint, dt_atendimento_p timestamp) FROM PUBLIC;

