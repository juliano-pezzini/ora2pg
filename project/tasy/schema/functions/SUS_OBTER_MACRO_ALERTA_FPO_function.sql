-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION sus_obter_macro_alerta_fpo (nr_sequencia_p bigint) RETURNS varchar AS $body$
DECLARE



ds_retorno_w		varchar(2000);
ds_enter_w		varchar(10) := chr(13) || chr(10);


BEGIN

ds_retorno_w :=	substr(	'@regra_fpo = Sequência da regra FPO;'|| ds_enter_w ||
			'@competencia = Data de competência;'|| ds_enter_w ||
			'@vl_orcam = Teto orçamentário;'|| ds_enter_w ||
			'@vl_exec = Valor já executado em conta;'|| ds_enter_w ||
			'@vl_prot = Valor já inserido em protocolo;'|| ds_enter_w ||
			'@qt_fisico = Teto físico;'|| ds_enter_w ||
			'@qt_exec = Quantidade já executada em conta;'|| ds_enter_w ||
			'@qt_prot = Quantidade já inserida em protocolo;',1,2000);

return ds_retorno_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION sus_obter_macro_alerta_fpo (nr_sequencia_p bigint) FROM PUBLIC;

