-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION cpoe_obter_orientacao_prep_amb ( cd_material_p bigint, qt_dose_p bigint, cd_unidade_medida_p text, ie_via_aplicacao_p text, cd_intervalo_p text) RETURNS varchar AS $body$
DECLARE

ds_retorno_w		varchar(255);

	function obter_texto(cd_mensagem bigint) return text is
	;
BEGIN
		return wheb_mensagem_pck.get_texto(cd_mensagem);
	end;

	function obter_desc_intervalo return varchar2 is
	begin
		if (cd_intervalo_p IS NOT NULL AND cd_intervalo_p::text <> '') then
			return Obter_desc_intervalo_prescr(cd_intervalo_p);
		end if;

		return '';
	end;

	function obter_desc_via_aplic return varchar2 is
	begin
		if (ie_via_aplicacao_p IS NOT NULL AND ie_via_aplicacao_p::text <> '') then
			return ' ' || obter_texto(1005806) || ' ' || obter_desc_via(ie_via_aplicacao_p);
		end if;

		return '';
	end;
begin
ds_retorno_w := null;
if (cd_material_p IS NOT NULL AND cd_material_p::text <> '') and (qt_dose_p IS NOT NULL AND qt_dose_p::text <> '') and (cd_unidade_medida_p IS NOT NULL AND cd_unidade_medida_p::text <> '') then
	ds_retorno_w := obter_texto(1005805) ||' '|| qt_dose_p ||' '|| cd_unidade_medida_p || ' ' || obter_desc_material(cd_material_p) || ' ' || obter_desc_intervalo || obter_desc_via_aplic;
end if;

return ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION cpoe_obter_orientacao_prep_amb ( cd_material_p bigint, qt_dose_p bigint, cd_unidade_medida_p text, ie_via_aplicacao_p text, cd_intervalo_p text) FROM PUBLIC;

