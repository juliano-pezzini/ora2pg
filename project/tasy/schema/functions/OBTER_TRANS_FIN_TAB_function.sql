-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_trans_fin_tab ( ie_restricao_p text, nr_seq_trans_financ_p bigint) RETURNS varchar AS $body$
DECLARE


cont_w		bigint;
cont_tab_w	bigint;


BEGIN

select	count(*)
into STRICT	cont_w
from	trans_fin_tab a
where	a.nr_seq_trans_financ	= nr_seq_trans_financ_p;

if (cont_w > 0) then

	select	count(*)
	into STRICT	cont_tab_w
	from	trans_fin_tab a
	where	a.nr_seq_trans_financ	= nr_seq_trans_financ_p
	and	a.ie_restricao		= ie_restricao_p;

	if (cont_tab_w = 0) then
		return	'N';
	end if;

end if;

return	'S';

end	;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_trans_fin_tab ( ie_restricao_p text, nr_seq_trans_financ_p bigint) FROM PUBLIC;

