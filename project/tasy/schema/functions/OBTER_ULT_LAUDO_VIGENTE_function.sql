-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_ult_laudo_vigente ( cd_pessoa_fisica_p text, dt_referencia_p timestamp) RETURNS bigint AS $body$
DECLARE


nr_atendimento_w	bigint;
nr_atend_retorno_w	bigint := null;
ie_tipo_convenio_w	smallint;


BEGIN

select	max(obter_ultimo_atendimento(cd_pessoa_fisica_p))
into STRICT	nr_atendimento_w
;

if (nr_atendimento_w > 0) then

	select	ie_tipo_convenio
	into STRICT	ie_tipo_convenio_w
	from	atendimento_paciente a
	where	nr_atendimento	= nr_atendimento_w;

	if (ie_tipo_convenio_w = '3') then

		select	Obter_atend_laudo_vigente(cd_pessoa_fisica_p, dt_referencia_p, 1)
		into STRICT	nr_atend_retorno_w
		;

	end if;

end if;

return	nr_atend_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_ult_laudo_vigente ( cd_pessoa_fisica_p text, dt_referencia_p timestamp) FROM PUBLIC;

