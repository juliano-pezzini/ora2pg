-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_setor_cadastrado ( cd_setor_atend_p bigint) RETURNS bigint AS $body$
DECLARE

ie_possui_w	varchar(1);
ds_retorno_w	bigint;

BEGIN
if (coalesce(cd_setor_atend_p,0) > 0) then

	select 	CASE WHEN count(*)=0 THEN 'N'  ELSE 'S' END
	into STRICT	ie_possui_w
	from	setor_atendimento
	where	cd_setor_atendimento = cd_setor_atend_p;

	if (ie_possui_w = 'S') then
		ds_retorno_w	:= cd_setor_atend_p +1;
	else
		ds_retorno_w	:= cd_setor_atend_p;
	end if;

end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_setor_cadastrado ( cd_setor_atend_p bigint) FROM PUBLIC;

