-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pat_obter_macro_tipo_msg ( cd_tipo_p bigint) RETURNS varchar AS $body$
DECLARE



ds_retorno_w		varchar(2000);
ds_enter_w		varchar(10) := chr(13) || chr(10);


BEGIN

ds_retorno_w := '';

if (cd_tipo_p in (1,2)) then
	/*ds_retorno_w := substr('@data = Data do documento de transferência/baixa.' || ds_enter_w ||
			'@doc = Número do documento de transferência/baixa.' || ds_enter_w ||
			'@observacao = É a observação do documento de transferência.' || ds_enter_w ||
			'@cargo = Descrição do cargo do usuário.' || ds_enter_w ||
			'@ramal = Ramal do usuário.' || ds_enter_w ||
			'@email = E-mail do usuário.' || ds_enter_w ||
			'@nm_usuario = Nome completo do usuário que envia o email.' || ds_enter_w ||
			'@usuario = Usuário que envia o email.' || ds_enter_w ||
			'@usuario_dest = Usuário destino.' || ds_enter_w ||
			'@nm_usuario_dest = Nome completo do destinatário',1,2000);*/
	ds_retorno_w := substr(wheb_mensagem_pck.get_texto(300015),1,2000);
end if;

return ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pat_obter_macro_tipo_msg ( cd_tipo_p bigint) FROM PUBLIC;

