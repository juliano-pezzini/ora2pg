-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_lado_proc_cir_agenda ( cd_procedimento_p bigint, ie_origem_proced_p bigint, nr_cirurgia_p bigint, nr_prescricao_p bigint, nr_sequencia_prescr_p bigint) RETURNS varchar AS $body$
DECLARE


ie_lado_w	varchar(01);


BEGIN

if (cd_procedimento_p IS NOT NULL AND cd_procedimento_p::text <> '') and (ie_origem_proced_p IS NOT NULL AND ie_origem_proced_p::text <> '') and (nr_cirurgia_p IS NOT NULL AND nr_cirurgia_p::text <> '') then

	SELECT MAX(ie_lado)
	into STRICT   ie_lado_w
	FROM   cirurgia
	WHERE  cd_procedimento_princ    = cd_procedimento_p
	AND    ie_origem_proced 	= ie_origem_proced_p
	AND    nr_cirurgia 		= nr_cirurgia_p;

end if;

if (coalesce(ie_lado_w::text, '') = '') then

	SELECT	MAX(ie_lado)
	INTO STRICT	ie_lado_w
	FROM	prescr_procedimento
	WHERE	nr_prescricao	= nr_prescricao_p
	AND	nr_sequencia	= nr_sequencia_prescr_p;

end if;

return	obter_valor_dominio(1372, ie_lado_w);

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_lado_proc_cir_agenda ( cd_procedimento_p bigint, ie_origem_proced_p bigint, nr_cirurgia_p bigint, nr_prescricao_p bigint, nr_sequencia_prescr_p bigint) FROM PUBLIC;

