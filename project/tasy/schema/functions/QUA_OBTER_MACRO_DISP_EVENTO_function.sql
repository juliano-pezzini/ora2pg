-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION qua_obter_macro_disp_evento (ie_status_p text) RETURNS varchar AS $body$
DECLARE

ds_retorno_w		varchar(2000);
ds_enter_w		varchar(10) := chr(13) || chr(10);

BEGIN

/* utilizada na function  qua_obter_macro_disp_evento  */

/*ds_retorno_w :=substr(	'@nr_atendimento = Número do atendimento.' 			|| ds_enter_w ||
			'@ds_evento = Descrição/nome do evento.'			|| ds_enter_w ||
			'@ds_descricao_evento = Descrição/detalhamento do evento.'	|| ds_enter_w ||
			'@ds_status = Descrição do status do evento.'			|| ds_enter_w ||
			'@nm_paciente = Nome do paciente.'				|| ds_enter_w ||
			'@ds_classificacao = Classificação do evento.'			|| ds_enter_w ||
			'@ds_evento_paciente = Descrição do evento.'			|| ds_enter_w ||
			'@ds_gravidade = Gravidade do evento.',1,2000);*/
ds_retorno_w := substr(wheb_mensagem_pck.get_texto(300973),1,2000);

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION qua_obter_macro_disp_evento (ie_status_p text) FROM PUBLIC;

