-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_pessoa_titular_convenio (cd_convenio_p bigint, cd_categoria_p text, cd_plano_p text, cd_matricula_p text) RETURNS varchar AS $body$
DECLARE


cd_pessoa_fisica_w	varchar(10);


BEGIN

select	max(cd_pessoa_fisica)
into STRICT	cd_pessoa_fisica_w
from	pessoa_titular_convenio
where	cd_convenio = cd_convenio_p
and 	coalesce(cd_categoria, cd_categoria_p) = cd_categoria_p
and 	coalesce(cd_plano_convenio, cd_plano_p) = cd_plano_p
and 	cd_usuario_convenio = cd_matricula_p;

RETURN	cd_pessoa_fisica_w;

END	;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_pessoa_titular_convenio (cd_convenio_p bigint, cd_categoria_p text, cd_plano_p text, cd_matricula_p text) FROM PUBLIC;

