-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_desc_modelo_atual (cd_pessoa_fisica_p text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w			varchar(255);


BEGIN

if (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') then

SELECT		MAX(SUBSTR(rp_obter_desc_modelo_agend(x.nr_seq_modelo_agendamento),1,80))
INTO STRICT		ds_retorno_w
FROM		RP_PAC_MODELO_AGENDAMENTO x,
		RP_PACIENTE_REABILITACAO a
WHERE		a.nr_sequencia = x.nr_seq_pac_reab
AND		a.cd_pessoa_fisica = cd_pessoa_fisica_p
AND		x.dt_inicio_modelo = (SELECT 	MAX(x.dt_inicio_modelo)
				  FROM	                RP_PAC_MODELO_AGENDAMENTO x,
						RP_PACIENTE_REABILITACAO a
				  WHERE	                a.nr_sequencia = x.nr_seq_pac_reab
				  AND	                a.cd_pessoa_fisica = cd_pessoa_fisica_p);

end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_desc_modelo_atual (cd_pessoa_fisica_p text) FROM PUBLIC;

