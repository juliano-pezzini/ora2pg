-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_leito_ant_pac ( nr_atendimento_p bigint, nr_seq_interno_p bigint) RETURNS varchar AS $body$
DECLARE

nr_seq_interno_w	bigint;
cd_unidade_basica_w	varchar(10);
cd_unidade_compl_w	varchar(10);
ds_retorno_w		varchar(255);


BEGIN
if (nr_atendimento_p IS NOT NULL AND nr_atendimento_p::text <> '')	then

	select	max(nr_seq_interno)
	into STRICT	nr_seq_interno_w
	from	atend_paciente_unidade
	where	nr_atendimento		= nr_atendimento_p
	and	dt_saida_unidade	= (
					SELECT	max(dt_entrada_unidade)
					from	atend_paciente_unidade
					where	nr_atendimento	= nr_atendimento_p
					and	nr_seq_interno	= nr_seq_interno_p);

end if;

return	nr_seq_interno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_leito_ant_pac ( nr_atendimento_p bigint, nr_seq_interno_p bigint) FROM PUBLIC;

