-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION bsc_obter_obj_indicador ( nr_seq_indicador_p bigint) RETURNS varchar AS $body$
DECLARE


nr_seq_objetivo_w			bigint;
ds_objetivo_w			varchar(4000);

BEGIN

select	max(nr_seq_objetivo)
into STRICT	nr_seq_objetivo_w
from	bsc_ind_obj
where	nr_seq_indicador = nr_seq_indicador_p;

select	max(ds_objetivo)
into STRICT	ds_objetivo_w
from	ple_objetivo
where	nr_sequencia	= nr_seq_objetivo_w;

return	ds_objetivo_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION bsc_obter_obj_indicador ( nr_seq_indicador_p bigint) FROM PUBLIC;

