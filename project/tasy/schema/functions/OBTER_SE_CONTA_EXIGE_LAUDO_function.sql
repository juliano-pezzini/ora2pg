-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_conta_exige_laudo (nr_interno_conta_p bigint) RETURNS varchar AS $body$
DECLARE


qt_registro_w	bigint;


BEGIN

select	coalesce(count(*),0)
into STRICT	qt_registro_w
from	procedimento b,
	procedimento_paciente a
where	nr_interno_conta = nr_interno_conta_p
and	a.cd_procedimento = b.cd_procedimento
and	a.ie_origem_proced = b.ie_origem_proced
and	coalesce(ie_exige_laudo,'N') = 'S'
and not exists (SELECT 1 from laudo_paciente x
		where a.nr_sequencia = x.nr_seq_proc)
and not exists (select 1 from laudo_paciente x
		where a.nr_prescricao = x.nr_prescricao)
and not exists (select 1 from laudo_paciente x
		where a.nr_laudo = x.nr_sequencia);

if (qt_registro_w	= 0) then
	return 'N';
else
	return 'S';
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_conta_exige_laudo (nr_interno_conta_p bigint) FROM PUBLIC;

