-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_ie_unidade_gas (nr_seq_gasoterapia_p bigint) RETURNS varchar AS $body$
DECLARE


ie_unidade_medida_w	varchar(15);


BEGIN

select	max(ie_unidade_medida)
into STRICT	ie_unidade_medida_w
from	prescr_gasoterapia_evento
where	nr_seq_gasoterapia 	= nr_seq_gasoterapia_p
and	IE_EVENTO 		= 'V'
and	coalesce(ie_evento_valido,'S') = 'S'
and	nr_sequencia	= (	SELECT	max(nr_sequencia)
				from	prescr_gasoterapia_evento
				where	nr_seq_gasoterapia 	= nr_seq_gasoterapia_p
				and	IE_EVENTO 		= 'V'
				and	coalesce(ie_evento_valido,'S') = 'S');

return	ie_unidade_medida_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_ie_unidade_gas (nr_seq_gasoterapia_p bigint) FROM PUBLIC;

