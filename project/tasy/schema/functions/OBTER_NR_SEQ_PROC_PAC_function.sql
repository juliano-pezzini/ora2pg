-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_nr_seq_proc_pac (nr_seq_proc_hor_p bigint) RETURNS bigint AS $body$
DECLARE


nr_sequencia_w	procedimento_paciente.nr_sequencia%type;


BEGIN

if (nr_seq_proc_hor_p IS NOT NULL AND nr_seq_proc_hor_p::text <> '') then

	select	max(a.nr_sequencia)
	into STRICT	nr_sequencia_w
	from	procedimento_paciente a,
			procedimento_paciente_dado b
	where	b.nr_seq_proc = a.nr_sequencia
	and		b.nr_seq_horario_proc = nr_seq_proc_hor_p;

end if;

return coalesce(nr_sequencia_w,0);

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_nr_seq_proc_pac (nr_seq_proc_hor_p bigint) FROM PUBLIC;

