-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_quantidade_prescr_proced (cd_intervalo_p text, ie_lado_p text, cd_procedimento_p bigint, ie_origem_proced_p bigint, nr_seq_proc_interno_p bigint, nr_atendimento_p bigint) RETURNS bigint AS $body$
DECLARE


qt_procedimento_w		double precision;
cd_grupo_proc_w			bigint;
cd_especialidade_w		bigint;
cd_area_procedimento_w		bigint;
cd_convenio_w			bigint;
ie_achou_w			varchar(1);
nr_atendimento_w		atendimento_paciente.nr_atendimento%type;
C01 CURSOR FOR
	SELECT	qt_procedimento
	from	prescr_proc_quant
	where	coalesce(cd_intervalo,coalesce(cd_intervalo_p,'XPTO'))	= coalesce(cd_intervalo_p,'XPTO')
	and	coalesce(coalesce(ie_lado, ie_lado_p),'XPTO') = coalesce(ie_lado_p,'XPTO')
	and	coalesce(cd_procedimento, cd_procedimento_p) = cd_procedimento_p
	and	coalesce(cd_area_procedimento, cd_area_procedimento_w)	= cd_area_procedimento_w
	and	coalesce(cd_especialidade, cd_especialidade_w)		= cd_especialidade_w
	and	coalesce(cd_grupo_proc, cd_grupo_proc_w)			= cd_grupo_proc_w
	and	coalesce(cd_convenio, coalesce(cd_convenio_w,0))			= coalesce(cd_convenio_w,0)
	and	coalesce(nr_seq_proc_interno, coalesce(nr_seq_proc_interno_p,0))	= coalesce(nr_seq_proc_interno_p,0)
	and	((coalesce(cd_procedimento::text, '') = '') or (coalesce(ie_origem_proced, ie_origem_proced_p) = ie_origem_proced_p))
	order by cd_convenio desc;


BEGIN
nr_atendimento_w := coalesce(nr_atendimento_p,0);
select	max(cd_grupo_proc),
	max(cd_especialidade),
	max(cd_area_procedimento)
into STRICT	cd_grupo_proc_w,
	cd_especialidade_w,
	cd_area_procedimento_w
from	estrutura_procedimento_v
where	cd_procedimento		= cd_procedimento_p
and	ie_origem_proced	= ie_origem_proced_p;

select	max(obter_convenio_atendimento(nr_atendimento_w))
into STRICT	cd_convenio_w
;

open C01;
loop
fetch C01 into
	qt_procedimento_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin

	ie_achou_w := 'S';

	end;
end loop;
close C01;

return	qt_procedimento_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_quantidade_prescr_proced (cd_intervalo_p text, ie_lado_p text, cd_procedimento_p bigint, ie_origem_proced_p bigint, nr_seq_proc_interno_p bigint, nr_atendimento_p bigint) FROM PUBLIC;

