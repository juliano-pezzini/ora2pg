-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_caixa_adiantamento_pago ( NR_ADIANTAMENTO_P bigint) RETURNS varchar AS $body$
DECLARE


ds_caixa_w		varchar(255);
nr_seq_caixa_w 		bigint;


BEGIN

select	max(a.nr_seq_caixa)
into STRICT	nr_seq_caixa_w
from	movto_trans_financ a
where	a.nr_adiant_pago	= nr_adiantamento_p;

select	max(a.ds_caixa)
into STRICT	ds_caixa_w
from	caixa a
where	a.nr_sequencia	= nr_seq_caixa_w;

return	ds_caixa_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_caixa_adiantamento_pago ( NR_ADIANTAMENTO_P bigint) FROM PUBLIC;

