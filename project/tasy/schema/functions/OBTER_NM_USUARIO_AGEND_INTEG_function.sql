-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_nm_usuario_agend_integ (ie_tipo_age_p text, ie_tipo_usu_p text, nr_seq_agend_integ_p bigint) RETURNS varchar AS $body$
DECLARE

ds_retorno_w varchar(255);


BEGIN
if (Ie_tipo_age_p = 'C') then
	if (ie_tipo_usu_p = 'O') then
		Select 	max(nm_usuario_origem)
		into STRICT 	ds_retorno_w
		from 	agenda_consulta
		where 	nr_sequencia = nr_seq_agend_integ_p;
	else
		Select 	max(nm_usuario_cancelamento)
		into STRICT 	ds_retorno_w
		from 	agenda_consulta
		where 	nr_sequencia = nr_seq_agend_integ_p;
	end if;
elsif (Ie_tipo_age_p = 'E') then
	if (ie_tipo_usu_p = 'O') then
		Select 	max(nm_usuario_orig)
		into STRICT 	ds_retorno_w
		from 	agenda_paciente
		where 	nr_sequencia = nr_seq_agend_integ_p;
	else
		Select 	max(nm_usuario_cancel)
		into STRICT 	ds_retorno_w
		from 	agenda_paciente
		where 	nr_sequencia = nr_seq_agend_integ_p;
	end if;
end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_nm_usuario_agend_integ (ie_tipo_age_p text, ie_tipo_usu_p text, nr_seq_agend_integ_p bigint) FROM PUBLIC;

