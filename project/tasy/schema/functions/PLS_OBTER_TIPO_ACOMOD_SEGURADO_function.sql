-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_tipo_acomod_segurado ( nr_seq_segurado_p bigint) RETURNS bigint AS $body$
DECLARE


nr_seq_acomodacao_w		bigint;
nr_seq_plano_w			bigint;

BEGIN
select	max(b.nr_sequencia)
into STRICT	nr_seq_plano_w
from	pls_segurado	a,
	pls_plano	b
where	b.nr_sequencia	= a.nr_seq_plano
and	a.nr_sequencia	= nr_seq_segurado_p;

select	pls_obter_dados_produto(nr_seq_plano_w,'AP')
into STRICT	nr_seq_acomodacao_w
;

return	nr_seq_acomodacao_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_tipo_acomod_segurado ( nr_seq_segurado_p bigint) FROM PUBLIC;

