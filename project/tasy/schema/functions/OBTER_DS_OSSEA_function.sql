-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_ds_ossea (qt_ossea_p bigint, nr_atendimento_p bigint) RETURNS varchar AS $body$
DECLARE

 
ds_result_w	varchar(25);
cd_pf_w bigint;
ds_sexo_w varchar(1);
sql_w varchar(250);

BEGIN
	if (qt_ossea_p > 0)then 
		select cd_pessoa_fisica 
		into STRICT cd_pf_w 
		from atendimento_paciente 
		where nr_atendimento = nr_atendimento_p;
 
		ds_sexo_w := obter_sexo_pf(cd_pf_w, 'C');
 --INICIO MD 1
  begin
    sql_w := 'CALL OBTER_DS_OSSEA_MD(:1, :2) INTO :ds_result_w';
    EXECUTE sql_w USING IN qt_ossea_p,
                                  IN ds_sexo_w,
                                  OUT ds_result_w;
  exception
    when others then
      ds_result_w := null;
    end;

--FIM MD 1
	end if;
return ds_result_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_ds_ossea (qt_ossea_p bigint, nr_atendimento_p bigint) FROM PUBLIC;

