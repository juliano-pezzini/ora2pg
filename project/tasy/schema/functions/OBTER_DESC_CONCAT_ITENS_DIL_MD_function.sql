-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_desc_concat_itens_dil_md ( ie_user_local_p text, ie_separar_p text, ds_conv_ml_p text, ds_aux_diluir_p text, ds_dose_diluicao_p text, ds_material_p text ) RETURNS varchar AS $body$
DECLARE

  ds_item_concat_w varchar(2000);

BEGIN

	if (ie_user_local_p = 'en_AU') then
		if (ie_separar_p = 'S') then
			ds_item_concat_w := wheb_mensagem_pck.get_texto(1090081,
									'DS_VOLUME=' || ds_conv_ml_p ||
									';DS_TIPO_DIL=' || ds_aux_diluir_p ||
									';DS_ADDITIONAL_ITEM=' || '' ||
									';DS_DOSE_DIL=' || ds_dose_diluicao_p ||
									';DS_MATERIAL=' || ds_material_p);
		else
			ds_item_concat_w := wheb_mensagem_pck.get_texto(1090082,
									'DS_VOLUME=' ||ds_conv_ml_p ||
									';DS_TIPO_DIL=' || ds_aux_diluir_p ||
									';DS_ADDITIONAL_ITEM=' || '' ||
									';DS_DOSE_DIL=' || ds_dose_diluicao_p || 
									';DS_MATERIAL='||ds_material_p);
		end if;
	
	end if;
	
	return ds_item_concat_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_desc_concat_itens_dil_md ( ie_user_local_p text, ie_separar_p text, ds_conv_ml_p text, ds_aux_diluir_p text, ds_dose_diluicao_p text, ds_material_p text ) FROM PUBLIC;

