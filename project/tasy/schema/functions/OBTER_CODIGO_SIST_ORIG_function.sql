-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_codigo_sist_orig ( cd_pessoa_fisica_p text, cd_medico_p text) RETURNS varchar AS $body$
DECLARE


cd_resultado_w		varchar(10) := null;


BEGIN

if (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') and (cd_medico_p IS NOT NULL AND cd_medico_p::text <> '') then
	begin
	select	max(cd_pessoa_sist_orig)
	into STRICT	cd_resultado_w
	from	med_cliente
	where	cd_pessoa_fisica	= cd_pessoa_fisica_p
	and	cd_medico		= cd_medico_p
	and	(cd_pessoa_sist_orig IS NOT NULL AND cd_pessoa_sist_orig::text <> '');
	end;
end if;

return	cd_resultado_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_codigo_sist_orig ( cd_pessoa_fisica_p text, cd_medico_p text) FROM PUBLIC;

