-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_qt_pend_entrega_item_oc ( nr_ordem_compra_p bigint, nr_item_oci_p bigint, dt_prevista_entrega_p timestamp) RETURNS bigint AS $body$
DECLARE


qt_pendente_w		double precision;


BEGIN

select	coalesce(sum(b.qt_prevista_entrega),0) - coalesce(sum(b.qt_real_entrega),0)
into STRICT	qt_pendente_w
from	ordem_compra_item_entrega b,
	ordem_compra_item a
where	a.nr_ordem_compra	= nr_ordem_compra_p
and	a.nr_item_oci		= nr_item_oci_p
and	a.nr_ordem_compra	= b.nr_ordem_compra
and	a.nr_item_oci		= b.nr_item_oci
and	trunc(dt_prevista_entrega,'dd') = trunc(dt_prevista_entrega_p,'dd')
and	coalesce(b.dt_cancelamento::text, '') = '';

return	qt_pendente_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_qt_pend_entrega_item_oc ( nr_ordem_compra_p bigint, nr_item_oci_p bigint, dt_prevista_entrega_p timestamp) FROM PUBLIC;

