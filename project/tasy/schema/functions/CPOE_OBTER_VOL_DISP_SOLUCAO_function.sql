-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION cpoe_obter_vol_disp_solucao (nr_seq_mat_cpoe_p bigint, ie_dispositivo_infusao_p text) RETURNS bigint AS $body$
DECLARE

qt_equipo_w	cpoe_trev.qt_equipo%type;

BEGIN

if (nr_seq_mat_cpoe_p IS NOT NULL AND nr_seq_mat_cpoe_p::text <> '') then
	select	max(qt_equipo)
	into STRICT	qt_equipo_w
	from	cpoe_trev
	where	coalesce(nr_seq_mat_cpoe, nr_seq_mat_cpoe_edit) = nr_seq_mat_cpoe_p;
end if;


return coalesce(qt_equipo_w, (obter_vol_dispositivo_solucao(ie_dispositivo_infusao_p))::numeric );
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION cpoe_obter_vol_disp_solucao (nr_seq_mat_cpoe_p bigint, ie_dispositivo_infusao_p text) FROM PUBLIC;

