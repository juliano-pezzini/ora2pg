-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION sus_obter_reg_proc_bpa ( cd_procedimento_p procedimento.cd_procedimento%type, ie_origem_proced_p procedimento.ie_origem_proced%type) RETURNS bigint AS $body$
DECLARE


cd_retorno_w		sus_proc_registro_bpa.cd_registro%type := null;
cd_estabelecimento_w	estabelecimento.cd_estabelecimento%type := coalesce(wheb_usuario_pck.get_cd_estabelecimento,0);


BEGIN

begin
select	cd_registro
into STRICT	cd_retorno_w
from	sus_proc_registro_bpa
where	cd_procedimento = cd_procedimento_p
and	ie_origem_proced = ie_origem_proced_p
and	coalesce(cd_estabelecimento,cd_estabelecimento_w) = cd_estabelecimento_w  LIMIT 1;
exception
when others then
	cd_retorno_w := null;
end;

return	cd_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION sus_obter_reg_proc_bpa ( cd_procedimento_p procedimento.cd_procedimento%type, ie_origem_proced_p procedimento.ie_origem_proced%type) FROM PUBLIC;

