-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION psa_uuid_generator () RETURNS varchar AS $body$
DECLARE

  v_uuid bytea;
  v_result varchar(36);

BEGIN
  v_uuid   := dbms_crypto.randombytes(16);
  v_result := lower(utl_raw.overlay(utl_raw.bit_or(utl_raw.bit_and(utl_raw.substr(v_uuid, 7, 1), '0F'), '40'), v_uuid, 7));

  return substr(v_result, 1, 8) || '-' ||
         substr(v_result, 9, 4) || '-' ||
         substr(v_result, 13, 4) || '-' ||
         substr(v_result, 17, 4) || '-' ||
         substr(v_result, 21);

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION psa_uuid_generator () FROM PUBLIC;

