-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_dias_pos_operatorio ( nr_atendimento_p bigint) RETURNS bigint AS $body$
DECLARE


dt_inicio_real_w	timestamp;
dt_alta_w		timestamp;
qt_dias_po_w		bigint;


BEGIN

begin
select	max(dt_inicio_real)
into STRICT	dt_inicio_real_w
from	cirurgia
where	nr_atendimento		= nr_atendimento_p
and	ie_status_cirurgia	= 2;
exception
	when others then
		dt_inicio_real_w	:= null;
end;




if (dt_inicio_real_w IS NOT NULL AND dt_inicio_real_w::text <> '') then
	begin
	
	
	begin
    select	dt_alta
	into STRICT	dt_alta_w	
	from	atendimento_paciente
	where	nr_atendimento		= nr_atendimento_p;
    exception
		when others then
			dt_alta_w	:= null;
	end;
		
	qt_dias_po_w	:= trunc(coalesce(ESTABLISHMENT_TIMEZONE_UTILS.startOfDay(dt_alta_w),ESTABLISHMENT_TIMEZONE_UTILS.startOfDay(clock_timestamp())) - ESTABLISHMENT_TIMEZONE_UTILS.startOfDay(dt_inicio_real_w));
	end;
end if;

RETURN qt_dias_po_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_dias_pos_operatorio ( nr_atendimento_p bigint) FROM PUBLIC;

