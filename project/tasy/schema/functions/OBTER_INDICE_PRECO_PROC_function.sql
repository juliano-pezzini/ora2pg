-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_indice_preco_proc (nr_sequencia_p bigint) RETURNS bigint AS $body$
DECLARE


cd_categoria_w			varchar(10);
cd_convenio_w			integer;
cd_edicao_amb_w			integer;
tx_ajuste_w			double precision := 0;
nr_seq_regra_w			bigint := 0;
cd_estabelecimento_w		smallint;


BEGIN

select	nr_seq_ajuste_proc,
	cd_convenio,
	cd_categoria,
	cd_edicao_amb,
	obter_estab_conta_paciente(nr_interno_conta)
into STRICT	nr_seq_regra_w,
	cd_convenio_w,
	cd_categoria_w,
	cd_edicao_amb_w,
	cd_estabelecimento_w
from	procedimento_paciente
where	nr_sequencia = nr_sequencia_p;

if (nr_seq_regra_w <> 0 AND nr_seq_regra_w IS NOT NULL AND nr_seq_regra_w::text <> '') then
	select	coalesce(max(tx_ajuste),0)
	into STRICT	tx_ajuste_w
	from	regra_ajuste_proc
	where	nr_sequencia = nr_seq_regra_w;
end if;

if ((coalesce(tx_ajuste_w::text, '') = '') or (tx_ajuste_w = 0)) then
	select	max(tx_ajuste_geral)
	into STRICT	tx_ajuste_w
	from	convenio_amb
	where	cd_convenio = cd_convenio_w
	and	cd_categoria = cd_categoria_w
	and	cd_edicao_amb = cd_edicao_amb_w
	and	cd_estabelecimento = cd_estabelecimento_w;
end if;


return	tx_ajuste_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_indice_preco_proc (nr_sequencia_p bigint) FROM PUBLIC;

