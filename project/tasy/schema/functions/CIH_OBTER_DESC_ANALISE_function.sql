-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION cih_obter_desc_analise (nr_seq_precaucao_p bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w			varchar(255);
ds_analise_w			varchar(200);


BEGIN

ds_retorno_w := '';

if (nr_seq_precaucao_p IS NOT NULL AND nr_seq_precaucao_p::text <> '') then

	select 	ie_analise
	into STRICT	ds_analise_w
	from 	atendimento_precaucao
	where	nr_sequencia = nr_seq_precaucao_p;

	if (ds_analise_w = 'S') then
		ds_retorno_w := Wheb_mensagem_pck.get_texto(306136); -- Confirmado
	elsif (ds_analise_w = 'N') then
		ds_retorno_w := Wheb_mensagem_pck.get_texto(306137); -- Não confirmado
	end if;

end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION cih_obter_desc_analise (nr_seq_precaucao_p bigint) FROM PUBLIC;

