-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_desc_escala_atice (QT_SCORE_P bigint) RETURNS varchar AS $body$
DECLARE


DS_SCORE_W	varchar(255) := '';


BEGIN

if (QT_SCORE_P IS NOT NULL AND QT_SCORE_P::text <> '') then
	begin
	if (QT_SCORE_P = 0) then
		begin
			DS_SCORE_W := obter_desc_expressao(971723);
		end;
	elsif (QT_SCORE_P = 20) then
		begin
			DS_SCORE_W := obter_desc_expressao(971719);
		end;
	end if;
	end;
end if;

return DS_SCORE_W;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_desc_escala_atice (QT_SCORE_P bigint) FROM PUBLIC;

