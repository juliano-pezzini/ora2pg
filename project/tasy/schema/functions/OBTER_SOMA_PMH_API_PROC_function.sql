-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_soma_pmh_api_proc ( nr_processo_p bigint, nr_seq_pmh_p bigint) RETURNS bigint AS $body$
DECLARE


qt_pmh_api_w	double precision := 0;


BEGIN
if (nr_processo_p IS NOT NULL AND nr_processo_p::text <> '') and (nr_seq_pmh_p IS NOT NULL AND nr_seq_pmh_p::text <> '') then
	begin

	select	coalesce(sum(coalesce(qt_material,0)),0)
	into STRICT	qt_pmh_api_w
	from	adep_processo_item
	where	nr_seq_processo	= nr_processo_p
	and	nr_seq_horario	= nr_seq_pmh_p;

	end;
end if;

return qt_pmh_api_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_soma_pmh_api_proc ( nr_processo_p bigint, nr_seq_pmh_p bigint) FROM PUBLIC;

