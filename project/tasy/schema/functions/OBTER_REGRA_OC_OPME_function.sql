-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_regra_oc_opme (cd_estabelecimento_p bigint, cd_cnpj_p text, cd_convenio_p bigint, cd_material_p bigint) RETURNS bigint AS $body$
DECLARE


pr_desc_financeiro_w		double precision;
cd_convenio_w			integer;
cd_grupo_material_w		smallint;
cd_subgrupo_material_w		smallint;
cd_classe_material_w		integer;

c01 CURSOR FOR
	SELECT	pr_desc_financeiro
	from	regra_oc_opme_pj
	where	cd_estabelecimento = cd_estabelecimento_p
	and	cd_cnpj = cd_cnpj_p
	and	coalesce(cd_convenio, cd_convenio_w) = cd_convenio_w
	and	coalesce(cd_grupo_material, cd_grupo_material_w) = cd_grupo_material_w
	and	coalesce(cd_subgrupo_material, cd_subgrupo_material_w) = cd_subgrupo_material_w
	and	coalesce(cd_classe_material, cd_classe_material_w) = cd_classe_material_w
	and (coalesce(cd_material, cd_material_p) = cd_material_p or cd_material_p = 0)
	order by
		coalesce(cd_material, 0),
		coalesce(cd_classe_material, 0),
		coalesce(cd_subgrupo_material, 0),
		coalesce(cd_grupo_material, 0),
		coalesce(cd_convenio, 0);


BEGIN

cd_convenio_w := cd_convenio_p;

select	cd_grupo_material,
	cd_subgrupo_material,
	cd_classe_material
into STRICT	cd_grupo_material_w,
	cd_subgrupo_material_w,
	cd_classe_material_w
from	estrutura_material_v
where	cd_material = cd_material_p;

open c01;
	loop
	fetch c01 into
	pr_desc_financeiro_w;
	EXIT WHEN NOT FOUND; /* apply on c01 */
	begin
	pr_desc_financeiro_w := pr_desc_financeiro_w;
	end;
	end loop;
close c01;

return pr_desc_financeiro_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_regra_oc_opme (cd_estabelecimento_p bigint, cd_cnpj_p text, cd_convenio_p bigint, cd_material_p bigint) FROM PUBLIC;

