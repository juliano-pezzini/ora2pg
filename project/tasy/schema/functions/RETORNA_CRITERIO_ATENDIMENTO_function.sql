-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION retorna_criterio_atendimento (NR_ATENDIMENTO_P ATENDIMENTO_PACIENTE.NR_ATENDIMENTO%TYPE, NR_SEQ_NIVEL_P bigint, NR_SEQUENCIA_P bigint default null) RETURNS varchar AS $body$
DECLARE

RETORNO_W varchar(30);

BEGIN
    BEGIN
      SELECT coalesce(
      (SELECT obter_desc_expressao(928855,null)
       FROM NURSING_CARE_ENCOUNTER X
       WHERE X.NR_ATENDIMENTO = NR_ATENDIMENTO_P
       AND  X.NR_SEQ_NIVEL = NR_SEQ_NIVEL_P 
        AND X.NR_SEQUENCIA = NR_SEQUENCIA_P
		AND (X.DT_LIBERACAO IS NOT NULL AND X.DT_LIBERACAO::text <> '')),obter_desc_expressao(928857,null)) TOTAL_NIVEL_1
       INTO STRICT RETORNO_W
;
    EXCEPTION
      WHEN no_data_found THEN
        RETORNO_W := ' ';
    END;
RETURN RETORNO_W;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION retorna_criterio_atendimento (NR_ATENDIMENTO_P ATENDIMENTO_PACIENTE.NR_ATENDIMENTO%TYPE, NR_SEQ_NIVEL_P bigint, NR_SEQUENCIA_P bigint default null) FROM PUBLIC;

