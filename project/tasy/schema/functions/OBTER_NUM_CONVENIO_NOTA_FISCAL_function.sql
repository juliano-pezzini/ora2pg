-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_num_convenio_nota_fiscal (nr_seq_nota_fiscal_p bigint) RETURNS varchar AS $body$
DECLARE


ds_convenio_ano_w 	 varchar(20);


BEGIN

select 	max(a.ds_convenio_ano)
into STRICT 	ds_convenio_ano_w
from 	projeto_recurso a,
	nota_fiscal_item b
where 	a.nr_sequencia = b.nr_seq_proj_rec
and 	b.nr_sequencia = nr_seq_nota_fiscal_p;

return	ds_convenio_ano_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_num_convenio_nota_fiscal (nr_seq_nota_fiscal_p bigint) FROM PUBLIC;

