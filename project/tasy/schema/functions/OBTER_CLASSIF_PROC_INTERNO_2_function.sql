-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_classif_proc_interno_2 (nr_seq_proc_interno_p bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(255);

BEGIN

begin
select	substr(a.ds_classificacao,1,255)
into STRICT	ds_retorno_w
from	proc_interno_classif a,
	proc_interno b
where	a.nr_sequencia 	= b.nr_seq_classif
and	b.nr_sequencia	= nr_seq_proc_interno_p  LIMIT 1;
exception
when others then
	ds_retorno_w	:= wheb_mensagem_pck.get_texto(322741);
end;

return ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_classif_proc_interno_2 (nr_seq_proc_interno_p bigint) FROM PUBLIC;

