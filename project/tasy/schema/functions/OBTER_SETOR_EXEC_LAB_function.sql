-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_setor_exec_lab ( nr_seq_exame_p bigint, cd_setor_origem_p bigint, cd_estab_p bigint, nr_atendimento_p bigint default null) RETURNS bigint AS $body$
DECLARE


cd_setor_atendimento_w	integer;
cd_convenio_w			convenio.cd_convenio%type;

C01 CURSOR FOR
SELECT	cd_setor_atendimento
from	exame_lab_setor
where	nr_seq_exame	= nr_seq_exame_p
and		((cd_setor_origem	= cd_setor_origem_p) or (coalesce(cd_setor_origem_p::text, '') = '') or (coalesce(cd_setor_origem::text, '') = ''))
and		coalesce(cd_estabelecimento,cd_estab_p) = cd_estab_p
and		coalesce(cd_convenio, cd_convenio_w, 0) = coalesce(cd_convenio_w, 0)
order by coalesce(cd_estabelecimento,0),
	 coalesce(cd_setor_origem,0);


BEGIN



if (nr_atendimento_p IS NOT NULL AND nr_atendimento_p::text <> '') then

	cd_convenio_w	:= obter_convenio_atendimento(nr_atendimento_p);

end if;

open C01;
loop
	fetch C01 into
		cd_setor_atendimento_w;
	EXIT WHEN NOT FOUND; /* apply on C01 */
	cd_setor_atendimento_w	:= cd_setor_atendimento_w;
end loop;
close C01;


return	cd_setor_atendimento_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_setor_exec_lab ( nr_seq_exame_p bigint, cd_setor_origem_p bigint, cd_estab_p bigint, nr_atendimento_p bigint default null) FROM PUBLIC;

