-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_unidade_disp_acomp ( cd_setor_atendimento_p atend_paciente_unidade.cd_setor_atendimento%type, cd_unidade_basica_p atend_paciente_unidade.cd_unidade_basica%type, cd_unidade_compl_p atend_paciente_unidade.cd_unidade_compl%type, nr_atendimento_p atend_paciente_unidade.nr_atendimento%type) RETURNS varchar AS $body$
DECLARE


cd_setor_atendimento_w	atend_paciente_unidade.cd_setor_atendimento%type;
cd_unidade_basica_w		atend_paciente_unidade.cd_unidade_basica%type;
cd_unidade_compl_w		atend_paciente_unidade.cd_unidade_compl%type;
ds_resultado_w		varchar(50);


BEGIN
ds_resultado_w := 'S';
if (nr_atendimento_p IS NOT NULL AND nr_atendimento_p::text <> '') then
    select cd_setor_atendimento, cd_unidade_basica, cd_unidade_compl
    into STRICT cd_setor_atendimento_w, cd_unidade_basica_w, cd_unidade_compl_w
    from atendimento_paciente_v
    where nr_atendimento = nr_atendimento_p;
end if;

if (cd_unidade_basica_w = cd_unidade_basica_p and cd_setor_atendimento_w = cd_setor_atendimento_p
and cd_unidade_compl_p = cd_unidade_compl_w) then
    ds_resultado_w := 'N';
end if;
return ds_resultado_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_unidade_disp_acomp ( cd_setor_atendimento_p atend_paciente_unidade.cd_setor_atendimento%type, cd_unidade_basica_p atend_paciente_unidade.cd_unidade_basica%type, cd_unidade_compl_p atend_paciente_unidade.cd_unidade_compl%type, nr_atendimento_p atend_paciente_unidade.nr_atendimento%type) FROM PUBLIC;

