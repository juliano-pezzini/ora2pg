-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_cod_proced_conta (nr_seq_conta_proc_p bigint, cd_procedimento_p text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w		varchar(30)	:= null;


BEGIN
if (nr_seq_conta_proc_p IS NOT NULL AND nr_seq_conta_proc_p::text <> '') then
	select	max(cd_procedimento)
	into STRICT	ds_retorno_w
	from	pls_conta_proc
	where	nr_sequencia	= nr_seq_conta_proc_p;
elsif (cd_procedimento_p IS NOT NULL AND cd_procedimento_p::text <> '') then
	select	max(nr_sequencia)
	into STRICT	ds_retorno_w
	from	pls_conta_proc
	where	cd_procedimento	= cd_procedimento_p;
end if;

return ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_cod_proced_conta (nr_seq_conta_proc_p bigint, cd_procedimento_p text) FROM PUBLIC;

