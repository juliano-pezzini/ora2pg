-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_score_escala_ciwaa_md (IE_MAL_ESTAR_P bigint, IE_TREMOR_BRACO_P bigint, IE_SUDORESE_P bigint, IE_PERTURBACAO_COCEIRA_P bigint, IE_PERTURBACAO_AUDITIVA_P bigint, IE_PERTURBACAO_VISUAL_P bigint, IE_NERVOSISMO_P bigint, IE_DOR_CABECA_P bigint, IE_AGITACAO_P bigint, IE_ALT_ORIENTACAO_P bigint ) RETURNS bigint AS $body$
DECLARE


   qt_resultado_w	bigint := 0;

BEGIN
    if (IE_MAL_ESTAR_P IS NOT NULL AND IE_MAL_ESTAR_P::text <> '') then
      qt_resultado_w	:= qt_resultado_w + coalesce(IE_MAL_ESTAR_P,0);
    end if;

    if (IE_TREMOR_BRACO_P IS NOT NULL AND IE_TREMOR_BRACO_P::text <> '') then
      qt_resultado_w	:= qt_resultado_w + coalesce(IE_TREMOR_BRACO_P,0);
    end if;

    if (IE_SUDORESE_P IS NOT NULL AND IE_SUDORESE_P::text <> '') then
      qt_resultado_w	:= qt_resultado_w + coalesce(IE_SUDORESE_P,0);
    end if;

    if (IE_PERTURBACAO_COCEIRA_P IS NOT NULL AND IE_PERTURBACAO_COCEIRA_P::text <> '') then
      qt_resultado_w	:= qt_resultado_w + coalesce(IE_PERTURBACAO_COCEIRA_P,0);
    end if;

    if (IE_PERTURBACAO_AUDITIVA_P IS NOT NULL AND IE_PERTURBACAO_AUDITIVA_P::text <> '') then
      qt_resultado_w	:= qt_resultado_w + coalesce(IE_PERTURBACAO_AUDITIVA_P,0);
    end if;

    if (IE_PERTURBACAO_VISUAL_P IS NOT NULL AND IE_PERTURBACAO_VISUAL_P::text <> '') then
      qt_resultado_w	:= qt_resultado_w + coalesce(IE_PERTURBACAO_VISUAL_P,0);
    end if;

    if (IE_NERVOSISMO_P IS NOT NULL AND IE_NERVOSISMO_P::text <> '') then
      qt_resultado_w	:= qt_resultado_w + coalesce(IE_NERVOSISMO_P,0);
    end if;

    if (IE_DOR_CABECA_P IS NOT NULL AND IE_DOR_CABECA_P::text <> '') then
      qt_resultado_w	:= qt_resultado_w + coalesce(IE_DOR_CABECA_P,0);
    end if;

    if (IE_AGITACAO_P IS NOT NULL AND IE_AGITACAO_P::text <> '') then
      qt_resultado_w	:= qt_resultado_w + coalesce(IE_AGITACAO_P,0);
    end if;

    if (IE_ALT_ORIENTACAO_P IS NOT NULL AND IE_ALT_ORIENTACAO_P::text <> '') then
      qt_resultado_w	:= qt_resultado_w + coalesce(IE_ALT_ORIENTACAO_P,0);
    end if;

    return coalesce(qt_resultado_w,0);
   
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_score_escala_ciwaa_md (IE_MAL_ESTAR_P bigint, IE_TREMOR_BRACO_P bigint, IE_SUDORESE_P bigint, IE_PERTURBACAO_COCEIRA_P bigint, IE_PERTURBACAO_AUDITIVA_P bigint, IE_PERTURBACAO_VISUAL_P bigint, IE_NERVOSISMO_P bigint, IE_DOR_CABECA_P bigint, IE_AGITACAO_P bigint, IE_ALT_ORIENTACAO_P bigint ) FROM PUBLIC;

