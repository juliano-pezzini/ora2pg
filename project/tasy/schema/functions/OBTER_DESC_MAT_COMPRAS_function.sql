-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_desc_mat_compras ( cd_material_p bigint, cd_estabelecimento_p bigint) RETURNS varchar AS $body$
DECLARE


cd_grupo_material_w	smallint;
cd_subgrupo_material_w	smallint;
cd_classe_material_w	integer;
nr_regras_w		integer;
ds_descricao_w		varchar(255)	:= '';

C01 CURSOR FOR
SELECT	coalesce(max(ds_descricao),'')
from	regra_desc_material_compra
where	coalesce(cd_grupo_material, cd_grupo_material_w)		= cd_grupo_material_w
and	coalesce(cd_subgrupo_material, cd_subgrupo_material_w)	= cd_subgrupo_material_w
and	coalesce(cd_classe_material, cd_classe_material_w)		= cd_classe_material_w
and	coalesce(cd_material, cd_material_p)			= cd_material_p
and	cd_estabelecimento = cd_estabelecimento_p
order by
	coalesce(cd_grupo_material, 0),
	coalesce(cd_subgrupo_material, 0),
	coalesce(cd_classe_material, 0),
	coalesce(cd_material, 0);


BEGIN
select	count(*)
into STRICT	nr_regras_w
from	regra_desc_material_compra
where	cd_estabelecimento = cd_estabelecimento_p;
if (nr_regras_w > 0) then
	select	cd_grupo_material,
		cd_subgrupo_material,
		cd_classe_material
	into STRICT	cd_grupo_material_w,
		cd_subgrupo_material_w,
		cd_classe_material_w
	from	estrutura_material_v
	where	cd_material = cd_material_p;

	open c01;
	loop
	fetch c01 into
		ds_descricao_w;
	EXIT WHEN NOT FOUND; /* apply on c01 */
	end loop;
	close c01;
end if;

return ds_descricao_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_desc_mat_compras ( cd_material_p bigint, cd_estabelecimento_p bigint) FROM PUBLIC;

