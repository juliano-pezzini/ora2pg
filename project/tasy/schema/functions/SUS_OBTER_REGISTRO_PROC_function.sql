-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION sus_obter_registro_proc ( cd_consulta_publica_p bigint) RETURNS bigint AS $body$
DECLARE


cd_registro_w	smallint;


BEGIN

/* Para buscar o registro através do CD_PROCEDIMENTO existe a function Sus_Obter_TipoReg_Proc */

select	coalesce(max(cd_registro),0)
into STRICT	cd_registro_w
from	sus_procedimento_registro
where	cd_consulta_publica = cd_consulta_publica_p;

return	cd_registro_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION sus_obter_registro_proc ( cd_consulta_publica_p bigint) FROM PUBLIC;

