-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_data_fim_conta_ant ( nr_atendimento_p bigint, nr_interno_conta_p bigint, cd_convenio_p bigint) RETURNS timestamp AS $body$
DECLARE

 
 
dt_periodo_final_w		timestamp;
nr_interno_ant_w		bigint;


BEGIN 
 
dt_periodo_final_w := null;
 
select coalesce(max(nr_interno_conta),0) 
into STRICT	nr_interno_ant_w 
from	conta_paciente 
where	nr_atendimento = nr_atendimento_p 
and	nr_interno_conta < nr_interno_conta_p 
and	cd_convenio_parametro = coalesce(cd_convenio_p,cd_convenio_parametro);
 
if (nr_interno_ant_w	> 0) then 
	begin 
	select max(dt_periodo_final) 
	into STRICT	dt_periodo_final_w 
	from	conta_paciente 
	where	nr_interno_conta = nr_interno_ant_w;
	end;
end if;
 
RETURN dt_periodo_final_w;
 
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_data_fim_conta_ant ( nr_atendimento_p bigint, nr_interno_conta_p bigint, cd_convenio_p bigint) FROM PUBLIC;

