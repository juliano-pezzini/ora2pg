-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_cd_kit_med_presc_md ( cd_kit_p bigint, dt_prescr_p timestamp, dt_prescricao_p timestamp, nr_horas_validade_kit_p bigint ) RETURNS bigint AS $body$
DECLARE

    cd_kit_out_w          bigint := cd_kit_p;
    nr_hora_ult_prescr_w  bigint;

BEGIN
    IF (dt_prescr_p IS NOT NULL AND dt_prescr_p::text <> '') THEN
        nr_hora_ult_prescr_w := obter_hora_entre_datas(dt_prescr_p, dt_prescricao_p);
        IF ( nr_hora_ult_prescr_w < nr_horas_validade_kit_p ) THEN
            cd_kit_out_w := NULL;
        END IF;
    END IF;

    RETURN cd_kit_out_w;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_cd_kit_med_presc_md ( cd_kit_p bigint, dt_prescr_p timestamp, dt_prescricao_p timestamp, nr_horas_validade_kit_p bigint ) FROM PUBLIC;

