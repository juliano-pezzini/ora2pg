-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION ageint_obter_conv_item (nr_seq_agenda_p bigint, nr_seq_agenda_item_p bigint) RETURNS varchar AS $body$
DECLARE


ds_convenio_w	varchar(255);


BEGIN

select	b.ds_convenio
into STRICT	ds_convenio_w
from	convenio b,
	agenda_integrada a
where	a.cd_convenio	= b.cd_convenio
and	a.nr_sequencia	= nr_seq_agenda_p;

select	coalesce(max(b.ds_convenio), ds_convenio_w)
into STRICT	ds_convenio_w
from	convenio b,
	agenda_integrada_conv_item a
where	a.cd_convenio		= b.cd_convenio
and	a.nr_seq_agenda_item	= nr_seq_agenda_item_p;

return	ds_convenio_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION ageint_obter_conv_item (nr_seq_agenda_p bigint, nr_seq_agenda_item_p bigint) FROM PUBLIC;

