-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_agrupar_proced_adep ( nr_prescricao_p bigint, nr_seq_procedimento_p bigint, ie_acm_sn_p text, ie_agrupar_acm_sn_p text, ie_associados_p text, ie_agrupar_associados_p text) RETURNS varchar AS $body$
DECLARE


ie_agrupar_proced_w	varchar(1);


BEGIN
if (ie_agrupar_associados_p = 'N') and (ie_associados_p = 'N') then
	begin
	ie_agrupar_proced_w	:= ie_associados_p;
	end;
elsif (nr_prescricao_p IS NOT NULL AND nr_prescricao_p::text <> '') and (nr_seq_procedimento_p IS NOT NULL AND nr_seq_procedimento_p::text <> '') and (ie_acm_sn_p = 'S') and (ie_agrupar_acm_sn_p = 'N') then
	begin
	ie_agrupar_proced_w	:= 'N';
	end;
else
	begin
	ie_agrupar_proced_w	:= 'S';
	end;
end if;
return ie_agrupar_proced_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_agrupar_proced_adep ( nr_prescricao_p bigint, nr_seq_procedimento_p bigint, ie_acm_sn_p text, ie_agrupar_acm_sn_p text, ie_associados_p text, ie_agrupar_associados_p text) FROM PUBLIC;

