-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_controle_medic ( cd_material_p bigint, cd_estabelecimento_p bigint, ie_controle_medico_p bigint, ie_tipo_atendimento_p bigint, cd_setor_atendimento_p bigint, ie_clinica_p bigint) RETURNS bigint AS $body$
DECLARE


ie_controle_medico_w	smallint;

BEGIN

select	max(ie_controle_medico)
into STRICT	ie_controle_medico_w
from	rep_regra_controle_atb
where	cd_material = cd_material_p
and	cd_estabelecimento = cd_estabelecimento_p
and		coalesce(ie_tipo_atendimento,coalesce(ie_tipo_atendimento_p,0))	= coalesce(ie_tipo_atendimento_p,0)
and		coalesce(cd_setor_atendimento,coalesce(cd_setor_atendimento_p,0)) = coalesce(cd_setor_atendimento_p,0)
and		coalesce(ie_clinica,coalesce(ie_clinica_p,0)) 						= coalesce(ie_clinica_p,0);

if (coalesce(ie_controle_medico_w::text, '') = '') then
	ie_controle_medico_w := ie_controle_medico_p;
end if;

return ie_controle_medico_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_controle_medic ( cd_material_p bigint, cd_estabelecimento_p bigint, ie_controle_medico_p bigint, ie_tipo_atendimento_p bigint, cd_setor_atendimento_p bigint, ie_clinica_p bigint) FROM PUBLIC;

