-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_relatorio_convenio ( cd_pessoa_fisica_p bigint, cd_convenio_p bigint) RETURNS bigint AS $body$
DECLARE


ie_possui_registro_w	varchar(1);
cd_relatorio_w		bigint:=0;


BEGIN

select	CASE WHEN count(*)=0 THEN 'N'  ELSE 'S' END
into STRICT	ie_possui_registro_w
from	med_relatorio_convenio
where	cd_pessoa_fisica = cd_pessoa_fisica_p;

if (ie_possui_registro_w = 'S') then
	select	cd_relatorio
	into STRICT	cd_relatorio_w
	from	med_relatorio_convenio
	where	cd_pessoa_fisica 	= cd_pessoa_fisica_p
	and	cd_convenio	= cd_convenio_p;
end if;


return cd_relatorio_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_relatorio_convenio ( cd_pessoa_fisica_p bigint, cd_convenio_p bigint) FROM PUBLIC;

