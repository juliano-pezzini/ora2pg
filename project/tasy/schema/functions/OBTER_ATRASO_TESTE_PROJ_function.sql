-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_atraso_teste_proj ( DT_INI_P timestamp, IE_TAMANHO_P text) RETURNS bigint AS $body$
DECLARE

  PR_RETORNO_W	bigint  := 0;

BEGIN
  IF (coalesce(DT_INI_P::text, '') = '') THEN
    PR_RETORNO_W := 0;
  ELSE
    PR_RETORNO_W := TRUNC(clock_timestamp()) - TRUNC(OBTER_DIA_UTIL_PERIODO(1, DT_INI_P, OBTER_DIAS_TESTE_PROJ(IE_TAMANHO_P)));
  END IF;

  RETURN	PR_RETORNO_W;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_atraso_teste_proj ( DT_INI_P timestamp, IE_TAMANHO_P text) FROM PUBLIC;

