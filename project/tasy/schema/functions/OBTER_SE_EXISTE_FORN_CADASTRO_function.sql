-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_existe_forn_cadastro ( cd_fornecedor_p text, cd_estabelecimento_p bigint) RETURNS varchar AS $body$
DECLARE

 
ie_cod_fornec_integracao_w			parametro_compras.ie_cod_fornec_integracao%type;			
qt_registros_w				bigint;
ie_existe_w				varchar(1) := 'N';
			

BEGIN 
 
select	coalesce(max(ie_cod_fornec_integracao),'CNPJ') 
into STRICT	ie_cod_fornec_integracao_w 
from	parametro_compras 
where	cd_estabelecimento = cd_estabelecimento_p;
 
if (ie_cod_fornec_integracao_w = 'CNPJ') then 
	select	count(*) 
	into STRICT	qt_registros_w 
	from	pessoa_juridica 
	where	cd_cgc = cd_fornecedor_p 
	and	ie_situacao = 'A';
elsif (ie_cod_fornec_integracao_w = 'RFC') then 
	select	count(*) 
	into STRICT	qt_registros_w 
	from	pessoa_juridica 
	where	cd_rfc = cd_fornecedor_p 
	and	ie_situacao = 'A';
elsif (ie_cod_fornec_integracao_w = 'CURP') then 
	select	count(*) 
	into STRICT	qt_registros_w 
	from	pessoa_juridica 
	where	cd_curp = cd_fornecedor_p 
	and	ie_situacao = 'A';
end if;
 
if (qt_registros_w > 0) then 
	ie_existe_w := 'S';
end if;
	 
 
return	ie_existe_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_existe_forn_cadastro ( cd_fornecedor_p text, cd_estabelecimento_p bigint) FROM PUBLIC;

