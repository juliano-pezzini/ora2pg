-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_prec_lib ( nr_atendimento_p bigint) RETURNS varchar AS $body$
DECLARE


ie_retorno_w	varchar(1) := 'N';


BEGIN

if (coalesce(nr_atendimento_p,0) > 0) then

	select	CASE WHEN count(*)=0 THEN 'N'  ELSE 'S' END
	into STRICT	ie_retorno_w
	from	atendimento_precaucao a ,
		cih_precaucao b
	where	a.nr_atendimento = nr_atendimento_p
	and	b.nr_sequencia	= a.nr_seq_precaucao
	and	coalesce(a.dt_inativacao::text, '') = ''
	and	((coalesce(a.dt_termino::text, '') = '') or (clock_timestamp() between coalesce(a.dt_inicio,clock_timestamp() - interval '1 days') and a.dt_termino))
	and	(a.dt_liberacao IS NOT NULL AND a.dt_liberacao::text <> '')
	and	a.nr_sequencia	= (SELECT max(nr_sequencia)
				   from	  atendimento_precaucao
				   where  nr_atendimento = nr_atendimento_p);

end if;

return ie_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_prec_lib ( nr_atendimento_p bigint) FROM PUBLIC;

