-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_exibe_item_oft ( nr_seq_consulta_p bigint, nr_seq_item_p bigint, cd_pessoa_fisica_p bigint, ds_atributo_p text, ie_vis_tipo_cons_p text) RETURNS varchar AS $body$
DECLARE

 
ds_retorno_w				varchar(255) := 'N';
ie_parametro_83_w			varchar(1);
ie_possui_regra_oft_w			varchar(1);
ie_visualiza_item_oft_w			varchar(1);
ie_visualizar_qt_atrib_w		varchar(1);

BEGIN
 
/* -----------------------------------------------------Restrições da procedure Monta_itens_oftalmo do delphi-----------------------------------------------------*/
 
 
--Carregar Parâmetros 
ie_parametro_83_w := Obter_Param_padrao_usuario(3010, 83, wheb_usuario_pck.get_cd_perfil, wheb_usuario_pck.get_nm_usuario, wheb_usuario_pck.get_cd_estabelecimento, ie_parametro_83_w);
 
select 	possui_regra_tipo_cons_oft(nr_seq_consulta_p,'') 
into STRICT	ie_possui_regra_oft_w
;
 
if ('S' = ie_possui_regra_oft_w) then 
	if ('E' = ie_vis_tipo_cons_p) then 
		select 	obter_se_visualiza_item_oft(nr_seq_consulta_p,nr_seq_item_p,'') 
		into STRICT	ie_visualiza_item_oft_w 
		;
 
		if ('S' = ie_visualiza_item_oft_w) then 
			ds_retorno_w := 'S';
			if ((trim(both ds_atributo_p) IS NOT NULL AND (trim(both ds_atributo_p))::text <> '')) then 
				if ('S' = ie_parametro_83_w) then 
					ds_retorno_w := ds_atributo_p||' '||substr(obter_quantidade_reg_itens_oft(nr_seq_item_p,coalesce(cd_pessoa_fisica_p,0),wheb_usuario_pck.get_nm_usuario),1,255);
				else 
					ds_retorno_w := ds_atributo_p;
				end if;
			end if;
		end if;
	else 
		if ((ie_vis_tipo_cons_p = 'F') 
		  and	obter_se_fim_consulta_oftalmo(nr_seq_consulta_p) = 'S')	then 
			if (se_possui_regist_item_consulta(nr_seq_consulta_p,nr_seq_item_p,cd_pessoa_fisica_p) = 'S') then 
				ds_retorno_w := 'S';
				if ((trim(both ds_atributo_p) IS NOT NULL AND (trim(both ds_atributo_p))::text <> '')) then 
					ds_retorno_w := ds_atributo_p;
				end if;
			end if;
		else 
			ds_retorno_w := 'S';
			if ((trim(both ds_atributo_p) IS NOT NULL AND (trim(both ds_atributo_p))::text <> '')) then 
				if ('S' = ie_parametro_83_w) then 
					ds_retorno_w := ds_atributo_p||' '||substr(obter_quantidade_reg_itens_oft(nr_seq_item_p,coalesce(cd_pessoa_fisica_p,0),wheb_usuario_pck.get_nm_usuario),1,255);
				else 
					ds_retorno_w := ds_atributo_p;
				end if;
			end if;
		end if;
	end if;
else 
	if 	((ie_vis_tipo_cons_p = 'F') and (obter_se_fim_consulta_oftalmo(nr_seq_consulta_p) = 'S')) then 
		if (se_possui_regist_item_consulta(nr_seq_consulta_p,nr_seq_item_p,cd_pessoa_fisica_p) = 'S') then 
			ds_retorno_w := 'S';
			if ((trim(both ds_atributo_p) IS NOT NULL AND (trim(both ds_atributo_p))::text <> '')) then 
				ds_retorno_w := ds_atributo_p;
			end if;
		end if;
	else 
		ds_retorno_w := 'S';
		if ((trim(both ds_atributo_p) IS NOT NULL AND (trim(both ds_atributo_p))::text <> '')) then 
			if ('S' = ie_parametro_83_w) then 
				ds_retorno_w := ds_atributo_p||' '||substr(obter_quantidade_reg_itens_oft(nr_seq_item_p,coalesce(cd_pessoa_fisica_p,0),wheb_usuario_pck.get_nm_usuario),1,255);
			else 
				ds_retorno_w := ds_atributo_p;
			end if;
		end if;
	end if;
end if;
 
return	ds_retorno_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_exibe_item_oft ( nr_seq_consulta_p bigint, nr_seq_item_p bigint, cd_pessoa_fisica_p bigint, ds_atributo_p text, ie_vis_tipo_cons_p text) FROM PUBLIC;

