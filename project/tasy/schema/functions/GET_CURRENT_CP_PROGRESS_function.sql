-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION get_current_cp_progress ( nr_seq_pe_prescr_p bigint ) RETURNS varchar AS $body$
DECLARE


si_progress_w	pe_prescr_cp_progress.si_progress%type;

c_progress CURSOR FOR

		SELECT		a.si_progress
		from		pe_prescr_cp_progress a
		where		a.nr_seq_prescr = nr_seq_pe_prescr_p
		and			(a.dt_liberacao IS NOT NULL AND a.dt_liberacao::text <> '')
		and			a.ie_situacao = 'A'
		and			coalesce(a.dt_suspensao::text, '') = ''
		order by    a.dt_liberacao;

BEGIN

    for r_c_progress in c_progress loop
        si_progress_w := r_c_progress.si_progress;
    end loop;

    return	si_progress_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION get_current_cp_progress ( nr_seq_pe_prescr_p bigint ) FROM PUBLIC;

