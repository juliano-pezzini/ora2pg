-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_data_ultimo_protoc_pac (cd_pessoa_fisica_p text) RETURNS timestamp AS $body$
DECLARE

dt_ultimo_w	timestamp;

BEGIN

IF (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') THEN
	SELECT	MAX(dt_protocolo)
	INTO STRICT 	dt_ultimo_w
	FROM	paciente_setor
	WHERE 	cd_pessoa_fisica = cd_pessoa_fisica_p;
END IF;

RETURN	dt_ultimo_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_data_ultimo_protoc_pac (cd_pessoa_fisica_p text) FROM PUBLIC;

