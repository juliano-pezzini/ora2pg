-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_linha_branco ( nr_linha_p bigint) RETURNS varchar AS $body$
DECLARE


ds_linha_w			varchar(255);
cont_linha_w		integer := 0;


BEGIN

ds_linha_w 	:= '';
ds_linha_w	:= ds_linha_w || chr(13) || chr(10);
if (nr_linha_p > 0) then
    	LOOP
      cont_linha_w 	:= cont_linha_w + 1;
      exit when cont_linha_w >= nr_linha_p;
      ds_linha_w		:= ds_linha_w || chr(13) || chr(10);
    	END LOOP;
end if;
return ds_linha_w;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_linha_branco ( nr_linha_p bigint) FROM PUBLIC;

