-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_exibe_atrib_diag (nr_seq_episodio_p bigint, ds_atributo_p text, cd_perfil_p bigint) RETURNS varchar AS $body$
DECLARE


ds_resultado_w            varchar(1);
ds_atrib_dominio_w        valor_dominio.vl_dominio%type;
nr_seq_classif_diag_w        classificacao_diagnostico.nr_sequencia%type;
nr_seq_tipo_episodio_w        restricao_diagnostico.nr_seq_tipo_episodio%type;
nr_seq_tipo_admi_fat_w        restricao_diagnostico.nr_seq_tipo_admissao_fat%type;


C01 CURSOR FOR
SELECT  a.nr_sequencia,
       b.cd_perfil,
       b.nr_seq_tipo_episodio,
       b.nr_seq_tipo_admissao_fat
from    classificacao_diagnostico a ,
       restricao_diagnostico b
where   b.nr_seq_classificacao   = a.nr_sequencia
and     a.ie_classificacao_diagnostico = ds_atrib_dominio_w;

c01_w    C01%rowtype;


BEGIN
ds_resultado_w     := 'S';

select  nr_seq_tipo_admissao_fat
into STRICT     nr_seq_tipo_admi_fat_w
from    atendimento_paciente
where   nr_atendimento = obter_atendimento_episodio(nr_seq_episodio_p);

select     nr_seq_tipo_episodio
into STRICT     nr_seq_tipo_episodio_w
from     episodio_paciente
where     nr_sequencia = nr_seq_episodio_p;

if (ds_atributo_p = 'IE_DIAG_PRINC_EPISODIO') then
    ds_atrib_dominio_w := 'DPE';
end if;

open C01;
loop
fetch C01 into
    c01_w;
EXIT WHEN NOT FOUND; /* apply on C01 */

        if (c01_w.cd_perfil = cd_perfil_p and coalesce(c01_w.nr_seq_tipo_episodio::text, '') = '' and coalesce(c01_w.nr_seq_tipo_admissao_fat::text, '') = '') then
            ds_resultado_w := 'S';
        elsif (c01_w.cd_perfil = cd_perfil_p and c01_w.nr_seq_tipo_episodio = nr_seq_tipo_episodio_w and coalesce(c01_w.nr_seq_tipo_admissao_fat::text, '') = '') then
            ds_resultado_w := 'S';
        elsif (c01_w.cd_perfil = cd_perfil_p and c01_w.nr_seq_tipo_episodio = nr_seq_tipo_episodio_w and c01_w.nr_seq_tipo_admissao_fat = nr_seq_tipo_admi_fat_w) then
            ds_resultado_w := 'S';
        else
            ds_resultado_w := 'N';
        end if;
	
end loop;
close C01;

return    ds_resultado_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_exibe_atrib_diag (nr_seq_episodio_p bigint, ds_atributo_p text, cd_perfil_p bigint) FROM PUBLIC;

