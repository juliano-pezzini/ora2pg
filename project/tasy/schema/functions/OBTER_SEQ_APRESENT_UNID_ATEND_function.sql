-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_seq_apresent_unid_atend ( cd_setor_atendimento_p bigint, cd_unidade_basica_p text, cd_unidade_compl_p text) RETURNS bigint AS $body$
DECLARE


nr_seq_interno_w	bigint;


BEGIN

IF (cd_setor_atendimento_p IS NOT NULL AND cd_setor_atendimento_p::text <> '') AND (cd_unidade_basica_p IS NOT NULL AND cd_unidade_basica_p::text <> '') AND (cd_unidade_compl_p IS NOT NULL AND cd_unidade_compl_p::text <> '') THEN
	BEGIN

	SELECT	coalesce(MAX(nr_seq_apresent),0)
	INTO STRICT	nr_seq_interno_w
	FROM	unidade_atendimento
	WHERE	cd_setor_atendimento	= cd_setor_atendimento_p
	AND	cd_unidade_basica	= cd_unidade_basica_p
	AND	cd_unidade_compl	= cd_unidade_compl_p;

	END;
END IF;

RETURN	nr_seq_interno_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_seq_apresent_unid_atend ( cd_setor_atendimento_p bigint, cd_unidade_basica_p text, cd_unidade_compl_p text) FROM PUBLIC;

