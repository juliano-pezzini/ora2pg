-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_prioridade_etapa_proj ( NR_OS_STATUS_GERAL_P bigint) RETURNS varchar AS $body$
DECLARE


 PR_RETORNO_W	varchar(1) := ' ';


BEGIN

	BEGIN
    SELECT coalesce(IE_PRIORIDADE, ' ')
      INTO STRICT PR_RETORNO_W
      FROM MAN_ORDEM_SERVICO OS
     WHERE OS.NR_SEQUENCIA = NR_OS_STATUS_GERAL_P  LIMIT 1;

	EXCEPTION
		WHEN no_data_found THEN
			PR_RETORNO_W := ' ';
		WHEN OTHERS THEN
			PR_RETORNO_W := ' ';
	END;

RETURN	PR_RETORNO_W;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_prioridade_etapa_proj ( NR_OS_STATUS_GERAL_P bigint) FROM PUBLIC;

