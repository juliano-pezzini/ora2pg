-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION get_last_nr_component_sepsis ( nr_atendimento_p bigint, ds_table_p text, ds_table_nr_field_p text, ds_table_dt_field_p text) RETURNS bigint AS $body$
DECLARE


ds_nr_sequencia_w		bigint := null;
ds_sql				varchar(1000);


BEGIN

if ((nr_atendimento_p IS NOT NULL AND nr_atendimento_p::text <> '')
	and (ds_table_p IS NOT NULL AND ds_table_p::text <> '')
	and (ds_table_nr_field_p IS NOT NULL AND ds_table_nr_field_p::text <> '')
	and (ds_table_dt_field_p IS NOT NULL AND ds_table_dt_field_p::text <> '')) then

	ds_sql :=	' select ' || ds_table_nr_field_p || ' from (' ||
				'                 select ' || ds_table_nr_field_p || ' from ' || ds_table_p ||
				'                 where nr_atendimento = :nr_atendimento_p' ||
				'                 order by ' || ds_table_dt_field_p || ' desc, nr_sequencia desc)' ||
				'       where  rownum = 1 ';

	begin
		EXECUTE
			ds_sql
		into STRICT
			ds_nr_sequencia_w
		using
			nr_atendimento_p;

		EXCEPTION WHEN no_data_found then
			ds_nr_sequencia_w := null;

	end;

end if;

return ds_nr_sequencia_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION get_last_nr_component_sepsis ( nr_atendimento_p bigint, ds_table_p text, ds_table_nr_field_p text, ds_table_dt_field_p text) FROM PUBLIC;

