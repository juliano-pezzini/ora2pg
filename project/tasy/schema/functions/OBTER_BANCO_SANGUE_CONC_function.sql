-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_banco_sangue_conc ( NR_SEQUENCIA_P bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w		varchar(255);


BEGIN

if (NR_SEQUENCIA_P IS NOT NULL AND NR_SEQUENCIA_P::text <> '') then
	SELECT	SUBSTR(OBTER_SELECT_CONCATENADO_BV('SELECT		SUBSTR(obter_desc_proc_interno(b.nr_seq_proc_interno),1,255)
	FROM	AGENDA_PAC_SANGUE b,
		AGENDA_PACIENTE a
	WHERE	nr_seq_agenda =  :nr_sequencia
	AND	a.nr_sequencia = b.nr_seq_agenda','nr_sequencia=' || NR_SEQUENCIA_P || '',', '),1,255) ds
	into STRICT	ds_retorno_w
	;

end if;

RETURN ds_retorno_w;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_banco_sangue_conc ( NR_SEQUENCIA_P bigint) FROM PUBLIC;

