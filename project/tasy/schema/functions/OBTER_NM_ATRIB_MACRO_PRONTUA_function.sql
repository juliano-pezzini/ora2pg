-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_nm_atrib_macro_prontua ( nm_macro_p text) RETURNS varchar AS $body$
DECLARE

nm_atributo_w	varchar(50);

BEGIN
if (nm_macro_p IS NOT NULL AND nm_macro_p::text <> '') then
	begin
	select	nm_atributo
	into STRICT	nm_atributo_w
	from	macro_prontuario
	where	upper(nm_macro) = upper(nm_macro_p);
	end;
end if;
return nm_atributo_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_nm_atrib_macro_prontua ( nm_macro_p text) FROM PUBLIC;

