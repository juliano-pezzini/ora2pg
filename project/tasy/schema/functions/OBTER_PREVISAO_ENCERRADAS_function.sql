-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_previsao_encerradas ( vl_media_encerrada_p bigint) RETURNS bigint AS $body$
DECLARE


resultado_w  bigint;
qtd_dias_mes_w 	bigint := 	SUBSTR(LAST_DAY(clock_timestamp()),1,2);
qtd_dia_atual_w	bigint := 	SUBSTR(clock_timestamp(),1,2);


BEGIN

	resultado_w := ((vl_media_encerrada_p * qtd_dias_mes_w) / qtd_dia_atual_w );


return resultado_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_previsao_encerradas ( vl_media_encerrada_p bigint) FROM PUBLIC;

