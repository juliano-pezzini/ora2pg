-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION proj_obter_media_valor_nivel () RETURNS bigint AS $body$
DECLARE


vl_nivel_w			double precision;


BEGIN

	select	avg(coalesce(vl_nivel, 0))
	into STRICT	vl_nivel_w
	from	proj_nivel_consultor
	where	ie_situacao = 'A'
	and	(vl_nivel IS NOT NULL AND vl_nivel::text <> '');

return vl_nivel_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION proj_obter_media_valor_nivel () FROM PUBLIC;

