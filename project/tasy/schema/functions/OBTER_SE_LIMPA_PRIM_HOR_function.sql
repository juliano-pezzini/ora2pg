-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_limpa_prim_hor ( cd_intervalo_p text, cd_setor_p bigint) RETURNS char AS $body$
DECLARE


ie_limpar_w		char(1) := 'N';


BEGIN
if (cd_intervalo_p IS NOT NULL AND cd_intervalo_p::text <> '') then
	ie_limpar_w	:= obter_dados_apraz_interv(cd_intervalo_p, cd_setor_p, '2');

	if (coalesce(ie_limpar_w::text, '') = '') then
		select	coalesce(ie_limpa_prim_hor,'N')
		into STRICT	ie_limpar_w
		from	intervalo_prescricao
		where	cd_intervalo = cd_intervalo_p;
	end if;
end if;

return ie_limpar_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_limpa_prim_hor ( cd_intervalo_p text, cd_setor_p bigint) FROM PUBLIC;

