-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION sus_obter_substituir_cnes ( cd_cnes_p bigint, cd_setor_atendimento_p bigint, cd_estabelecimento_p bigint, ie_tipo_atendimento_p bigint) RETURNS bigint AS $body$
DECLARE


cd_retorno_w			bigint;
ie_tipo_atend_cnes_setor_w		smallint := 0;


BEGIN

begin
select	coalesce(ie_tipo_atend_cnes_setor,0)
into STRICT	ie_tipo_atend_cnes_setor_w
from	sus_parametros_bpa
where	cd_estabelecimento = cd_estabelecimento_p;
exception
when others then
	ie_tipo_atend_cnes_setor_w := 0;
end;

if (ie_tipo_atend_cnes_setor_w = ie_tipo_atendimento_p) or (ie_tipo_atend_cnes_setor_w = 0) or (ie_tipo_atendimento_p = 0) then
	begin
	select	coalesce(max(cd_cnes),cd_cnes_p)
	into STRICT	cd_retorno_w
	from	setor_atendimento
	where	cd_setor_atendimento	= cd_setor_atendimento_p;
	end;
else
	begin
	cd_retorno_w := cd_cnes_p;
	end;
end if;

return cd_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION sus_obter_substituir_cnes ( cd_cnes_p bigint, cd_setor_atendimento_p bigint, cd_estabelecimento_p bigint, ie_tipo_atendimento_p bigint) FROM PUBLIC;

