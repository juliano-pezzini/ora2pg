-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obtain_nurse_pract_proc_acc ( nr_atendimento_p bigint,cd_especialidade_p bigint) RETURNS varchar AS $body$
DECLARE

ds_retorno_w  varchar(1) := 'N';
qt_registro_w bigint  := 0;

BEGIN

select	count(1)
into STRICT	qt_registro_w
from	procedimento_paciente
where	nr_atendimento   = nr_atendimento_p
and     ie_origem_proced = 20
and     (obter_dados_estrut_proc(cd_procedimento,ie_origem_proced,'C','G'))::numeric  in (
                SELECT
                        cd_grupo_proc
                from
                        grupo_proc
                where
                        cd_especialidade = cd_especialidade_p
	);

if (qt_registro_w > 0) then
        ds_retorno_w := 'S';
end if;
return ds_retorno_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obtain_nurse_pract_proc_acc ( nr_atendimento_p bigint,cd_especialidade_p bigint) FROM PUBLIC;

