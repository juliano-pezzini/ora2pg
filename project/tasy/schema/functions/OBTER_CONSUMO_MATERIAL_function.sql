-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_consumo_material ( ie_tipo_p text, cd_estabelecimento_p bigint, cd_material_p bigint, dt_inicial_p timestamp, dt_final_p timestamp) RETURNS bigint AS $body$
DECLARE


ds_retorno_w			double precision;
qt_consumo_w			double precision;
vl_consumo_w			double precision;


BEGIN

select	sum(coalesce(a.qt_consumo,0)) qt_estoque,
	sum(coalesce(a.vl_consumo,0)) vl_consumo
into STRICT	qt_consumo_w,
	vl_consumo_w
from	movto_estoque_operacao_v a
where	a.cd_estabelecimento	= cd_estabelecimento_p
and	a.cd_material		= cd_material_p
and	a.dt_mesano_referencia between dt_inicial_p and dt_final_p
and	(a.cd_centro_custo IS NOT NULL AND a.cd_centro_custo::text <> '');

if (ie_tipo_p = 'Q') then
	ds_retorno_w := qt_consumo_w;
elsif (ie_tipo_p = 'V') then
	ds_retorno_w := vl_consumo_w;
end if;

return  ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_consumo_material ( ie_tipo_p text, cd_estabelecimento_p bigint, cd_material_p bigint, dt_inicial_p timestamp, dt_final_p timestamp) FROM PUBLIC;

