-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_desc_redu_mat_prescr ( nr_prescricao_p bigint, nr_sequencia_p bigint, ie_localizacao_p text, cd_local_estoque_p bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w		varchar(128);
ds_material_w		varchar(100);
ie_tipo_fonte_w	varchar(003);
ds_local_w		varchar(80);
cd_material_w		integer;
dt_baixa_w		timestamp;
ie_urgencia_w		varchar(01);
cd_local_w		integer;


BEGIN

if (ie_localizacao_p = 'S') and (coalesce(cd_local_estoque_p,0) = 0) then
	SELECT	max(b.cd_local_estoque)
	into STRICT	cd_local_w
	FROM	setor_atendimento b,
		prescr_medica a
	where	nr_prescricao	= nr_prescricao_p
	and	a.cd_setor_atendimento	= b.cd_setor_atendimento;
end if;

cd_local_w := coalesce(cd_local_w, cd_local_estoque_p);

select	cd_material,
	dt_baixa,
	coalesce(ie_urgencia,'N')
into STRICT	cd_material_w,
	dt_baixa_w,
	ie_urgencia_w
from	Prescr_material
where	nr_prescricao		= nr_prescricao_p
  and	nr_sequencia		= nr_sequencia_p;

select	substr(max(ds_reduzida),1,100),
	max(coalesce(ie_tipo_fonte_prescr,' '))
into STRICT	ds_material_w,
	ie_tipo_fonte_w
from 	material
where cd_material = cd_material_w;

ds_local_w			:= '';
if (ie_localizacao_p = 'S') and (coalesce(cd_local_w,0) > 0) then
	Select substr(obter_localizacao_material(cd_material_w, cd_local_w),1,80)
	into STRICT	ds_local_w
	;
	if (ds_local_w IS NOT NULL AND ds_local_w::text <> '') then
		ds_local_w	:= ' [' || ds_local_w || ']';
	end if;
end if;

ds_retorno_w		:= ds_material_w || ds_local_w;

if (ie_urgencia_w = 'S') then
	ds_retorno_w	:= '#' || ds_retorno_w || '#' || 'NI' ||'#P';
elsif (ie_tipo_fonte_w = 'MS') then
	begin
	ds_retorno_w 	:= upper(ds_retorno_w);
	ie_tipo_fonte_w	:= 'S';
	ds_retorno_w	:= '#' || ds_retorno_w || '#' || ie_tipo_fonte_w ||'#P';
	end;
elsif (ie_tipo_fonte_w <> ' ') and (ie_tipo_fonte_w <> 'M')  then
	ds_retorno_w	:= '#' || ds_retorno_w || '#' || ie_tipo_fonte_w ||'#P';
elsif (ie_tipo_fonte_w = 'M') then
	ds_retorno_w	:= upper(ds_retorno_w);
elsif (ie_tipo_fonte_w = 'MN') then
	ds_retorno_w	:= upper(ds_retorno_w);
	ds_retorno_w	:= '#' || ds_retorno_w || '#' || 'N' ||'#P';
end if;

RETURN ds_retorno_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_desc_redu_mat_prescr ( nr_prescricao_p bigint, nr_sequencia_p bigint, ie_localizacao_p text, cd_local_estoque_p bigint) FROM PUBLIC;

