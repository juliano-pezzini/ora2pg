-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_qt_status_pac_agecons (cd_pessoa_fisica_p text, ie_status_agenda_p text, cd_estabelecimento_p bigint) RETURNS bigint AS $body$
DECLARE

 
qt_retorno_w	bigint;
			

BEGIN 
 
if (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') and (ie_status_agenda_p IS NOT NULL AND ie_status_agenda_p::text <> '') then 
	select	count(*) 
	into STRICT	qt_retorno_w 
	from	agenda a, 
		agenda_consulta b 
	where	a.cd_agenda	= b.cd_agenda 
	and	a.cd_estabelecimento	= cd_estabelecimento_p 
	and	b.cd_pessoa_fisica	= cd_pessoa_fisica_p 
	and	b.ie_status_agenda	= ie_status_agenda_p;
end if;
 
return	qt_retorno_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_qt_status_pac_agecons (cd_pessoa_fisica_p text, ie_status_agenda_p text, cd_estabelecimento_p bigint) FROM PUBLIC;

