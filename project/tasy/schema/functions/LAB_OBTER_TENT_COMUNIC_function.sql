-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION lab_obter_tent_comunic ( nr_prescricao_p bigint, nr_seq_prescr_p bigint) RETURNS varchar AS $body$
DECLARE

    ie_comunicacao_critico_w prescr_procedimento_compl.ie_comunicacao_critico%type;
    ds_valor_dominio valor_dominio.ds_valor_dominio%type;

BEGIN

    SELECT ppc.ie_comunicacao_critico
    INTO STRICT ie_comunicacao_critico_w
    FROM prescr_procedimento pp
    INNER JOIN prescr_procedimento_compl ppc
    ON pp.nr_seq_proc_compl      = ppc.nr_sequencia
    WHERE pp.nr_prescricao       = nr_prescricao_p
    AND pp.nr_sequencia          = nr_seq_prescr_p;

    IF (ie_comunicacao_critico_w IS NOT NULL AND ie_comunicacao_critico_w::text <> '') THEN
      ds_valor_dominio          := obter_valor_dominio(8811,ie_comunicacao_critico_w);
    END IF;

    RETURN ds_valor_dominio;

  END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION lab_obter_tent_comunic ( nr_prescricao_p bigint, nr_seq_prescr_p bigint) FROM PUBLIC;

