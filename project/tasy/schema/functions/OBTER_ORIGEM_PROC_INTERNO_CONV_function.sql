-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_origem_proc_interno_conv (nr_seq_proc_interno_p bigint, cd_convenio_p bigint, cd_categoria_p bigint, dt_vigencia_p timestamp, cd_estabelecimento_p bigint) RETURNS bigint AS $body$
DECLARE


cd_edicao_amb_w		integer;
ie_origem_Proced_w	bigint;
dt_vigencia_w		timestamp;


BEGIN

dt_vigencia_w	:= trunc(coalesce(dt_vigencia_p, clock_timestamp()));

select	obter_edicao_amb(cd_estabelecimento_p, cd_convenio_p, cd_categoria_p, dt_vigencia_p)
into STRICT	cd_edicao_amb_w
;

select	coalesce(max(a.ie_origem_Proced),0)
into STRICT	ie_origem_Proced_w
from	proc_interno_conv a
where	a.nr_seq_proc_interno 			= nr_seq_proc_interno_p
and	coalesce(a.cd_convenio, cd_convenio_p)	= cd_convenio_p
and	coalesce(a.cd_edicao_amb, coalesce(cd_edicao_amb_w,0))	= coalesce(cd_edicao_amb_w,0)
and	dt_vigencia_w between coalesce(a.dt_inicio_vigencia, dt_vigencia_w) and coalesce(a.dt_final_vigencia, dt_vigencia_w);

if (ie_origem_Proced_w = 0) then
	select	b.ie_origem_Proced
	into STRICT	ie_origem_Proced_w
	from	proc_interno b
	where	b.nr_sequencia = nr_seq_proc_interno_p;
end if;


RETURN	ie_origem_Proced_w;

END	;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_origem_proc_interno_conv (nr_seq_proc_interno_p bigint, cd_convenio_p bigint, cd_categoria_p bigint, dt_vigencia_p timestamp, cd_estabelecimento_p bigint) FROM PUBLIC;

