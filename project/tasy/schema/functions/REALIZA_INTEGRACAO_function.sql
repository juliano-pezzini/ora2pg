-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION realiza_integracao (cd_procedimento_p bigint, nr_seq_proc_interno_p bigint, integracao_p text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w varchar(1);
qtd_integracao_w bigint;


BEGIN
  select count(1)
    into STRICT qtd_integracao_w
    from proc_interno_integracao
   where cd_integracao = integracao_p and
         ((nr_seq_proc_interno = nr_seq_proc_interno_p) or (cd_procedimento = cd_procedimento_p));

  if (qtd_integracao_w > 0) then
    ds_retorno_w := 'S';
  else
    ds_retorno_w := 'N';
  end if;

  RETURN	ds_retorno_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION realiza_integracao (cd_procedimento_p bigint, nr_seq_proc_interno_p bigint, integracao_p text) FROM PUBLIC;

