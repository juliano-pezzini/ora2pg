-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_qtd_etiqueta_regra ( cd_setor_atendimento_p bigint, cd_procedimento_p bigint, ie_origem_proced_p bigint, nr_seq_proc_interno_p bigint) RETURNS bigint AS $body$
DECLARE

qt_etiqueta_w	bigint	:= 1;

C01 CURSOR FOR
	SELECT	QT_ETIQUETA
	from	REGRA_ETIQUETA_SETOR
	where	cd_setor_atendimento	= cd_setor_atendimento_p
	and (coalesce(cd_procedimento,cd_procedimento_p)	= cd_procedimento_p)
	and (coalesce(ie_origem_proced,ie_origem_proced_p)	= ie_origem_proced_p or coalesce(cd_procedimento::text, '') = '')
	and (coalesce(nr_seq_proc_interno,nr_seq_proc_interno_p)	= nr_seq_proc_interno_p or coalesce(nr_seq_proc_interno_p::text, '') = '')
	order by coalesce(cd_procedimento, 0),
		coalesce(nr_seq_proc_interno,0);


BEGIN
open C01;
loop
fetch C01 into
	qt_etiqueta_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
end loop;
close C01;

return	qt_etiqueta_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_qtd_etiqueta_regra ( cd_setor_atendimento_p bigint, cd_procedimento_p bigint, ie_origem_proced_p bigint, nr_seq_proc_interno_p bigint) FROM PUBLIC;

