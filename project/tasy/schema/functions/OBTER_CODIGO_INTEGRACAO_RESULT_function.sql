-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_codigo_integracao_result ( nr_seq_result_lab_p bigint, cd_exame_integracao_p text) RETURNS varchar AS $body$
DECLARE


ds_result_w		varchar(2000);


BEGIN

select	max(a.DS_EXAME_INTEGRACAO)
into STRICT	ds_result_w
from	RESULT_LABORATORIO_INTEG a
where	a.nr_seq_result_lab = nr_seq_result_lab_p
and		a.cd_exame_integracao = cd_exame_integracao_p;

return ds_result_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_codigo_integracao_result ( nr_seq_result_lab_p bigint, cd_exame_integracao_p text) FROM PUBLIC;

