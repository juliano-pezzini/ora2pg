-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_senha_chamada_pep (nr_seq_senha_p bigint) RETURNS varchar AS $body$
DECLARE

qtd_registros_w	bigint;

BEGIN

select   count(*)
into STRICT	qtd_registros_w
FROM 	paciente_senha_fila_hist
WHERE 	nr_seq_senha = nr_seq_senha_p
AND 	cd_funcao_ativa = 281;

if (qtd_registros_w > 0) then
	return	'S';
else
	return	'N';
end if;


end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_senha_chamada_pep (nr_seq_senha_p bigint) FROM PUBLIC;

