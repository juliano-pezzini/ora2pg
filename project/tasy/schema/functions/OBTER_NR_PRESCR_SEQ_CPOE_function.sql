-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_nr_prescr_seq_cpoe (ie_tipo_item_p text, nr_seq_cpoe_p bigint) RETURNS bigint AS $body$
DECLARE


nr_prescricao_w		prescr_medica.nr_prescricao%type;


BEGIN

if (nr_seq_cpoe_p IS NOT NULL AND nr_seq_cpoe_p::text <> '') then
	if (ie_tipo_item_p in ('IAH', 'M', 'MAT', 'SOL')) then
		select 	max(nr_prescricao)
		into STRICT	nr_prescricao_w
		from	prescr_material
		where	nr_seq_mat_cpoe = nr_seq_cpoe_p;
	elsif (ie_tipo_item_p in ('S', 'SNE', 'LD')) then
		select 	max(nr_prescricao)
		into STRICT	nr_prescricao_w
		from	prescr_material
		where	nr_seq_dieta_cpoe = nr_seq_cpoe_p;
	elsif (ie_tipo_item_p in ('P', 'L', 'HM', 'CIG', 'CCG')) then
		select 	max(nr_prescricao)
		into STRICT	nr_prescricao_w
		from	prescr_procedimento
		where	nr_seq_proc_cpoe = nr_seq_cpoe_p;
	elsif (ie_tipo_item_p = 'O') then
		select 	max(nr_prescricao)
		into STRICT	nr_prescricao_w
		from	prescr_gasoterapia
		where	nr_seq_gas_cpoe = nr_seq_cpoe_p;
	elsif (ie_tipo_item_p = 'R') then
		select 	max(nr_prescricao)
		into STRICT	nr_prescricao_w
		from	prescr_recomendacao
		where	nr_seq_rec_cpoe = nr_seq_cpoe_p;
	elsif (ie_tipo_item_p = 'D') then
		select 	max(nr_prescricao)
		into STRICT	nr_prescricao_w
		from	prescr_solucao
		where	nr_seq_dialise_cpoe = nr_seq_cpoe_p;
	elsif (ie_tipo_item_p = 'J') then
		select 	max(nr_prescricao)
		into STRICT	nr_prescricao_w
		from	rep_jejum
		where	nr_seq_dieta_cpoe = nr_seq_cpoe_p;
	elsif (ie_tipo_item_p in ('DO')) then
		select 	max(nr_prescricao)
		into STRICT	nr_prescricao_w
		from	prescr_dieta
		where	nr_seq_dieta_cpoe = nr_seq_cpoe_p;
	end if;
else
	nr_prescricao_w := 0;
end if;

return nr_prescricao_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_nr_prescr_seq_cpoe (ie_tipo_item_p text, nr_seq_cpoe_p bigint) FROM PUBLIC;

