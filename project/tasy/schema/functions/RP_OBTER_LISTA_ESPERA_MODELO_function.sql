-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';



--
-- dblink wrapper to call function rp_obter_lista_espera_modelo as an autonomous transaction
--
CREATE EXTENSION IF NOT EXISTS dblink;

CREATE OR REPLACE FUNCTION rp_obter_lista_espera_modelo (nr_seq_modelo_p bigint) RETURNS varchar AS $body$
DECLARE
	-- Change this to reflect the dblink connection string
	v_conn_str  text := format('port=%s dbname=%s user=%s', current_setting('port'), current_database(), current_user);
	v_query     text;

	v_ret	varchar;
BEGIN
	v_query := 'SELECT * FROM rp_obter_lista_espera_modelo_atx ( ' || quote_nullable(nr_seq_modelo_p) || ' )';
	SELECT * INTO v_ret FROM dblink(v_conn_str, v_query) AS p (ret varchar);
	RETURN v_ret;

END;
$body$ LANGUAGE plpgsql SECURITY DEFINER;




CREATE OR REPLACE FUNCTION rp_obter_lista_espera_modelo_atx (nr_seq_modelo_p bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(255);
BEGIN

ds_retorno_w := 0;
if (nr_seq_modelo_p IS NOT NULL AND nr_seq_modelo_p::text <> '') and (nr_seq_modelo_p > 0) then
	select	count(*)
	into STRICT	ds_retorno_w
	from	rp_lista_espera_modelo
	where	ie_status = 'A'
	and	nr_seq_modelo = nr_seq_modelo_p;
end if;


return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION rp_obter_lista_espera_modelo (nr_seq_modelo_p bigint) FROM PUBLIC; -- REVOKE ALL ON FUNCTION rp_obter_lista_espera_modelo_atx (nr_seq_modelo_p bigint) FROM PUBLIC;

