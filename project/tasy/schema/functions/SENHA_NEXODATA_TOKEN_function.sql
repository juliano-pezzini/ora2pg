-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION senha_nexodata_token () RETURNS varchar AS $body$
DECLARE

DS_RETORNO_W    varchar(200);


BEGIN

SELECT 	MAX(DS_SENHA) INTO STRICT DS_RETORNO_W
FROM 	CREDENCIAIS_INTEGRACAO
WHERE 	IE_SISTEMA = 'NEXODATAREC'
AND	(DS_ENDERECO IS NOT NULL AND DS_ENDERECO::text <> '')
AND 	(DS_LOGIN IS NOT NULL AND DS_LOGIN::text <> '')
AND 	(DS_SENHA IS NOT NULL AND DS_SENHA::text <> '');

RETURN DS_RETORNO_W;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION senha_nexodata_token () FROM PUBLIC;

