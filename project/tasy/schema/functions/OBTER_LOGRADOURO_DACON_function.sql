-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_logradouro_dacon ( ds_tipo_logradouro_p text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w		varchar(20);


BEGIN
if (ds_tipo_logradouro_p = wheb_mensagem_pck.get_texto(309578)) then -- Avenida
	begin
	ds_retorno_w 	:= ds_tipo_logradouro_p;
	end;
elsif (ds_tipo_logradouro_p = wheb_mensagem_pck.get_texto(309579)) then -- Estrada
	begin
	ds_retorno_w 	:= ds_tipo_logradouro_p;
	end;
elsif (ds_tipo_logradouro_p = wheb_mensagem_pck.get_texto(309580)) then -- Rodovia
	begin
	ds_retorno_w 	:= ds_tipo_logradouro_p;
	end;

elsif (ds_tipo_logradouro_p = wheb_mensagem_pck.get_texto(309581)) then -- Rua
	begin
	ds_retorno_w 	:= ds_tipo_logradouro_p;
	end;
else
	begin
	ds_retorno_w	:= wheb_mensagem_pck.get_texto(309573); -- Outro
	end;
end if;

return ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_logradouro_dacon ( ds_tipo_logradouro_p text) FROM PUBLIC;

