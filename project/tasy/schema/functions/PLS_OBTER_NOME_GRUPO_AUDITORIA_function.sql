-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_nome_grupo_auditoria ( nr_seq_grupo_auditoria_p bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w		varchar(120);
nr_seq_grupo_w		bigint;


BEGIN

select	nr_seq_grupo
into STRICT	nr_seq_grupo_w
from	PLS_AUDITORIA_GRUPO
where	nr_sequencia = nr_seq_grupo_auditoria_p;

select	coalesce(max(nm_grupo_auditor),'')
into STRICT	ds_retorno_w
from	pls_grupo_auditor
where	nr_sequencia	= nr_seq_grupo_w;


return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_nome_grupo_auditoria ( nr_seq_grupo_auditoria_p bigint) FROM PUBLIC;

