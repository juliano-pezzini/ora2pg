-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_dispositivo_solucao ( cd_setor_atendimento_p bigint, cd_perfil_p bigint) RETURNS varchar AS $body$
DECLARE


ie_bomba_infusao_w	varchar(5);

c01 CURSOR FOR
SELECT	ie_bomba_infusao
from	rep_dispositivo_solucao
where	((coalesce(cd_setor_atendimento::text, '') = '') or (cd_setor_atendimento = cd_setor_atendimento_p))
and	((coalesce(cd_perfil::text, '') = '') or (cd_perfil = cd_perfil_p))
order by coalesce(cd_setor_atendimento,0),
	coalesce(cd_perfil,0);


BEGIN

open C01;
loop
fetch C01 into
	ie_bomba_infusao_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	ie_bomba_infusao_w	:= ie_bomba_infusao_w;
end loop;
close C01;

return	ie_bomba_infusao_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_dispositivo_solucao ( cd_setor_atendimento_p bigint, cd_perfil_p bigint) FROM PUBLIC;

