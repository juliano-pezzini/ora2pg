-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_tipo_convenio (cd_convenio_p bigint) RETURNS bigint AS $body$
DECLARE


cd_tipo_convenio_w	smallint;


BEGIN
if (coalesce(cd_convenio_p,0) > 0) then
	select	max(ie_tipo_convenio)
	into STRICT	cd_tipo_convenio_w
	from	convenio
	where 	cd_convenio	= cd_convenio_p;
end if;

return cd_tipo_convenio_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_tipo_convenio (cd_convenio_p bigint) FROM PUBLIC;

