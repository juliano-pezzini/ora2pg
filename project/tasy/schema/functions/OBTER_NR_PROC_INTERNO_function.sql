-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_nr_proc_interno (cd_procedimento_p bigint, ie_origem_proced_p text) RETURNS varchar AS $body$
DECLARE


nr_proc_interno_w procedimento.nr_proc_interno%type;


BEGIN

select max(nr_proc_interno)
into STRICT   nr_proc_interno_w
from   procedimento
where  cd_procedimento = cd_procedimento_p
and    ie_origem_proced =  coalesce(ie_origem_proced_p,ie_origem_proced);

return	nr_proc_interno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_nr_proc_interno (cd_procedimento_p bigint, ie_origem_proced_p text) FROM PUBLIC;

