-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_dose_npt_adult_md ( ds_unidade_medida_usu_p text, ds_unidade_medida_p text, qt_volume_p bigint, qt_conversao_ml_p bigint ) RETURNS bigint AS $body$
DECLARE

  qt_dose_w bigint;

BEGIN

  if (ds_unidade_medida_usu_p = ds_unidade_medida_p) then
    qt_dose_w := qt_volume_p;
  else
    qt_dose_w := coalesce(qt_volume_p, 0) * coalesce(qt_conversao_ml_p, 0);
  end if;

  return qt_dose_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_dose_npt_adult_md ( ds_unidade_medida_usu_p text, ds_unidade_medida_p text, qt_volume_p bigint, qt_conversao_ml_p bigint ) FROM PUBLIC;

