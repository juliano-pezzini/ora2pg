-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_qtd_atend_benef_mes ( dt_mes_referencia_p timestamp) RETURNS bigint AS $body$
DECLARE


qt_retorno_w			double precision	:= 0;


BEGIN

select	count(*)
into STRICT	qt_retorno_w
from (SELECT	b.nr_seq_segurado,
		b.cd_guia,
		count(*)
	from	pls_conta		b,
		pls_protocolo_conta	a
	where	a.nr_sequencia		= b.nr_seq_protocolo
	and	trunc(a.dt_mes_competencia, 'month') = trunc(dt_mes_referencia_p,'month')
	and	(b.nr_seq_segurado IS NOT NULL AND b.nr_seq_segurado::text <> '')
	group by b.nr_seq_segurado, b.cd_guia) alias5;

return	qt_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_qtd_atend_benef_mes ( dt_mes_referencia_p timestamp) FROM PUBLIC;

