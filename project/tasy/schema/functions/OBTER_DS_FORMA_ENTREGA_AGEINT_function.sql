-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_ds_forma_entrega_ageint (nr_seq_ageint_p bigint) RETURNS varchar AS $body$
DECLARE


nr_seq_forma_entrega_w	bigint;
ds_retorno_w		varchar(255);


BEGIN

if (nr_seq_ageint_p IS NOT NULL AND nr_seq_ageint_p::text <> '') then

	select	max(nr_seq_forma_laudo)
	into STRICT	nr_seq_forma_entrega_w
	from	agenda_integrada
	where	nr_sequencia = nr_seq_ageint_p;

	if (nr_seq_forma_entrega_w IS NOT NULL AND nr_seq_forma_entrega_w::text <> '') then

		select	max(ds_forma)
		into STRICT	ds_retorno_w
		from	forma_entrega_laudo
		where	nr_sequencia = nr_seq_forma_entrega_w;

	end if;

end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_ds_forma_entrega_ageint (nr_seq_ageint_p bigint) FROM PUBLIC;

