-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_vias_admins (nr_seq_atendimento_p bigint) RETURNS varchar AS $body$
DECLARE


ds_vias_aplicacao	varchar(250);
ds_via_aplicacao	varchar(5);

c01 CURSOR FOR
SELECT 	distinct ie_via_aplicacao
from 	paciente_atend_medic
where 	nr_seq_atendimento = nr_seq_atendimento_p
and 	(ie_via_aplicacao IS NOT NULL AND ie_via_aplicacao::text <> '')
order by 1;


BEGIN
open c01;
loop
fetch c01 into
	ds_via_aplicacao;
EXIT WHEN NOT FOUND; /* apply on c01 */
	begin
	if (coalesce(ds_vias_aplicacao::text, '') = '') then
		ds_vias_aplicacao := ds_via_aplicacao;
	else
		ds_vias_aplicacao := ds_vias_aplicacao || ', ' || ds_via_aplicacao;
	end if;
	end;
end loop;
close C01;

return	ds_vias_aplicacao;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_vias_admins (nr_seq_atendimento_p bigint) FROM PUBLIC;

