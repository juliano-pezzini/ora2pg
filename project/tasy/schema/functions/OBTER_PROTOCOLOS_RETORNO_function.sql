-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_protocolos_retorno ( cd_estabelecimento_p bigint, cd_convenio_p bigint, nr_seq_retorno_p bigint) RETURNS varchar AS $body$
DECLARE


nr_seq_protocolo_w	bigint;
nr_seq_prot_adic_w	convenio_retorno.nr_seq_prot_adic%type;
ds_protocolos_w		varchar(2000) := '';

c01 CURSOR FOR
	SELECT	coalesce(nr_seq_protocolo,0),
		coalesce(nr_seq_prot_adic,'0')
	from	convenio_retorno
	where cd_estabelecimento = cd_estabelecimento_p
	  and cd_convenio	 = cd_convenio_p
	  and ((nr_seq_protocolo IS NOT NULL AND nr_seq_protocolo::text <> '') or (nr_seq_prot_adic IS NOT NULL AND nr_seq_prot_adic::text <> ''))
	  and CASE WHEN coalesce(nr_seq_retorno_p,0)=0 THEN nr_sequencia  ELSE nr_seq_retorno_p END  = nr_sequencia
	order by nr_sequencia desc;


BEGIN

open c01;
loop
	fetch c01 into	nr_seq_protocolo_w,
			nr_seq_prot_adic_w;
	EXIT WHEN NOT FOUND; /* apply on c01 */

	ds_protocolos_w := substr(ds_protocolos_w || ';' || nr_seq_protocolo_w || ';' || nr_seq_prot_adic_w,1,1990);

end loop;
close c01;

ds_protocolos_w := ds_protocolos_w || ';';

RETURN ds_protocolos_w;

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_protocolos_retorno ( cd_estabelecimento_p bigint, cd_convenio_p bigint, nr_seq_retorno_p bigint) FROM PUBLIC;

