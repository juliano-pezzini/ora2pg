-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION cpoe_obter_duracao_padrao ( nr_atendimento_p cpoe_procedimento.nr_atendimento%type, nr_seq_proc_interno_p cpoe_procedimento.nr_seq_proc_interno%type) RETURNS varchar AS $body$
DECLARE

					 
cd_setor_atendimento_w	setor_atendimento.cd_setor_atendimento%type;
ie_duracao_w			exame_regra_cpoe.ie_duracao%type;
					
c01 CURSOR FOR 
SELECT	ie_duracao 
from	exame_regra_cpoe 
where	nr_seq_proc_interno = nr_seq_proc_interno_p 
and		coalesce(cd_setor_atendimento,cd_setor_atendimento_w) = cd_setor_atendimento_w 
and		coalesce(ie_duracao,'X') <> 'X' 
order 	by	coalesce(cd_setor_atendimento,0);				

BEGIN 
cd_setor_atendimento_w := obter_setor_atendimento(nr_atendimento_p);
 
open c01;
loop 
fetch c01 into ie_duracao_w;
EXIT WHEN NOT FOUND; /* apply on c01 */
end loop;
close c01;
 
return ie_duracao_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION cpoe_obter_duracao_padrao ( nr_atendimento_p cpoe_procedimento.nr_atendimento%type, nr_seq_proc_interno_p cpoe_procedimento.nr_seq_proc_interno%type) FROM PUBLIC;

