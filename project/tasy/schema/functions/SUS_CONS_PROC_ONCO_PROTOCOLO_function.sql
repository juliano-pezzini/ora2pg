-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION sus_cons_proc_onco_protocolo ( nr_interno_conta_p conta_paciente.nr_interno_conta%type, nr_seq_protocolo_p protocolo_convenio.nr_seq_protocolo%type, cd_estabelecimento_p estabelecimento.cd_estabelecimento%type) RETURNS ATENDIMENTO_PACIENTE.NR_ATENDIMENTO%TYPE AS $body$
DECLARE

				 
cd_pessoa_fisica_w		pessoa_fisica.cd_pessoa_fisica%type := '0';
nr_atendimento_w			atendimento_paciente.nr_atendimento%type := 0;
ie_consist_proc_onco_prot_w		sus_parametros_aih.ie_consist_proc_onco_prot%type := 'N';


BEGIN 
 
begin 
select	coalesce(ie_consist_proc_onco_prot,'N') 
into STRICT	ie_consist_proc_onco_prot_w 
from	sus_parametros_aih 
where	cd_estabelecimento = cd_estabelecimento_p  LIMIT 1;
exception 
when others then 
	ie_consist_proc_onco_prot_w := 'N';
end;
 
if (ie_consist_proc_onco_prot_w = 'S') then 
	begin 
	 
	begin 
	select	coalesce(b.cd_pessoa_fisica,'0') 
	into STRICT	cd_pessoa_fisica_w 
	from	conta_paciente a, 
		atendimento_paciente b, 
		procedimento_paciente c 
	where	a.nr_atendimento = b.nr_atendimento 
	and	a.nr_interno_conta = c.nr_interno_conta 
	and	substr(sus_obter_estrut_proc(c.cd_procedimento,c.ie_origem_proced,'C','S'),1,3) = '416' 
	and	a.nr_interno_conta = nr_interno_conta_p  LIMIT 1;
	exception 
	when others then 
		cd_pessoa_fisica_w := '0';
	end;
	 
	if (cd_pessoa_fisica_w <> '0') then 
		begin 
		 
		begin 
		select	b.nr_atendimento 
		into STRICT	nr_atendimento_w 
		from	conta_paciente a, 
			atendimento_paciente b, 
			procedimento_paciente c 
		where	a.nr_atendimento = b.nr_atendimento 
		and	a.nr_interno_conta = c.nr_interno_conta 
		and	substr(sus_obter_estrut_proc(c.cd_procedimento,c.ie_origem_proced,'C','S'),1,3) = '416' 
		and	b.cd_pessoa_fisica = cd_pessoa_fisica_w 
		and	a.nr_seq_protocolo = nr_seq_protocolo_p  LIMIT 1;	
		exception 
		when others then 
			nr_atendimento_w := 0;
		end;
	 
		end;
	end if;
	 
	if (nr_atendimento_w = 0) then 
		begin 
		 
		begin 
		select	coalesce(b.cd_pessoa_fisica,'0') 
		into STRICT	cd_pessoa_fisica_w 
		from	conta_paciente a, 
			atendimento_paciente b 
		where	a.nr_atendimento = b.nr_atendimento 
		and	Sus_Obter_Proc_Aih_Unif(a.nr_interno_conta,2,'C') = '415020050' 
		and	a.nr_interno_conta = nr_interno_conta_p  LIMIT 1;
		exception 
		when others then 
			cd_pessoa_fisica_w := '0';
		end;
	 
		if (cd_pessoa_fisica_w <> '0') then 
			begin 
			 
			begin 
			select	b.nr_atendimento 
			into STRICT	nr_atendimento_w 
			from	conta_paciente a, 
				atendimento_paciente b 
			where	a.nr_atendimento = b.nr_atendimento 
			and	Sus_Obter_Proc_Aih_Unif(a.nr_interno_conta,2,'C') = '415020050' 
			and	b.cd_pessoa_fisica = cd_pessoa_fisica_w 
			and	a.nr_seq_protocolo = nr_seq_protocolo_p  LIMIT 1;	
			exception 
			when others then 
				nr_atendimento_w := 0;
			end;
	 
			end;
		end if;
		 
		end;
	end if;
	 
	end;
end if;
 
return coalesce(nr_atendimento_w,0);
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION sus_cons_proc_onco_protocolo ( nr_interno_conta_p conta_paciente.nr_interno_conta%type, nr_seq_protocolo_p protocolo_convenio.nr_seq_protocolo%type, cd_estabelecimento_p estabelecimento.cd_estabelecimento%type) FROM PUBLIC;

