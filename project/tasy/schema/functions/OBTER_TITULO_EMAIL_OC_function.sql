-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_titulo_email_oc ( nr_ordem_compra_p bigint, ie_opcao_p bigint) RETURNS varchar AS $body$
DECLARE

 
/*ie_opcao 
0        Ordem de compra + Nr ordem 
1        Ordem de compra + Nr ordem + Nm fornecedor*/
 
 
ds_fornecedor_w			varchar(100);
ds_retorno_w			varchar(255);
	

BEGIN 
 
select	substr(obter_nome_pf_pj(cd_pessoa_fisica, cd_cgc_fornecedor),1,100) 
into STRICT	ds_fornecedor_w 
from	ordem_compra 
where	nr_ordem_compra = nr_ordem_compra_p;
 
if (ie_opcao_p = 0) then 
	ds_retorno_w := substr( wheb_mensagem_pck.get_texto(298461) || ' ' || nr_ordem_compra_p,1,255);
elsif (ie_opcao_p = 1) then 
	ds_retorno_w := substr( wheb_mensagem_pck.get_texto(298461) || ' ' || nr_ordem_compra_p || ' - ' || ds_fornecedor_w,1,255);
end if;
 
return	ds_retorno_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_titulo_email_oc ( nr_ordem_compra_p bigint, ie_opcao_p bigint) FROM PUBLIC;

