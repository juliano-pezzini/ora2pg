-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_uf_ibge (cd_municipio_ibge_p text) RETURNS varchar AS $body$
DECLARE


ds_uf_w	varchar(2);


BEGIN
if (cd_municipio_ibge_p IS NOT NULL AND cd_municipio_ibge_p::text <> '') then

	select	max(ds_unidade_federacao)
	into STRICT	ds_uf_w
	from	sus_municipio
	where	cd_municipio_ibge = cd_municipio_ibge_p;

end if;

return ds_uf_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_uf_ibge (cd_municipio_ibge_p text) FROM PUBLIC;

