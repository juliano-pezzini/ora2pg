-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_valor_dominio_status_lis (vl_dominio_p text) RETURNS varchar AS $body$
DECLARE


ds_valor_dominio_w varchar(255) := null;


BEGIN

if (vl_dominio_p IS NOT NULL AND vl_dominio_p::text <> '') then
	begin

	select substr(	coalesce(ds_valor_dominio_cliente,
			coalesce((	select	max(w.ds_status)
				from 	regra_exame_status w
				where	w.ie_status_regra = vl_dominio
				and	coalesce(w.ie_situacao, 'A') = 'A'),
			obter_desc_expressao(cd_exp_valor_dominio, ds_valor_dominio))), 1, 255)
		into STRICT 	ds_valor_dominio_w
		from 	valor_dominio
		where 	cd_dominio	= 1030
		and 	vl_dominio	= vl_dominio_p
		and 	coalesce(ie_situacao, 'A') = 'A';
	exception when others then
		ds_valor_dominio_w := null;
	end;
end if;

return ds_valor_dominio_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_valor_dominio_status_lis (vl_dominio_p text) FROM PUBLIC;

