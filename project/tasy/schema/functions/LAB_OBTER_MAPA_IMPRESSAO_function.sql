-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION lab_obter_mapa_impressao ( nr_seq_imp_mapa_p bigint) RETURNS varchar AS $body$
DECLARE

cd_mapa_w	bigint;

BEGIN

select	cd_mapa
into STRICT	cd_mapa_w
from	lab_impressao_mapa
where	nr_sequencia = nr_seq_imp_mapa_p;

if (coalesce(cd_mapa_w,0) > 0) then
	return	cd_mapa_w;
else
	return	'';
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION lab_obter_mapa_impressao ( nr_seq_imp_mapa_p bigint) FROM PUBLIC;

