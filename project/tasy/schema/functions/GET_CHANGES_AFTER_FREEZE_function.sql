-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION get_changes_after_freeze ( nr_sequencia_p bigint) RETURNS varchar AS $body$
DECLARE


qt_return_w	bigint;


BEGIN

  if (nr_sequencia_p IS NOT NULL AND nr_sequencia_p::text <> '')then
    select count(nr_sequencia)
    into STRICT qt_return_w
    from agenda_pac_frozen_log
    where nr_seq_agenda = nr_sequencia_p;
  end if;
  
  if (qt_return_w > 0) then
    return 'S';
  else
    return 'N';
  end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION get_changes_after_freeze ( nr_sequencia_p bigint) FROM PUBLIC;

