-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_nm_local_estoque (cd_local_estoque_p text) RETURNS varchar AS $body$
DECLARE


ds_local_estoque_w	local_estoque.ds_local_estoque%type;

BEGIN
if (cd_local_estoque_p IS NOT NULL AND cd_local_estoque_p::text <> '') then
	begin

	select max(ds_local_estoque)
	into STRICT ds_local_estoque_w
	from local_estoque
	where cd_local_estoque = cd_local_estoque_p;

	end;
end if;

return	ds_local_estoque_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_nm_local_estoque (cd_local_estoque_p text) FROM PUBLIC;

