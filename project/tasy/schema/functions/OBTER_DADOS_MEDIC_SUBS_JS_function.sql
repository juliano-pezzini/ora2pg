-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_dados_medic_subs_js ( nr_prescricao_p text, cd_intervalo_p text, cd_material_p bigint, nm_usuario_p text) RETURNS varchar AS $body$
DECLARE


nr_posicao_w	integer := 0;
nr_prescricao_w	bigint;
ds_retorno_w	varchar(10) := '';


BEGIN

select	position(',' in nr_prescricao_p)
into STRICT	nr_posicao_w
;

if (nr_posicao_w > 0) then
	nr_prescricao_w := Obter_Max_NrPrescricao(nr_prescricao_p);
else
	nr_prescricao_w := (nr_prescricao_p)::numeric;
end if;

ds_retorno_w := obter_primeiro_horario_subs(cd_intervalo_p, nr_prescricao_w, cd_material_p);

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_dados_medic_subs_js ( nr_prescricao_p text, cd_intervalo_p text, cd_material_p bigint, nm_usuario_p text) FROM PUBLIC;

