-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_dializador_extra ( nr_dializador_p bigint, cd_pessoa_fisica_p text) RETURNS varchar AS $body$
DECLARE


nr_dializador_w		bigint;
ds_retorno_w		varchar(1);


BEGIN


select	min(nr_dializador)
into STRICT	nr_dializador_w
from	hd_dializador
where	ie_status <> 'D'
and	cd_pessoa_fisica 	= cd_pessoa_fisica_p;


if (nr_dializador_w <> nr_dializador_p) then
	ds_retorno_w := 'S';
else
	ds_retorno_w := 'N';
end if;


return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_dializador_extra ( nr_dializador_p bigint, cd_pessoa_fisica_p text) FROM PUBLIC;

