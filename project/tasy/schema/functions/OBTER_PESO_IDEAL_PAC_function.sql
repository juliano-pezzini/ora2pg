-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_peso_ideal_pac ( cd_pessoa_fisica_p text, qt_altura_cm_p bigint ) RETURNS double precision AS $body$
DECLARE
		
    qt_peso_ideal_w	double precision := 0.0;
    ie_sexo_w		varchar(1);
    sql_w varchar(250);

BEGIN
if (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') then
	select 	max(ie_sexo)
	into STRICT 	ie_sexo_w
	from 	pessoa_fisica
	where 	cd_pessoa_fisica = cd_pessoa_fisica_p;

    begin
        sql_w := 'CALL OBTER_PESO_IDEAL_PAC_MD(:1, :2) INTO :qt_peso_ideal';
        EXECUTE sql_w USING IN qt_altura_cm_p, IN ie_sexo_w, OUT qt_peso_ideal_w;
    exception
        when others then
            qt_peso_ideal_w := null;
    end;

end if;

return qt_peso_ideal_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_peso_ideal_pac ( cd_pessoa_fisica_p text, qt_altura_cm_p bigint ) FROM PUBLIC;

