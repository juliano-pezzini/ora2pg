-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_etiqueta_barras_frac (cd_barras_frac_p text) RETURNS bigint AS $body$
DECLARE


nr_seq_etiqueta_w  prescr_mat_hor.nr_seq_etiqueta%type;
barras_w	   varchar(20);
qt_algarismo_w	   integer;


BEGIN

qt_algarismo_w := length(cd_barras_frac_p);

barras_w := substr(cd_barras_frac_p,2,qt_algarismo_w-2);

nr_seq_etiqueta_w :=  (barras_w)::numeric;

return	nr_seq_etiqueta_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_etiqueta_barras_frac (cd_barras_frac_p text) FROM PUBLIC;

