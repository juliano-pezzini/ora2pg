-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_seq_impressora_setor ( cd_setor_atendimento_p bigint, cd_perfil_p bigint) RETURNS bigint AS $body$
DECLARE


nr_seq_impressora_w		bigint;
ie_padrao_w				varchar(10);

C01 CURSOR FOR
SELECT	nr_seq_impressora,
		coalesce(ie_impressora_default,'N')
from	setor_impressora
where	cd_setor_atendimento = cd_setor_atendimento_p
and		((coalesce(cd_perfil,0) = 0) or (cd_perfil = cd_perfil_p))
order by	coalesce(nr_seq_apresentacao,9999999) desc,
			coalesce(cd_perfil,99999),
			dt_atualizacao_nrec desc;


BEGIN

open C01;
loop
fetch C01 into
	nr_seq_impressora_w,
	ie_padrao_w;
EXIT WHEN NOT FOUND or (ie_padrao_w = 'S');  /* apply on C01 */
end loop;
close C01;

return	nr_seq_impressora_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_seq_impressora_setor ( cd_setor_atendimento_p bigint, cd_perfil_p bigint) FROM PUBLIC;

