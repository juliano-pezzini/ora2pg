-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_atendimento_precaucao_dt (nr_atendimento_p bigint, ie_opcao_p text) RETURNS timestamp AS $body$
DECLARE


nr_seq_precaucao_w		bigint;
ds_precaucao_w			varchar(100);
nr_seq_atend_precaucao_w	bigint;
dt_inicio_w			timestamp;
dt_termino_w			timestamp;
ds_motivo_isolamento_w		varchar(255);


BEGIN

select	coalesce(max(nr_Sequencia),0)
into STRICT	nr_seq_atend_precaucao_w
from	atendimento_precaucao
where	nr_atendimento = nr_atendimento_p
and	(dt_liberacao IS NOT NULL AND dt_liberacao::text <> '')
and	coalesce(dt_inativacao::text, '') = ''
and	coalesce(dt_final_precaucao, clock_timestamp() + interval '1 days') > clock_timestamp();

if (nr_seq_atend_precaucao_w > 0) then
	select  dt_inicio,
		dt_termino
	into STRICT	dt_inicio_w,
		dt_termino_w
	from	atendimento_precaucao
	where	nr_sequencia = nr_seq_atend_precaucao_w;
end if;

if (upper(ie_opcao_p) = 'I') then
	return	dt_inicio_w;
elsif (upper(ie_opcao_p) = 'T') then
	return 	dt_termino_w;
end if;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_atendimento_precaucao_dt (nr_atendimento_p bigint, ie_opcao_p text) FROM PUBLIC;

