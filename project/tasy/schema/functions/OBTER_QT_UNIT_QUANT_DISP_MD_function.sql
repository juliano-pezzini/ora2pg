-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_qt_unit_quant_disp_md (ie_regra_p text, qt_unitaria_p bigint ) RETURNS bigint AS $body$
DECLARE


  qt_unitaria_w double precision;

BEGIN
  qt_unitaria_w := qt_unitaria_p;

  if (ie_regra_p = 'S') and (trunc(qt_unitaria_p,0) <> qt_unitaria_p) then
    qt_unitaria_w := trunc(qt_unitaria_p,0) + 1;
  elsif (ie_regra_p = 'I') and (trunc(qt_unitaria_p,0) <> qt_unitaria_p) then
    qt_unitaria_w := trunc(qt_unitaria_p,0);
  end if;

  return qt_unitaria_w;
   
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_qt_unit_quant_disp_md (ie_regra_p text, qt_unitaria_p bigint ) FROM PUBLIC;

