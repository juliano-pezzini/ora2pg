-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pls_obter_valor_fat_receb_a800 ( nr_seq_fatura_p bigint, ie_valor_p text) RETURNS bigint AS $body$
DECLARE


vl_retorno_w	double precision;


BEGIN

begin
if (ie_valor_p = 1) then
	select	vl_total_mensalidade
	into STRICT	vl_retorno_w
	from	ptu_fatura_pre_total
	where	nr_seq_fatura	= nr_seq_fatura_p;
elsif (ie_valor_p = 2) then
	select	vl_total_inscricao
	into STRICT	vl_retorno_w
	from	ptu_fatura_pre_total
	where	nr_seq_fatura	= nr_seq_fatura_p;
elsif (ie_valor_p = 3) then
	select	vl_total_ajuste
	into STRICT	vl_retorno_w
	from	ptu_fatura_pre_total
	where	nr_seq_fatura	= nr_seq_fatura_p;
elsif (ie_valor_p = 4) then
	select	vl_tot_mens_original
	into STRICT	vl_retorno_w
	from	ptu_fatura_pre_total
	where	nr_seq_fatura	= nr_seq_fatura_p;
elsif (ie_valor_p = 5) then
	select	vl_tot_insc_original
	into STRICT	vl_retorno_w
	from	ptu_fatura_pre_total
	where	nr_seq_fatura	= nr_seq_fatura_p;
elsif (ie_valor_p = 6) then
	select	qt_total_r802
	into STRICT	vl_retorno_w
	from	ptu_fatura_pre_total
	where	nr_seq_fatura	= nr_seq_fatura_p;
end if;
exception
when others then
	vl_retorno_w	:= 0;
end;

return	coalesce(vl_retorno_w,0);

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_obter_valor_fat_receb_a800 ( nr_seq_fatura_p bigint, ie_valor_p text) FROM PUBLIC;

