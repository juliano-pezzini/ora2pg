-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_local_lib_nf ( cd_local_estoque_p bigint, cd_operacao_nf_p bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w		varchar(1);
ie_permite_w		varchar(1);
cd_perfil_ativo_w		integer;

c01 CURSOR FOR
	SELECT	ie_permite
	from	sup_regra_local_nf
	where	cd_local_estoque			= cd_local_estoque_p
	and	coalesce(cd_perfil, cd_perfil_ativo_w)	= cd_perfil_ativo_w
	and	coalesce(cd_operacao_nf, cd_operacao_nf_p)	= cd_operacao_nf_p
	order by coalesce(cd_perfil,0);


BEGIN

cd_perfil_ativo_w	:= coalesce(obter_perfil_ativo, 0);
ds_retorno_w 	:= 'S';

open C01;
loop
fetch C01 into
	ie_permite_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	ds_retorno_w := ie_permite_w;
end loop;
close C01;

return ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_local_lib_nf ( cd_local_estoque_p bigint, cd_operacao_nf_p bigint) FROM PUBLIC;

