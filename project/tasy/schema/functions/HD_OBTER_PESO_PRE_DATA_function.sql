-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION hd_obter_peso_pre_data (cd_pessoa_fisica_p text, seq_grid_p bigint) RETURNS bigint AS $body$
DECLARE


peso_pre_w		double precision;
dt_referencia_w		timestamp;


BEGIN

select 	dt_referencia
into STRICT	dt_referencia_w
from	HD_EXAME_GRID_CONTROLE
where 	nr_sequencia = seq_grid_p;

select 	round((sum(qt_peso_pre)/count(*))::numeric,2)
into STRICT	peso_pre_w
from	hd_dialise
where	cd_pessoa_fisica = cd_pessoa_fisica_p
and	qt_peso_pre > 0
and	trunc(dt_dialise,'month') = trunc(dt_referencia_w,'month');

return peso_pre_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION hd_obter_peso_pre_data (cd_pessoa_fisica_p text, seq_grid_p bigint) FROM PUBLIC;

