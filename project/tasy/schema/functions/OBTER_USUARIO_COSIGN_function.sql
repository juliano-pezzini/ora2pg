-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_usuario_cosign (NM_USUARIO_P text, NR_SEQUENCIA_P bigint) RETURNS varchar AS $body$
DECLARE

qt_registros bigint;
 -- verifica se usuario pode assinar
BEGIN

    select
         count(*) into STRICT qt_registros
    from 
        documento_aprov_prof a
    where
        a.nr_sequencia = NR_SEQUENCIA_P
    and
        ((obter_codigo_usuario(NM_USUARIO_P) = a.cd_pessoa_fisica
    OR
        (SELECT NR_SEQ_FUNCAO_PF from PESSOA_FISICA x where x.CD_PESSOA_FISICA = (obter_codigo_usuario(NM_USUARIO_P))) = a.nr_seq_funcao_pf));

    return qt_registros;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_usuario_cosign (NM_USUARIO_P text, NR_SEQUENCIA_P bigint) FROM PUBLIC;

