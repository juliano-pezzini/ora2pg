-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_total_temp_real_os ( nr_seq_ordem_p bigint, nm_executor_p text) RETURNS bigint AS $body$
DECLARE


qt_total_prev_w		bigint;


BEGIN

	select	sum(obter_tempo_entre_datas_util(dt_atividade,dt_fim_atividade,'M'))
	into STRICT	qt_total_prev_w
	from	man_ordem_serv_ativ
	where	nr_seq_ordem_serv = nr_seq_ordem_p
	and		nm_usuario_exec = nm_executor_p;

return	qt_total_prev_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_total_temp_real_os ( nr_seq_ordem_p bigint, nm_executor_p text) FROM PUBLIC;

