-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_se_permite_prioridade ( nm_usuario_p text, nm_usuario_alt_p text) RETURNS varchar AS $body$
DECLARE


nr_seq_grupo_w	bigint;
nr_seq_grupo_ww	bigint;
ie_retorno_w	varchar(1) := 'N';

BEGIN
select	coalesce(max(nr_seq_grupo),0)
into STRICT	nr_seq_grupo_w
from	usuario_grupo_des
where	nm_usuario_grupo = nm_usuario_p;

select	coalesce(max(nr_seq_grupo),0)
into STRICT	nr_seq_grupo_ww
from	usuario_grupo_des
where	nm_usuario_grupo = nm_usuario_alt_p;

if (nr_seq_grupo_w > 0) and (nr_seq_grupo_ww > 0) and (nr_seq_grupo_w = nr_seq_grupo_ww) then
	ie_retorno_w := 'S';
end if;


return	ie_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_se_permite_prioridade ( nm_usuario_p text, nm_usuario_alt_p text) FROM PUBLIC;

