-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_dados_os_usuario ( nm_usuario_des_p text, dt_referencia_p timestamp, ie_opcao_p text) RETURNS bigint AS $body$
DECLARE


qt_retorno_w		double precision;
qt_ordens_trab_w	bigint;
cd_pessoa_fisica_w	varchar(255);


BEGIN

if (ie_opcao_p = 'QTOS') then
	select	count(distinct a.nr_sequencia)
	into STRICT	qt_retorno_w
	from	man_ordem_servico a
	where	exists (SELECT 1
			from	man_ordem_serv_ativ b
			where	a.nr_sequencia		= b.nr_seq_ordem_serv
			and	trunc(b.dt_atividade,'year') = trunc(dt_referencia_p,'year')
			and	b.nm_usuario_exec	= nm_usuario_des_p);
elsif (ie_opcao_p = 'PRDEF') then

	select	count(distinct a.nr_sequencia)
	into STRICT	qt_ordens_trab_w
	from	man_ordem_servico a
	where	exists (SELECT 1
			from	man_ordem_serv_ativ b
			where	a.nr_sequencia		= b.nr_seq_ordem_serv
			and	trunc(b.dt_atividade,'year') = trunc(dt_referencia_p,'year')
			and	b.nm_usuario_exec	= nm_usuario_des_p);

	select	max(cd_pessoa_fisica)
	into STRICT	cd_pessoa_fisica_w
	from	usuario
	where	nm_usuario = nm_usuario_des_p;

	select	count(distinct a.nr_sequencia)
	into STRICT	qt_retorno_w
	from	man_doc_erro a,
		man_ordem_servico b
	where	b.nr_sequencia		= a.nr_seq_ordem
	and	(a.dt_liberacao IS NOT NULL AND a.dt_liberacao::text <> '')
	and	trunc(b.dt_ordem_servico,'year') = trunc(dt_referencia_p,'year')
	and	a.cd_pessoa_fisica	= cd_pessoa_fisica_w
	and	a.ie_origem_erro	= 'W'
	and	coalesce(a.ie_recorrente,'N') = 'N';

	qt_retorno_w	:= dividir(qt_retorno_w *100, qt_ordens_trab_w);

elsif (ie_opcao_p = 'QTDEF') then

	select	max(cd_pessoa_fisica)
	into STRICT	cd_pessoa_fisica_w
	from	usuario
	where	nm_usuario = nm_usuario_des_p;

	select	count(distinct a.nr_sequencia)
	into STRICT	qt_retorno_w
	from	man_doc_erro a,
		man_ordem_servico b
	where	b.nr_sequencia		= a.nr_seq_ordem
	and	(a.dt_liberacao IS NOT NULL AND a.dt_liberacao::text <> '')
	and	trunc(b.dt_ordem_servico,'year') = trunc(dt_referencia_p,'year')
	and	a.cd_pessoa_fisica	= cd_pessoa_fisica_w
	and	a.ie_origem_erro	= 'W'
	and	coalesce(a.ie_recorrente,'N') = 'N';
end if;

return	qt_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_dados_os_usuario ( nm_usuario_des_p text, dt_referencia_p timestamp, ie_opcao_p text) FROM PUBLIC;

