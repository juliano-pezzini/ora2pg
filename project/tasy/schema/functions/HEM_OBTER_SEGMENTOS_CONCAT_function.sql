-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION hem_obter_segmentos_concat (nr_seq_coron_p bigint) RETURNS varchar AS $body$
DECLARE


ds_retorno_w		varchar(2000) := '';
cd_segmento_w		hem_segmento_princ.cd_segmento%type;

C01 CURSOR FOR
	SELECT	b.cd_segmento
	FROM	hem_coron_localizacao a,
		hem_segmento_princ b
	WHERE	a.nr_seq_coron =  nr_seq_coron_p
	AND	b.nr_sequencia = a.nr_seq_segmento;


BEGIN

open C01;
loop
fetch C01 into
	cd_segmento_w;
EXIT WHEN NOT FOUND; /* apply on C01 */
	begin

	ds_retorno_w := ds_retorno_w||cd_segmento_w||'-';

	end;
end loop;
close C01;

if (ds_retorno_w IS NOT NULL AND ds_retorno_w::text <> '') then
	ds_retorno_w := substr(ds_retorno_w,1,length(ds_retorno_w)-1);
end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION hem_obter_segmentos_concat (nr_seq_coron_p bigint) FROM PUBLIC;

