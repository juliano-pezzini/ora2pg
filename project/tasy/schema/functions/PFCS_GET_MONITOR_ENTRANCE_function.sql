-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION pfcs_get_monitor_entrance (nr_seq_device_p bigint) RETURNS timestamp AS $body$
DECLARE

	dt_monitor_entrance_w		pfcs_device_note.dt_time%type;


BEGIN
	select	dt_time
	  into STRICT	dt_monitor_entrance_w
	  from (SELECT	note.dt_time
			   from	pfcs_device_note note
			  where	note.nr_seq_device = nr_seq_device_p
				and	note.ds_note = 'Association'
			order by note.dt_time desc) alias0 LIMIT 1;

	return dt_monitor_entrance_w;

	exception
		-- If there are no records found
		when no_data_found then
		  return null;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pfcs_get_monitor_entrance (nr_seq_device_p bigint) FROM PUBLIC;

