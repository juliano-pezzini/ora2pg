-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_count_dados_classif ( nr_seq_escala_p bigint, cd_pessoa_fisica_p text) RETURNS bigint AS $body$
DECLARE


qt_existe_w	bigint;


BEGIN

if (coalesce(nr_seq_escala_p,0) > 0) then
	select	count(*)
	into STRICT	qt_existe_w
	from	escala_diaria_adic
	where	nr_seq_escala_diaria = nr_seq_escala_p
	and	cd_pessoa_fisica = cd_pessoa_fisica_p;
end if;

return	qt_existe_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_count_dados_classif ( nr_seq_escala_p bigint, cd_pessoa_fisica_p text) FROM PUBLIC;

