-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_ds_reconstituir_md (ds_reconstituir_p text, ds_diluir_p text, ds_rediluir_p text, ds_aplicar_p text) RETURNS varchar AS $body$
DECLARE


  ds_retorno_w varchar(500);

BEGIN

  ds_retorno_w := ds_reconstituir_p || ds_diluir_p || ds_rediluir_p ||
                  ds_aplicar_p;
  ds_retorno_w := replace(ds_retorno_w, ' ,', ' 0,');
  ds_retorno_w := replace(ds_retorno_w, ' .', ' 0,');

  return ds_retorno_w;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_ds_reconstituir_md (ds_reconstituir_p text, ds_diluir_p text, ds_rediluir_p text, ds_aplicar_p text) FROM PUBLIC;

