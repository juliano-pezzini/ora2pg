-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION rp_obter_tipo_evolucao_param (cd_estabelecimento_p text, cd_perfil_ativo_p text) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(255);
nr_sequencia_w	bigint;


BEGIN
ds_retorno_w := '';

select	max(nr_sequencia)
into STRICT	nr_sequencia_w
from	rp_parametros
where	coalesce(cd_estabelecimento,cd_estabelecimento_p) = cd_estabelecimento_p
and	coalesce(cd_perfil,cd_perfil_ativo_p) = cd_perfil_ativo_p;

if (nr_sequencia_w IS NOT NULL AND nr_sequencia_w::text <> '') then
	select 	cd_tipo_evolucao
	into STRICT	ds_retorno_w
	from	rp_parametros
	where	nr_sequencia = nr_sequencia_w;
end if;

return	ds_retorno_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION rp_obter_tipo_evolucao_param (cd_estabelecimento_p text, cd_perfil_ativo_p text) FROM PUBLIC;

