-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_ultima_data_orcamento (cd_pessoa_fisica_p text) RETURNS timestamp AS $body$
DECLARE

dt_orcamento_w			timestamp;
				

BEGIN

if (cd_pessoa_fisica_p IS NOT NULL AND cd_pessoa_fisica_p::text <> '') then
   select 	max(dt_orcamento)
   into STRICT	dt_orcamento_w
   from	orcamento_paciente
   where	cd_pessoa_fisica = cd_pessoa_fisica_p;
end if;

return	dt_orcamento_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_ultima_data_orcamento (cd_pessoa_fisica_p text) FROM PUBLIC;

