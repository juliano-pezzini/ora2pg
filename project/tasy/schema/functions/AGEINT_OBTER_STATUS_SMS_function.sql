-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION ageint_obter_status_sms (dt_contato_p timestamp, ie_status_agenda_p text, ie_transf_p text) RETURNS varchar AS $body$
DECLARE


ds_status_w		varchar(80);


BEGIN

if (dt_contato_p IS NOT NULL AND dt_contato_p::text <> '') and (ie_status_agenda_p <> 'C') and (ie_transf_p = 'N') then
		ds_status_w :=	obter_desc_expressao(608472);
elsif (ie_status_agenda_p = 'C') and (ie_transf_p = 'N') then
		ds_status_w :=	obter_desc_expressao(322155);
elsif (ie_transf_p = 'S') then
		ds_status_w :=	obter_desc_expressao(595194);
else
		ds_status_w :=	obter_desc_expressao(294073);
end if;

return	ds_status_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION ageint_obter_status_sms (dt_contato_p timestamp, ie_status_agenda_p text, ie_transf_p text) FROM PUBLIC;

