-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION get_waiting_list_chemotherapy ( cd_perfil_p bigint, nm_usuario_p text, cd_estabelecimento_p text, nr_seq_atendimento_p bigint, dt_real_p timestamp, dt_prevista_p timestamp ) RETURNS varchar AS $body$
DECLARE

    vl_param_obtido   varchar(2) := '';
    vl_param_10       varchar(2) := '';

BEGIN
    vl_param_10 := coalesce(obter_valor_param_usuario(380, 10, cd_perfil_p, nm_usuario_p, cd_estabelecimento_p), 'S');

    if ( vl_param_10 = 'S' ) then
        vl_param_obtido := qt_obter_se_dia_agendado(nr_seq_atendimento_p, coalesce(dt_real_p, dt_prevista_p));
    else
        vl_param_obtido := qt_obter_se_agendado(nr_seq_atendimento_p, coalesce(dt_real_p, dt_prevista_p));
    end if;

    return vl_param_obtido;
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION get_waiting_list_chemotherapy ( cd_perfil_p bigint, nm_usuario_p text, cd_estabelecimento_p text, nr_seq_atendimento_p bigint, dt_real_p timestamp, dt_prevista_p timestamp ) FROM PUBLIC;

