-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION san_obter_abreviacao_hemocomp ( nr_seq_derivado_p bigint, ie_opcao_p text) RETURNS varchar AS $body$
DECLARE

i smallint;
k smallint;
/*
ie_opcao_p - Para futuras customizações
*/
ds_deriv_temp_w		varchar(255);
ds_derivado_abreviado_w varchar(50);


BEGIN

select	initcap(obter_desc_san_derivado(nr_seq_derivado_p))
into STRICT		ds_deriv_temp_w
;

k:=position(' ' in ds_deriv_temp_w);

ds_derivado_abreviado_w := substr(ds_deriv_temp_w,1,1);

while length(ds_deriv_temp_w)	 > 0
loop
	if (substr(ds_deriv_temp_w,1,1) = ' ') then
			ds_derivado_abreviado_w := ds_derivado_abreviado_w ||substr(ds_deriv_temp_w,2,1);
	end if;
	ds_deriv_temp_w:= substr(ds_deriv_temp_w,2,length(ds_deriv_temp_w));
	end loop;

return	ds_derivado_abreviado_w;

end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION san_obter_abreviacao_hemocomp ( nr_seq_derivado_p bigint, ie_opcao_p text) FROM PUBLIC;

