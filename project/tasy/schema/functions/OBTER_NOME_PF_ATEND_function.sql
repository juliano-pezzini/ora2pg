-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';





CREATE OR REPLACE FUNCTION obter_nome_pf_atend (nr_atendimento_p bigint) RETURNS varchar AS $body$
DECLARE

			 
cd_pessoa_fisica_w	varchar(10);
nm_pessoa_fisica_w	varchar(60);	

BEGIN 
 
nm_pessoa_fisica_w := '';
 
if (coalesce(nr_atendimento_p,0) > 0) then 
 
	select	cd_pessoa_fisica 
	into STRICT	cd_pessoa_fisica_w 
	from	atendimento_paciente 
	where	nr_atendimento = nr_atendimento_p;
 
	if (cd_pessoa_fisica_w IS NOT NULL AND cd_pessoa_fisica_w::text <> '') then 
		nm_pessoa_fisica_w := SUBSTR(OBTER_NOME_PF(cd_pessoa_fisica_w), 0, 60);
	end if;
 
end if;
 
return	nm_pessoa_fisica_w;
 
end;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION obter_nome_pf_atend (nr_atendimento_p bigint) FROM PUBLIC;

