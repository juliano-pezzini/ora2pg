-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

CREATE OR REPLACE VIEW vime_adep_v (ds_data, cd, cd_status, ie_ordem, nm_usuario, dt_medicacao) AS SELECT  '00' ds_data,
		DS_HORA_00 cd,
		SUBSTR(DS_HORA_00, LENGTH(DS_HORA_00), 1) cd_status,
		0 ie_ordem,
		nm_usuario,
		dt_medicacao
FROM    vime_adep

union

SELECT  '01' ds_data,
		DS_HORA_01 cd,
		SUBSTR(DS_HORA_01, LENGTH(DS_HORA_01), 1) cd_status,
		1 ie_ordem,
		nm_usuario,
		dt_medicacao
FROM    vime_adep

union

SELECT  '02' ds_data,
		DS_HORA_02 cd,
		SUBSTR(DS_HORA_02, LENGTH(DS_HORA_02), 1) cd_status,
		2 ie_ordem,
		nm_usuario,
		dt_medicacao
FROM    vime_adep

union

SELECT  '03' ds_data,
		DS_HORA_03 cd,
		SUBSTR(DS_HORA_03, LENGTH(DS_HORA_03), 1) cd_status,
		3 ie_ordem,
		nm_usuario,
		dt_medicacao
FROM    vime_adep

union

SELECT  '04' ds_data,
		DS_HORA_04 cd,
		SUBSTR(DS_HORA_04, LENGTH(DS_HORA_04), 1) cd_status,
		4 ie_ordem,
		nm_usuario,
		dt_medicacao
FROM    vime_adep

union

SELECT  '05' ds_data,
		DS_HORA_05 cd,
		SUBSTR(DS_HORA_05, LENGTH(DS_HORA_05), 1) cd_status,
		5 ie_ordem,
		nm_usuario,
		dt_medicacao
FROM    vime_adep

union

SELECT  '06' ds_data,
		DS_HORA_06 cd,
		SUBSTR(DS_HORA_06, LENGTH(DS_HORA_06), 1) cd_status,
		6 ie_ordem,
		nm_usuario,
		dt_medicacao
FROM    vime_adep

union

SELECT  '07' ds_data,
		DS_HORA_07 cd,
		SUBSTR(DS_HORA_07, LENGTH(DS_HORA_07), 1) cd_status,
		7 ie_ordem,
		nm_usuario,
		dt_medicacao
FROM    vime_adep

union

SELECT  '08' ds_data,
		DS_HORA_08 cd,
		SUBSTR(DS_HORA_08, LENGTH(DS_HORA_08), 1) cd_status,
		8 ie_ordem,
		nm_usuario,
		dt_medicacao
FROM    vime_adep

union

SELECT  '09' ds_data,
		DS_HORA_09 cd,
		SUBSTR(DS_HORA_09, LENGTH(DS_HORA_09), 1) cd_status,
		9 ie_ordem,
		nm_usuario,
		dt_medicacao
FROM    vime_adep

union

SELECT  '10' ds_data,
		DS_HORA_10 cd,
		SUBSTR(DS_HORA_10, LENGTH(DS_HORA_10), 1) cd_status,
		10 ie_ordem,
		nm_usuario,
		dt_medicacao
FROM    vime_adep

union

SELECT  '11' ds_data,
		DS_HORA_11 cd,
		SUBSTR(DS_HORA_11, LENGTH(DS_HORA_11), 1) cd_status,
		11 ie_ordem,
		nm_usuario,
		dt_medicacao
FROM    vime_adep

union

SELECT  '12' ds_data,
		DS_HORA_12 cd,
		SUBSTR(DS_HORA_12, LENGTH(DS_HORA_12), 1) cd_status,
		12 ie_ordem,
		nm_usuario,
		dt_medicacao
FROM    vime_adep

union

SELECT  '13' ds_data,
		DS_HORA_13 cd,
		SUBSTR(DS_HORA_13, LENGTH(DS_HORA_13), 1) cd_status,
		13 ie_ordem,
		nm_usuario,
		dt_medicacao
FROM    vime_adep

union

SELECT  '14' ds_data,
		DS_HORA_14 cd,
		SUBSTR(DS_HORA_14, LENGTH(DS_HORA_14), 1) cd_status,
		14 ie_ordem,
		nm_usuario,
		dt_medicacao
FROM    vime_adep

union

SELECT  '15' ds_data,
		DS_HORA_15 cd,
		SUBSTR(DS_HORA_15, LENGTH(DS_HORA_15), 1) cd_status,
		15 ie_ordem,
		nm_usuario,
		dt_medicacao
FROM    vime_adep

union

SELECT  '16' ds_data,
		DS_HORA_16 cd,
		SUBSTR(DS_HORA_16, LENGTH(DS_HORA_16), 1) cd_status,
		16 ie_ordem,
		nm_usuario,
		dt_medicacao
FROM    vime_adep

union

SELECT  '17' ds_data,
		DS_HORA_17 cd,
		SUBSTR(DS_HORA_17, LENGTH(DS_HORA_17), 1) cd_status,
		17 ie_ordem,
		nm_usuario,
		dt_medicacao
FROM    vime_adep

union

SELECT  '18' ds_data,
		DS_HORA_18 cd,
		SUBSTR(DS_HORA_18, LENGTH(DS_HORA_18), 1) cd_status,
		18 ie_ordem,
		nm_usuario,
		dt_medicacao
FROM    vime_adep

union

SELECT  '19' ds_data,
		DS_HORA_19 cd,
		SUBSTR(DS_HORA_19, LENGTH(DS_HORA_19), 1) cd_status,
		19 ie_ordem,
		nm_usuario,
		dt_medicacao
FROM    vime_adep

union

SELECT  '20' ds_data,
		DS_HORA_20 cd,
		SUBSTR(DS_HORA_20, LENGTH(DS_HORA_20), 1) cd_status,
		20 ie_ordem,
		nm_usuario,
		dt_medicacao
FROM    vime_adep

union

SELECT  '21' ds_data,
		DS_HORA_21 cd,
		SUBSTR(DS_HORA_21, LENGTH(DS_HORA_21), 1) cd_status,
		21 ie_ordem,
		nm_usuario,
		dt_medicacao
FROM    vime_adep

union

SELECT  '22' ds_data,
		DS_HORA_22 cd,
		SUBSTR(DS_HORA_22, LENGTH(DS_HORA_22), 1) cd_status,
		22 ie_ordem,
		nm_usuario,
		dt_medicacao
FROM    vime_adep

union

SELECT  '23' ds_data,
		DS_HORA_23 cd,
		SUBSTR(DS_HORA_23, LENGTH(DS_HORA_23), 1) cd_status,
		23 ie_ordem,
		nm_usuario,
		dt_medicacao
FROM    vime_adep;

