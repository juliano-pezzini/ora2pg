-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

CREATE OR REPLACE VIEW reg_licitacao_anexos_v (nr_seq_licitacao, ds_arquivo, nr_seq_tipo_anexo, ie_envia_zip) AS select	nr_seq_licitacao,
		ds_arquivo,
		nr_seq_tipo_anexo,
		'S' ie_envia_zip
FROM	reg_lic_edital
where	ds_arquivo is not null

union

select	nr_seq_licitacao,
		ds_arquivo,
		nr_seq_tipo_anexo,
		'S'
from	reg_lic_public_edital
where	ds_arquivo is not null

union

select	nr_seq_licitacao,
		ds_arquivo,
		nr_seq_tipo_anexo,
		'S'
from	reg_lic_impug_edital
where	ds_arquivo is not null

union

select	nr_seq_licitacao,
		ds_arquivo,
		nr_seq_tipo_anexo,
		'S'
from	reg_lic_errata_edital
where	ds_arquivo is not null

union

select	nr_seq_licitacao,
		ds_arquivo,
		nr_seq_tipo_anexo,
		'S'
from	reg_lic_anexo_edital
where	ds_arquivo is not null

union

select	nr_seq_licitacao,
		nm_arquivo,
		nr_seq_tipo_anexo,
		'S'
from	reg_lic_documento_edital
where	nm_arquivo is not null

union

select	coalesce(lic_obter_licitacao_anexo_doc(nr_sequencia),0),
		ds_arquivo,
		nr_seq_tipo_anexo,
		'S'
from	pessoa_juridica_doc_anexo
where	ds_arquivo is not null
and	coalesce(lic_obter_licitacao_anexo_doc(nr_sequencia),0) > 0

union

select	nr_seq_licitacao,
		ds_arquivo,
		nr_seq_tipo_anexo,
		'S'
from	reg_lic_anexo
where	ds_arquivo is not null

union

select	nr_seq_licitacao,
		ds_arquivo,
		nr_seq_tipo_anexo,
		'S'
from	reg_lic_ata
where	ds_arquivo is not null

union

select	nr_seq_licitacao,
		ds_arquivo,
		nr_seq_tipo_anexo,
		'S'
from	reg_lic_public_ata
where	ds_arquivo is not null

union

select	nr_seq_licitacao,
		ds_arquivo,
		nr_seq_tipo_anexo,
		'S'
from	reg_lic_anexo_recurso
where	ds_arquivo is not null

union

select	nr_seq_licitacao,
		ds_arquivo,
		nr_seq_tipo_anexo,
		'S'
from	reg_lic_resultado
where	ds_arquivo is not null

union

select	nr_seq_licitacao,
		ds_arquivo,
		nr_seq_tipo_anexo,
		'S'
from	reg_lic_public_result
where	ds_arquivo is not null

union

select	nr_seq_licitacao,
		ds_arquivo,
		nr_seq_tipo_anexo,
		'S'
from	reg_lic_homologacao
where	ds_arquivo is not null

union

select	nr_seq_licitacao,
		ds_arquivo,
		nr_seq_tipo_anexo,
		'S'
from	reg_lic_homol_public
where	ds_arquivo is not null

union

select	nr_seq_licitacao,
		ds_arquivo,
		nr_seq_tipo_anexo,
	coalesce(ie_anexa_zip,'N')
from	reg_lic_anexo_encer
where	ds_arquivo is not null;

