-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

CREATE OR REPLACE VIEW kv_form_data_v (ds_kv_form, dt_atualizacao, nm_profissional, nr_atendimento, dt_liberacao) AS SELECT 'Arbeitsunfähigkeit (Muster 1)' ds_kv_form,
      pkg_date_formaters.to_varchar(dt_atualizacao,'shortDate',1,wheb_usuario_pck.get_cd_estabelecimento,wheb_usuario_pck.get_nm_usuario) dt_atualizacao,
      SUBSTR(obter_nome_pf(CD_MEDICO),1,255) nm_profissional,
	   nr_atendimento,
     dt_liberacao
FROM   ATESTADO_PACIENTE
WHERE  dt_liberacao IS NOT NULL
AND    dt_inativacao IS NULL

UNION ALL

SELECT	 'Verordnung einer Krankenbeförderung (Muster 4)' ds_kv_form,
      	 pkg_date_formaters.to_varchar(dt_atualizacao,'shortDate',1,wheb_usuario_pck.get_cd_estabelecimento,wheb_usuario_pck.get_nm_usuario) dt_atualizacao,
      	 nm_usuario nm_profissional,
	  nr_atendimento,
    dt_liberacao
FROM   	 FORMULARIO_TRANSPORTE
WHERE  	 dt_liberacao IS NOT NULL
AND    	 dt_inativacao IS NULL

UNION ALL

SELECT	 'Überweisungsschein (Muster 6)' ds_kv_from,
		 pkg_date_formaters.to_varchar(dt_atualizacao,'shortDate',1,wheb_usuario_pck.get_cd_estabelecimento,wheb_usuario_pck.get_nm_usuario) dt_atualizacao,
		 nm_usuario nm_profissional,
	     nr_atendimento,
       dt_liberacao
FROM	 CARTA_REFERENCIA
WHERE  	 dt_liberacao IS NOT NULL
AND    	 dt_inativacao IS NULL

UNION ALL

SELECT	 'Laboratoriumsuntersuchungen als Auftragsleistung (Muster 10)' ds_kv_from,
		 pkg_date_formaters.to_varchar(dt_atualizacao,'shortDate',1,wheb_usuario_pck.get_cd_estabelecimento,wheb_usuario_pck.get_nm_usuario) dt_atualizacao,
		 SUBSTR(obter_nome_usuario(nm_usuario),1,255) nm_profissional,
	  nr_atendimento,
    dt_liberacao
FROM	 FORMULARIO_EXAME_LAB
WHERE  	 dt_liberacao IS NOT NULL
AND    	 dt_inativacao IS NULL

UNION ALL

SELECT	 'Verordnung häuslicher Krankenpflege (Muster 12)' ds_kv_from,
		 pkg_date_formaters.to_varchar(dt_atualizacao,'shortDate',1,wheb_usuario_pck.get_cd_estabelecimento,wheb_usuario_pck.get_nm_usuario) dt_atualizacao,
		 SUBSTR(obter_nome_usuario(nm_usuario),1,255) nm_profissional,
	     nr_atendimento,
       dt_liberacao
FROM	 FORMULARIO_HOME_CARE
WHERE  	 dt_liberacao IS NOT NULL
AND    	 dt_inativacao IS NULL

UNION ALL

SELECT	 'Physikalische/Podologische Therapie (Muster 13)' ds_kv_from,
		 pkg_date_formaters.to_varchar(dt_atualizacao,'shortDate',1,wheb_usuario_pck.get_cd_estabelecimento,wheb_usuario_pck.get_nm_usuario) dt_atualizacao,
		 SUBSTR(obter_nome_usuario(nm_usuario),1,255) nm_profissional,
	     nr_atendimento,
       dt_liberacao
FROM	 FORMULARIO_TERAPIA
WHERE  	 dt_liberacao IS NOT NULL
AND    	 dt_inativacao IS NULL

UNION ALL

SELECT	 'Stimm-, Sprech(und Sprachtherapie (Muster 14)' ds_kv_from,
		 pkg_date_formaters.to_varchar(dt_atualizacao,'shortDate',1,wheb_usuario_pck.get_cd_estabelecimento,wheb_usuario_pck.get_nm_usuario) dt_atualizacao,
		 SUBSTR(obter_nome_usuario(nm_usuario),1,255) nm_profissional,
	     nr_atendimento,
       dt_liberacao
FROM	 FORMULARIO_TERAPIA
WHERE  	 dt_liberacao IS NOT NULL
AND    	 dt_inativacao IS NULL

UNION ALL

SELECT	 'Ohrenärztliche Verordnung einer Hörhilfe (Muster 15)' ds_kv_from,
		 pkg_date_formaters.to_varchar(dt_atualizacao,'shortDate',1,wheb_usuario_pck.get_cd_estabelecimento,wheb_usuario_pck.get_nm_usuario) dt_atualizacao,
		 nm_usuario nm_profissional,
	     nr_atendimento,
       dt_liberacao
FROM	 FORMULARIO_AUDICAO
WHERE  	 dt_liberacao IS NOT NULL
AND    	 dt_inativacao IS NULL

UNION ALL

SELECT	 'Arzneiverordnungsblatt (Muster 16)' ds_kv_from,
		 pkg_date_formaters.to_varchar(dt_atualizacao,'shortDate',1,wheb_usuario_pck.get_cd_estabelecimento,wheb_usuario_pck.get_nm_usuario) dt_atualizacao,
		 nm_usuario nm_profissional,
	     nr_atendimento,
       dt_liberacao
FROM	 KV_RECEITA_FARMACIA
WHERE  	 dt_liberacao IS NOT NULL
AND    	 dt_inativacao IS NULL

UNION ALL

SELECT	 'Ergotherapie (Muster 18)' ds_kv_from,
		 pkg_date_formaters.to_varchar(dt_atualizacao,'shortDate',1,wheb_usuario_pck.get_cd_estabelecimento,wheb_usuario_pck.get_nm_usuario) dt_atualizacao,
		 SUBSTR(obter_nome_usuario(nm_usuario),1,255) nm_profissional,
	     nr_atendimento,
       dt_liberacao
FROM	 FORMULARIO_TERAPIA
WHERE  	 dt_liberacao IS NOT NULL
AND    	 dt_inativacao IS NULL

UNION ALL

SELECT	 'Verordnung Soziotherapie gem. § 37a SGB V (Muster 26)' ds_kv_from,
		 pkg_date_formaters.to_varchar(dt_atualizacao,'shortDate',1,wheb_usuario_pck.get_cd_estabelecimento,wheb_usuario_pck.get_nm_usuario) dt_atualizacao,
		 nm_usuario nm_profissional,
	     nr_atendimento,
       dt_liberacao
FROM	 FORMULARIO_SOCIOTERAPIA
WHERE  	 dt_liberacao IS NOT NULL
AND    	 dt_inativacao IS NULL

UNION ALL

SELECT	 'Soziotherapeutischer Betreuungsplan gem. § 37a SGB V (Muster 27)' ds_kv_from,
		 pkg_date_formaters.to_varchar(dt_atualizacao,'shortDate',1,wheb_usuario_pck.get_cd_estabelecimento,wheb_usuario_pck.get_nm_usuario) dt_atualizacao,
		 nm_usuario nm_profissional,
	     nr_atendimento,
       dt_liberacao
FROM	 FORMULARIO_SOCIOTERAPIA
WHERE  	 dt_liberacao IS NOT NULL
AND    	 dt_inativacao IS NULL

UNION ALL

SELECT	 'Verordnung bei Überweisung zur Indikationsstellung für Soziotherapie gemäß § 37a SGB V im Umfang von maximal 5 Therapieeinheiten (Muster 28)' ds_kv_from,
		 pkg_date_formaters.to_varchar(dt_atualizacao,'shortDate',1,wheb_usuario_pck.get_cd_estabelecimento,wheb_usuario_pck.get_nm_usuario) dt_atualizacao,
		 nm_usuario nm_profissional,
		 nr_atendimento,
     dt_liberacao
FROM	 FORMULARIO_SOCIOTERAPIA
WHERE  	 dt_liberacao IS NOT NULL
AND    	 dt_inativacao IS NULL;

