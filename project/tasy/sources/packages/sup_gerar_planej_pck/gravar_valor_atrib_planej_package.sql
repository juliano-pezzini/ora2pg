-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE PROCEDURE sup_gerar_planej_pck.gravar_valor_atrib_planej ( nm_atributo_p text, vl_atributo_p bigint, cd_material_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) AS $body$
BEGIN

	insert into w_valor_atributo(
		dt_atualizacao,
		nm_usuario,
		dt_atualizacao_nrec,
		nm_usuario_nrec,
		cd_estabelecimento,
		nm_atributo,
		cd_material,
		vl_atributo)
	values (	clock_timestamp(),
		nm_usuario_p,
		clock_timestamp(),
		nm_usuario_p,
		cd_estabelecimento_p,
		nm_atributo_p,
		cd_material_p,
		vl_atributo_p);

	PERFORM set_config('sup_gerar_planej_pck.qt_registros_w', current_setting('sup_gerar_planej_pck.qt_registros_w')::bigint + 1, false);

	if (current_setting('sup_gerar_planej_pck.qt_registros_w')::bigint > 300) then
		commit;
		PERFORM set_config('sup_gerar_planej_pck.qt_registros_w', 0, false);
	end if;

	END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE sup_gerar_planej_pck.gravar_valor_atrib_planej ( nm_atributo_p text, vl_atributo_p bigint, cd_material_p bigint, cd_estabelecimento_p bigint, nm_usuario_p text) FROM PUBLIC;
