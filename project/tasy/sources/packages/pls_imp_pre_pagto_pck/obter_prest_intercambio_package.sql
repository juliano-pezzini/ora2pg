-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION pls_imp_pre_pagto_pck.obter_prest_intercambio ( cd_cpf_p pessoa_fisica.nr_cpf%type, cd_cgc_p pls_prestador_intercambio.cd_cgc_intercambio%type) RETURNS PLS_PRESTADOR_INTERCAMBIO.NR_SEQUENCIA%TYPE AS $body$
DECLARE

					
nr_seq_prest_w	pls_prestador_intercambio.nr_sequencia%type;


BEGIN
	if (cd_cpf_p IS NOT NULL AND cd_cpf_p::text <> '') then
		select 	max(a.nr_sequencia)
		into STRICT	nr_seq_prest_w
		from	pls_prestador_intercambio a,
			pessoa_fisica b
		where 	a.cd_pessoa_fisica = b.cd_pessoa_fisica
		and	b.nr_cpf = cd_cpf_p;
	else
		select 	max(a.nr_sequencia)
		into STRICT	nr_seq_prest_w
		from	pls_prestador_intercambio a
		where 	a.cd_cgc_intercambio = cd_cgc_p;
	end if;

return nr_seq_prest_w;

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION pls_imp_pre_pagto_pck.obter_prest_intercambio ( cd_cpf_p pessoa_fisica.nr_cpf%type, cd_cgc_p pls_prestador_intercambio.cd_cgc_intercambio%type) FROM PUBLIC;
