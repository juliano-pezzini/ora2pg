-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION pls_ocor_imp_pck.ie_reg_valido_fil_mat ( hr_inicial_fil_p timestamp, hr_inicial_mat_p timestamp, hr_final_fil_p timestamp, hr_final_mat_p timestamp) RETURNS boolean AS $body$
DECLARE

	
ie_registro_valido_w boolean;


BEGIN
--Valida os dados que foram incluados como campos para nao carregar

--a clausula WHERE com OR

ie_registro_valido_w := true;

--Verifica a hora inicial do material. Caso a hora do filtro seja menor que a do material, o registro nao a valido para continuar o processo.

if (hr_inicial_fil_p IS NOT NULL AND hr_inicial_fil_p::text <> '') and (hr_inicial_fil_p < hr_inicial_mat_p) then
	ie_registro_valido_w	:= false;
end if;

--Verifica a hora final do material. Caso a hora do filtro seja menor que a do material, o registro nao a valido para continuar o processo.

if (hr_final_fil_p IS NOT NULL AND hr_final_fil_p::text <> '') and (hr_final_fil_p > hr_final_mat_p) then
	ie_registro_valido_w	:= false;
end if;

return ie_registro_valido_w;
					
END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION pls_ocor_imp_pck.ie_reg_valido_fil_mat ( hr_inicial_fil_p timestamp, hr_inicial_mat_p timestamp, hr_final_fil_p timestamp, hr_final_mat_p timestamp) FROM PUBLIC;
