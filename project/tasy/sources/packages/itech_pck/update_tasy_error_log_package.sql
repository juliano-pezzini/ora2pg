-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

   /* 
  +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
	resend the outbound message from error folder
	+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
   **/
 


CREATE OR REPLACE PROCEDURE itech_pck.update_tasy_error_log ( file_name_p text, file_like_p text) AS $body$
DECLARE

      nr_seq_int_call_log_w integration_call_log.nr_sequencia%type;
      nm_interface_w	integration_call_log.nm_interface%type;
      nm_message_w		integration_call_log.nm_message%type;
      ds_error_w		integration_message_log.ds_error%type;
      ie_message_type_w integration_call_log.ie_message_type%type;

BEGIN
    if (file_name_p like file_like_p||'%')then
        select nr_sequencia, 
               nm_message, 
               nm_interface, 
               ie_message_type
        into STRICT   nr_seq_int_call_log_w,
               nm_message_w,
               nm_interface_w,
               ie_message_type_w
        from  integration_call_log where nr_sequencia = (  SELECT max(nr_seq_int_call_log)
                                                           from integration_message_log a 
                                                           where a.ds_message_id = file_name_p);
        ds_error_w := 'Error occured in file' || file_name_p ||' , Time of Error' || clock_timestamp();

       record_integration_call_log('tasy_itech', 'tasy_itech', clock_timestamp()	, nm_interface_w, nm_message_w, 'E', ie_message_type_w, null, null, null, ds_error_w, file_name_p, nr_seq_int_call_log_w);
   end if;
   END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE itech_pck.update_tasy_error_log ( file_name_p text, file_like_p text) FROM PUBLIC;
