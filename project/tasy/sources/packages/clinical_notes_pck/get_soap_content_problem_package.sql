-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION clinical_notes_pck.get_soap_content_problem ( nr_atendimento_p bigint, nr_sequencia_p bigint, ie_record_type_p text, ie_soap_type text, ie_stage_p bigint) RETURNS varchar AS $body$
DECLARE

    ds_content_w    varchar(32000);
    ds_cid_doenca_w varchar(255);
    cd_doenca_w diagnostico_doenca.cd_doenca%type;
    dt_inicio_w               varchar(255);
    ds_problema_w      varchar(255);
    ds_observacao_w    varchar(255);
    ds_status_w        varchar(255);
    ds_tipo_problema_w varchar(255);
    c01 CURSOR FOR
      SELECT *
      from table(clinical_notes_pck.get_clinical_note_rule(nr_atendimento_p,ie_record_type_p,null,ie_soap_type,ie_stage_p));

BEGIN
        select obter_desc_cid(cd_doenca),
          cd_doenca,
          obter_valor_dominio(13,ie_tipo_diagnostico),
          pkg_date_formaters.to_varchar(dt_inicio, 'shortDate', wheb_usuario_pck.get_cd_estabelecimento, wheb_usuario_pck.get_nm_usuario),
          ds_observacao,
          obter_valor_dominio(8221,ie_status),
          obter_desc_problema('LP',nr_seq_cad_problema)
        into STRICT ds_cid_doenca_w,
          cd_doenca_w,
          ds_tipo_problema_w,
          dt_inicio_w,
          ds_observacao_w,
          ds_status_w,
          ds_problema_w
        from lista_problema_pac
        where nr_sequencia     = nr_sequencia_p;

    for r_c01 in c01
    loop
     if (ie_record_type_p = 'PROBLEM') then

        if (r_c01.ie_field    = 'PRBLM_DESC' and (ds_problema_w IS NOT NULL AND ds_problema_w::text <> ''))then
          if (coalesce(ds_content_w::text, '') = '' ) then
            ds_content_w    := '<b>' || obter_desc_expressao(296350) || '</b>';
            ds_content_w    := ds_content_w|| '</br>' || coalesce(r_c01.nm_field,r_c01.ds_expressao) || ' - '|| ds_problema_w;
          else
            ds_content_w := ds_content_w|| '</br>' || coalesce(r_c01.nm_field,r_c01.ds_expressao) || ' - '|| ds_problema_w;
          end if;
        end if;
        if (r_c01.ie_field   = 'PRBLM_ICD10' and (cd_doenca_w IS NOT NULL AND cd_doenca_w::text <> ''))then
          if (coalesce(ds_content_w::text, '') = '' ) then
            ds_content_w   := '<b>' || obter_desc_expressao(296350) || '</b>';
            ds_content_w   := ds_content_w|| '</br>' || coalesce(r_c01.nm_field,r_c01.ds_expressao) || ' - '|| cd_doenca_w;
          else
            ds_content_w := ds_content_w|| '</br>' || coalesce(r_c01.nm_field,r_c01.ds_expressao) || ' - '|| cd_doenca_w;
          end if;
        end if;
        if (r_c01.ie_field   = 'PRBLM_TYPE' and (ds_tipo_problema_w IS NOT NULL AND ds_tipo_problema_w::text <> ''))then
          if (coalesce(ds_content_w::text, '') = '' ) then
            ds_content_w   := '<b>' || obter_desc_expressao(296350) || '</b>';
            ds_content_w   := ds_content_w|| '</br>' || coalesce(r_c01.nm_field,r_c01.ds_expressao) || ' - '|| ds_tipo_problema_w;
          else
            ds_content_w := ds_content_w|| '</br>' || coalesce(r_c01.nm_field,r_c01.ds_expressao) || ' - '|| ds_tipo_problema_w;
          end if;
        end if;
        if (r_c01.ie_field   = 'PRBLM_STATUS' and (ds_status_w IS NOT NULL AND ds_status_w::text <> ''))then
          if (coalesce(ds_content_w::text, '') = '' ) then
            ds_content_w   := '<b>' || obter_desc_expressao(296350) || '</b>';
            ds_content_w   := ds_content_w|| '</br>' || coalesce(r_c01.nm_field,r_c01.ds_expressao) || ' - '|| ds_status_w;
          else
            ds_content_w := ds_content_w|| '</br>' || coalesce(r_c01.nm_field,r_c01.ds_expressao) || ' - '|| ds_status_w;
          end if;
        end if;
        if (r_c01.ie_field   = 'DT_START' and (dt_inicio_w IS NOT NULL AND dt_inicio_w::text <> ''))then
          if (coalesce(ds_content_w::text, '') = '' ) then
            ds_content_w   := '<b>' || obter_desc_expressao(296350) || '</b>';
            ds_content_w   := ds_content_w|| '</br>' || coalesce(r_c01.nm_field,r_c01.ds_expressao) || ' - '|| dt_inicio_w;
          else
            ds_content_w := ds_content_w|| '</br>' || coalesce(r_c01.nm_field,r_c01.ds_expressao) || ' - '|| dt_inicio_w;
          end if;
        end if;
        if (r_c01.ie_field   = 'PRBLM_NOTES' and (ds_observacao_w IS NOT NULL AND ds_observacao_w::text <> ''))then
          if (coalesce(ds_content_w::text, '') = '' ) then
            ds_content_w   := '<b>' || obter_desc_expressao(296350) || '</b>';
            ds_content_w   := ds_content_w|| '</br>' || coalesce(r_c01.nm_field,r_c01.ds_expressao) || ' - '|| ds_observacao_w;
          else
            ds_content_w := ds_content_w|| '</br>' || coalesce(r_c01.nm_field,r_c01.ds_expressao) || ' - '|| ds_observacao_w;
          end if;
        end if;
        if (r_c01.ie_field   = 'PRBLM_ICDDESC' and (ds_cid_doenca_w IS NOT NULL AND ds_cid_doenca_w::text <> ''))then
          if (coalesce(ds_content_w::text, '') = '' ) then
            ds_content_w   := '<b>' || obter_desc_expressao(296350) || '</b>';
            ds_content_w   := ds_content_w|| '</br>' || coalesce(r_c01.nm_field,r_c01.ds_expressao) || ' - '|| ds_cid_doenca_w;
          else
            ds_content_w := ds_content_w|| '</br>' || coalesce(r_c01.nm_field,r_c01.ds_expressao) || ' - '|| ds_cid_doenca_w;
          end if;
        end if;
      end if;
    end loop;
    return ds_content_w;
  END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION clinical_notes_pck.get_soap_content_problem ( nr_atendimento_p bigint, nr_sequencia_p bigint, ie_record_type_p text, ie_soap_type text, ie_stage_p bigint) FROM PUBLIC;
