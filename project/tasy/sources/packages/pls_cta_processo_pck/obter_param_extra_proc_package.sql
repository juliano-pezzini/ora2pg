-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION pls_cta_processo_pck.obter_param_extra_proc ( cd_processo_p pls_cta_processo.cd_processo%type, ds_param_extra_processo_p pls_cta_log_exec.ds_param_extra_processo%type) RETURNS varchar AS $body$
DECLARE


ds_param_extra_w varchar(3000);

BEGIN
-- retorna somente os parâmetros do processo solicitado
select 	max(ds_valor_vchr2)
into STRICT	ds_param_extra_w
from 	table(pls_util_pck.converter_lista_valores(ds_param_extra_processo_p, ';'))
where  	upper(ds_valor_vchr2) like 'PROCESSO=' || to_char(cd_processo_p) || '%';

return ds_param_extra_w;

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION pls_cta_processo_pck.obter_param_extra_proc ( cd_processo_p pls_cta_processo.cd_processo%type, ds_param_extra_processo_p pls_cta_log_exec.ds_param_extra_processo%type) FROM PUBLIC;
