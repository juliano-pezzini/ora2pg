-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION import_users_to_psa_tws_pack.retrive_beneficiary ( cd_carteira_atual pls_segurado_carteira.cd_usuario_plano%TYPE, cd_carteira_anterior pls_segurado_cart_ant.cd_usuario_ant%TYPE) RETURNS SUBJECT.ID%TYPE AS $body$
DECLARE

				
resultado   subject.id%TYPE;


BEGIN
select	a.id
into STRICT	resultado
from    subject a,
	subject_client b,
	client c
where   b.id_subject = a.id
and     b.id_client = c.id
and     a.ds_replacement_login = cd_carteira_atual
and     c.nm_client = 'beneficiary';

RETURN resultado;
EXCEPTION
WHEN no_data_found THEN
    resultado := NULL;
    RETURN resultado;
END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION import_users_to_psa_tws_pack.retrive_beneficiary ( cd_carteira_atual pls_segurado_carteira.cd_usuario_plano%TYPE, cd_carteira_anterior pls_segurado_cart_ant.cd_usuario_ant%TYPE) FROM PUBLIC;
