-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';


-- Create and open the file by file modes
CREATE OR REPLACE PROCEDURE jpn_utl_file_pck.file_options_by_mode ( ds_local_p text, nm_arquivo_p text, ie_acao_p text, qt_tamanho_max_linha_p bigint) AS $body$
BEGIN

begin
-- Create and open the text file by UTL_FILE inside the directory recognized by ORACLE
ds_arquivo_w := utl_file.fopen( ds_local_p, nm_arquivo_p, ie_acao_p, qt_tamanho_max_linha_p);
exception
when others then
	-- Return the generated error
	nr_seq_erro_w := jpn_utl_file_pck.get_error_code(SQLSTATE);
end;

-- If you have an error creating or opening the file by UTL_FILE, stop the process
CALL jpn_utl_file_pck.error_handling( null, nr_seq_erro_w);

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE jpn_utl_file_pck.file_options_by_mode ( ds_local_p text, nm_arquivo_p text, ie_acao_p text, qt_tamanho_max_linha_p bigint) FROM PUBLIC;
