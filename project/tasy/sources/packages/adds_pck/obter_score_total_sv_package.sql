-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION adds_pck.obter_score_total_sv ( nr_atendimento_p bigint, nr_seq_adds_p bigint, nr_seq_sv_p bigint) RETURNS varchar AS $body$
DECLARE

dt_sinal_vital_w	timestamp;

BEGIN

select	max(dt_sinal_vital)
into STRICT	dt_sinal_vital_w
from	atendimento_sinal_vital
where	nr_sequencia = nr_seq_sv_p;

if (dt_sinal_vital_w IS NOT NULL AND dt_sinal_vital_w::text <> '') then
	return adds_pck.calcular_score_total_data(nr_atendimento_p,nr_seq_adds_p,dt_sinal_vital_w);
end if;

return null;
END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION adds_pck.obter_score_total_sv ( nr_atendimento_p bigint, nr_seq_adds_p bigint, nr_seq_sv_p bigint) FROM PUBLIC;
