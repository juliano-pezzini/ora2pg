-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';


--
-- dblink wrapper to call function cpoe_mat_json_pck.getcpoeintegracaomat() as an autonomous transaction
--
CREATE EXTENSION IF NOT EXISTS dblink;

CREATE OR REPLACE PROCEDURE cpoe_mat_json_pck.getcpoeintegracaomat (nr_seq_mat_cpoe_p bigint, nr_entity_identifier_p INOUT bigint) AS $body$
DECLARE
	-- Change this to reflect the dblink connection string
	v_conn_str  text := format('port=%s dbname=%s user=%s', current_setting('port'), current_database(), current_user);
	v_query     text;

BEGIN
	v_query := 'SELECT * FROM cpoe_mat_json_pck.getcpoeintegracaomat_atx ( ' || quote_nullable(nr_seq_mat_cpoe_p) || ',' || quote_nullable(nr_entity_identifier_p) || ' )';
	SELECT v_ret INTO nr_entity_identifier_p FROM dblink(v_conn_str, v_query) AS p (v_ret bigint);

END;
$body$ LANGUAGE plpgsql SECURITY DEFINER;




CREATE OR REPLACE PROCEDURE cpoe_mat_json_pck.getcpoeintegracaomat_atx (nr_seq_mat_cpoe_p bigint, nr_entity_identifier_p INOUT bigint) AS $body$
BEGIN

	select max(nr_sequencia)
	into STRICT nr_entity_identifier_p
	from cpoe_integracao
	where nr_seq_mat_cpoe = nr_seq_mat_cpoe_p;
	
	if (coalesce(nr_entity_identifier_p::text, '') = '') then
		select nextval('cpoe_integracao_seq')
		into STRICT nr_entity_identifier_p
		;

		insert into cpoe_integracao(
			nr_sequencia,
			nr_seq_mat_cpoe
			)
		values (
			nr_entity_identifier_p,
			nr_seq_mat_cpoe_p
		);
		commit;
	end if;	
	END;	

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE cpoe_mat_json_pck.getcpoeintegracaomat (nr_seq_mat_cpoe_p bigint, nr_entity_identifier_p INOUT bigint) FROM PUBLIC; -- REVOKE ALL ON PROCEDURE cpoe_mat_json_pck.getcpoeintegracaomat_atx (nr_seq_mat_cpoe_p bigint, nr_entity_identifier_p INOUT bigint) FROM PUBLIC;
