-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION cpoe_mat_json_pck.escapehl7 (hl7_message_p text) RETURNS varchar AS $body$
DECLARE


  escape_char_w                 varchar(1) := '\';
  field_separator_char_w        varchar(1) := '|';
  repetition_separator_char_w   varchar(1) := '~';
  component_separator_char_w    varchar(1) := '^';
  line_feed_w                   varchar(1) := chr(10);
  carriage_return_w             varchar(1) := chr(13);

  ds_escaped_msg_w  varchar(4000);


BEGIN

  ds_escaped_msg_w := replace(hl7_message_p, escape_char_w, '\E\');
  ds_escaped_msg_w := replace(ds_escaped_msg_w, field_separator_char_w, '\F\');
  ds_escaped_msg_w := replace(ds_escaped_msg_w, repetition_separator_char_w, '\R\');
  ds_escaped_msg_w := replace(ds_escaped_msg_w, component_separator_char_w, '\S\');
  ds_escaped_msg_w := replace(ds_escaped_msg_w, line_feed_w, '\.br\');
  ds_escaped_msg_w := replace(ds_escaped_msg_w, carriage_return_w, '\.br\');

  return ds_escaped_msg_w;

  END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION cpoe_mat_json_pck.escapehl7 (hl7_message_p text) FROM PUBLIC;
