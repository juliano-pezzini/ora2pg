-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION atualiza_versao_pck.is_single_thread_operation (p_operacao text) RETURNS varchar AS $body$
DECLARE

    v_ret varchar(1);

BEGIN
    v_ret := 'N';
    if p_operacao in ('SERVICE PACK UPDATE', 'BEFORE DATABASE UPDATE SCRIPTS (PREPARA)',
        'AFTER DATABASE UPDATE SCRIPTS (AJUSTE)', 'ENABLE INDEX LOGGING',
        'AFTER UPDATE DICTIONARY TABLES', 'BEFORE UPDATE DICTIONARY TABLES',
        'RECOMPILE PLSQL OBJECTS', 'ENABLE APPLICATION',
        'DISABLE APPLICATION' /*, 'ALTER TABLE ADD COLUMN',
               'ALTER TABLE MODIFY COLUMN'*/
       ) then
      v_ret := 'S';
    end if;
    return v_ret;
  end;

  --------------------------------------------------------------------------------------
  --                                                                                 
  -- Retorna se um código de erro SQL para uma determinada operação pode ser
  -- considerado apenas "Warning", sendo ignorado como erro.
  --                                                                                 
  --------------------------------------------------------------------------------------
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION atualiza_versao_pck.is_single_thread_operation (p_operacao text) FROM PUBLIC;
