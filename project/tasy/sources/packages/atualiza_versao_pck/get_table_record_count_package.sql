-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION atualiza_versao_pck.get_table_record_count (p_table_owner text, p_table_name text) RETURNS bigint AS $body$
DECLARE

    v_ret bigint;

BEGIN
    begin
      EXECUTE 'select count(1) from ' || p_table_owner || '.' ||
                        p_table_name
        into STRICT v_ret;
    exception
      when others then
        v_ret := null;
    end;
    return v_ret;
  end;

  --------------------------------------------------------------------------------------
  --                                                                                 
  -- Retorna a edição do banco de dados (Standard / Enterprise). Se a edição do banco
  -- de dados é Enterprise, são utilizadas features como paralelismo e online rebuild. 
  --                                                                                 
  --------------------------------------------------------------------------------------
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION atualiza_versao_pck.get_table_record_count (p_table_owner text, p_table_name text) FROM PUBLIC;
