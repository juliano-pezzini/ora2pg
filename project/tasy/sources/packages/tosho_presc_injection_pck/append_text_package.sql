-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE PROCEDURE tosho_presc_injection_pck.append_text ( DS_TEXT_P text, QT_SIZE_P bigint, IE_LEFT_RIGTH_P text DEFAULT 'L', DS_DEFAULT_P text DEFAULT ' ') AS $body$
BEGIN
		        IF ( IE_LEFT_RIGTH_P = 'R' ) THEN
		            PERFORM set_config('tosho_presc_injection_pck.ds_line_w', current_setting('tosho_presc_injection_pck.ds_line_w')::varchar(32767) || SUBSTR(RPAD(DS_TEXT_P, QT_SIZE_P, DS_DEFAULT_P), 1, QT_SIZE_P), false);

		        ELSE
		            PERFORM set_config('tosho_presc_injection_pck.ds_line_w', current_setting('tosho_presc_injection_pck.ds_line_w')::varchar(32767) || SUBSTR(LPAD(DS_TEXT_P, QT_SIZE_P, DS_DEFAULT_P), 1, QT_SIZE_P), false);
		        END IF;
		    END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE tosho_presc_injection_pck.append_text ( DS_TEXT_P text, QT_SIZE_P bigint, IE_LEFT_RIGTH_P text DEFAULT 'L', DS_DEFAULT_P text DEFAULT ' ') FROM PUBLIC;
