-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION tosho_presc_injection_pck.obter_set_atend_unidade ( NR_ATENDIMENTO_P bigint, IE_UNIDADE_P text) RETURNS bigint AS $body$
DECLARE



			CD_SETOR_ATENDIMENTO_W	bigint;

			
BEGIN

			SELECT	CD_SETOR_ATENDIMENTO
			INTO STRICT	CD_SETOR_ATENDIMENTO_W
			FROM	ATEND_PACIENTE_UNIDADE
			WHERE	NR_SEQ_INTERNO = (
				SELECT OBTER_ATEPACU_PACIENTE(NR_ATENDIMENTO_P, IE_UNIDADE_P)
				);

			RETURN CD_SETOR_ATENDIMENTO_W;

			END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION tosho_presc_injection_pck.obter_set_atend_unidade ( NR_ATENDIMENTO_P bigint, IE_UNIDADE_P text) FROM PUBLIC;
