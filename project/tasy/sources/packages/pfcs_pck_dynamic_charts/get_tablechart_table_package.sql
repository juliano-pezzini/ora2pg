-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION pfcs_pck_dynamic_charts.get_tablechart_table (nr_seq_card_p bigint, cd_establishment_p bigint, nr_seq_tab_p bigint) RETURNS SETOF DYNAMIC_TABLECHART_TP AS $body$
DECLARE

        dynamic_tablechart_w    DYNAMIC_TABLECHART_TP := DYNAMIC_TABLECHART_TP();
        dynamic_cursor_w        REFCURSOR;

BEGIN
        dynamic_cursor_w := pfcs_pck_dynamic_charts.get_dynamic_cursor(nr_seq_card_p, cd_establishment_p, nr_seq_tab_p);
        loop
            fetch dynamic_cursor_w
            bulk collect into dynamic_tablechart_w limit pls_util_pck.qt_registro_transacao_w;
                exit when dynamic_tablechart_w.count = 0;

            for i in 1..dynamic_tablechart_w.count loop
                RETURN NEXT dynamic_tablechart_w(i);
            end loop;
        end loop;
        close dynamic_cursor_w;

        return;
    END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION pfcs_pck_dynamic_charts.get_tablechart_table (nr_seq_card_p bigint, cd_establishment_p bigint, nr_seq_tab_p bigint) FROM PUBLIC;
