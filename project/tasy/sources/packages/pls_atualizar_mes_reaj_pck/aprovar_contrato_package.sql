-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE PROCEDURE pls_atualizar_mes_reaj_pck.aprovar_contrato ( nr_seq_contrato_p pls_contrato.nr_sequencia%type, nm_usuario_p usuario.nm_usuario%type, cd_estabelecimento_p estabelecimento.cd_estabelecimento%type) AS $body$
DECLARE


nr_mes_reajuste_grupo_w		bigint;
dt_reaj_contrato_w		timestamp;
nr_seq_grupo_w			pls_grupo_contrato.nr_sequencia%type;
ie_reajuste_w			pls_contrato.ie_reajuste%type;


BEGIN

select	coalesce(dt_reajuste, dt_contrato),
	ie_reajuste
into STRICT	dt_reaj_contrato_w,
	ie_reajuste_w
from	pls_contrato
where	nr_sequencia = nr_seq_contrato_p;

SELECT * FROM pls_atualizar_mes_reaj_pck.verificar_grupo_contrato(nr_seq_contrato_p, null, nr_seq_grupo_w, nr_mes_reajuste_grupo_w) INTO STRICT nr_seq_grupo_w, nr_mes_reajuste_grupo_w;

CALL pls_atualizar_mes_reaj_pck.atualizar_mes_reajuste(	nr_seq_contrato_p, null, ie_reajuste_w,
			coalesce(nr_mes_reajuste_grupo_w, (to_char(dt_reaj_contrato_w, 'mm'))::numeric ), nr_seq_grupo_w, 'S',
			'N', 'S', nm_usuario_p);

commit;

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_atualizar_mes_reaj_pck.aprovar_contrato ( nr_seq_contrato_p pls_contrato.nr_sequencia%type, nm_usuario_p usuario.nm_usuario%type, cd_estabelecimento_p estabelecimento.cd_estabelecimento%type) FROM PUBLIC;
