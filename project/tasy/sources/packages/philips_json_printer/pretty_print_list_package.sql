-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION philips_json_printer.pretty_print_list (obj philips_json_list, spaces boolean default true, line_length bigint default 0) RETURNS varchar AS $body$
DECLARE

    buf varchar(32767) :='';

BEGIN
    PERFORM set_config('philips_json_printer.max_line_len', line_length, false);
    PERFORM set_config('philips_json_printer.cur_line_len', 0, false);
    buf := philips_json_printer.add_buf(buf, philips_json_printer.llcheck('['));
    buf := philips_json_printer.ppea(obj, 0, buf, spaces);
    buf := philips_json_printer.add_buf(buf, philips_json_printer.llcheck(']'));
    return buf;
  end;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION philips_json_printer.pretty_print_list (obj philips_json_list, spaces boolean default true, line_length bigint default 0) FROM PUBLIC;
