-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION philips_json_printer.escapechar (ch char) RETURNS varchar AS $body$
DECLARE

     result varchar(20);

BEGIN

      result := ch;

      case ch
      when chr( 8) then result := '\b';
      when chr( 9) then result := '\t';
      when chr(10) then result := '\n';
      when chr(12) then result := '\f';
      when chr(13) then result := '\r';
      when chr(34) then result := '\"';
      when chr(47) then if (current_setting('philips_json_printer.escape_solidus')::boolean) then result := '\/'; end if;
      when chr(92) then result := '\\';
      else if (ascii(ch) < 32) then
             result :=  '\u'||replace(substr(to_char(ascii(ch), 'XXXX'),2,4), ' ', '0');
        elsif (current_setting('philips_json_printer.ascii_output')::boolean) then
             result := replace(asciistr(ch), '\', '\u');
        end if;
      end case;
      return result;
  end;




$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 IMMUTABLE;
-- REVOKE ALL ON FUNCTION philips_json_printer.escapechar (ch char) FROM PUBLIC;
