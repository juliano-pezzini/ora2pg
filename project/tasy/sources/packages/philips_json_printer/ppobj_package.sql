-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE PROCEDURE philips_json_printer.ppobj (obj philips_json, indent bigint, buf INOUT text, spaces boolean) AS $body$
BEGIN
    buf := philips_json_printer.add_buf(buf, philips_json_printer.llcheck('{') || philips_json_printer.newline(spaces));
    for m in 1 .. obj.json_data.count loop
      buf := philips_json_printer.ppmem(obj.json_data(m), indent+1, buf, spaces);
      if (m != obj.json_data.count) then buf := philips_json_printer.add_buf(buf, philips_json_printer.llcheck(',') || philips_json_printer.newline(spaces));
      else buf := philips_json_printer.add_buf(buf, philips_json_printer.newline(spaces)); end if;
    end loop;
    buf := philips_json_printer.add_buf(buf, philips_json_printer.llcheck(philips_json_printer.tab(indent, spaces)) || philips_json_printer.llcheck('}'));
  END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE philips_json_printer.ppobj (obj philips_json, indent bigint, buf INOUT text, spaces boolean) FROM PUBLIC;
