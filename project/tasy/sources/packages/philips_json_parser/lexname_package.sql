-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION philips_json_parser.lexname (jsrc INOUT json_src, tok INOUT rToken, indx INOUT integer) AS $body$
DECLARE

    varbuf varchar(32767) := '';
    buf varchar(4);
    num bigint;

  
BEGIN
    jsrc := philips_json_parser.next_char(indx, jsrc);
    while(REGEXP_LIKE(buf, '^[[:alnum:]\_]$', 'i')) loop
      varbuf := varbuf || buf;
      indx := indx + 1;
      jsrc := philips_json_parser.next_char(indx, jsrc);
      if (coalesce(buf::text, '') = '') then
        goto retname;

      end if;
    end loop;
    <<retname>>

    
    
    
    tok.data := varbuf;
    return;
  END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION philips_json_parser.lexname (jsrc INOUT json_src, tok INOUT rToken, indx INOUT integer) FROM PUBLIC;
