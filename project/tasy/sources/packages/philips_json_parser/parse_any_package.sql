-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION philips_json_parser.parse_any (str text) RETURNS PHILIPS_JSON_VALUE AS $body$
DECLARE

    tokens lTokens;
    obj philips_json_list;
    indx integer := 1;
    jsrc json_src;

BEGIN
    jsrc := philips_json_parser.prepareclob(str);
    jsrc := philips_json_parser.lexer(jsrc);
    tokens[tokens.count+1].type_name := ']';
    indx := philips_json_parser.parsearr(tokens, indx);
    if (tokens.count != indx) then
      CALL philips_json_parser.p_error('] should end the philips_json List object', tokens(indx));
    end if;

    return obj.head();
  END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION philips_json_parser.parse_any (str text) FROM PUBLIC;
