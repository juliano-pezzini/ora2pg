-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION lista_pck.obter_lista_char ( ds_valores_P text, ie_separador_p text default ',') RETURNS SETOF T_OBJETO_ROW_CHAR_DATA AS $body$
DECLARE

t_objeto_row_char_w			t_objeto_row_char;
lista_w		lista_varchar_pck.tabela_varchar;
cd_registro_w	varchar(255);
BEGIN

if (ds_valores_P IS NOT NULL AND ds_valores_P::text <> '') then
    lista_w	:= obter_lista_string2(ds_valores_P,coalesce(ie_separador_p,','));


        for	i in lista_w.first..lista_w.last loop						 						
            begin
            cd_registro_w	:= lista_w(i);
            t_objeto_row_char_w.cd_registro	:= cd_registro_w;
            RETURN NEXT t_objeto_row_char_w;

            end;
        end loop;

end if;

return;
END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION lista_pck.obter_lista_char ( ds_valores_P text, ie_separador_p text default ',') FROM PUBLIC;
