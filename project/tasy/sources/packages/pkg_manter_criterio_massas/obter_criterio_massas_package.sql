-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION pkg_manter_criterio_massas.obter_criterio_massas ( nr_seq_resultado_p bigint ) RETURNS LAB_LOTE_CRIT_MASSA%ROWTYPE AS $body$
DECLARE

		crit                   lab_lote_crit_massa%rowtype;

BEGIN

        begin
            select * into STRICT crit
            from lab_lote_crit_massa
            where nr_seq_resultado = nr_seq_resultado_p;
        exception
            when no_data_found or too_many_rows then
                crit.nr_sequencia := null;
        end;

		return crit;
    END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pkg_manter_criterio_massas.obter_criterio_massas ( nr_seq_resultado_p bigint ) FROM PUBLIC;
