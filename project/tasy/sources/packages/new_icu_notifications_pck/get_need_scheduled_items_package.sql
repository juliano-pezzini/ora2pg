-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION new_icu_notifications_pck.get_need_scheduled_items (nr_atendimento_p bigint) RETURNS bigint AS $body$
DECLARE


qt_need_scheduled_w	bigint;


BEGIN

select  count(*)
into STRICT	qt_need_scheduled_w
from 	cpoe_material
where 	nr_atendimento = nr_atendimento_p
and 	(dt_liberacao IS NOT NULL AND dt_liberacao::text <> '')
and 	((CASE WHEN coalesce(dt_lib_suspensao::text, '') = '' THEN  coalesce(dt_fim_cih, dt_fim)  ELSE coalesce(dt_suspensao, dt_fim) END  >= clock_timestamp())
or (CASE WHEN coalesce(dt_lib_suspensao::text, '') = '' THEN  coalesce(dt_fim_cih, dt_fim)  ELSE coalesce(dt_suspensao, dt_fim) coalesce(END::text, '') = ''))
and		coalesce(ds_horarios::text, '') = ''
and 	coalesce(ie_se_necessario, 'N') = 'N'
and 	coalesce(ie_acm, 'N') = 'N';

return qt_need_scheduled_w;

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION new_icu_notifications_pck.get_need_scheduled_items (nr_atendimento_p bigint) FROM PUBLIC;
