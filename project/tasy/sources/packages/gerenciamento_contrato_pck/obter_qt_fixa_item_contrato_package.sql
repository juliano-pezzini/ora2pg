-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION gerenciamento_contrato_pck.obter_qt_fixa_item_contrato (nr_seq_contrato_p bigint, nr_seq_regra_contrato_p bigint) RETURNS bigint AS $body$
DECLARE

		
		qt_material_fixa_w contrato_regra_nf.qt_material_fixa%type;
		
		
BEGIN
		
        if (coalesce(nr_seq_regra_contrato_p, 0) <> 0) then
            select coalesce(qt_material_fixa, 0)
              into STRICT qt_material_fixa_w
              from contrato_regra_nf
             where nr_sequencia = nr_seq_regra_contrato_p
               and nr_seq_contrato = nr_seq_contrato_p;
        else
            qt_material_fixa_w := 0;
        end if;

		return qt_material_fixa_w;
		
	END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION gerenciamento_contrato_pck.obter_qt_fixa_item_contrato (nr_seq_contrato_p bigint, nr_seq_regra_contrato_p bigint) FROM PUBLIC;
