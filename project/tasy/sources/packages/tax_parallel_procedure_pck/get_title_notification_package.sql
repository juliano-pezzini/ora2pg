-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION tax_parallel_procedure_pck.get_title_notification (generation_type_p fis_fila_arquivo.ie_tipo_geracao%type, tax_obligation_p fis_fila_arquivo.ie_obrigacao%type, batch_code_p fis_fila_arquivo.nr_seq_lote%type, language_code_p bigint) RETURNS varchar AS $body$
DECLARE


        title_w                 varchar(1000);
        type_message_code_w     bigint := 0;


BEGIN
        if generation_type_p = 1 then
            type_message_code_w := 1108700;
        else
            type_message_code_w := 1108701;
        end if;

        title_w := obter_texto_tasy(1108694, language_code_p);
        title_w := replace(title_w, '#@ie_tipo_geracao#@', obter_texto_tasy(type_message_code_w, language_code_p));
        title_w := replace(title_w, '#@ie_obrigacao#@', obter_descricao_dominio(9533, tax_obligation_p));
        title_w := replace(title_w, '#@nr_seq_lote#@', batch_code_p);

        return title_w;
    end;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION tax_parallel_procedure_pck.get_title_notification (generation_type_p fis_fila_arquivo.ie_tipo_geracao%type, tax_obligation_p fis_fila_arquivo.ie_obrigacao%type, batch_code_p fis_fila_arquivo.nr_seq_lote%type, language_code_p bigint) FROM PUBLIC;
