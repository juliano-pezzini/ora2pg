-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE PROCEDURE tax_parallel_procedure_pck.run_in_parallel (procedure_name_p text, parameters_p dbms_sql.varchar2_table, package_name_p text default null) AS $body$
DECLARE

        dynamic_action_w    varchar(4000) := 'begin ';
        ln_dummy_w          bigint;

BEGIN
        if (package_name_p IS NOT NULL AND package_name_p::text <> '') then
            dynamic_action_w := dynamic_action_w || package_name_p || '.';
        end if;

        dynamic_action_w := dynamic_action_w || procedure_name_p;

        if parameters_p.count <> 0 then
            dynamic_action_w := dynamic_action_w || '(' || tax_parallel_procedure_pck.build_parameters(parameters_p) || ')';
        end if;

        dynamic_action_w := dynamic_action_w || '; end;';

        dbms_job.submit(ln_dummy_w, dynamic_action_w);
        commit;
    END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE tax_parallel_procedure_pck.run_in_parallel (procedure_name_p text, parameters_p dbms_sql.varchar2_table, package_name_p text default null) FROM PUBLIC;
