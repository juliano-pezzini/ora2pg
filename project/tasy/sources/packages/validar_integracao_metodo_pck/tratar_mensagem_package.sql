-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION validar_integracao_metodo_pck.tratar_mensagem (mensagem1 text, mensagem2 text) RETURNS varchar AS $body$
DECLARE

    retorno_w varchar(32000);

BEGIN
    retorno_w := mensagem1;
    if (mensagem1 IS NOT NULL AND mensagem1::text <> '') and (mensagem2 IS NOT NULL AND mensagem2::text <> '') then
      retorno_w := mensagem1 || chr(10) || mensagem2;
    else if (mensagem2 IS NOT NULL AND mensagem2::text <> '') then
          retorno_w := mensagem2;
      end if;
    end if;
    return retorno_w;
  end;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION validar_integracao_metodo_pck.tratar_mensagem (mensagem1 text, mensagem2 text) FROM PUBLIC;
