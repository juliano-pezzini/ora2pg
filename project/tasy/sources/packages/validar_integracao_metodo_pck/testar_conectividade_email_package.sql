-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION validar_integracao_metodo_pck.testar_conectividade_email (smtp_p text, porta_p text) RETURNS varchar AS $body$
DECLARE

    c UTL_SMTP.CONNECTION;
    retorno varchar(5000) := '';

BEGIN
    c := UTL_SMTP.OPEN_CONNECTION(smtp_p, porta_p);
    UTL_SMTP.QUIT(c);
    return 'OK';
    exception
      when others then
        return 'ERRO: ' || SQLERRM;
  end;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION validar_integracao_metodo_pck.testar_conectividade_email (smtp_p text, porta_p text) FROM PUBLIC;
