-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION ctb_contab_onl_repasse_pck.get_ctb_documento ( cd_estabelecimento_p estabelecimento.cd_estabelecimento%type, nr_seq_protocolo_p protocolo_convenio.nr_seq_protocolo%type, nr_interno_conta_p conta_paciente.nr_interno_conta%type, nm_usuario_p text) RETURNS CTB_DOCUMENTO%ROWTYPE AS $body$
DECLARE


doc_w   ctb_documento%rowtype;


BEGIN

begin
select x.*
into STRICT   doc_w
from (SELECT  a.*
    from    ctb_documento a
    where   a.cd_estabelecimento  = 1
    and     a.nr_documento    = coalesce(nr_seq_protocolo_p,0)
    and     a.nm_tabela   = 'PROTOCOLO_CONVENIO'
    and     a.cd_tipo_lote_contabil = 14

union all

    SELECT  a.*
    from    ctb_documento a
    where   a.cd_estabelecimento  = 1
    and     a.nr_documento    = coalesce(nr_interno_conta_p,0)
    and     a.nm_tabela   = 'CONTA_PACIENTE'
    and     a.cd_tipo_lote_contabil = 14) x LIMIT 1;
exception when no_data_found then
    doc_w := ctb_contab_onl_repasse_pck.criar_ctb_documento(cd_estabelecimento_p, nr_seq_protocolo_p, nr_interno_conta_p, nm_usuario_p, doc_w);
end;

return doc_w;

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION ctb_contab_onl_repasse_pck.get_ctb_documento ( cd_estabelecimento_p estabelecimento.cd_estabelecimento%type, nr_seq_protocolo_p protocolo_convenio.nr_seq_protocolo%type, nr_interno_conta_p conta_paciente.nr_interno_conta%type, nm_usuario_p text) FROM PUBLIC;
