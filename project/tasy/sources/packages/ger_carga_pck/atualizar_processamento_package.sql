-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';


--
-- dblink wrapper to call function ger_carga_pck.atualizar_processamento() as an autonomous transaction
--
CREATE EXTENSION IF NOT EXISTS dblink;

CREATE OR REPLACE PROCEDURE ger_carga_pck.atualizar_processamento (nm_tabela_p text, nr_sequencia_p bigint) AS $body$
DECLARE
	-- Change this to reflect the dblink connection string
	v_conn_str  text := format('port=%s dbname=%s user=%s', current_setting('port'), current_database(), current_user);
	v_query     text;

BEGIN
	v_query := 'CALL ger_carga_pck.atualizar_processamento_atx ( ' || quote_nullable(nm_tabela_p) || ',' || quote_nullable(nr_sequencia_p) || ' )';
	PERFORM * FROM dblink(v_conn_str, v_query) AS p (ret boolean);

END;
$body$ LANGUAGE plpgsql SECURITY DEFINER;




CREATE OR REPLACE PROCEDURE ger_carga_pck.atualizar_processamento_atx (nm_tabela_p text, nr_sequencia_p bigint) AS $body$
DECLARE

ds_update_w varchar(255);
BEGIN

ds_update_w := 'update '||nm_tabela_p||' set dt_inicio_proc = sysdate where nr_sequencia = '||nr_sequencia_p;
CALL exec_sql_dinamico('LOAD_TASY', ds_update_w);

commit;
end;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE ger_carga_pck.atualizar_processamento (nm_tabela_p text, nr_sequencia_p bigint) FROM PUBLIC; -- REVOKE ALL ON PROCEDURE ger_carga_pck.atualizar_processamento_atx (nm_tabela_p text, nr_sequencia_p bigint) FROM PUBLIC;
