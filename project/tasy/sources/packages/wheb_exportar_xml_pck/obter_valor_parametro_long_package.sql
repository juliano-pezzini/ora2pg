-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION wheb_exportar_xml_pck.obter_valor_parametro_long (nm_parametro_p text, ar_parametros_p myArray) RETURNS DBMS_SQL.VARCHAR2A AS $body$
DECLARE

	ds_retorno DBMS_SQL.VARCHAR2A;
	
BEGIN
		for p in 1..ar_parametros_p.count loop
			if (ar_parametros_p[p].nm = upper(nm_parametro_p)) then        
				if ( ar_parametros_p[p].v2.count > 0 )then    
					ds_retorno := ar_parametros_p[p].v2;
				end if;
				exit;
			end if;
		end loop;
		return ds_retorno;
	end;
	
	/*
	Realiza a eliminacao dos caracteres especiais informados via parametro e retorna a string
	Verifica se o caracter esta entre o validos, se estiver verificar se esta entre os especiais, se esta, nao adiciona o mesmo
	*/
	

$body$
LANGUAGE PLPGSQL
 STABLE;
-- REVOKE ALL ON FUNCTION wheb_exportar_xml_pck.obter_valor_parametro_long (nm_parametro_p text, ar_parametros_p myArray) FROM PUBLIC;
