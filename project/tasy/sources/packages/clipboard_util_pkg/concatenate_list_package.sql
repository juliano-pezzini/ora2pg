-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION clipboard_util_pkg.concatenate_list ( p_cursor REFCURSOR ) RETURNS varchar AS $body$
DECLARE

        l_return   varchar(32000);
        l_temp     varchar(32000);

BEGIN
        LOOP
            FETCH p_cursor INTO l_temp;
            EXIT WHEN NOT FOUND; /* apply on p_cursor */
            l_return := l_return
                        || ';'
                        || l_temp;
        END LOOP;

        RETURN ltrim(l_return, ';');
    END;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION clipboard_util_pkg.concatenate_list ( p_cursor REFCURSOR ) FROM PUBLIC;
