-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';


    -- Main occupancy cursor
    

    --=========================================== Procedures ===========================================
CREATE OR REPLACE PROCEDURE pfcs_pck_census.check_missing_encounters_ref () AS $body$
BEGIN
        update  pfcs_service_request svc
        set     svc.nr_seq_encounter = (
                    SELECT  max(enc.nr_sequencia)
                    from    pfcs_encounter enc
                    where   svc.id_serv_request = enc.id_encounter
                    and     svc.nr_seq_patient = enc.nr_seq_patient)
        where   coalesce(svc.nr_seq_encounter::text, '') = ''
        and     (svc.nr_seq_patient IS NOT NULL AND svc.nr_seq_patient::text <> '')
        and     exists (
                    SELECT  1
                    from    pfcs_encounter enc
                    where   svc.id_serv_request = enc.id_encounter
                    and     svc.nr_seq_patient = enc.nr_seq_patient);
        commit;
    END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pfcs_pck_census.check_missing_encounters_ref () FROM PUBLIC;
