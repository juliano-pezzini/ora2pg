-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE PROCEDURE sncm_controle_mat_pck.gera_lote_job (nr_seq_lote_p controle_mat_lote.nr_sequencia%type, nm_usuario_p usuario.nm_usuario%type, cd_estabelecimento_p estabelecimento.cd_estabelecimento%type, minutes_p bigint) AS $body$
DECLARE

				
	job_w		bigint;
	command_w	varchar(32000);
	
	
BEGIN
	command_w := 'SNCM_CONTROLE_MAT_PCK.GERAR_LOTE('||nr_seq_lote_p||','''||nm_usuario_p||''','||cd_estabelecimento_p||');';

	job_p => job_w := sncm_controle_mat_pck.gerar_job(	command_p => command_w, nm_procedure_p => 'SNCM_CONTROLE_MAT_PCK.GERAR_LOTE()', minutes_p => minutes_p, job_p => job_w);

	update	controle_mat_lote
	set	nm_id_job	= job_w,
		nm_usuario	= nm_usuario_p,
		dt_atualizacao	= clock_timestamp()
	where	nr_sequencia	= nr_seq_lote_p;
	commit;
	
	END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE sncm_controle_mat_pck.gera_lote_job (nr_seq_lote_p controle_mat_lote.nr_sequencia%type, nm_usuario_p usuario.nm_usuario%type, cd_estabelecimento_p estabelecimento.cd_estabelecimento%type, minutes_p bigint) FROM PUBLIC;
