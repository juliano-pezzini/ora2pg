-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION wheb_db.is_index_of_constraint (nm_tabela_p text, nm_indice_p text) RETURNS boolean AS $body$
DECLARE

	count_w bigint;
	
BEGIN
	SELECT COUNT(1)
	  INTO STRICT count_w
	  FROM USER_CONSTRAINTS
	 WHERE TABLE_NAME = UPPER(nm_tabela_p)
	   AND INDEX_NAME = UPPER(nm_indice_p);

	RETURN count_w > 0;
	END;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION wheb_db.is_index_of_constraint (nm_tabela_p text, nm_indice_p text) FROM PUBLIC;
