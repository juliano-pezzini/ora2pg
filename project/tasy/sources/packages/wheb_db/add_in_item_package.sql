-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE PROCEDURE wheb_db.add_in_item (item_p text) AS $body$
BEGIN
	  IF coalesce(item_p::text, '') = '' THEN
		RETURN;
	  END IF;

	  IF (sql_in_w IS NOT NULL AND sql_in_w::text <> '') THEN
		sql_in_w := sql_in_w || ', ';
	  END IF;
	  sql_in_w := sql_in_w || wheb_db.quoted_str(item_p);
	END;

	BEGIN
	sql_in_w := NULL;

	CALL wheb_db.add_in_item(item1_p);
	CALL wheb_db.add_in_item(item2_p);
	CALL wheb_db.add_in_item(item3_p);
	CALL wheb_db.add_in_item(item4_p);
	CALL wheb_db.add_in_item(item5_p);
	CALL wheb_db.add_in_item(item6_p);
	CALL wheb_db.add_in_item(item7_p);
	CALL wheb_db.add_in_item(item8_p);
	CALL wheb_db.add_in_item(item9_p);

	RETURN;
	END;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE wheb_db.add_in_item (item_p text) FROM PUBLIC;
