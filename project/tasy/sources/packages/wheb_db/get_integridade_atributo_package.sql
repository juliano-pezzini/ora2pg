-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION wheb_db.get_integridade_atributo (nm_tabela_p text, nm_integridade_referencial_p text, nm_atributo_p text) RETURNS INTEGRIDADE_ATRIBUTO%ROWTYPE AS $body$
DECLARE

	integridade_atributo_w INTEGRIDADE_ATRIBUTO%rowtype;
	
BEGIN
	BEGIN
		SELECT *
		  INTO STRICT integridade_atributo_w
		  FROM INTEGRIDADE_ATRIBUTO
		 WHERE NM_TABELA                  = nm_tabela_p
		   AND NM_INTEGRIDADE_REFERENCIAL = nm_integridade_referencial_p
		   AND NM_ATRIBUTO                = nm_atributo_p
	  ORDER BY IE_SEQUENCIA_CRIACAO;

	EXCEPTION
	  WHEN no_data_found THEN
		integridade_atributo_w := NULL;
	END;

	RETURN integridade_atributo_w;
	END;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION wheb_db.get_integridade_atributo (nm_tabela_p text, nm_integridade_referencial_p text, nm_atributo_p text) FROM PUBLIC;
