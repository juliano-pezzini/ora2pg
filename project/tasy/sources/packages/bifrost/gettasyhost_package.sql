-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION bifrost.gettasyhost ( userName text, usesTasyBackend char ) RETURNS varchar AS $body$
DECLARE

    hostValue varchar(1024);

BEGIN
    if coalesce(usesTasyBackend::text, '') = '' or usesTasyBackend <> 'S' then
        hostValue := obter_valor_param_usuario(9041, 11, 0, coalesce(wheb_usuario_pck.get_nm_usuario, userName), wheb_usuario_pck.get_cd_estabelecimento);
    end if;
    if coalesce(hostValue::text, '') = '' then
        hostValue := obter_valor_param_usuario(0, 216, 0, coalesce(wheb_usuario_pck.get_nm_usuario, userName), wheb_usuario_pck.get_cd_estabelecimento);
    end if;

    while(substr(hostValue, -1, 1) = '/') loop
        hostValue := substr(hostValue, 1, length(hostValue) - 1);
    end loop;

    return hostValue;
END;

$body$
LANGUAGE PLPGSQL
;
-- REVOKE ALL ON FUNCTION bifrost.gettasyhost ( userName text, usesTasyBackend char ) FROM PUBLIC;
