-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE PROCEDURE gerar_pre_rep_pck.gerar_pre_rep ( nr_atendimento_p bigint, nm_usuario_p text) AS $body$
DECLARE

 
	nr_prescricao_w	prescr_medica.nr_prescricao%type;

	
BEGIN 
 
	delete from w_item_pre_rep where nm_usuario = nm_usuario_p;
 
	select	max(nr_prescricao) 
	into STRICT	nr_prescricao_w 
	from	prescr_medica 
	where	nr_atendimento	= nr_atendimento_p 
	and	ie_hemodialise 	= 'R';
 
	CALL gerar_pre_rep_pck.gerar_pre_rep_medic(	nr_atendimento_p, 
				nr_prescricao_w, 
				nm_usuario_p);
 
	CALL gerar_pre_rep_pck.gerar_pre_rep_mat(	nr_atendimento_p, 
				nr_prescricao_w, 
				nm_usuario_p);
 
	CALL CALL gerar_pre_rep_pck.gerar_pre_rep_proc(	nr_atendimento_p, 
				nr_prescricao_w, 
				nm_usuario_p);
 
	CALL CALL gerar_pre_rep_pck.gerar_pre_rep_sol(	nr_atendimento_p, 
				nr_prescricao_w, 
				nm_usuario_p);
 
	commit;
 
	END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE gerar_pre_rep_pck.gerar_pre_rep ( nr_atendimento_p bigint, nm_usuario_p text) FROM PUBLIC;
