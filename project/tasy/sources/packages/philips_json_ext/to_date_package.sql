-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION philips_json_ext.to_date (v philips_json_value) RETURNS timestamp AS $body$
BEGIN
    if (v.is_string) then
      return standard.to_date(v.get_string, current_setting('philips_json_ext.format_string')::varchar(30));
    else
      --rase_application_error(-20110, 'Anydata did not contain a date-value');
	  CALL wheb_mensagem_pck.exibir_mensagem_abort('Anydata did not contain a date-value');
    end if;
  exception
    when others then
      --rase_application_error(-20110, 'Anydata did not contain a date on the format: '||format_string);
	  CALL wheb_mensagem_pck.exibir_mensagem_abort('Anydata did not contain a date on the format: '||current_setting('philips_json_ext.format_string')::varchar(30));
  end;



$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION philips_json_ext.to_date (v philips_json_value) FROM PUBLIC;
