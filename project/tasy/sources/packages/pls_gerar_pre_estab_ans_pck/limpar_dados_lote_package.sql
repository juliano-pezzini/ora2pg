-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE PROCEDURE pls_gerar_pre_estab_ans_pck.limpar_dados_lote (limpar_dados_lote_p INOUT dados_lote) AS $body$
BEGIN

if (limpar_dados_lote_p.nr_sequencia.count > 0) then
	limpar_dados_lote_p.nr_sequencia.delete;
	limpar_dados_lote_p.ie_tipo_ident_prest.delete;
	limpar_dados_lote_p.cd_cpf_cnpj.delete;
	limpar_dados_lote_p.cd_cnes.delete;
	limpar_dados_lote_p.cd_municipio_ibge_prest.delete;
	limpar_dados_lote_p.nr_registro_operadora_inter.delete;
	limpar_dados_lote_p.nr_identificador.delete;
	limpar_dados_lote_p.vl_total_franquia.delete;
	limpar_dados_lote_p.nr_seq_prestador.delete;
	limpar_dados_lote_p.cd_cooperativa.delete;
end if;

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_gerar_pre_estab_ans_pck.limpar_dados_lote (limpar_dados_lote_p INOUT dados_lote) FROM PUBLIC;
