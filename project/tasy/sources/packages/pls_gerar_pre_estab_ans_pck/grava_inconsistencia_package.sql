-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE PROCEDURE pls_gerar_pre_estab_ans_pck.grava_inconsistencia (dados_inconsistencia_p INOUT dados_inconsistencia, nm_usuario_p usuario.nm_usuario%type) AS $body$
BEGIN

if (dados_inconsistencia_p.nr_seq_pre_estab_val.count > 0) then

	forall i in dados_inconsistencia_p.nr_seq_pre_estab_val.first .. dados_inconsistencia_p.nr_seq_pre_estab_val.last
		insert	into	pls_monitor_tiss_inc_pre(	cd_inconsistencia, dt_atualizacao, dt_atualizacao_nrec,
				nm_usuario, nm_usuario_nrec, nr_seq_pre_estab_val,
				nr_sequencia)
		values (	dados_inconsistencia_p.cd_inconsistencia(i), clock_timestamp(), clock_timestamp(),
				nm_usuario_p, nm_usuario_p, dados_inconsistencia_p.nr_seq_pre_estab_val(i),
				nextval('pls_monitor_tiss_inc_pre_seq'));
	commit;

	dados_inconsistencia_p.nr_seq_pre_estab_val.delete;
	dados_inconsistencia_p.cd_inconsistencia.delete;
end if;

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_gerar_pre_estab_ans_pck.grava_inconsistencia (dados_inconsistencia_p INOUT dados_inconsistencia, nm_usuario_p usuario.nm_usuario%type) FROM PUBLIC;
