-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';


-- Rotina responsável somente para fazer o update sobre a tabela pls_monit_tiss_pre_est_val com os dados adicionais
CREATE OR REPLACE PROCEDURE pls_gerar_pre_estab_ans_pck.atualiza_dados_lote (dados_lote_update_p INOUT dados_lote) AS $body$
BEGIN

if (dados_lote_update_p.nr_sequencia.count > 0) then

	forall i in dados_lote_update_p.nr_sequencia.first .. dados_lote_update_p.nr_sequencia.last
		update	pls_monit_tiss_pre_est_val
		set	dt_atualizacao			= clock_timestamp(),
			ie_tipo_ident_prest		= dados_lote_update_p.ie_tipo_ident_prest(i),
			cd_cpf_cnpj			= dados_lote_update_p.cd_cpf_cnpj(i),
			cd_cnes				= dados_lote_update_p.cd_cnes(i),
			cd_municipio_ibge_prest		= dados_lote_update_p.cd_municipio_ibge_prest(i),
			nr_registro_operadora_inter	= dados_lote_update_p.nr_registro_operadora_inter(i),
			nr_identificador		= dados_lote_update_p.nr_identificador(i),
			vl_total_franquia		= dados_lote_update_p.vl_total_franquia(i)
		where	nr_sequencia			= dados_lote_update_p.nr_sequencia(i);
	commit;

	dados_lote_update_p := pls_gerar_pre_estab_ans_pck.limpar_dados_lote(dados_lote_update_p);
end if;

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_gerar_pre_estab_ans_pck.atualiza_dados_lote (dados_lote_update_p INOUT dados_lote) FROM PUBLIC;
