-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION pls_preco_beneficiario_pck.obter_idade_anos ( dt_inicio_p timestamp, dt_fim_p timestamp, nr_seq_segurado_p pls_segurado.nr_sequencia%type) RETURNS bigint AS $body$
BEGIN

begin
	return (obter_idade(dt_inicio_p, dt_fim_p, 'A'))::numeric;
exception
when others then
	CALL wheb_mensagem_pck.exibir_mensagem_abort(1043276, 'DS_BENEFICIARIO='||nr_seq_segurado_p || ' - ' || pls_obter_dados_segurado(nr_seq_segurado_p,'N'));
end;

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION pls_preco_beneficiario_pck.obter_idade_anos ( dt_inicio_p timestamp, dt_fim_p timestamp, nr_seq_segurado_p pls_segurado.nr_sequencia%type) FROM PUBLIC;
