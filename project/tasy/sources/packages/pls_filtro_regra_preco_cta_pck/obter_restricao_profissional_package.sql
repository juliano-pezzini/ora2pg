-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION pls_filtro_regra_preco_cta_pck.obter_restricao_profissional ( cd_medico_p pls_cp_cta_filtro_prof.cd_medico%type, valor_bind_p INOUT sql_pck.t_dado_bind) AS $body$
DECLARE


dados_restricao_w	varchar(2500);


BEGIN
-- Inicializar o retorno

dados_restricao_w := null;

-- Codigo do medico

if (cd_medico_p IS NOT NULL AND cd_medico_p::text <> '') then
	
	dados_restricao_w := dados_restricao_w || '	and ' || pls_filtro_regra_preco_cta_pck.obter_alias_tabela('conta') || '.cd_medico_executor = :cd_medico_pc' || pls_util_pck.enter_w;
	valor_bind_p := sql_pck.bind_variable(	':cd_medico_pc', cd_medico_p, valor_bind_p);
end if;

return;

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION pls_filtro_regra_preco_cta_pck.obter_restricao_profissional ( cd_medico_p pls_cp_cta_filtro_prof.cd_medico%type, valor_bind_p INOUT sql_pck.t_dado_bind) FROM PUBLIC;
