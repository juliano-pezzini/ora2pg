-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION pls_sib_dados_benef_pck.ajustar_caracteres ( nm_ajuste_p text, ie_remove_cedilha_p text) RETURNS varchar AS $body$
DECLARE

nm_ajuste_w	varchar(255);

BEGIN
nm_ajuste_w	:= regexp_replace(elimina_acentos(nm_ajuste_p, ie_remove_cedilha_p),' {2,}', ' ');
nm_ajuste_w	:= trim(both replace(replace(replace(replace(replace(replace(replace(replace(replace(nm_ajuste_w,'>',''),'<',''),'.',''),'|',''),chr(166),''),chr(207),''),chr(9),''),chr(186),''),chr(170),''));
return nm_ajuste_w;
END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION pls_sib_dados_benef_pck.ajustar_caracteres ( nm_ajuste_p text, ie_remove_cedilha_p text) FROM PUBLIC;
