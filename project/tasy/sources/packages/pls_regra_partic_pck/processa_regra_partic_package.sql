-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';


-- faz o processamento da regra dentro da sequencia do procedimento para verificar se o participante esta duplicado



CREATE OR REPLACE PROCEDURE pls_regra_partic_pck.processa_regra_partic ( dados_val_partic_p pls_tipos_ocor_pck.dados_val_partic, nr_id_transacao_p pls_oc_cta_selecao_ocor_v.nr_id_transacao%type, dados_regra_p pls_tipos_ocor_pck.dados_regra, nm_usuario_p usuario.nm_usuario%type) AS $body$
BEGIN
-- foram feitas duplicatas das rotinas ao inves de utilizar sql dinamico

-- o motivo e que a complexidade de executar o select ja e desafiadora enquanto performance e o select dinamico neste caso

-- agravaria a situacao.


-- importacao xml

if (dados_regra_p.ie_evento = 'IMP') then

	if (dados_val_partic_p.ie_incidencia = 'GPDH') then
		-- busca pela regra dentro do segurado, guia, procedimento, data e hora do item

		CALL CALL pls_regra_partic_pck.regra_partic_guia_dth_imp(	dados_val_partic_p, nr_id_transacao_p, nm_usuario_p);

	elsif (dados_val_partic_p.ie_incidencia = 'GPD') then
		-- busca pela regra dentro do segurado, guia, procedimento e data

		CALL CALL pls_regra_partic_pck.regra_partic_guia_dt_imp(	dados_val_partic_p, nr_id_transacao_p, nm_usuario_p);
	else
		-- buscar pela regra dentro do procedimento

		CALL CALL pls_regra_partic_pck.regra_partic_proc_imp(	dados_val_partic_p, nr_id_transacao_p, nm_usuario_p);
	end if;
else

	if (dados_val_partic_p.ie_incidencia = 'GPDH') then
		-- busca pela regra dentro do segurado, guia, procedimento, data e hora do item

		CALL pls_regra_partic_pck.regra_partic_guia_dth(	dados_val_partic_p, nr_id_transacao_p, nm_usuario_p);

	elsif (dados_val_partic_p.ie_incidencia = 'GPD') then
		-- busca pela regra dentro do segurado, guia, procedimento e data

		CALL pls_regra_partic_pck.regra_partic_guia_dt(	dados_val_partic_p, nr_id_transacao_p, nm_usuario_p);
	else
		-- buscar pela regra dentro do procedimento

		CALL pls_regra_partic_pck.regra_partic_proc(	dados_val_partic_p, nr_id_transacao_p, nm_usuario_p);
	end if;
end if;

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_regra_partic_pck.processa_regra_partic ( dados_val_partic_p pls_tipos_ocor_pck.dados_val_partic, nr_id_transacao_p pls_oc_cta_selecao_ocor_v.nr_id_transacao%type, dados_regra_p pls_tipos_ocor_pck.dados_regra, nm_usuario_p usuario.nm_usuario%type) FROM PUBLIC;
