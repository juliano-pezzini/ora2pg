-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE PROCEDURE pls_tipos_cta_val_pck.atualiza_sel_ie_valido ( nr_id_transacao_p pls_rp_cta_selecao.nr_id_transacao%type, dados_filtro_p pls_tipos_cta_val_pck.dados_filtro, ie_filtro_validacao_p text) AS $body$
DECLARE

/* +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Finalidade:  Atualizar o IE_VALIDO para N quando o IE_VALIDO_TEMP  for N  no comeco de cada aplicacao de filtro das regras das ocorrencias.
-------------------------------------------------------------------------------------------------------------------

Locais de chamada direta:
[ X]  Objetos do dicionario [ ] Tasy (Delphi/Java) [  ] Portal [  ]  Relat?rios [ ] Outros:
 ------------------------------------------------------------------------------------------------------------------

Pontos de atencao:

Usar sempre o nr_id_transacao

Alteracaes: 
------------------------------------------------------------------------------------------------------------------

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ */


BEGIN
-- As acoes de filtro e validacao devem ser separadas para que a atualizacao do IE_VALIDO_TEMP seja feita da forma correta para cada situacao.

if (ie_filtro_validacao_p = 'F') then

	-- Se o filtro nao for um filtro de excecao entao ele so podera alterar os registros dele mesmo.

	-- Os filtros de Exececao nao devem atualizar o IE_VALIDO_TEMP  pois os filtros de excecao irao retornar os registros que nao deve ser gerada a ocorrencia, atualizando o campo

	-- IE_VALIDO para N.


	update	pls_rp_cta_selecao
	set	ie_valido 		= 'N'
	where	nr_id_transacao 	= nr_id_transacao_p
	and	ie_valido_temp 		= 'N'
	and	nr_seq_rp_cta_filtro    = dados_filtro_p.nr_sequencia;
	
-- Quando for uma validacao o campo IE_VALIDO deve ser atualizado para N em todos os registros em que o IE_VALIDO_TEMP estiver como N quando for chamado esta procedure.

elsif (ie_filtro_validacao_p = 'V') then

	update	pls_rp_cta_selecao
	set	ie_valido = 'N'
	where	nr_id_transacao = nr_id_transacao_p
	and	ie_valido_temp = 'N';
end if;

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_tipos_cta_val_pck.atualiza_sel_ie_valido ( nr_id_transacao_p pls_rp_cta_selecao.nr_id_transacao%type, dados_filtro_p pls_tipos_cta_val_pck.dados_filtro, ie_filtro_validacao_p text) FROM PUBLIC;
