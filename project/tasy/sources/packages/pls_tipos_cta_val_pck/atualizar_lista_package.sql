-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';


-- Atualiza todos os registros contidos na lista passada por par?metro em um unico acesso ao contexto de SQL. 

-- Preferimos deixar o Oracle resolver tudo sozinho utilizado o FORALL e as listas por que desenvolver algo para fazer isto nao e viavel.

-- Nesta procedure, e possivel passar mais uma sequencia da selecao em um registro da lista da selecao, para que os mesmo dados sejam atualizados para

-- todos os registros informados separados por virgula, isto foi feito para que, ao selecionar uma conta em um filtro de excecao, buscarmos todos os seus itens

-- e atualiza-los em um comando apenas.



CREATE OR REPLACE PROCEDURE pls_tipos_cta_val_pck.atualizar_lista ( nr_seq_selecao_p dbms_sql.number_table, ie_valido_p dbms_sql.varchar2_table, nm_usuario_p usuario.nm_usuario%type) AS $body$
BEGIN

if (nr_seq_selecao_p.count > 0) then
	forall i in nr_seq_selecao_p.first .. nr_seq_selecao_p.last
		update	pls_rp_cta_selecao
		set	ie_valido_temp = ie_valido_p(i),
			dt_atualizacao_nrec = dt_atualizacao,
			nm_usuario_nrec = nm_usuario,
			dt_atualizacao = clock_timestamp(),
			nm_usuario = nm_usuario_p
		where	nr_sequencia = nr_seq_selecao_p(i);
	commit;
end if;

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_tipos_cta_val_pck.atualizar_lista ( nr_seq_selecao_p dbms_sql.number_table, ie_valido_p dbms_sql.varchar2_table, nm_usuario_p usuario.nm_usuario%type) FROM PUBLIC;
