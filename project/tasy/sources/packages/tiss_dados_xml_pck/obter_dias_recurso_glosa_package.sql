-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION tiss_dados_xml_pck.obter_dias_recurso_glosa (cd_convenio_p convenio.cd_convenio%type) RETURNS bigint AS $body$
DECLARE

	qt_dias_w	convenio.qt_dias_regurso_glosa%type;

BEGIN
	begin
		select	qt_dias_regurso_glosa
		into STRICT	qt_dias_w
		from	convenio
		where	cd_convenio = cd_convenio_p;
	exception
	when others then
		qt_dias_w := 0;
	end;

	return coalesce(qt_dias_w, 0);
END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION tiss_dados_xml_pck.obter_dias_recurso_glosa (cd_convenio_p convenio.cd_convenio%type) FROM PUBLIC;
