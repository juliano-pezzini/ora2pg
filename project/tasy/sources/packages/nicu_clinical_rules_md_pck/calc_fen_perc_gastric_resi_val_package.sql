-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION nicu_clinical_rules_md_pck.calc_fen_perc_gastric_resi_val ( lastResultValue_p bigint, accumulated_p bigint ) RETURNS varchar AS $body$
BEGIN
	
	if (coalesce(lastResultValue_p,0) > 0) and (coalesce(accumulated_p,0) > 0) then
		return to_char(lastResultValue_p / accumulated_p * 100);
	end if;	
	
	return null;
	end;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION nicu_clinical_rules_md_pck.calc_fen_perc_gastric_resi_val ( lastResultValue_p bigint, accumulated_p bigint ) FROM PUBLIC;
