-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION alto_custo_pck.get_municipio_pessoa ( cd_pessoa_fisica_p text ) RETURNS varchar AS $body$
DECLARE

    ds_retorno_w      varchar(255);

BEGIN
    begin
      select  get_info_end_endereco(a.nr_seq_pessoa_endereco, 'MUNICIPIO', 'C')
      into STRICT    ds_retorno_w
      from    compl_pessoa_fisica a
      where   a.cd_pessoa_fisica = cd_pessoa_fisica_p
      and     nr_sequencia = (
                                SELECT  max(b.nr_sequencia)
                                from    compl_pessoa_fisica b
                                where   b.cd_pessoa_fisica = a.cd_pessoa_fisica
                              );
    exception
      when others then
        ds_retorno_w := '';
    end;
    return elimina_caracteres_especiais(ds_retorno_w);
  END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION alto_custo_pck.get_municipio_pessoa ( cd_pessoa_fisica_p text ) FROM PUBLIC;
