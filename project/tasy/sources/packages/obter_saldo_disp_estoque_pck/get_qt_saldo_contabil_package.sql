-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION obter_saldo_disp_estoque_pck.get_qt_saldo_contabil ( cd_estabelecimento_p bigint, cd_material_p bigint, cd_local_estoque_p bigint, dt_mesano_referencia_p timestamp, nr_seq_lote_p bigint default null) RETURNS bigint AS $body$
DECLARE


	qt_saldo_contabil_w	double precision;
	cd_material_estoque_ww		material.cd_material%type;

	
BEGIN
	CALL CALL CALL CALL CALL CALL CALL CALL CALL obter_saldo_disp_estoque_pck.set_cd_estabelecimento(cd_estabelecimento_p);
	CALL obter_saldo_disp_estoque_pck.set_nr_seq_lote(nr_seq_lote_p);
	CALL obter_saldo_disp_estoque_pck.set_cd_material(cd_material_p);
	cd_material_estoque_ww		:=	obter_saldo_disp_estoque_pck.get_cd_material_estoque();

	if	((nr_seq_lote_p > 0) and (current_setting('obter_saldo_disp_estoque_pck.ie_estoque_lote_w')::material_estab.ie_estoque_lote%type = 'S')) then
		qt_saldo_contabil_w	:=	obter_saldo_estoque_lote(cd_estabelecimento_p, cd_material_estoque_ww, cd_local_estoque_p, dt_mesano_referencia_p, nr_seq_lote_p);
	else
		obter_saldo_estoque(cd_estabelecimento_p, cd_material_estoque_ww, cd_local_estoque_p, dt_mesano_referencia_p, qt_saldo_contabil_w);
	end if;

	return qt_saldo_contabil_w;
	end;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION obter_saldo_disp_estoque_pck.get_qt_saldo_contabil ( cd_estabelecimento_p bigint, cd_material_p bigint, cd_local_estoque_p bigint, dt_mesano_referencia_p timestamp, nr_seq_lote_p bigint default null) FROM PUBLIC;
