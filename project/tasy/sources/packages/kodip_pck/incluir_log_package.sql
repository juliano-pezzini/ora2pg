-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';


--
-- dblink wrapper to call function kodip_pck.incluir_log() as an autonomous transaction
--
CREATE EXTENSION IF NOT EXISTS dblink;

CREATE OR REPLACE PROCEDURE kodip_pck.incluir_log ( reg_integracao_p gerar_int_padrao.reg_integracao_conv, ds_log_p intpd_log_recebimento.ds_log%type, nm_usuario_p text, ie_tipo_p text) AS $body$
DECLARE
	-- Change this to reflect the dblink connection string
	v_conn_str  text := format('port=%s dbname=%s user=%s', current_setting('port'), current_database(), current_user);
	v_query     text;

BEGIN
	v_query := 'CALL kodip_pck.incluir_log_atx ( ' || quote_nullable(reg_integracao_p) || ',' || quote_nullable(ds_log_p) || ',' || quote_nullable(nm_usuario_p) || ',' || quote_nullable(ie_tipo_p) || ' )';
	PERFORM * FROM dblink(v_conn_str, v_query) AS p (ret boolean);

END;
$body$ LANGUAGE plpgsql SECURITY DEFINER;




CREATE OR REPLACE PROCEDURE kodip_pck.incluir_log_atx ( reg_integracao_p gerar_int_padrao.reg_integracao_conv, ds_log_p intpd_log_recebimento.ds_log%type, nm_usuario_p text, ie_tipo_p text) AS $body$
BEGIN
intpd_gravar_log_recebimento(	
	reg_integracao_p.nr_seq_fila_transmissao,
	ds_log_p,
	nm_usuario_p,
	null,
	'E');
commit;
END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE kodip_pck.incluir_log ( reg_integracao_p gerar_int_padrao.reg_integracao_conv, ds_log_p intpd_log_recebimento.ds_log%type, nm_usuario_p text, ie_tipo_p text) FROM PUBLIC; -- REVOKE ALL ON PROCEDURE kodip_pck.incluir_log_atx ( reg_integracao_p gerar_int_padrao.reg_integracao_conv, ds_log_p intpd_log_recebimento.ds_log%type, nm_usuario_p text, ie_tipo_p text) FROM PUBLIC;
