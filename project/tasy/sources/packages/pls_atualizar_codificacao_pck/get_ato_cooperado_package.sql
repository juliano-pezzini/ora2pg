-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION pls_atualizar_codificacao_pck.get_ato_cooperado (ie_ato_cooperado_p text) RETURNS varchar AS $body$
BEGIN
	if (ie_ato_cooperado_p = '1') then
		if (current_setting('pls_atualizar_codificacao_pck.ie_ato_coop_prin_w')::coalesce(varchar(10)::text, '') = '') then
			CALL wheb_mensagem_pck.exibir_mensagem_abort(221914);
			/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Tipo de ato cooperado" para ato cooperativo principal! Favor verificar. */

		else
			return current_setting('pls_atualizar_codificacao_pck.ie_ato_coop_prin_w')::varchar(10);
		end if;
	elsif (ie_ato_cooperado_p = '2') then
		if (current_setting('pls_atualizar_codificacao_pck.ie_ato_coop_aux_w')::coalesce(varchar(10)::text, '') = '') then
			CALL wheb_mensagem_pck.exibir_mensagem_abort(221915);
			/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Tipo de ato cooperado" para ato cooperativo auxiliar! Favor verificar. */

		else
			return current_setting('pls_atualizar_codificacao_pck.ie_ato_coop_aux_w')::varchar(10);
		end if;
	elsif (ie_ato_cooperado_p = '3') then
		if (current_setting('pls_atualizar_codificacao_pck.ie_ato_nao_coop_w')::coalesce(varchar(10)::text, '') = '') then
			CALL wheb_mensagem_pck.exibir_mensagem_abort(221916);
			/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Tipo de ato cooperado" para ato não cooperativo! Favor verificar. */

		else
			return current_setting('pls_atualizar_codificacao_pck.ie_ato_nao_coop_w')::varchar(10);
		end if;
	else
		return 'TA';
	end if;
	end;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_atualizar_codificacao_pck.get_ato_cooperado (ie_ato_cooperado_p text) FROM PUBLIC;
