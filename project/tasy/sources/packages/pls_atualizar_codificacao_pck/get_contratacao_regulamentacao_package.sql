-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION pls_atualizar_codificacao_pck.get_contratacao_regulamentacao (ie_tipo_contratacao_p text, ie_regulamentacao_p text) RETURNS varchar AS $body$
BEGIN
	if (ie_tipo_contratacao_p = 'I') then
		if (ie_regulamentacao_p = 'R') then
			if (current_setting('pls_atualizar_codificacao_pck.ie_individual_pre_w')::coalesce(varchar(2)::text, '') = '') then
				CALL wheb_mensagem_pck.exibir_mensagem_abort(222875);
				/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Tipo de contratação / Regulamentação" para produto não regulamentado individual/familiar! Favor verificar. */

			else
				return current_setting('pls_atualizar_codificacao_pck.ie_individual_pre_w')::varchar(2);
			end if;
		elsif (ie_regulamentacao_p = 'P') then
			if (current_setting('pls_atualizar_codificacao_pck.ie_individual_pos_w')::coalesce(varchar(2)::text, '') = '') then
				CALL wheb_mensagem_pck.exibir_mensagem_abort(222876);
				/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Tipo de contratação / Regulamentação" para produto regulamentado individual/familiar! Favor verificar. */

			else
				return current_setting('pls_atualizar_codificacao_pck.ie_individual_pos_w')::varchar(2);
			end if;
		elsif (ie_regulamentacao_p = 'A') then
			if (current_setting('pls_atualizar_codificacao_pck.ie_individual_adaptado_w')::coalesce(varchar(2)::text, '') = '') then
				CALL wheb_mensagem_pck.exibir_mensagem_abort(222877);
				/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Tipo de contratação / Regulamentação" para produto adaptado individual/familiar! Favor verificar. */

			else
				return current_setting('pls_atualizar_codificacao_pck.ie_individual_adaptado_w')::varchar(2);
			end if;
		else
			return 'RC';
		end if;
	elsif (ie_tipo_contratacao_p = 'CE') then
		if (ie_regulamentacao_p = 'R') then
			if (current_setting('pls_atualizar_codificacao_pck.ie_empresarial_pre_w')::coalesce(varchar(2)::text, '') = '') then
				CALL wheb_mensagem_pck.exibir_mensagem_abort(222878);
				/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Tipo de contratação / Regulamentação" para produto não regulamentado coletivo empresarial! Favor verificar. */

			else
				return current_setting('pls_atualizar_codificacao_pck.ie_empresarial_pre_w')::varchar(2);
			end if;
		elsif (ie_regulamentacao_p = 'P') then
			if (current_setting('pls_atualizar_codificacao_pck.ie_empresarial_pos_w')::coalesce(varchar(2)::text, '') = '') then
				CALL wheb_mensagem_pck.exibir_mensagem_abort(222879);
				/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Tipo de contratação / Regulamentação" para produto regulamentado coletivo empresarial! Favor verificar. */

			else
				return current_setting('pls_atualizar_codificacao_pck.ie_empresarial_pos_w')::varchar(2);
			end if;
		elsif (ie_regulamentacao_p = 'A') then
			if (current_setting('pls_atualizar_codificacao_pck.ie_empresarial_adaptado_w')::coalesce(varchar(2)::text, '') = '') then
				CALL wheb_mensagem_pck.exibir_mensagem_abort(222880);
				/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Tipo de contratação / Regulamentação" para produto adaptado coletivo empresarial! Favor verificar. */

			else
				return current_setting('pls_atualizar_codificacao_pck.ie_empresarial_adaptado_w')::varchar(2);
			end if;
		else
			return 'RC';
		end if;
	elsif (ie_tipo_contratacao_p = 'CA') then
		if (ie_regulamentacao_p = 'R') then
			if (current_setting('pls_atualizar_codificacao_pck.ie_adesao_pre_w')::coalesce(varchar(2)::text, '') = '') then
				CALL wheb_mensagem_pck.exibir_mensagem_abort(222881);
				/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Tipo de contratação / Regulamentação" para produto não regulamentado coletivo por adesão! Favor verificar. */

			else
				return current_setting('pls_atualizar_codificacao_pck.ie_adesao_pre_w')::varchar(2);
			end if;
		elsif (ie_regulamentacao_p = 'P') then
			if (current_setting('pls_atualizar_codificacao_pck.ie_adesao_pos_w')::coalesce(varchar(2)::text, '') = '') then
				CALL wheb_mensagem_pck.exibir_mensagem_abort(222882);
				/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Tipo de contratação / Regulamentação" para produto regulamentado coletivo por adesão! Favor verificar. */

			else
				return current_setting('pls_atualizar_codificacao_pck.ie_adesao_pos_w')::varchar(2);
			end if;
		elsif (ie_regulamentacao_p = 'A') then
			if (current_setting('pls_atualizar_codificacao_pck.ie_adesao_adaptado_w')::coalesce(varchar(2)::text, '') = '') then
				CALL wheb_mensagem_pck.exibir_mensagem_abort(222883);
				/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Tipo de contratação / Regulamentação" para produto adaptado coletivo por adesão! Favor verificar. */

			else
				return current_setting('pls_atualizar_codificacao_pck.ie_adesao_adaptado_w')::varchar(2);
			end if;
		else
			return 'RC';
		end if;
	else
		return 'RC';
	end if;
	end;



$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_atualizar_codificacao_pck.get_contratacao_regulamentacao (ie_tipo_contratacao_p text, ie_regulamentacao_p text) FROM PUBLIC;
