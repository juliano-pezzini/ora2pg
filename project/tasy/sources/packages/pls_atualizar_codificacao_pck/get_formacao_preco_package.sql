-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION pls_atualizar_codificacao_pck.get_formacao_preco (ie_preco_p text) RETURNS varchar AS $body$
BEGIN
	if (ie_preco_p = '1') then
		if (current_setting('pls_atualizar_codificacao_pck.ie_form_pre_w')::coalesce(varchar(2)::text, '') = '') then
			CALL wheb_mensagem_pck.exibir_mensagem_abort(221877);
			/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Formação de preço" para produtos pré-estabelecidos! Favor verificar. */

		else
			return current_setting('pls_atualizar_codificacao_pck.ie_form_pre_w')::varchar(2);
		end if;
	elsif (ie_preco_p = '2') then
		if (current_setting('pls_atualizar_codificacao_pck.ie_form_pos_rateio_w')::coalesce(varchar(2)::text, '') = '') then
			CALL wheb_mensagem_pck.exibir_mensagem_abort(221878);
			/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Formação de preço" para produtos pós-estabelecidos por rateio! Favor verificar. */

		else
			return current_setting('pls_atualizar_codificacao_pck.ie_form_pos_rateio_w')::varchar(2);
		end if;
	elsif (ie_preco_p = '3') then
		if (current_setting('pls_atualizar_codificacao_pck.ie_form_pos_custo_w')::coalesce(varchar(2)::text, '') = '') then
			CALL wheb_mensagem_pck.exibir_mensagem_abort(221881);
			/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Formação de preço" para produtos pós-estabelecidos por custo operacional! Favor verificar. */

		else
			return current_setting('pls_atualizar_codificacao_pck.ie_form_pos_custo_w')::varchar(2);
		end if;
	elsif (ie_preco_p = '4') then
		if (current_setting('pls_atualizar_codificacao_pck.ie_form_misto_w')::coalesce(varchar(2)::text, '') = '') then
			CALL wheb_mensagem_pck.exibir_mensagem_abort(221882);
			/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Formação de preço" para produtos mistos! Favor verificar. */

		else
			return current_setting('pls_atualizar_codificacao_pck.ie_form_misto_w')::varchar(2);
		end if;
	else
		return 'FP';
	end if;
	end;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_atualizar_codificacao_pck.get_formacao_preco (ie_preco_p text) FROM PUBLIC;
