-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION pls_atualizar_codificacao_pck.get_regulamentacao (ie_regulamentacao_p text) RETURNS varchar AS $body$
BEGIN
	if (ie_regulamentacao_p = 'P') then
		if (current_setting('pls_atualizar_codificacao_pck.ie_regul_pos_w')::coalesce(varchar(2)::text, '') = '') then
			CALL wheb_mensagem_pck.exibir_mensagem_abort(221908);
			/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Regulamentação" para produtos pós! Favor verificar. */

		else
			return current_setting('pls_atualizar_codificacao_pck.ie_regul_pos_w')::varchar(2);
		end if;
	elsif (ie_regulamentacao_p = 'A') then
		if (current_setting('pls_atualizar_codificacao_pck.ie_regul_adaptado_w')::coalesce(varchar(2)::text, '') = '') then
			CALL wheb_mensagem_pck.exibir_mensagem_abort(221909);
			/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Regulamentação" para produtos adaptados! Favor verificar. */

		else
			return current_setting('pls_atualizar_codificacao_pck.ie_regul_adaptado_w')::varchar(2);
		end if;
	elsif (ie_regulamentacao_p = 'R') then
		if (current_setting('pls_atualizar_codificacao_pck.ie_regul_pre_w')::coalesce(varchar(2)::text, '') = '') then
			CALL wheb_mensagem_pck.exibir_mensagem_abort(221910);
			/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Regulamentação" para produtos pré! Favor verificar. */

		else
			return current_setting('pls_atualizar_codificacao_pck.ie_regul_pre_w')::varchar(2);
		end if;
	else
		return 'R';
	end if;
	end;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_atualizar_codificacao_pck.get_regulamentacao (ie_regulamentacao_p text) FROM PUBLIC;
