-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION pls_atualizar_codificacao_pck.get_tipo_relacao (ie_tipo_relacao_p text) RETURNS varchar AS $body$
BEGIN
	if (ie_tipo_relacao_p = 'P') then
		if (current_setting('pls_atualizar_codificacao_pck.ie_relac_rede_prop_w')::coalesce(varchar(2)::text, '') = '') then
			CALL wheb_mensagem_pck.exibir_mensagem_abort(221920);
			/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Tipo de relação com a OPS" para rede própria! Favor verificar. */

		else
			return current_setting('pls_atualizar_codificacao_pck.ie_relac_rede_prop_w')::varchar(2);
		end if;
	elsif (ie_tipo_relacao_p = 'D') then
		if (current_setting('pls_atualizar_codificacao_pck.ie_relac_rede_dir_w')::coalesce(varchar(2)::text, '') = '') then
			CALL wheb_mensagem_pck.exibir_mensagem_abort(221921);
			/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Tipo de relação com a OPS" para rede contratualizada direta! Favor verificar. */

		else
			return current_setting('pls_atualizar_codificacao_pck.ie_relac_rede_dir_w')::varchar(2);
		end if;
	elsif (ie_tipo_relacao_p = 'I') then
		if (current_setting('pls_atualizar_codificacao_pck.ie_relac_rede_indir_w')::coalesce(varchar(2)::text, '') = '') then
			CALL wheb_mensagem_pck.exibir_mensagem_abort(221922);
			/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Tipo de relação com a OPS" para rede contratualizada indireta! Favor verificar. */

		else
			return current_setting('pls_atualizar_codificacao_pck.ie_relac_rede_indir_w')::varchar(2);
		end if;
	elsif (ie_tipo_relacao_p = 'C') then
		if (current_setting('pls_atualizar_codificacao_pck.ie_relac_cooper_w')::coalesce(varchar(2)::text, '') = '') then
			CALL wheb_mensagem_pck.exibir_mensagem_abort(221923);
			/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Tipo de relação com a OPS" para cooperados! Favor verificar. */

		else
			return current_setting('pls_atualizar_codificacao_pck.ie_relac_cooper_w')::varchar(2);
		end if;
	elsif (ie_tipo_relacao_p = 'F') then
		if (current_setting('pls_atualizar_codificacao_pck.ie_relac_fornec_w')::coalesce(varchar(2)::text, '') = '') then
			CALL wheb_mensagem_pck.exibir_mensagem_abort(221924);
			/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Tipo de relação com a OPS" para fornecedores! Favor verificar. */

		else
			return current_setting('pls_atualizar_codificacao_pck.ie_relac_fornec_w')::varchar(2);
		end if;
	elsif (ie_tipo_relacao_p = 'N') then
		if (current_setting('pls_atualizar_codificacao_pck.ie_relac_nao_w')::coalesce(varchar(2)::text, '') = '') then
			CALL wheb_mensagem_pck.exibir_mensagem_abort(1043084);
			/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Tipo de relação com a OPS" para rede não contratualizada! Favor verificar. */

		else
			return current_setting('pls_atualizar_codificacao_pck.ie_relac_nao_w')::varchar(2);
		end if;
	else
		return 'TR';
	end if;
	end;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_atualizar_codificacao_pck.get_tipo_relacao (ie_tipo_relacao_p text) FROM PUBLIC;
