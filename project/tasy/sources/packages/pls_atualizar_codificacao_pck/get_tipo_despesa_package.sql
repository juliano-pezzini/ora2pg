-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION pls_atualizar_codificacao_pck.get_tipo_despesa (ie_tipo_despesa_p text) RETURNS varchar AS $body$
BEGIN
	if (ie_tipo_despesa_p = 1) then
		if (current_setting('pls_atualizar_codificacao_pck.ie_despesa_proced_w')::coalesce(varchar(2)::text, '') = '') then
			CALL wheb_mensagem_pck.exibir_mensagem_abort(1055884);
			/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Tipo de despesa" para procedimentos! Favor verificar. */

		else
			return current_setting('pls_atualizar_codificacao_pck.ie_despesa_proced_w')::varchar(2);
		end if;
	elsif (ie_tipo_despesa_p = 2) then
		if (current_setting('pls_atualizar_codificacao_pck.ie_despesa_taxas_w')::coalesce(varchar(2)::text, '') = '') then
			CALL wheb_mensagem_pck.exibir_mensagem_abort(1055885);
			/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Tipo de despesa" para taxas! Favor verificar. */

		else
			return current_setting('pls_atualizar_codificacao_pck.ie_despesa_taxas_w')::varchar(2);
		end if;
	elsif (ie_tipo_despesa_p = 3) then
		if (current_setting('pls_atualizar_codificacao_pck.ie_despesa_diarias_w')::coalesce(varchar(2)::text, '') = '') then
			CALL wheb_mensagem_pck.exibir_mensagem_abort(1055886);
			/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Tipo de despesa" para diárias! Favor verificar. */

		else
			return current_setting('pls_atualizar_codificacao_pck.ie_despesa_diarias_w')::varchar(2);
		end if;
	elsif (ie_tipo_despesa_p = 4) then
		if (current_setting('pls_atualizar_codificacao_pck.ie_despesa_pacotes_w')::coalesce(varchar(2)::text, '') = '') then
			CALL wheb_mensagem_pck.exibir_mensagem_abort(1055887);
			/* Mensagem: Não foi encontrada uma regra de codificação do tipo "Tipo de despesa" para pacotes! Favor verificar. */

		else
			return current_setting('pls_atualizar_codificacao_pck.ie_despesa_pacotes_w')::varchar(2);
		end if;
	else
		return 'TD';
	end if;
	end;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_atualizar_codificacao_pck.get_tipo_despesa (ie_tipo_despesa_p text) FROM PUBLIC;
