-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION product_mgmt_pck.get_version_list (p_product_name text) RETURNS varchar AS $body$
DECLARE

    v_ret varchar(4000);

  r RECORD;

BEGIN
    for r in (SELECT product_version
                from product_version
               where product_name = p_product_name
               order by product_version) loop
      v_ret := v_ret || ',' || r.product_version;
    end loop;
    v_ret := trim(both ',' from v_ret);
    return v_ret;
  end;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION product_mgmt_pck.get_version_list (p_product_name text) FROM PUBLIC;
