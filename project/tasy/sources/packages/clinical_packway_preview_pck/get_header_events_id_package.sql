-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION clinical_packway_preview_pck.get_header_events_id (seq_etapa_p protocolo_int_pac_etapa.nr_sequencia%TYPE, nr_dia_evento_p protocolo_int_pac_evento.nr_dia_protocolo%TYPE, nr_ordem_p protocolo_int_pac_evento.nr_seq_ordem%TYPE) RETURNS varchar AS $body$
DECLARE

        ids_w varchar(255) := '';

  i RECORD;

BEGIN

        FOR i IN ( SELECT nr_sequencia
                     from protocolo_int_pac_evento
                    where nr_seq_prt_int_pac_etapa = seq_etapa_p
                      and nr_seq_ordem = nr_ordem_p
                      and nr_dia_protocolo = nr_dia_evento_p ) LOOP

        ids_w := ids_w || i.nr_sequencia || '_';

        END LOOP;

        RETURN ids_w;
    END;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION clinical_packway_preview_pck.get_header_events_id (seq_etapa_p protocolo_int_pac_etapa.nr_sequencia%TYPE, nr_dia_evento_p protocolo_int_pac_evento.nr_dia_protocolo%TYPE, nr_ordem_p protocolo_int_pac_evento.nr_seq_ordem%TYPE) FROM PUBLIC;
