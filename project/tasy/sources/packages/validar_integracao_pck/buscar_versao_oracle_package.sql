-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION validar_integracao_pck.buscar_versao_oracle () RETURNS varchar AS $body$
DECLARE

    retorno varchar(5000) := '';
    banner_w varchar(5000);
  C01 CURSOR FOR
      SELECT BANNER FROM V$VERSION;

BEGIN

    open C01;
    loop
    fetch C01 into
          banner_w;
    EXIT WHEN NOT FOUND; /* apply on C01 */
            begin
              retorno := VALIDAR_INTEGRACAO_METODO_PCK.tratar_mensagem(retorno, ' - ' ||banner_w);
            end;
    end loop;
    close C01;

    return retorno;
  end;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION validar_integracao_pck.buscar_versao_oracle () FROM PUBLIC;
