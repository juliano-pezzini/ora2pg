-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION med_guidance_pkg.get_dt_entrada_inter (cd_pessoa_fisica_p PESSOA_FISICA.CD_PESSOA_FISICA%type) RETURNS timestamp AS $body$
DECLARE


    dt_entrada_inter_w timestamp;
    nr_seq_interv_inten_w varchar(255);

    
BEGIN
	
	SELECT  MIN(NR_ATENDIMENTO),
                MIN(DT_ENTRADA)
        INTO STRICT    nr_seq_interv_inten_w,
                dt_entrada_inter_w
        FROM    ATENDIMENTO_PACIENTE
        WHERE   IE_TIPO_ATENDIMENTO = 1
        AND     CD_PESSOA_FISICA = cd_pessoa_fisica_p;
	
     RETURN dt_entrada_inter_w;

    END;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION med_guidance_pkg.get_dt_entrada_inter (cd_pessoa_fisica_p PESSOA_FISICA.CD_PESSOA_FISICA%type) FROM PUBLIC;
