-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION med_guidance_pkg.compare_regra_period (dt_start_p timestamp, dt_end_p timestamp, si_period_p bigint, si_condition_p text, qt_value_2_p bigint) RETURNS boolean AS $body$
DECLARE


    qt_value_w bigint;


BEGIN

        CASE si_period_p
            WHEN 1 THEN
                qt_value_w := MED_GUIDANCE_PKG.CONVERT_IN_WEEKS(dt_start_p, dt_end_p);
		RETURN MED_GUIDANCE_PKG.COMPARE_RULE(qt_value_w, si_condition_p, qt_value_2_p);

            WHEN 2 THEN
                qt_value_w := MED_GUIDANCE_PKG.CONVERT_IN_MONTHS(dt_start_p, dt_end_p);
                RETURN MED_GUIDANCE_PKG.COMPARE_RULE(qt_value_w, si_condition_p, qt_value_2_p);

            WHEN 3 THEN
                qt_value_w := TRUNC(dt_end_p - dt_start_p);
		RETURN MED_GUIDANCE_PKG.COMPARE_RULE(qt_value_w, si_condition_p, qt_value_2_p);
            ELSE
                RETURN false;
        END CASE;

    END;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION med_guidance_pkg.compare_regra_period (dt_start_p timestamp, dt_end_p timestamp, si_period_p bigint, si_condition_p text, qt_value_2_p bigint) FROM PUBLIC;
