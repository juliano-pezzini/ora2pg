-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE PROCEDURE pls_consistir_conferencia_pck.inserir_divergencia ( ie_final_p text, nm_usuario_p usuario.nm_usuario%type) AS $body$
BEGIN
if (current_setting('pls_consistir_conferencia_pck.indice_w')::bigint = pls_util_pck.qt_registro_transacao_w) or (ie_final_p = 'S') then
	if (current_setting('pls_consistir_conferencia_pck.tb_nr_seq_conferencia_w')::pls_util_cta_pck.t_number_table.count > 0) then
		forall i in current_setting('pls_consistir_conferencia_pck.tb_nr_seq_conferencia_w')::pls_util_cta_pck.t_number_table.first..tb_nr_seq_conferencia_w.last
			insert	into	pls_sib_conf_divergencia(
							nr_sequencia,
							nr_seq_conferencia,
							nr_seq_divergencia,
							dt_atualizacao,
							nm_usuario,
							dt_atualizacao_nrec,
							nm_usuario_nrec)
					values (	nextval('pls_sib_conf_divergencia_seq'),
							current_setting('pls_consistir_conferencia_pck.tb_nr_seq_conferencia_w')::pls_util_cta_pck.t_number_table(i),
							current_setting('pls_consistir_conferencia_pck.tb_nr_seq_divergencia_w')::pls_util_cta_pck.t_number_table(i),
							clock_timestamp(),
							nm_usuario_p,
							clock_timestamp(),
							nm_usuario_p);
			commit;
			CALL CALL CALL pls_consistir_conferencia_pck.limpar_vetores();
	end if;
else
	PERFORM set_config('pls_consistir_conferencia_pck.indice_w', current_setting('pls_consistir_conferencia_pck.indice_w')::bigint + 1, false);
end if;
END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_consistir_conferencia_pck.inserir_divergencia ( ie_final_p text, nm_usuario_p usuario.nm_usuario%type) FROM PUBLIC;
