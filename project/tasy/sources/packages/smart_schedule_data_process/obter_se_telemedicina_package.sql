-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION smart_schedule_data_process.obter_se_telemedicina (nr_seq_agenda_p bigint, cd_tipo_agenda_p bigint) RETURNS varchar AS $body$
DECLARE

		current_setting('smart_schedule_data_process.ie_retorno_w')::varchar(1)	varchar(1) := 'N';
	
BEGIN
		if (cd_tipo_agenda_p in (3,4,5)) then
			SELECT	coalesce(MAX(B.IE_UTILIZA_TELEMEDICINA),'N')  IE_UTILIZA_TELEMEDICINA 
			INTO STRICT	current_setting('smart_schedule_data_process.ie_retorno_w')::varchar(1)
			FROM 	AGENDA_CONSULTA A 
			INNER 	JOIN AGENDA_CLASSIF B ON A.IE_CLASSIF_AGENDA = B.CD_CLASSIFICACAO
			WHERE 	A.NR_SEQUENCIA = nr_seq_agenda_p;
		end if;
		
		return current_setting('smart_schedule_data_process.ie_retorno_w')::varchar(1);
	  END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION smart_schedule_data_process.obter_se_telemedicina (nr_seq_agenda_p bigint, cd_tipo_agenda_p bigint) FROM PUBLIC;
