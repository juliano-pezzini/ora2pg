-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE PROCEDURE smart_schedule_data_process.obter_info_padrao_medico (cd_pessoa_fisica_p text, cd_agenda_p bigint) AS $body$
BEGIN
      IF (NOT current_setting('smart_schedule_data_process.info_medico_age_w')::info_medico_age_table.exists(cd_agenda_p)) OR (NOT current_setting('smart_schedule_data_process.info_medico_age_w')::info_medico_age_table[cd_agenda_p].info_medico_w.exists(cd_pessoa_fisica_p)) THEN
        SELECT COUNT(*)
          INTO STRICT current_setting('smart_schedule_data_process.info_medico_age_w')::info_medico_age_table[cd_agenda_p].info_medico_w(cd_pessoa_fisica_p).qt_regra_bloq_med
          FROM ageint_bloqueio_medico
         WHERE cd_pessoa_fisica = cd_pessoa_fisica_p
           AND coalesce(cd_agenda, cd_agenda_p) = cd_agenda_p
           AND trunc(dt_agenda_p) BETWEEN dt_inicial AND dt_final;
      END IF;
    END;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE smart_schedule_data_process.obter_info_padrao_medico (cd_pessoa_fisica_p text, cd_agenda_p bigint) FROM PUBLIC;
