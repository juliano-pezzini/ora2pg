-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION pls_compartilhamento_risco_pck.municipio_atuacao (cd_municipio_ibge_p text) RETURNS varchar AS $body$
BEGIN

if (current_setting('pls_compartilhamento_risco_pck.tb_cd_municipio_ibge_w')::pls_util_cta_pck.t_varchar2_table_10.count > 0) then
	for i in current_setting('pls_compartilhamento_risco_pck.tb_cd_municipio_ibge_w')::pls_util_cta_pck.t_varchar2_table_10.first..tb_cd_municipio_ibge_w.last loop
		begin
		if (current_setting('pls_compartilhamento_risco_pck.tb_cd_municipio_ibge_w')::pls_util_cta_pck.t_varchar2_table_10(i) = cd_municipio_ibge_p) then
			return 'S';
		end if;
		end;
	end loop;
	
	return 'N';
else
	return 'S';
end if;

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION pls_compartilhamento_risco_pck.municipio_atuacao (cd_municipio_ibge_p text) FROM PUBLIC;
