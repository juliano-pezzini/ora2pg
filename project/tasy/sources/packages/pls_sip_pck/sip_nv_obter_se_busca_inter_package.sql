-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION pls_sip_pck.sip_nv_obter_se_busca_inter ( nr_seq_item_p sip_item_assistencial.nr_sequencia%type) RETURNS boolean AS $body$
DECLARE


retorno_w	boolean;


BEGIN
	retorno_w := false;
	
	/*Nao precisa verificar para todas as regras, apenas para as que estiverem no intervalo abaixo que compreende em
	regime de internacao, causas selecionadas de internacao e nascidos vivos.
	Um dia pode acontecer que a sequencia da tabela ou a ordem logica dela mude. Entao, provavelmente sera necessaria um select recursivo.
	Enquanto isso, filtro pelas sequencias de forma fixa*/

	if (nr_seq_item_p between 83 and 108) then
		retorno_w := true;
	end if;
	
	return retorno_w;

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION pls_sip_pck.sip_nv_obter_se_busca_inter ( nr_seq_item_p sip_item_assistencial.nr_sequencia%type) FROM PUBLIC;
