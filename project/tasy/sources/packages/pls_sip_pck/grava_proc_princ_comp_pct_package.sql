-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE PROCEDURE pls_sip_pck.grava_proc_princ_comp_pct ( tb_cd_procedimento_p INOUT dbms_sql.number_table, tb_ie_origem_proced_p INOUT dbms_sql.number_table, tb_nr_seq_sip_nv_dados_p INOUT dbms_sql.number_table, ie_commit_p text, ie_esvaziar_table_p text) AS $body$
BEGIN


forall i in tb_nr_seq_sip_nv_dados_p.first..tb_nr_seq_sip_nv_dados_p.last
	update	sip_nv_dados
	set	cd_procedimento_pct	= tb_cd_procedimento_p(i),
		ie_origem_proced_pct	= tb_ie_origem_proced_p(i)
	where	nr_sequencia		= tb_nr_seq_sip_nv_dados_p(i);
	
-- se foi marcado para commitar

if (coalesce(ie_commit_p, 'N') = 'S') then

	commit;
end if;

-- se foi marcado para limpar as tabelas

if (coalesce(ie_esvaziar_table_p, 'S') = 'S') then

	tb_cd_procedimento_p.delete;
	tb_ie_origem_proced_p.delete;
	tb_nr_seq_sip_nv_dados_p.delete;

end if;


END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_sip_pck.grava_proc_princ_comp_pct ( tb_cd_procedimento_p INOUT dbms_sql.number_table, tb_ie_origem_proced_p INOUT dbms_sql.number_table, tb_nr_seq_sip_nv_dados_p INOUT dbms_sql.number_table, ie_commit_p text, ie_esvaziar_table_p text) FROM PUBLIC;
