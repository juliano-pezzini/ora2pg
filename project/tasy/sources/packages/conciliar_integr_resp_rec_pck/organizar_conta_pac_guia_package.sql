-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE PROCEDURE conciliar_integr_resp_rec_pck.organizar_conta_pac_guia () AS $body$
DECLARE

current_setting('conciliar_integr_resp_rec_pck.i')::integer integer;
k integer;
indxaux integer;




BEGIN
PERFORM set_config('conciliar_integr_resp_rec_pck.i', 0, false);
k := 0;
indxaux := 0;
if (current_setting('conciliar_integr_resp_rec_pck.campos_conta_w')::campos_conta_v.count > 0) then
	FOR current_setting('conciliar_integr_resp_rec_pck.i')::integer IN current_setting('conciliar_integr_resp_rec_pck.campos_conta_w')::campos_conta_v.first .. current_setting('conciliar_integr_resp_rec_pck.campos_conta_w')::campos_conta_v.last LOOP

		if (current_setting('conciliar_integr_resp_rec_pck.campos_conta_w')::campos_conta_v(current_setting('conciliar_integr_resp_rec_pck.i')::integer).seq_guia > 1) then

			current_setting('conciliar_integr_resp_rec_pck.campos_conta_aux_w')::campos_conta_v(indxaux) := current_setting('conciliar_integr_resp_rec_pck.campos_conta_w')::campos_conta_v(current_setting('conciliar_integr_resp_rec_pck.i')::integer);
			current_setting('conciliar_integr_resp_rec_pck.campos_conta_w')::campos_conta_v(current_setting('conciliar_integr_resp_rec_pck.i')::integer).ie_guias := 'S';
			indxaux := indxaux + 1;
		end if;

	end loop;
end if;
PERFORM set_config('conciliar_integr_resp_rec_pck.i', 0, false);
k := 0;
indxaux := 0;
if (current_setting('conciliar_integr_resp_rec_pck.campos_conta_w')::campos_conta_v.count > 0) then
	FOR current_setting('conciliar_integr_resp_rec_pck.i')::integer IN current_setting('conciliar_integr_resp_rec_pck.campos_conta_w')::campos_conta_v.first .. current_setting('conciliar_integr_resp_rec_pck.campos_conta_w')::campos_conta_v.last LOOP

	    if (current_setting('conciliar_integr_resp_rec_pck.campos_conta_aux_w')::campos_conta_v.count > 0) then
		for k in current_setting('conciliar_integr_resp_rec_pck.campos_conta_aux_w')::campos_conta_v.first .. current_setting('conciliar_integr_resp_rec_pck.campos_conta_aux_w')::campos_conta_v.last loop

		    if (current_setting('conciliar_integr_resp_rec_pck.campos_conta_w')::campos_conta_v.count > 1) then
			if (current_setting('conciliar_integr_resp_rec_pck.campos_conta_w')::campos_conta_v(current_setting('conciliar_integr_resp_rec_pck.i')::integer).nr_interno_conta = current_setting('conciliar_integr_resp_rec_pck.campos_conta_aux_w')::campos_conta_v[k].nr_interno_conta) then

			    current_setting('conciliar_integr_resp_rec_pck.campos_conta_aux2_w')::campos_conta_v(indxaux) := current_setting('conciliar_integr_resp_rec_pck.campos_conta_w')::campos_conta_v(current_setting('conciliar_integr_resp_rec_pck.i')::integer);
			    current_setting('conciliar_integr_resp_rec_pck.campos_conta_w')::campos_conta_v(current_setting('conciliar_integr_resp_rec_pck.i')::integer).ie_guias := 'S';
			    indxaux := indxaux + 1;

			end if;
		    end if;
		end loop;
	    end if;
	end loop;
end if;

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE conciliar_integr_resp_rec_pck.organizar_conta_pac_guia () FROM PUBLIC;
