-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION pls_util_pck.limita_string ( ds_string_1_p text, ds_string_2_p text, nr_limite_p bigint default 4000) RETURNS varchar AS $body$
DECLARE


ds_retorno_w	varchar(4000);
nr_limite_w	integer;


BEGIN
if (nr_limite_p > 4000) then
	nr_limite_w := 4000;
else
	nr_limite_w := coalesce(nr_limite_p, 4000);
end if;

ds_retorno_w := substr((ds_string_1_p) || (ds_string_2_p), 1, nr_limite_w);

return ds_retorno_w;

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION pls_util_pck.limita_string ( ds_string_1_p text, ds_string_2_p text, nr_limite_p bigint default 4000) FROM PUBLIC;
