-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE PROCEDURE dashboard_html5_pck.obter_dt_fim_prev_real (nr_seq_proj_p bigint, ds_atividade_p text, dt_fim_prev_p INOUT timestamp, dt_fim_real_p INOUT timestamp) AS $body$
BEGIN
        select max(a.dt_fim_prev),
               max(a.dt_fim_real)
          into STRICT dt_fim_prev_p,
               dt_fim_real_p
          from proj_cron_etapa a,
               proj_cronograma b
         where a.nr_seq_cronograma = b.nr_sequencia
           and b.nr_seq_proj = nr_seq_proj_p
           and b.ie_situacao = 'A'
           and trim(both a.ds_atividade) = ds_atividade_p;

    END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE dashboard_html5_pck.obter_dt_fim_prev_real (nr_seq_proj_p bigint, ds_atividade_p text, dt_fim_prev_p INOUT timestamp, dt_fim_real_p INOUT timestamp) FROM PUBLIC;
