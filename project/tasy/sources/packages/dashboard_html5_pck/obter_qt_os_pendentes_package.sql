-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE PROCEDURE dashboard_html5_pck.obter_qt_os_pendentes (nr_seq_proj_analise_p bigint, nr_seq_proj_prog_p bigint, nr_seq_proj_teste_p bigint, ie_papel_executor1_p text, ie_papel_executor2_p text, ie_atividade_p text, qt_os_pendente_p INOUT bigint) AS $body$
BEGIN
        if (coalesce(ie_papel_executor1_p::text, '') = '') and (coalesce(ie_papel_executor2_p::text, '') = '') and (coalesce(ie_atividade_p::text, '') = '') then
            select count(1)
              into STRICT qt_os_pendente_p
              from man_estagio_processo m,
                   man_ordem_servico    a,
                   proj_cron_etapa      e,
                   proj_cronograma      p
             where a.nr_seq_proj_cron_etapa = e.nr_sequencia
               and e.nr_seq_cronograma = p.nr_sequencia
               and a.nr_seq_estagio = m.nr_sequencia
               and m.ie_desenv = 'S'
               and p.nr_seq_proj in (nr_seq_proj_analise_p, nr_seq_proj_prog_p, nr_seq_proj_teste_p)
               and p.ie_situacao = 'A';
        end if;
        if ((ie_papel_executor1_p IS NOT NULL AND ie_papel_executor1_p::text <> '') or (ie_papel_executor2_p IS NOT NULL AND ie_papel_executor2_p::text <> '')) and (coalesce(ie_atividade_p::text, '') = '') then
            select count(1)
              into STRICT qt_os_pendente_p
              from man_estagio_processo m,
                   man_ordem_servico    a,
                   proj_cron_etapa      e,
                   proj_cronograma      p
             where a.nr_seq_proj_cron_etapa = e.nr_sequencia
               and e.nr_seq_cronograma = p.nr_sequencia
               and a.nr_seq_estagio = m.nr_sequencia
               and m.ie_desenv = 'S'
               and p.nr_seq_proj in (nr_seq_proj_analise_p, nr_seq_proj_prog_p, nr_seq_proj_teste_p)
               and p.ie_situacao = 'A'
               and e.ie_papel_executor in (ie_papel_executor1_p, ie_papel_executor2_p);
        end if;
        if ((ie_papel_executor1_p IS NOT NULL AND ie_papel_executor1_p::text <> '') or (ie_papel_executor2_p IS NOT NULL AND ie_papel_executor2_p::text <> '')) and (ie_atividade_p IS NOT NULL AND ie_atividade_p::text <> '') then
            select count(1)
              into STRICT qt_os_pendente_p
              from man_estagio_processo m,
                   man_ordem_servico    a,
                   proj_cron_etapa      e,
                   proj_cronograma      p
             where a.nr_seq_proj_cron_etapa = e.nr_sequencia
               and e.nr_seq_cronograma = p.nr_sequencia
               and a.nr_seq_estagio = m.nr_sequencia
               and m.ie_desenv = 'S'
               and p.nr_seq_proj in (nr_seq_proj_analise_p, nr_seq_proj_prog_p, nr_seq_proj_teste_p)
               and p.ie_situacao = 'A'
               and e.ie_papel_executor in (ie_papel_executor1_p, ie_papel_executor2_p)
               and e.ie_atividade = ie_atividade_p;
        end if;
    END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE dashboard_html5_pck.obter_qt_os_pendentes (nr_seq_proj_analise_p bigint, nr_seq_proj_prog_p bigint, nr_seq_proj_teste_p bigint, ie_papel_executor1_p text, ie_papel_executor2_p text, ie_atividade_p text, qt_os_pendente_p INOUT bigint) FROM PUBLIC;
