-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE PROCEDURE pls_cta_consist_carac_item_pck.tratar_erro ( ds_sql_p text, dados_regra_p dados_regra) AS $body$
DECLARE


ds_erro_w	varchar(4000);
ds_trace_w	varchar(4000);


BEGIN

ds_trace_w := dbms_utility.format_call_stack;

-- Desfazer as alterações feitas.
rollback;

-- Montar a descrição do erro.
ds_erro_w :=	'Erro: ' || sqlerrm || pls_util_pck.enter_w ||
		pls_util_pck.enter_w ||
		'SQL executado: ' || pls_util_pck.enter_w ||
		ds_sql_p || pls_util_pck.enter_w ||
		pls_util_pck.enter_w ||
		'Stack: ' || pls_util_pck.enter_w ||
		ds_trace_w;

-- Exibir a mensagem de erro:
--Ocorreu um erro ao efetuar a verificação da regra de Caracterísiticas da conta X Caracterísiticas do item n° #@REGRA#@. Favor entrar em contato com o suporte a apresentar a seguinte mensagem:
--#@ERRO#@.
CALL wheb_mensagem_pck.exibir_mensagem_abort(268334, 'REGRA=' || dados_regra_p.nr_sequencia || ';ERRO=' || ds_erro_w, -20012);

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_cta_consist_carac_item_pck.tratar_erro ( ds_sql_p text, dados_regra_p dados_regra) FROM PUBLIC;
