-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION tiss_dashboard_data_pck.obter_dt_previsao_pag_recurso (nr_interno_conta_p lote_audit_hist_guia.nr_interno_conta%type, cd_autorizacao_p lote_audit_hist_guia.cd_autorizacao%type) RETURNS timestamp AS $body$
DECLARE


dt_ret_previsto_w	lote_audit_hist.dt_ret_previsto%type;


BEGIN

	begin
		select	min(b.dt_ret_previsto)
		into STRICT	dt_ret_previsto_w
		from	lote_audit_hist_guia a,
			lote_audit_hist b
		where 	a.nr_seq_lote_hist 	= b.nr_sequencia
		and	coalesce(a.cd_autorizacao, wheb_mensagem_pck.get_texto(1097738)) 	= cd_autorizacao_p
		and	a.nr_interno_conta 	= nr_interno_conta_p;
	exception
	when others then
		dt_ret_previsto_w := null;
	end;

	return dt_ret_previsto_w;

end;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION tiss_dashboard_data_pck.obter_dt_previsao_pag_recurso (nr_interno_conta_p lote_audit_hist_guia.nr_interno_conta%type, cd_autorizacao_p lote_audit_hist_guia.cd_autorizacao%type) FROM PUBLIC;
