-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION pls_decl_segurado_web_pck.validar_cd_acesso_web (nr_seq_declaracao_segurado_p pls_declaracao_segurado.nr_sequencia%type, cd_codigo_acesso_p text, dt_nascimento_p timestamp) RETURNS varchar AS $body$
DECLARE


/* +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Finalidade:
Validar se codigo de acesso eh valido.
Esta eh a segunda validacao do acesso.
Se o codigo for valido, retorna o cd_pessoa_fisica para o acesso web.
-------------------------------------------------------------------------------------------------
Locais de chamada direta: 
[  ]  Objetos do dicionario [ ] Tasy (Delphi/Java/HTML5) [x] Portal [  ]  Relatorios [ ] Outros:

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ */
qt_acesso_w		integer;
cd_pessoa_fisica_w	pessoa_fisica.cd_pessoa_fisica%type;
ds_retorno_w		pessoa_fisica.cd_pessoa_fisica%type;
ds_hash_cd_acesso_w	pls_decl_segurado_web.ds_hash_cd_acesso%type;
				

BEGIN

ds_hash_cd_acesso_w	:= OBTER_MD5(cd_codigo_acesso_p);

select	count(1)
into STRICT	qt_acesso_w
from	pls_decl_segurado_web
where	nr_seq_declaracao_segurado	= nr_seq_declaracao_segurado_p
and	ds_hash_cd_acesso		= ds_hash_cd_acesso_w
and	coalesce(dt_utilizacao_hash::text, '') = ''
and	clock_timestamp() <= fim_dia(dt_validade_hash);

if (qt_acesso_w > 0) then
	select	cd_pessoa_fisica
	into STRICT	cd_pessoa_fisica_w
	from	pls_declaracao_segurado
	where	nr_sequencia	= nr_seq_declaracao_segurado_p
	and	ie_status	= 'E';
	
	if (cd_pessoa_fisica_w IS NOT NULL AND cd_pessoa_fisica_w::text <> '') then
		select	cd_pessoa_fisica
		into STRICT	ds_retorno_w
		from	pessoa_fisica
		where	cd_pessoa_fisica	= cd_pessoa_fisica_w
		and	dt_nascimento		= trunc(dt_nascimento_p);
	end if;
end if;

return	ds_retorno_w;

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION pls_decl_segurado_web_pck.validar_cd_acesso_web (nr_seq_declaracao_segurado_p pls_declaracao_segurado.nr_sequencia%type, cd_codigo_acesso_p text, dt_nascimento_p timestamp) FROM PUBLIC;
