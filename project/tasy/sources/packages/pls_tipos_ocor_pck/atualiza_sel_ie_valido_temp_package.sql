-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE PROCEDURE pls_tipos_ocor_pck.atualiza_sel_ie_valido_temp ( nr_id_transacao_p pls_selecao_ocor_cta.nr_id_transacao%type, dados_filtro_p pls_tipos_ocor_pck.dados_filtro, ie_filtro_validacao_p text, dados_regra_p dados_regra default null) AS $body$
DECLARE

/* +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Finalidade:  Atualizar os campos _temp que sao responsaveis por auxiliar no controle do que
	deve ser validado
-------------------------------------------------------------------------------------------------------------------

Locais de chamada direta:
[ X]  Objetos do dicionario [ ] Tasy (Delphi/Java) [  ] Portal [  ]  Relatorios [ ] Outros:
 ------------------------------------------------------------------------------------------------------------------

Pontos de atencao:
Usar sempre o nr_id_transacao

Alteracoes:
 ------------------------------------------------------------------------------------------------------------------

 usuario OS XXXXXX 01/01/2000 -
 Alteracao:	Descricao da alteracao.
Motivo:	Descricao do motivo.
 ------------------------------------------------------------------------------------------------------------------

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ */



BEGIN
-- As acoes de filtro e validacao devem ser separadas para que a atualizacao do ie_valido_temp seja feita da forma correta para cada situacao.

if (dados_regra_p.ie_excecao = 'S') then

	-- Quando for regra de excecao sempre faz para os campos ie_excecao_temp ou ie_excecao_excecao_temp para N

	if (ie_filtro_validacao_p = 'F') then
		CALL pls_tipos_ocor_pck.atualiza_sel_temp_filtro_exc(nr_id_transacao_p, dados_filtro_p);

	-- A validacao em regras de excecao trabalha no primeiro nivel de exceccao.

	elsif (ie_filtro_validacao_p = 'V') then

		update	pls_selecao_ocor_cta
		set	ie_excecao_temp = 'N'
		where	nr_id_transacao = nr_id_transacao_p;
		
	end if;
else
	-- Em regras comuns trabalha-se apenas com IE_VALIDO e IE_VALIDO_TEMP.

	if (ie_filtro_validacao_p = 'F') then

		-- Vai atualizar o campo IE_VALIDO_TEMP para N se for filtro comum

		-- e o campo IE_EXCECAO_TEMP para N se for filtro de excecao.

		CALL pls_tipos_ocor_pck.atualiza_sel_temp_filtro(nr_id_transacao_p, dados_filtro_p);

	-- Quando for uma validacao o campo IE_VALIDO_TEMP deve ser atualizado para N em todos os registros quando for chamado esta procedure.

	elsif (ie_filtro_validacao_p = 'V') then

		update	pls_selecao_ocor_cta
		set	ie_valido_temp = 'N'
		where	nr_id_transacao = nr_id_transacao_p;
		
	end if;
end if;

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_tipos_ocor_pck.atualiza_sel_ie_valido_temp ( nr_id_transacao_p pls_selecao_ocor_cta.nr_id_transacao%type, dados_filtro_p pls_tipos_ocor_pck.dados_filtro, ie_filtro_validacao_p text, dados_regra_p dados_regra default null) FROM PUBLIC;
