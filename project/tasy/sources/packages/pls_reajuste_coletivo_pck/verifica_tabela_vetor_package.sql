-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION pls_reajuste_coletivo_pck.verifica_tabela_vetor ( nr_seq_tabela_p pls_tabela_preco.nr_sequencia%type) RETURNS varchar AS $body$
BEGIN

if (tb_nr_seq_tabela_w.count > 0) then
	for i in tb_nr_seq_tabela_w.first..tb_nr_seq_tabela_w.last loop
		begin
		if (tb_nr_seq_tabela_w(i) = nr_seq_tabela_p) then
			return 'S';
		end if;
		end;
	end loop;
else
	return 'N';
end if;

return 'N';

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_reajuste_coletivo_pck.verifica_tabela_vetor ( nr_seq_tabela_p pls_tabela_preco.nr_sequencia%type) FROM PUBLIC;
