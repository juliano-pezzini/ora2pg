-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE PROCEDURE lote_ent_sec_ficha_pck.load_row_by_prescr (nr_prescricao_p lote_ent_sec_ficha.nr_prescricao%TYPE) AS $body$
DECLARE

nr_sequencia_w lote_ent_sec_ficha.nr_sequencia%TYPE;

BEGIN
    if (lote_ent_sec_ficha_pck.is_loadble() = 'S') then
        select max(nr_sequencia)
        into STRICT nr_sequencia_w
        from lote_ent_sec_ficha
        where nr_prescricao = nr_prescricao_p;

        if (nr_sequencia_w IS NOT NULL AND nr_sequencia_w::text <> '') then
            CALL lote_ent_sec_ficha_pck.load_row_by_sec(nr_sequencia_w);
        end if;

    end if;
end;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE lote_ent_sec_ficha_pck.load_row_by_prescr (nr_prescricao_p lote_ent_sec_ficha.nr_prescricao%TYPE) FROM PUBLIC;
