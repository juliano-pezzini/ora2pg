-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION cancelar_por_periodo.get_itens_integracao () RETURNS SETOF INFO_INTE_STATUS_REC_TABLE AS $body$
BEGIN
  if current_setting('cancelar_por_periodo.info_integracao_status_w')::info_integracao_status_table.count > 0 then
    for i in current_setting('cancelar_por_periodo.info_integracao_status_w')::info_integracao_status_table.first ..info_integracao_status_w.last loop
      if current_setting('cancelar_por_periodo.info_integracao_status_w')::info_integracao_status_table.exists(i) then
        RETURN NEXT current_setting('cancelar_por_periodo.info_integracao_status_w'::info_integracao_status_table(i));
      end if;
    end loop;
  end if;
end;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION cancelar_por_periodo.get_itens_integracao () FROM PUBLIC;
