-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION pls_mens_itens_limite_pck.obter_indice_regra_item ( ie_tipo_item_p pls_mensalidade_seg_item.ie_tipo_item%type, nr_seq_tipo_lanc_p pls_mensalidade_seg_item.nr_seq_tipo_lanc%type) RETURNS bigint AS $body$
BEGIN
if (current_setting('pls_mens_itens_limite_pck.regra_limite_item_w')::vetor.count > 0) then
	for i in current_setting('pls_mens_itens_limite_pck.regra_limite_item_w')::vetor.first..regra_limite_item_w.last loop
		begin
		if (current_setting('pls_mens_itens_limite_pck.regra_limite_item_w')::vetor[i].regra_item.count > 0) then
			for j in current_setting('pls_mens_itens_limite_pck.regra_limite_item_w')::vetor[i].regra_item.first..regra_limite_item_w[i].regra_item.last loop
				begin
				if (current_setting('pls_mens_itens_limite_pck.regra_limite_item_w')::vetor[i].regra_item(j).ie_tipo_item = ie_tipo_item_p) and (current_setting('pls_mens_itens_limite_pck.regra_limite_item_w')::vetor[i].regra_item(j)coalesce(.nr_seq_tipo_lanc::text, '') = '' or current_setting('pls_mens_itens_limite_pck.regra_limite_item_w')::vetor[i].regra_item(j).nr_seq_tipo_lanc = nr_seq_tipo_lanc_p) then
					return	i;
				end if;
				end;
			end loop;
		end if;
		end;
	end loop;
end if;
return null;
END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION pls_mens_itens_limite_pck.obter_indice_regra_item ( ie_tipo_item_p pls_mensalidade_seg_item.ie_tipo_item%type, nr_seq_tipo_lanc_p pls_mensalidade_seg_item.nr_seq_tipo_lanc%type) FROM PUBLIC;
