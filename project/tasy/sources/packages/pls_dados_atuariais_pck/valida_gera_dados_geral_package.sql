-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE PROCEDURE pls_dados_atuariais_pck.valida_gera_dados_geral ( lote_benef_p lote_atuarial) AS $body$
DECLARE

/* +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Finalidade:	Realiza as validacoes "gerais" cabiveis a todos os tipos de lotes de dados atuariais gerados 

-------------------------------------------------------------------------------------------------------------------

Locais de chamada direta: 
[X]  Objetos do dicionario [ ] Tasy (Delphi/Java) [  ] Portal [  ]  Relatorios [ ] Outros:
-------------------------------------------------------------------------------------------------------------------

Pontos de atencao:

	
Alteracoes:
-------------------------------------------------------------------------------------------------------------------

++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ */


BEGIN

if (coalesce(lote_benef_p.nr_sequencia::text, '') = '') then

	--Lote nao encontrado!

	CALL wheb_mensagem_pck.exibir_mensagem_abort(882199);
end if;


if	((coalesce(lote_benef_p.nr_seq_benef_regra::text, '') = '') and (coalesce(lote_benef_p.nr_seq_conta_regra::text, '') = '') and (coalesce(lote_benef_p.nr_seq_estip_regra::text, '') = '') and (coalesce(lote_benef_p.nr_seq_mens_regra::text, '') = '') and (coalesce(lote_benef_p.nr_seq_plano_regra::text, '') = '') and (coalesce(lote_benef_p.nr_seq_prest_regra::text, '') = '') and (coalesce(lote_benef_p.nr_seq_proced_regra::text, '') = ''))then

	-- Regra nao informada

	CALL wheb_mensagem_pck.exibir_mensagem_abort(882201);
end if;


END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_dados_atuariais_pck.valida_gera_dados_geral ( lote_benef_p lote_atuarial) FROM PUBLIC;
