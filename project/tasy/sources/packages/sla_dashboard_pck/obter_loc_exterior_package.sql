-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION sla_dashboard_pck.obter_loc_exterior (nr_seq_ordem_servico_p bigint) RETURNS varchar AS $body$
DECLARE

		
ie_loc_exterior_w	varchar(1);
nr_seq_localizacao_w	bigint;
nm_timezone_database_w	varchar(50);

BEGIN

select 	max(nr_seq_localizacao)
into STRICT	nr_seq_localizacao_w
from	man_ordem_servico
where	nr_sequencia = nr_seq_ordem_servico_p;

if (nr_seq_localizacao_w IS NOT NULL AND nr_seq_localizacao_w::text <> '') then
	select	max(nm_timezone_database)
	into STRICT	nm_timezone_database_w
	from	man_localizacao_horario
	where	nr_seq_localizacao = nr_seq_localizacao_w;
else
	nm_timezone_database_w := null;
end if;

if (nm_timezone_database_w IS NOT NULL AND nm_timezone_database_w::text <> '') then
	ie_loc_exterior_w := 'S';
else
	ie_loc_exterior_w := 'N';
end if;

return	ie_loc_exterior_w;

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION sla_dashboard_pck.obter_loc_exterior (nr_seq_ordem_servico_p bigint) FROM PUBLIC;
