-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION rep_gerar_resumo_pck.get_tr_rtf ( col1_p text, col2_p text, col3_p text, col4_p text, col5_p text, col6_p text, col7_p text, qt_col_p bigint) RETURNS varchar AS $body$
DECLARE

	ds_retorno_w 	varchar(20000);
	ds_cor_susp_w	varchar(255);
	ds_enter_w		varchar(255);
	
BEGIN
	
	if (current_setting('rep_gerar_resumo_pck.ie_receita_w')::varchar(1) = 'S') then
		ds_enter_w := ' \par ';
	else
		ds_enter_w := chr(13);
	end if;

	ds_retorno_w := '   ';
	
	if (qt_col_p >= 2) then
		ds_retorno_w  := ds_retorno_w || coalesce(col2_p,'  ') || '  ';
	end if;
	if (qt_col_p >= 3) then
		ds_retorno_w  := ds_retorno_w || ' ' || coalesce(col3_p,'    ') || '    ';
	end if;
	ds_retorno_w  := ds_retorno_w || coalesce(col1_p,'    ') || ds_enter_w;
	if (qt_col_p >= 4) then
		ds_retorno_w  := ds_retorno_w || '     ' || coalesce(col4_p,'    ') || '    ';
	end if;
	if (qt_col_p >= 5) then
		ds_retorno_w  := ds_retorno_w || '	' || coalesce(col5_p,'    ') || '    ';
	end if;
	if (qt_col_p >= 6) then
		ds_retorno_w  := ds_retorno_w || '	' || coalesce(col6_p,'    ') || '    ';
	end if;
	if (qt_col_p >= 7) then
		ds_retorno_w  := ds_retorno_w || '	' || coalesce(col7_p,'    ') || '    ' || ds_enter_w;
	end if;
	
	ds_retorno_w  := ds_retorno_w;
	
	return	ds_retorno_w;
	
	end;
	

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION rep_gerar_resumo_pck.get_tr_rtf ( col1_p text, col2_p text, col3_p text, col4_p text, col5_p text, col6_p text, col7_p text, qt_col_p bigint) FROM PUBLIC;
