-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION tax_dmed_ir_pck.find_log_expressions (code_expression text) RETURNS bigint AS $body$
DECLARE

    i_w bigint:=1;

BEGIN

  if (current_setting('tax_dmed_ir_pck.log_expressions')::log_expressions_reference.count <> 0) then
      while(i_w < current_setting('tax_dmed_ir_pck.log_expressions')::log_expressions_reference.count) loop	        
        if (current_setting('tax_dmed_ir_pck.log_expressions')::log_expressions_reference[i_w].code_expression = code_expression) then
          return i_w;
        end if;
        i_w := i_w + 1;
      end loop;
      return 0;
    else
      return 0;
    end if;
  END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION tax_dmed_ir_pck.find_log_expressions (code_expression text) FROM PUBLIC;
