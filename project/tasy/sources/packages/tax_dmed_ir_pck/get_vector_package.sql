-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION tax_dmed_ir_pck.get_vector (ie_option text, additional_index bigint) RETURNS GENERIC_TYPE AS $body$
BEGIN
      if ie_option = 'R' then
          return current_setting('tax_dmed_ir_pck.receipt_type_rule_vector')::generic_type;
      elsif ie_option = 'C' then
          return current_setting('tax_dmed_ir_pck.class_rule_vector')::generic_type;
      elsif ie_option = 'A' then
          return current_setting('tax_dmed_ir_pck.attend_sector_rule_vector')::generic_type;
      elsif ie_option = 'B' then
          return current_setting('tax_dmed_ir_pck.benef_type_rule_vector')::generic_type;
      elsif ie_option = 'O' then
          return current_setting('tax_dmed_ir_pck.origin_rule_vector')::generic_type;
      elsif ie_option = 'I' then
          return current_setting('tax_dmed_ir_pck.item_type_rule_vector')::generic_type;
      elsif ie_option = 'T' then
          return current_setting('tax_dmed_ir_pck.cont_type_rule_vector')::generic_type;
      elsif ie_option = 'OP' then
          return current_setting('tax_dmed_ir_pck.origin_protocol_rule_vector')::generic_type;
      elsif ie_option = 'BM' then
          return current_setting('tax_dmed_ir_pck.billing_form_rule_vector')::generic_type;
      elsif ie_option = 'RR' then
          return current_setting('tax_dmed_ir_pck.refund_reason_rule_vector')::generic_type;
      elsif ie_option = 'AP' then
          return tax_dmed_ir_pck.get_appropriation_center(additional_index);
      elsif ie_option = 'D' then
          return tax_dmed_ir_pck.get_additional_items(additional_index);
      elsif ie_option = 'P' then
          return current_setting('tax_dmed_ir_pck.type_payable_rule_vector')::generic_type;
      else
          return null;
      end if;
  end;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION tax_dmed_ir_pck.get_vector (ie_option text, additional_index bigint) FROM PUBLIC;
