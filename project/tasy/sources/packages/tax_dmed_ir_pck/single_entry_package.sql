-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION tax_dmed_ir_pck.single_entry (patient_care_p atendimento_paciente.nr_atendimento%type) RETURNS ATENDIMENTO_PAGADOR.CD_PESSOA_FISICA%TYPE AS $body$
DECLARE

    physical_person_w atendimento_pagador.cd_pessoa_fisica%type := null;

BEGIN
    if current_setting('tax_dmed_ir_pck.general_rule_vector')::general_rule_reference[1].ie_entrada_unica = 'S' then
      select  max(a.cd_pessoa_fisica)
      into STRICT    physical_person_w
      from    atendimento_pagador a
      where   a.nr_atendimento = patient_care_p;
    end if;

    return physical_person_w;
  END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION tax_dmed_ir_pck.single_entry (patient_care_p atendimento_paciente.nr_atendimento%type) FROM PUBLIC;
