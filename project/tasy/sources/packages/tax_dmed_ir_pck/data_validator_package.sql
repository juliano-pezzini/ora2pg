-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE PROCEDURE tax_dmed_ir_pck.data_validator (batch_monthly_sequence_p tax_dmed_lote_mensal.nr_sequencia%type, document_sequence_p titulo_receber.nr_titulo%type, establishment_code_p titulo_receber.cd_estabelecimento%type, ie_provider_p tax_dmed_dados_gerais.ie_prestador%type, response_p INOUT text) AS $body$
DECLARE

    response_w varchar(4000);
    index_w bigint;


BEGIN
    PERFORM set_config('tax_dmed_ir_pck.batch_monthly_sequence', batch_monthly_sequence_p, false);
    CALL tax_dmed_ir_pck.load_general_rule();

    if (ie_provider_p = 'S') then
      SELECT * FROM tax_dmed_ir_pck.provider_validation(document_sequence_p, establishment_code_p, response_w, index_w, batch_monthly_sequence_p) INTO STRICT response_w, index_w;
    else
      SELECT * FROM tax_dmed_ir_pck.operator_validation(document_sequence_p, establishment_code_p, response_w, index_w, batch_monthly_sequence_p) INTO STRICT response_w, index_w;
    end if;

    if (index_w = 0) then
      response_w := 'Dados nao encontrados.' || current_setting('tax_dmed_ir_pck.line_break_w')::varchar(2);
    end if;

    response_p := response_w;

  END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE tax_dmed_ir_pck.data_validator (batch_monthly_sequence_p tax_dmed_lote_mensal.nr_sequencia%type, document_sequence_p titulo_receber.nr_titulo%type, establishment_code_p titulo_receber.cd_estabelecimento%type, ie_provider_p tax_dmed_dados_gerais.ie_prestador%type, response_p INOUT text) FROM PUBLIC;
