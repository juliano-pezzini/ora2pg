-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION tax_dmed_ir_pck.total_monthly_dmed (sequence_p tax_dmed_dados_gerais.nr_seq_lote_mensal%type) RETURNS bigint AS $body$
DECLARE

    total_monthly_dmed_w double precision;

BEGIN
    select 	sum(vl_pago) 
    into STRICT	  total_monthly_dmed_w
    from 	  tax_dmed_dados_gerais
    where	  nr_seq_lote_mensal = sequence_p;

    return total_monthly_dmed_w;
  END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION tax_dmed_ir_pck.total_monthly_dmed (sequence_p tax_dmed_dados_gerais.nr_seq_lote_mensal%type) FROM PUBLIC;
