-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';

  --
CREATE OR REPLACE FUNCTION pkg_report_data.get_vent_mode (IE_MODO_VENT_REL_P MODALIDADE_VENTILATORIA.IE_MODO_VENT_REL%TYPE, CD_MODALIDADE_P MODALIDADE_VENTILATORIA.CD_MODALIDADE%TYPE DEFAULT NULL) RETURNS MODALIDADE_VENTILATORIA.CD_MODALIDADE%TYPE AS $body$
DECLARE


    CD_MODE_W MODALIDADE_VENTILATORIA.CD_MODALIDADE%TYPE;

  
BEGIN
  
    CD_MODE_W := NULL;

    IF (IE_MODO_VENT_REL_P IS NOT NULL AND IE_MODO_VENT_REL_P::text <> '') THEN
      IF coalesce(CD_MODALIDADE_P::text, '') = '' THEN
        SELECT MAX(CD_MODALIDADE)
          INTO STRICT CD_MODE_W
          FROM MODALIDADE_VENTILATORIA T
         WHERE T.IE_SITUACAO = 'A'
           AND T.IE_MODO_VENT_REL = IE_MODO_VENT_REL_P;
      ELSE
        SELECT MAX(CD_MODALIDADE)
          INTO STRICT CD_MODE_W
          FROM MODALIDADE_VENTILATORIA T
         WHERE T.IE_SITUACAO = 'A'
           AND T.IE_MODO_VENT_REL = IE_MODO_VENT_REL_P
           AND T.CD_MODALIDADE = CD_MODALIDADE_P;
      END IF;
    END IF;

    RETURN CD_MODE_W;

  END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION pkg_report_data.get_vent_mode (IE_MODO_VENT_REL_P MODALIDADE_VENTILATORIA.IE_MODO_VENT_REL%TYPE, CD_MODALIDADE_P MODALIDADE_VENTILATORIA.CD_MODALIDADE%TYPE DEFAULT NULL) FROM PUBLIC;
