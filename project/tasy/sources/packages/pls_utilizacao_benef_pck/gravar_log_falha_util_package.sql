-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE PROCEDURE pls_utilizacao_benef_pck.gravar_log_falha_util (ds_log_p text) AS $body$
BEGIN

if (ds_log_p IS NOT NULL AND ds_log_p::text <> '') then
	insert into log_tasy(dt_atualizacao,
		nm_usuario,
		cd_log,
		ds_log)
	values (clock_timestamp(),
		'Portal',
		38938,
		substr(ds_log_p,1,2000));
	commit;
end if;

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE pls_utilizacao_benef_pck.gravar_log_falha_util (ds_log_p text) FROM PUBLIC;
