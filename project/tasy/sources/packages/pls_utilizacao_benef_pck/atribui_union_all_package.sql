-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION pls_utilizacao_benef_pck.atribui_union_all ( sql_p text) RETURNS varchar AS $body$
DECLARE

-- Centraliza o If para ver se deve colocar um union all antes
ds_ret_w	varchar(32000) := '';

BEGIN

if (sql_p IS NOT NULL AND sql_p::text <> '') then

	ds_ret_w :=  sql_p || pls_util_pck.enter_w || 'union all ' || pls_util_pck.enter_w;
end if;

return ds_ret_w;

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_utilizacao_benef_pck.atribui_union_all ( sql_p text) FROM PUBLIC;
