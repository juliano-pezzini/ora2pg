-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION timezone_utils.getbaselinetimezone () RETURNS varchar AS $body$
BEGIN
        if (current_setting('timezone_utils.baseline_timezone')::coalesce(varchar(100)::text, '') = '') then
            begin
                select time_zone_id into STRICT current_setting('timezone_utils.baseline_timezone')::varchar(100) from global_settings;
                if (current_setting('timezone_utils.baseline_timezone')::coalesce(varchar(100)::text, '') = '') then
                    PERFORM set_config('timezone_utils.baseline_timezone', 'UNDEFINED', false);
                end if;
            exception
                when no_data_found then
                    PERFORM set_config('timezone_utils.baseline_timezone', 'UNDEFINED', false);
            end;
        end if;
        if (current_setting('timezone_utils.baseline_timezone')::varchar(100) = 'UNDEFINED') then
            return null;
        end if;
        return current_setting('timezone_utils.baseline_timezone')::varchar(100);
    end;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION timezone_utils.getbaselinetimezone () FROM PUBLIC;
