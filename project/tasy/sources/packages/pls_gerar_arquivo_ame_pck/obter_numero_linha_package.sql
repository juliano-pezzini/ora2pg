-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION pls_gerar_arquivo_ame_pck.obter_numero_linha ( nr_seq_segurado_p pls_segurado.nr_sequencia%type, nr_seq_titular_p pls_segurado.nr_sequencia%type) RETURNS bigint AS $body$
DECLARE


qt_segurado_w	bigint;

BEGIN

qt_segurado_w	:= 0;

if (tb_nr_seq_segurado_w.count > 0) then
	for i in tb_nr_seq_segurado_w.first..tb_nr_seq_segurado_w.last loop
		begin
		if (nr_seq_segurado_p IS NOT NULL AND nr_seq_segurado_p::text <> '') then
			if (nr_seq_segurado_p = tb_nr_seq_segurado_w(i)) then
				qt_segurado_w := qt_segurado_w + 1;
			end if;
		elsif (nr_seq_titular_p IS NOT NULL AND nr_seq_titular_p::text <> '') then
			if (nr_seq_titular_p = tb_nr_seq_titular_w(i)) then
				qt_segurado_w := qt_segurado_w + 1;
			end if;
		end if;
		end;
	end loop;
end if;

return	qt_segurado_w;

END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION pls_gerar_arquivo_ame_pck.obter_numero_linha ( nr_seq_segurado_p pls_segurado.nr_sequencia%type, nr_seq_titular_p pls_segurado.nr_sequencia%type) FROM PUBLIC;
