-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION anzics_pck.get_temperature ( nr_atendimento_p bigint, nr_seq_atepacu_p bigint, ie_opcao_p text) RETURNS bigint AS $body$
DECLARE

    qt_min_temp_w atendimento_sinal_vital.qt_temp%type;
    qt_max_temp_w atendimento_sinal_vital.qt_temp%type;

BEGIN
    CALL anzics_pck.set_icu_date(nr_seq_atepacu_p);
    SELECT MAX(a.qt_temp),
      MIN(a.qt_temp)
    INTO STRICT qt_max_temp_w,
      qt_min_temp_w
    FROM atendimento_sinal_vital a
    WHERE a.nr_atendimento = nr_atendimento_p
    AND a.dt_sinal_vital BETWEEN current_setting('anzics_pck.dt_inicio_w')::timestamp AND current_setting('anzics_pck.dt_fim_w')::timestamp
    AND (a.dt_liberacao IS NOT NULL AND a.dt_liberacao::text <> '')
    AND coalesce(a.dt_inativacao::text, '') = '';
    IF (ie_opcao_p       = 'TEMPHI') THEN
      RETURN qt_max_temp_w;
    elsif (ie_opcao_p = 'TEMPLO') THEN
      RETURN qt_min_temp_w;
    END IF;
    RETURN NULL;
  END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION anzics_pck.get_temperature ( nr_atendimento_p bigint, nr_seq_atepacu_p bigint, ie_opcao_p text) FROM PUBLIC;
