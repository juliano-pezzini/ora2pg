-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION anzics_pck.get_sql_varchar ( ds_sql_p text, ds_parameters_p text) RETURNS varchar AS $body$
DECLARE

    cursor_w sql_pck.t_cursor;
    result_w varchar(4000);

BEGIN
    cursor_w := anzics_pck.execute_sql(ds_sql_p,ds_parameters_p);
    LOOP
      FETCH cursor_w INTO result_w;
      EXIT WHEN NOT FOUND; /* apply on cursor_w */
    END LOOP;
    CLOSE cursor_w;
    RETURN result_w;
  END;

$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION anzics_pck.get_sql_varchar ( ds_sql_p text, ds_parameters_p text) FROM PUBLIC;
