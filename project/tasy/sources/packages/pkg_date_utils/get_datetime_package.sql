-- Generated by Ora2Pg, the Oracle database Schema converter, version 23.1
-- Copyright 2000-2022 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=srv-dbora-03.whebdc.com.br;service_name=DEV_1815;port=1521

SET client_encoding TO 'UTF8';




CREATE OR REPLACE FUNCTION pkg_date_utils.get_datetime ( baseDate timestamp, baseTime timestamp, CALENDAR CALENDAR DEFAULT ESTABLISHMENT) RETURNS timestamp AS $body$
BEGIN
	IF (coalesce(baseDate::text, '') = '' OR coalesce(baseTime::text, '') = '') THEN
		RETURN NULL;
	END IF;
	RETURN pkg_date_utils.get_datetime(TO_CHAR(baseDate,'yyyy-mm-dd') || 'T' || TO_CHAR(baseTime,'hh24:mi:ss'));
  END;


$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 IMMUTABLE;
-- REVOKE ALL ON FUNCTION pkg_date_utils.get_datetime ( baseDate timestamp, baseTime timestamp, CALENDAR CALENDAR DEFAULT ESTABLISHMENT) FROM PUBLIC;
